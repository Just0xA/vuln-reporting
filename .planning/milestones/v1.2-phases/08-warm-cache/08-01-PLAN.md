---
phase: 08-warm-cache
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/fetchers.py
autonomous: true
requirements: [CACHE-04]
must_haves:
  truths:
    - "`data/fetchers.py` defines `_atomic_write_parquet(df, path)` that writes to a temp file in the same directory and uses `os.replace` to swap atomically"
    - "`_save_cache` body delegates its parquet write to `_atomic_write_parquet`, so all eight existing `_save_cache` call sites in `data/fetchers.py` (lines 334, 446, 546, 686, 939, 1028, 1070, 1124) inherit atomicity by construction"
    - "A concurrent reader never observes a partially-written parquet file (verified by an 8-process stress test that finds no `*.tmp.*` residue and reads the final parquet cleanly)"
    - "run_all.py's existing pre-fetch path is unchanged at the call site but now writes atomically by virtue of the `_save_cache` change"
  artifacts:
    - path: "data/fetchers.py"
      provides: "_atomic_write_parquet helper; _save_cache routes through it"
      contains: "_atomic_write_parquet"
  key_links:
    - from: "data/fetchers.py::_save_cache"
      to: "data/fetchers.py::_atomic_write_parquet"
      via: "direct call"
      pattern: "_atomic_write_parquet\\("
    - from: "data/fetchers.py::_atomic_write_parquet"
      to: "os.replace"
      via: "stdlib"
      pattern: "os\\.replace\\("
---

<objective>
Introduce a single atomic-write helper for parquet caches inside `data/fetchers.py` and reroute the existing `_save_cache` write through it. All eight `_save_cache(df, cache)` call sites in this file (lines 334, 446, 546, 686, 939, 1028, 1070, 1124) — which already are the ONLY parquet-write path in the file — inherit atomicity for free.

Purpose: Satisfy CACHE-04 — concurrent daemon + cron invocations cannot observe partial parquet files. Picking Option A from the discussion artifact (helper inside `data/fetchers.py`) was a deliberate choice: Option B (atomic only in the warm-cache script) would leave the daemon write path non-atomic, which violates CACHE-04 the moment the two run concurrently.

Output: A modified `data/fetchers.py` with `_atomic_write_parquet(df, path)` used by `_save_cache()`. No public API change; no new dependencies; no changes to `run_all.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- Key code that this plan modifies. Executor should NOT re-explore the codebase. -->

From data/fetchers.py (current state, lines 175-196):

```python
def _cache_path(cache_dir: Path, dataset: str) -> Path:
    """Return the parquet cache path for a given cache directory and dataset name."""
    return cache_dir / f"{dataset}.parquet"


def _load_cache(path: Path) -> Optional[pd.DataFrame]:
    """Return cached DataFrame if the file exists, else None."""
    if path.exists():
        logger.info("[CACHE HIT] Loading %s from cache", path.name)
        return pd.read_parquet(path, engine="fastparquet")
    return None


def _save_cache(df: pd.DataFrame, path: Path) -> None:
    """Persist DataFrame to parquet, logging any write errors non-fatally."""
    try:
        path.parent.mkdir(parents=True, exist_ok=True)
        df.to_parquet(path, index=False, engine="fastparquet")
        logger.debug("Cached %d rows to %s", len(df), path)
    except Exception as exc:  # noqa: BLE001
        logger.warning("Could not write cache file %s: %s", path, exc)
```

Fact established by grep before planning: the ONLY `df.to_parquet(...)` call in `data/fetchers.py` is the one inside `_save_cache` at line 192. Every cache-writing fetcher (`fetch_all_vulnerabilities`, `fetch_fixed_vulnerabilities`, `fetch_all_assets`, `fetch_recast_rules`, and four more `_save_cache` consumers) already routes through `_save_cache(df, cache)` at lines 334, 446, 546, 686, 939, 1028, 1070, 1124. Therefore the atomic-write change is narrow: introduce the helper and replace the single inline `df.to_parquet(...)` call inside `_save_cache`. No fetcher body is touched.

Dataset filenames written via `_save_cache` (verified from `_cache_path(cache_dir, "...")` call sites): `vulns_all.parquet`, `vulns_fixed.parquet`, `assets_all.parquet`, `recast_rules.parquet` (plus two secondary datasets `assets.parquet` and `tags.parquet` for non-pre-fetch paths). All inherit atomicity by construction once `_save_cache` delegates to the helper.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add _atomic_write_parquet helper and reroute _save_cache through it</name>
  <files>data/fetchers.py</files>
  <behavior>
    - Calling `_atomic_write_parquet(df, target)` writes `df` to a sibling `target.with_name(f"{target.name}.tmp.{os.getpid()}")` file, then calls `os.replace(tmp, target)` so the final filename never points at an incomplete file.
    - If the write raises, the temp file is removed (best-effort `unlink(missing_ok=True)`) and the exception propagates to `_save_cache`, which already swallows and logs at WARNING.
    - `_save_cache()` calls `_atomic_write_parquet(df, path)` in place of its current inline `df.to_parquet(...)`. Its outer try/except + `logger.warning` shape is preserved so caller-visible behavior is identical.
    - All eight existing `_save_cache` call sites (lines 334, 446, 546, 686, 939, 1028, 1070, 1124) are untouched — they inherit atomicity through `_save_cache`.
    - Under concurrent writers (8 processes targeting the same path), the final file is a complete, readable parquet and no `*.tmp.*` residue remains in the directory.
  </behavior>
  <action>
    Edit `data/fetchers.py`:

    1. Add `import os` to the top imports if not already present (needed for `os.replace` and `os.getpid`). Do NOT add `tempfile` — using `os.replace` with a `.tmp.<pid>` sibling in the SAME parent directory is sufficient and avoids cross-filesystem rename issues.

    2. Insert `_atomic_write_parquet(df: pd.DataFrame, path: Path) -> None` immediately after `_cache_path` (i.e. above `_load_cache`). Implementation contract:
       - `path.parent.mkdir(parents=True, exist_ok=True)`.
       - `tmp = path.with_name(f"{path.name}.tmp.{os.getpid()}")` (same directory ensures `os.replace` is an atomic rename on POSIX and Windows/NTFS).
       - `try:` `df.to_parquet(tmp, index=False, engine="fastparquet")`; `os.replace(tmp, path)`.
       - `except` (broad): best-effort `tmp.unlink(missing_ok=True)`, then `raise`.
       - Do NOT log inside the helper — `_save_cache` (its caller) owns the non-fatal warning surface so the helper stays pure.

    3. Modify `_save_cache` so its single `df.to_parquet(...)` call is replaced by `_atomic_write_parquet(df, path)`. Keep the outer `try/except Exception` and `logger.warning(...)` and `logger.debug("Cached %d rows to %s", ...)` exactly as they are. After this edit there is exactly one `.to_parquet(` call in the file, and it lives inside `_atomic_write_parquet`.

    4. Do NOT touch any fetcher function body. The eight `_save_cache(df, cache)` call sites are correct already; verifying that grep returns exactly those eight sites (`grep -n "_save_cache(" data/fetchers.py`) is part of the verify step.

    5. Minimum diff per Karpathy guideline #3 (Surgical Changes). No docstring rewrites of unrelated code, no signature changes.
  </action>
  <verify>
    <automated>cd D:/Projects/vuln-reporting && python -c "from data.fetchers import _atomic_write_parquet, _save_cache; import pandas as pd, tempfile, pathlib, os; d = pathlib.Path(tempfile.mkdtemp()); p = d / 'x.parquet'; df = pd.DataFrame({'a':[1,2,3]}); _atomic_write_parquet(df, p); assert p.exists(); assert not any(f.name.startswith('x.parquet.tmp') for f in d.iterdir()); df2 = pd.read_parquet(p, engine='fastparquet'); assert df2['a'].tolist() == [1,2,3]; print('OK')"</automated>
    <!-- Structural assertion: the helper exists AND _save_cache's source body contains a call to it. This is robust against the false-PASS failure mode where `grep to_parquet` returns 1 both before AND after the change (because the existing inline `df.to_parquet` in `_save_cache` is also a single match). -->
    <automated>cd D:/Projects/vuln-reporting && python -c "import inspect, data.fetchers as f; assert '_atomic_write_parquet' in dir(f), 'helper missing'; src = inspect.getsource(f._save_cache); assert '_atomic_write_parquet' in src, '_save_cache does not delegate to _atomic_write_parquet'; assert '.to_parquet(' not in src, '_save_cache still calls .to_parquet directly'; print('OK')"</automated>
    <!-- File-level invariant: exactly one .to_parquet( call survives, comments excluded. -->
    <automated>cd D:/Projects/vuln-reporting && python -c "import pathlib, re; src = pathlib.Path('data/fetchers.py').read_text(); body = '\n'.join(line for line in src.splitlines() if not line.lstrip().startswith('#')); n = len(re.findall(r'\.to_parquet\(', body)); print(f'to_parquet calls: {n}'); assert n == 1, f'expected exactly one to_parquet call (inside _atomic_write_parquet), got {n}'; print('OK')"</automated>
    <!-- Existing call-site count is preserved: eight _save_cache calls (lines 334, 446, 546, 686, 939, 1028, 1070, 1124). -->
    <automated>cd D:/Projects/vuln-reporting && python -c "import pathlib, re; src = pathlib.Path('data/fetchers.py').read_text(); body = '\n'.join(line for line in src.splitlines() if not line.lstrip().startswith('#')); n = len(re.findall(r'\b_save_cache\(', body)); defn = len(re.findall(r'def _save_cache\(', body)); calls = n - defn; print(f'_save_cache call sites: {calls}'); assert calls == 8, f'expected 8 call sites, got {calls}'; print('OK')"</automated>
    <!-- Concurrent-write stress: 8 subprocesses writing to the same target via the helper. Asserts (a) no .tmp.* residue remains, (b) final file reads cleanly as parquet. This exercises the "no torn writes under concurrency" property rather than asserting it via documentation. -->
    <automated>cd D:/Projects/vuln-reporting && python -c "
import multiprocessing as mp, pathlib, tempfile, sys, os
def _writer(target_str, seed):
    import pandas as pd
    from data.fetchers import _atomic_write_parquet
    df = pd.DataFrame({'a': list(range(seed, seed + 100))})
    _atomic_write_parquet(df, pathlib.Path(target_str))
if __name__ == '__main__':
    d = pathlib.Path(tempfile.mkdtemp())
    target = d / 'concurrent.parquet'
    procs = [mp.Process(target=_writer, args=(str(target), i)) for i in range(8)]
    for p in procs: p.start()
    for p in procs: p.join()
    for p in procs: assert p.exitcode == 0, f'worker exit {p.exitcode}'
    residue = [f.name for f in d.iterdir() if '.tmp.' in f.name]
    assert residue == [], f'tmp residue left behind: {residue}'
    import pandas as pd
    df = pd.read_parquet(target, engine='fastparquet')
    assert len(df) == 100, f'expected 100 rows, got {len(df)}'
    print('concurrent-write stress OK')
" </automated>
  </verify>
  <done>
    `_atomic_write_parquet` exists in `data/fetchers.py`; `_save_cache` source contains a call to it and no direct `.to_parquet(`; exactly one `.to_parquet(` survives in the file (inside the helper); the eight existing `_save_cache` call sites are unchanged; round-trip smoke test passes; 8-process concurrent-write stress leaves no `.tmp.*` residue and produces a readable parquet.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Concurrent processes → shared filesystem | Daemon `run_all.py` and cron `warm_cache.py` may write to the same `data/cache/<date>/*.parquet` simultaneously. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | `data/cache/*.parquet` partial writes | mitigate | `_atomic_write_parquet` writes to `<file>.tmp.<pid>` then `os.replace` — readers always see either the previous complete file or the new complete file, never a torn one. Exercised by the 8-process stress verify. |
| T-08-02 | Denial of Service | Leftover `.tmp.<pid>` files on crash | accept | On write exception we `unlink(missing_ok=True)` the tmp file. A hard process kill mid-write may still leak a tmp file, but it is named with `.tmp.` so it will never be read as cache, and the next `--prune-stale` sweep (plan 02) removes the parent dir on day-rollover. |
</threat_model>

<verification>
- `python -c "from data.fetchers import _atomic_write_parquet; ..."` round-trip smoke (above).
- Inspect-based assertion that `_save_cache` body delegates to `_atomic_write_parquet` and no longer calls `.to_parquet` directly (robust against the false-PASS shape where `to_parquet` count is 1 both before and after).
- File-level invariant: exactly one `.to_parquet(` call survives (inside the helper); `_load_cache`'s `read_parquet` is the unchanged read path.
- Eight `_save_cache` call sites still present (lines 334, 446, 546, 686, 939, 1028, 1070, 1124).
- 8-process concurrent-write stress leaves no `*.tmp.*` residue and produces a clean-reading parquet.
- Existing `python run_all.py --dry-run` still completes without error (regression check on the daemon write path).
</verification>

<success_criteria>
- Helper exists and is the sole parquet-write site in `data/fetchers.py`.
- `_save_cache` is the only function that calls the helper; the eight pre-existing `_save_cache` call sites in fetcher bodies inherit atomicity without being modified.
- Concurrent-write semantics: `os.replace` provides atomic rename on both POSIX and Windows (NTFS), so any process reading `vulns_all.parquet` / `vulns_fixed.parquet` / `assets_all.parquet` / `recast_rules.parquet` sees either the pre-write or post-write version, never a partial — proven by the 8-process stress verify.
- No public API change. `run_all.py` is not modified.
- Single atomic commit: `feat(08-01): atomic parquet writes in data/fetchers.py for CACHE-04`.
</success_criteria>

<output>
Create `.planning/phases/08-warm-cache/08-01-SUMMARY.md` when done.
</output>
