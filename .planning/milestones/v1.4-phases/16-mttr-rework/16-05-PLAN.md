---
phase: 16-mttr-rework
plan: 05
type: tdd
wave: 5
depends_on: ["16-04", "16-03"]
files_modified:
  - tests/test_mttr_trend_module.py
  - tests/baselines/mttr_trend_test_pull.json
  - tests/baselines/mttr_trend_test_pull_zero_match.json
autonomous: true
requirements: [RPT-05]
gap_closure: true
must_haves:
  truths:
    - "Test: `mttr_view=owner` (and unset → default owner, D-16-12) → only the Owner table/rows render (no severity rows) in PDF + Excel + email panel"
    - "Test: `mttr_view=severity` → only the Severity table renders (no Owner rows)"
    - "Test: `mttr_view=both` → both render as two DISTINCT tables/sections, not one concatenated list"
    - "Test: the Owner cut no longer emits the hard-coded Critical-SLA (15) 'SLA Target' — owner-view Excel headers do not contain 'SLA Target (Days)' and no Owner row carries sla_days==15 as an SLA target"
    - "Test: a bad `mttr_view` value falls back to owner (no crash) and validate_config returns an error for it"
    - "Test: single-cut (owner-only) output fits one PDF page for a representative owner/severity count (structural/page-count assertion)"
    - "board_summary zero-diff still green (D-16-10): the three board_summary structural baselines remain byte-identical"
    - "Existing mttr_trend structural baselines regenerated to reflect the NEW default owner-only render; the self-guarding compare_snapshots(actual, baseline) == [] tests pass against the regenerated files"
    - "All fixtures synthetic (RFC 5737/6761; asset_uuid all-zero prefix) — QUAL-05"
  artifacts:
    - path: "tests/test_mttr_trend_module.py"
      provides: "mttr_view owner/severity/both/default + Owner-SLA-drop + bad-value-fallback + single-page-fit tests (extends existing suite, does not fork it)"
      contains: "mttr_view"
    - path: "tests/baselines/mttr_trend_test_pull.json"
      provides: "Regenerated populated structural baseline reflecting owner-only default render"
    - path: "tests/baselines/mttr_trend_test_pull_zero_match.json"
      provides: "Regenerated zero-match structural baseline (cold-start path; likely unchanged but re-verified)"
  key_links:
    - from: "tests/test_mttr_trend_module.py"
      to: "MTTRTrendModule.compute / render_pdf_section / render_excel_tabs / render_email_panel"
      via: "ModuleConfig options={'mttr_view': ...} then assert which rows/headers render"
      pattern: "mttr_view"
    - from: "tests/test_mttr_trend_module.py TestStructuralBaselines"
      to: "tests/baselines/mttr_trend_test_pull*.json"
      via: "compare_snapshots(actual, load_baseline(name)) == []"
      pattern: "mttr_trend_test_pull"
---

<objective>
Encode the D-16-11 / D-16-12 locked gap-closure spec as automated tests and regenerate the `mttr_trend` structural baselines for the new owner-only default render. Re-assert the board_summary zero-diff guarantee (D-16-10) is still green. The existing `tests/test_mttr_trend_module.py` suite already covers the reopened-clock lodestar, cold-start, min_sample, Owner-drift, tie-break, partial-month, four-channel empty guard, composed-pipeline non-None smoke, and structural baselines — EXTEND it with view-selector tests; do not fork it.

Purpose: Lock the audience-split MTTR view behavior against regression and prove the byte-unchanged board_summary guarantee survives the gap-closure change.
Output: New view-selector test classes appended to the existing suite, two regenerated mttr_trend baseline JSONs (self-guarded by the existing compare_snapshots == [] tests), and a confirmed board_summary zero-diff. All fixtures synthetic (QUAL-05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-mttr-rework/16-CONTEXT.md
@.planning/phases/16-mttr-rework/16-UAT.md
@.planning/phases/16-mttr-rework/16-04-PLAN.md
@CLAUDE.md

<interfaces>
<!-- Test infra + the existing suite this plan extends. Read before editing. -->

tests/test_mttr_trend_module.py (existing, ~986 lines):
- Fixture helpers already present: `_uuid(n)`, `_make_fixed_vulns(rows)`, `_make_assets(rows)`,
  `_make_snapshot_mttr(...)`, `_make_trend_snapshots(...)`, `_config(**options)`, `_run(fixed_rows, trend_snapshots, asset_rows, **options)`.
  REUSE these — do not redefine.
- REF = 2026-06-12 UTC; `_UUID_PREFIX = "00000000-0000-0000-0000-00000000000"`.
- `pd.options.mode.copy_on_write = True` set at module top.
- TestStructuralBaselines already builds populated + zero-match bundles via `_build_populated_bundle()` /
  `_build_zero_match_bundle()` (mod.render_excel_tabs / render_pdf_section / render_email_panel) and has
  `@pytest.mark.baseline` tests `test_populated_baseline_matches_committed_file` /
  `test_zero_match_baseline_matches_committed_file` that compare against the committed JSONs.
- `_run(...)` passes `min_sample_size` via options — add `mttr_view` the same way (e.g. `_run(rows, mttr_view="severity")`).

tests/baseline_utils.py: extract_structural_snapshot(bundle, slug) -> dict of structural keys
(incl. pdf_page_count); compare_snapshots(actual, baseline) -> list[str] (empty == no regression);
write_baseline(...) / load_baseline(...). Read signatures before use.

tests/test_board_summary_baseline.py (created in 16-03): asserts compare_snapshots == [] for the three
board_summary baselines. RE-RUN it; it must still pass (D-16-10).

The owner-only default render is NEW after 16-04 — so the committed mttr_trend baselines from 16-03
(which captured the OLD combined render) WILL drift. Regenerate them deliberately (this is expected,
not a failure). board_summary baselines must NOT change.
</interfaces>
</context>

<feature>
  <name>mttr_view selector acceptance tests + regenerated mttr_trend baselines + board_summary zero-diff re-assert</name>
  <files>tests/test_mttr_trend_module.py, tests/baselines/mttr_trend_test_pull.json, tests/baselines/mttr_trend_test_pull_zero_match.json</files>
  <behavior>
    RED then GREEN against the 16-04 implementation. New test classes (append to the existing suite, reuse its fixture helpers):
    - TestMttrViewDefault: `_run(rows)` with mttr_view UNSET → the rendered PDF/Excel/email show the Owner cut only; no Severity rows in the owner-default headline render. Assert default resolves to "owner" (e.g. data.metadata["mttr_view"] == "owner").
    - TestMttrViewOwner: explicit `mttr_view="owner"` → only Owner table/rows render; severity rows absent from the headline channels (gauges may be absent too). Build a fixture with both severities AND owners so absence is meaningful.
    - TestMttrViewSeverity: `mttr_view="severity"` → only the Severity table renders; no Owner rows in headline channels.
    - TestMttrViewBoth: `mttr_view="both"` → BOTH render as two distinct tables/sections. Assert there are two separately-headed regions (e.g. the Excel tab contains both a Severity header region and an Owner header region; the PDF has two tables or two section headers), NOT one concatenated "Severity / Owner" table.
    - TestOwnerSlaBasisFix: in owner-view, the Excel headers do NOT contain "SLA Target (Days)", and no Owner row dict carries sla_days == SLA_DAYS["critical"] (15) as an SLA target. The latent ~:605 bug is gone.
    - TestMttrViewBadValueFallback: `mttr_view="bogus"` → no crash; resolves to "owner" (metadata["mttr_view"]=="owner"); and validate_config(ModuleConfig("mttr_trend", options={"mttr_view":"bogus"})) returns a non-empty error list.
    - TestSingleCutFitsOnePage: build a representative owner-only bundle (e.g. 4-6 owners, >=5 findings each) and assert the structural pdf_page_count for the owner-only render is 1 (single page) — use extract_structural_snapshot(bundle, slug)["pdf_page_count"] == 1, mirroring TestStructuralBaselines bundle assembly.
    - All new fixtures synthetic (asset_uuid "00000000-...000N"; no real hostnames/IPs/CVE/plugin names — QUAL-05).

    Baseline regeneration: the committed mttr_trend_test_pull.json / _zero_match.json were captured against the OLD combined render. After the suite is green, regenerate both via the same extract_structural_snapshot + write_baseline path used by TestStructuralBaselines so the @pytest.mark.baseline self-guard tests pass against the NEW owner-only default shape. The zero-match (cold-start) baseline likely does not change structurally — re-verify and only rewrite if it genuinely drifts.

    board_summary zero-diff: re-run tests/test_board_summary_baseline.py — it MUST stay green and the three board_summary baseline JSONs MUST remain byte-identical (D-16-10). Do not rewrite them.
  </behavior>
  <implementation>
    Append the new TestMttrView* / TestOwnerSlaBasisFix / TestSingleCutFitsOnePage classes to tests/test_mttr_trend_module.py using the existing `_run` / `_make_*` / `_config` helpers and the TestStructuralBaselines bundle-assembly pattern (`mod.render_excel_tabs(data, wb, cfg)`, `mod.render_pdf_section`, `mod.render_email_panel`). To assert "only owner rows render", inspect both the ModuleData table groups (the new metadata/table keys 16-04 introduced — read the real key names from the edited module, do not assume) AND the rendered Excel cell values / PDF HTML. For "two distinct tables in both-view", assert two header regions in the Excel tab (search cell values for both a Severity header and an Owner header) or two <table>/<h*> regions in the PDF HTML.

    Read the real metadata/option key names and the owner-table header string from reports/modules/mttr_trend_module.py as edited by 16-04 — do NOT hardcode assumed names. For single-page-fit, reuse extract_structural_snapshot's pdf_page_count key (confirm it exists in tests/baseline_utils.py).

    Regenerate baselines: run the capture path (the same write_baseline call shape TestStructuralBaselines / 16-03 used). Confirm the @pytest.mark.baseline tests then pass with `python -m pytest tests/test_mttr_trend_module.py -m baseline`.
  </implementation>
</feature>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED→GREEN — mttr_view selector + Owner-SLA-drop + single-page tests</name>
  <read_first>
    - tests/test_mttr_trend_module.py (the existing suite — reuse _run/_make_*/_config and the TestStructuralBaselines bundle-assembly pattern; do NOT redefine helpers)
    - reports/modules/mttr_trend_module.py (as edited by 16-04 — read the REAL mttr_view metadata key, the owner/severity table keys, and the owner-table header strings; assert against real names)
    - .planning/phases/16-mttr-rework/16-04-PLAN.md (what 16-04 actually built — metadata["mttr_view"], split table groups, dropped Owner SLA Target)
    - .planning/phases/16-mttr-rework/16-UAT.md (`## Gaps` — the required-tests list is the authoritative acceptance set)
    - tests/baseline_utils.py (extract_structural_snapshot pdf_page_count key for the single-page assertion)
  </read_first>
  <files>tests/test_mttr_trend_module.py</files>
  <action>
    Append new test classes to the existing suite reusing its fixture helpers: TestMttrViewDefault, TestMttrViewOwner, TestMttrViewSeverity, TestMttrViewBoth, TestOwnerSlaBasisFix, TestMttrViewBadValueFallback, TestSingleCutFitsOnePage. Use `_run(rows, mttr_view=...)` (options pass-through) for compute, then assemble Excel/PDF/email via the existing render-method calls. Assert: owner/default → Owner-only rows (no Severity rows) in headline channels; severity → Severity-only; both → two distinct headed regions (not one concat); owner-view Excel headers exclude "SLA Target (Days)" and no Owner row carries sla_days==15 as an SLA target; bad value → metadata view == "owner" + validate_config returns a non-empty error; owner-only representative bundle → extract_structural_snapshot(...)["pdf_page_count"] == 1. Read the real metadata/option/header key names from the 16-04 module — do not hardcode assumed strings. All fixtures synthetic (asset_uuid "00000000-...000N").
  </action>
  <verify>
    <automated>python -m pytest tests/test_mttr_trend_module.py -x -k "MttrView or OwnerSlaBasis or SingleCut" -W error::FutureWarning</automated>
  </verify>
  <acceptance_criteria>
    - The four view classes exist: `grep -c "class TestMttrView" tests/test_mttr_trend_module.py` >= 4.
    - Owner-SLA-drop test present: `grep -c "class TestOwnerSlaBasisFix" tests/test_mttr_trend_module.py` >= 1 and it asserts "SLA Target" is absent from owner-view headers AND no owner row sla_days == 15.
    - Bad-value fallback test present: `grep -c "TestMttrViewBadValueFallback" tests/test_mttr_trend_module.py` >= 1, asserting default-to-owner + a non-empty validate_config error.
    - Single-page test present and asserts page count 1: `grep -c "TestSingleCutFitsOnePage" tests/test_mttr_trend_module.py` >= 1; `grep -c "pdf_page_count" tests/test_mttr_trend_module.py` >= 1.
    - `python -m pytest tests/test_mttr_trend_module.py -k "MttrView or OwnerSlaBasis or SingleCut"` exits 0.
    - Suite passes under CoW strict mode: `python -m pytest tests/test_mttr_trend_module.py -W error::FutureWarning` exits 0 (zero ChainedAssignmentError).
    - PII-clean: new fixtures use the synthetic uuid prefix; `grep -c "00000000-0000-0000-0000-00000000000" tests/test_mttr_trend_module.py` >= 1; no real IPs/hostnames added.
  </acceptance_criteria>
  <done>The locked D-16-11/D-16-12 view-selector behavior (owner/severity/both/default), the Owner-SLA-basis fix, the bad-value fallback, and single-page fit are all encoded as passing synthetic tests; the existing suite is extended, not forked.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate mttr_trend structural baselines for the owner-only default render</name>
  <read_first>
    - tests/test_mttr_trend_module.py (TestStructuralBaselines — _build_populated_bundle / _build_zero_match_bundle + the @pytest.mark.baseline self-guard tests)
    - tests/baseline_utils.py (extract_structural_snapshot / compare_snapshots / write_baseline / load_baseline)
    - tests/baselines/mttr_trend_test_pull.json + tests/baselines/mttr_trend_test_pull_zero_match.json (the OLD combined-render baselines that will drift)
    - .planning/phases/16-mttr-rework/16-04-PLAN.md (the new default is owner-only — the populated baseline shape changes)
  </read_first>
  <files>tests/baselines/mttr_trend_test_pull.json, tests/baselines/mttr_trend_test_pull_zero_match.json</files>
  <action>
    The committed mttr_trend baselines were captured against the OLD combined Severity+Owner render. After Task 1 is green, regenerate both via the same extract_structural_snapshot + write_baseline path TestStructuralBaselines uses (the populated bundle now renders owner-only by default; the zero-match cold-start bundle likely does not change). Run `python -m pytest tests/test_mttr_trend_module.py -m baseline` and confirm `test_populated_baseline_matches_committed_file` / `test_zero_match_baseline_matches_committed_file` pass against the regenerated files. Only rewrite the zero-match JSON if it genuinely drifts (cold-start is structurally identical). Keep baselines structural-only — no metric values (QUAL-05).
  </action>
  <verify>
    <automated>python -m pytest tests/test_mttr_trend_module.py -m baseline -x</automated>
  </verify>
  <acceptance_criteria>
    - Both baseline files are valid JSON with structural keys only: `python -c "import json; d=json.load(open('tests/baselines/mttr_trend_test_pull.json')); assert 'pdf_page_count' in d and 'overall_mttr' not in d"` exits 0.
    - The self-guarding baseline tests pass: `python -m pytest tests/test_mttr_trend_module.py -m baseline` exits 0.
    - The populated baseline reflects the owner-only default (its structural shape differs from the pre-16-04 committed version): `git diff --stat tests/baselines/mttr_trend_test_pull.json` shows the file changed.
    - No PII in either JSON (structural-only by construction).
  </acceptance_criteria>
  <done>The two mttr_trend structural baselines are regenerated to the new owner-only default render and self-guarded by passing compare_snapshots == [] tests.</done>
</task>

<task type="auto">
  <name>Task 3: Re-assert board_summary zero-diff (D-16-10) + full-suite green</name>
  <read_first>
    - tests/test_board_summary_baseline.py (the 16-03 zero-diff assertion — re-run, do not modify)
    - tests/baselines/board_summary_test_pull.json + board_summary_test_pull_analyst_off.json + board_summary_test_pull_zero_match.json (must stay byte-identical)
    - .planning/phases/16-mttr-rework/16-CONTEXT.md (D-16-10 — a board_summary diff means something broke)
  </read_first>
  <files>tests/test_mttr_trend_module.py</files>
  <action>
    Run the existing board_summary zero-diff test (tests/test_board_summary_baseline.py) and the full suite to prove the gap-closure change did not regress board_summary or any other module. The three board_summary baseline JSONs MUST remain byte-identical — a non-zero diff is a FAILURE signal (scope violation), per D-16-10. Do NOT rewrite the board_summary baselines. No production-code edits here; if the full suite surfaces a regression, the fix belongs in the 16-04 module change (return to that plan's files), not in loosening the test. (This task's <files> is the mttr_trend test module only because any incidental test-only adjustment lands there; board_summary baselines and test are read-only.)
  </action>
  <verify>
    <automated>python -m pytest tests/ -x</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_board_summary_baseline.py -x` exits 0 (all three board_summary baselines assert zero diff).
    - board_summary baseline JSONs byte-unchanged: `git diff --quiet -- tests/baselines/board_summary_test_pull.json tests/baselines/board_summary_test_pull_analyst_off.json tests/baselines/board_summary_test_pull_zero_match.json` exits 0.
    - mttr_by_severity_module.py byte-unchanged: `git diff --quiet -- reports/modules/mttr_by_severity_module.py` exits 0.
    - Full suite green: `python -m pytest tests/ -x` exits 0.
  </acceptance_criteria>
  <done>An automated test proves board_summary structural baselines are byte-identical after the gap-closure change (D-16-10), mttr_by_severity is byte-unchanged, and the full suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixtures → committed repo | Synthetic data + structural baselines persisted under version control |
| operator config (`mttr_view`) → render path | Exercised in tests to prove whitelist + safe-default behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-16 | Information Disclosure | committed test fixtures + regenerated baselines | mitigate | All fixtures synthetic (asset_uuid all-zero prefix; RFC 5737/6761). Regenerated baselines capture only structural keys — never metric values or row data (QUAL-05). |
| T-16-17 | Tampering | board_summary regression undetected | mitigate | Re-run the D-16-10 zero-diff assertion; any structural drift in board_summary fails the build. board_summary baselines asserted byte-identical via `git diff --quiet`. |
| T-16-18 | Tampering | `mttr_view` whitelist bypass | mitigate | TestMttrViewBadValueFallback proves an unknown value falls back to owner and validate_config rejects it — the operator-config trust boundary is enforced by test. |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | No package installs — zero new dependencies. No legitimacy gate required. |

No high-severity threats. Tests over synthetic, aggregate, structural-only data; no new external I/O or credentials.
</threat_model>

<verification>
- `python -m pytest tests/test_mttr_trend_module.py -x -W error::FutureWarning` exits 0 (view selector + Owner-SLA-drop + single-page + existing gates).
- `python -m pytest tests/test_mttr_trend_module.py -m baseline` exits 0 (regenerated self-guarded baselines).
- `python -m pytest tests/test_board_summary_baseline.py -x` exits 0; board_summary baselines byte-unchanged (D-16-10).
- `git diff --quiet -- reports/modules/mttr_by_severity_module.py` exits 0.
- `python -m pytest tests/ -x` full suite green.
</verification>

<success_criteria>
- mttr_view=owner/severity/both/default(owner) are each asserted by passing synthetic tests.
- The Owner cut no longer emits the hard-coded Critical-SLA "SLA Target" (proven by test).
- A bad mttr_view value falls back to owner and is rejected by validate_config (proven by test).
- Single-cut owner-only output fits one PDF page (page-count assertion).
- mttr_trend structural baselines regenerated for the owner-only default; board_summary baselines byte-identical (D-16-10).
- All committed fixtures synthetic (QUAL-05).
</success_criteria>

<output>
Create `.planning/phases/16-mttr-rework/16-05-SUMMARY.md` when done.
</output>
