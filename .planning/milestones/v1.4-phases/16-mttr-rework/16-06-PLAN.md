---
phase: 16-mttr-rework
plan: 06
type: execute
wave: 5
depends_on: ["16-04"]
files_modified:
  - reports/modules/mttr_trend_module.py
  - reports/composed_report.py
  - scripts/capture_trend_snapshot.py
  - delivery_config.yaml
  - CLAUDE.md
autonomous: true
requirements: [RPT-05]
gap_closure: true
must_haves:
  truths:
    - "D-16-13: a 4-gauge per-severity MTTR headline band (Critical/High/Medium/Low) renders in ALL views (owner / application / gauges-only) — the prior `if mttr_view in ('severity','both')` gate on the gauge block (~:799-852) is removed so gauges always render"
    - "D-16-13: each gauge reads its SLA from config.py SLA_DAYS (never hardcoded) and draws via reports.modules.chart_utils.draw_gauge with a per-severity threshold band + SLA reference tick"
    - "D-16-13: each gauge carries a MoM DIRECTION indicator derived from the existing per-severity MoM delta — ▼ GREEN when this-month MTTR < last-month (faster/improving), ▲ RED when higher (slipping), — when flat / no prior month (polarity: MTTR lower-is-better)"
    - "D-16-13: the Severity table is REMOVED from PDF, email, and Excel (gauges replace it); Excel substitutes a compact 4-row severity numeric block (Severity | MTTR | SLA | Status | MoM Delta) because Excel cannot embed gauge images"
    - "D-16-13: the detail table is FOCUS-DRIVEN — unfocused (no single Owner= filter) → Owner table; focused on a single Owner group (tag_category=='Owner' and tag_value set) → Application table (using the `application` column from extract_owner); focused to a single Application (or narrower) → NO table (gauges only)"
    - "D-16-13: the `mttr_view` option from 16-04 is RETIRED (removed from compute(), validate_config, and delivery_config.yaml docs); replaced by an optional override `mttr_table ∈ {auto, owner, application}` (default `auto` = focus-driven)"
    - "D-16-13: gauge + table scope auto-follows the report's tag filter; per-severity averaging stays SAMPLE-WEIGHTED across that severity's findings (mean of days_to_fix, not a mean of per-owner means) — consistent with D-16-02"
    - "Focus signal plumbed: composed_report.py forwards the active tag_category/tag_value to the mttr_trend module (via its module_options) so compute() can resolve focus depth; no other module's options are affected"
    - "Empty-data guard (QUAL-03): every channel survives a zero-row ModuleData using safe_pct/safe_int/safe_format — no bare f-string format spec on a possibly-None value (guards the latent CR-01 'None' formatting issue); cold-start and error branches unchanged"
    - "Out of scope held byte-for-byte: MTTR math, rolling window, MoM computation, RAG strip, and reports/modules/mttr_by_severity_module.py are UNCHANGED (D-16-10); board_summary output unaffected"
    - "Snapshot-script bootstrap fix: scripts/capture_trend_snapshot.py gains a project-root sys.path bootstrap BEFORE its first-party imports (matching scripts/smoke_board_summary_cutover.py) so `python scripts/capture_trend_snapshot.py` runs from any CWD without ModuleNotFoundError"
    - "CLAUDE.md SLA reference table Medium row corrected 45→60 to match config.py SLA_DAYS"
  artifacts:
    - path: "reports/modules/mttr_trend_module.py"
      provides: "Always-on 4-gauge band with MoM arrows; severity table removed; focus-driven Owner/Application/no-table detail; mttr_view retired and mttr_table override added"
      contains: "draw_gauge"
    - path: "reports/composed_report.py"
      provides: "Forwards active tag_category/tag_value to the mttr_trend module so compute() resolves focus depth"
      contains: "mttr_trend"
    - path: "scripts/capture_trend_snapshot.py"
      provides: "Project-root sys.path bootstrap before first-party imports"
      contains: "sys.path.insert"
    - path: "delivery_config.yaml"
      provides: "mttr_view docs retired; optional mttr_table {auto,owner,application} documented"
      contains: "mttr_table"
    - path: "CLAUDE.md"
      provides: "SLA table Medium 45→60 doc fix"
      contains: "60"
  key_links:
    - from: "reports/modules/mttr_trend_module.py render_pdf_section"
      to: "reports.modules.chart_utils.draw_gauge"
      via: "per-severity gauge band rendered unconditionally (gate removed)"
      pattern: "draw_gauge"
    - from: "reports/modules/mttr_trend_module.py compute()"
      to: "config.py SLA_DAYS"
      via: "per-severity SLA read for gauge thresholds + reference tick"
      pattern: "SLA_DAYS\\["
    - from: "reports/composed_report.py run_report()"
      to: "mttr_trend module_options"
      via: "inject active tag_category/tag_value into the mttr_trend options dict"
      pattern: "mttr_trend"
    - from: "scripts/capture_trend_snapshot.py"
      to: "config / data / tenable_client first-party imports"
      via: "sys.path.insert(0, repo_root) before the imports"
      pattern: "sys\\.path\\.insert"
---

<objective>
Close the Phase 16 UAT round-2 gaps locked as D-16-13 (supersedes the D-16-11/12 table-toggle) and the Test-5 snapshot-script blocker.

D-16-13 redesigns the `mttr_trend` presentation: (1) a 4 per-severity MTTR gauge headline band that renders in EVERY view (the per-severity gauge block at ~:799-852 is currently gated behind `if mttr_view in ("severity","both")` — un-gate it), each gauge vs its config.py SLA with a MoM direction arrow; (2) REMOVE the severity table (gauges replace it) from PDF/email/Excel, with Excel keeping a compact 4-row severity numeric block since it can't embed gauges; (3) a FOCUS-DRIVEN detail table replacing the `mttr_view` toggle — Owner table when unfocused, Application table when focused on a single Owner group, no table when focused to a single Application; (4) retire `mttr_view`, add an optional `mttr_table ∈ {auto, owner, application}` override.

The focus signal (`tag_category`/`tag_value`) does NOT currently reach `compute()` — the composer forwards only `self._kwargs`, and composed_report.py does not include the tag scope. This plan plumbs it through the module's `module_options`.

The Test-5 blocker is mechanical: `scripts/capture_trend_snapshot.py` lacks a `sys.path` root bootstrap, so the documented `python scripts/capture_trend_snapshot.py` invocation fails with `ModuleNotFoundError: No module named 'config'`. Add the bootstrap and audit sibling scripts.

Purpose: Leadership always sees the 4-gauge severity headline (with improving/slipping direction) at any focus depth, and the scheduled snapshot capture actually runs in production.
Output: A redesigned `mttr_trend_module.py` (always-on gauge band, focus-driven table), focus-signal plumbing in `composed_report.py`, a bootstrapped `capture_trend_snapshot.py`, a doc-fixed `delivery_config.yaml` + `CLAUDE.md`. MTTR math/window/MoM/RAG and `mttr_by_severity_module.py` / board_summary are untouched (D-16-10).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-mttr-rework/16-CONTEXT.md
@.planning/phases/16-mttr-rework/16-UAT.md
@.planning/phases/16-mttr-rework/16-04-PLAN.md
@CLAUDE.md

<interfaces>
<!-- Key contracts the executor needs. Read the real source before editing. -->

config.py SLA_DAYS (authoritative — NEVER hardcode an SLA in the module):
  {"critical": 15, "high": 30, "medium": 60, "low": 120}
  CLAUDE.md line 93 SLA table shows Medium=45 — STALE; config.py wins. Fix the doc 45→60.

reports/modules/chart_utils.py — draw_gauge signature (already imported in the module):
  draw_gauge(value, min_val=0, max_val=100, thresholds=[(thresh_val, hex_color),...],
             title="", unit="%", reference_line=None, reference_label=None, figsize=(3,2)) -> str (base64 PNG)
  The existing gauge block (~:799-852) already calls it with unit="d", reference_line=sla,
  reference_label="SLA", thresholds=[(sla,color),(sla*1.25,amber),(sla*2,red)], max_val=sla*2.
  REUSE that call shape; just un-gate it and add the MoM direction arrow next to/under each gauge.

reports/modules/board_report_utils.py — extract_owner(assets_df) already returns BOTH:
  - `owner` column (Owner= tag value, "Unassigned" default)
  - `application` column (Application= tag value, "" default)
  APPLICATION_TAG_CATEGORY="Application", OWNER_TAG_CATEGORY="Owner".
  The module already calls extract_owner(assets_df) for the Owner cut (~:477). For the
  Application cut, build a uuid→application map the same way (uuid_to_app) and group fixed
  findings by application — reuse the existing owner-grouping loop shape (~:494-512).

reports/modules/mttr_trend_module.py — current shape (read in full before editing):
  - compute() reads options ~:332-345 (window_days, min_sample, mttr_view). REMOVE the mttr_view
    block; ADD an mttr_table read (`str(config.options.get("mttr_table","auto")).lower().strip()`,
    whitelist {auto,owner,application}, warn+fallback to "auto") AND read the focus signal
    (`tag_category = config.options.get("tag_category")`, `tag_value = config.options.get("tag_value")`).
  - Per-severity MoM delta is NOT currently computed (only owner_mom exists ~:582-585). Derive a
    per-severity MoM delta from the existing sev_series (~:556,563-564) using the same
    _owner_mom_delta(series) helper (curr-prev of the last two points). Stash per-severity
    mom deltas so render_pdf_section can draw the arrow.
  - The per-severity gauge block is render_pdf_section ~:799-852 and is gated by
    `if mttr_view in ("severity","both")`. Remove the gate so it always builds.
  - table_data_severity (~:605-616) feeds the PDF severity table (~:913-931), the Excel severity
    block, and analyst rows. The Severity TABLE (PDF ~:913-931, Excel sev_headers path ~:1096-1104)
    must be removed from the headline channels; analyst_rows keep both cuts.
  - table_data_owner (~:621-632) feeds the Owner table. Keep an Owner builder; ADD a parallel
    table_data_application builder (group fixed findings by `application`, same row shape, no SLA anchor).
  - metadata stashes mttr_view ~:716 + table_data_severity/owner ~:717-718. Replace mttr_view with
    the resolved table mode ("owner"/"application"/"none") and add table_data_application.
  - validate_config ~:1245-1270 validates mttr_view — REMOVE that block; ADD mttr_table validation
    (whitelist {auto,owner,application} when explicitly set).

Empty-data guard (CLAUDE.md QUAL-03): safe_pct / safe_int / safe_format for any possibly-None value;
return safe defaults, never raise. The overall_line at ~:788-797 and the gauge "msg" path already
guard None; preserve that. Cold-start path (metrics["cold_start"]) renders unchanged.

scripts/smoke_board_summary_cutover.py lines 51-52 — the bootstrap PATTERN to copy:
  REPO_ROOT = Path(__file__).resolve().parent.parent
  sys.path.insert(0, str(REPO_ROOT))
  (placed BEFORE the first-party imports; sibling smoke_email_phase2.py uses the same shape.)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Snapshot-script bootstrap fix + sibling audit (Test-5 blocker)</name>
  <read_first>
    - scripts/capture_trend_snapshot.py (the blocker — first-party imports at lines 33-41 with no sys.path bootstrap; module-level imports run at lines 23-41)
    - scripts/smoke_board_summary_cutover.py (lines 40-52 — the REPO_ROOT + sys.path.insert pattern to copy, placed before first-party imports with noqa: E402 on the deferred imports)
    - scripts/warm_cache.py, scripts/smoke_email_phase2.py, scripts/setup_github_labels.py (audit targets for the same missing-bootstrap footgun)
  </read_first>
  <files>scripts/capture_trend_snapshot.py</files>
  <action>
    Add a project-root sys.path bootstrap to scripts/capture_trend_snapshot.py BEFORE its first-party imports (the `from config import CACHE_DIR` block at line 33 onward). Insert, immediately after the stdlib imports (`from pathlib import Path` is already imported at line 30): `sys.path.insert(0, str(Path(__file__).resolve().parent.parent))`. Match the exact shape used by scripts/smoke_board_summary_cutover.py (a REPO_ROOT local then insert, OR the inline insert). Keep the `from __future__` line first. Add `# noqa: E402` to the first-party import block if a linter would flag import-after-statement (smoke_board_summary_cutover uses this). Do NOT reorder or change any of the first-party imports themselves; do NOT touch any other logic in the file.

    Audit the other scripts/*.py for the same footgun: check each of scripts/warm_cache.py, scripts/smoke_email_phase2.py, scripts/setup_github_labels.py for whether they (a) import first-party modules at module level and (b) already bootstrap sys.path. Record findings in the SUMMARY. Fix ONLY if the missing bootstrap is trivially the same pattern (a script that imports `config`/`data`/`reports`/`tenable_client` at top level with no `sys.path.insert` and is meant to be run as `python scripts/<name>.py`); otherwise flag it in the SUMMARY for a follow-up rather than touching it in this plan. Note: scripts run as `python -m scripts.<name>` do not need the bootstrap — only the direct `python scripts/<name>.py` invocation does.
  </action>
  <verify>
    <automated>python scripts/capture_trend_snapshot.py --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - The bootstrap is present before the first-party imports: `grep -n "sys.path.insert" scripts/capture_trend_snapshot.py` shows an insert of the resolved parent.parent, and it appears at a line number LESS than the `from config import` line (`grep -n "from config import" scripts/capture_trend_snapshot.py`).
    - Real CLI invocation from a non-root CWD succeeds (the exact UAT-5 reproduction): running `python scripts/capture_trend_snapshot.py --dry-run` from the repo root exits 0 with no `ModuleNotFoundError`. (The 16-07 subprocess regression test locks the non-root-CWD case.)
    - First-party imports are byte-equivalent in identity (only their position/ordering may shift for the bootstrap): `grep -c "from config import CACHE_DIR" scripts/capture_trend_snapshot.py` == 1 and the data/trend_store + tenable_client + utils.asset_count imports remain present.
    - Sibling audit recorded: the SUMMARY lists, for warm_cache.py / smoke_email_phase2.py / setup_github_labels.py, whether each already bootstraps or needs one (and whether it was fixed or flagged).
  </acceptance_criteria>
  <done>`python scripts/capture_trend_snapshot.py --dry-run` runs without ModuleNotFoundError from a non-root CWD; the bootstrap precedes the first-party imports; sibling scripts audited (trivially-same fixed, others flagged in SUMMARY).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Always-on 4-gauge band with MoM arrows + focus-driven Owner/Application detail in compute() + focus plumbing</name>
  <read_first>
    - reports/modules/mttr_trend_module.py (read compute() ~:258-729 in full — esp. the options read ~:332-345, per-severity loop ~:437-472, owner loop ~:477-517, sev_series MoM source ~:556-585, table builders ~:601-635, metadata stash ~:711-724)
    - reports/modules/board_report_utils.py (extract_owner — `owner` AND `application` columns; reuse the uuid→label map pattern)
    - reports/composed_report.py (run_report ~:303-310 builds module_configs from module_options; ~:281-301 applies the tag filter — this is where tag_category/tag_value are known)
    - .planning/phases/16-mttr-rework/16-UAT.md (`## Gaps` — the D-16-13 entry is the authoritative spec with line numbers)
    - CLAUDE.md (four-channel render contract; safe_pct/safe_int/safe_format empty-data guard; SLA table)
  </read_first>
  <files>reports/modules/mttr_trend_module.py, reports/composed_report.py</files>
  <behavior>
    - mttr_table default ("auto") + unfocused population → resolved table mode is "owner"; the Owner detail rows are built.
    - mttr_table "auto" + focused on a single Owner group (tag_category=="Owner" and a non-empty tag_value) → resolved table mode is "application"; the Application detail rows are built from the `application` column.
    - mttr_table "auto" + focused to a single Application (tag_category=="Application" and tag_value set, OR a population with exactly one distinct owner+application) → resolved table mode is "none"; no detail rows.
    - mttr_table explicit "owner"/"application" overrides auto regardless of focus.
    - Per-severity MoM delta is computed from the persisted sev_series (curr-prev of last two points via _owner_mom_delta); when there is no prior month or the delta is None/0 the direction is "flat".
    - Per-severity gauge VALUES are unchanged (sample-weighted mean of days_to_fix per severity, D-16-02) — only their gating and the MoM-arrow metadata change.
    - bad mttr_table value → warns, falls back to "auto"; validate_config returns a non-empty error for an explicit bad value.
    - Empty-data / cold-start ModuleData still returns coherently (no crash; mode resolves to "none" or "owner" with empty rows).
  </behavior>
  <action>
    In compute(): REMOVE the `mttr_view` whitelist block (~:335-345). ADD: read the focus signal `tag_category = config.options.get("tag_category")` and `tag_value = config.options.get("tag_value")` (both may be None); read `mttr_table = str(config.options.get("mttr_table","auto")).lower().strip()`, whitelist to {auto, owner, application}, warn + fall back to "auto" on an unknown value (mirror the prior mttr_view pattern). NEVER interpolate the raw tag_category/tag_value/mttr_table into any HTML/Excel sink — only validated tokens reach render branches.

    Resolve the table mode (do not call it mttr_view): if `mttr_table == "owner"` → "owner"; if `mttr_table == "application"` → "application"; if "auto": focused-on-single-Owner (`str(tag_category).lower()=="owner"` and `tag_value`) → "application"; focused-to-single-Application (`str(tag_category).lower()=="application"` and `tag_value`) → "none"; otherwise → "owner". Document the resolution in a short comment. Stash the resolved mode in `metadata["mttr_table_mode"]` (one source of truth for renderers).

    Build the Application detail rows: build `uuid_to_app` from `extract_owner(assets_df)` (the `application` column — extract_owner is already called at ~:477; reuse `enriched_assets["application"]`). Map fixed_df["asset_uuid"] → application (fillna ""→ label as "Unassigned" or "" consistently with the owner path), group by application, compute sample-weighted MTTR per application (mean of days_to_fix), with the same min_sample sparse-wording and `_status_from_ratio(m/critical_sla)` status as the owner path. Build `table_data_application` rows with the SAME shape as table_data_owner (label/mttr_days/sla_days=None/variance=None/status/sample_size/mom_delta=None/insufficient). Keep table_data_owner as built. Stash `table_data_application` in metadata alongside table_data_owner. KEEP table_data_severity built (the gauges + the Excel numeric block + analyst rows still need its per-severity values) — but it will no longer drive a headline TABLE.

    Compute per-severity MoM direction: for each severity, take `sev_series[sev]` (already built ~:556,563-564), compute `sev_mom = _owner_mom_delta(sev_series[sev])` (reuse the existing helper). Derive a direction token per severity: "down" when sev_mom is not None and < 0 (MTTR decreased → improving), "up" when > 0 (slipping), "flat" when None or == 0. Stash a `per_sev_mom_direction` dict (and the raw delta) into metadata so render_pdf_section / email can draw ▼green / ▲red / —. (MTTR lower-is-better — down = green = good.)

    Keep ALL existing compute() math byte-identical otherwise: overall_mttr, per_sev_mttr values, owner_mttr, RAG strip, MoM series, analyst_rows (still both cuts), summary_text, window/cold-start branches. Use .assign() for any pandas mutation (CoW). Use safe_* for any None-able value.

    Extend validate_config: REMOVE the mttr_view validation block (~:1261-1269). ADD mttr_table validation — when `config.options.get("mttr_table")` is explicitly set and `str(...).lower().strip() not in {auto, owner, application}`, append a clear error (mirror the prior format).

    In reports/composed_report.py run_report(): after the tag filter is resolved (the `if tag_category and tag_value:` block ~:281), inject the active tag scope into the mttr_trend module's options so compute() can resolve focus depth. In the `opts_map`/module_configs build (~:306-310), if "mttr_trend" is in `modules`, merge `{"tag_category": tag_category, "tag_value": tag_value}` into that module's options dict (do not overwrite an operator-supplied mttr_table; only add the focus keys). Keep this surgical — only the mttr_trend entry's options are augmented; all other modules' options are untouched. Document with a one-line comment referencing D-16-13 focus-driven table.
  </action>
  <verify>
    <automated>python -c "import pandas as pd; pd.options.mode.copy_on_write=True; import reports.modules.mttr_trend_module as m; import reports.modules.registry as r; assert 'mttr_trend' in r._modules; from reports.modules.base import ModuleConfig; mod=m.MTTRTrendModule(); assert mod.validate_config(ModuleConfig('mttr_trend', options={'mttr_table':'bogus'})); assert not mod.validate_config(ModuleConfig('mttr_trend', options={'mttr_table':'owner'})); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - mttr_view is gone from compute() and validate_config: reading the file, the `_valid_views`/`mttr_view` whitelist in compute() and the mttr_view branch in validate_config are removed. `grep -c "mttr_table" reports/modules/mttr_trend_module.py` >= 4 (read, whitelist, metadata stash, validate_config).
    - Focus signal read in compute(): `grep -c "options.get(\"tag_category\"" reports/modules/mttr_trend_module.py` >= 1 and `options.get("tag_value"` >= 1.
    - Application rows built from extract_owner's application column: `grep -c "application" reports/modules/mttr_trend_module.py` >= 2 and `grep -c "table_data_application" reports/modules/mttr_trend_module.py` >= 2 (built + stashed in metadata).
    - Resolved table mode stashed: `grep -c "mttr_table_mode" reports/modules/mttr_trend_module.py` >= 2 (stash + at least one renderer read in Task 3).
    - Per-severity MoM direction stashed: `grep -c "per_sev_mom_direction" reports/modules/mttr_trend_module.py` >= 1 and `_owner_mom_delta` is reused for severity series (grep shows a call passing a sev_series entry).
    - validate_config rejects bad mttr_table, accepts a good one: the verify one-liner exits 0.
    - Focus plumbed in composed_report: `grep -c "mttr_trend" reports/composed_report.py` >= 2 (existing frozenset memberships + the new options injection) and the injection references tag_category/tag_value near the module_configs build (verify by reading ~:306-310).
    - MTTR math untouched: `grep -c "days_to_fix.mean" reports/modules/mttr_trend_module.py` unchanged from before (the overall + per-severity + per-owner means are byte-identical); no change to the COALESCE clock or window filter.
    - mttr_by_severity_module.py byte-unchanged: `git diff --quiet -- reports/modules/mttr_by_severity_module.py` exits 0.
  </acceptance_criteria>
  <done>compute() drops mttr_view, reads the focus signal + mttr_table override, resolves a table mode (owner/application/none), builds Owner AND Application detail rows from extract_owner, computes per-severity MoM direction, and stashes mode + direction in metadata. composed_report forwards the active tag scope into the mttr_trend options. validate_config validates mttr_table. All MTTR math and mttr_by_severity are unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Always-on gauge band (PDF/email) + remove severity table + focus-driven render across PDF/Excel/email; doc fixes</name>
  <read_first>
    - reports/modules/mttr_trend_module.py (as edited by Task 2 — read render_pdf_section ~:742-974, render_excel_tabs ~:980-1122, render_email_panel ~:1128-1195; read the new metadata keys mttr_table_mode / table_data_application / per_sev_mom_direction)
    - reports/modules/chart_utils.py (draw_gauge call shape — already used at ~:832-841)
    - .planning/phases/16-mttr-rework/16-UAT.md (`## Gaps` — D-16-13 CHANNELS section: PDF+email gauges as base64 + focus table; Excel 4-row severity numeric block + focus table; analyst keeps full detail)
    - CLAUDE.md (email inline-CSS only, no <style> blocks, base64 CID; four-channel contract; SLA table line 93 Medium=45 → fix to 60)
    - config.py (SLA_DAYS Medium=60 — the authoritative value for the doc fix)
    - delivery_config.yaml (the composed-report example group with module_options.mttr_view added by 16-04 — retire mttr_view, document mttr_table)
  </read_first>
  <files>reports/modules/mttr_trend_module.py, delivery_config.yaml, CLAUDE.md</files>
  <behavior>
    - PDF render: the 4-gauge severity band renders for ALL modes (owner / application / none) — no gating; each gauge carries a ▼green / ▲red / — direction marker from metadata["per_sev_mom_direction"]. No severity <table> anywhere in render_pdf_section. The focus-driven detail table is the Owner table (mode "owner"), the Application table (mode "application"), or absent (mode "none").
    - Excel render: no severity TABLE; instead a compact 4-row numeric block with columns Severity | MTTR | SLA | Status | MoM Delta (from table_data_severity + per_sev_mom direction). Below it, the focus-driven Owner/Application table (or none). Tab name stays "MTTR Trend"; window disclosure in row 1.
    - Email panel: overall MTTR headline retained; gauges referenced as inline base64 images (or, if the existing panel does not embed images, retain the headline + the focus-table disclosure note — keep inline CSS only). No severity table. View-disclosure note reflects the resolved mode (Owner / Application / gauges only).
    - Analyst tab (CONTRACT-02) unchanged — keeps both severity + owner cuts; ADD application detail to the analyst frame if trivially available (do not regress existing analyst columns).
    - Cold-start and error branches render exactly as before.
    - Zero-row ModuleData survives every channel (safe_* guards; no bare format on None — guards CR-01).
  </behavior>
  <action>
    render_pdf_section: REMOVE the `if mttr_view in ("severity","both")` gate on the gauge block (~:801) so the 4 gauges always build. For each gauge, after the existing draw_gauge call, render a MoM direction marker from `data.metadata.get("per_sev_mom_direction", {})[sev]`: ▼ in green (#388e3c) for "down", ▲ in red (#d32f2f) for "up", — in grey for "flat" — inline-styled span beneath/beside the gauge title (keep it simple, no new CSS classes). REMOVE the Severity <table> section entirely (~:913-931) and the `if mttr_view in ("severity","both"):` branch that emits it. Replace the table-mode branching (~:933-950) with the resolved mode read `mode = data.metadata.get("mttr_table_mode","owner")`: mode "owner" → emit the Owner table (existing _pdf_owner_rows over table_data_owner); mode "application" → emit an Application table (same column shape as Owner but header "MTTR by Application", rows over metadata["table_data_application"]); mode "none" → emit no detail table. Update the trailing explanatory text so it no longer references the severity table; keep the reopened-aware / sample-weighted disclosure and add a one-line "Gauges show per-severity MTTR vs SLA; arrow = month-over-month direction (▼ faster, ▲ slower)."

    render_excel_tabs: keep tab name "MTTR Trend" + row-1 window disclosure. REMOVE the severity TABLE path; instead always write a compact 4-row severity numeric block with headers [Severity, MTTR (Days), SLA Target (Days), Status, MoM Delta (Days)] from table_data_severity (+ per_sev_mom direction/delta). Then, below a blank separator row, write the focus-driven table: mode "owner" → Owner block (existing owner headers/writer); mode "application" → Application block (Owner-shaped headers relabeled "Application"); mode "none" → no second block. Read `mode = data.metadata.get("mttr_table_mode","owner")`. Preserve the cold-start/error early returns.

    render_email_panel: keep the overall MTTR headline + driver_narrative + window disclosure. REMOVE any severity-table emission. Update the `_view_labels` map (~:1172-1177) to reflect the resolved mode (Owner breakdown / Application breakdown / Gauges only) read from metadata["mttr_table_mode"]. If the panel currently embeds gauge images, reference them as inline base64; if not, keep the headline + disclosure (do not add a <style> block; inline CSS only). Cold-start/error branches unchanged.

    render_analyst_tabs: unchanged contract (returns data.analyst_rows). If table_data_application was added to the analyst frame in Task 2, it flows through automatically; otherwise leave analyst_rows as-is (full detail preserved).

    delivery_config.yaml: on the composed-report example group, REMOVE the `module_options.mttr_view` example added by 16-04 and document `mttr_table` instead — show `mttr_trend: {mttr_table: auto}` with an inline comment listing allowed values {auto, owner, application}, default auto = focus-driven (Owner unfocused, Application when focused on a single Owner, gauges-only at Application depth). Touch no other group.

    CLAUDE.md: fix the SLA reference table Medium row from 45 to 60 (line ~93) to match config.py SLA_DAYS. Surgical one-cell edit; do not touch other rows.
  </action>
  <verify>
    <automated>python -c "import pandas as pd; pd.options.mode.copy_on_write=True; import reports.modules.mttr_trend_module as m; import yaml; d=yaml.safe_load(open('delivery_config.yaml')); assert isinstance(d['groups'], list); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Gauge band is no longer gated: the `if mttr_view in ("severity","both")` guard on the gauge block is gone; reading render_pdf_section, the gauges_html build runs unconditionally. `grep -c "draw_gauge" reports/modules/mttr_trend_module.py` >= 1 and `grep -c "mttr_view" reports/modules/mttr_trend_module.py` == 0.
    - Severity table removed from headline channels: the PDF `<th>...Severity...</th>` table header block and "MTTR by Severity" <h3> table are gone from render_pdf_section (verify by reading — the only "Severity" strings remaining in render_pdf_section are gauge titles / the Excel numeric-block headers / analyst columns, NOT a headline <table>).
    - MoM direction markers present: `grep -c "per_sev_mom_direction" reports/modules/mttr_trend_module.py` >= 1 (read in render_pdf_section) and the green/red/grey arrow glyphs appear (`grep -c "▼\|▲" reports/modules/mttr_trend_module.py` >= 1, or the chosen arrow representation is present).
    - Focus-driven mode read in all three headline renderers: `grep -c "mttr_table_mode" reports/modules/mttr_trend_module.py` >= 4 (compute stash + PDF + Excel + email).
    - Application table path present: `grep -c "MTTR by Application\|table_data_application" reports/modules/mttr_trend_module.py` >= 2.
    - Excel severity numeric block (not a removed table): render_excel_tabs writes a Severity/MTTR/SLA/Status/MoM-Delta header set; verify by reading the headers list.
    - delivery_config.yaml: `grep -c "mttr_view" delivery_config.yaml` == 0 and `grep -c "mttr_table" delivery_config.yaml` >= 1; YAML parses (verify exits 0); only the one example group changed (`git diff delivery_config.yaml` touches no other group).
    - CLAUDE.md SLA fix: `grep -c "| Medium   | 4.0 – 6.9       | 45" CLAUDE.md` == 0 and the Medium row now shows 60.
    - mttr_by_severity_module.py byte-unchanged: `git diff --quiet -- reports/modules/mttr_by_severity_module.py` exits 0.
    - Module imports clean: `python -c "import reports.modules; import reports.modules.registry as r; assert 'mttr_trend' in r._modules"` exits 0.
  </acceptance_criteria>
  <done>PDF + email render the always-on 4-gauge band (with ▼/▲/— MoM arrows) plus the focus-driven Owner/Application/no-table detail; the severity table is gone from all headline channels; Excel keeps a compact severity numeric block + the focus table; analyst keeps full detail; delivery_config.yaml documents mttr_table (mttr_view retired) and CLAUDE.md's Medium SLA is corrected to 60. mttr_by_severity unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| delivery_config.yaml (operator-supplied) → mttr_trend render branches | `mttr_table` operator option selects a render path; whitelisted before use |
| tag_category / tag_value (resolved from operator filter) → focus resolution | drives which detail table renders; never interpolated into a sink |
| scripts/capture_trend_snapshot.py CWD → first-party import resolution | bootstrap makes the repo root authoritative regardless of invocation CWD |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-19 | Tampering | `mttr_table` operator config | mitigate | Whitelist to {auto, owner, application} with a safe `auto` default; unknown values log a WARNING and fall back. validate_config rejects bad values at `--dry-run`. Never branch on or interpolate the raw value into any HTML/Excel sink. |
| T-16-20 | Tampering | focus signal (tag_category/tag_value) → render | mitigate | Only used to resolve a mode token (owner/application/none); the raw tag strings are never written into HTML/Excel by the new code path (they already reach the existing scope banner via the unchanged chrome path). Lower-casing + exact-category comparison only. |
| T-16-21 | Information Disclosure | gauge band / Application detail render | accept | No new data source, no new I/O, no credentials. Same already-fetched aggregate MTTR data; per-application MTTR is an aggregate count (QUAL-05) — no per-finding PII. |
| T-16-22 | Tampering | board_summary / mttr_by_severity regression | mitigate | mttr_by_severity_module.py asserted byte-unchanged (`git diff --quiet`); board_summary untouched. The 16-07 zero-diff baseline test re-confirms (D-16-10). |
| T-16-23 | Denial of Service | snapshot-script bootstrap path | accept | Bootstrap only prepends the resolved repo root to sys.path; no untrusted input, no path traversal (Path is __file__-derived). Restores documented behavior. |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | No package installs — zero new dependencies (matplotlib/openpyxl already in requirements). No legitimacy gate required. |

No high-severity threats. The change is a config-driven view selector + a focus-resolution token over already-fetched aggregate data, plus a deterministic sys.path bootstrap. No new external I/O or credentials. (ASVS L1: minimal surface — operator config validation + a path bootstrap, both honestly documented.)
</threat_model>

<verification>
- `python scripts/capture_trend_snapshot.py --dry-run` exits 0 (no ModuleNotFoundError).
- `python -c "import reports.modules; import reports.modules.registry as r; assert 'mttr_trend' in r._modules"` exits 0.
- `grep -c "mttr_view" reports/modules/mttr_trend_module.py` == 0; `grep -c "mttr_table" reports/modules/mttr_trend_module.py` >= 4.
- `grep -c "draw_gauge" reports/modules/mttr_trend_module.py` >= 1; gauge block no longer gated.
- `git diff --quiet -- reports/modules/mttr_by_severity_module.py` exits 0 (D-16-10).
- delivery_config.yaml parses; mttr_view retired, mttr_table documented; only the example group changed.
- CLAUDE.md Medium SLA row shows 60, not 45.
- Full behavioral proof (gauges present, arrow polarity, focus routing, severity-table absent, single-page, snapshot real-invocation) is the 16-07 TDD plan.
</verification>

<success_criteria>
- A 4 per-severity MTTR gauge band renders in every view, each gauge vs its config.py SLA with a ▼green/▲red/— MoM direction arrow (polarity: lower MTTR = ▼ green).
- The severity table is removed from PDF/email/Excel; Excel keeps a compact 4-row severity numeric block; the detail table is focus-driven (Owner / Application / none).
- mttr_view is retired; mttr_table {auto,owner,application} (default auto) is the new override; the focus signal is plumbed via composed_report.
- `python scripts/capture_trend_snapshot.py` runs from any CWD without ModuleNotFoundError.
- MTTR math, window, MoM, RAG, board_summary, and mttr_by_severity are unchanged (D-16-10). CLAUDE.md Medium SLA corrected to 60.
</success_criteria>

<output>
Create `.planning/phases/16-mttr-rework/16-06-SUMMARY.md` when done.
SUMMARY MUST: (1) record the sibling-script bootstrap audit findings (which scripts already bootstrap, which were fixed, which were flagged); (2) call out the backward-compat note — any existing composed group that set `module_options.mttr_trend.mttr_view` will now see that option ignored (mttr_view retired) and the table auto-resolves by focus; groups can pin via `mttr_table`; (3) note the focus-signal plumbing added to composed_report.py.
</output>
