---
phase: 15-independent-new-modules
plan: "05"
type: execute
wave: 4
depends_on: ["15-02", "15-04"]
files_modified:
  - reports/modules/vuln_density_module.py
  - tests/test_vuln_density_module.py
autonomous: true
requirements: [RPT-02, QUAL-01, QUAL-03, QUAL-05]
user_setup: []

must_haves:
  truths:
    - "RPT-02: A reader sees Vulnerability Density (vulns per asset) trended MoM, using each month's OWN snapshot asset-count denominator"
    - "Phase-14 D-01/D-02: density denominator = on-time-scanned licensed assets; each historical point uses its snapshot on_time_asset_count, NEVER current len(assets_df) (Pitfall 4)"
    - "RPT-02: denominator MoM change is flagged when it exceeds 10% (success criterion 2)"
    - "QUAL-01: cold-starts on the NEW on_time_asset_count field — branches on insufficient_data / absent field; never NaN% or crash; accumulates forward (no back-reuse of all-asset asset_count)"
    - "QUAL-03: zero-asset / division-by-zero guarded; safe_format throughout; survives zero-row input on all four channels"
    - "D-15-08: current month labeled 'Month-to-date (partial)'; D-15-07 RAG bands module_options-overridable"
  artifacts:
    - path: "reports/modules/vuln_density_module.py"
      provides: "VulnDensityModule four-channel MoM module, MODULE_ID=vuln_density"
      contains: "@register_module"
      min_lines: 180
    - path: "tests/test_vuln_density_module.py"
      provides: "per-snapshot denominator, denom-drift flag, cold-start, zero-asset guard, empty-data tests"
      min_lines: 110
  key_links:
    - from: "reports/modules/vuln_density_module.py"
      to: "utils.asset_count.count_on_time_assets"
      via: "current-run denominator (None-checked)"
      pattern: "count_on_time_assets"
    - from: "reports/modules/vuln_density_module.py"
      to: "snapshot on_time_asset_count"
      via: "per-snapshot historical denominator (Pitfall 4)"
      pattern: "on_time_asset_count"
---

<objective>
Build `vuln_density`: a MoM four-channel module showing open vulns-per-asset
trended over time. Each historical density point divides by THAT snapshot's own
`on_time_asset_count` (Phase-14 D-01/D-02; new field from 15-02) — never the
current `len(assets_df)` (Pitfall 4 — retroactive drift). Flags denominator MoM
change >10%. Cold-starts on the new field. Depends on 15-04 having registered
`vuln_density` in `_MODULES_NEEDING_TREND_SNAPSHOTS` (so this plan does NOT touch
composed_report.py).

Purpose: Fleet-size-normalized vuln density MoM (RPT-02).
Output: `reports/modules/vuln_density_module.py` + its unit test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-independent-new-modules/15-CONTEXT.md
@.planning/phases/15-independent-new-modules/15-PATTERNS.md
@.planning/phases/14-shared-substrates-composed-report-gates/14-02-SUMMARY.md

<interfaces>
<!-- trend_snapshots kwarg (via gate fan-out): {"snapshots": [{month, critical, high,
     medium, low, asset_count, on_time_asset_count, ...}], "insufficient_data": bool}
     on_time_asset_count is the NEW 15-02 field; may be None on older snapshots (cold-start). -->

<!-- count_on_time_assets(assets_df, report_date) -> int | None  (None sentinel D-14) — current-run denom -->

<!-- Four-channel + helpers identical to 15-01/15-04; copy the cold-start result pattern from
     new_vs_remediated_module.py (built in 15-04). -->

<!-- RAG defaults (D-15-07): _DEFAULT_GREEN_DENSITY=2.0, _DEFAULT_YELLOW_DENSITY=4.0
     vulns/asset; direction="lower_is_better"; overridable via config.options -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build VulnDensityModule with per-snapshot denominator + drift flag + cold-start</name>
  <files>reports/modules/vuln_density_module.py, tests/test_vuln_density_module.py</files>
  <read_first>
    - reports/modules/new_vs_remediated_module.py (in-repo cold-start + MoM shape from 15-04 — copy _build_cold_start_result, partial-month label, four-channel render)
    - .planning/phases/15-independent-new-modules/15-PATTERNS.md "reports/modules/vuln_density_module.py" section (lines 371-417 — count_on_time_assets denom, per-snapshot density loop, denom-drift flag, RAG defaults)
    - utils/asset_count.py (count_on_time_assets None-sentinel — D-14)
    - .planning/phases/14-shared-substrates-composed-report-gates/14-02-SUMMARY.md (D-02: density cold-starts on on_time_asset_count; do NOT reuse all-asset asset_count)
    - reports/modules/rag_utils.py + format_utils.py
  </read_first>
  <behavior>
    - Test: each snapshot's density = open_count(snapshot) / snapshot["on_time_asset_count"] — a two-snapshot fixture with DIFFERENT on_time_asset_count per month uses each month's own value (Pitfall 4)
    - Test: density does NOT change when assets_df length changes between runs (historical points immutable — Pitfall 4 warning sign)
    - Test: a snapshot whose on_time_asset_count is None is skipped/cold-start for that point (does not crash, does not divide by None — D-15-06 backward-compat)
    - Test: current-run denom from count_on_time_assets None → _empty_result (no division)
    - Test: denom MoM change >10% sets a drift flag in metrics/metadata (success criterion 2)
    - Test: insufficient_data / fewer than 2 usable snapshots → cold-start notice (QUAL-01)
    - Test: zero-asset / empty input → safe four-channel render, gray cell, no ZeroDivisionError (QUAL-03)
    - Test: current month labeled "partial" (D-15-08)
  </behavior>
  <action>
    Create class `VulnDensityModule(BaseModule)` with `MODULE_ID="vuln_density"`,
    `DISPLAY_NAME="Vulnerability Density"`, `@register_module`. Module-level RAG defaults
    `_DEFAULT_GREEN_DENSITY=2.0`, `_DEFAULT_YELLOW_DENSITY=4.0` overridable via config.options
    (D-15-07). In compute(): cold-start branch on `trend_snapshots is None or
    insufficient_data` (copy _build_cold_start_result from new_vs_remediated). Current-run
    denominator `current_denom = count_on_time_assets(assets_df, report_date)`; if None return
    _empty_result (no division). Per snapshot compute density = sum(critical/high/medium/low) /
    `snap.get("on_time_asset_count")`, SKIPPING any snapshot whose on_time_asset_count is None
    (cold-start for that point, D-15-06) — NEVER use len(assets_df) for historical points
    (Pitfall 4). Compute denom-drift flag: when len(usable points)>=2 and
    abs(curr_denom-prev_denom)/max(prev_denom,1)*100 > 10.0, set
    metadata["denom_drift_flag"]=True (success criterion 2). RAG from current_density,
    direction="lower_is_better". Apply partial-month label (D-15-08). Owner cut via
    extract_owner for per-Owner density. Implement all four channels (render_email_panel =
    CONTRACT-01); use safe_format/safe_pct/safe_int for every numeric (zero-division guarded).
    Wrap compute() in try/except → _empty_result; .assign() only (CoW). Write tests with CoW
    strict mode + synthetic fixtures (two snapshots with differing on_time_asset_count).
  </action>
  <verify>
    <automated>cd D:/Projects/vuln-reporting && python -m pytest tests/test_vuln_density_module.py -x -q -W error::FutureWarning</automated>
  </verify>
  <acceptance_criteria>
    - reports/modules/vuln_density_module.py contains @register_module and MODULE_ID = "vuln_density"
    - vuln_density present in registry on import reports.modules
    - per-snapshot denominator test asserts each month divides by its own on_time_asset_count (NOT len(assets_df)) — Pitfall 4
    - denom-drift test asserts the >10% flag is set on a fixture with a >10% denom change
    - cold-start fixture (insufficient_data) renders notice, no "NaN" string in any channel (QUAL-01)
    - zero-asset fixture returns _empty_result with gray cell, no ZeroDivisionError (QUAL-03)
    - pytest tests/test_vuln_density_module.py exits 0 under -W error::FutureWarning
    - grep confirms NO len(assets_df) used as a historical-point denominator
  </acceptance_criteria>
  <done>Module registered; each historical point uses its own snapshot denominator; drift flagged >10%; cold-start + zero-asset safe; tests pass under CoW strict mode.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| snapshot on_time_asset_count → density divisor | wrong/zero/None divisor produces drift artifacts, inf, or crash |
| trend_snapshots history → MoM render | cold-start unguarded → NaN%/crash |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-05-DRIFT | Tampering (correctness) | per-snapshot denominator | mitigate | Each point divides by its own on_time_asset_count; len(assets_df) never used for history (Pitfall 4); denom-drift >10% flagged |
| T-15-05-DIV0 | Denial of Service | zero/None asset count | mitigate | count_on_time_assets None → _empty_result; None snapshot denom skipped; safe_format throughout (QUAL-03) |
| T-15-05-COLDSTART | Denial of Service | <2 usable snapshots | mitigate | insufficient_data + None-field cold-start branch; accumulates forward (D-02) |
| T-15-05-PII | Information Disclosure | per-Owner density fixtures | mitigate | Aggregate counts only; synthetic fixtures (QUAL-05) |
| T-15-05-SC | Tampering | npm/pip installs | accept | Zero new dependencies; no install task |
</threat_model>

<verification>
- `pytest tests/test_vuln_density_module.py -x -q -W error::FutureWarning` exits 0
- `vuln_density` present in registry (gate registration done in 15-04)
- per-snapshot denominator + >10% drift flag asserted by tests
</verification>

<success_criteria>
RPT-02 satisfied: vulns-per-asset trended MoM with each month using its own on_time_asset_count
denominator (D-01/D-02, Pitfall 4); denom MoM change >10% flagged; cold-start safe on the new
field (QUAL-01); zero-asset safe (QUAL-03); partial month labeled (D-15-08); fixtures synthetic.
</success_criteria>

<output>
Create `.planning/phases/15-independent-new-modules/15-05-SUMMARY.md` when done
</output>
