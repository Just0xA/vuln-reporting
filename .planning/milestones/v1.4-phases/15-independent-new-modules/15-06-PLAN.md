---
phase: 15-independent-new-modules
plan: "06"
type: execute
wave: 4
depends_on: ["15-02", "15-04"]
files_modified:
  - reports/modules/accepted_recast_module.py
  - tests/test_accepted_recast_module.py
autonomous: true
requirements: [RPT-04, QUAL-01, QUAL-03, QUAL-05]
user_setup: []

must_haves:
  truths:
    - "RPT-04: A reader sees Accepted & Recast posture with ACCEPTED and RECASTED tracked SEPARATELY (never silently aggregated), current vs prior-month delta arrow, and an Owner cut"
    - "RPT-04: expired-rule findings are flagged 'pending re-evaluation' rather than counted as currently accepted (Pitfall 6a)"
    - "RPT-04: finding counts drive the headline; rule counts live in the analyst tab (Pitfall 6c)"
    - "Pitfall 6b: severity_modification_type filtered via .isin({ACCEPTED}/{RECASTED}); empty-string and NONE treated as unmodified"
    - "QUAL-01: MoM delta cold-starts on insufficient_data; prior-month-missing omits the delta arrow (no '▲ 0%'); never NaN%/crash"
    - "QUAL-03: zero-exception input renders safe gray cell on all four channels; QUAL-05 fixtures synthetic, no real recast_rule_uuid"
  artifacts:
    - path: "reports/modules/accepted_recast_module.py"
      provides: "AcceptedRecastModule four-channel module, MODULE_ID=accepted_recast"
      contains: "@register_module"
      min_lines: 200
    - path: "tests/test_accepted_recast_module.py"
      provides: "separate accepted/recast counts, expiry exclusion, finding-vs-rule, cold-start delta, empty-data tests"
      min_lines: 120
  key_links:
    - from: "reports/modules/accepted_recast_module.py"
      to: "recast_rules_df kwarg (expiry cross-check)"
      via: "expired-rule exclusion (Pitfall 6a)"
      pattern: "recast_rules_df"
    - from: "reports/modules/accepted_recast_module.py"
      to: "data.fetchers._summarize_filter"
      via: "analyst-tab filter display (no inline tree parse)"
      pattern: "_summarize_filter"
---

<objective>
Build `accepted_recast`: a four-channel module showing risk-management posture
with ACCEPTED and RECASTED tracked SEPARATELY (D — never silently aggregated),
a current-vs-prior-month delta arrow, and an Owner cut. Expired-rule findings are
flagged "pending re-evaluation" via the `recast_rules_df` kwarg cross-check
(Pitfall 6a). Finding counts drive the headline; rule counts go in the analyst
tab (Pitfall 6c). Depends on 15-04 having registered `accepted_recast` in BOTH
gate frozensets (trend + recast), so this plan does NOT touch composed_report.py.

Purpose: Accepted/Recast exception posture with MoM delta and expiry awareness (RPT-04).
Output: `reports/modules/accepted_recast_module.py` + its unit test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-independent-new-modules/15-CONTEXT.md
@.planning/phases/15-independent-new-modules/15-PATTERNS.md

<interfaces>
<!-- vulns_df fields (data/fetchers.py lines 357-359): severity_modification_type
     ("ACCEPTED"/"RECASTED"/"NONE"/""), recast_rule_uuid, state -->

<!-- recast_rules_df kwarg (via gate fan-out — fetch_recast_rules, data/fetchers.py lines 584-635):
     columns rule_id, rule_name, plugin_id (int nullable), action (RECAST|ACCEPT),
     new_severity, original_severity, expires_at, created_at, filter
     May be None — degrade gracefully (skip expiry cross-check, log warning). -->

<!-- _summarize_filter(filter_dict) -> readable <=120-char string ("No filter" for empty)
     (data/fetchers.py lines 117-160) — use for analyst-tab filter display, do NOT parse tree inline -->

<!-- trend_snapshots kwarg carries accepted_count/recast_count (NEW 15-02 fields) for MoM delta. -->

<!-- RAG defaults (D-15-07): _DEFAULT_GREEN_EXCEPTION_RATE=5.0, _DEFAULT_YELLOW_EXCEPTION_RATE=15.0 (%) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build AcceptedRecastModule with separate counts, expiry cross-check, MoM delta, four channels</name>
  <files>reports/modules/accepted_recast_module.py, tests/test_accepted_recast_module.py</files>
  <read_first>
    - reports/modules/new_vs_remediated_module.py (in-repo cold-start + MoM delta shape from 15-04)
    - .planning/phases/15-independent-new-modules/15-PATTERNS.md "reports/modules/accepted_recast_module.py" section (lines 421-494 — classification filter Pitfall 6, expiry cross-check, fetch_recast_rules columns, _summarize_filter usage, RAG defaults)
    - data/fetchers.py lines 117-160 (_summarize_filter) and 584-635 (fetch_recast_rules columns)
    - reports/modules/rag_utils.py + format_utils.py + board_report_utils.extract_owner
  </read_first>
  <behavior>
    - Test: accepted_count and recast_count are SEPARATE metrics (never summed into one "exceptions" headline) — both surfaced distinctly (Pitfall 6b)
    - Test: severity_modification_type filtered via .isin({"ACCEPTED"}) / .isin({"RECASTED"}); rows with "" or "NONE" excluded from both (Pitfall 6b)
    - Test: a finding whose recast_rule_uuid maps to an expired rule (expires_at < report_date) is excluded from the current accepted/recast count and flagged "pending re-evaluation" (Pitfall 6a)
    - Test: recast_rules_df=None → expiry cross-check skipped, warning logged, finding-level counts still computed (graceful degradation)
    - Test: headline uses finding counts (from vulns_df), rule list/counts appear only in analyst_rows (Pitfall 6c)
    - Test: MoM delta from trend snapshots accepted_count/recast_count; prior-month missing/insufficient_data → omit delta arrow, no "▲ 0%", no NaN% (QUAL-01, Pitfall 5)
    - Test: analyst tab uses _summarize_filter for the rule filter column (no inline tree parse); accepts None plugin_id
    - Test: zero-exception input → green RAG "0 managed exceptions in scope.", error None; empty vulns_df → safe four-channel render (QUAL-03)
  </behavior>
  <action>
    Create class `AcceptedRecastModule(BaseModule)` with `MODULE_ID="accepted_recast"`,
    `DISPLAY_NAME="Accepted & Recast"`, `@register_module`. Module-level RAG defaults
    `_DEFAULT_GREEN_EXCEPTION_RATE=5.0`, `_DEFAULT_YELLOW_EXCEPTION_RATE=15.0` overridable via
    config.options (D-15-07). In compute(): uppercase severity_modification_type, build
    accepted_mask = .isin({"ACCEPTED"}), recasted_mask = .isin({"RECASTED"}) — "" and "NONE"
    excluded (Pitfall 6b). Rate denominator = total open findings (state in {OPEN, REOPENED}).
    Keep accepted_count and recast_count as SEPARATE metrics (Pitfall 6b — never silently
    aggregated). Expiry cross-check (Pitfall 6a): `recast_rules_df = kwargs.get("recast_rules_df")`;
    when present+non-empty, build expired_ids = rule_ids whose expires_at < report_date, exclude
    those findings from current counts via `~recast_rule_uuid.isin(expired_ids)` and surface them
    as "pending re-evaluation"; when None, log warning and skip cross-check. MoM delta: read
    accepted_count/recast_count from trend snapshots; if trend_snapshots insufficient_data or
    prior-month absent, OMIT the delta arrow (no "▲ 0%", no NaN% — QUAL-01/Pitfall 5) via a
    safe-delta helper. Headline = finding counts; analyst_rows carry the per-rule detail
    (rule_id, action, original_severity, new_severity, expires_at, created_at, finding_count,
    filter via _summarize_filter — accept None plugin_id) (Pitfall 6c). Owner cut via
    extract_owner. Build rag_strip from exception rate, direction="lower_is_better". Implement
    all four channels (render_email_panel = CONTRACT-01); distinguish Accepted vs Recasted in
    PDF/email text (different operational meaning). Wrap compute() in try/except → _empty_result;
    safe_pct/safe_int/safe_format only; .assign() only (CoW). Write tests with CoW strict mode +
    synthetic fixtures (recast_rule_uuid "00000000-...-0001"; no real 32-char Tenable UUIDs — QUAL-05).
  </action>
  <verify>
    <automated>cd D:/Projects/vuln-reporting && python -m pytest tests/test_accepted_recast_module.py -x -q -W error::FutureWarning</automated>
  </verify>
  <acceptance_criteria>
    - reports/modules/accepted_recast_module.py contains @register_module and MODULE_ID = "accepted_recast"
    - accepted_recast present in registry on import reports.modules
    - test asserts accepted_count and recast_count are distinct metrics (not a single aggregated count) — Pitfall 6b
    - expiry test asserts an expired-rule finding is excluded from the current accepted count and flagged pending re-evaluation — Pitfall 6a
    - finding-vs-rule test asserts headline metric is finding count, rule detail only in analyst_rows — Pitfall 6c
    - cold-start/prior-missing test asserts no "▲" delta arrow and no "NaN" string when prior month absent (QUAL-01)
    - pytest tests/test_accepted_recast_module.py exits 0 under -W error::FutureWarning
    - grep confirms _summarize_filter used (no inline filter-tree parse) and no df["col"]= after filter
  </acceptance_criteria>
  <done>Module registered; accepted/recast separate; expired rules flagged; finding-count headline; MoM delta cold-start safe; tests pass under CoW strict mode.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recast_rules_df + vulns_df → accepted/recast counts | expired rules inflate accepted posture if not cross-checked |
| recast_reason / rule detail → logs / analyst tab / fixtures | free-text reason may carry hostnames/IPs; recast_rule_uuid may be real |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-06-EXPIRY | Tampering (correctness) | accepted/recast classification | mitigate | Expiry cross-check against recast_rules_df excludes expired rules (Pitfall 6a); accepted/recast kept separate (6b); finding-count headline (6c) |
| T-15-06-COLDSTART | Denial of Service | MoM delta on missing prior month | mitigate | insufficient_data / prior-absent omits delta arrow via safe-delta helper; no NaN%/crash (QUAL-01/Pitfall 5) |
| T-15-06-PII | Information Disclosure | recast_reason logging + fixtures | mitigate | recast_reason NOT logged at INFO (DEBUG/truncated only); fixtures use synthetic UUIDs, no real recast_rule_uuid (QUAL-05) |
| T-15-06-DEGRADE | Denial of Service | recast_rules_df None / fetch failure | mitigate | None-guard skips cross-check, logs warning, finding-level counts still computed — never crashes the batch |
| T-15-06-SC | Tampering | npm/pip installs | accept | Zero new dependencies; no install task |
</threat_model>

<verification>
- `pytest tests/test_accepted_recast_module.py -x -q -W error::FutureWarning` exits 0
- `accepted_recast` present in registry and in both gate frozensets (registered in 15-04)
- expired-rule exclusion + separate accepted/recast counts asserted by tests
</verification>

<success_criteria>
RPT-04 satisfied: ACCEPTED and RECASTED tracked separately with a current-vs-prior MoM delta and
Owner cut on all four channels; expired rules flagged pending re-evaluation (Pitfall 6a); finding
counts headline / rule counts in analyst tab (6c); cold-start safe (QUAL-01); zero-exception safe
(QUAL-03); fixtures synthetic with no real UUIDs (QUAL-05).
</success_criteria>

<output>
Create `.planning/phases/15-independent-new-modules/15-06-SUMMARY.md` when done
</output>
