---
phase: 19-v1-4-closure-int-warn-1-2-3-fixes-phase-17-human-verificatio
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - utils/sla_calculator.py
  - utils/open_count.py
  - reports/modules/program_health_module.py
  - scripts/capture_trend_snapshot.py
  - tests/test_sla_rate_crit_high.py
  - tests/test_program_health_module.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "D-05: a single shared helper utils/sla_calculator.py::compute_sla_rate_crit_high() computes the Crit+High SLA rate, excluding first_found=NaT rows from BOTH numerator and denominator"
    - "D-05 / 17-REVIEW WR-01: all 3 SLA-rate sites (capture_trend_snapshot.py, program_health_module cold-start, program_health_module live tile) call the shared helper — no inline len(ch_df)-denominator copy remains"
    - "17-REVIEW WR-02: an unmapped severity (NaN sla_days) does not inflate the denominator — guarded inside the shared helper"
    - "17-REVIEW WR-03: program_health_module _signal_direction returns 'missing' (not 'red') when curr/prev is NaN, via pd.isna guard"
    - "17-REVIEW WR-04: program_health_module net-velocity sparkline uses s.get(key) with explicit None check, not s.get(key, 0)"
    - "17-REVIEW WR-05: program_health_module analyst_df drops NaN before count ops and casts count metadata to int"
    - "D-05 sequencing: the SLA-rate fix lands BEFORE the Phase 17 human check so the live snapshot the operator inspects is already correct"
  artifacts:
    - path: "utils/sla_calculator.py"
      provides: "compute_sla_rate_crit_high shared helper"
      exports: ["compute_sla_rate_crit_high"]
    - path: "tests/test_sla_rate_crit_high.py"
      provides: "Helper unit tests incl. NaT-exclusion and all-NaT cold-start"
      exports: ["test_nat_excluded_from_denominator", "test_all_nat_returns_none", "test_unmapped_severity_excluded"]
    - path: "scripts/capture_trend_snapshot.py"
      provides: "Calls shared helper for sla_rate_crit_high"
      contains: "compute_sla_rate_crit_high"
    - path: "reports/modules/program_health_module.py"
      provides: "Calls shared helper at both SLA sites; WR-03/04/05 fixes"
      contains: "compute_sla_rate_crit_high"
  key_links:
    - from: "scripts/capture_trend_snapshot.py"
      to: "utils.sla_calculator.compute_sla_rate_crit_high"
      via: "import + call replacing inline SLA-rate logic"
      pattern: "compute_sla_rate_crit_high"
    - from: "reports/modules/program_health_module.py"
      to: "utils.sla_calculator.compute_sla_rate_crit_high"
      via: "import + call at cold-start and live-tile sites"
      pattern: "compute_sla_rate_crit_high"
---

<objective>
Fix the `sla_rate_crit_high` NaT-denominator downward bias (D-05 / 17-REVIEW WR-01, WR-02) via a SINGLE shared helper in `utils/sla_calculator.py`, called by all 3 sites, and clean up the remaining `program_health_module` robustness findings (WR-03 NaN-as-real, WR-04 sparkline default-to-0, WR-05 analyst NaN/int). Per D-05 this lands BEFORE the Phase 17 human check (Plan 08) so the live snapshot value the operator inspects is already correct.

Purpose: Stop the three copy-pasted SLA-rate sites from drifting and from biasing the published rate downward; make the program_health signals NaN-safe.
Output: New `compute_sla_rate_crit_high()` helper + unit tests; 3 call sites converted; WR-03/04/05 guards.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-v1-4-closure-int-warn-1-2-3-fixes-phase-17-human-verificatio/19-PATTERNS.md
@.planning/phases/17-program-health-overview/17-REVIEW.md

<interfaces>
<!-- SLA_DAYS source of truth (CLAUDE.md: authoritative, per-install configurable — never hardcode) -->
From config.py:
  SLA_DAYS: dict[str, int]   # {"critical": 15, "high": 30, "medium": 60, "low": 120}

From utils/open_count.py:
  open_findings_at(df, date) -> pd.DataFrame   # reopened-aware; deliberately KEEPS first_found=NaT rows

<!-- Helper signature to create (per 19-PATTERNS.md L614-635) -->
utils/sla_calculator.py:
  compute_sla_rate_crit_high(open_df: pd.DataFrame, report_date: datetime, sla_days: dict) -> Optional[float]
    # returns % of open Crit+High within SLA; NaT-first_found excluded from BOTH numerator and denominator;
    # returns None when no SLA-classifiable Crit+High rows
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create compute_sla_rate_crit_high shared helper + unit tests (D-05, WR-01, WR-02)</name>
  <files>utils/sla_calculator.py, tests/test_sla_rate_crit_high.py</files>
  <read_first>
    - utils/sla_calculator.py (the file being modified — read existing module structure / imports; this is the SLA-utility home per 19-PATTERNS.md)
    - 19-PATTERNS.md L596-637 (authoritative computation + the exact helper body to mirror)
    - 17-REVIEW.md L48-94 (WR-01 statement of the bias and the recommended both-sides exclusion)
    - scripts/capture_trend_snapshot.py L390-418 (the authoritative inline block being replaced — confirm exact column names: `severity`, `first_found`)
  </read_first>
  <behavior>
    - test_nat_excluded_from_denominator: a Crit+High frame with N rows in-SLA and M rows with first_found=NaT returns round(N/(N) *100,1) when all non-NaT rows are in-SLA — i.e. the M NaT rows are NOT in the denominator (rate is 100.0, not N/(N+M)*100).
    - test_all_nat_returns_none: a Crit+High frame where every first_found is NaT returns None (no SLA-classifiable rows) — not 0.0.
    - test_empty_crit_high_returns_none: a frame with zero critical/high rows returns None.
    - test_unmapped_severity_excluded: a row whose severity maps to NaN in sla_days does not count toward numerator-without-denominator inflation (within mask guards sla_days notna).
    - test_in_sla_vs_overdue_split: a mix of within-SLA and overdue Crit+High (all with valid first_found) yields the correct percentage.
  </behavior>
  <action>
    Add `compute_sla_rate_crit_high(open_df, report_date, sla_days) -> Optional[float]` to utils/sla_calculator.py with the body from 19-PATTERNS.md L616-634: filter to critical/high (lowercased), return None when empty; build `ff_ts = pd.to_datetime(ch_df["first_found"], utc=True, errors="coerce")`; `valid_mask = ff_ts.notna()`; if no valid rows return None; compute `days_open = (pd.Timestamp(report_date, tz="UTC") - ff_ts[valid_mask]).dt.days.clip(lower=0)`; `sla_days_col = ch_valid["severity"].str.lower().map(sla_days)`; `within = days_open.notna() & sla_days_col.notna() & (days_open <= sla_days_col)`; return `round(float(within.sum()) / len(ch_valid) * 100, 1)`. Read SLA from the caller-passed `sla_days` dict (callers pass `config.SLA_DAYS`) — never hardcode day counts. Add type hints + docstring. Create tests/test_sla_rate_crit_high.py with the five behaviors using synthetic aggregate-safe frames (QUAL-05: no real hostnames/IPs).
  </action>
  <acceptance_criteria>
    - `pytest tests/test_sla_rate_crit_high.py -q -o addopts=""` exits 0 with all five tests passing.
    - `grep -v '^#' utils/sla_calculator.py | grep -c "def compute_sla_rate_crit_high"` equals 1.
    - No hardcoded SLA day integers (15/30/60/120) appear in the helper body — `grep -nE "(^|[^0-9])(15|30|60|120)([^0-9]|$)" utils/sla_calculator.py` shows no SLA-day literal inside compute_sla_rate_crit_high (the helper reads `sla_days`).
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_sla_rate_crit_high.py -q -o addopts=""</automated>
  </verify>
  <done>Helper exists, excludes NaT from both sides, returns None on cold-start, five tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Convert all 3 SLA-rate sites to the shared helper (D-05, WR-01/WR-02 single-site fix)</name>
  <files>scripts/capture_trend_snapshot.py, reports/modules/program_health_module.py</files>
  <read_first>
    - scripts/capture_trend_snapshot.py L390-418 (site 1 — the live snapshot SLA block being replaced; this is the value the Phase 17 operator inspects)
    - reports/modules/program_health_module.py L255-273 (site 2 — cold-start) and L519-536 (site 3 — live tile)
    - 17-REVIEW.md L258-270 (IN-06: the three near-copies + the call signature recommendation `_sla_rate_crit_high(open_df, snap_ts)`)
    - 19-PATTERNS.md L596-641 (all 3 sites replace inline logic with the helper call)
  </read_first>
  <action>
    Replace the inline `len(ch_df)`-denominator SLA-rate computation at each of the three sites with a call to `from utils.sla_calculator import compute_sla_rate_crit_high` and pass `(open_df, snapshot_date_or_report_date, SLA_DAYS)`. At capture_trend_snapshot.py: keep the surrounding fail-soft try/except (QUAL-03) — the helper call goes inside it; the `sla_rate_crit_high=` kwarg on the severity `capture_snapshot()` call is unchanged. At program_health_module.py both sites: pass the same `open_df` already computed at each site and the module's report date; import `SLA_DAYS` from config (already imported in this module per 17-VERIFICATION). Remove the now-dead inline `days_open`/`within`/`sla_days_col` locals at each site. Do not change any other behavior in these files in this task (WR-03/04/05 are Task 3).
  </action>
  <acceptance_criteria>
    - `grep -c "compute_sla_rate_crit_high" scripts/capture_trend_snapshot.py` >= 1 AND `grep -c "compute_sla_rate_crit_high" reports/modules/program_health_module.py` >= 2.
    - No inline `len(ch_df)` SLA-rate denominator remains: `grep -n "len(ch_df)" scripts/capture_trend_snapshot.py reports/modules/program_health_module.py` returns no SLA-rate division site (the only matches, if any, are unrelated).
    - `pytest tests/test_program_health_module.py tests/content/test_trend_store.py -q -o addopts=""` exits 0 (no regression in the 48 + 29 existing tests).
    - `python scripts/capture_trend_snapshot.py --dry-run` (or `python -c "import scripts.capture_trend_snapshot"` from repo root) imports cleanly.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_program_health_module.py tests/content/test_trend_store.py -q -o addopts=""</automated>
  </verify>
  <done>All 3 sites call the shared helper; no inline NaT-biased denominator remains; existing suites green.</done>
</task>

<task type="auto">
  <name>Task 3: program_health_module robustness — WR-03 NaN guard, WR-04 sparkline default, WR-05 analyst NaN/int</name>
  <files>reports/modules/program_health_module.py, tests/test_program_health_module.py</files>
  <read_first>
    - reports/modules/program_health_module.py L174-180 (WR-03 `_signal_direction` guard), L559-566 (WR-04 sparkline `s.get(...,0)`), L625-633 + L793-796 (WR-05 analyst NaN round + int-typed metadata miscount)
    - 17-REVIEW.md L121-202 (WR-03, WR-04, WR-05 exact fixes)
    - 19-PATTERNS.md L643-660 (WR-03/04/05 fix patterns)
  </read_first>
  <action>
    WR-03 (L174-180): broaden the `_signal_direction` guard to `if curr is None or prev is None or pd.isna(curr) or pd.isna(prev): return "missing"` (pd already imported). WR-04 (L559-566): drop the `, 0` defaults in the net-velocity sparkline builder — use `s.get("new_findings_count")` / `s.get("fixed_findings_count")` with the existing `is not None` guard producing `None` for a gap (matching Signal 2 semantics). WR-05 (L625-633, L793-796): add a `pd.isna` / `.dropna(subset=[...])` guard before the analyst-frame count ops, and cast the resulting count fields to Python `int` (not numpy int64). Prefer the allowlist-by-current-owner-set approach (or extract the metadata-key blocklist into a single module-level constant) per 17-REVIEW WR-05 to stop a future int-typed snapshot field being misread as an owner — minimal version: at least guard the count and int-cast. Add/extend targeted tests in tests/test_program_health_module.py asserting: a NaN curr/prev yields "missing" direction; a present-null net-velocity key produces a sparkline gap (None) not 0; analyst counts are int-typed and NaN rows excluded.
  </action>
  <acceptance_criteria>
    - `pytest tests/test_program_health_module.py -q -o addopts=""` exits 0 with the new WR-03/04/05 assertions present and passing.
    - `grep -n "s.get(\"new_findings_count\", 0)" reports/modules/program_health_module.py` returns nothing (the `, 0` default is gone from the net-velocity builder).
    - `_signal_direction` guard line contains `pd.isna` — `grep -n "pd.isna" reports/modules/program_health_module.py` shows it in the `_signal_direction` region.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_program_health_module.py -q -o addopts=""</automated>
  </verify>
  <done>WR-03 NaN→missing, WR-04 sparkline gap semantics, WR-05 analyst NaN/int all fixed with tests.</done>
</task>

</tasks>

<verification>
- `pytest tests/test_sla_rate_crit_high.py tests/test_program_health_module.py tests/content/test_trend_store.py -q -o addopts=""` → exits 0.
- Shared helper called from all 3 sites; no inline NaT-biased denominator remains.
</verification>

<success_criteria>
D-05 closed via a single shared helper adopted at all 3 sites (NaT excluded both sides); 17-REVIEW WR-01..05 all fixed; sequenced before the Phase 17 human check.
</success_criteria>

<output>
Create `.planning/phases/19-v1-4-closure-int-warn-1-2-3-fixes-phase-17-human-verificatio/19-02-SUMMARY.md` when done
</output>
