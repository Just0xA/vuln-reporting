---
phase: 19-v1-4-closure-int-warn-1-2-3-fixes-phase-17-human-verificatio
plan: 04
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - data/trend_store.py
  - scripts/backfill_trend_reconstruction.py
  - tests/test_backfill_reconstruction.py
  - tests/content/test_trend_store.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "CR-B1: backfill add-back rows fixed after the boundary are counted open for the boundary predicate only (no historical undercount); original fixed metadata preserved"
    - "CR-B2: backfill cached branch applies the same open/reopened status filter the live fetch applies (cached vs live runs agree)"
    - "CR-B3: backfill reconstruction-window month attribution uses a consistent clock matching capture_snapshot's local-month key convention"
    - "CR-B4: --window-start is validated up front via an argparse _month_arg YYYY-MM type validator"
    - "CR-B6: trend_store fixed_findings_count is explicit 0 (not None) when fixed_vulns_df is present-but-empty"
    - "CR-B7 + 18-REVIEW WR-07: _load_trend_json validates root-is-dict and snapshots-is-list-of-dicts; genuine parse/validation failure renames *.corrupt and returns []"
    - "18-REVIEW WR-05: read_trend flags the current-local-month newest entry as partial (MTD) using the same local-time month-key convention as capture_snapshot, so MoM delta math can exclude the partial month-to-date point instead of treating it as a completed latest point"
    - "18-REVIEW WR-06: capture_snapshot month key derived from local time consistently so month label, open_findings_at instant, and new/fixed buckets agree"
    - "CR-T3 (pairs CR-B1): add-back reconstruction test asserts exact/zero-tolerance row counts"
    - "CR-T4: backfill partial-flag test asserts the expected taper/non-taper month set exists before checking the flag"
  artifacts:
    - path: "data/trend_store.py"
      provides: "CR-B6 explicit-zero + CR-B7/WR-07 validated load + WR-06 local-month key + WR-05 partial-month flag"
      contains: "isinstance"
    - path: "scripts/backfill_trend_reconstruction.py"
      provides: "CR-B1 add-back, CR-B2 cached filter, CR-B3 clock, CR-B4 arg validator"
      contains: "_month_arg"
    - path: "tests/test_backfill_reconstruction.py"
      provides: "CR-T3 exact add-back + CR-T4 taper-set assertions"
  key_links:
    - from: "tests/test_backfill_reconstruction.py"
      to: "scripts/backfill_trend_reconstruction.py"
      via: "exact add-back count assertion proving CR-B1"
      pattern: "open_findings_at"
    - from: "data/trend_store.py::_load_trend_json"
      to: "json.load + isinstance validation"
      via: "root-dict + snapshots-list-of-dicts shape check before return"
      pattern: "isinstance"
    - from: "data/trend_store.py::read_trend"
      to: "data/trend_store.py::capture_snapshot month_str convention"
      via: "WR-05: current-local-month detection reuses capture_snapshot's local strftime('%Y-%m') so the partial-month flag agrees with the write key"
      pattern: "partial"
---

<objective>
Close the Work-stream-B trend/backfill correctness findings plus the paired tests. CR-B1 (add-back drop) lands together with CR-T3 (the exact-tolerance test that proves it); CR-B7 lands together with 18-REVIEW WR-07 (same `_load_trend_json` block). Also closes CR-B2/B3/B4/B6, WR-05 (MTD/partial-month delta), and WR-06 (local-vs-UTC month attribution).

Purpose: Stop historical reconstruction undercounting, stop cached-vs-live divergence, make empty≠missing explicit, harden corrupt-file handling without data loss, and stop MoM deltas from treating the partial current month as a completed point.
Output: Hardened backfill script + trend_store load/count/read logic + exact-tolerance reconstruction tests.

Note: `data/fetchers.py` CR-B5 (lookback_days cache key) is intentionally NOT in this plan — it is owned by Plan 06 to keep fetcher edits off the trend-store ownership boundary.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-v1-4-closure-int-warn-1-2-3-fixes-phase-17-human-verificatio/19-PATTERNS.md
@.planning/phases/19-v1-4-closure-int-warn-1-2-3-fixes-phase-17-human-verificatio/19-CODERABBIT-TRIAGE.md
@.planning/phases/18-management-summary-migration-docs/18-REVIEW.md

<interfaces>
From utils/open_count.py:
  open_findings_at(df, date) -> pd.DataFrame   # treats state=fixed as terminal; reopened-aware

<!-- CLAUDE.md timezone policy: report timestamps UTC; cache-folder names + month keys = server local -->
From data/trend_store.py:
  capture_snapshot(...)   # month_str = date.strftime("%Y-%m") at ~L390 (local-month key)
  _load_trend_json(path) -> list   # ~L153-193; returns data.get("snapshots", []) unchecked today
  read_trend(dimension, tag_filter, months, trend_dir) -> dict   # ~L499+; newest entry can be the current-month MTD/partial snapshot (WR-05)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CR-B1 add-back fix + CR-T3 exact-tolerance test (paired)</name>
  <files>scripts/backfill_trend_reconstruction.py, tests/test_backfill_reconstruction.py</files>
  <read_first>
    - scripts/backfill_trend_reconstruction.py L133-149 (the add-back / open_findings_at boundary call)
    - tests/test_backfill_reconstruction.py L247-264 (the too-loose add-back tolerance to tighten)
    - 19-PATTERNS.md L224-234 (CR-B1 fix: copy df, flip add-back rows to "open" for the boundary predicate only) and L440-448 (CR-T3 exact-match)
    - .planning/codebase/CONVENTIONS.md CoW rule (df.copy() before mutate; never mutate original)
  </read_first>
  <behavior>
    - Test (tighten existing): an add-back row fixed AFTER the boundary is counted as open at the boundary — the reconstructed open count at the boundary equals the EXACT expected value with zero tolerance (a dropped add-back row fails the test).
    - Test: original fixed metadata on the add-back rows is preserved (the flip-to-open is local to the boundary check, not persisted).
  </behavior>
  <action>
    CR-B1 (L133-149): for the boundary check only, build `_df_boundary = df.copy()` and set `_df_boundary.loc[add_back_mask, "state"] = "open"` before calling `open_findings_at(_df_boundary, boundary_date)`. Do not mutate the original `df` (pandas CoW). Use the existing add-back mask logic that identifies rows fixed after the boundary.
    CR-T3 (tests/test_backfill_reconstruction.py L247-264): replace the loose tolerance with an exact-equality assertion (`assert result_count == expected_count`, message citing CR-B1) so the test fails if rows 3/4 are dropped.
  </action>
  <acceptance_criteria>
    - `pytest tests/test_backfill_reconstruction.py -q -o addopts=""` exits 0 with the tightened exact-match assertion.
    - `grep -n "\.copy()" scripts/backfill_trend_reconstruction.py` shows the boundary-frame copy near the add-back/open_findings_at call (no in-place mutation of the source df).
    - The add-back tolerance assertion uses `==` (exact), not an `abs(... ) <=` tolerance band — confirm in tests/test_backfill_reconstruction.py around L247-264.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_backfill_reconstruction.py -q -o addopts=""</automated>
  </verify>
  <done>Add-back rows counted at the boundary; exact-tolerance test proves it; original metadata preserved.</done>
</task>

<task type="auto">
  <name>Task 2: CR-B2 cached filter + CR-B3 clock + CR-B4 arg validator + CR-T4 taper test</name>
  <files>scripts/backfill_trend_reconstruction.py, tests/test_backfill_reconstruction.py</files>
  <read_first>
    - scripts/backfill_trend_reconstruction.py L503-511 (CR-B2 cached branch missing open/reopened filter), L216-218 (CR-B3 month attribution clock), L477-482 (CR-B4 unvalidated --window-start)
    - tests/test_backfill_reconstruction.py L565-588 (CR-T4 conditional partial-flag checks)
    - 19-PATTERNS.md L236-266 (CR-B2/B3/B4 fix patterns) and L450-458 (CR-T4 assert taper set first)
  </read_first>
  <action>
    CR-B2 (L503-511): after loading from the parquet cache, apply the same `state.str.lower().isin({"open","reopened"})` filter the live `fetch_all_vulnerabilities()` applies, returning the filtered+reset frame (pattern in 19-PATTERNS.md L242-245).
    CR-B3 (L216-218): make the reconstruction-window month-attribution clock consistent with `capture_snapshot`'s local-month key convention (CLAUDE.md: month keys = server local). Use `datetime.now()` (local-naive) consistently for the window boundary so rollover attribution agrees with the snapshot store; confirm whether the site computes a window boundary vs a snapshot month key and apply the matching convention per 19-PATTERNS.md L247-251.
    CR-B4 (L477-482): add a `_month_arg(value)` argparse type validator that `re.fullmatch(r"\d{4}-\d{2}", value)` and raises `argparse.ArgumentTypeError` on malformed input; wire `type=_month_arg` onto the `--window-start` argument.
    CR-T4 (tests/test_backfill_reconstruction.py L565-588): assert the expected taper/non-taper month set exists (`set(produced_months) >= expected_months`) BEFORE checking the partial flag, so the test cannot pass when taper months were never produced.
  </action>
  <acceptance_criteria>
    - `grep -c "_month_arg" scripts/backfill_trend_reconstruction.py` >= 2 (def + `type=` wiring).
    - `python scripts/backfill_trend_reconstruction.py --window-start not-a-month --help` (or a unit invocation of `_month_arg("not-a-month")`) raises/exits on the malformed value (argparse rejects up front).
    - CR-B2 cached branch contains an `isin({"open"` / `"reopened"` status filter — `grep -n "reopened" scripts/backfill_trend_reconstruction.py` shows it on the cached path.
    - `pytest tests/test_backfill_reconstruction.py -q -o addopts=""` exits 0 with the CR-T4 set-membership assertion present.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_backfill_reconstruction.py -q -o addopts=""</automated>
  </verify>
  <done>Cached path filters to open/reopened; clock consistent; --window-start validated; taper-set asserted first.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: CR-B6 explicit-zero + CR-B7/WR-07 validated load + WR-06 local-month key + WR-05 partial-month delta</name>
  <files>data/trend_store.py, tests/content/test_trend_store.py</files>
  <read_first>
    - data/trend_store.py L153-193 (_load_trend_json corrupt-file block — CR-B7 + WR-07), L427-433 (fixed_findings_count guard — CR-B6), L389-390 + L422-433 (month-key + new/fixed buckets — WR-06)
    - data/trend_store.py L483-496 (the (month, tag_filter) idempotent-overwrite block — WR-05 capture-after-read ordering site) and L499+ (`read_trend` slice — WR-05 partial-month surfacing site)
    - 18-REVIEW.md L206-270 (WR-05 + WR-06 + WR-07 statements). WR-05 (L206-228): `read_trend(..., months=13)` surfaces the just-written current-month MTD/partial snapshot as the newest "completed" point, so MoM deltas compare a partial current month against a complete prior month.
    - 19-PATTERNS.md L285-333 (CR-B6 + CR-B7/WR-07 fix patterns), L545-556 (WR-05 month-key convention + WR-06 local-date conversion)
    - NOTE: data/trend_store.py was also edited by Plan 01 (CR-S2 filename). This plan owns it in Wave 2; do not re-touch the CR-S2 filename site.
  </read_first>
  <behavior>
    - Test CR-B6: capture_snapshot with a present-but-empty fixed_vulns_df writes fixed_findings_count == 0 (explicit), not None.
    - Test CR-B7: _load_trend_json on a JSON file whose root is a bare list (non-dict) renames it to *.corrupt and returns []; on a dict with a non-list "snapshots" it does the same; on a dict with non-dict snapshot entries it does the same.
    - Test WR-06: the month key written by capture_snapshot for a UTC-aware date near a month boundary on a non-UTC-equivalent input is the local-month key (consistent with the new/fixed bucket attribution).
    - Test WR-05: a snapshot whose `month` equals the current local month is flagged partial (`is_partial=True` / `partial=True`) by `read_trend`, and the local-month-key convention `read_trend` uses to detect "current month" matches `capture_snapshot`'s `month_str` derivation — so a MoM consumer can exclude the partial newest point from `latest − prior` delta math.
  </behavior>
  <action>
    CR-B6 (L427-433): when `fixed_vulns_df is not None` and `fixed_vulns_df.empty`, set `fixed_findings_count = 0` explicitly (empty ≠ missing). Keep the existing non-empty derivation in the else branch.
    CR-B7 + WR-07 (L153-193): extend the load/parse block — after `json.load`, validate `isinstance(data, dict)` (else raise ValueError), validate `snapshots = data.get("snapshots", [])` `isinstance(..., list)` (else raise), and validate each entry `isinstance(s, dict)` (else raise listing the bad indices). On `(json.JSONDecodeError, ValueError, AttributeError)`, log the failure, rename the file to `*.corrupt` (best-effort, log if rename fails), and return `[]`. Mirror 19-PATTERNS.md L308-332 exactly.
    WR-06 (L389-390): derive `month_str` from a single clock so the month label, the `open_findings_at` evaluation instant, and the new/fixed buckets agree. Per CLAUDE.md (month keys local) convert the incoming `date` to local before strftime (`date.astimezone().strftime("%Y-%m")` if `date` is tz-aware) so a UTC-aware `generated_at` does not misattribute the boundary; ensure the new/fixed bucket comparison uses the same local convention (15-REVIEW WR-01 already strips tz on ff/lf — keep consistent).
    WR-05 (data/trend_store.py L499+ `read_trend`, paired with the L483-496 capture-after-read overwrite): the MTD/partial-month delta defect is that the current-month snapshot `capture_snapshot` writes (keyed to the current local month at L483-496) is the newest entry `read_trend` returns, so MoM consumers treat a partial month-to-date count as a completed `latest` point. Fix in `read_trend`: flag the newest entry as partial when its `month` equals the current local month, using the SAME local-month-key convention `capture_snapshot` now uses post-WR-06 (`datetime.now().strftime("%Y-%m")` server-local, per CLAUDE.md) — emit a `partial`/`is_partial` boolean on the returned newest point (and/or expose the current-month key) so MoM consumers can exclude it from `latest − prior`. Also add a one-line docstring note on `capture_snapshot`/`read_trend` documenting the capture-after-read ordering contract (a future reorder must not silently turn the partial point into a "prior completed" month). Keep this confined to `data/trend_store.py` — do NOT edit `reports/management_summary.py` (its read/capture call ordering is unchanged; only the trend-store layer flags the partial point).
    Add the four behavior tests to tests/content/test_trend_store.py.
  </action>
  <acceptance_criteria>
    - `pytest tests/content/test_trend_store.py -q -o addopts=""` exits 0 with the four new tests passing.
    - `grep -c "isinstance" data/trend_store.py` increases (root-dict + list + dict-entry checks present in _load_trend_json).
    - CR-B6: a unit test asserts `fixed_findings_count == 0` for present-but-empty fixed_vulns_df.
    - WR-05: `read_trend` flags the current-local-month newest entry as partial; `grep -n "partial" data/trend_store.py` shows the flag, and the current-month detection uses the same local `strftime("%Y-%m")` convention as `capture_snapshot`.
    - `python -c "import data.trend_store"` (repo root) imports cleanly.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/content/test_trend_store.py -q -o addopts=""</automated>
  </verify>
  <done>empty→0 explicit; _load_trend_json validates shape and fails-safe with rename; month key local-consistent; WR-05 partial current-month point flagged via the shared local-month convention; four tests pass.</done>
</task>

</tasks>

<verification>
- `pytest tests/test_backfill_reconstruction.py tests/content/test_trend_store.py -q -o addopts=""` → exits 0.
- `python -c "import data.trend_store, scripts.backfill_trend_reconstruction"` (repo root) imports cleanly.
</verification>

<success_criteria>
CR-B1/B2/B3/B4/B6/B7 + WR-05/WR-06/WR-07 + CR-T3/T4 all closed; reconstruction no longer undercounts; corrupt-file handling is data-loss-safe; the partial current-month point is flagged so MoM deltas don't treat it as completed; paired tests prove the fixes.
</success_criteria>

<output>
Create `.planning/phases/19-v1-4-closure-int-warn-1-2-3-fixes-phase-17-human-verificatio/19-04-SUMMARY.md` when done
</output>
</content>
</invoke>
