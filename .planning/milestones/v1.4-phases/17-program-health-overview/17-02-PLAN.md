---
phase: 17-program-health-overview
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - reports/modules/program_health_module.py
  - reports/composed_report.py
  - tests/test_program_health_module.py
autonomous: true
requirements: [RPT-07]
tags: [program-health, module, rag, cold-start, sla, owner]

must_haves:
  truths:
    - "RPT-07: program_health is an auto-discovered four-channel module (@register_module, MODULE_ID program_health) that re-derives 4 velocity signals from the shared S1 substrate — Open-Critical MoM, Net velocity, SLA posture, MTTR (D-17-01)"
    - "D-17-02: definition parity is a correctness bar — net-velocity and MTTR signals are derived by reading the canonical snapshot fields (new_findings_count/fixed_findings_count, mttr_overall_days) already computed with the D-15-01/02 and D-16-02 definitions, so the one-pager never disagrees with new_vs_remediated / mttr_trend"
    - "OD-5/D-17-05: composite RAG = Green when all 4 signals green, Amber when 2–3 green, Red when 0–1 green; thresholds (green_count_min, amber_count_min) overridable via module_options (D-15-07 pattern)"
    - "D-17-06: any missing signal caps the composite at Amber 'data incomplete' (never Green) and the missing signal is named; available signals still count toward the green tally — the cap is structural and cannot be bypassed via module_options"
    - "D-17-07: each signal is colored by MoM direction (improved=green, flat=amber, worsened=red) with per-signal flat bands (open_crit_flat_abs, sla_rate_flat_pct, mttr_flat_days) overridable via module_options"
    - "D-17-08/QUAL-01: with <2 snapshots all 4 signals are missing → composite Amber 'Trend Being Established' (status yellow, NOT no_data) with current-value tiles, never NaN% or a crash"
    - "D-17-03/QUAL-02: SLA posture in compute reads snap.get('sla_rate_crit_high'); the current-value SLA tile is computed reopened-aware (open_findings_at) over Critical+High using config.SLA_DAYS"
    - "D-17-09: Owner velocity is computed from extract_owner + open_findings_at on Critical+High with a >20% MoM-rise outlier flag (owner_outlier_pct, default 20.0), reading the owner-dimension snapshot via read_trend('owner', ...) and degrading gracefully when owner trend is insufficient"
    - "D-17-01: program_health is added to _MODULES_NEEDING_TREND_SNAPSHOTS in composed_report.py with no run_report() signature change and no _MODULES_NEEDING_FIXED_VULNS membership"
    - "QUAL-03: a zero-row vulns_df returns _empty_result() (no_data strip) without raising"
  artifacts:
    - path: "reports/modules/program_health_module.py"
      provides: "ProgramHealthModule.compute() + _composite_rag_od5/_signal_direction/_build_cold_start_result helpers + module_options validate_config"
      contains: "@register_module"
    - path: "reports/composed_report.py"
      provides: "program_health added to _MODULES_NEEDING_TREND_SNAPSHOTS frozenset"
      contains: "program_health"
    - path: "tests/test_program_health_module.py"
      provides: "compute-layer tests: cold-start 0/1, composite all-green, missing-caps-amber, missing-signal-named, sla reopened-aware, empty-data guard, owner outlier"
      contains: "program_health"
  key_links:
    - from: "reports/composed_report.py"
      to: "ProgramHealthModule.compute"
      via: "_MODULES_NEEDING_TREND_SNAPSHOTS → **composer_kwargs trend_snapshots fan-out"
      pattern: "program_health"
    - from: "reports/modules/program_health_module.py"
      to: "data.trend_store.read_trend + utils.open_count.open_findings_at"
      via: "owner-dimension snapshot read + reopened-aware Owner/SLA population"
      pattern: "read_trend|open_findings_at"
    - from: "reports/modules/program_health_module.py"
      to: "snapshot sla_rate_crit_high field (Plan 17-01)"
      via: "snap.get('sla_rate_crit_high') signal re-derivation"
      pattern: "sla_rate_crit_high"
---

<objective>
Build the `program_health` module's compute layer: signal re-derivation from the
S1 substrate, the OD-5 composite-RAG rule with missing-signal folding, per-signal
MoM-direction coloring, the cold-start Amber path, the Owner velocity table, and the
one-line `composed_report.py` kwargs-gate registration. Plus the compute-layer unit
tests.

This plan delivers compute + ModuleData ONLY. The four render channels (PDF
sparklines, Excel tabs, email panel, analyst tabs) are Plan 17-03 — this plan emits
the no-op-default renderers inherited from BaseModule plus a populated ModuleData
(metrics, table_data, analyst_rows, rag_strip, driver_narrative) that Plan 03 renders.

Purpose: RPT-07 compute correctness — the composite RAG and 4 signals must be right
before any pixels are drawn (OD-5 must be locked before render code, per phase brief).
Output: `reports/modules/program_health_module.py` (compute + helpers + validate_config),
`reports/composed_report.py` (frozenset add), `tests/test_program_health_module.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-program-health-overview/17-CONTEXT.md
@.planning/phases/17-program-health-overview/17-RESEARCH.md
@.planning/phases/17-program-health-overview/17-PATTERNS.md
@.planning/phases/17-program-health-overview/17-UI-SPEC.md
@.planning/phases/17-program-health-overview/17-01-SUMMARY.md

<interfaces>
<!-- The exact analogs to copy. PATTERNS.md cites every line range; read those files. -->

Primary analog — reports/modules/mttr_trend_module.py:
  imports 69–93; @register_module + class decl 212–244; compute() + cold-start 280–344;
  snapshot dedup 646–651 (copy VERBATIM); _mom_direction 194–205; _owner_mom_delta 179–191;
  _build_cold_start_result 250–274 (returns status="no_data" at line 271 — Phase 17 changes
  this ONE thing to status="yellow"); module_options read 359–360; validate_config 1388–1413;
  get_audit_info 1419–1462; fail-soft try/except → _empty_result 862–867.

Secondary analog — reports/modules/new_vs_remediated_module.py:
  cold-start path 208–262; net-velocity definition (D-15-01/02); Owner cut via
  open_findings_at + extract_owner + .map(uuid_to_owner).fillna("Unassigned") 346–360, 461–471.

Snapshot field locations (data/trend_store.py):
  per-severity open counts at top level (critical/high/medium/low) ~336–341;
  new_findings_count / fixed_findings_count / mttr_overall_days ~384–388;
  sla_rate_crit_high — NEW from Plan 17-01.

Signal re-derivation (read with .get(), NEVER []):
  curr, prev = snapshots_deduped[-1], snapshots_deduped[-2]
  Signal 1 Open-Critical: curr.get("critical") vs prev.get("critical")   (higher_is_better=False)
  Signal 2 Net velocity:  curr.get("new_findings_count") - curr.get("fixed_findings_count")
                          MoM = compare curr net_delta vs prev net_delta  (higher_is_better=False; lower net_delta = improving)
  Signal 3 SLA posture:   curr.get("sla_rate_crit_high") vs prev.get(...)  (higher_is_better=True)
  Signal 4 MTTR overall:  curr.get("mttr_overall_days") vs prev.get(...)  (higher_is_better=False)

OD-5 + per-signal pure functions (NEW — design from RESEARCH §3 lines 637–682):
  _composite_rag_od5(signal_statuses, green_min=4, amber_min=2) -> (status, data_incomplete)
  _signal_direction(curr, prev, higher_is_better, flat_band=0.0) -> "green"|"amber"|"red"|"missing"

module_options defaults (UI-SPEC §Module Options):
  green_count_min=4, amber_count_min=2, open_crit_flat_abs=5, sla_rate_flat_pct=2.0,
  mttr_flat_days=1.0, owner_outlier_pct=20.0   (net velocity has NO flat band — binary direction)

Owner velocity (read owner-dimension snapshot INSIDE compute — Option A, no second gate):
  from data.trend_store import read_trend, _sanitise_tag_for_filename
  tag_filter = _sanitise_tag_for_filename(config.options.get("tag_category"), config.options.get("tag_value"))
  owner_trend = read_trend(dimension="owner", tag_filter=tag_filter, months=13)
  owner_insufficient = owner_trend.get("insufficient_data", True)   # degrade: current-only, suppress MoM Delta

reports/composed_report.py — _MODULES_NEEDING_TREND_SNAPSHOTS frozenset at line 89
(members: sc4_kwargs_stub, new_vs_remediated, vuln_density, accepted_recast, mttr_trend).
need_trend intersection check at line 219. program_health is NOT in _MODULES_NEEDING_FIXED_VULNS.

tests/test_mttr_trend_module.py — fixture template lines 1–95:
  pd.options.mode.copy_on_write = True (line 42); _UUID_PREFIX "00000000-0000-0000-0000-00000000000"
  (line 51); REF datetime 2026-06-12 UTC (line 53); _uuid(n) (line 61); _make_assets tags="Owner=Engineering".
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: program_health_module.py — class shell, signal re-derivation, OD-5 composite, cold-start, Owner velocity (compute only)</name>
  <files>reports/modules/program_health_module.py</files>
  <read_first>
    - reports/modules/mttr_trend_module.py (imports 69–93; class 212–244; compute+cold-start 280–344; dedup 646–651; _mom_direction 194–205; _build_cold_start_result 250–274; module_options read 359–360; fail-soft 862–867)
    - reports/modules/new_vs_remediated_module.py (cold-start 208–262; net-velocity definition; Owner cut 346–360, 461–471)
    - reports/modules/base.py (BaseModule four-channel contract, _empty_result, ModuleData, ModuleConfig)
    - reports/modules/rag_utils.py (build_rag_strip_entry, STATUS_COLOR, STATUS_LABEL, NO_DATA_HEADLINE, NO_DATA_DRIVER)
    - reports/modules/format_utils.py (safe_pct, safe_int, safe_format)
    - reports/modules/board_report_utils.py (extract_owner)
    - utils/open_count.py (open_findings_at)
    - config.py (SLA_DAYS)
    - .planning/phases/17-program-health-overview/17-RESEARCH.md (§3 Code Examples lines 637–710; Open Questions for net-velocity direction + cold-start MTTR tile = "—")
    - .planning/phases/17-program-health-overview/17-UI-SPEC.md (§Module Options; RAG Strip table; driver_narrative templates)
  </read_first>
  <behavior>
    - cold_start (trend_snapshots None or insufficient_data True) → _build_cold_start_result: composite_rag "amber", data_incomplete True, rag_strip status "yellow" headline "Trend Being Established"; metrics carry current-value Open-Crit/SLA tiles from live vulns_df; MTTR current tile is None ("—", no fixed_vulns_df)
    - normal path (≥2 deduped snapshots): 4 signal statuses derived via _signal_direction; composite via _composite_rag_od5
    - 4 green signals → composite "green"; 2–3 green → "amber"; 0–1 green → "red"
    - 3 green + 1 missing → composite "amber", data_incomplete True, missing signal name present in metrics/driver_narrative
    - zero-row vulns_df → _empty_result (no_data strip), no raise
    - Owner velocity: Crit+High open counts per owner (current) + MoM delta from owner snapshot; >owner_outlier_pct rise → outlier flag; owner-trend insufficient → current-only, MoM Delta suppressed, no crash
  </behavior>
  <action>
    Create `reports/modules/program_health_module.py`. Copy the imports block from
    mttr_trend_module.py 69–93 and ADD `import base64, io, matplotlib;
    matplotlib.use("Agg"); import matplotlib.pyplot as plt` (used by Plan 03 renderers;
    declare now). Declare `@register_module class ProgramHealthModule(BaseModule)` with
    MODULE_ID="program_health", DISPLAY_NAME="Program Health Overview",
    REQUIRED_DATA=["vulns","assets","trend_snapshots"], SUPPORTED_OUTPUTS=["pdf","excel","email"],
    VERSION="1.0.0".

    Add module-level pure helpers `_composite_rag_od5(signal_statuses, green_min=4, amber_min=2)
    -> tuple[str,bool]` (green_count = count of "green"; raw = green if >=green_min else amber
    if >=amber_min else red; has_missing = any =="missing"; if has_missing and raw=="green"
    return ("amber", True) — the D-17-06 cap is structural; return (raw, has_missing)) and
    `_signal_direction(curr, prev, higher_is_better, flat_band=0.0) -> str` (None on either →
    "missing"; delta=curr-prev; abs(delta)<=flat_band → "amber"; improved = delta>0 if
    higher_is_better else delta<0; "green" if improved else "red").

    Implement `compute(self, vulns_df, assets_df, report_date, config, **kwargs) -> ModuleData`:
    wrap the whole body in `try/except Exception → return self._empty_result(str(exc), config)`
    (QUAL-03 fail-soft). Zero-row guard: if vulns_df empty → self._empty_result(...). Read
    `trend_snapshots = kwargs.get("trend_snapshots")`; cold_start branch on
    `trend_snapshots is None or trend_snapshots.get("insufficient_data", True)` →
    `return self._build_cold_start_result(vulns_df, report_date, config)`. Otherwise dedup
    snapshots VERBATIM from mttr 646–651 → snapshots_deduped; if len<2 also cold-start.

    Derive the 4 signals from snapshots_deduped[-1]/[-2] using `.get()` (NEVER []), per the
    <interfaces> mapping, each with its module_options flat band read via
    `config.options.get(...)` (open_crit_flat_abs default 5, sla_rate_flat_pct 2.0,
    mttr_flat_days 1.0; net velocity flat_band 0.0 — binary). Net-velocity MoM direction =
    "delta of deltas": curr_net_delta < prev_net_delta is improving (document this choice in
    the docstring per RESEARCH Open Question 1). Build signal_statuses list and call
    `_composite_rag_od5(statuses, green_min=int(config.options.get("green_count_min",4)),
    amber_min=int(config.options.get("amber_count_min",2)))`.

    Owner velocity (D-17-09): `open_df = open_findings_at(vulns_df, report_date)`; Crit+High
    filter; `extract_owner(assets_df)` → uuid_to_owner; `open_df["asset_uuid"].map(uuid_to_owner)
    .fillna("Unassigned")` (assign to LOCAL series, not a column — CoW). Read owner-dimension
    snapshot via `read_trend(dimension="owner", tag_filter=_sanitise_tag_for_filename(...),
    months=13)` (Option A — direct read inside compute, no composed_report gate). For each owner
    compute current Crit+High open count + prev-month count + MoM delta + outlier flag
    (`(curr-prev)/prev*100 > owner_outlier_pct` if prev>0 else curr>0). If
    owner_trend.get("insufficient_data", True): current-only rows, MoM delta None, set a
    metrics flag the renderer uses to suppress the MoM Delta column (Pitfall 5 graceful degrade).

    Populate ModuleData: metrics (composite_rag, data_incomplete, the 4 signal current values +
    statuses + arrows, missing_signal_names list, cold_start False, sparkline series per signal
    for Plan 03), table_data (owner rows), analyst_rows (aggregate-only Owner per-metric MoM —
    QUAL-05, no asset UUIDs/IPs/plugins), driver_narrative (UI-SPEC templates; append missing-
    signal note when data_incomplete), summary_text, and rag_strip via
    `build_rag_strip_entry(self.DISPLAY_NAME, headline_value_str, status)` where status maps
    composite_rag → "green"/"yellow"/"red" (use "yellow" for amber — STATUS_COLOR["yellow"]).

    `_build_cold_start_result(self, vulns_df, report_date, config)`: copy mttr 250–274 but set
    rag_strip status="yellow", headline_value_str="Trend Being Established" (D-17-08 — the ONE
    divergence from the no_data analog). Compute current-value Open-Crit count and current SLA
    posture from live vulns_df (reopened-aware open_findings_at + Crit+High + SLA_DAYS vectorized
    test, same math as Plan 17-01 Task 2) so the cold-start tiles show real current values; MTTR
    current tile = None (no fixed_vulns_df — RESEARCH Open Question 2). metrics["cold_start"]=True,
    composite_rag="amber", data_incomplete=True. Use safe_pct/safe_int/safe_format for every
    possibly-None interpolation (QUAL-03 / Pitfall 8) — never f-string a None.

    Do NOT implement the four render channels here — inherit BaseModule no-op defaults; Plan 03
    overrides them. Add `validate_config()` and `get_audit_info()` (Task 2 covers their content
    but stub them now if Task ordering requires — or implement in this task; either is fine since
    same file). Add NO entry to run_all.py / _VALID_REPORTS / schema (it is a module, not a slug).
  </action>
  <verify>
    <automated>python -c "from reports.modules.registry import discover, get_module; discover(); m=get_module('program_health'); print('REGISTERED', m.MODULE_ID); from reports.modules.program_health_module import _composite_rag_od5, _signal_direction; assert _composite_rag_od5(['green']*4)==('green',False); assert _composite_rag_od5(['green','green','green','missing'])==('amber',True); assert _composite_rag_od5(['green','red','red','red'])==('red',False); assert _signal_direction(40,50,False,0)=='green'; assert _signal_direction(None,50,False,0)=='missing'; print('PURE OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `program_health_module.py` contains `@register_module` and `MODULE_ID = "program_health"`; registry.discover() finds it
    - `_composite_rag_od5(["green","green","green","missing"])` returns `("amber", True)` — missing caps green to amber (D-17-06)
    - `_composite_rag_od5(["green"]*4)` returns `("green", False)` and `["green","red","red","red"]` returns `("red", False)` (D-17-05 green-count rule)
    - `_signal_direction(None, 50, False, 0)` returns `"missing"`; an improved value returns `"green"` (D-17-07)
    - compute() returns a ModuleData (not a raise) on an empty vulns_df — `assert isinstance(result, ModuleData) and result.rag_strip` (QUAL-03)
    - Cold-start result rag_strip status is "yellow" (not "no_data") with headline "Trend Being Established" (D-17-08)
    - No new entry added to run_all.py (grep run_all.py for "program_health" returns nothing)
  </acceptance_criteria>
  <done>ProgramHealthModule is auto-registered; the OD-5 composite (with missing-cap), per-signal MoM coloring, cold-start Amber path, and Owner velocity are computed correctly into ModuleData; empty input is fail-soft.</done>
</task>

<task type="auto">
  <name>Task 2: Register program_health in composed_report trend-snapshots gate + module_options validation</name>
  <files>reports/composed_report.py, reports/modules/program_health_module.py</files>
  <read_first>
    - reports/composed_report.py (_MODULES_NEEDING_TREND_SNAPSHOTS frozenset L89; need_trend intersection L219; _MODULES_NEEDING_FIXED_VULNS L73–76)
    - reports/modules/mttr_trend_module.py (validate_config 1388–1413; get_audit_info 1419–1462)
    - reports/modules/program_health_module.py (the file from Task 1)
  </read_first>
  <action>
    In `reports/composed_report.py`, add `"program_health"` to the
    `_MODULES_NEEDING_TREND_SNAPSHOTS` frozenset (after "mttr_trend"), with a
    `# Phase 17 (D-17-01)` comment. This is the ONLY composed_report.py edit — the
    `**composer_kwargs` fan-out at line 219+ already delivers `trend_snapshots`
    (severity dimension). Do NOT add program_health to `_MODULES_NEEDING_FIXED_VULNS`
    (no fixed_vulns_df input — cold-start MTTR tile shows "—" per RESEARCH Open Q2).
    The owner-dimension snapshot is read inside compute() (Option A), so no second gate.

    In `program_health_module.py`, implement (or finalize) `validate_config()` cloned
    from mttr_trend_module 1388–1413: coerce each module_options numeric
    (green_count_min, amber_count_min → int; open_crit_flat_abs → int;
    sla_rate_flat_pct, mttr_flat_days, owner_outlier_pct → float) and on a bad value
    log a WARNING and fall back to the default (Security V5 / threshold-injection
    mitigation T-17-04) and append to an errors list. Implement `get_audit_info()`
    cloned from mttr 1419–1462 documenting each signal's definition, the OD-5 green-
    count rule, and the D-17-06 missing-signal Amber cap.
  </action>
  <verify>
    <automated>python -c "from reports.composed_report import _MODULES_NEEDING_TREND_SNAPSHOTS as S, _MODULES_NEEDING_FIXED_VULNS as F; assert 'program_health' in S; assert 'program_health' not in F; print('GATE OK')" && python run_all.py --dry-run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `program_health` is a member of `_MODULES_NEEDING_TREND_SNAPSHOTS` and NOT of `_MODULES_NEEDING_FIXED_VULNS`
    - `run_all.py --dry-run` exits 0 with no regression to existing composed-report groups
    - validate_config() coerces all 6 module_options keys and logs WARNING + falls back to default on a non-numeric value (the threshold-injection cap cannot be bypassed)
    - get_audit_info() returns a non-empty dict naming the OD-5 rule and the missing-signal Amber cap
  </acceptance_criteria>
  <done>program_health receives the severity trend_snapshots kwarg via the gate with no signature change; module_options are validated with safe fallbacks; existing groups pass --dry-run.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Compute-layer unit tests (cold-start, composite, missing-cap, SLA reopened-aware, empty-data, owner outlier)</name>
  <files>tests/test_program_health_module.py</files>
  <read_first>
    - tests/test_mttr_trend_module.py (fixture scaffold 1–95: copy_on_write True, _UUID_PREFIX, REF, _uuid, _make_assets)
    - tests/test_new_vs_remediated_module.py (synthetic snapshot-list construction + cold-start test shape)
    - reports/modules/program_health_module.py (the module under test)
    - .planning/phases/17-program-health-overview/17-RESEARCH.md (Validation Architecture test map lines 772–784)
  </read_first>
  <behavior>
    - test_cold_start_no_snapshots: trend_snapshots None → composite amber, no crash
    - test_cold_start_one_snapshot: 1 snapshot (insufficient_data True) → composite amber, current-value Open-Crit/SLA tiles present
    - test_composite_all_green: 2 snapshots crafted so all 4 signals improved → composite green
    - test_composite_missing_caps_amber: 3 green + 1 missing (e.g. sla_rate_crit_high absent on curr) → composite amber, data_incomplete True
    - test_missing_signal_named: the missing signal name appears in metrics/driver_narrative
    - test_sla_rate_reopened_aware: a REOPENED finding is included in the current SLA posture population (open_findings_at), not dropped
    - test_empty_data_guard: zero-row vulns_df → _empty_result, no raise, no_data strip
    - test_owner_outlier_flagging: owner rising >20% MoM → outlier flag True; owner within band → False
  </behavior>
  <action>
    Create `tests/test_program_health_module.py`. Copy the fixture scaffold verbatim
    from test_mttr_trend_module.py 1–95 (pd.options.mode.copy_on_write = True so strict
    CoW catches Pitfall 3; _UUID_PREFIX, REF, _uuid, _make_assets with
    tags="Owner=Engineering"). Build synthetic snapshot-list fixtures (dicts with month,
    generated_at, critical/high/medium/low, new_findings_count, fixed_findings_count,
    mttr_overall_days, sla_rate_crit_high) to drive each composite scenario — craft the
    curr/prev values so the intended signal directions hold. All synthetic data QUAL-05:
    IPs 192.0.2.x (RFC 5737), hostnames *.test.invalid (RFC 6761), owner names
    Engineering/Operations/Unassigned. Cover the 8 behaviors above. For the
    reopened-aware test, include a finding with resurfaced_date set and assert it is
    counted in the SLA denominator. For the owner outlier test, drive curr vs prev owner
    counts through the module (or a small helper) and assert the flag. Run under the same
    pytest discovery as the other module tests.
  </action>
  <verify>
    <automated>python -m pytest tests/test_program_health_module.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_program_health_module.py -x -q` collects ≥8 tests and exits 0
    - test names present: test_cold_start_no_snapshots, test_cold_start_one_snapshot, test_composite_all_green, test_composite_missing_caps_amber, test_missing_signal_named, test_sla_rate_reopened_aware, test_empty_data_guard, test_owner_outlier_flagging
    - The missing-cap test asserts both composite=="amber" AND data_incomplete is True
    - The reopened-aware test fails if open_findings_at is replaced by a naive state filter (the REOPENED finding must be in the SLA population)
  </acceptance_criteria>
  <done>Eight compute-layer tests pass, proving the OD-5 composite, missing-signal cap, cold-start Amber, reopened-aware SLA, empty-data guard, and Owner outlier logic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator module_options (YAML) → composite RAG thresholds | untrusted numeric thresholds influence the headline status |
| snapshot JSON → compute() | optional fields may be absent (old snapshots) |
| owner tag strings → ModuleData table_data/analyst_rows | tag-derived strings flow toward render channels (escaped in Plan 03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04 | Tampering | module_options threshold injection (e.g. green_count_min=0 to force Green) | mitigate | validate_config() coerces to int/float, logs WARNING + falls back to default on bad values; AND the D-17-06 missing-signal Amber cap is structural in _composite_rag_od5 — cannot be bypassed via options (RESEARCH Security table) |
| T-17-05 | Information Disclosure | analyst_rows / table_data leaking asset-level data | mitigate | QUAL-05: Owner table + analyst tabs carry owner tag name + aggregate Crit+High counts + derived MoM delta only — no asset UUIDs, IPs, hostnames, or plugin IDs (test_analyst_aggregate_only-style coverage in Plan 03; compute emits aggregates only) |
| T-17-06 | Denial of Service | KeyError on absent snapshot field aborting module | mitigate | snap.get("sla_rate_crit_high") (never []), per-signal None → "missing" → Amber fold; whole compute wrapped in try/except → _empty_result (fail-soft batch) |

No new auth/session/crypto surface (ASVS V2/V3/V6 N/A). Owner-string → HTML/PDF
escaping is a render concern handled in Plan 03 (T-17-07 there). No `high`-severity
threat introduced — all `mitigate` with concrete in-code controls; the only novel
input is operator module_options, capped structurally.
</threat_model>

<verification>
- `registry.discover()` finds `program_health`; the three pure-function assertions in Task 1 verify pass.
- `_MODULES_NEEDING_TREND_SNAPSHOTS` contains program_health; `_MODULES_NEEDING_FIXED_VULNS` does not.
- `run_all.py --dry-run` exits 0 (no regression).
- `pytest tests/test_program_health_module.py -x -q` green.
</verification>

<success_criteria>
- The 4 velocity signals are re-derived from the S1 substrate; the OD-5 composite (Green=all4 / Amber=2–3 / Red=0–1) with the structural missing-signal Amber cap is correct and module_options-overridable (D-17-05/06).
- Per-signal MoM coloring with overridable flat bands (D-17-07); cold-start with <2 snapshots yields Amber "Trend Being Established" + current-value tiles, never NaN%/crash (D-17-08, QUAL-01).
- Owner velocity is reopened-aware over Crit+High with a >20% outlier flag, degrading gracefully when owner trend is insufficient (D-17-09).
- composed_report gate registered with no signature change; empty input is fail-soft (QUAL-03); aggregate-only analyst output (QUAL-05).
</success_criteria>

<output>
Create `.planning/phases/17-program-health-overview/17-02-SUMMARY.md` when done.
</output>
