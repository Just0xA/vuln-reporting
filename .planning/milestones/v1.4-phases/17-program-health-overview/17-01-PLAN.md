---
phase: 17-program-health-overview
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/trend_store.py
  - scripts/capture_trend_snapshot.py
  - tests/content/test_trend_store.py
autonomous: true
requirements: [RPT-07]
tags: [trend, snapshot, sla, program-health]

must_haves:
  truths:
    - "OD-5/D-17-04: the S1 severity snapshot persists a new sla_rate_crit_high float field (% of open Critical+High findings within SLA), forward-accumulating"
    - "D-17-03: SLA posture is computed reopened-aware (open_findings_at) over Critical+High only, using config.SLA_DAYS — never hardcoded SLA days"
    - "D-17-04/D-16-09: old snapshots lacking sla_rate_crit_high read back as None (cold-start), no KeyError, no schema_version, no migration (QUAL-01)"
    - "QUAL-05: only an aggregate float is persisted — no hostnames, IPs, plugin names, or asset-level fields enter the snapshot"
    - "QUAL-03: SLA-posture computation in the capture script is fail-soft — a computation error sets the field to None and does not abort the severity snapshot"
  artifacts:
    - path: "data/trend_store.py"
      provides: "capture_snapshot() sla_rate_crit_high optional param + new_entry persistence + backward-compat __main__ smoke assertion"
      contains: "sla_rate_crit_high"
    - path: "scripts/capture_trend_snapshot.py"
      provides: "fail-soft SLA-posture aggregate computation block + sla_rate_crit_high=... passed to the severity capture_snapshot() call"
      contains: "sla_rate_crit_high"
    - path: "tests/content/test_trend_store.py"
      provides: "round-trip + default-None + old-snapshot-backward-compat tests for sla_rate_crit_high"
      contains: "sla_rate_crit_high"
  key_links:
    - from: "scripts/capture_trend_snapshot.py"
      to: "data.trend_store.capture_snapshot"
      via: "sla_rate_crit_high keyword argument on the severity snapshot call"
      pattern: "sla_rate_crit_high\\s*="
    - from: "scripts/capture_trend_snapshot.py"
      to: "utils.open_count.open_findings_at + config.SLA_DAYS"
      via: "reopened-aware open population + authoritative SLA targets"
      pattern: "open_findings_at|SLA_DAYS"
---

<objective>
Extend the S1 trend substrate with a forward-accumulating SLA-posture aggregate
(`sla_rate_crit_high`) so the Phase 17 `program_health` module gets a real
month-over-month SLA-rate signal (D-17-03/04) rather than a current-only number.

This plan delivers ONLY the snapshot field + its computation + round-trip tests.
It is the upstream contract the `program_health` module consumes. No module code
here.

Purpose: D-17-04 — persist SLA posture so "SLA rate delta" (roadmap criterion 2)
is a true MoM signal, cold-start-gated until ≥2 snapshots exist (QUAL-01).
Output: `data/trend_store.py` + `scripts/capture_trend_snapshot.py` modified;
3 appended tests in `tests/content/test_trend_store.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-program-health-overview/17-CONTEXT.md
@.planning/phases/17-program-health-overview/17-RESEARCH.md
@.planning/phases/17-program-health-overview/17-PATTERNS.md

<interfaces>
<!-- The exact prior-art (Phase 16 MTTR field) this plan repeats. Read these — copy the pattern. -->

data/trend_store.py — capture_snapshot() signature begins at line 222; the
mttr_overall_days/mttr_by_severity/mttr_by_owner params end at line 238; the
new_entry dict persists them at lines ~384–390 ("mttr_by_owner": mttr_by_owner).
The __main__ backward-compat block uses an _old_snap dict (~lines 534–551) with
existing assertions like:
    assert (_old_snap.get("mttr_by_owner") or {}) == {}, ...

scripts/capture_trend_snapshot.py — the MTTR aggregate block is at lines 300–385;
mttr_overall_days computed at line 348; the severity capture_snapshot(...) call is
at line 388 (mttr_overall_days=... at line 395); a SECOND owner-dimension
capture_snapshot(...) call is at line 411 (do NOT add the SLA field there — SLA is
severity-dimension only).

tests/content/test_trend_store.py — the analog tests to clone are:
  test_mttr_aggregate_fields_written (line 874)  — round-trip a supplied float
  test_mttr_params_default_to_none  (line 921)   — default None when not supplied
  test_old_snapshot_readable_without_mttr_fields (line 952) — old snapshot .get() → None
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add sla_rate_crit_high field to capture_snapshot() + backward-compat smoke</name>
  <files>data/trend_store.py</files>
  <read_first>
    - data/trend_store.py (capture_snapshot signature ~222–240; new_entry dict ~384–390; __main__ _old_snap block ~534–551)
  </read_first>
  <behavior>
    - capture_snapshot(..., sla_rate_crit_high=87.3) → the written severity snapshot entry has entry["sla_rate_crit_high"] == 87.3
    - capture_snapshot(...) without the param → entry["sla_rate_crit_high"] is None (implicit-optional-field default)
    - A hand-built old snapshot dict lacking the key → snap.get("sla_rate_crit_high") is None, no KeyError
  </behavior>
  <action>
    Add `sla_rate_crit_high: Optional[float] = None` to the `capture_snapshot()`
    signature immediately after the `mttr_by_owner` param (after line 238), with a
    `# ---- Phase 17 addition (D-17-04) ----` comment matching the Phase 16 comment
    style. Add a one-line docstring entry mirroring the `mttr_overall_days` docstring
    bullet. Persist it in the `new_entry` dict immediately after the
    `"mttr_by_owner": mttr_by_owner,` line as `"sla_rate_crit_high": sla_rate_crit_high,`.
    In the `__main__` block, after the existing `mttr_by_owner` backward-compat
    assertion (~line 550), add:
    `assert _old_snap.get("sla_rate_crit_high") is None, "Old snapshot must cold-start sla_rate_crit_high to None (no KeyError)"`
    followed by a confirming `print(...)`. This is the D-15-06/D-16-09 implicit-
    optional-field convention: no schema_version, no migration, absent → None →
    cold-start. Do NOT touch the owner-dimension snapshot — SLA posture is a
    severity-dimension field only (per-Owner SLA is out of scope, D-17-04).
  </action>
  <verify>
    <automated>python -c "import data.trend_store" && python data/trend_store.py 2>&1 | grep -i "sla_rate_crit_high"</automated>
  </verify>
  <acceptance_criteria>
    - `data/trend_store.py` capture_snapshot signature contains `sla_rate_crit_high: Optional[float] = None`
    - new_entry dict contains the literal `"sla_rate_crit_high": sla_rate_crit_high,`
    - `python data/trend_store.py` __main__ smoke run prints a backward-compat OK line for sla_rate_crit_high and exits 0
    - No `schema_version` key introduced anywhere in the file (grep for `schema_version` in data/trend_store.py returns no new match)
  </acceptance_criteria>
  <done>The severity snapshot persists sla_rate_crit_high; supplying it round-trips the value, omitting it yields None, and an old snapshot dict cold-starts to None without KeyError.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compute + pass sla_rate_crit_high in capture_trend_snapshot.py (reopened-aware, Crit+High, fail-soft)</name>
  <files>scripts/capture_trend_snapshot.py</files>
  <read_first>
    - scripts/capture_trend_snapshot.py (MTTR aggregate block 300–385; severity capture_snapshot call ~388–397; owner call ~411)
    - utils/open_count.py (open_findings_at signature + two-interval reopened-aware predicate)
    - utils/sla_calculator.py (get_sla_status — read for definition; the snapshot uses the vectorized SLA_DAYS test, not a per-row call)
    - config.py (SLA_DAYS — authoritative per-severity targets; Critical=15, High=30)
  </read_first>
  <behavior>
    - For a vulns frame with a mix of open Critical/High findings some within and some over SLA, sla_rate_crit_high == round(within_count / crit_high_open_count * 100, 1)
    - REOPENED findings are included via open_findings_at (not dropped) — QUAL-02
    - Zero open Crit+High findings → sla_rate_crit_high stays None (no divide-by-zero), severity snapshot still written
    - A raised exception inside the SLA block → field is None and the severity snapshot still completes (fail-soft) — QUAL-03
  </behavior>
  <action>
    Insert a new fail-soft block before the severity `capture_snapshot(...)` call at
    line 388, mirroring the structure of the MTTR aggregate block (300–385). Compute
    `sla_rate_crit_high: Optional[float] = None` inside a `try/except Exception` that
    logs a WARNING ("SLA-posture aggregate failed — field will cold-start: %s") and
    leaves the field None on any error. Inside the try: call
    `open_findings_at(df, snapshot_date)` (reopened-aware open population, QUAL-02),
    filter to Critical+High via `open_df["severity"].str.lower().isin({"critical","high"})`
    (D-17-03 — Crit+High only, never Low/Medium), and only when the filtered frame is
    non-empty compute a vectorized within-SLA test:
    `snap_ts = pd.Timestamp(snapshot_date, tz="UTC")` (Pitfall 7 — match the MTTR
    block's tz handling at line 313; snapshot_date is tz-naive here),
    `ff_ts = pd.to_datetime(ch_df["first_found"], utc=True, errors="coerce")`,
    `days_open = (snap_ts - ff_ts).dt.days.clip(lower=0)` assigned to a LOCAL variable
    (never `ch_df[...] =` after the filter — pandas-3.0 CoW, Pitfall 3),
    `sla_days_col = ch_df["severity"].str.lower().map(SLA_DAYS)`,
    `within = days_open.notna() & sla_days_col.notna() & (days_open <= sla_days_col)`,
    then `sla_rate_crit_high = round(float(within.sum()) / len(ch_df) * 100, 1)` (a
    float percentage 0–100, NOT a fraction — Assumption A3). Pass
    `sla_rate_crit_high=sla_rate_crit_high` into the severity `capture_snapshot(...)`
    call alongside `mttr_overall_days=...`. Do NOT add it to the owner-dimension call
    at line 411. Import `open_findings_at` and `SLA_DAYS` inside the try (or reuse
    existing top-level imports if already present — check first).
  </action>
  <verify>
    <automated>python -c "import ast,sys; src=open('scripts/capture_trend_snapshot.py').read(); ast.parse(src); assert 'sla_rate_crit_high' in src and 'open_findings_at' in src and 'isin({\"critical\", \"high\"})' in src.replace(chr(39),chr(34)).replace(' ','') or 'critical' in src; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/capture_trend_snapshot.py` parses (ast.parse) and contains `sla_rate_crit_high=` passed to the severity capture_snapshot call (grep confirms a `sla_rate_crit_high=sla_rate_crit_high` argument)
    - The SLA block references `open_findings_at` and `SLA_DAYS` (reopened-aware + authoritative SLA, not hardcoded day counts)
    - The block is wrapped in `try/except Exception` with a `logger.warning(...)` fallback that leaves the field None (fail-soft)
    - The owner-dimension capture_snapshot call (~line 411) does NOT receive sla_rate_crit_high (grep: only one occurrence of `sla_rate_crit_high=` in the file)
    - No `ch_df["..."] =` assignment after the Crit+High filter (CoW-safe; grep confirms days_open is a local, not a column write)
  </acceptance_criteria>
  <done>The capture script computes the reopened-aware Crit+High SLA-posture rate fail-soft and passes it to the severity snapshot; failures cold-start the field to None without aborting the snapshot.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Append round-trip + default + backward-compat tests for sla_rate_crit_high</name>
  <files>tests/content/test_trend_store.py</files>
  <read_first>
    - tests/content/test_trend_store.py (test_mttr_aggregate_fields_written L874; test_mttr_params_default_to_none L921; test_old_snapshot_readable_without_mttr_fields L952 — clone these three)
  </read_first>
  <behavior>
    - test_sla_rate_crit_high_roundtrip: capture_snapshot(..., sla_rate_crit_high=87.3) then read back → entry["sla_rate_crit_high"] == 87.3
    - test_sla_rate_params_default_to_none: capture_snapshot without the param → entry["sla_rate_crit_high"] is None
    - test_sla_rate_backward_compat: a hand-built old snapshot dict lacking the key → snap.get("sla_rate_crit_high") is None, no KeyError
  </behavior>
  <action>
    Append three tests to `tests/content/test_trend_store.py` cloned from the three
    MTTR analog tests (L874 / L921 / L952). Reuse the existing fixture helpers and
    `tmp_path` pattern already in the file (do not invent new fixtures). The
    round-trip test passes `sla_rate_crit_high=87.3` to capture_snapshot and asserts
    the read-back float equals 87.3; the default test omits the param and asserts the
    persisted value is None; the backward-compat test builds an old-style snapshot
    dict WITHOUT the key and asserts `snap.get("sla_rate_crit_high") is None`
    (D-16-09 / Pitfall 4). All synthetic data must follow QUAL-05 — no real
    hostnames/IPs/plugin names (reuse the file's existing synthetic conventions).
  </action>
  <verify>
    <automated>python -m pytest tests/content/test_trend_store.py -k "sla_rate" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/content/test_trend_store.py -k "sla_rate" -x -q` collects ≥3 tests and exits 0
    - The three test function names exist: test_sla_rate_crit_high_roundtrip, test_sla_rate_params_default_to_none (or equivalent default-None name), test_sla_rate_backward_compat
    - Full file still green: `pytest tests/content/test_trend_store.py -q` exits 0 (no regression to existing snapshot tests)
  </acceptance_criteria>
  <done>Three new tests prove the SLA-posture field round-trips, defaults to None, and cold-starts on old snapshots; the existing trend_store test suite is unregressed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live vulns_df → persisted snapshot JSON | Aggregate-only float must cross; no row-level data may leak into a committed/persisted file |
| operator-supplied snapshot date / config | tz-naive date handed to a vectorized pandas computation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Information Disclosure | sla_rate_crit_high persisted in trend snapshot | mitigate | Only a single aggregate float (0–100) is stored; QUAL-05 verified by test_no_pii_in_snapshot-style coverage — no hostnames/IPs/plugin/asset fields. D-04-08 honored. |
| T-17-02 | Tampering / Denial of Service | SLA-posture computation aborting the whole capture batch | mitigate | try/except Exception fail-soft — a compute error sets the field None and the severity snapshot still completes (fail-soft batch semantics) |
| T-17-03 | Tampering | tz-naive snapshot_date causing silent wrong SLA math | mitigate | snap_ts wrapped as pd.Timestamp(snapshot_date, tz="UTC") matching the MTTR block; ff_ts coerced utc=True (Pitfall 7) |

No new high-severity threats beyond the established locked patterns: no new auth/session/crypto surface (ASVS V2/V3/V6 N/A), no network input, no new external package. The only new persisted value is an aggregate float — the PII-in-snapshot risk class is the same one Phase 15/16 already mitigated and is re-covered here. Justification for not blocking: all three threats are `mitigate` with concrete in-code controls; none are `high` per security_block_on.
</threat_model>

<verification>
- `python data/trend_store.py` __main__ smoke passes (backward-compat assertions incl. the new field) and exits 0.
- `python -c "import ast; ast.parse(open('scripts/capture_trend_snapshot.py').read())"` succeeds.
- `pytest tests/content/test_trend_store.py -q` is fully green.
- The new field appears exactly once in the capture script's `capture_snapshot(...)` argument lists (severity call only, never the owner call).
</verification>

<success_criteria>
- `sla_rate_crit_high` is a persisted, forward-accumulating severity-snapshot field computed reopened-aware over Critical+High using config.SLA_DAYS (D-17-03/04).
- Old snapshots without the field read back as None — cold-start safe, no migration (D-16-09, QUAL-01).
- Only an aggregate float is persisted (QUAL-05); computation is fail-soft (QUAL-03).
- Three new tests prove round-trip / default-None / backward-compat; existing trend_store suite unregressed.
</success_criteria>

<output>
Create `.planning/phases/17-program-health-overview/17-01-SUMMARY.md` when done.
</output>
