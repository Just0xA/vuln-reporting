---
phase: 4
plan: 04-03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - delivery_config.yaml
autonomous: true   # Operator-locked tag_value (D-04-04 revised) removes the interaction checkpoint
requirements: [CONFIG-04]
tags: [delivery_config, test-groups, analyst_detail, zero-match, sensitive-data, example-invalid]
performance_estimate:
  total_diff_lines: ~30-45  # Two new groups appended to delivery_config.yaml
  duration_estimate: ~10-15 minutes
  risk_level: low  # YAML-only edit; schema enforcement (Plan 04-01) catches structural mistakes immediately

must_haves:
  truths:
    - "delivery_config.yaml contains exactly three groups: existing 'Test Pull' (unchanged), new 'Test Pull — Analyst Off' with analyst_detail: false, new 'Test Pull — Zero Match' with tag_category: 'Application' and tag_value: 'DoesNotExist' (operator-confirmed 2026-05-07 per D-04-04)."
    - "Both new groups use the example.invalid recipient domain per D-04-08 (no real address in the committed YAML)."
    - "Both new groups otherwise mirror 'Test Pull' (frequency: on_demand, reports: [board_summary], same email subject pattern)."
    - "python run_all.py --dry-run exits 0 — schema validation accepts all three groups (proves Plan 04-01's schema edits are sufficient)."
    - "tag_category 'Application' is a real tag category in the operator's Tenable tenant; tag_value 'DoesNotExist' is a literal sentinel known to be unused — together they yield zero scoped assets."
  artifacts:
    - path: "delivery_config.yaml"
      provides: "Three groups exercising baseline (analyst on, scoped data), opt-out (analyst off), and empty-data path (zero assets in scope)."
      contains: "Test Pull — Analyst Off"
    - path: "delivery_config.yaml"
      provides: "Zero-match group with locked tag_value: DoesNotExist."
      contains: "DoesNotExist"
  key_links:
    - from: "delivery_config.yaml 'Test Pull — Analyst Off'"
      to: "Plan 04-02's run_group dispatch"
      via: "analyst_detail: false → group_config.get('analyst_detail', True) returns False → board_summary.run_report(analyst_detail=False) → bundle['analyst_excel'] is None"
      pattern: "analyst_detail: false"
    - from: "delivery_config.yaml 'Test Pull — Zero Match'"
      to: "Plan 04-04's smoke harness empty-data assertions"
      via: "tag_category=Application + tag_value=DoesNotExist selects zero assets → all four board modules return ModuleData with empty metrics → render methods produce gray 'No Data' RAG cells, '—' headlines, 'No data in scope.' driver narrative"
      pattern: "tag_value: \"DoesNotExist\""

threat_model:
  trust_boundaries:
    - boundary: "operator-edited YAML → committed git history"
      description: "Sensitive data (real recipients, identifiable BU tag values) MUST NOT enter the committed config per D-04-08. The example.invalid domain plus the literal sentinel `DoesNotExist` are the locked-decision mitigations."
  stride_register:
    - id: T-04-03-01
      category: "Information disclosure"
      component: "Committed delivery_config.yaml"
      disposition: mitigate
      mitigation: "Both new groups use reports-test@example.invalid — RFC 6761 reserved test domain, guaranteed never to deliver. A YAML leak via PR description, screenshot, or stray clipboard copy exposes nothing useful. Existing 'Test Pull' recipient stays as-is per D-04-08 ('operator's call' on personal address)."
    - id: T-04-03-02
      category: "Information disclosure"
      component: "Zero-match tag_value committed to YAML"
      disposition: mitigate
      mitigation: "Operator-confirmed literal `DoesNotExist` (2026-05-07 per D-04-04). The string identifies no real BU/owner/team — it is an obvious sentinel that signals intent to readers. tag_category 'Application' is the structure name (non-sensitive)."
    - id: T-04-03-03
      category: "Tampering"
      component: "Schema rejects new groups"
      disposition: mitigate
      mitigation: "Plan 04-01 (depends_on) reconciled the reports enum and added analyst_detail. Task 3 verifies --dry-run still exits 0 immediately after each group is added; if validation rejects, the operator gets a clear error and can fix in-place."
---

<objective>
Phase 4 Wave 2 (parallel with 04-02): extend `delivery_config.yaml` from one group to three so each Phase 4 behavior has a corresponding test group.

Per D-04-04 (revised 2026-05-07), the three groups are:

| Group | Purpose | analyst_detail | tag filter |
|-------|---------|----------------|------------|
| Test Pull (existing — UNCHANGED) | Baseline regression | omitted (default true) | none |
| Test Pull — Analyst Off (NEW) | Exercises CONFIG-03 opt-out | false | none |
| Test Pull — Zero Match (NEW) | Exercises empty-data path (gray "No Data" cells, em-dash headlines) | omitted (default true) | tag_category: "Application", tag_value: "DoesNotExist" |

Per D-04-08, both new groups use `reports-test@example.invalid` (RFC 6761 reserved test domain). The existing "Test Pull" recipient is unchanged (operator's call).

Per D-04-04 (revised), the operator confirmed the literal pair `tag_category: "Application"` (a known tag category in their tenant) and `tag_value: "DoesNotExist"` (a literal value known to be unused). No interaction checkpoint is needed; the values are locked at planning time.

**Why no other code touches this plan:** the schema accepts `analyst_detail: false` (Plan 04-01 Task 2), the run_group dispatch reads `group_config.get("analyst_detail", True)` (Plan 04-02 Task 2 — Wave 2 parallel; lands in same wave), and the empty-data render path was hardened in Phase 3 (gray "No Data" cells per the empty-data guard pattern in CLAUDE.md). This plan only adds YAML rows.

Purpose: deliver CONFIG-04 (a sample group in `delivery_config.yaml` demonstrates the opt-out) plus a third group exercising the empty-data render path. Both will be consumed by Plan 04-04's regression-smoke harness.

Output: 1 atomic commit appending two new groups to `delivery_config.yaml`. `python run_all.py --dry-run` exits 0 against the post-commit file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-yaml-config-and-regression-cutover/04-CONTEXT.md
@.planning/phases/04-yaml-config-and-regression-cutover/04-RESEARCH.md
@.planning/phases/04-yaml-config-and-regression-cutover/04-01-SUMMARY.md
@delivery_config.yaml
@delivery_config.schema.yaml
@CLAUDE.md

<interfaces>
**Current `delivery_config.yaml` (verified at planning time):**
```yaml
groups:
  - name: "Test Pull"
    description: "First of the month full executive package"
    schedule:
      frequency: on_demand
    reports:
      - board_summary
    filters: {}
    email:
      subject: "Monthly Vulnerability Management Report"
      recipients:
        - monroe.justin@gmail.com
      reply_to: monroe.justin@gmail.com
```

**Schema constraints (delivery_config.schema.yaml after Plan 04-01):**
- `definitions.group.required`: `name`, `schedule`, `reports`, `email`
- `definitions.group.additionalProperties: false` — typo'd keys rejected
- `definitions.email.recipients`: `minItems: 1`, `format: email` — example.invalid will pass format check (RFC 6761 valid syntax)
- `definitions.filters.dependencies`: `tag_category` requires `tag_value` and vice versa — both must be present together
- `analyst_detail`: optional boolean (Plan 04-01 Task 2)

**Locked literal values (D-04-04 revised 2026-05-07):**
- `tag_category: "Application"` — known tag category in operator's Tenable tenant
- `tag_value: "DoesNotExist"` — literal sentinel, known to be unused

**Empty-data render path (CLAUDE.md "Empty-data guard pattern"):**
When the zero-match group runs, all four board modules' `compute()` methods receive a zero-row scoped DataFrame. The pattern at `BaseModule._empty_result()` produces a coherent `ModuleData` with `error` set, `metrics={}`, gray "No Data" RAG cell, "No data in scope." driver narrative. Plan 04-04 will assert this STRUCTURAL shape (counts, booleans only — no metric values) against the locked baseline.

**RFC 6761 reserved domain:**
`example.invalid` is one of four IETF-reserved domains guaranteed to never resolve. Canonical choice for committed test addresses.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append "Test Pull — Analyst Off" group to delivery_config.yaml</name>
  <files>delivery_config.yaml</files>
  <read_first>delivery_config.yaml (current shape — single 'Test Pull' group), delivery_config.schema.yaml (definitions.group properties — confirm analyst_detail is now permitted post-Plan-04-01)</read_first>
  <action>
Append a new group as the SECOND entry in `groups:` (preserve "Test Pull" as the first entry — Plan 04-04's baseline relies on its position). Mirror the existing group's structure exactly except for the changes called out below.

```yaml
  - name: "Test Pull — Analyst Off"
    description: "Phase 4 CONFIG-03 opt-out exerciser. Same scope as 'Test Pull' but with analyst_detail: false — should produce PDF + standard Excel + email panels but NO analyst companion workbook. Test recipient uses RFC 6761 reserved domain per D-04-08."
    schedule:
      frequency: on_demand
    reports:
      - board_summary
    filters: {}
    analyst_detail: false
    email:
      subject: "[Phase 4 test] Board Summary — analyst_detail=false"
      recipients:
        - reports-test@example.invalid
      reply_to: reports-test@example.invalid
```

Key invariants:
- `name` includes the em-dash for visual distinction in `--dry-run` output.
- `analyst_detail: false` is a top-level group property.
- Recipients use `reports-test@example.invalid` — D-04-08 mitigation.
- `subject` prefix `[Phase 4 test]` makes it obvious in any future log/audit-table output that this is not a production delivery.

**Note:** do NOT commit at the end of Task 1 — Tasks 1 and 2 belong to the same logical change. Single commit at end of Task 2.
  </action>
  <verify>
    <automated>python -c "import yaml; d=yaml.safe_load(open('delivery_config.yaml')); names=[g['name'] for g in d['groups']]; assert 'Test Pull — Analyst Off' in names, names; off=[g for g in d['groups'] if g['name']=='Test Pull — Analyst Off'][0]; assert off['analyst_detail'] is False, off; assert off['email']['recipients']==['reports-test@example.invalid'], off; print('OK')"</automated>
    <automated>grep -c "example.invalid" delivery_config.yaml | grep -E "^[2-9]" || (echo "FAIL: expected at least 2 example.invalid mentions (recipients + reply_to)"; exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - delivery_config.yaml has exactly 2 group entries at this point (existing + new)
    - The new group's `analyst_detail` field is the literal Python boolean `False` after parse
    - All recipients/reply_to in the new group use the `example.invalid` domain
    - `name` is exactly `Test Pull — Analyst Off` (em-dash, not hyphen)
  </acceptance_criteria>
  <done>The opt-out test group exists; ready for Task 2's zero-match group.</done>
</task>

<task type="auto">
  <name>Task 2: Append "Test Pull — Zero Match" group with locked tag values + commit</name>
  <files>delivery_config.yaml</files>
  <read_first>delivery_config.yaml (post-Task-1 shape with two groups), .planning/phases/04-yaml-config-and-regression-cutover/04-CONTEXT.md (D-04-04 revised — operator-locked tag_category/tag_value literals)</read_first>
  <action>
Append the third group as the LAST entry in `groups:` using the operator-locked literals from D-04-04 (revised 2026-05-07): `tag_category: "Application"`, `tag_value: "DoesNotExist"`.

```yaml
  - name: "Test Pull — Zero Match"
    description: "Phase 4 empty-data exerciser. Tag filter selects zero scoped assets so all four board modules return empty ModuleData and render the no-data path (gray 'No Data' RAG cells, em-dash headlines, 'No data in scope.' driver narrative). tag_category 'Application' is a real category in the operator's Tenable tenant; tag_value 'DoesNotExist' is a literal sentinel known to be unused per D-04-04 (revised 2026-05-07)."
    schedule:
      frequency: on_demand
    reports:
      - board_summary
    filters:
      tag_category: "Application"
      tag_value: "DoesNotExist"
    email:
      subject: "[Phase 4 test] Board Summary — zero-match scope"
      recipients:
        - reports-test@example.invalid
      reply_to: reports-test@example.invalid
```

Key invariants:
- `analyst_detail` is OMITTED (group exercises the empty-data path with default-true analyst behavior — the empty-data RAG strip and panel rendering are tested, not the opt-out).
- `filters` block uses BOTH `tag_category` and `tag_value` together (the schema's `dependencies` rule rejects either alone).
- `tag_value` is the LOCKED LITERAL `"DoesNotExist"` per D-04-04 revised — not a placeholder, not operator-supplied at execution time.
- Recipients use `example.invalid`.

**Single commit for Tasks 1 + 2 combined:**

```
feat(04): add Test Pull analyst-off and zero-match groups to delivery_config

Two new test groups for Phase 4 behaviors (per CONTEXT.md D-04-04 revised
2026-05-07):

  - "Test Pull — Analyst Off"  : exercises CONFIG-03 (analyst_detail: false)
  - "Test Pull — Zero Match"   : exercises empty-data render path
    (tag_category: Application, tag_value: DoesNotExist — operator-
    confirmed literal sentinel that yields zero scoped assets)

Both use reports-test@example.invalid per D-04-08 (no real recipients
in committed config).

Schema validation against the post-Plan-04-01 schema accepts all three
groups; --dry-run exits 0.

Refs: CONFIG-04, D-04-04, D-04-08
```
  </action>
  <verify>
    <automated>python -c "import yaml; d=yaml.safe_load(open('delivery_config.yaml')); names=[g['name'] for g in d['groups']]; assert names==['Test Pull','Test Pull — Analyst Off','Test Pull — Zero Match'], names; zm=d['groups'][2]; assert zm['filters']['tag_category']=='Application', zm; assert zm['filters']['tag_value']=='DoesNotExist', zm; assert zm['email']['recipients']==['reports-test@example.invalid']; print('OK')"</automated>
    <automated>grep -nE 'tag_value:\s*"DoesNotExist"' delivery_config.yaml | wc -l | grep -E "^\s*1\s*$" || (echo "FAIL: expected exactly 1 'tag_value: \"DoesNotExist\"' match"; exit 1)</automated>
    <automated>grep -nE 'tag_category:\s*"Application"' delivery_config.yaml | wc -l | grep -E "^\s*1\s*$" || (echo "FAIL: expected exactly 1 'tag_category: \"Application\"' match"; exit 1)</automated>
    <automated>python run_all.py --dry-run; test $? -eq 0 || (echo "FAIL: schema rejected new groups"; exit 1)</automated>
    <automated>python tests/test_phase4_schema_validation.py 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - delivery_config.yaml has exactly 3 groups in this order: 'Test Pull', 'Test Pull — Analyst Off', 'Test Pull — Zero Match'
    - The third group's `filters.tag_category` is exactly `"Application"` and `filters.tag_value` is exactly `"DoesNotExist"`
    - `grep -nE 'tag_value:\s*"DoesNotExist"' delivery_config.yaml` returns exactly 1 match
    - The third group has NO `analyst_detail` field (default-true behavior)
    - All recipients in the two new groups use `example.invalid`
    - Existing 'Test Pull' group is byte-identical to its pre-Plan state (only an addition, never a modification)
    - `python run_all.py --dry-run` exits 0
  </acceptance_criteria>
  <done>Three test groups committed with locked literal tag values; schema validation passes; Plan 04-04's smoke harness has all three baseline targets to capture.</done>
</task>

<task type="auto">
  <name>Task 3: Confirm three-group --dry-run table renders correctly</name>
  <files></files>
  <read_first></read_first>
  <action>
Manual smoke verification — no code changes. Run `python run_all.py --dry-run` and confirm the rich table shows three rows, all with green OK status. Defense-in-depth verification that the schema-enforced validation (Plan 04-01 Task 4) plus the three-group config (Tasks 1+2 above) cooperate without surprises.

If `--dry-run` exits non-zero, do NOT proceed to Plan 04-04. Loop back, investigate the schema error, and fix the YAML in-place before claiming the plan complete.

No commit at this task — verification only.
  </action>
  <verify>
    <automated>OUT=$(python run_all.py --dry-run 2>&1); RC=$?; test $RC -eq 0 || (echo "FAIL: --dry-run exit non-zero"; echo "$OUT"; exit 1); echo "$OUT" | grep -F "Test Pull — Analyst Off" || (echo "FAIL: analyst-off row missing"; exit 1); echo "$OUT" | grep -F "Test Pull — Zero Match" || (echo "FAIL: zero-match row missing"; exit 1); echo OK</automated>
    <manual>Visual: rich table shows three rows; status column shows OK / OK / OK (green); Filter column shows 'All Assets' for the first two groups and `Application = DoesNotExist` for the third.</manual>
  </verify>
  <acceptance_criteria>
    - `python run_all.py --dry-run` exits 0
    - Output contains all three group names verbatim
    - "Validation FAILED" string is NOT present anywhere in output
  </acceptance_criteria>
  <done>Three groups validated end-to-end against the schema; Plan 04-04 unblocked.</done>
</task>

</tasks>

<verification>
Phase-level acceptance gates for plan 04-03:
1. `delivery_config.yaml` has exactly 3 groups in the locked order: Test Pull, Test Pull — Analyst Off, Test Pull — Zero Match.
2. 'Test Pull' group is unmodified from its Phase-3 state (only additions; no edits).
3. Both new groups use `reports-test@example.invalid`.
4. The zero-match group's `tag_category` is `"Application"` and `tag_value` is `"DoesNotExist"` (literal sentinels per D-04-04 revised).
5. `python run_all.py --dry-run` exits 0; rich table shows 3 OK rows.
6. `python tests/test_phase4_schema_validation.py` continues to pass.
7. Single atomic commit lands with both new groups together.
</verification>

<success_criteria>
- Three test groups exist in `delivery_config.yaml`, each exercising a distinct Phase 4 behavior.
- Sensitive data hygiene maintained: example.invalid recipients, locked sentinel tag values (Application / DoesNotExist), no internal-team identifiers committed.
- Schema enforcement (Plan 04-01) accepts all three groups without errors.
- Single atomic commit landed; commit message references CONFIG-04, D-04-04, D-04-08.
</success_criteria>

<output>
After completion, create `.planning/phases/04-yaml-config-and-regression-cutover/04-03-SUMMARY.md` per the standard template — confirm the dry-run output shows 3 OK rows, record the locked tag pair (Application / DoesNotExist), and note that Plan 04-04 can now capture structural baselines for all three groups.
</output>
