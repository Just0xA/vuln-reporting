---
phase: 06-cover-redesign-board-summary-integration
plan: 05
subsystem: testing
tags: [pdf-chrome, baselines, smoke-test, visual-uat, board_summary, weasyprint]

requires:
  - phase: 06-cover-redesign-board-summary-integration/06-01
    provides: composer chrome wiring
  - phase: 06-cover-redesign-board-summary-integration/06-02
    provides: trimmed cover body (scope subtitle + RAG strip only)
  - phase: 06-cover-redesign-board-summary-integration/06-03
    provides: PdfChromeConfig fields on board_summary
  - phase: 06-cover-redesign-board-summary-integration/06-04
    provides: run_group threading of chrome config
provides:
  - Regenerated structural smoke baselines for the 3 board_summary scenarios (test_pull, analyst_off, zero_match)
  - Operator visual UAT checklist (PENDING human sign-off)
  - Zero-drift smoke re-run confirmation (CHROME-INT-03)
affects: [Phase 7+ refactors of board_summary, composer, or pdf_chrome]

tech-stack:
  added: []
  patterns:
    - "Auto-init baseline regen via smoke script first-run path (no --regenerate flag; per RESEARCH.md Q4)"
    - "Structural baselines do NOT capture cover text — visual UAT is the correctness gate (RESEARCH.md Finding 3)"

key-files:
  created: []
  modified:
    - tests/baselines/board_summary_test_pull.json (regenerated; byte-identical post-regen)
    - tests/baselines/board_summary_test_pull_analyst_off.json (regenerated; byte-identical post-regen)
    - tests/baselines/board_summary_test_pull_zero_match.json (regenerated; byte-identical post-regen)

key-decisions:
  - "No commit for regenerated baselines: the wipe-then-regen cycle produced byte-identical JSON because structural snapshots capture only counts/keys/CIDs (RESEARCH.md Finding 3), and Phase 6 changed none of those. CHROME-INT-03 is satisfied: 0 structural drift, and the regen path was exercised."
  - "Visual UAT is the correctness gate — surfaced as a PENDING checklist in this SUMMARY for the human operator to execute against the rendered PDF."

patterns-established:
  - "When structural baselines are byte-identical after a planned visual redesign, record that explicitly in the SUMMARY rather than forcing an empty commit"

requirements-completed: [CHROME-INT-03, CHROME-COMPAT-02]

duration: ~5min
completed: 2026-05-13
---

# Phase 6 Plan 05: Baseline Regen + Operator Visual UAT Summary

**Wiped + auto-regenerated 3 board_summary smoke baselines via `scripts/smoke_board_summary_cutover.py`; zero-drift confirmed on re-run; rendered fresh `board_summary.pdf` and surfaced D-04 visual UAT checklist for operator sign-off (PENDING).**

## Status

- **Automation:** COMPLETE
- **Operator Visual UAT:** **PENDING** — the human operator must open the rendered PDF and walk the checklist below before Phase 6 / v1.1 can close.

## Performance

- **Duration:** ~5 minutes (automation only; excludes pending human UAT)
- **Started:** 2026-05-13T16:08:00Z
- **Completed (automation):** 2026-05-13T16:10:00Z
- **Tasks:** 6 (1 confirmed inline, 2 auto, 1 PENDING UAT, 1 deferred-empty commit, 1 zero-drift verify)
- **Files modified on disk:** 3 (baselines regenerated byte-identically — no commit needed)

## Accomplishments

- Confirmed group names in `delivery_config.yaml`: "Test Pull", "Test Pull — Analyst Off", "Test Pull — Zero Match" (group_slugs `test_pull`, `test_pull_analyst_off`, `test_pull_zero_match`).
- Wiped all 3 baseline JSONs and re-initialized them via the smoke script's auto-init path (RESEARCH.md Q4 — no `--regenerate` flag).
- Verified the R1 marker `pdf_has_risk_status_summary_header: true` survived the cover trim in all 3 regenerated baselines.
- Re-ran the smoke script post-regen: all 3 scenarios reported `OK`, exit code 0 — 0 structural drift (CHROME-INT-03 satisfied).
- Rendered a fresh board_summary PDF for operator visual UAT at:
  `D:\Projects\vuln-reporting\output\2026-05-13_16-09_test_pull\board_summary\board_summary.pdf`

## Task Commits

1. **Task 1: Confirm "Test Pull" group name** — resolved inline (no code change; group names matched the smoke script's `GROUP_SLUGS` dict).
2. **Task 2: Wipe 3 baselines** — disk-only operation; no commit at this step (per plan: commit lands in Task 5 after regen).
3. **Task 3: Warm cache + auto-init regen** — cache for `2026-05-13` was already warm (assets_all.parquet, vulns_all.parquet, vulns_fixed.parquet present); ran `python scripts/smoke_board_summary_cutover.py` which printed `BASELINE INITIALIZED` for all 3 scenarios.
4. **Task 4: Operator visual UAT** — **PENDING** (see checklist below). Rendered PDF available at the path noted above.
5. **Task 5: Commit regenerated baselines** — **NO COMMIT NEEDED.** `git status tests/baselines/` reported clean working tree after regen because the regenerated JSON files are byte-identical to the prior baselines. This is the expected outcome per RESEARCH.md Finding 3: structural snapshots capture only `analyst_excel_present`, `bundle_keys_present`, `email_inline_image_cids_per_module`, `email_panel_count`, `excel_tab_names_sorted`, `group_slug`, `panel_drivers_all_no_data_in_scope`, `pdf_has_risk_status_summary_header`, `pdf_page_count`, `pdf_rag_cell_count`, `rag_cells_all_no_data`, `schema_version` — and Phase 6 changed NONE of those. The cover text/layout changes are invisible to the structural baseline by design. The regen path was still exercised end-to-end (wipe → smoke auto-init → 3 fresh JSONs on disk → identical bytes), which is what CHROME-INT-03 actually requires.
6. **Task 6: Re-run smoke — 0 drift** — exit code 0; all 3 scenarios `OK`.

**Plan metadata commit:** _(only the SUMMARY commit, captured below)_

## BASELINE INITIALIZED stdout (Task 3)

```
[smoke] BASELINE INITIALIZED for Test Pull: D:\Projects\vuln-reporting\tests\baselines\board_summary_test_pull.json — review and commit.
[smoke] BASELINE INITIALIZED for Test Pull — Analyst Off: D:\Projects\vuln-reporting\tests\baselines\board_summary_test_pull_analyst_off.json — review and commit.
[smoke] BASELINE INITIALIZED for Test Pull — Zero Match: D:\Projects\vuln-reporting\tests\baselines\board_summary_test_pull_zero_match.json — review and commit.

=== Test Pull -> board_summary_test_pull.json ===
=== Test Pull — Analyst Off -> board_summary_test_pull_analyst_off.json ===
=== Test Pull — Zero Match -> board_summary_test_pull_zero_match.json ===
```

## Smoke re-run (Task 6) — zero-drift confirmation

```
=== Test Pull -> board_summary_test_pull.json ===
[smoke] Test Pull: OK
=== Test Pull — Analyst Off -> board_summary_test_pull_analyst_off.json ===
[smoke] Test Pull — Analyst Off: OK
=== Test Pull — Zero Match -> board_summary_test_pull_zero_match.json ===
[smoke] Test Pull — Zero Match: OK
EXIT=0
```

## Regenerated baseline contents (R1 marker verification)

All 3 baselines confirmed:

- `pdf_has_risk_status_summary_header: True` ✅ (R1 marker preserved through cover trim)
- `pdf_page_count: 5`, `pdf_rag_cell_count: 4` for all 3
- `analyst_excel_present`: True (test_pull), False (analyst_off + zero_match) — as expected per CONFIG-03
- `rag_cells_all_no_data`: False (test_pull + analyst_off), True (zero_match) — empty-data guard pattern intact

## Operator Visual UAT Checklist (D-04) — PENDING HUMAN SIGN-OFF

**Render to inspect:** `D:\Projects\vuln-reporting\output\2026-05-13_16-09_test_pull\board_summary\board_summary.pdf`

Open the PDF and walk through each checkbox. Per user memory `feedback_layout_fixes`, WeasyPrint flex/CSS bugs are real and the structural baseline cannot catch them — only this visual inspection can.

- [ ] **Header band visible on EVERY page** with title + scope subtitle (white text on `#1a2332` dark navy). Title-only header is the expected default (no logo, per D-03).
- [ ] **Footer LEFT on EVERY page:** `Confidential` (or this group's privacy_label override).
- [ ] **Footer RIGHT on EVERY page:** UTC timestamp matching `YYYY-MM-DD HH:MM UTC` format.
- [ ] **Footer CENTER, Page 1 (cover):** EMPTY (no `Page 1 of N`).
- [ ] **Footer CENTER, Pages 2+:** `Page N of M`.
- [ ] **Cover body (page 1 only):** scope subtitle (value-only — `Production` / `All assets`), then "Risk Status Summary" header, then the RAG strip. NO inline title repeat, NO `Generated:` line, NO `Sections:` line, NO `<hr>` divider, NO `.cover-meta` wrapper.
- [ ] **RAG strip parity vs v1.0:** cell count, cell colors, cell labels, and headline values match v1.0. Compare against an archived pre-Phase-6 PDF or screenshot.
- [ ] **Sanity sweep:** flip through every page. No orphaned blank pages. Page numbering on pages 2+ is monotonic. The header band does not bleed into module content.

**To approve:** when all 8 checks pass, reply `approved` and update this SUMMARY's Status line from PENDING to PASSED with the operator's name + timestamp.

**If any check fails:** describe the visual regression and which prior plan needs revisiting — typically 06-01 (chrome injection bugs), 06-02 (cover body bugs), 06-03 (PdfChromeConfig field bugs), 06-04 (run_group threading bugs).

## Files Created/Modified

- `tests/baselines/board_summary_test_pull.json` — regenerated (byte-identical to prior; no commit)
- `tests/baselines/board_summary_test_pull_analyst_off.json` — regenerated (byte-identical to prior; no commit)
- `tests/baselines/board_summary_test_pull_zero_match.json` — regenerated (byte-identical to prior; no commit)
- `output/2026-05-13_16-09_test_pull/board_summary/board_summary.pdf` — fresh render for operator UAT (output artifact, not committed)

## Decisions Made

- **Skipped Task 5 commit (empty diff).** The wipe-then-regen produced byte-identical JSON because the Phase 6 cover redesign changed none of the fields captured by the structural baseline. Forcing an empty commit would be misleading; instead this SUMMARY documents that the regen path was exercised end-to-end and the bytes happened to match. CHROME-INT-03 ("0 structural drift after re-baseline") is satisfied: 0 drift was confirmed by the Task 6 re-run.
- **Visual UAT remains PENDING.** Per the executor objective and the threat register (T-06-08 — Repudiation: "we re-baselined and shipped without visual UAT"), this SUMMARY explicitly does NOT claim UAT pass on the operator's behalf. The checklist above is the resume signal.

## Deviations from Plan

### Deferred — Not Auto-Fixed

**1. [Plan deviation — Task 5 commit] Skipped baseline commit because diff was empty**
- **Found during:** Task 5 (Commit regenerated baselines)
- **Issue:** `git add tests/baselines/...` followed by `git commit` reported `nothing to commit, working tree clean` — the wipe-then-regen produced byte-identical JSON.
- **Why this is correct (not a bug):** RESEARCH.md Finding 3 establishes that the structural baseline captures only counts/keys/CIDs and NOT cover text or layout. Phase 6's redesign trims cover body markup but does not change page count (still 5), RAG cell count (still 4), bundle keys, excel tab names, panel CIDs, or the `Risk Status Summary` marker. So byte-identical regen is the expected outcome.
- **Action:** documented this outcome in this SUMMARY rather than forcing an empty commit. The regen workflow (wipe → smoke auto-init → 0-drift re-run) WAS exercised end-to-end and the auto-init `BASELINE INITIALIZED` banners did print, satisfying the plan's intent.
- **Files modified:** none committed
- **Verification:** Task 6 smoke re-run exit code 0, all 3 scenarios `OK`.

---

**Total deviations:** 1 (Task 5 commit deferred as empty-diff per RESEARCH.md Finding 3)
**Impact on plan:** None — CHROME-INT-03 is satisfied and the regen path was exercised. CHROME-COMPAT-02 unchanged (no `privacy_label` field added to `delivery_config.yaml`).

## Issues Encountered

- None blocking. The empty-diff Task-5 commit is the only flow deviation, addressed above.

## CHROME-COMPAT-02 verification

`delivery_config.yaml` was NOT modified in this plan and continues to validate without a `privacy_label` field (default `Confidential` falls back via the chrome config defaults wired in 06-03 / 06-04).

## User Setup Required

None.

## Next Phase Readiness

- **Phase 6 / v1.1 PDF Chrome Redesign milestone close:** PENDING operator visual UAT sign-off on the checklist above.
- Once UAT is `approved`, Phase 6 closes (5/5 plans complete) and v1.1 milestone reaches 9/9 plans.

## Self-Check: PASSED

- Created file `.planning/phases/06-cover-redesign-board-summary-integration/06-05-SUMMARY.md` ✅
- All 3 baseline JSONs exist and are valid ✅
- Smoke re-run exit code 0 ✅
- Rendered PDF exists at the documented path ✅
- No source code modified (Phase 6 code freeze respected) ✅

---
*Phase: 06-cover-redesign-board-summary-integration*
*Plan: 05*
*Automation completed: 2026-05-13*
*Visual UAT: PENDING operator*
