---
phase: quick-260611-b1x
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - reports/owner_supplemental.py
  - tests/unit/test_owner_supplemental.py
  - .planning/v1.3-MILESTONE-AUDIT.md
autonomous: true
requirements: [WR-13-01, WR-13-02, DOC-AUDIT-CORRECT]
must_haves:
  truths:
    - "A duplicate asset_uuid carrying two different Owner/Application tags is counted as exactly ONE physical asset in the Asset Count column (no phantom asset_count=1/open_count=0 row)"
    - "The Asset Count column total equals the count of distinct physical asset_uuids"
    - "_build_owner_app_df runs with no ChainedAssignmentError FutureWarning under pytest -W error::FutureWarning"
    - "open_count is a proper int column (not float64-with-NaN)"
    - "v1.3-MILESTONE-AUDIT.md marks Phase-12 WR-01 already-closed (cite commit 71207e6 + test_fixed_state_nat_last_fixed_excluded) and the two Phase-13 owner_supplemental items RESOLVED by 260611-b1x"
  artifacts:
    - path: "reports/owner_supplemental.py"
      provides: "Deduped asset-count aggregation + F-DTYPE-compliant open_count cast"
    - path: "tests/unit/test_owner_supplemental.py"
      provides: "Two new TDD regression tests (dup-uuid asset count, CoW/no-warning int)"
    - path: ".planning/v1.3-MILESTONE-AUDIT.md"
      provides: "Corrected tech-debt status: all 3 items closed"
  key_links:
    - from: "reports/owner_supplemental.py::_build_owner_app_df"
      to: "enriched.drop_duplicates(\"asset_uuid\")"
      via: "dedup BEFORE asset_counts groupby"
      pattern: "drop_duplicates\\(\"asset_uuid\"\\)"
---

<objective>
Close the two genuinely-open v1.3 owner_supplemental tech-debt items deferred by the v1.3 milestone audit, each with a TDD regression test, plus correct the now-stale audit doc.

Purpose: The two defects (Asset Count over-count on duplicate asset_uuid; pandas-3.0 CoW chained assignment on the open_count cast) are correctness refinements flagged BEFORE v1.4 report modules consume this path. The audit doc is also stale: it still lists Phase-12 WR-01 (open_findings_at NaT-FIXED) as open even though that was fixed and regression-tested in commit 71207e6.

Output: Two code fixes in `reports/owner_supplemental.py::_build_owner_app_df`, two new tests in `tests/unit/test_owner_supplemental.py`, and a corrected `.planning/v1.3-MILESTONE-AUDIT.md`.

SCOPE GUARD: Do NOT modify `utils/open_count.py` — the Phase-12 WR-01 NaT-FIXED finding is ALREADY fixed (clause 1 `(st == "FIXED")` at lines ~108-112) and regression-tested. The doc task only CORRECTS the audit's description of it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

@reports/owner_supplemental.py
@tests/unit/test_owner_supplemental.py
@reports/modules/board_report_utils.py

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase. No exploration required. -->

reports/modules/board_report_utils.py::extract_owner(assets_df, ...) -> pd.DataFrame
  - Returns a COPY of assets_df with `owner` and `application` columns appended.
  - ONE row per input asset row; multi-valued tags joined into a single "; "-joined string (NOT exploded).
  - Therefore the only source of duplicate (owner, application) attribution is duplicate asset_uuid ROWS in the source assets_df.

reports/owner_supplemental.py::_build_owner_app_df(assets_df, vulns_df, report_date=None) -> pd.DataFrame
  - Returns columns: owner, application, open_count (int), asset_count (int).
  - asset_counts (lines ~100-105): enriched.groupby(["owner","application"], dropna=False)["asset_uuid"].nunique()
    — built from the RAW (un-deduped) enriched frame → the bug.
  - The open-count path ALREADY deduplicates (lines ~119-123):
        uuid_to_owner = enriched[["asset_uuid","owner","application"]].drop_duplicates("asset_uuid").set_index("asset_uuid")
    — keep-first owner/application per uuid (the CR-01 pattern). FIX 1 mirrors this for the asset-count path.
  - line ~139: result["open_count"] = result["open_count"].fillna(0).astype(int)  — the ONLY production line that fires ChainedAssignmentError FutureWarning.

CLAUDE.md F-DTYPE convention (see board_report_utils.py lines ~496-504 for the canonical example):
  - Prefer `.assign(col=...)` (replaces column on a fresh frame, bypasses CoW chained-assignment + preserves int dtype).
  - `.loc[:, col] = ...` is the secondary acceptable form. Bare `df[col] = df[col]...` after a merge is forbidden (chains through the tracked parent frame).
  - Use `.assign()` here to match the existing style in this codebase's merge-then-cast pattern.

tests/unit/test_owner_supplemental.py patterns to mirror:
  - `_REF` UTC anchor + `_ts(days_ago)` helper.
  - `_assets_df_with_dup_uuid()` already builds a duplicate-asset_uuid frame (rows 0/1 share "dup-uuid" with different Owner tags). Reuse or mirror it.
  - `_vulns_df_simple(asset_uuids)` builds genuinely-open findings.
  - Tests read back the CSV via csv.DictReader on result["supplemental_csv"] to assert per-owner counts.
  - pytestmark = pytest.mark.unit at module top.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD — fix Asset Count over-count on duplicate asset_uuid (Phase-13 WR-01)</name>
  <files>tests/unit/test_owner_supplemental.py, reports/owner_supplemental.py</files>
  <behavior>
    - RED first: add a test (e.g. `test_dup_uuid_asset_count_counts_once`) mirroring existing patterns.
      Build an assets_df with a duplicate asset_uuid whose two rows carry DIFFERENT Owner/Application tags
      (reuse/adapt `_assets_df_with_dup_uuid()`), plus at least one genuinely-distinct asset.
      Call write_owner_supplemental (no report_date needed) and read back the CSV.
      Assert: (a) no phantom row with asset_count=1 AND open_count=0 for the second owner;
      (b) sum of the Asset Count column == number of DISTINCT physical asset_uuids in the input
      (e.g. 2 distinct uuids → total asset_count == 2, not 3).
      This test FAILS before the fix because the un-deduped enriched frame counts "dup-uuid"
      under both "First Owner" and "Second Owner".
  </behavior>
  <action>
    Write the failing test FIRST and confirm it fails (RED). Then fix `_build_owner_app_df` in
    reports/owner_supplemental.py: dedup `enriched` on `asset_uuid` (keep-first owner/application)
    BEFORE the `asset_counts` groupby at lines ~100-105, mirroring the existing CR-01 keep-first
    pattern at lines ~119-123. Build a single deduped frame once and use it for BOTH the asset-count
    groupby and the existing uuid_to_owner index so one physical asset_uuid contributes to exactly
    ONE (owner, application) row — the same attribution the open-count path already uses, making the
    Asset Count and Open Findings columns consistent. Surgical change only: do not alter the
    open-count logic semantics, the empty-data guard, or the public signature. Keep type hints.
  </action>
  <verify>
    <automated>python -m pytest tests/unit/test_owner_supplemental.py -x -q</automated>
  </verify>
  <done>New dup-uuid asset-count test passes (and pre-existing tests still pass); Asset Count total equals distinct physical asset_uuids; no phantom asset_count=1/open_count=0 row.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TDD — fix pandas-3.0 CoW chained assignment on open_count cast (Phase-13 WR-02/CoW)</name>
  <files>tests/unit/test_owner_supplemental.py, reports/owner_supplemental.py</files>
  <behavior>
    - RED first: add a regression test (e.g. `test_open_count_no_chained_assignment_warning`) that
      runs _build_owner_app_df (or write_owner_supplemental) on a normal non-empty fixture under
      strict FutureWarning. Use a filterwarnings("error", category=FutureWarning) context (e.g.
      warnings.catch_warnings + simplefilter, or @pytest.mark.filterwarnings) so a
      ChainedAssignmentError FutureWarning raises. Assert the call completes without raising, and
      assert the returned open_count column is integer dtype (pd.api.types.is_integer_dtype) with no
      NaN. This FAILS before the fix because line ~139 `result["open_count"] = ...` chains through
      the merge's tracked parent frame and emits the FutureWarning.
  </behavior>
  <action>
    Write the failing test FIRST and confirm it fails (RED). Then convert line ~139
    `result["open_count"] = result["open_count"].fillna(0).astype(int)` to the CLAUDE.md F-DTYPE
    convention using `.assign(open_count=result["open_count"].fillna(0).astype(int))` to match the
    canonical merge-then-cast pattern in board_report_utils.py (lines ~496-504). Surgical change only:
    touch just that line. Do NOT touch lines 128/129 — they do NOT warn (confirmed); changing them is
    out of scope. After the fix, run the full unit suite with `-W error::FutureWarning` and confirm
    owner_supplemental.py no longer appears as a warning source.
  </action>
  <verify>
    <automated>python -m pytest tests/unit/test_owner_supplemental.py -q -W error::FutureWarning</automated>
  </verify>
  <done>New CoW regression test passes; full owner_supplemental suite passes under -W error::FutureWarning; open_count is int dtype with no NaN.</done>
</task>

<task type="auto">
  <name>Task 3: Correct v1.3-MILESTONE-AUDIT.md tech-debt status</name>
  <files>.planning/v1.3-MILESTONE-AUDIT.md</files>
  <action>
    Update the audit doc to reflect that all 3 tech-debt items are now closed. NO code changes.
    1. Phase-12 WR-01 (NaT-FIXED) is STALE/already-closed: the entry cites utils/open_count.py:82-88
       and claims the FIXED/NaT row "escapes all three fixed-mask clauses." That is no longer true —
       clause 1 is an unconditional `(st == "FIXED")` at lines ~108-112, committed in 71207e6, proven
       by `tests/unit/test_open_count.py::test_fixed_state_nat_last_fixed_excluded`. Rewrite this
       entry (both the frontmatter `tech_debt` block AND the prose "Tech Debt" section) to mark it
       ALREADY-CLOSED with the correct line ref (~108-112), cite commit 71207e6, and name the test.
    2. Mark the two Phase-13 owner_supplemental items (asset-count over-count; pandas-3.0 CoW) as
       RESOLVED by quick task 260611-b1x (both fixed in reports/owner_supplemental.py::_build_owner_app_df
       with TDD regression tests in tests/unit/test_owner_supplemental.py).
    3. Keep frontmatter coherent: update `status:` away from `tech_debt` (e.g. to `passed`/`closed`)
       and either empty the `tech_debt:` items or annotate each with its resolution, so a future
       milestone-close reads the doc correctly. Update the prose "Status" line (line ~31) and the
       "Total: 3 items" summary (line ~132) to match. Surgical edits — do not rewrite unrelated
       sections (Requirements Coverage, E2E Flows, etc.).
  </action>
  <verify>
    <automated>python -c "import re,sys; t=open('.planning/v1.3-MILESTONE-AUDIT.md',encoding='utf-8').read(); assert '71207e6' in t, 'commit cite missing'; assert 'test_fixed_state_nat_last_fixed_excluded' in t, 'test cite missing'; assert '260611-b1x' in t, 'quick-id resolution missing'; assert 'status: tech_debt' not in t, 'frontmatter status still tech_debt'; print('audit doc updated OK')"</automated>
  </verify>
  <done>Audit doc cites commit 71207e6 + test name for Phase-12 WR-01, marks both Phase-13 items resolved by 260611-b1x, and frontmatter status no longer reads tech_debt.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/unit/test_owner_supplemental.py -q -W error::FutureWarning` — all pass, no FutureWarning from owner_supplemental.py.
- `python -m pytest tests/unit/test_open_count.py -q` — untouched, still green (proves no collateral change to the NaT-FIXED path).
- `git diff --stat` shows ONLY reports/owner_supplemental.py, tests/unit/test_owner_supplemental.py, .planning/v1.3-MILESTONE-AUDIT.md modified.
</verification>

<success_criteria>
- Duplicate asset_uuid counted exactly once in Asset Count; column ties out to distinct physical assets and to the deduped Open Findings path.
- open_count cast uses F-DTYPE `.assign()`; no ChainedAssignmentError FutureWarning; open_count is int dtype.
- Two new TDD regression tests added (written RED-first), full suite green.
- Audit doc corrected: Phase-12 WR-01 marked already-closed (commit + test cited); both Phase-13 items marked resolved by 260611-b1x; frontmatter coherent.
- utils/open_count.py NOT modified.
</success_criteria>

<output>
Create `.planning/quick/260611-b1x-close-two-open-v1-3-owner-supplemental-t/260611-b1x-SUMMARY.md` when done
</output>
