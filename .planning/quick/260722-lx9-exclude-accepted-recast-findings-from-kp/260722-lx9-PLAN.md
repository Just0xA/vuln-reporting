---
phase: quick-260722-lx9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - reports/modules/board_report_utils.py
  - reports/modules/__init__.py
  - reports/modules/high_risk_assets_module.py
  - reports/modules/aged_vulns_assets_module.py
  - reports/modules/critical_remediation_sla_module.py
  - reports/board_summary.py
  - tests/test_board_report_utils.py
  - tests/test_kpi_risk_managed_exclusion.py
  - tests/test_board_accepted_recast.py
  - tests/test_phase6_board_summary_chrome.py
  - docs/board_summary_calculations.md
  - CLAUDE.md
autonomous: true
requirements: [QUICK-260722-lx9]

must_haves:
  truths:
    - "Risk-accepted (ACCEPTED) and recast (RECASTED) open findings are excluded from board_summary, management_summary, and composed_report KPI module metrics"
    - "The accepted_recast module renders in the board_summary bundle (PDF section, Excel/analyst tab, RAG-strip entry)"
    - "accepted_recast still reads the FULL vulns_df — it is NOT filtered by exclude_risk_managed"
    - "exclude_risk_managed is empty-safe, missing-column-safe, case-insensitive, and returns a .copy() (Hard Rule 5)"
  artifacts:
    - path: "reports/modules/board_report_utils.py"
      provides: "exclude_risk_managed(df) shared helper"
      contains: "def exclude_risk_managed"
    - path: "tests/test_board_report_utils.py"
      provides: "Unit tests for exclude_risk_managed"
      contains: "exclude_risk_managed"
  key_links:
    - from: "reports/modules/high_risk_assets_module.py"
      to: "reports/modules/board_report_utils.py"
      via: "exclude_risk_managed(vulns_df) at top of compute()"
      pattern: "exclude_risk_managed"
    - from: "reports/board_summary.py"
      to: "reports/modules/accepted_recast_module.py"
      via: "ModuleConfig(accepted_recast) in _BOARD_MODULE_CONFIGS + recast_rules_df/trend_snapshots forwarded to ReportComposer"
      pattern: "accepted_recast"
---

<objective>
Two coordinated changes to the modular reporting suite:

Part A — Exclude risk-managed findings (`severity_modification_type` in {ACCEPTED, RECASTED}) from KPI module metrics UNCONDITIONALLY inside compute(). This affects board_summary, management_summary, and composed_report (all consume the shared modules). Intended and approved by the operator.

Part B — Wire the existing `accepted_recast` module into the board_summary bundle so the board surfaces the Accepted/Recast-by-Owner metric (previously management_summary only).

Purpose: Accepted/recast findings remain `state=open` and currently inflate high-risk, aged-vuln, and critical-SLA KPIs — a correctness defect. Board leadership also needs visibility into the risk-managed population.
Output: shared helper + 3 module filters + board wiring + tests + updated baselines/regression + docs.

The approach is DECIDED — do not re-investigate. Implement exactly as tasked.
</objective>

<execution_context>
@/home/jmonroe/.claude/plugins/cache/gsd-plugin/gsd/4.0.2/workflows/execute-plan.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md

<interfaces>
Extracted from codebase — use directly, no exploration needed.

reports/modules/board_report_utils.py — helpers already exported/imported by the three target modules:
    def extract_owner(assets_df, tag_column_name="tags", unassigned_label="Unassigned") -> pd.DataFrame
    def identify_on_time_assets(assets_df, report_date, window_days=30) -> tuple[pd.DataFrame, pd.DataFrame]
    NEW helper to add (co-located here):
    def exclude_risk_managed(df: pd.DataFrame) -> pd.DataFrame

data/fetchers.py — fetch_recast_rules return schema (NO asset_uuid column -> Part B3 scoping is SKIPPED):
    columns: rule_id, rule_name, plugin_id, action, new_severity, original_severity, expires_at, created_at

reports/management_summary.py — the EXACT template for Part B (mirror it):
  - top import: from data.trend_store import capture_snapshot, read_trend
  - deferred fail-soft recast fetch (L337-347): from data.fetchers import fetch_recast_rules inside try/except, recast_rules_df = None on failure
  - trend read (L319-323): read_trend("severity", tag_filter_label, months=13); tag_filter_label = "all_assets" or f"{tag_category}_{tag_value}"
  - ReportComposer construction (L388-397) forwards trend_snapshots=trend_snapshots and recast_rules_df=recast_rules_df

reports/modules/accepted_recast_module.py:
  - REQUIRED_DATA = ["vulns", "assets", "recast_rules", "trend_snapshots"]
  - compute() reads the FULL shared vulns_df — MUST NOT be filtered by exclude_risk_managed
  - Degrades gracefully (MoM omitted) when prior-month trend snapshot absent

board_summary.py anchors: _BOARD_MODULE_CONFIGS L82-87; fetch block L194-206; tag-filter block L211-230 (scoped_uuids); ReportComposer(...) L278-285.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add exclude_risk_managed helper + package export + unit tests</name>
  <files>reports/modules/board_report_utils.py, reports/modules/__init__.py, tests/test_board_report_utils.py</files>
  <behavior>
    - Empty df in -> same df returned (no error)
    - df without "severity_modification_type" column -> returned unchanged (no error)
    - Rows with ACCEPTED and RECASTED (any case: "accepted", "Recasted", "ACCEPTED") -> dropped
    - Rows with "NONE", "", None, or other values -> kept
    - Returned frame is a fresh .copy() (no ChainedAssignmentError under pandas CoW strict mode)
  </behavior>
  <action>
    In reports/modules/board_report_utils.py, add a new function exclude_risk_managed(df: pd.DataFrame) -> pd.DataFrame co-located near extract_owner (in the "Owner extraction" section area, after extract_owner). Body EXACTLY as specified: guard on df.empty or "severity_modification_type" not in df.columns (return df unchanged); otherwise compute the uppercased modification series and return df filtered by ~mod.isin(["ACCEPTED", "RECASTED"]) with a trailing .copy(). Honors Hard Rule 5 (returns .copy(), no chained assignment) and Rule 6 (empty/missing-column guard). Write a NumPy-style docstring matching the repo convention (Parameters/Returns/Notes sections); note it drops risk-managed findings that remain state=open. Add the function name to the module docstring "Shared utilities" bullet list at the top of the file.

    In reports/modules/__init__.py, re-export the helper alongside the existing populate_rag_strip board re-export: extend the "from reports.modules.board_report_utils import ..." line to also import exclude_risk_managed, and add "exclude_risk_managed" to __all__.

    Create tests/test_board_report_utils.py following the repo test convention (see tests/test_accepted_recast_module.py header): set pd.options.mode.copy_on_write = True at module top; synthetic data only (Hard Rule 2 — no real hostnames/IPs/UUIDs/plugin names). Cover all five behaviors above. Verify case-insensitivity explicitly.
  </action>
  <verify>
    <automated>cd /home/jmonroe/projects/vuln-reporting && python -m pytest tests/test_board_report_utils.py -x -q && python -c "from reports.modules import exclude_risk_managed; print('export ok')"</automated>
  </verify>
  <done>exclude_risk_managed exists, is importable from reports.modules package, all unit tests pass, no CoW warnings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply filter in the three KPI modules + per-module exclusion tests</name>
  <files>reports/modules/high_risk_assets_module.py, reports/modules/aged_vulns_assets_module.py, reports/modules/critical_remediation_sla_module.py, tests/test_kpi_risk_managed_exclusion.py</files>
  <behavior>
    - high_risk_assets: synthetic vulns_df containing ACCEPTED/RECASTED rows -> those rows do not contribute to the high-risk asset count/metric
    - aged_vulns_assets: same — ACCEPTED/RECASTED aged findings excluded from the aged-asset metric
    - critical_remediation_sla: ACCEPTED/RECASTED rows in the FIXED population excluded from the SLA computation and analyst "missed SLA" rows
    - accepted_recast module (Task 3 test) still counts ACCEPTED/RECASTED — NOT filtered
  </behavior>
  <action>
    Add exclude_risk_managed to the existing "from reports.modules.board_report_utils import (...)" import block in each of the three modules (high_risk_assets_module.py L37-45, aged_vulns_assets_module.py L36-44, critical_remediation_sla_module.py L53-59).

    high_risk_assets_module.py — in compute() (def at L140), as the FIRST statement inside the try block (right before "# ---- Step 1: derive on-time asset set ----" at L177-178), reassign vulns_df = exclude_risk_managed(vulns_df) so the filtered frame flows through all downstream logic.

    aged_vulns_assets_module.py — identical treatment in compute() (def at L137): vulns_df = exclude_risk_managed(vulns_df) as the first statement in the try block before Step 1 (L174-175).

    critical_remediation_sla_module.py — in compute() (def at L148), the FIXED population is obtained at L187-189 (fixed_vulns_df = kwargs.get("fixed_vulns_df", pd.DataFrame())). Immediately after that assignment add fixed_vulns_df = exclude_risk_managed(fixed_vulns_df). THEN remove the now-redundant local WR-03 exclusion block at L316-318 (the "if severity_modification_type in missed.columns: _smod = ...; missed = missed[~_smod.isin([accepted, recasted])].copy()" three lines). Preserve the surrounding WR-03 explanatory comment (L306-315) and the "missed = fixed[...].copy()" slice and all analyst-rows logic that follows (L319+). Replace the deleted three lines with a single one-line comment noting exclusion now happens upstream via exclude_risk_managed(fixed_vulns_df) at compute() entry.

    Do NOT touch scan_coverage_sla_module.py (assets-only; vulns_df unused). Do NOT touch accepted_recast_module.py.

    Create tests/test_kpi_risk_managed_exclusion.py (synthetic fixtures only, CoW strict mode, Hard Rule 2). For each of the three modules, build a minimal vulns_df/fixed_vulns_df + assets_df where a subset of qualifying findings carry severity_modification_type ACCEPTED/RECASTED, and assert the module's headline count/metric equals the value computed WITHOUT those rows (i.e. they are excluded). Reuse fixture-builder patterns from tests/test_accepted_recast_module.py where convenient.
  </action>
  <verify>
    <automated>cd /home/jmonroe/projects/vuln-reporting && python -m pytest tests/test_kpi_risk_managed_exclusion.py -x -q && grep -l exclude_risk_managed reports/modules/high_risk_assets_module.py reports/modules/aged_vulns_assets_module.py reports/modules/critical_remediation_sla_module.py | grep -c module | grep -qx 3 && ! grep -q 'isin(\["accepted", "recasted"\])' reports/modules/critical_remediation_sla_module.py</automated>
  </verify>
  <done>All three modules call exclude_risk_managed at compute() entry (crit on the fixed df), the redundant WR-03 block is removed, per-module exclusion tests pass, no CoW warnings.</done>
</task>

<task type="auto">
  <name>Task 3: Wire accepted_recast into board_summary + board integration test + fix chrome mock harness</name>
  <files>reports/board_summary.py, tests/test_board_accepted_recast.py, tests/test_phase6_board_summary_chrome.py</files>
  <action>
    Part B1 — In reports/board_summary.py add ModuleConfig("accepted_recast") to _BOARD_MODULE_CONFIGS (L82-87), appended after aged_vulns_assets.

    Part B2 — Add the two extra fetches mirroring management_summary EXACTLY:
      - Add top import: from data.trend_store import read_trend (board only needs the reader).
      - Derive tag_filter_label: default "all_assets"; inside the existing tag-filter branch (L211-230) set tag_filter_label = f"{tag_category}_{tag_value}" (mirror management_summary L262/L312).
      - After the fetch block (after fixed_vulns_df fetch, ~L201-206) and after tag scoping, add a trend read: trend_snapshots = read_trend("severity", tag_filter_label, months=13) with an info log mirroring management_summary L324-329. read_trend reads the LOCAL trend store (not Tenable) — Hard Rule 1 safe.
      - Add the deferred fail-soft recast fetch mirroring management_summary L337-347: recast_rules_df = None; try: from data.fetchers import fetch_recast_rules; recast_rules_df = fetch_recast_rules(tio, cache_dir); except Exception: log non-fatal and keep None.
      - Forward both into the ReportComposer(...) construction (L278-285): add trend_snapshots=trend_snapshots and recast_rules_df=recast_rules_df kwargs (composer fans them out via **kwargs; only accepted_recast consumes them).

    Part B3 — Tag-scope filter for recast_rules_df: fetch_recast_rules returns NO asset_uuid column (schema: rule_id, rule_name, plugin_id, action, new_severity, original_severity, expires_at, created_at). Therefore SKIP per-asset scoping of recast_rules_df. Add a one-line comment at the recast fetch site stating recast rules carry no asset_uuid, so they are not tag-scoped (consistent with management_summary, which also does not scope them).

    Board integration test — create tests/test_board_accepted_recast.py. Assert accepted_recast is present in _BOARD_MODULE_CONFIGS. Then run the board compute path for the accepted_recast module with synthetic vulns/assets/recast_rules/trend fixtures (Hard Rule 2) and assert it produces: a non-empty render_pdf_section, at least one analyst/excel tab (render_analyst_tabs or render_excel_tabs non-empty), and a populated rag_strip entry (label + rag_color present, not the gray "No Data" default). Follow the four-channel assertion style in tests/test_accepted_recast_module.py.

    Chrome mock harness — tests/test_phase6_board_summary_chrome.py::_invoke_and_capture_pdf_chrome (L32-71) patches only fetch_all_vulnerabilities/fetch_all_assets/fetch_fixed_vulnerabilities/ReportComposer/_render_pdf. Board now also calls read_trend (module-level import bs.read_trend) and fetch_recast_rules (deferred import). Add patch.object(bs, "read_trend", return_value={"snapshots": [], "insufficient_data": True}) to the with-block so the harness stays hermetic (no real trend-store read). The deferred fetch_recast_rules is wrapped in try/except and returns an empty df with no credentials, so it is already safe; no patch strictly required, but if the test env raises, patch data.fetchers.fetch_recast_rules to return an empty df. Confirm the existing chrome assertions still pass.
  </action>
  <verify>
    <automated>cd /home/jmonroe/projects/vuln-reporting && python -m pytest tests/test_board_accepted_recast.py tests/test_phase6_board_summary_chrome.py -x -q && grep -q 'ModuleConfig("accepted_recast")' reports/board_summary.py && grep -q 'recast_rules_df' reports/board_summary.py && grep -q 'trend_snapshots' reports/board_summary.py</automated>
  </verify>
  <done>Board bundle includes accepted_recast producing all four channels + RAG strip; recast_rules_df and trend_snapshots fetched fail-soft and forwarded; chrome tests green.</done>
</task>

<task type="auto">
  <name>Task 4: Regression — update management_summary / composed_report expected values; flag board baselines for operator regeneration</name>
  <files>tests/test_management_summary.py, tests/fixtures/management_summary_parity, tests/baselines/board_summary_test_pull.json</files>
  <action>
    The shared modules (high_risk_assets, aged_vulns_assets, critical_remediation_sla) now exclude ACCEPTED/RECASTED. Any management_summary or composed_report test that pins expected KPI VALUES for those modules against a fixture containing risk-managed rows WILL shift. Do NOT merely re-run — update the baselines to the NEW CORRECT numbers.

    Steps:
      1. Run the full suite to enumerate breakage: python -m pytest tests/ -q. Focus on failures in test_management_summary.py (value_golden parity), test_composed_report_*.py, and any *_module tests for the three affected modules.
      2. For each genuine value shift caused by the exclusion, recompute the correct expected value BY HAND from the synthetic fixture (identify which fixture rows carry ACCEPTED/RECASTED and subtract their contribution), and update the golden/expected assertion. Add a short comment at each updated assertion: "value shifted by risk-managed exclusion (quick-260722-lx9)".
      3. management_summary golden lives in tests/fixtures/management_summary_parity and/or tests/baselines/management_summary_value_golden.json — update only the metrics driven by the three affected modules; do NOT touch unrelated metric buckets.
      4. IMPORTANT — board structural baselines (tests/baselines/board_summary_test_pull.json, board_summary_test_pull_analyst_off.json, board_summary_test_pull_zero_match.json) were captured from REAL Tenable-exported parquet (see tests/test_board_summary_baseline.py header) and now have one MORE module (accepted_recast page/tab/RAG entry). These CANNOT be regenerated inside Claude Code (Hard Rule 1 — no live pulls). test_board_summary_baseline.py is a self-consistency/tamper-evidence test and will still pass, but the operator's smoke_board_summary_cutover.py will diff on the next live run. Do NOT edit these JSON files. Instead, record in the SUMMARY a clear FLAG: "Operator must regenerate the three board_summary structural baselines via smoke_board_summary_cutover.py against warmed parquet cache after this change (added accepted_recast module)."
      5. Re-run the full suite and confirm green (excepting nothing — all must pass).
  </action>
  <verify>
    <automated>cd /home/jmonroe/projects/vuln-reporting && python -m pytest tests/ -q</automated>
  </verify>
  <done>Full test suite passes; management_summary/composed_report expected values updated to post-exclusion numbers with traceability comments; board structural-baseline regeneration flagged for the operator in the SUMMARY (files not edited).</done>
</task>

<task type="auto">
  <name>Task 5: Docs — board_summary_calculations.md + CLAUDE.md slug row</name>
  <files>docs/board_summary_calculations.md, CLAUDE.md</files>
  <action>
    docs/board_summary_calculations.md — add a new section documenting the Accepted/Recast-by-Owner metric now included in the board bundle (mirror the depth of the existing metric sections; reference accepted_recast_module.py and note counts are cut by the Owner tag category via extract_owner, ACCEPTED and RECASTED tracked separately, MoM delta cold-starts when the prior-month trend snapshot is absent). ALSO add a short "Exclusion of risk-managed findings" note explaining that scan_coverage_sla is unaffected (assets-only) but high_risk_assets, aged_vulns_assets, and critical_remediation_sla now exclude ACCEPTED/RECASTED findings via exclude_risk_managed(), and that accepted_recast intentionally does NOT apply that filter.

    CLAUDE.md — in the "Report Scripts — Slug Index" table, update the board_summary row Notes cell from "4 board KPIs; modules-based. See docs/board_summary_calculations.md" to reflect 5 modules (e.g. "5 board metrics; modules-based. Incl. Accepted/Recast-by-Owner. See docs/board_summary_calculations.md"). Surgical single-cell edit — do not touch other rows.
  </action>
  <verify>
    <automated>cd /home/jmonroe/projects/vuln-reporting && grep -qi "accepted/recast\|accepted_recast" docs/board_summary_calculations.md && grep -qi "exclude_risk_managed\|risk-managed" docs/board_summary_calculations.md && grep -q "5 board" CLAUDE.md</automated>
  </verify>
  <done>board_summary_calculations.md documents the new metric and the exclusion behavior; CLAUDE.md slug-index row reflects 5 modules.</done>
</task>

</tasks>

<verification>
- python -m pytest tests/ -q  (full suite green)
- No live Tenable pulls anywhere (Hard Rule 1): tests use synthetic fixtures; read_trend reads the local store; fetch_recast_rules is fail-soft and returns empty without creds. Any manual end-to-end check uses run_all.py --dry-run / warmed parquet cache only.
- pandas CoW strict mode (Hard Rule 5): exclude_risk_managed returns .copy(); no ChainedAssignmentError in new/changed code.
- Empty-data guard (Hard Rule 6): helper guards empty df and missing column.
- Synthetic data only (Hard Rule 2): all new fixtures use 00000000-... UUIDs and non-real names.
- Zero new dependencies (Hard Rule 8).
- Severity tiering (Hard Rule 4) untouched — this filters rows only.
</verification>

<success_criteria>
- exclude_risk_managed added, exported, unit-tested (Task 1).
- high_risk_assets, aged_vulns_assets, critical_remediation_sla exclude ACCEPTED/RECASTED at compute() entry; redundant WR-03 block removed; per-module tests pass (Task 2).
- accepted_recast renders in the board bundle (4 channels + RAG strip); recast_rules_df + trend_snapshots fetched fail-soft and forwarded; chrome tests green (Task 3).
- management_summary/composed_report expected values updated to post-exclusion numbers; board structural baselines flagged for operator regeneration (Task 4).
- Docs updated (Task 5).
- Full pytest suite passes.
</success_criteria>

<output>
Create .planning/quick/260722-lx9-exclude-accepted-recast-findings-from-kp/260722-lx9-SUMMARY.md when done. In the SUMMARY, include the explicit operator FLAG that the three board_summary structural baseline JSON files must be regenerated via smoke_board_summary_cutover.py against warmed parquet after this change.
</output>
