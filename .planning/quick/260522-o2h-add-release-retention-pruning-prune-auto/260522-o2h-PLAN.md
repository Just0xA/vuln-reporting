---
phase: quick-260522-o2h
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/update_from_github.sh
  - DEPLOYMENT.md
autonomous: true
requirements: [QUICK-260522-o2h]
must_haves:
  truths:
    - "`--prune` deletes old release dirs, keeping the N newest (default 3, --keep N overrides)"
    - "The active release (current target) and the .last breadcrumb target are never deleted, even outside the keep-N window"
    - "A successful install auto-prunes; a failed/rolled-back install never reaches the prune hook"
    - "Prune failures log a WARNING and never abort (best-effort)"
    - "--prune and --keep N appear in --help"
  artifacts:
    - path: scripts/update_from_github.sh
      provides: "prune_releases helper, cmd_prune, --prune/--keep parsing, auto-prune hook"
      contains: "prune_releases"
    - path: DEPLOYMENT.md
      provides: "Documentation for --prune, default keep-3, --keep N, auto-prune on success"
      contains: "--prune"
  key_links:
    - from: "cmd_install (post-health-check, happy path)"
      to: "prune_releases ${RELEASE_RETENTION}"
      via: "best-effort call before log_completed success"
      pattern: "prune_releases"
    - from: "cmd_prune"
      to: "prune_releases"
      via: "shared single implementation"
      pattern: "prune_releases"
---

<objective>
Add release retention/pruning to `scripts/update_from_github.sh`. Old per-release dirs under `${INSTALL_ROOT}/releases/<tag>/` accumulate forever (each carrying a heavy `.venv`). Add a `--prune` command and an auto-prune hook on successful installs, keeping the N most recent releases (default 3).

Purpose: Bound disk usage without operator intervention while never deleting the active release or the rollback target.
Output: Modified `update_from_github.sh` (one `prune_releases` helper shared by `cmd_prune` and the auto-prune hook) + documented `--prune` in DEPLOYMENT.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@scripts/update_from_github.sh
@DEPLOYMENT.md

<interfaces>
Existing helpers/patterns to reuse (do NOT reinvent):
- `log_line "msg"` — timestamped line to update.log; write failures never propagate.
- `log_completed "status"` / `log_started` — terminal status logging.
- `usage_error "reason"` — prints to stderr, logs, exits 2 (the existing usage-error code). Reuse for bad --keep.
- Constants block (~line 33): INSTALL_ROOT, LOG_FILE, SCRIPT_NAME, PYTHON_BIN, IN_AUTO_ROLLBACK. Add RELEASE_RETENTION here.
- parse_args() (~line 199): sets CMD/VERSION/FORCE/SKIP_RESTART via a while/case loop. Add KEEP var + --prune + --keep N.
- cmd_list() dir-filtering (~line 661): `for d in "${releases_dir}"/*/; do [[ -d "$d" ]] || continue; ...` and `sort -V`. Mirror for enumeration.
- cmd_rollback() inside-releases guard (~line 825): `case "$target" in "${INSTALL_ROOT}/releases/"*) ;; *) refuse ;; esac`. Mirror before any rm -rf.
- cmd_install() happy path (~lines 770-782): after health_check passes, prints Rollback one-liner, cleans tempdir, `log_completed "success"`. Insert auto-prune just before `log_completed "success"`.
- auto_rollback() always exits 12/13 (never returns) — so the auto-prune hook is unreachable on failure paths by construction.
- main() dispatch case (~line 914): add `prune) cmd_prune ;;`.
- print_usage() (~line 151): Flags section.
- on_exit trap cleanup style: `rm -rf "$X" || true`. Mirror for best-effort deletes.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add prune_releases helper, cmd_prune, and flag parsing</name>
  <files>scripts/update_from_github.sh</files>
  <action>
Implement release retention in `update_from_github.sh`. Match the script's existing bash style: `set -euo pipefail` (no `set +e` regions), `local` vars, `if`/`||` guards, the script's quoting, and `log_line`/`log_completed`.

1. Constants block (~line 33-43): add `RELEASE_RETENTION="${RELEASE_RETENTION:-3}"` near the other constants with a one-line comment ("default # of releases --prune and auto-prune keep").

2. parse_args() (~line 199): add `KEEP=""` to the initializer block alongside CMD/VERSION/FORCE/SKIP_RESTART. Add two new case arms:
   - `--prune)` — set CMD="prune" with the same conflicting-command guard pattern the other command flags use; `shift`.
   - `--keep)` — require a following value (`[[ $# -lt 2 ]]` → usage_error "missing value after --keep (expected a positive integer)"); validate the value is a positive integer with `[[ "$2" =~ ^[1-9][0-9]*$ ]]` else `usage_error "invalid --keep value '$2' (expected a positive integer)"` (usage_error already exits 2); set `KEEP="$2"; shift 2`.

3. main() orthogonal-flag rejection (~lines 891-897): add a guard that `--keep` is only meaningful with `--prune` (mirror the existing --force/--skip-restart guards): if KEEP is non-empty and CMD != "prune", `usage_error "--keep is only meaningful with --prune"`.

4. Add a single `prune_releases <keep_n>` helper (place it near the swap/breadcrumb helpers, e.g. after write_breadcrumb ~line 569, since cmd_install and cmd_prune both call it). Behavior:
   - `local keep_n="$1"; local releases_dir="${INSTALL_ROOT}/releases"`.
   - Resolve the protected set: active = readlink of `${INSTALL_ROOT}/current` normalized to an absolute `${INSTALL_ROOT}/releases/<tag>` path (mirror the bare-basename→absolute `case` promotion used in cmd_install ~line 737); and last_target = contents of `${INSTALL_ROOT}/releases/.last` if the file exists and is non-empty, normalized the same way. Reduce both to basenames for comparison against enumerated dir names.
   - Enumerate release dirs ONLY (mirror cmd_list ~line 661): `for d in "${releases_dir}"/*/; do [[ -d "$d" ]] || continue; entries+=("$(basename "$d")"); done`. This naturally excludes the `.last` file and any non-dir entries.
   - If `${#entries[@]} -le keep_n`: log_line "prune: N releases <= keep K; nothing to remove", print a one-line summary, return 0.
   - Sort with `sort -V` (semver-aware, same as cmd_list). The newest `keep_n` are kept; the remainder are deletion candidates.
   - For each deletion candidate: skip (preserve) if its basename equals the active basename OR the last_target basename — log_line "prune: preserving <tag> (active/rollback target)". Track kept vs removed tags.
   - Before each `rm -rf`, re-assert the inside-releases guard (mirror cmd_rollback ~line 825): build the absolute path `${releases_dir}/<tag>`, and `case` it against `"${INSTALL_ROOT}/releases/"*`; if it does not match, log_line a WARNING and skip (never rm outside releases/).
   - Delete best-effort, non-fatal: `rm -rf "$path" || { log_line "WARNING: prune failed to remove $path"; continue; }`. log_line each successful deletion.
   - Print a concise stdout summary: kept count + tags and removed count + tags (e.g. "pruned: kept 3 (v1.2.0 v1.2.1 v1.2.2), removed 1 (v1.1.0)").
   - The helper must `return 0` on all paths so an auto-prune call can never fail an otherwise-successful install. Do NOT relax `set -e` with `set +e`; rely on per-command `|| ...` guards.

5. Add `cmd_prune()`: `local keep_n="${KEEP:-$RELEASE_RETENTION}"; log_line "prune requested (keep=${keep_n})"; prune_releases "$keep_n"; log_completed "success"`.

6. main() dispatch case (~line 914): add `prune)    cmd_prune    ;;`.

Surgical: do not touch unrelated code, comments, or formatting. Do NOT add a new exit code — --keep validation reuses usage_error's exit 2; leave the header exit-code comment block (~lines 9-25) unchanged.
  </action>
  <verify>
    <automated>bash -n scripts/update_from_github.sh && grep -q 'RELEASE_RETENTION' scripts/update_from_github.sh && grep -q 'prune_releases' scripts/update_from_github.sh && grep -q 'cmd_prune' scripts/update_from_github.sh && grep -q 'prune)' scripts/update_from_github.sh</automated>
  </verify>
  <done>`bash -n` passes (syntax clean); `--prune` and `--keep N` parse; `prune_releases` is a single helper; inside-releases guard re-asserted before every rm -rf; active + .last targets always preserved; deletions best-effort/non-fatal and logged; no new exit code added.</done>
</task>

<task type="auto">
  <name>Task 2: Wire auto-prune into cmd_install, add --help and DEPLOYMENT.md docs</name>
  <files>scripts/update_from_github.sh, DEPLOYMENT.md</files>
  <action>
1. cmd_install() (~lines 770-782): insert the auto-prune call on the HAPPY PATH only — after the post-swap health check has passed and after the Rollback one-liner / tempdir cleanup, immediately BEFORE `log_completed "success"` (~line 782). Call `prune_releases "$RELEASE_RETENTION"`. Confirm by inspection that this point is unreachable on failure/auto-rollback paths: auto_rollback() always exits (12/13) and never returns, and pre-swap failures exit earlier — so no extra guard is needed. Add a one-line comment noting it is best-effort and only on full success.

2. print_usage() (~line 159 Flags block): add two flag entries matching the existing two-space-indented style:
   - `--prune` — "Delete old release dirs, keeping the ${RELEASE_RETENTION} most recent (default). The active release and the rollback (.last) target are always preserved. Successful --version installs auto-prune."
   - `--keep <N>` — "(Only with --prune) keep the N most recent releases instead of the default. N must be a positive integer."
   Also add `${SCRIPT_NAME} --prune` and `${SCRIPT_NAME} --prune --keep 5` to the Examples block. Optionally add `--prune` to the top Usage synopsis line (~line 153). Do not alter unrelated help text.

3. DEPLOYMENT.md (~lines 381-392, the "Additional flags" block): add a surgical entry matching the existing style — a bash example plus one-line prose:
   ```bash
   # Prune old releases, keeping the 3 most recent (active + rollback target always kept)
   sudo /opt/vuln-reporting/current/scripts/update_from_github.sh --prune

   # Override the retention count
   sudo /opt/vuln-reporting/current/scripts/update_from_github.sh --prune --keep 5
   ```
   Add a short note: successful `--version` installs auto-prune to the default retention (keep 3); the active release and the `.last` rollback target are never deleted; prune is best-effort and never fails an install. Match the surrounding tone; do not restructure the section.
  </action>
  <verify>
    <automated>bash -n scripts/update_from_github.sh && grep -q 'prune_releases "\$RELEASE_RETENTION"' scripts/update_from_github.sh && grep -q -- '--prune' DEPLOYMENT.md && grep -c -- '--prune' scripts/update_from_github.sh</automated>
  </verify>
  <done>Auto-prune call sits on the happy path just before `log_completed "success"` in cmd_install and is provably unreachable on failure paths; `--prune` and `--keep` documented in print_usage (with examples) and in DEPLOYMENT.md's Additional flags section; `bash -n` passes.</done>
</task>

</tasks>

<verification>
- `bash -n scripts/update_from_github.sh` passes (no syntax errors).
- `prune_releases` is the single deletion implementation; both `cmd_prune` and the cmd_install auto-prune hook call it.
- Inside-releases guard (`case ... "${INSTALL_ROOT}/releases/"*`) re-asserted before every `rm -rf`.
- Active release + `.last` target always preserved (verified by reading prune_releases logic).
- Auto-prune hook is on the happy path only, before `log_completed "success"`; auto_rollback never returns so failure paths can't reach it.
- `--keep` non-integer / missing value → exit 2 via usage_error (no new exit code).
- `--prune` and `--keep N` in `--help` and DEPLOYMENT.md.
</verification>

<success_criteria>
- `--prune` keeps the N newest releases (default 3, `--keep N` overrides) and removes the rest, never touching the active or rollback-target release.
- Successful installs auto-prune; failed/rolled-back installs do not.
- All prune deletions are best-effort, logged, with a concise stdout kept/removed summary; a prune failure never aborts.
- Bash style preserved (`set -euo pipefail`, no `set +e`); changes are surgical.
- DEPLOYMENT.md documents the command, default, override, and auto-prune behavior.
</success_criteria>

<output>
Create `.planning/quick/260522-o2h-add-release-retention-pruning-prune-auto/260522-o2h-SUMMARY.md` when done.
</output>
