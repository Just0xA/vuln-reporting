---
phase: quick-260813-ga2
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [BOARD-VARIANT-01]
files_modified:
  - reports/modules/critical_remediation_sla_module.py
  - reports/modules/high_risk_assets_module.py
  - reports/modules/aged_vulns_assets_module.py
  - reports/board_summary.py
  - run_all.py
  - delivery_config.schema.yaml
  - delivery/email_template.py
  - deliveries.d/board.yaml
  - deliveries.d/board_test.yaml
  - deliveries.d/adhoc.yaml
  - tests/test_phase6_run_group_chrome.py
  - tests/test_phase6_board_summary_chrome.py
  - tests/test_board_accepted_recast.py
  - tests/test_kpi_risk_managed_exclusion.py
  - tests/test_board_summary_variants.py
  - docs/board_summary_calculations.md
  - docs/GLOSSARY.md
  - CLAUDE.md
  - RUNBOOK.md

must_haves:
  truths:
    - "A board delivery group listing both slugs produces two PDFs and two main Excel workbooks with distinct filenames."
    - "The existing board_summary slug produces byte-equivalent metric numbers to today (default include_risk_managed=False)."
    - "The new board_summary_incl_risk_managed slug computes Metrics 2/3/4 over the full population including ACCEPTED/RECASTED findings."
    - "The inclusive variant contributes no email panels and no inline images; the excluded variant's body carries a one-line pointer to the inclusive PDF."
    - "run_all.py --dry-run exits 0 with all board deliveries listing two reports."
    - "No ModuleConfig state leaks between successive run_report() calls in one process."
  artifacts:
    - path: "tests/test_board_summary_variants.py"
      provides: "Variant registration, title/filename, option, supplemental-skip, email-emptiness, and no-leak coverage"
    - path: "reports/board_summary.py"
      contains: "_board_module_configs"
  key_links:
    - from: "run_all.py::run_group"
      to: "reports/board_summary.py::run_report"
      via: "include_risk_managed kwarg keyed off slug"
      pattern: "include_risk_managed"
    - from: "reports/board_summary.py::_board_module_configs"
      to: "reports/modules/*_module.py::compute"
      via: "ModuleConfig.options['include_risk_managed']"
      pattern: "config.options.get\\(\"include_risk_managed\""
---

<objective>
Split `board_summary` into two report variants: the existing slug (metrics **excluding**
risk-managed findings — today's behavior, numbers unchanged) and a new
`board_summary_incl_risk_managed` slug (metrics **including** ACCEPTED/RECASTED findings).
Both slugs run in the same board delivery, so one email carries both bundles.

Purpose: the board asked to see the gross view beside the net-of-acceptances view. Today
`exclude_risk_managed()` is applied unconditionally in three of the five board modules and
the board has only ever seen the net view.

Output: a module option `include_risk_managed` (default `False` = today's behavior), a
variant-parameterized `reports/board_summary.py` (no new report file), slug registration in
`run_all.py` + schema + email template, updated operator YAML, docs, and tests.
</objective>

<execution_context>
@/home/jmonroe/.claude/plugins/cache/gsd-plugin/gsd/4.0.2/workflows/execute-plan.md
@/home/jmonroe/.claude/plugins/cache/gsd-plugin/gsd/4.0.2/templates/summary.md
</execution_context>

<context>
@/home/jmonroe/.claude/plans/the-group-that-receives-humble-whisper.md
@CLAUDE.md
@.planning/STATE.md

**The approved plan above is the authoritative spec.** Its design decisions are settled after
two review passes — do not re-litigate them. Two corrections are already folded in and must
be respected:

1. `delivery/email_sender.py:425-434` selects panels with `next(...)` — first non-empty wins —
   and `templates/report_email.html:98-102` has a single panel slot. The INCLUSIVE variant
   therefore returns empty `email_body_html` and empty `email_inline_images`.
   **There is NO composer `cid_namespace` work in this task. Do not add any.**
2. The new slug maps to the EXISTING `reports/board_summary.py` via `_REPORT_MODULE_MAP`.
   **Do NOT create `reports/board_summary_inclusive.py`** — a new file under `reports/`
   calling `fetch_fixed_vulnerabilities` trips the discovery gate at
   `tests/test_consumer_audit.py:566-571`.

Naming is settled: option `include_risk_managed`, slug `board_summary_incl_risk_managed`.

<discovered_during_planning>
Three findings verified against the codebase during planning that the approved plan does
not name. They are implementation constraints, not design changes:

**A. A THIRD test must be co-edited.** `tests/test_board_accepted_recast.py:82` reads
`bs._BOARD_MODULE_CONFIGS` directly:
```
module_ids = [cfg.module_id for cfg in bs._BOARD_MODULE_CONFIGS]
```
Turning that module-level list into a factory breaks it. Co-edit it in Task 2's commit
alongside the two co-edits already named in the constraints.

**B. `get_audit_info()` CANNOT be config-aware.** `reports/modules/base.py:510` defines it as
`def get_audit_info(self) -> dict:` with no `config` parameter, and `registry.py:173` invokes
it on a bare `cls()` instance with no config at all. The approved plan asks for
`critical_remediation_sla_module.py:1069`'s `cohort` string to "follow the option"; the
achievable form that satisfies the intent is to make the string **document the conditional**
statically (see Task 1 action). The per-run auditor-facing truth is carried by the two
config-aware surfaces: the Excel `Scope:` row and the new `risk_managed_scope` metadata key.

**C. `scripts/sanity_vpr_severity_cache.py` cannot be reused as-is for verification.** Its
reference values and `--cache-dir` default target `data/cache/2026-07-01`, which no longer
exists on disk. The only warm cache is `data/cache/2026-08-05/`
(`vulns_all.parquet`, `assets_all.parquet`, `vulns_fixed_365d.parquet`, `recast_rules.parquet`).
Do NOT add it to the pytest run and do NOT "fix" its constants — write the impact check as a
throwaway scratchpad script instead (Task 1).
</discovered_during_planning>

<interfaces>
<!-- Extracted from the codebase. Use directly — no exploration needed. -->

reports/modules/base.py — `ModuleConfig(module_id, options={...})`; `options` is a dict.
Established option-read pattern (`mttr_trend_module.py:359`):
`window_days = int(config.options.get("mttr_window_days", 30))`

reports/board_summary.py:88-98 (to be replaced):
- `_BOARD_MODULE_CONFIGS: list[ModuleConfig]` — 5 entries, literal order:
  `scan_coverage_sla`, `critical_remediation_sla`, `high_risk_assets`, `aged_vulns_assets`,
  `accepted_recast`
- `_REPORT_TITLE = "Board Vulnerability Metrics Summary"`
- `_PDF_FILENAME = "board_summary.pdf"` / `_EXCEL_FILENAME = "board_summary.xlsx"`

reports/board_summary.py consumption points:
- `:316` `effective_title = report_title or _REPORT_TITLE`
- `:331` `module_configs = _BOARD_MODULE_CONFIGS,` (inside `ReportComposer(...)`)
- `:350` `slug = "board_summary",` (inside `composer.run_full_pipeline(...)`)
- `:371-381` owner supplemental block (`write_owner_supplemental`, fail-soft try/except)
- `:389` / `:405` `output_dir / _PDF_FILENAME` / `_EXCEL_FILENAME`
- `:414-454` `result_dict`, incl. `email_body_html`, `email_inline_images`, `_bundle`

run_all.py:
- `_VALID_REPORTS` frozenset `:77-90`
- `_CHROME_AWARE_SLUGS` frozenset `:100-104`
- `_REPORT_MODULE_MAP` dict `:117-130`
- `run_group()` slug-specific kwargs block `:850-856`:
  ```
  if slug == "board_summary":
      report_kwargs["analyst_detail"] = group_config.get("analyst_detail", True)
  ```

Exclusion call sites (all inside `compute()`'s `try:`, all have `config` in scope):
- `critical_remediation_sla_module.py:259-260` — BOTH `vulns_df` and `fixed_vulns_df`
- `high_risk_assets_module.py:183`
- `aged_vulns_assets_module.py:180`
`add_vpr_severity()` runs immediately after each and stays unconditional (Hard Rule 4).

`_build_metadata(report_date)` — `high_risk_assets_module.py:1069` (called `:224`, `:478`);
`aged_vulns_assets_module.py:1089` (called `:221`, `:476`).
`critical_remediation_sla_module.py` has no `_build_metadata()`; its metadata is an inline
dict at `:517` carrying `window` / `critical_sla_days` / `sla_source` / `on_time_scope`.

Warm cache (read with `pd.read_parquet` only — never via `data/fetchers.py`):
`data/cache/2026-08-05/{vulns_all,assets_all,vulns_fixed_365d,recast_rules}.parquet`
</interfaces>
</context>

<hard_rules_for_this_task>
- **Hard Rule 1 — no live Tenable pulls.** Permitted commands: `.venv/bin/python run_all.py --dry-run`,
  pytest, and scratchpad scripts that `pd.read_parquet` the warm cache directly.
  Never import `data.fetchers` or `tenable_client` in a script you run.
- **Hard Rule 2 — synthetic fixtures only.** No real hostnames, IPs, MACs, plugin names, or
  asset UUIDs in any new test data. Reuse the existing `_UUID_PREFIX` / `plugin_id 1000NN`
  conventions from `tests/test_kpi_risk_managed_exclusion.py`.
- **Hard Rule 5 — pandas CoW.** `.assign()` only; never `df["col"] = val` after a filter/slice.
- **Hard Rule 6 — empty-data guard.** The no-data early-return paths must keep working with
  the new metadata parameter.
- Use `.venv/bin/python` for every python and pytest invocation.
- `deliveries.d/` is **gitignored** (`.gitignore:19`) — those YAML edits are local operator
  config, needed for the `--dry-run` gate but not committed. Do not force-add them.
</hard_rules_for_this_task>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Module option `include_risk_managed` + variant-aware narrative, metadata, and audit strings</name>
  <files>
    reports/modules/critical_remediation_sla_module.py,
    reports/modules/high_risk_assets_module.py,
    reports/modules/aged_vulns_assets_module.py,
    tests/test_kpi_risk_managed_exclusion.py
  </files>
  <behavior>
    - With no option set, all three modules produce numbers identical to today (regression: the existing assertions in tests/test_kpi_risk_managed_exclusion.py must pass unchanged).
    - With `options={"include_risk_managed": True}`, `high_risk_assets` counts ACCEPTED/RECASTED aged Crit/High findings toward the high-risk threshold.
    - With the option True, `aged_vulns_assets` qualifies an asset whose only aged findings are ACCEPTED/RECASTED.
    - With the option True, `critical_remediation_sla` picks up risk-managed rows in BOTH the open and the fixed population.
    - Garbage option values behave via `bool()`: `"false"` (non-empty string) and `1` are truthy; `0`, `""`, `None`, and absent are falsy. No exception is raised for any of them.
    - `metadata["risk_managed_scope"]` differs between the two option states for all three modules.
    - The no-data early-return paths still populate `risk_managed_scope`.
  </behavior>
  <action>
Read the option once at the top of each module's `compute()` `try:` block, before the
exclusion, using the established pattern:
`include_risk_managed = bool(config.options.get("include_risk_managed", False))`
then guard the exclusion with `if not include_risk_managed:`. Leave `add_vpr_severity()`
unconditional and immediately after the guard (Hard Rule 4). Add no `validate_config()`
override — `bool()` of any YAML scalar cannot raise.

Per file:
- `critical_remediation_sla_module.py:259-260` — guard BOTH `vulns_df` and `fixed_vulns_df`
  behind the single flag; they must never diverge.
- `high_risk_assets_module.py:183` and `aged_vulns_assets_module.py:180` — guard the single
  `vulns_df` call.

Then make the three hardcoded assertions of the exclusion follow the option. Both renderer
methods receive `config`, so they read the flag the same way:
- `critical_remediation_sla_module.py:724` — the PDF explanatory paragraph currently reads
  *"(VPR 9.0–10.0, excluding risk-accepted and recast findings)"*. Select between
  "excluding risk-accepted and recast findings" and "including risk-accepted and recast
  findings" via a local variable assigned before the f-string. Do not put a conditional
  expression inside the f-string — Hard Rule 6 forbids inline format specs on possibly-None
  values and the surrounding code deliberately pre-computes its format strings.
- `critical_remediation_sla_module.py:832` — the Excel `Scope:` row makes the same claim.
  Apply the same selection. This row is auditor-facing.
- `critical_remediation_sla_module.py:1069` `get_audit_info()["calculations"]["cohort"]` —
  see finding B in `<discovered_during_planning>`: this method has no `config` and is called
  on a bare `cls()` by `registry.py:173`, so it MUST stay static. Rewrite the cohort string
  to document the conditional rather than assert one branch, e.g. state that
  `severity_modification_type NOT IN (ACCEPTED, RECASTED)` is applied to both populations
  **unless the `include_risk_managed` module option is set**, in which case the full
  population is used. Keep the existing quick-260805-ezo provenance sentence.

Add a `risk_managed_scope` metadata key beside the existing `severity_scope` so the Excel
`_Metadata` tab states which population produced the numbers — `composer._write_metadata_tab`
does not record `ModuleConfig.options`, so without this the two workbooks are structurally
indistinguishable:
- `high_risk_assets_module.py:1069` and `aged_vulns_assets_module.py:1089` —
  `_build_metadata()` gains a second parameter `include_risk_managed: bool`. Update BOTH call
  sites in each file (`high_risk:224,478`; `aged:221,476`), including the no-data early
  returns.
- `critical_remediation_sla_module.py:517` — add the same key to the inline metadata dict.
  Its no-data return at `:279` is `{"email_gauge_b64": ""}` only; leave that shape alone.
Two literal strings, used verbatim in all three modules so the workbooks are diffable:
  - False: `"Excludes risk-managed findings (severity_modification_type in {ACCEPTED, RECASTED})"`
  - True:  `"Includes risk-managed findings (severity_modification_type in {ACCEPTED, RECASTED})"`

Extend `tests/test_kpi_risk_managed_exclusion.py` with inverted cases covering the
`<behavior>` list above. Reuse the existing `_make_vulns` / `_make_assets` /
`_uuid` synthetic helpers and `_REPORT_DATE` — do not introduce new fixture data shapes.
Keep every existing assertion intact; they are the "numbers unchanged by default" regression.
  </action>
  <verify>
    <automated>cd /home/jmonroe/projects/vuln-reporting && .venv/bin/python -m pytest tests/test_kpi_risk_managed_exclusion.py tests/test_board_report_utils.py tests/unit tests/content -q</automated>
    <automated>cd /home/jmonroe/projects/vuln-reporting && grep -c "include_risk_managed" reports/modules/critical_remediation_sla_module.py reports/modules/high_risk_assets_module.py reports/modules/aged_vulns_assets_module.py</automated>
    <manual>Cache-backed impact check. Write a THROWAWAY script to the scratchpad (not
    `scripts/`, not committed — see finding C): load `vulns_all.parquet`,
    `assets_all.parquet`, and `vulns_fixed_365d.parquet` from `data/cache/2026-08-05/` with
    `pd.read_parquet`, then call each module's `compute()` twice — once with a bare
    `ModuleConfig(id)` and once with `options={"include_risk_managed": True}` — at
    `report_date = 2026-08-05`, all-assets scope. Print aggregate counts only (Hard Rule 2).
    Expected inclusive-variant values from the approved plan: Critical Remediation SLA
    **78.9%** (open criticals 4,019, numerator unchanged at 15,479), High-Risk Assets
    **52 / 0.1%** (unchanged from the excluded variant on this snapshot), Aged Vulnerability
    Assets **6,279 / 17.4%**. Record the observed numbers in the SUMMARY; if any differ,
    STOP and report rather than adjusting the docs table in Task 3.</manual>
  </verify>
  <done>
    All three modules read `include_risk_managed` from `config.options` and skip
    `exclude_risk_managed()` when it is truthy; `add_vpr_severity()` still runs
    unconditionally; the PDF paragraph, Excel `Scope:` row, and `risk_managed_scope`
    metadata key in all three modules follow the flag; `get_audit_info()` documents the
    conditional statically; extended tests green with zero changes to the existing
    default-behavior assertions.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Variant-parameterize board_summary.py, register the new slug, and co-edit the three coupled tests</name>
  <files>
    reports/board_summary.py,
    run_all.py,
    delivery_config.schema.yaml,
    delivery/email_template.py,
    deliveries.d/board.yaml,
    deliveries.d/board_test.yaml,
    deliveries.d/adhoc.yaml,
    tests/test_phase6_run_group_chrome.py,
    tests/test_phase6_board_summary_chrome.py,
    tests/test_board_accepted_recast.py
  </files>
  <behavior>
    - `_CHROME_AWARE_SLUGS` contains both board slugs; the frozenset-equality test asserts the new 4-element set.
    - `run_group()` dispatching `board_summary_incl_risk_managed` passes `include_risk_managed=True`; dispatching `board_summary` passes `False`.
    - `run_group()` honors `analyst_detail: false` for BOTH board slugs.
    - `bs._board_module_configs(False)` and `(True)` return fresh ModuleConfig objects each call — mutating one call's result does not affect the next.
    - Cover title = base title + variant suffix; a group-level `report_title` still composes with the suffix.
    - The inclusive variant returns empty `email_body_html` and empty `email_inline_images`; the excluded variant's `email_body_html` is non-empty and ends with the cross-reference `<p>`.
    - The inclusive variant does not call `write_owner_supplemental`.
  </behavior>
  <action>
**`reports/board_summary.py`** — replace the module-level constants at `:88-98` with a frozen
variant record (`@dataclass(frozen=True)`) carrying: `slug`, `title_suffix`, `pdf_filename`,
`excel_filename`, `include_risk_managed`, `write_supplemental`, `email_panels`. Define the two
instances and a selector keyed on the boolean:
- excluded: slug `board_summary`, suffix `" (Excluding Risk-Accepted & Recast)"`,
  `board_summary.pdf` / `board_summary.xlsx`, `write_supplemental=True`, `email_panels=True`
- included: slug `board_summary_incl_risk_managed`, suffix `" (Including Risk-Accepted & Recast)"`,
  `board_summary_incl_risk_managed.pdf` / `board_summary_incl_risk_managed.xlsx`,
  `write_supplemental=False`, `email_panels=False`

Keep `_REPORT_TITLE` as the base-title constant (`tests/test_phase6_board_summary_chrome.py`
imports it) but stop using `_PDF_FILENAME` / `_EXCEL_FILENAME`; delete them.

Convert `_BOARD_MODULE_CONFIGS` into a factory `_board_module_configs(include_risk_managed: bool)
-> list[ModuleConfig]` that builds **fresh** `ModuleConfig` objects on every call, preserving the
literal 5-module order. This is load-bearing, not stylistic: the list is currently module-level,
mutable, and handed straight to the composer at `:331`; mutating it in place would leak the
inclusive options into every subsequent `board_summary` run inside the same
`scheduler.py --mode daemon` process, and the only symptom would be "the two reports agree."
Set `options={"include_risk_managed": ...}` on the three consuming modules only
(`critical_remediation_sla`, `high_risk_assets`, `aged_vulns_assets`); leave
`scan_coverage_sla` and `accepted_recast` with default options, which documents that they are
variant-invariant by construction.

`run_report()` gains one keyword-only `include_risk_managed: bool = False`; resolve the variant
from it once at the top, then:
- `:316` → `effective_title = (report_title or _REPORT_TITLE) + variant.title_suffix`
- `:331` → `module_configs = _board_module_configs(include_risk_managed)`
- `:350` → `slug = variant.slug`
- `:371-381` → run the owner-supplemental block only `if variant.write_supplemental`. It reads
  the unfiltered frames so both variants would produce identical `owner_segmentation.*`, and it
  is not emailed — writing it twice runs `extract_owner` + `open_findings_at` over ~196k rows
  for nothing. Keep `_supp_excel` / `_supp_csv` as `None` when skipped.
- `:389` / `:405` → `variant.pdf_filename` / `variant.excel_filename`. Distinct basenames are
  required, not cosmetic: `email_sender._attach_file` uses `path.name` only, so identical names
  collide in the recipient's mail client even though the on-disk paths differ.
- `:414-454` result dict → when `not variant.email_panels`, set `email_body_html` to `""` and
  `email_inline_images` to `[]`. For the excluded variant, append a one-line cross-reference
  `<p>` to `email_body_html` pointing the reader at the attached inclusive PDF — **inline CSS
  only**, no `<style>` block (Outlook/Gmail/Apple Mail constraint). Leave `_bundle` untouched;
  `scripts/smoke_board_summary_cutover.py` depends on it.
- Add `--include-risk-managed` (`store_true`) to the existing argparse block and forward it.

**`run_all.py`:**
- `_VALID_REPORTS` `:77-90` — add `board_summary_incl_risk_managed`.
- `_REPORT_MODULE_MAP` `:117-130` — `"board_summary_incl_risk_managed": "reports.board_summary"`,
  with a comment explaining the intentional two-slugs-one-module mapping.
- `_CHROME_AWARE_SLUGS` `:100-104` — add it, or the new variant's PDF chrome silently diverges.
- Add `_BOARD_SUMMARY_SLUGS: frozenset[str]` containing both board slugs, and widen the
  `run_group()` block at `:850-856` from `if slug == "board_summary":` to
  `if slug in _BOARD_SUMMARY_SLUGS:`, injecting BOTH `analyst_detail` (unchanged `.get(..., True)`)
  and `include_risk_managed = (slug == "board_summary_incl_risk_managed")`. Without the
  widening, `analyst_detail: false` is silently ignored for the new slug and a group that opted
  out still receives an analyst workbook.

**`delivery_config.schema.yaml`** — add the slug to the `reports` enum (`:54-72`). Also fix the
stale `report_title` description at `:113-119`, which claims "used only by composed_report";
`board_summary` and `management_summary` have consumed it since Phase 6/18.

**`delivery/email_template.py:82-91`** — add `_REPORT_DESCRIPTIONS` entries for **both** board
slugs. Neither is present today, so the attachment list falls through to
`slug.replace("_", " ").title()` and the new one would read "Board Summary Incl Risk Managed".

**`deliveries.d/`** (gitignored local operator config — edit but do not commit): add the second
slug to all 5 deliveries in `board.yaml`, to `board_test.yaml`, and to the "Board Summary —
Live Test" entry in `adhoc.yaml`. List `board_summary` **first** in every `reports:` array — it
is the report whose panels drive the email body via the `next(...)` selector. Update the
`subject:` lines to say both views are attached.

No `delivery/config_loader.py` change — `reports` is already in `_PASSTHROUGH_GROUP_KEYS` (`:160-173`).

**Co-edit the three coupled tests in this same commit** or the run goes red:
- `tests/test_phase6_run_group_chrome.py:38` — exact `_CHROME_AWARE_SLUGS` frozenset equality;
  add the new slug to the expected set and extend the docstring's co-edit note.
- `tests/test_phase6_board_summary_chrome.py:107` (`cfg.title == bs._REPORT_TITLE`) and `:115`
  (`cfg.title == "Executive Security Posture"`) — both now carry the variant suffix.
- `tests/test_board_accepted_recast.py:82` — reads `bs._BOARD_MODULE_CONFIGS` directly (finding A
  in `<discovered_during_planning>`); switch it to the factory.
  </action>
  <verify>
    <automated>cd /home/jmonroe/projects/vuln-reporting && .venv/bin/python run_all.py --dry-run; echo "exit=$?"</automated>
    <automated>cd /home/jmonroe/projects/vuln-reporting && .venv/bin/python -m pytest tests/test_phase6_run_group_chrome.py tests/test_phase6_board_summary_chrome.py tests/test_board_accepted_recast.py tests/test_phase4_schema_validation.py tests/test_phase4_analyst_detail_toggle.py tests/test_board_summary_baseline.py tests/test_consumer_audit.py -q</automated>
    <automated>cd /home/jmonroe/projects/vuln-reporting && .venv/bin/python -c "import yaml, run_all; e=set(yaml.safe_load(open('delivery_config.schema.yaml'))['definitions']['group']['properties']['reports']['items']['enum']); assert e == set(run_all._VALID_REPORTS), e ^ set(run_all._VALID_REPORTS); print('enum reconciled', len(e))"</automated>
  </verify>
  <done>
    `--dry-run` exits 0 and every board delivery lists two reports; the new slug resolves
    through `_VALID_REPORTS` / `_REPORT_MODULE_MAP` / `_CHROME_AWARE_SLUGS` / schema enum;
    `_board_module_configs()` returns fresh objects per call; the three coupled tests pass in
    the same commit as the code that changed them; no new file exists under `reports/`.
    (The final `-c` one-liner's dict path may need adjusting to the schema's actual nesting —
    locate the `reports` enum and compare it to `_VALID_REPORTS`; the assertion, not the path,
    is what matters.)
  </done>
</task>

<task type="auto">
  <name>Task 3: New variant test suite + docs</name>
  <files>
    tests/test_board_summary_variants.py,
    docs/board_summary_calculations.md,
    docs/GLOSSARY.md,
    CLAUDE.md,
    RUNBOOK.md
  </files>
  <action>
**`tests/test_board_summary_variants.py`** (new). Reuse the
`_invoke_and_capture_pdf_chrome` patching pattern from
`tests/test_phase6_board_summary_chrome.py:31-73` — patch `ReportComposer`,
`fetch_all_vulnerabilities`, `fetch_all_assets`, `fetch_fixed_vulnerabilities`, `read_trend`,
and `_render_pdf` so no Tenable call and no WeasyPrint render happens. Generalize the spy to
also capture `module_configs` and the `run_full_pipeline` kwargs. Cover:
- The slug is present in `run_all._VALID_REPORTS`, `_REPORT_MODULE_MAP`, `_CHROME_AWARE_SLUGS`,
  and the schema `reports` enum — and assert **set equality** between the schema enum and
  `_VALID_REPORTS`. Nothing enforces that reconciliation today; this test is the enforcement.
- Title suffixes for both variants, including composition with an explicit `report_title`.
- Per-variant module options: the three consuming modules carry
  `options["include_risk_managed"]` matching the variant; `scan_coverage_sla` and
  `accepted_recast` do not.
- PDF/Excel filenames differ between variants; `run_full_pipeline` receives `slug=variant.slug`.
- `write_owner_supplemental` is NOT called for the inclusive variant (patch it and assert
  call count 0; assert 1 for the excluded variant).
- Inclusive variant returns `email_body_html == ""` and `email_inline_images == []`; excluded
  variant returns a non-empty body containing the cross-reference pointer.
- **No state leak:** call `_board_module_configs(True)`, mutate the returned configs' options,
  then call `_board_module_configs(False)` and assert the fresh configs are unaffected; and
  run `run_report()` for the inclusive variant followed by the excluded variant in the same
  process, asserting the second run's captured `module_configs` carry `False`.

**`docs/board_summary_calculations.md`:**
- The "Exclusion of risk-managed findings" section (`:78-87`) becomes the description of the
  **default** variant.
- Add a "Two report variants" section; renumber the sections that follow and update the TOC
  at `:13-27`. Include the impact table (all-assets, `data/cache/2026-08-05`) with the numbers
  confirmed in Task 1, plus these three caveats — without them the reports will be misread:
  1. **Metrics 1 and 5 are identical by construction** (Metric 1 is assets-only and never reads
     `vulns_df`; Metric 5 deliberately reads the full frame), and Metric 3 is unchanged on the
     current snapshot because its threshold is ≥10 Crit/High >30d per asset and the extra
     findings spread thinly (≥7/≥8/≥9 are unchanged too). Three of five pages will look like
     photocopies. That is correct, not a defect.
  2. **The inclusive Metric 2 is not a comparable SLA rate.** Its numerator is unchanged
     (15,479 both ways) — the entire 2.9-point drop lands in `open_past_due`. Accepted findings
     are by definition never remediated, so this reads as "SLA rate penalized by acceptance
     volume," not "remediation got worse."
  3. **"Including risk-accepted" delivers +712 criticals, not +15,610.** Under VPR-only tiering
     12,077 of the 15,610 natively-Critical accepted findings land in the *medium* tier.
- Add one line to Metrics 2/3/4 *Edge cases* naming the `include_risk_managed` option.
- Note the Metric 5 tie-out gap: `exclude_risk_managed` ignores rule expiry while
  `accepted_recast_module.py:250-266` treats findings under an expired rule as "pending
  re-evaluation" — zero today (no rule has lapsed), but it will diverge silently on the first
  expiry.
- Document that the per-owner BU tables under Metrics 3/4 **re-sort by risk score computed on
  the post-exclusion frame**, so the inclusive report can rank owners differently even where
  the headline number is identical.

**`docs/GLOSSARY.md`** — add a **risk-managed finding** entry (no entry exists today):
a finding whose `severity_modification_type` is `ACCEPTED` or `RECASTED`. Note that a recast is
not an acceptance, which is why the codebase says "risk-managed" rather than "risk-accepted".

**`CLAUDE.md`** — add a `board_summary_incl_risk_managed` row to the "Report Scripts — Slug
Index" table (same module, inclusive variant) and note the two-slugs-one-module mapping in the
`board_summary` row.

**`RUNBOOK.md:142-145`** — add the slug to the valid-slug list.
  </action>
  <verify>
    <automated>cd /home/jmonroe/projects/vuln-reporting && .venv/bin/python -m pytest tests/test_board_summary_variants.py -q</automated>
    <automated>cd /home/jmonroe/projects/vuln-reporting && .venv/bin/python -m pytest tests/unit tests/content -q && .venv/bin/python -m pytest tests/test_kpi_risk_managed_exclusion.py tests/test_board_report_utils.py tests/test_phase6_board_summary_chrome.py tests/test_phase6_run_group_chrome.py tests/test_board_accepted_recast.py tests/test_phase4_schema_validation.py tests/test_phase4_analyst_detail_toggle.py tests/test_consumer_audit.py tests/test_board_summary_baseline.py tests/test_board_summary_variants.py -q</automated>
    <automated>cd /home/jmonroe/projects/vuln-reporting && grep -v '^#' RUNBOOK.md | grep -c "board_summary_incl_risk_managed" && grep -c "board_summary_incl_risk_managed" CLAUDE.md docs/board_summary_calculations.md</automated>
  </verify>
  <done>
    New variant test file green and enforcing schema-enum ↔ `_VALID_REPORTS` set equality and
    the no-leak property; the named test files are green with no new failures versus the
    pre-change baseline (the 5 order-dependent stub-registry failures in
    `tests/unit/test_modules.py` are pre-existing — record the before/after counts rather than
    fixing them); docs updated with the two-variants section, renumbered TOC, impact table,
    three caveats, GLOSSARY entry, and slug added to CLAUDE.md + RUNBOOK.md.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `deliveries.d/*.yaml` → `run_group()` | Operator-authored config drives slug dispatch and module options |
| module output → recipient mailbox | Report content leaves the host as PDF/Excel/HTML |
| warm parquet cache → verification scripts | Real (non-synthetic) vulnerability data enters a local script |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-ga2-01 | Information disclosure | Task 1 cache-backed impact script | mitigate | Script prints aggregate counts only, is written to the scratchpad, and is never committed (Hard Rule 2 / D-04-08). No hostname, IP, MAC, plugin name, or asset UUID emitted. |
| T-ga2-02 | Information disclosure | Docs impact table | mitigate | Only aggregate percentages and asset/finding counts recorded — the same aggregate class already published in `board_summary_calculations.md`. |
| T-ga2-03 | Information disclosure | `deliveries.d/` edits | mitigate | Directory is gitignored (`.gitignore:19`); no recipient address or contact name enters a committed artifact. Do not `git add -f`. |
| T-ga2-04 | Tampering | Shared mutable `_BOARD_MODULE_CONFIGS` | mitigate | Replaced with a per-call factory; a dedicated no-leak test asserts successive `run_report()` calls in one process do not cross-contaminate options (silent-wrong-numbers failure mode in the long-lived daemon). |
| T-ga2-05 | Spoofing / confusion | Identical attachment basenames | mitigate | Distinct `pdf_filename` / `excel_filename` per variant — `email_sender._attach_file` keys on `path.name` only, so identical names collide in the recipient's client and the board could read the wrong view. |
| T-ga2-06 | Elevation / scope | Live Tenable pull during verification | mitigate | Only `run_all.py --dry-run`, pytest, and direct `pd.read_parquet` of `data/cache/2026-08-05/` are permitted; the `block_tenable_fetch.py` PreToolUse hook is the backstop (Hard Rule 1). |
| T-ga2-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies — `requirements.txt` is untouched (Hard Rule 8). No install task in this plan. |
</threat_model>

<verification>
Whole-task gates, run after Task 3:

1. `.venv/bin/python run_all.py --dry-run` → exit 0, all 5 board deliveries plus the two test
   groups listing two reports each. This is the single gate proving `_VALID_REPORTS`, the
   schema enum, and the loader agree.
2. `.venv/bin/python -m pytest tests/unit tests/content -q`
3. The explicit named-file pytest run in Task 3 (these files sit outside `pytest.ini`
   testpaths and must be named).
4. Schema-enum ↔ `_VALID_REPORTS` symmetric difference is empty (asserted by
   `tests/test_board_summary_variants.py`).
5. Cache-backed impact check reproduces 78.9% / 52 / 6,279 on `data/cache/2026-08-05`.

**Operator-only follow-ups** (need a warm cache and a real run — record these in the SUMMARY,
do not attempt them):
- `python run_all.py --group "Board Summary - test" --no-email` → 2 PDFs, 2 main + 2 analyst
  Excels with distinct filenames; `owner_segmentation.*` present under `board_summary/` and
  **absent** under the inclusive directory; `[CACHE HIT]` on the second slug's fetches; cover
  titles carry the right qualifiers; Metrics 1 and 5 identical, 2 and 4 moved.
- Capture the MIME: exactly 5 `Content-ID` parts, every `cid:` in the body resolving to one of
  them with no orphans; 6 attachments with 6 distinct filenames.
- Render in Outlook + Gmail + Apple Mail — a CID regression shows as a wrong image, not an error.
- The cover title is 22pt; if the suffix wraps badly, shorten to `(Excl. …)` / `(Incl. …)` —
  operator judgment, no code change beyond the two literals.
- Regenerate the stale `tests/baselines/board_summary_test_pull*.json` structural baselines via
  `scripts/smoke_board_summary_cutover.py` (stale since quick-260722-lx9; unrelated to this task).
</verification>

<success_criteria>
- Both board slugs deliver from one group with distinct PDF/Excel filenames.
- `board_summary` numbers are unchanged by default; `board_summary_incl_risk_managed`
  reproduces the approved plan's measured impact on the 2026-08-05 cache.
- Only one report's panels reach the email body; the inclusive variant contributes none.
- No new file under `reports/`; no composer change; zero new dependencies.
- The three coupled tests were co-edited in the same commit as the code that broke them.
- `run_all.py --dry-run` exits 0.
</success_criteria>

<output>
Create `.planning/quick/260813-ga2-split-board-summary-into-risk-managed-ex/260813-ga2-SUMMARY.md`
when done. Record: observed cache-backed impact numbers, before/after full-suite failure
counts, and the operator-only follow-ups listed above.
</output>
</content>
</invoke>
