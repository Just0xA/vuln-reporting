---
phase: quick-260709-7ww
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DEPLOYMENT.md
  - RUNBOOK.md
  - deploy/smoke_bootstrap.sh
  - scripts/update_from_github.sh
  - CONTRIBUTING.md
  - CLAUDE.md
  - .github/PULL_REQUEST_TEMPLATE/enhancement.md
  - .github/PULL_REQUEST_TEMPLATE/fix.md
  - .github/PULL_REQUEST_TEMPLATE/feature.md
  - .github/ISSUE_TEMPLATE/feature_request.yml
autonomous: true
requirements: [DOCS-3.12-MIGRATION]

must_haves:
  truths:
    - "Deploy docs, scripts, CI templates, and contributor docs reference Python 3.12, not 3.11"
    - "requirements.txt is documented as the supported server install path; uv/devcontainer is dev-only"
    - "No application logic, pyproject.toml, or .python-version changes"
  artifacts:
    - path: "DEPLOYMENT.md"
      provides: "3.12 install/venv instructions + requirements.txt-is-server-path note"
      contains: "python3.12"
    - path: "deploy/smoke_bootstrap.sh"
      provides: "3.12 dnf install + alternatives/symlink"
      contains: "python3.12"
    - path: "scripts/update_from_github.sh"
      provides: "3.12 example in failure message; resolver loop unchanged"
  key_links:
    - from: "DEPLOYMENT.md"
      to: "requirements.txt"
      via: "documented supported server install path"
      pattern: "requirements\\.txt"
---

<objective>
Make deploy docs, shell scripts, CI/GitHub templates, and contributor docs consistent with the already-completed Python 3.12 toolchain migration (pyproject.toml `requires-python = ">=3.12"`, `.python-version = 3.12`, `.venv` on python3.12).

Purpose: The uv migration moved the toolchain to 3.12 but left prose/config artifacts referencing 3.11. This closes that gap and explicitly documents requirements.txt as the supported SERVER install path (uv/devcontainer being dev-only convenience).

Output: Updated DEPLOYMENT.md, RUNBOOK.md, deploy/smoke_bootstrap.sh, scripts/update_from_github.sh, CONTRIBUTING.md, CLAUDE.md, and .github PR/issue templates. Docs/config only — NO application logic changes.
</objective>

<execution_context>
@/home/jmonroe/.claude/plugins/cache/gsd-plugin/gsd/4.0.2/workflows/execute-plan.md
@/home/jmonroe/.claude/plugins/cache/gsd-plugin/gsd/4.0.2/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

# Hard constraints for this task
# - DO NOT modify pyproject.toml or .python-version (already correct at 3.12).
# - DO NOT touch the `apscheduler==3.11.0` pin anywhere — it is a package version, not a Python version.
# - Keep uv/devcontainer as dev-only; requirements.txt remains the supported SERVER install path.
# - Known pre-existing uncommitted working-tree edits `M CLAUDE.md` and `M DEPLOYMENT.md` must be FOLDED IN, not reverted.
# - .github/workflows/release.yml does NOT pin Python (no setup-python/python-version) — leave it unchanged.
# - scripts/update_from_github.sh resolver loop (line ~103: `for name in python3.13 python3.12 python3.11 python3.10`) stays AS-IS — it already prefers 3.12 and back-compat probing is fine.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate deploy docs and shell scripts to 3.12</name>
  <files>DEPLOYMENT.md, RUNBOOK.md, deploy/smoke_bootstrap.sh, scripts/update_from_github.sh</files>
  <action>
  DEPLOYMENT.md — replace every Python 3.11 reference with 3.12 at the confirmed lines:
  - line ~51 comment "install 3.11 from AppStream" → 3.12
  - line ~52 `sudo dnf install -y python3.11 python3.11-pip python3.11-devel` → `python3.12 python3.12-pip python3.12-devel`
  - line ~55 `python3.11 --version` → `python3.12 --version`
  - lines ~60-61 interpreter-resolution note ("On a host with only `python3.11` ... resolve `python3.11` automatically") → 3.12
  - line ~67 `sudo alternatives --install /usr/bin/python3 python3 /usr/bin/python3.11 1` → python3.12
  - line ~151 `sudo -u vuln-reports python3.11 -m venv "${RELEASE_DIR}/.venv"` → python3.12
  Also add ONE sentence (near the install/venv instructions) stating that requirements.txt is the supported server install path and uv/devcontainer is a dev-only convenience.

  RUNBOOK.md line ~629 `python3.11 -m venv .venv` → `python3.12 -m venv .venv`.

  deploy/smoke_bootstrap.sh lines ~51-69 — update the Step 1 header comment, the "Python 3.11 is available" comment, the echo "--- installing Python 3.11 ---", `dnf install -y python3.11 python3.11-pip python3.11-devel`, the `python3 --version` check `!= *"3.11"*`, the two comment lines referencing 3.11, and the alternatives/symlink lines (`alternatives --install /usr/bin/python3 python3 /usr/bin/python3.11 1`, `alternatives --set python3 /usr/bin/python3.11`, `ln -sf /usr/bin/python3.11 /usr/local/bin/python3`, and their echo messages) → all 3.12.

  scripts/update_from_github.sh line ~113 — inside the failure `log_completed` message, change the `'alternatives --install /usr/bin/python3 python3 /usr/bin/python3.11 1'` example to python3.12. LEAVE the resolver loop at line ~103 UNCHANGED.
  </action>
  <verify>
    <automated>! grep -rniE 'python3\.11|3\.11' DEPLOYMENT.md RUNBOOK.md deploy/smoke_bootstrap.sh <(grep -v 'for name in' scripts/update_from_github.sh)</automated>
  </verify>
  <done>No 3.11 references remain in DEPLOYMENT.md, RUNBOOK.md, deploy/smoke_bootstrap.sh, or scripts/update_from_github.sh (except the resolver loop line ~103, intentionally preserved). DEPLOYMENT.md contains the requirements.txt-is-server-path note.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate contributor docs, GitHub templates, and CLAUDE.md to 3.12</name>
  <files>CONTRIBUTING.md, CLAUDE.md, .github/PULL_REQUEST_TEMPLATE/enhancement.md, .github/PULL_REQUEST_TEMPLATE/fix.md, .github/PULL_REQUEST_TEMPLATE/feature.md, .github/ISSUE_TEMPLATE/feature_request.yml</files>
  <action>
  CONTRIBUTING.md line ~131 — "Python 3.10+ (currently tested on 3.10, 3.11, 3.12)." → "Python 3.12+ (currently tested on 3.12)."

  CLAUDE.md line ~56 ("Python 3.10+, `pyTenable`, pandas, ...") and line ~439 ("- **Tech stack**: Python 3.10+, `pyTenable` SDK, ...") — change "Python 3.10+" → "Python 3.12+" in both. Fold in the pre-existing uncommitted `M CLAUDE.md` edits rather than reverting them.

  .github/PULL_REQUEST_TEMPLATE/enhancement.md (line ~36), fix.md (line ~39), feature.md (line ~46) — each has a `- [ ] 3.11` checkbox line. If a `3.12` checkbox already exists in that file, DELETE the `3.11` line; otherwise change the `3.11` line to `3.12`. Inspect each file's Python-version checkbox block to decide.

  .github/ISSUE_TEMPLATE/feature_request.yml line ~113 — `- label: Python 3.11`. If a `Python 3.12` label already exists, delete this line; otherwise change it to `- label: Python 3.12`.
  </action>
  <verify>
    <automated>! grep -rniE 'python 3\.11|3\.11' CONTRIBUTING.md CLAUDE.md .github/PULL_REQUEST_TEMPLATE/ .github/ISSUE_TEMPLATE/feature_request.yml; ! grep -niE 'Python 3\.10' CLAUDE.md CONTRIBUTING.md</automated>
  </verify>
  <done>No 3.11 references in CONTRIBUTING.md, CLAUDE.md, or the GitHub templates. CLAUDE.md and CONTRIBUTING.md say "Python 3.12+" (no lingering "3.10+"). Pre-existing CLAUDE.md working-tree edits preserved.</done>
</task>

</tasks>

<verification>
Whole-repo gate (the plan's success criterion). Excludes .planning/, .git/, .venv/, and the unrelated `apscheduler==3.11.0` pin:

```
grep -rniE '3\.11|python3\.11' --include=*.md --include=*.sh --include=*.py --include=*.toml --include=*.yml --include=*.yaml . \
  | grep -vE '\.planning/|\.git/|\.venv/|apscheduler' \
  | grep -v 'for name in python3.13 python3.12 python3.11 python3.10'
```

Must return NO output. The only intentionally-retained `python3.11` token is the resolver loop in scripts/update_from_github.sh (back-compat probing), filtered out above.

Confirm NOT changed: `git diff --name-only` must NOT list pyproject.toml or .python-version.

Note: .github/workflows/release.yml does not pin a Python version (no setup-python step / python-version key) — left unchanged by design.
</verification>

<success_criteria>
- All Python 3.11 references in deploy docs, scripts, CI/GitHub templates, and contributor docs are updated to 3.12 (resolver-loop back-compat line and `apscheduler==3.11.0` excepted).
- DEPLOYMENT.md documents requirements.txt as the supported server install path; uv/devcontainer noted as dev-only.
- CLAUDE.md and CONTRIBUTING.md read "Python 3.12+".
- pyproject.toml and .python-version untouched.
- Pre-existing uncommitted CLAUDE.md and DEPLOYMENT.md edits folded in, not reverted.
- No application logic changed.
</success_criteria>

<output>
Create `.planning/quick/260709-7ww-migrate-deploy-docs-scripts-ci-from-pyth/260709-7ww-SUMMARY.md` when done.
</output>
