---
quick_id: 260709-983
title: Apply 2026-07-02 harness optimization patch (selective, rebased)
date: 2026-07-09
status: complete
commits: [6862d4d, 3dfa847, ddc0b8d]
---

# Summary — Apply 2026-07-02 harness optimization patch

The patch (`harness-optimization.patch`, authored 2026-07-02) had **drifted** and did
not apply cleanly. Blob-level reconciliation against the current tree drove a selective,
rebased application rather than a blind `git apply`.

## Reconciliation

| File(s) | Patch base→target | Current | Decision |
|---------|-------------------|---------|----------|
| `block_tenable_fetch.py`, `settings.json` (new), `test_block_tenable_fetch.py`, `SKILL.md`, `vuln-metric-substrate.md`, `CONVENTIONS.md` | bases match | clean | **Applied** |
| `CLAUDE.md` | →`289de1d` | WT already `289de1d` | **Committed** (already-applied rewrite) |
| `MILESTONES.md` (+21 rows) / `STATE.md` (−21 rows) | STATE base `2acc2c5` diverged | STATE now `b851043`+ | **Rebased move** |

## What shipped (3 commits)

1. **`6862d4d` — hook activation + hardening.** Added the previously-**missing**
   `.claude/settings.json` that wires the `block_tenable_fetch.py` PreToolUse hook —
   Hard Rule 1 (no live Tenable pulls from Claude Code) was **not actually enforced**
   without it. Expanded the GUARDED sets + verified pre-auth dry-run allowlist in the
   hook. Added `tests/test_block_tenable_fetch.py` — **26 tests, all green**. Minor
   `SKILL.md` / `vuln-metric-substrate.md` / `CONVENTIONS.md` edits from the patch.
2. **`3dfa847` — CLAUDE.md rewrite.** The patch's CLAUDE.md target was byte-identical
   to the working-tree rewrite already in place (applied manually earlier this session).
   Committing it adds the 9 Hard-Rules invariants + Planning-Systems boundary + restructure,
   and restores the SLA-table Medium row to **60** (matching `config.py SLA_DAYS`),
   correcting the stray **45** that task 260709-7ww had left in HEAD.
3. **`ddc0b8d` — quick-tasks archive move (rebased).** Applied the MILESTONES.md
   "Quick Tasks Archive" (21 pre-v1.4 rows) and pruned exactly those 21 rows from the
   *current* STATE.md, leaving a pointer note and keeping the two post-v1.4 rows
   (`260701-da9`, `260709-7ww`). Verified zero row overlap between the two files.

## Deliberately not done
- `git apply` **excluded** CLAUDE.md and STATE.md; both were handled by hand so the
  stale patch could not drop newer STATE.md rows or double-apply CLAUDE.md.
- The **SLA-config validation** question (do all reports read `config.py SLA_DAYS` vs
  hardcode?) remains open — captured for the roadmap, not addressed here.
- The **v1.5.0 release** (push + tag) remains held per the user.

## Verification
- `pytest tests/test_block_tenable_fetch.py` → 26 passed.
- No-overlap check: archived slugs present in MILESTONES.md / absent in STATE.md; kept
  slugs absent in MILESTONES.md / present in STATE.md.
