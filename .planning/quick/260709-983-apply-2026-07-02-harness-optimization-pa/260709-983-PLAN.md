---
quick_id: 260709-983
title: Apply 2026-07-02 harness optimization patch (selective, rebased)
date: 2026-07-09
status: in-progress
---

# Apply 2026-07-02 harness optimization patch

## Context

`harness-optimization.patch` (created 2026-07-02) touches 9 files but has drifted.
Blob-level reconciliation against the current tree decided scope:

- **CLAUDE.md** — patch target blob `289de1d` is byte-identical to the current
  working tree. Already applied (manually, earlier this session). **SKIP.**
- **STATE.md ↔ MILESTONES.md** — a coupled "archive move": pre-v1.4 Quick-Task
  rows move out of STATE.md into MILESTONES.md ("Quick Tasks Archive"). STATE.md
  diverged since 2026-07-02, so apply MILESTONES.md cleanly and **rebase** the
  STATE.md prune onto today's file (user decision: "Rebase the move").
- **6 harness hunks** apply cleanly (bases match) — the actual optimization.

## Tasks

### Task 1 — Apply harness hunks (hook activation + tests)
Files: `.claude/hooks/block_tenable_fetch.py`, `.claude/settings.json` (new — wires
the PreToolUse hook that was previously not active), `tests/test_block_tenable_fetch.py`,
`.claude/skills/spike-findings-vuln-reporting/SKILL.md`,
`.claude/skills/spike-findings-vuln-reporting/references/vuln-metric-substrate.md`,
`.planning/codebase/CONVENTIONS.md`.
Action: `git apply --exclude=CLAUDE.md --exclude=.planning/STATE.md harness-optimization.patch`
(this also applies MILESTONES.md — Task 2 covers the STATE.md side).
Verify: `pytest tests/test_block_tenable_fetch.py` green; `.claude/settings.json` present.
Commit: harness/hook activation.

### Task 2 — Rebase the archive move
Action: MILESTONES.md archive applied in Task 1. Prune the 21 archived pre-v1.4
rows from the CURRENT STATE.md "Quick Tasks Completed" table and add the pointer
note; KEEP the two post-v1.4 rows (`260701-da9`, `260709-7ww`).
Verify: STATE.md table contains only post-v1.4 rows + pointer; MILESTONES.md holds
the archive; no row appears in both.
Commit: docs archive move.

## Out of scope
- CLAUDE.md (already applied), the SLA-config validation question, and the v1.5.0
  release (held separately).
