---
phase: 15-independent-new-modules
plan: "03"
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - reports/modules/external_dmz_module.py
  - tests/test_external_dmz_module.py
autonomous: true
requirements: [RPT-06, QUAL-03, QUAL-05]
user_setup: []

must_haves:
  truths:
    - "RPT-06: A reader sees External/DMZ exposure counts (Critical/High/Medium) on externally-scoped assets, current-snapshot only"
    - "RPT-06: the analyst mismatch list follows the LOCKED schema asset_uuid, ip_address, owner_tag, untagged_reason, finding_count (aggregate) — NO per-finding CVE/plugin detail (D-11/D-04-08)"
    - "RPT-06: zero-external-asset groups render a gray 'No external assets in scope' cell, NOT an error"
    - "Phase-14 D-03: external_dmz is CURRENT-SNAPSHOT ONLY — no trend_snapshots kwarg, no cold-start branch, NOT added to _MODULES_NEEDING_TREND_SNAPSHOTS (EXT-TREND-01 deferred to v1.5)"
    - "QUAL-03: module survives zero-row filtered input on all four channels; safe_pct/safe_int/safe_format + gray cell"
    - "QUAL-05: mismatch list + fixtures contain only synthetic RFC 6761/5737 data; committed/aggregate external counts stay finding_count-only"
  artifacts:
    - path: "reports/modules/external_dmz_module.py"
      provides: "ExternalDmzModule four-channel module, MODULE_ID=external_dmz, consumes external_scope()"
      contains: "@register_module"
      min_lines: 180
    - path: "tests/test_external_dmz_module.py"
      provides: "Unit tests: external scope counts, mismatch schema lock, zero-external gray cell, empty-data guard x4"
      min_lines: 100
  key_links:
    - from: "reports/modules/external_dmz_module.py"
      to: "utils.external_scope.external_scope"
      via: "inline scope classification (Phase-14 substrate)"
      pattern: "external_scope"
    - from: "reports/modules/external_dmz_module.py"
      to: "reports.modules.registry.register_module"
      via: "auto-discovery self-registration"
      pattern: "@register_module"
---

<objective>
Build `external_dmz`: a CURRENT-SNAPSHOT four-channel module showing open vuln
counts (Critical/High/Medium) on externally-scoped assets, plus the analyst
mismatch list. Scope comes from the Phase-14 `external_scope()` substrate called
inline in `compute()` — no new kwargs gate. It copies the proven pathfinder shape
from `reopened_vulns` (15-01). External MoM trend is deferred to v1.5 (Phase-14
D-03), so this module has NO trend branch.

Purpose: External/DMZ exposure cut + tagging-gap mismatch list (RPT-06).
Output: `reports/modules/external_dmz_module.py` + its unit test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-independent-new-modules/15-CONTEXT.md
@.planning/phases/15-independent-new-modules/15-PATTERNS.md
@.planning/phases/14-shared-substrates-composed-report-gates/14-01-SUMMARY.md

<interfaces>
<!-- Phase-14 substrate (utils/external_scope.py) -->
external_scope(assets_df) -> (scoped_df, mismatches_df)
  scoped_df: Location=External/DMZ tagged assets + public-IP-untagged gap assets
  mismatches_df columns: asset_uuid, ip_address, owner_tag, untagged_reason
  Empty/missing-column input -> two zero-row DataFrames, no exception (fail-soft)
  D-06: Location=DMZ + private IP is NOT a mismatch; D-08: gap assets appear in BOTH frames

<!-- Four-channel + helpers: same as 15-01 — BaseModule (reports/modules/base.py),
     extract_owner (board_report_utils.py:214), rag_utils, format_utils.
     The pathfinder reopened_vulns_module.py (built in 15-01) is the in-repo shape to copy. -->

<!-- RAG defaults (D-15-07): _DEFAULT_GREEN_EXT_CRIT=0, _DEFAULT_YELLOW_EXT_CRIT=5
     (0 crit=green, 1-5=yellow, >5=red); overridable via config.options -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build ExternalDmzModule current-snapshot core + four channels</name>
  <files>reports/modules/external_dmz_module.py, tests/test_external_dmz_module.py</files>
  <read_first>
    - reports/modules/reopened_vulns_module.py (the in-repo pathfinder shape built in 15-01 — copy the module skeleton, four-channel render methods, empty-data guard, ModuleData construction)
    - utils/external_scope.py (external_scope() tuple contract; D-06/D-08 classification rules)
    - .planning/phases/14-shared-substrates-composed-report-gates/14-01-SUMMARY.md (external_scope behavior + PII boundary D-11)
    - .planning/phases/15-independent-new-modules/15-PATTERNS.md "reports/modules/external_dmz_module.py" section (lines 498-555 — substrate call, no-trend note, mismatch analyst schema lock)
    - reports/modules/rag_utils.py + format_utils.py
  </read_first>
  <behavior>
    - Test: assets_df with 3 external-scoped + 2 internal assets; vulns on external assets counted by severity (Critical/High/Medium) on scoped asset_uuids only
    - Test: external_scope returns empty scoped_df (internal-only group) → summary "No external-scope assets in scope.", rag_strip gray "no_data", error None (NOT an error)
    - Test: mismatch analyst tab DataFrame has EXACTLY columns asset_uuid, ip_address, owner_tag, untagged_reason, finding_count — no plugin/CVE/per-severity columns (schema lock, Pitfall 11)
    - Test: finding_count per mismatch asset is an aggregate groupby size mapped onto mismatches_df via .assign() (no df["col"]= after filter)
    - Test: module is NOT in _MODULES_NEEDING_TREND_SNAPSHOTS and compute() has no trend_snapshots/cold_start branch (Phase-14 D-03)
    - Test: empty vulns_df / empty assets_df → safe four-channel render, gray cell, no raise (QUAL-03)
    - Test: RAG status from current external Critical count via direction="lower_is_better" with green=0/yellow=5 defaults
  </behavior>
  <action>
    Create class `ExternalDmzModule(BaseModule)` with `MODULE_ID="external_dmz"`,
    `DISPLAY_NAME="External / DMZ Exposure"`, `@register_module`. Module-level RAG defaults
    `_DEFAULT_GREEN_EXT_CRIT=0`, `_DEFAULT_YELLOW_EXT_CRIT=5` overridable via
    `config.options` (D-15-07). In `compute()`: call `scoped_assets_df, mismatches_df =
    external_scope(assets_df)` inline; if `scoped_assets_df.empty` return a valid ModuleData
    with gray "no_data" rag_strip and `summary_text="No external-scope assets in scope."`
    (error=None — valid state, NOT _empty_result-error). Otherwise scope vulns via
    `vulns_df[vulns_df["asset_uuid"].isin(set(scoped_assets_df["asset_uuid"].dropna()))]`,
    count by severity, build finding_count per mismatch asset via groupby size mapped onto
    mismatches_df with `.assign(finding_count=...)`. Set `analyst_rows=[("External Scope
    Mismatches", mismatches_df)]` with ONLY the locked columns asset_uuid, ip_address,
    owner_tag, untagged_reason, finding_count (Pitfall 11 — no plugin/CVE/per-severity).
    Add Owner cut via extract_owner for the per-Owner external table. Build rag_strip from
    external Critical count, direction="lower_is_better". Do NOT add a trend_snapshots kwarg
    or cold-start branch and do NOT add external_dmz to any frozenset (Phase-14 D-03).
    Implement all four render channels (render_email_panel = CONTRACT-01). Wrap compute() in
    try/except → _empty_result. Use safe_pct/safe_int/safe_format throughout. Write
    tests/test_external_dmz_module.py with CoW strict mode and synthetic-only fixtures
    (ip_address "203.0.113.x" TEST-NET-3, asset_uuid "00000000-...-0001").
  </action>
  <verify>
    <automated>cd D:/Projects/vuln-reporting && python -m pytest tests/test_external_dmz_module.py -x -q -W error::FutureWarning</automated>
  </verify>
  <acceptance_criteria>
    - reports/modules/external_dmz_module.py contains @register_module and MODULE_ID = "external_dmz"
    - python -c "import reports.modules; from reports.modules.registry import REGISTRY; assert 'external_dmz' in REGISTRY" exits 0
    - mismatch analyst DataFrame columns == [asset_uuid, ip_address, owner_tag, untagged_reason, finding_count] exactly (asserted by test)
    - grep confirms external_dmz is NOT present in reports/composed_report.py _MODULES_NEEDING_TREND_SNAPSHOTS, and the module file has no "trend_snapshots" / "cold_start" reference (Phase-14 D-03)
    - pytest tests/test_external_dmz_module.py exits 0 under -W error::FutureWarning
    - zero-external-asset input renders a gray "no_data" cell with error None
  </acceptance_criteria>
  <done>Module registered, mismatch schema locked, current-snapshot-only (no trend), four channels safe on zero-row input, tests pass under CoW strict mode.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mismatches_df (asset-level: asset_uuid/ip/owner) → analyst tab / committed fixtures | asset-level mismatch fields are operator-local only; must never be committed or sent to AI |
| external scope determination → aggregate counts | only finding_count aggregates may cross into committed/email channels |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03-PII | Information Disclosure | mismatch analyst tab + test fixtures | mitigate | Locked schema (asset_uuid, ip_address, owner_tag, untagged_reason, finding_count) enforced in render_analyst_tabs; no plugin/CVE/per-finding rows (Pitfall 11 / D-11); fixtures synthetic RFC 5737 (QUAL-05) |
| T-15-03-SCOPE | Tampering (correctness) | external_scope classification | mitigate | Reuses Phase-14 substrate (tags authoritative, is_global gap detector); no re-implementation; DMZ+private not a mismatch (D-06) |
| T-15-03-DOS | Denial of Service | compute() on zero-row/malformed input | mitigate | Empty scoped_df gray-cell branch + try/except → _empty_result (QUAL-03) |
| T-15-03-SC | Tampering | npm/pip installs | accept | Zero new dependencies; no install task |
</threat_model>

<verification>
- `pytest tests/test_external_dmz_module.py -x -q -W error::FutureWarning` exits 0
- `external_dmz` present in registry; absent from `_MODULES_NEEDING_TREND_SNAPSHOTS`
- mismatch analyst columns match the locked schema exactly
</verification>

<success_criteria>
RPT-06 satisfied: external/DMZ Critical/High/Medium counts on externally-scoped assets render
on all four channels current-snapshot-only; mismatch list follows the locked aggregate schema;
zero-external groups render a gray cell; fixtures synthetic (QUAL-05); no trend branch (D-03).
</success_criteria>

<output>
Create `.planning/phases/15-independent-new-modules/15-03-SUMMARY.md` when done
</output>
