---
phase: 15-independent-new-modules
plan: "04"
type: execute
wave: 3
depends_on: ["15-01", "15-02"]
files_modified:
  - reports/modules/new_vs_remediated_module.py
  - reports/composed_report.py
  - tests/test_new_vs_remediated_module.py
autonomous: true
requirements: [RPT-01, QUAL-01, QUAL-02, QUAL-03, QUAL-05]
user_setup: []

must_haves:
  truths:
    - "RPT-01: A reader sees a monthly New vs Remediated trend (inflow vs outflow bars, net delta) with an Owner cut"
    - "D-15-01: inflow = first_found in month M OR resurfaced_date in month M (the accurate definition — NOT first_found-only; do not simplify)"
    - "D-15-02: inflow displayed as TWO stacked components — net-new (first_found) and resurfaced (resurfaced_date) — visible in PDF chart + supporting table"
    - "D-15-08: the in-progress current month is shown but labeled 'Month-to-date (partial)' in all four channels"
    - "QUAL-01: module branches on trend_snapshots insufficient_data and renders 'Trend data being established' — never NaN% or crash"
    - "QUAL-02: open-count context uses open_findings_at() (reopened-aware) — no ~19% REOPENED drop"
    - "Registers new_vs_remediated, vuln_density, accepted_recast in composed_report.py gate frozensets (pre-registers all three trend modules; 15-05/15-06 do not touch composed_report.py)"
  artifacts:
    - path: "reports/modules/new_vs_remediated_module.py"
      provides: "NewVsRemediatedModule four-channel MoM module, MODULE_ID=new_vs_remediated"
      contains: "@register_module"
      min_lines: 200
    - path: "reports/composed_report.py"
      provides: "frozenset registration of all three trend-dependent module IDs (+ accepted_recast recast gate)"
      contains: "new_vs_remediated"
    - path: "tests/test_new_vs_remediated_module.py"
      provides: "cold-start, stacked inflow split, partial-month label, reopened-aware, empty-data tests"
      min_lines: 120
  key_links:
    - from: "reports/modules/new_vs_remediated_module.py"
      to: "trend_snapshots kwarg (insufficient_data)"
      via: "QUAL-01 cold-start branch"
      pattern: "insufficient_data"
    - from: "reports/composed_report.py"
      to: "_MODULES_NEEDING_TREND_SNAPSHOTS"
      via: "gate registration for new_vs_remediated/vuln_density/accepted_recast"
      pattern: "new_vs_remediated"
---

<objective>
Build `new_vs_remediated`: a MoM four-channel module showing monthly inflow vs
outflow with a net-delta line and Owner cut. Inflow is the LOCKED accurate
definition (D-15-01: first_found OR resurfaced_date) displayed as TWO stacked
components (D-15-02: net-new + resurfaced). It is the first trend consumer, so
this plan ALSO registers all three Phase-15 trend-dependent module IDs in the
`composed_report.py` gate frozensets (so 15-05/15-06 need not touch that file).

Purpose: New-vs-Remediated velocity with explicit reopen churn (RPT-01).
Output: module + composed_report gate registration + unit test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-independent-new-modules/15-CONTEXT.md
@.planning/phases/15-independent-new-modules/15-PATTERNS.md
@.planning/phases/14-shared-substrates-composed-report-gates/14-03-SUMMARY.md

<interfaces>
<!-- trend_snapshots kwarg arrives via composed_report gate fan-out (Phase-14 D-16):
     {"snapshots": [ {month, tag_filter, critical, high, medium, low, asset_count,
       on_time_asset_count, reopened_count, accepted_count, recast_count,
       new_findings_count, fixed_findings_count, generated_at}, ... ],
      "insufficient_data": bool}  -- new fields from 15-02 may be None on older snapshots -->

<!-- OUTFLOW SOURCE (Option B, checker fix): the per-month remediated/outflow count is the
     snapshot AGGREGATE field trend_snapshots["snapshots"][i]["fixed_findings_count"] written by
     capture_snapshot() in plan 15-02. There is NO live fixed_vulns_df kwarg for this module:
     new_vs_remediated is NOT in _MODULES_NEEDING_FIXED_VULNS (only critical_remediation_sla is),
     so fixed_vulns_df=None is the NORMAL production path and NO _MODULES_NEEDING_FIXED_VULNS
     registration is needed. fixed_findings_count already arrives via the existing
     _MODULES_NEEDING_TREND_SNAPSHOTS trend gate that Task 1 registers this module into. -->

<!-- open_findings_at (utils/open_count.py): open_findings_at(vulns_df, report_date) -> filtered df.
     Reopened-aware two-interval predicate (QUAL-02). Requires fetcher-normalized date columns. -->

<!-- Four-channel + helpers identical to 15-01; copy the in-repo reopened_vulns_module.py shape.
     Cold-start result pattern + partial-month label helper are in 15-PATTERNS.md. -->

<!-- composed_report.py current frozensets (lines ~73-90 / 644-659 in 15-PATTERNS.md):
     _MODULES_NEEDING_TREND_SNAPSHOTS = frozenset({"sc4_kwargs_stub"})
     _MODULES_NEEDING_RECAST_RULES    = frozenset({"sc4_kwargs_stub"}) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register the three trend-dependent module IDs in composed_report.py gates</name>
  <files>reports/composed_report.py</files>
  <read_first>
    - reports/composed_report.py lines 69-91 (existing frozensets) and 197-249 (conditional fetch + composer_kwargs fan-out blocks — already implemented in Phase 14)
    - .planning/phases/14-shared-substrates-composed-report-gates/14-03-SUMMARY.md (D-15/D-16/D-17 gate contract; stub-only seeding to be expanded now)
    - .planning/phases/15-independent-new-modules/15-PATTERNS.md "reports/composed_report.py — frozenset gate additions" section (lines 636-694)
  </read_first>
  <action>
    Extend (do NOT rewrite the fetch blocks) the two existing frozensets in
    reports/composed_report.py per D-17: add "new_vs_remediated", "vuln_density",
    "accepted_recast" to `_MODULES_NEEDING_TREND_SNAPSHOTS` (keeping "sc4_kwargs_stub"),
    and add "accepted_recast" to `_MODULES_NEEDING_RECAST_RULES` (keeping "sc4_kwargs_stub").
    Do NOT add new_vs_remediated to `_MODULES_NEEDING_FIXED_VULNS` — it consumes the
    aggregate fixed_findings_count from the trend snapshot (Task 2, Option B), NOT a live
    fixed_vulns_df; leave that frozenset as {"critical_remediation_sla"}.
    Do NOT add external_dmz (D-03 — current-snapshot only). Do NOT change the conditional
    fetch blocks, composer_kwargs assembly, or run_report() signature (D-15 — already correct
    from Phase 14). This pre-registers all three trend modules so 15-05 (vuln_density) and
    15-06 (accepted_recast) need not touch this file.
  </action>
  <verify>
    <automated>cd D:/Projects/vuln-reporting && python run_all.py --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - reports/composed_report.py _MODULES_NEEDING_TREND_SNAPSHOTS contains new_vs_remediated, vuln_density, accepted_recast, sc4_kwargs_stub
    - reports/composed_report.py _MODULES_NEEDING_RECAST_RULES contains accepted_recast, sc4_kwargs_stub
    - new_vs_remediated is NOT added to _MODULES_NEEDING_FIXED_VULNS (still {"critical_remediation_sla"}) — grep confirms (Option B: outflow from trend snapshot, not live fixed_vulns_df)
    - external_dmz is NOT in either trend/recast frozenset (grep confirms)
    - python run_all.py --dry-run exits 0 (no regression to existing composed groups)
    - run_report() signature unchanged (D-15) — confirmed by source inspection
  </acceptance_criteria>
  <done>All three trend module IDs registered; recast gate includes accepted_recast; fixed-vulns gate untouched; --dry-run green; fetch blocks/signature untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build NewVsRemediatedModule with stacked inflow + cold-start + four channels</name>
  <files>reports/modules/new_vs_remediated_module.py, tests/test_new_vs_remediated_module.py</files>
  <read_first>
    - reports/modules/reopened_vulns_module.py (in-repo pathfinder shape from 15-01)
    - .planning/phases/15-independent-new-modules/15-PATTERNS.md "reports/modules/new_vs_remediated_module.py" section (lines 265-367 — trend kwarg receipt, cold-start result, inflow D-15-01/02 mask logic, partial-month label, chart_data structure)
    - .planning/phases/15-independent-new-modules/15-02-PLAN.md (capture_snapshot fixed_findings_count contract — the aggregate outflow source this task consumes)
    - utils/open_count.py (open_findings_at two-interval predicate — QUAL-02)
    - data/trend_store.py read_trend contract (insufficient_data semantics) + 15-02 new snapshot fields (fixed_findings_count, new_findings_count)
    - reports/modules/rag_utils.py + format_utils.py + board_report_utils.extract_owner
  </read_first>
  <behavior>
    - Test: trend_snapshots is None OR insufficient_data=True → _build_cold_start_result; metrics["cold_start"]==True; summary contains "Trend data being established"; error None; no NaN% (QUAL-01)
    - Test: inflow net_new mask = first_found month==M; resurfaced mask = resurfaced_date month==M AND NOT net_new (D-15-01/02) — both counts tracked separately
    - Test: a finding with first_found and resurfaced_date in different months counts as net_new in its first_found month and resurfaced in its resurfaced_date month (no double-count within a month)
    - Test (OUTFLOW SOURCE, Option B): a fixture whose snapshot for month M carries fixed_findings_count=N renders an outflow/remediated bar of N for month M AND a net-delta of (total_inflow − N) for month M — outflow is sourced from trend_snapshots["snapshots"][i]["fixed_findings_count"], NOT from a fixed_vulns_df kwarg
    - Test (cold-start outflow, D-15-06): a snapshot whose fixed_findings_count is absent/None (older snapshot) branches that month to the insufficient_data / "Trend data being established" outflow notice — it is NOT treated as a silent zero outflow
    - Test: current month label includes "MTD" / "partial" in chart_data + table (D-15-08)
    - Test: open-count context uses open_findings_at (reopened-aware) — REOPENED not dropped (QUAL-02)
    - Test: MoM net-delta uses a safe zero-denominator helper — prior=0 yields "N/A" not ZeroDivisionError (Pitfall 5)
    - Test: fixed_vulns_df kwarg is NOT consulted — module works with fixed_vulns_df absent/None (the normal production path); no AttributeError/KeyError when it is missing
    - Test: empty vulns_df / multi-snapshot present-but-zero-owner → safe four-channel render (QUAL-03)
  </behavior>
  <action>
    Create class `NewVsRemediatedModule(BaseModule)` with `MODULE_ID="new_vs_remediated"`,
    `DISPLAY_NAME="New vs Remediated"`, `@register_module`. In compute(): receive
    `trend_snapshots = kwargs.get("trend_snapshots")`; compute `cold_start = trend_snapshots
    is None or trend_snapshots.get("insufficient_data", True)` and return
    `_build_cold_start_result(config)` if so (QUAL-01 — coherent ModuleData, error=None,
    metrics["cold_start"]=True, "Trend data being established" notice). Otherwise per month
    compute inflow as two SEPARATE components (D-15-01/02): net_new = findings with first_found
    in month M; resurfaced = findings with resurfaced_date in month M AND NOT net_new (exclude
    the first_found==resurfaced edge); total_inflow = net_new + resurfaced.

    OUTFLOW (Option B — checker fix, D-15-06): derive the per-month remediated/outflow count
    from the AGGREGATE snapshot field `trend_snapshots["snapshots"][i]["fixed_findings_count"]`
    (written by capture_snapshot() in plan 15-02). This is the canonical remediated source.
    Do NOT read or rely on a live `fixed_vulns_df` kwarg — `fixed_vulns_df=None` is the NORMAL
    production path (new_vs_remediated is intentionally NOT in _MODULES_NEEDING_FIXED_VULNS),
    and NO _MODULES_NEEDING_FIXED_VULNS registration is needed. Per month: outflow =
    that snapshot's fixed_findings_count; net_delta = total_inflow − outflow. PRESERVE the
    cold-start contract: when a month's snapshot LACKS fixed_findings_count (older snapshots
    pre-15-02, D-15-06 — value absent/None), branch that month to the
    insufficient_data / "Trend data being established" notice for the outflow/net-delta —
    NEVER treat an absent fixed_findings_count as a silent zero outflow.

    Use open_findings_at for any open-count context (QUAL-02). Build a module-level
    `_safe_mom_delta(curr, prev)` helper returning "N/A" when prev is None/0 (Pitfall 5);
    never compute % inline in a render method. Apply the partial-month label helper
    (current period → "{month} (MTD — partial)", D-15-08) in ALL four channels. chart_data
    carries per-month net_new, resurfaced, outflow (= fixed_findings_count), net_delta arrays
    for the stacked PDF bar + delta line. Owner cut via extract_owner. Build rag_strip
    (Green if net negative/backlog shrinking, Amber flat, Red growing). Implement all four
    channels (render_email_panel = CONTRACT-01); headline may use combined inflow but the
    PDF chart + table MUST show the net-new/resurfaced split (D-15-02). Wrap compute() in
    try/except → _empty_result. Only safe_pct/safe_int/safe_format for numerics; .assign() only
    (CoW). Write tests with CoW strict mode + synthetic fixtures.
  </action>
  <verify>
    <automated>cd D:/Projects/vuln-reporting && python -m pytest tests/test_new_vs_remediated_module.py -x -q -W error::FutureWarning</automated>
  </verify>
  <acceptance_criteria>
    - reports/modules/new_vs_remediated_module.py contains @register_module and MODULE_ID = "new_vs_remediated"
    - new_vs_remediated present in registry on import reports.modules
    - cold-start test: single-snapshot/insufficient_data fixture returns ModuleData error None, metrics["cold_start"] True, no "NaN" string in any rendered channel
    - inflow split test asserts net_new and resurfaced are distinct counts surfaced in chart_data/table (D-15-02)
    - OUTFLOW test (Option B): a fixture snapshot carrying fixed_findings_count=N renders an outflow bar of N and a net-delta of (total_inflow − N) for that month — outflow sourced from trend_snapshots[...]["fixed_findings_count"], asserted by the test
    - cold-start-outflow test: a snapshot with absent/None fixed_findings_count yields the "Trend data being established" notice for that month's outflow — NOT a silent zero outflow
    - grep confirms the module does NOT read kwargs.get("fixed_vulns_df") for the outflow count (Option B); fixed_vulns_df=None tolerated
    - partial-month test asserts current month label contains "partial" (D-15-08)
    - pytest tests/test_new_vs_remediated_module.py exits 0 under -W error::FutureWarning
    - grep confirms open_findings_at used for open-count context (QUAL-02) and no df["col"]= after filter
  </acceptance_criteria>
  <done>Module registered; cold-start safe; stacked inflow split visible; outflow sourced from trend fixed_findings_count with cold-start guard for absent values; partial-month labeled; reopened-aware; tests pass under CoW strict mode.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| trend_snapshots dict → MoM delta render | missing/short history can produce NaN%/crash if cold-start unguarded |
| absent fixed_findings_count → outflow count | an older snapshot lacking the field must cold-start, not silently read as zero outflow |
| vulns_df → analyst rows / fixtures | new-arrivals list could carry asset-level PII |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04-COLDSTART | Denial of Service | MoM delta on <2 snapshots | mitigate | insufficient_data branch returns coherent cold-start ModuleData; _safe_mom_delta guards prior=0 (QUAL-01/Pitfall 5) |
| T-15-04-OUTFLOW | Tampering (correctness) | absent fixed_findings_count treated as zero outflow | mitigate | Option B: outflow = snapshot fixed_findings_count; absent value → "Trend data being established" notice, never silent zero (D-15-06); test asserts the branch |
| T-15-04-PII | Information Disclosure | analyst rows + test fixtures | mitigate | Aggregate per-Owner/per-month counts only; fixtures synthetic RFC 6761/5737 (QUAL-05) |
| T-15-04-CORRECT | Tampering (correctness) | reopened double-count in inflow/outflow | mitigate | D-15-01/02 stacked split + outflow from aggregate fixed_findings_count (FIXED-only, REOPENED excluded at capture, Pitfall 2); open_findings_at for open context (QUAL-02) |
| T-15-04-REGRESS | Tampering | composed_report.py existing groups | mitigate | Only frozenset membership extended (fixed-vulns gate untouched); fetch blocks/signature untouched; --dry-run no-regression check |
| T-15-04-SC | Tampering | npm/pip installs | accept | Zero new dependencies; no install task |
</threat_model>

<verification>
- `python run_all.py --dry-run` exits 0 (gate registration no-regression)
- `pytest tests/test_new_vs_remediated_module.py -x -q -W error::FutureWarning` exits 0
- cold-start fixture renders the notice, never "NaN%"
- outflow fixture (fixed_findings_count=N) renders an outflow bar of N; absent fixed_findings_count renders the cold-start notice, not zero
</verification>

<success_criteria>
RPT-01 satisfied: monthly inflow (stacked net-new + resurfaced, D-15-01/02) vs outflow
(sourced from snapshot fixed_findings_count, Option B) + net delta + Owner cut on all four
channels; cold-start safe (QUAL-01); reopened-aware (QUAL-02); current month labeled partial
(D-15-08); all three trend module IDs registered in composed_report.py.
</success_criteria>

<output>
Create `.planning/phases/15-independent-new-modules/15-04-SUMMARY.md` when done
</output>
</output>
