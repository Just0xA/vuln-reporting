---
phase: 15-independent-new-modules
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - data/trend_store.py
  - scripts/capture_trend_snapshot.py
  - tests/content/test_trend_store.py
autonomous: true
requirements: [RPT-02, RPT-03, RPT-04, QUAL-05]
user_setup: []

must_haves:
  truths:
    - "D-15-04: capture_snapshot() is EXTENDED (not replaced) with new aggregate count fields — one trend file per scope, no module-local snapshot files"
    - "D-15-05: new aggregate fields added — on_time_asset_count (density denom, Phase-14 D-02), reopened_count, accepted_count, recast_count, new_findings_count, fixed_findings_count"
    - "D-15-06: extension is BACKWARD-COMPATIBLE — existing snapshots lacking the new fields are a valid cold-start (insufficient_data) for the new dimensions, never a crash; existing read_trend()/management_summary callers unaffected"
    - "QUAL-05: all new snapshot fields are AGGREGATE COUNTS ONLY — no hostnames/IPs/plugin names/asset-level rows persisted"
    - "Density MoM cold-starts on the NEW on_time_asset_count field and accumulates forward — no back-reuse of the all-asset asset_count history (Phase-14 D-02)"
  artifacts:
    - path: "data/trend_store.py"
      provides: "capture_snapshot() with new optional aggregate params + new_entry fields"
      contains: "on_time_asset_count"
    - path: "scripts/capture_trend_snapshot.py"
      provides: "cron entry point passing the new aggregate counts into capture_snapshot()"
      contains: "on_time_asset_count"
    - path: "tests/content/test_trend_store.py"
      provides: "backward-compat + new-field smoke tests (absent field cold-start, not crash)"
  key_links:
    - from: "scripts/capture_trend_snapshot.py"
      to: "utils.asset_count.count_on_time_assets"
      via: "on_time_asset_count denominator (Phase-14 substrate)"
      pattern: "count_on_time_assets"
    - from: "data/trend_store.py new_entry"
      to: "vuln_density / new_vs_remediated / accepted_recast modules"
      via: "MoM trend fields consumed downstream"
      pattern: "on_time_asset_count|reopened_count|accepted_count|recast_count"
---

<objective>
Extend `data/trend_store.py:capture_snapshot()` (backward-compatibly, D-15-04/05/06)
with new aggregate count fields and wire the cron entry point
`scripts/capture_trend_snapshot.py` to supply them. This is the shared substrate
prerequisite for the three trend-dependent Phase-15 modules (vuln_density,
new_vs_remediated, accepted_recast). Existing snapshots and existing callers
(management_summary, read_trend) MUST continue to work unchanged — absent new
fields are a valid cold-start, never a crash.

Purpose: Unblock the trend-dependent modules with new MoM dimensions, accumulating forward.
Output: extended capture_snapshot(), wired entry point, backward-compat tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-independent-new-modules/15-CONTEXT.md
@.planning/phases/15-independent-new-modules/15-PATTERNS.md
@.planning/phases/14-shared-substrates-composed-report-gates/14-02-SUMMARY.md

<interfaces>
<!-- Current capture_snapshot signature (data/trend_store.py line 222) -->
capture_snapshot(df, assets_df, date, dimension="severity", tag_filter="all_assets",
                 trend_dir=None, enriched_assets=None) -> Path

<!-- Current new_entry dict (data/trend_store.py lines 307-313) -->
new_entry = {"month": month_str, "tag_filter": tag_filter, **count_entry,
             "asset_count": asset_count, "generated_at": generated_at_str}

<!-- read_trend (data/trend_store.py line 338) cold-start safe; returns
     {"snapshots": [...], "insufficient_data": bool}; insufficient_data True when <2 snapshots -->

<!-- Phase-14 substrate (utils/asset_count.py): count_on_time_assets(assets_df, report_date,
     window_days=ON_TIME_SCAN_WINDOW_DAYS) -> int | None  (None when no on-time licensed assets, D-14) -->

<!-- cron entry point (scripts/capture_trend_snapshot.py lines 259-282) currently calls
     capture_snapshot twice: severity dimension + owner dimension, both tag_filter="all_assets" -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend capture_snapshot() with backward-compatible aggregate fields</name>
  <files>data/trend_store.py, tests/content/test_trend_store.py</files>
  <read_first>
    - data/trend_store.py lines 222-335 (capture_snapshot signature, docstring IN-06 scope-coupling, new_entry dict, idempotent overwrite block) and lines 338-393 (read_trend contract)
    - .planning/phases/15-independent-new-modules/15-PATTERNS.md "data/trend_store.py — backward-compatible extension" section (lines 559-631 — new optional params, new_entry extension, backward-compat read guard)
    - tests/content/test_trend_store.py (existing test patterns to mirror; do not regress)
    - .planning/phases/14-shared-substrates-composed-report-gates/14-02-SUMMARY.md (count_on_time_assets None-sentinel contract — D-14)
  </read_first>
  <behavior>
    - Test: capture_snapshot called WITHOUT new params writes a snapshot whose new fields are None/absent — and a subsequent read returns it without error (backward compat, D-15-06)
    - Test: capture_snapshot called WITH on_time_asset_count=N, reopened_count=R, accepted_count=A, recast_count=C, fixed_vulns_df=df writes new_entry containing those aggregate fields
    - Test: an OLD snapshot dict (no new fields) loaded by read_trend means a consumer's snap.get("on_time_asset_count") returns None treated as cold-start for that dimension (no KeyError, no crash)
    - Test: new fields are ints (or None) never DataFrames or row lists (QUAL-05 aggregate-only)
    - Test: existing severity + owner dimension snapshots still write identically (no regression to asset_count/count_entry)
    - Test: new_findings_count / fixed_findings_count derived as aggregate ints from the supplied frames
  </behavior>
  <action>
    Add new OPTIONAL keyword params to capture_snapshot() with None defaults
    (backward-compat, D-15-06): on_time_asset_count: Optional[int]=None,
    reopened_count: Optional[int]=None, accepted_count: Optional[int]=None,
    recast_count: Optional[int]=None, fixed_vulns_df: Optional[pd.DataFrame]=None.
    Derive new_findings_count (count of df rows whose first_found month == snapshot
    month) and fixed_findings_count (count of fixed_vulns_df rows whose last_fixed
    month == snapshot month AND state==FIXED) as aggregate ints when the inputs are present,
    else None. Append all six new keys to the new_entry dict AFTER the existing
    asset_count key and BEFORE generated_at, writing None when a source param/derivation is
    absent. Do NOT change the dimension dispatch, the idempotent (month, tag_filter)
    overwrite block, or _atomic_write_json. Keep the IN-06 scope-coupling docstring note and
    add a one-line note that the new fields are aggregate-only (QUAL-05). Add tests to
    tests/content/test_trend_store.py with pd.options.mode.copy_on_write = True, synthetic
    data only, covering the behavior list above. Use .assign() for any derived column work
    (no df["col"]= after a filter).
  </action>
  <verify>
    <automated>cd D:/Projects/vuln-reporting && python -m pytest tests/content/test_trend_store.py -x -q -W error::FutureWarning</automated>
  </verify>
  <acceptance_criteria>
    - data/trend_store.py capture_snapshot signature contains on_time_asset_count, reopened_count, accepted_count, recast_count, fixed_vulns_df as optional params
    - new_entry dict contains the six new aggregate keys
    - A snapshot written without the new params is read back by read_trend with no exception and snap.get("on_time_asset_count") is None (backward-compat proven by test)
    - pytest tests/content/test_trend_store.py exits 0 under -W error::FutureWarning
    - grep confirms new_entry stores ints/None only among the new keys — no DataFrame or list values (QUAL-05)
  </acceptance_criteria>
  <done>capture_snapshot extended, all trend-store tests pass, old snapshots remain readable, new fields are aggregate ints/None.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the new aggregate counts into the cron capture entry point</name>
  <files>scripts/capture_trend_snapshot.py</files>
  <read_first>
    - scripts/capture_trend_snapshot.py lines 227-285 (fetch + capture_snapshot call sites; severity + owner dimensions, exit-code contract 0/2/3)
    - utils/asset_count.py (count_on_time_assets signature + None sentinel, Phase-14 D-14)
    - data/fetchers.py (fetch_all_vulnerabilities / fetch_all_assets already imported; check whether a fixed-vulns fetch helper exists to optionally supply fixed_vulns_df)
    - .planning/phases/15-independent-new-modules/15-PATTERNS.md trend_store extension section
  </read_first>
  <action>
    In scripts/capture_trend_snapshot.py main(), after the assets/vulns fetch and before
    the SEVERITY capture_snapshot call, compute the new aggregate counts: on_time_asset_count
    = count_on_time_assets(assets_df, snapshot_date) (import from utils.asset_count; None-safe
    per D-14); reopened_count = int((df["state"].astype(str).str.upper()=="REOPENED").sum());
    accepted_count and recast_count from df["severity_modification_type"].astype(str).str.upper()
    via .isin({"ACCEPTED"}) / .isin({"RECASTED"}). Pass these as the new keyword args into the
    SEVERITY capture_snapshot(...) call only (owner-dimension call unchanged). If a fixed-vulns
    fetch helper exists in data/fetchers, fetch it fail-soft (try/except, log warning, proceed
    with fixed_vulns_df=None on failure) and pass it for the new/fixed derivation; if no such
    helper exists, pass fixed_vulns_df=None and log that fixed-count fields cold-start until the
    fixed export is wired. Preserve the severity-then-owner ordering and the exit-code contract
    (0/2/3). Log the resolved aggregate counts at INFO (counts only, no row data, QUAL-05).
  </action>
  <verify>
    <automated>cd D:/Projects/vuln-reporting && python scripts/capture_trend_snapshot.py --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - scripts/capture_trend_snapshot.py imports count_on_time_assets and passes on_time_asset_count, reopened_count, accepted_count, recast_count into the severity capture_snapshot call
    - python -c "import ast; ast.parse(open('scripts/capture_trend_snapshot.py').read())" exits 0 (valid syntax)
    - python scripts/capture_trend_snapshot.py --dry-run exits 0 (entry point still loads, no regression)
    - INFO log line emits aggregate counts only (no asset-level rows) — confirmed by source inspection
  </acceptance_criteria>
  <done>Cron entry point supplies the new aggregate counts to the severity snapshot; --dry-run exits 0; counts logged aggregate-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live vulns/assets data → persisted trend JSON | snapshot file is committed-adjacent / operator-shared; must hold aggregate counts only |
| new params → new_entry dict | a DataFrame accidentally stored instead of a count would persist row-level PII |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02-PII | Information Disclosure | new_entry aggregate fields | mitigate | New keys store int/None only (counts); test asserts no DataFrame/list values persist (QUAL-05 / D-04-08); data/trend/ is gitignored |
| T-15-02-COMPAT | Tampering (data integrity) | existing snapshots / read_trend / management_summary | mitigate | All new params optional with None defaults; backward-compat test proves old snapshots read without KeyError; idempotent overwrite block unchanged (D-15-06) |
| T-15-02-LOG | Information Disclosure | INFO log of aggregate counts | accept | Counts only logged, no row-level fields; consistent with existing trend logging |
| T-15-02-SC | Tampering | npm/pip installs | accept | Zero new dependencies; no package-manager install task in this plan |
</threat_model>

<verification>
- `pytest tests/content/test_trend_store.py -x -q -W error::FutureWarning` exits 0
- `python scripts/capture_trend_snapshot.py --dry-run` exits 0 (no regression)
- A pre-extension snapshot dict is read back without error and new-field access returns None
</verification>

<success_criteria>
capture_snapshot() carries the six new aggregate dimensions backward-compatibly (D-15-04/05/06);
the cron entry point supplies them; old snapshots and existing callers are unaffected; new fields
are aggregate ints/None only (QUAL-05). Trend-dependent modules in Wave 3 can now cold-start on
the new fields and accumulate forward.
</success_criteria>

<output>
Create `.planning/phases/15-independent-new-modules/15-02-SUMMARY.md` when done
</output>
