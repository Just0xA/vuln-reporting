---
phase: 15-independent-new-modules
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - reports/modules/reopened_vulns_module.py
  - tests/test_reopened_vulns_module.py
autonomous: false
requirements: [RPT-03, QUAL-02, QUAL-03, QUAL-05]
user_setup: []

must_haves:
  truths:
    - "RPT-03: A reader sees Reopened Vulnerabilities count + rate with an Owner cut and an analyst drill-down (plugin_id, resurfaced_date, reopen_lag_days)"
    - "D-15-09: reopened_vulns is the PATHFINDER — builds first, proves the full four-channel shape before the other 4 modules copy it"
    - "RPT-03: state==REOPENED is the PRIMARY filter for the current-month count (read directly from vulns_df, not via open_findings_at)"
    - "RPT-03: rate denominator gracefully degrades to count-only when the fixed export is absent — explicit disclosure note, never a silent zero"
    - "QUAL-03: module survives a zero-row filtered vulns_df on all four channels without raising; gray No-Data RAG cell"
    - "QUAL-05: the analyst drill-down + test fixtures contain only synthetic RFC 6761/5737 data — no real hostnames/IPs/plugin names"
    - "Live-tenant resurfaced_date population spot-check finalizes the analyst drill-down schema (plugin_id, resurfaced_date, reopen_lag_days) BEFORE parallel modules copy the shape"
  artifacts:
    - path: "reports/modules/reopened_vulns_module.py"
      provides: "ReopenedVulnsModule four-channel module, MODULE_ID=reopened_vulns"
      contains: "@register_module"
      min_lines: 200
    - path: "tests/test_reopened_vulns_module.py"
      provides: "Unit tests: REOPENED filter, reopen-lag, count-only degradation, empty-data guard x4 channels, synthetic fixtures"
      min_lines: 120
  key_links:
    - from: "reports/modules/reopened_vulns_module.py"
      to: "reports.modules.board_report_utils.extract_owner"
      via: "Owner cut (Unassigned catch-all)"
      pattern: "extract_owner"
    - from: "reports/modules/reopened_vulns_module.py"
      to: "reports.modules.registry.register_module"
      via: "auto-discovery self-registration"
      pattern: "@register_module"
---

<objective>
Build the PATHFINDER module `reopened_vulns` (D-15-09): a current-snapshot
four-channel metric module counting findings in `state==REOPENED` with a reopen
rate, Owner cut, and analyst drill-down. It has NO trend dependency, so it
validates the full four-channel render contract + empty-data guard in isolation
before the other four Phase-15 modules copy the proven shape.

A live-tenant `resurfaced_date`-population spot-check (checkpoint) finalizes the
analyst drill-down schema (plugin_id, resurfaced_date, reopen_lag_days) before
the parallel modules in later waves mirror it.

Purpose: De-risk the four-channel shape + lock the Reopened analyst schema (RPT-03).
Output: `reports/modules/reopened_vulns_module.py` + its unit test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-independent-new-modules/15-CONTEXT.md
@.planning/phases/15-independent-new-modules/15-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs. Use directly — no codebase exploration. -->

ModuleData fields (reports/modules/base.py lines 112-142) the module must populate:
  module_id: str, display_name: str, metrics: dict, table_data: list,
  chart_data: dict, summary_text: str, metadata: dict,
  driver_narrative: str = ""          # 1-line "what's driving it" for render_email_panel
  analyst_rows: list[tuple[str, pd.DataFrame]] = []   # drill-down for render_analyst_tabs
  rag_strip: dict = {}                # pre-built cover-page strip cell
  error: Optional[str] = None         # None = valid no-data, str = render error

BaseModule contract (reports/modules/base.py):
  compute(self, vulns_df, assets_df, report_date, config, **kwargs) -> ModuleData  (line 200)
  _empty_result(self, message, config) -> ModuleData  (line 558) — gray RAG, error set
  render_pdf_section(data, config) -> str            (line 256, default "")
  render_excel_tabs(data, workbook, config) -> list[str]  (line 285, default [])
  render_email_panel(data, config) -> str            (line 330, default "")  -- CONTRACT-01, REQUIRED
  render_analyst_tabs(data, config) -> list[tuple]   (line 371, default [])
  render_rag_strip_entry(data, config) -> dict       (line 404; honors data.rag_strip)

extract_owner (reports/modules/board_report_utils.py line 214):
  extract_owner(assets_df) -> assets_df copy with "owner" column; "Unassigned" when Owner tag absent

rag_utils (reports/modules/rag_utils.py): rag_status_from_value(value, green_threshold, yellow_threshold, direction), build_rag_strip_entry(display_name, headline_value_str, status), STATUS_COLOR, NO_DATA_HEADLINE
format_utils (reports/modules/format_utils.py): safe_pct(v), safe_int(v), safe_format(v, spec)

vulns_df fields already fetched (data/fetchers.py lines 354-360): state, resurfaced_date, first_found, last_fixed, plugin_id, asset_uuid, severity
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build ReopenedVulnsModule current-snapshot core + four channels</name>
  <files>reports/modules/reopened_vulns_module.py, tests/test_reopened_vulns_module.py</files>
  <read_first>
    - reports/modules/mttr_by_severity_module.py (the EXACT four-channel analog — copy import block, module skeleton, compute() guard pattern, render_pdf_section/render_excel_tabs error guards, ModuleData construction)
    - reports/modules/base.py lines 112-142, 200, 256-459, 558-603 (ModuleData fields, _empty_result, four-channel signatures, render_rag_strip_entry honoring data.rag_strip)
    - reports/modules/board_report_utils.py lines 214-280 (extract_owner + Unassigned catch-all)
    - reports/modules/rag_utils.py lines 82-163 (rag_status_from_value, build_rag_strip_entry)
    - reports/modules/format_utils.py (safe_pct, safe_int, safe_format)
    - .planning/phases/15-independent-new-modules/15-PATTERNS.md "reports/modules/reopened_vulns_module.py (PATHFINDER)" section (lines 25-261 — full code pattern incl. reopen-lag, owner cut, RAG strip, render method shapes)
  </read_first>
  <behavior>
    - Test: vulns_df with 3 REOPENED + 2 OPEN + 1 FIXED rows → metrics["reopened_count"] == 3
    - Test: filter is `vulns_df["state"].astype(str).str.upper() == "REOPENED"` (current snapshot, NOT open_findings_at)
    - Test: reopen_lag_days == (resurfaced_date - last_fixed).days per finding; None when resurfaced_date absent (NaT)
    - Test: Owner cut maps asset_uuid→owner via extract_owner; rows with no matching asset → "Unassigned"
    - Test: empty vulns_df → _empty_result; rag_strip is gray "no_data"; error is None on valid-zero, set on exception
    - Test: zero REOPENED rows (valid) → reopened_count==0, green RAG, error None (not _empty_result-error)
    - Test: render_email_panel returns non-empty HTML containing display name + count when data present; "" when data.error
    - Test: render_pdf_section returns str (never None); render_excel_tabs returns [tab_name]; render_analyst_tabs returns data.analyst_rows
  </behavior>
  <action>
    Create class `ReopenedVulnsModule(BaseModule)` with `MODULE_ID="reopened_vulns"`,
    `DISPLAY_NAME="Reopened Vulnerabilities"`, `REQUIRED_DATA=["vulns","assets"]`,
    `SUPPORTED_OUTPUTS=["pdf","excel","email"]`, decorated `@register_module`.
    Module-level RAG defaults `_DEFAULT_GREEN_RATE=5.0`, `_DEFAULT_YELLOW_RATE=10.0`
    overridable via `config.options.get("green_rate_threshold"/"yellow_rate_threshold")`
    per D-15-07. In `compute()`: empty-data guard first (QUAL-03); filter to REOPENED
    by uppercased `state`; reopen-lag via `.assign()` of `resurfaced_ts`/`last_fixed_ts`
    then `reopen_lag_days` (NEVER `df["col"]=` after filter — QUAL-03 CoW); Owner cut via
    `extract_owner` uuid→owner map with `.fillna("Unassigned")`; reopen rate from
    `kwargs.get("fixed_vulns_df")` denominator `reopened/(reopened+fixed_and_stayed_fixed)`
    — when absent, set rate None and `metadata["rate_disclosure"]="Rate omitted — fixed export unavailable."`
    Build `rag_strip` via `rag_status_from_value(reopen_rate, ..., direction="lower_is_better")`
    + `build_rag_strip_entry`. Populate `analyst_rows=[("Reopened Detail", df), ("By Owner", df)]`
    where detail columns are `plugin_id, resurfaced_date, reopen_lag_days, owner` ONLY
    (QUAL-05 — no hostnames/IPs). Implement all four render channels using the mttr analog's
    error guards; `render_email_panel` (CONTRACT-01, NOT render_email_kpis) returns "" on
    `data.error`. Wrap compute() body in try/except returning `_empty_result(str(exc), config)`.
    Use only `safe_pct`/`safe_int`/`safe_format` for numeric render values.
    Write `tests/test_reopened_vulns_module.py` with `pd.options.mode.copy_on_write = True`
    set at module level; all fixtures synthetic (asset_uuid `00000000-...-0001`, plugin_id
    `100001+`, owner names "Engineering"/"Operations"/"Unassigned").
  </action>
  <verify>
    <automated>cd D:/Projects/vuln-reporting && python -m pytest tests/test_reopened_vulns_module.py -x -q -W error::FutureWarning</automated>
  </verify>
  <acceptance_criteria>
    - `reports/modules/reopened_vulns_module.py` contains `@register_module` and `MODULE_ID = "reopened_vulns"`
    - `python -c "import reports.modules; from reports.modules.registry import REGISTRY; assert 'reopened_vulns' in REGISTRY"` exits 0 (auto-discovery works)
    - `pytest tests/test_reopened_vulns_module.py` exits 0 with `-W error::FutureWarning` (no pandas CoW ChainedAssignmentError)
    - grep confirms NO `df["` assignment after a filter and NO inline `f"{...:.1f}"` on metric values in the module file
    - render methods return correct types on empty input: `render_pdf_section`→str, `render_excel_tabs`→list, `render_email_panel`→"", `render_analyst_tabs`→[], `render_rag_strip_entry`→dict with gray cell
  </acceptance_criteria>
  <done>Module registered, all unit tests pass under CoW strict mode, four channels survive zero-row input.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live-tenant resurfaced_date population spot-check (D-15-09 / RPT-03)</name>
  <files>reports/modules/reopened_vulns_module.py</files>
  <action>
    HUMAN CHECKPOINT (D-15-09): pause for the operator to confirm `resurfaced_date`
    population on the live tenant BEFORE the four parallel Phase-15 modules copy the
    reopened analyst-drill-down shape. The executor presents the inspection one-liner
    (see how-to-verify), the operator runs it against a real cache and reports AGGREGATE
    coverage only, and the executor adjusts the analyst-tab disclosure wording if
    `resurfaced_date` is sparsely populated. No code change beyond that disclosure tweak.
  </action>
  <what-built>
    `reopened_vulns_module.py` with an analyst drill-down schema of
    `plugin_id, resurfaced_date, reopen_lag_days, owner`. The reopen-lag computation
    assumes `resurfaced_date` is populated on REOPENED findings on the live tenant.
    This checkpoint confirms that assumption before the four parallel Phase-15 modules
    copy this shape.
  </what-built>
  <how-to-verify>
    Run against a real cache (or live fetch) and inspect the REOPENED population's
    resurfaced_date coverage:
    1. `cd D:/Projects/vuln-reporting`
    2. Ensure a recent vulns export exists in `data/cache/<YYYY-MM-DD>/vulns_all.parquet`
       (or run `python scripts/warm_cache.py` / `python scripts/capture_trend_snapshot.py --date <today>`).
    3. Run this inspection one-liner (reads cache, prints aggregate coverage only — no PII):
       `python -c "import pandas as pd, glob; f=sorted(glob.glob('data/cache/*/vulns_all.parquet'))[-1]; df=pd.read_parquet(f); r=df[df['state'].astype(str).str.upper()=='REOPENED']; print('REOPENED rows:', len(r)); print('resurfaced_date populated:', r['resurfaced_date'].replace('', pd.NA).notna().sum() if len(r) else 0)"`
    4. Confirm: if `resurfaced_date populated` is a healthy fraction of `REOPENED rows`,
       the lag-days schema stands. If it is ~0, report that reopen_lag_days will be
       predominantly None and the analyst tab should disclose "reopen lag unavailable
       (resurfaced_date not populated on this tenant)".
    Do NOT paste row-level data (hostnames/IPs/plugin names) into the conversation —
    aggregate counts only (QUAL-05 / D-04-08).
  </how-to-verify>
  <resume-signal>Type "approved" if resurfaced_date coverage confirms the lag schema, or describe the coverage gap so the analyst-tab disclosure wording is adjusted before parallel modules copy the shape.</resume-signal>
  <done>Operator has confirmed resurfaced_date coverage; analyst-tab disclosure wording finalized; the reopened drill-down schema is locked for the parallel modules to copy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vulns_df → analyst_rows / committed test fixtures | asset-level finding rows could carry PII (hostnames, IPs, plugin names) if drill-down over-populated |
| live-tenant cache → conversation (Task 2) | spot-check could leak row-level REOPENED data to the AI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01-PII | Information Disclosure | analyst_rows drill-down + test fixtures | mitigate | Drill-down columns restricted to plugin_id, resurfaced_date, reopen_lag_days, owner — no hostnames/IPs (QUAL-05); all fixtures synthetic RFC 6761/5737 |
| T-15-01-SPOT | Information Disclosure | Task 2 live-tenant spot-check | mitigate | Inspection one-liner prints aggregate counts only; checkpoint instructions forbid pasting row-level data into the conversation (D-04-08) |
| T-15-01-DOS | Denial of Service | compute() on zero-row / malformed vulns_df | mitigate | Empty-data guard + try/except → _empty_result; render methods return safe defaults (QUAL-03) |
| T-15-01-SC | Tampering | npm/pip installs | accept | Zero new dependencies — pandas/openpyxl already pinned; no package-manager install task in this plan |
</threat_model>

<verification>
- `pytest tests/test_reopened_vulns_module.py -x -q -W error::FutureWarning` exits 0
- Module auto-discovered: `reopened_vulns` present in registry on `import reports.modules`
- Task 2 checkpoint approved (resurfaced_date coverage confirmed or disclosure wording adjusted)
</verification>

<success_criteria>
RPT-03 satisfied: Reopened count + rate + Owner cut + analyst drill-down render on all four
channels; state==REOPENED is the primary filter; rate degrades to count-only when fixed export
absent; zero-row input is safe (QUAL-03); fixtures are synthetic (QUAL-05). Pathfinder shape locked.
</success_criteria>

<output>
Create `.planning/phases/15-independent-new-modules/15-01-SUMMARY.md` when done
</output>
