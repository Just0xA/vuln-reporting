---
phase: 20-config-language-loader-matrix
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - run_all.py
  - contacts.example.yaml
autonomous: true
requirements: [CONF-03]
must_haves:
  truths:
    - "run_all.py --dry-run surfaces a duplicate delivery name as a prominent error and exits non-zero"
    - "run_all.py --dry-run surfaces an undefined contact: ref as a prominent error and exits non-zero"
    - "run_all.py --dry-run surfaces an inline email: block in directory mode as a prominent error and exits non-zero"
    - "run_all.py --dry-run surfaces the deprecated groups: alias as a warning and still exits zero when otherwise valid"
    - "contacts.example.yaml exists as a committed reference holding contacts + defaults only, using example.invalid addresses"
  artifacts:
    - path: "contacts.example.yaml"
      provides: "Committed reference contacts.yaml shape: named contact groups + top-level defaults.analyst_mailbox, example.invalid domain only"
      contains: "defaults"
    - path: "run_all.py"
      provides: "--dry-run surfacing of loader-level errors/warnings (D-10) flipping exit code 1 on error"
      exports: []
  key_links:
    - from: "run_all.py::_dry_run"
      to: "delivery/config_loader.py::resolve_config"
      via: "directory-mode error/warning surfacing block"
      pattern: "resolve_config"
---

<objective>
Make loader-level errors and warnings visible to operators and the future Phase 21 CI gate. Extend `run_all.py --dry-run` to surface directory-mode resolution errors (undefined `contact:` ref, duplicate delivery name, inline `email:` in directory mode) as prominent red output that flips the exit code to 1, and the deprecated `groups:` alias as a warning that keeps loading (D-10). Author the committed `contacts.example.yaml` reference shape (D-02) — the living template for the new "who" source, holding contacts + `defaults` only.

Purpose: CONF-03's error surfacing is the signal the Phase 21 pre-auth `--dry-run` CI gate depends on. The example file documents the new source language.

Output: extended `run_all.py::_dry_run` surfacing block, and a new committed `contacts.example.yaml`.
</objective>

<execution_context>
@/home/jmonroe/.claude/plugins/cache/gsd-plugin/gsd/4.0.2/workflows/execute-plan.md
@/home/jmonroe/.claude/plugins/cache/gsd-plugin/gsd/4.0.2/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-config-language-loader-matrix/20-CONTEXT.md
@.planning/phases/20-config-language-loader-matrix/20-PATTERNS.md
@.planning/phases/20-config-language-loader-matrix/20-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (delivery/config_loader.py) -->
- `resolve_config(config_path: Path) -> tuple[list[dict], list[str], list[str], dict[str, dict]]` returning `(groups, errors, warnings, metadata_by_delivery_name)`. This plan only needs `errors` + `warnings` for surfacing — unpack the 4-tuple and discard `groups` and `metadata_by_delivery_name` (e.g. `_groups, errors, warnings, _meta = resolve_config(config_path)`).

<!-- From run_all.py (existing --dry-run surfacing pattern to extend) -->
- `_dry_run(groups: list[dict]) -> int` — returns 0 all valid, 1 any error (run_all.py:394-489).
- The whole-config schema-error surfacing block re-reads raw YAML and prints red errors, flipping `any_errors = True` (run_all.py:413-430).
- `console.print("[bold red]...[/bold red]")` is the established error style; `console.print("[bold yellow]...[/bold yellow]")` fits warnings.
- Exit-code contract: `0` all valid, `1` any error (run_all.py:484-486).

<!-- Reference shape to mirror -->
- `delivery_config.example.yaml` header comment + `example.invalid` domain on every address (delivery_config.example.yaml:1-13, 27-34).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author contacts.example.yaml reference shape (D-02)</name>
  <files>contacts.example.yaml</files>
  <read_first>
    - delivery_config.example.yaml (full file — header-comment convention, example.invalid domain, the email: shapes at lines 27-34, 72-76)
    - .planning/phases/20-config-language-loader-matrix/20-CONTEXT.md (D-02 contacts.yaml holds contacts + defaults only; D-05/D-08 example.invalid only)
    - .planning/phases/20-config-language-loader-matrix/20-PATTERNS.md (contacts.example.yaml section — conventions to mirror)
  </read_first>
  <action>Create `contacts.example.yaml` at repo root mirroring `delivery_config.example.yaml`'s header-comment convention (a leading block explaining copy-to-`shared/` usage and stating every address uses the RFC 6761 reserved `example.invalid` domain so the template never delivers mail). Structure: a top-level `contacts:` mapping of named contact groups (keys like `exec_team`, `remediation_team`), each with `recipients:` (list of `example.invalid` addresses) and optional `cc:` / `reply_to:`; plus a top-level `defaults:` block with `analyst_mailbox: analyst-team@example.invalid` (D-02 — the one universal knob). Include at least one contact that overrides `reply_to` (to document the override path) and one that does not. Hold contacts + defaults ONLY — NO deliveries (the "nothing defined twice" guardrail). Every address MUST use the `example.invalid` domain (Hard Rule 2 / D-08).</action>
  <verify>
    <automated>python -c "import yaml; d=yaml.safe_load(open('contacts.example.yaml')); assert 'contacts' in d and 'defaults' in d and 'analyst_mailbox' in d['defaults'] and 'deliveries' not in d and 'groups' not in d, d; print('ok')"; grep -c "example.invalid" contacts.example.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `contacts.example.yaml` parses with `yaml.safe_load` and has top-level keys `contacts` and `defaults`.
    - `defaults.analyst_mailbox` is present.
    - No `deliveries:` or `groups:` key present (contacts + defaults only).
    - At least one contact defines its own `reply_to` and at least one does not.
    - Every email address uses `example.invalid` (`grep -c "example.invalid"` > 0; no other domains present via `grep -oE "@[a-zA-Z0-9.-]+" contacts.example.yaml | sort -u` showing only `@example.invalid`).
  </acceptance_criteria>
  <done>Committed `contacts.example.yaml` reference documents the new "who" source language with synthetic addresses; enforces contacts+defaults-only (D-02) and the analyst_mailbox knob (CONF-02).</done>
</task>

<task type="auto">
  <name>Task 2: Surface loader errors/warnings in run_all.py --dry-run (D-10)</name>
  <files>run_all.py</files>
  <read_first>
    - run_all.py (lines 394-489 — the full _dry_run body; lines 413-430 the exact whole-config surfacing block to extend; lines 484-489 exit-code contract)
    - delivery/config_loader.py (Plan 01 — resolve_config returns a 4-tuple; errors/warnings are elements 2 and 3)
    - .planning/phases/20-config-language-loader-matrix/20-CONTEXT.md (D-10 logger + --dry-run surfacing; deprecated alias = warning keeps loading, hard errors = error + non-zero exit)
    - .planning/phases/20-config-language-loader-matrix/20-PATTERNS.md (run_all.py --dry-run surfacing section — the console.print red-block pattern and any_errors flip)
  </read_first>
  <action>Extend `run_all.py::_dry_run` to add a directory-mode surfacing block alongside the existing whole-config schema-error block (run_all.py:413-430). After the existing `config_path` resolution, check `config_path.parent / "deliveries.d"`; if it is a directory, call `resolve_config(config_path)` and unpack the 4-tuple as `_groups, errors, warnings, _meta = resolve_config(config_path)` (this plan surfaces `errors`/`warnings` only; the resolved groups and metadata map are not needed here). Print each warning in a `console.print("[bold yellow]Config warnings:[/bold yellow]")` block with `console.print(f"  [yellow]! {w}[/yellow]")` (the deprecated `groups:` alias surfaces here and does NOT flip the exit code). Print each error in a `console.print("[bold red]Config errors:[/bold red]")` block with `console.print(f"  [red]x {e}[/red]")` and set `any_errors = True` (undefined contact ref, duplicate delivery name, inline email in directory mode — these flip exit code to 1 per the existing contract at run_all.py:484-486). Keep the existing single-file schema-error block intact for legacy/migrated single-file mode. Do not otherwise change the rich-table rendering. Import `resolve_config` at the first-party import group if not already imported.</action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('run_all.py').read()); print('parse ok')"; python -c "import run_all; print('import ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Given a synthetic directory-mode config with two team files sharing a delivery name, `python run_all.py --dry-run` (pointed at that config dir) prints a line containing `duplicate delivery name` in red and the process exits with code 1 (Hard Rule 1: this is a pre-auth dry-run, no live fetch).
    - Given a synthetic directory-mode config using the deprecated `groups:` alias but otherwise valid, `--dry-run` prints a yellow warning referencing the deprecated alias and exits 0.
    - Given a synthetic directory-mode config with an undefined `contact:` ref, `--dry-run` prints a red error and exits 1.
    - Given a synthetic directory-mode config with an inline `email:` block in a team file, `--dry-run` prints a red error and exits 1.
    - `python -c "import run_all"` succeeds; existing single-file `--dry-run` behavior for a legacy config is unchanged (the existing schema-error block still runs).
  </acceptance_criteria>
  <done>`--dry-run` prominently surfaces loader errors (exit 1) and the deprecated-alias warning (exit 0), extending the existing fail-loud pattern; the Phase 21 CI gate signal (exit code) is wired. CONF-03 surfacing complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator-authored YAML → --dry-run resolver call | dry-run re-resolves operator config to surface errors; malformed input crosses here |
| resolved error/warning strings → console output (CI artifact) | dry-run output is consumed by operators and the future Phase 21 CI gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-06 | Information disclosure | error strings printed by --dry-run | mitigate | Loader error/warning strings reference contact-key names and delivery names, not expanded recipient addresses (D-05). No `email.recipients` values printed in the error blocks. |
| T-20-07 | Tampering | contacts.example.yaml as a copy-to-prod template | mitigate | Template uses only `example.invalid` addresses (Hard Rule 2 / D-08); header comment instructs replacement before going live, mirroring delivery_config.example.yaml. |
| T-20-08 | Elevation of privilege | --dry-run triggering a live Tenable fetch | mitigate | `--dry-run` is a Hard-Rule-1-permitted pre-auth dry-run; the surfacing block only re-reads/resolves YAML (no fetcher entry point invoked). |
| T-20-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependencies (Hard Rule 8); no install task in this plan. |
</threat_model>

<verification>
- `python -c "import run_all"` succeeds.
- Synthetic directory-mode configs drive the four `--dry-run` outcomes (dup name → exit 1, undefined ref → exit 1, inline email → exit 1, deprecated alias → warning + exit 0).
- `contacts.example.yaml` parses and holds contacts + defaults only, `example.invalid` addresses only.
</verification>

<success_criteria>
- Loader errors surface prominently in `--dry-run` and flip exit code 1 (CONF-03, D-10).
- Deprecated `groups:` alias surfaces as a warning and keeps loading (exit 0).
- `contacts.example.yaml` documents the new "who" source shape (CONF-02 defaults, D-02).
</success_criteria>

<output>
Create `.planning/phases/20-config-language-loader-matrix/20-02-SUMMARY.md` when done
</output>
</content>
