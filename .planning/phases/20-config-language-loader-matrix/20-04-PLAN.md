---
phase: 20-config-language-loader-matrix
plan: 04
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - tests/fixtures/phase20_config_twin/contacts.yaml
  - tests/fixtures/phase20_config_twin/deliveries.d/exec.yaml
  - tests/fixtures/phase20_config_twin/deliveries.d/remediation.yaml
  - tests/fixtures/phase20_config_twin/deliveries.d/tag_profile.yaml
  - tests/fixtures/phase20_config_legacy/delivery_config.yaml
  - tests/baselines/effective_config_golden.json
  - tests/test_effective_config_golden.py
autonomous: true
requirements: [QUAL-06]
must_haves:
  truths:
    - "A committed JSON golden captures the normalized effective config resolved from the fixtures"
    - "The legacy single-file fixture resolves byte-identical to the golden"
    - "The migrated twin (contacts + defaults + contact: refs, deliveries: key + deliveries.d/) resolves to the SAME golden"
    - "The resolved effective config from both fixtures passes the unchanged delivery_config.schema.yaml"
    - "All fixtures and the golden use synthetic example.invalid identifiers only"
  artifacts:
    - path: "tests/baselines/effective_config_golden.json"
      provides: "Committed normalized effective-config golden (sorted-key JSON)"
      contains: "groups"
    - path: "tests/test_effective_config_golden.py"
      provides: "Two-way equality gate: legacy byte-identical + migrated-twin same-golden + schema-passes"
      min_lines: 60
    - path: "tests/fixtures/phase20_config_twin/contacts.yaml"
      provides: "Migrated-twin contacts + defaults source (example.invalid)"
      contains: "defaults"
  key_links:
    - from: "tests/test_effective_config_golden.py"
      to: "delivery/config_loader.py + run_all.py::_load_config"
      via: "resolve both fixtures and normalize to JSON"
      pattern: "_load_config|resolve_config"
    - from: "tests/test_effective_config_golden.py"
      to: "run_all.py::_validate_with_schema"
      via: "resolved effective config passes the unchanged schema"
      pattern: "_validate_with_schema"
---

<objective>
Build the effective-config golden test — the safety gate that makes the cutover reversible in review (QUAL-06). Author a migrated twin derived from `delivery_config.example.yaml` (a `contacts.yaml` + `deliveries.d/` team files using `contact:` refs and the `deliveries:` key) AND a legacy single-file twin (inline `email:`, `groups:` key) that represent the SAME deliveries. Commit a normalized JSON golden of the resolved effective config, and assert two-way equality: the legacy single-file resolves byte-identical to the golden, and the migrated twin resolves to the SAME golden (D-07). Prove the resolved effective config still passes the unchanged `delivery_config.schema.yaml`.

Purpose: QUAL-06 is the correctness bar for the whole phase — it proves the new source language resolves to today's group shape identically, so backward compatibility is demonstrable and the Phase 21 cutover is reversible.

Output: twin fixtures under `tests/fixtures/`, a committed golden under `tests/baselines/`, and `tests/test_effective_config_golden.py`.
</objective>

<execution_context>
@/home/jmonroe/.claude/plugins/cache/gsd-plugin/gsd/4.0.2/workflows/execute-plan.md
@/home/jmonroe/.claude/plugins/cache/gsd-plugin/gsd/4.0.2/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-config-language-loader-matrix/20-CONTEXT.md
@.planning/phases/20-config-language-loader-matrix/20-PATTERNS.md
@.planning/phases/20-config-language-loader-matrix/20-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (delivery/config_loader.py + run_all.py) -->
- `resolve_config(config_path: Path) -> tuple[list[dict], list[str], list[str]]`.
- `_load_config(config_path: Optional[Path] = None) -> list[dict]` — directory mode delegates to resolve_config; single-file mode uses legacy path. Point it at a fixture path to resolve that fixture.
- `_load_schema()` / `_validate_with_schema(raw, schema) -> list[str]` (empty list == valid).

<!-- delivery_config.example.yaml — the source shape the twin is derived from -->
- Groups exercised: "Executive Team" (weekly, cc + reply_to), "Remediation Team" (weekly, empty filters {}, recipients only), "Tag Severity & Type Profile" (on_demand, composed_report + modules, reply_to). The twin MUST exercise composed_report `modules:`, `cc`, `reply_to`, `on_demand`, and empty `filters: {}`.

<!-- Golden-path convention (tests/test_management_summary.py:59, test_board_summary_baseline.py:68-71) -->
- `_BASELINES_DIR = Path(__file__).parent / "baselines"`; golden loaded via `json.load(open(path))`.
- Fixture-directory layout: a named subdir under tests/fixtures/ holds a coherent bundle (like tests/fixtures/management_summary_parity/).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author legacy + migrated-twin fixtures from delivery_config.example.yaml (D-08)</name>
  <files>tests/fixtures/phase20_config_twin/contacts.yaml, tests/fixtures/phase20_config_twin/deliveries.d/exec.yaml, tests/fixtures/phase20_config_twin/deliveries.d/remediation.yaml, tests/fixtures/phase20_config_twin/deliveries.d/tag_profile.yaml, tests/fixtures/phase20_config_legacy/delivery_config.yaml</files>
  <read_first>
    - delivery_config.example.yaml (full file — the exact groups the twin must reproduce: names, schedules, filters, reports, modules, email cc/reply_to/on_demand/empty-filters)
    - contacts.example.yaml (Plan 02 — the contacts + defaults source shape to follow)
    - .planning/phases/20-config-language-loader-matrix/20-CONTEXT.md (D-07 two-way equality, D-08 twin derived from example, example.invalid only)
    - .planning/phases/20-config-language-loader-matrix/20-PATTERNS.md (deliveries.d/ twin fixture section — fixture-directory layout; exercise every current key)
  </read_first>
  <action>Create two fixture sets, both using `example.invalid` addresses ONLY (D-08 / Hard Rule 2). (A) LEGACY single-file `tests/fixtures/phase20_config_legacy/delivery_config.yaml`: a copy of the three `delivery_config.example.yaml` groups (Executive Team, Remediation Team, Tag Severity & Type Profile) with inline `email:` blocks and the legacy `groups:` key — this is the pre-migration form. IMPORTANT for the byte-identical golden (D-07): because the migrated twin will inject `defaults.analyst_mailbox` as a standing Cc + default Reply-To on every delivery, the legacy fixture's inline `email:` blocks MUST already contain that same analyst mailbox in `cc` and as `reply_to` (where the twin's contact does not override), so both resolve to identical effective configs. Set the analyst mailbox to a single synthetic address (e.g. `analyst-team@example.invalid`) consistently. (B) MIGRATED TWIN `tests/fixtures/phase20_config_twin/`: `contacts.yaml` holding named contact groups (one per delivery's recipient set) + a `defaults:` block with `analyst_mailbox: analyst-team@example.invalid`; plus `deliveries.d/exec.yaml`, `deliveries.d/remediation.yaml`, `deliveries.d/tag_profile.yaml` each with an `owner:` field and a `deliveries:` list referencing recipients via `contact:` (Exec uses cc+reply_to via its contact; Remediation uses recipients only + empty `filters: {}`; tag_profile uses `composed_report` + `modules:` + `on_demand` + a contact reply_to). The twin's contacts must be authored so that after resolution (contact recipients + analyst_mailbox standing Cc + reply_to rules) the effective config equals the legacy fixture's inline blocks exactly. Do NOT author the golden JSON yet (Task 2 generates it from the resolver to avoid hand-drift).</action>
  <verify>
    <automated>python -c "import yaml; [yaml.safe_load(open(p)) for p in ['tests/fixtures/phase20_config_legacy/delivery_config.yaml','tests/fixtures/phase20_config_twin/contacts.yaml','tests/fixtures/phase20_config_twin/deliveries.d/exec.yaml','tests/fixtures/phase20_config_twin/deliveries.d/remediation.yaml','tests/fixtures/phase20_config_twin/deliveries.d/tag_profile.yaml']]; print('all parse ok')"</automated>
  </verify>
  <acceptance_criteria>
    - All five fixture files parse with `yaml.safe_load`.
    - Legacy fixture uses the `groups:` key with inline `email:` blocks; twin uses `deliveries:` key with `contact:` refs and no inline `email.recipients`.
    - Twin exercises every current key: `composed_report` + `modules:`, `cc`, `reply_to`, `on_demand`, empty `filters: {}`.
    - Each twin team file has an `owner:` field; `contacts.yaml` has `contacts` + `defaults.analyst_mailbox`.
    - Every address across all five files uses `example.invalid` (`grep -rh "@" tests/fixtures/phase20_config_twin tests/fixtures/phase20_config_legacy | grep -oE "@[a-zA-Z0-9.-]+" | sort -u` shows only `@example.invalid`).
  </acceptance_criteria>
  <done>Legacy single-file + migrated-twin fixtures authored from the example config, both representing the same deliveries with synthetic addresses; twin exercises every current key including composed_report modules and empty filters (D-08).</done>
</task>

<task type="auto">
  <name>Task 2: Generate golden + two-way equality + schema-passes test (QUAL-06, D-07)</name>
  <files>tests/baselines/effective_config_golden.json, tests/test_effective_config_golden.py</files>
  <read_first>
    - tests/test_management_summary.py (lines 59, 111-118, 398-449 — golden-path constant, json.load, test_value_golden_parity resolve→compare pattern)
    - tests/test_phase4_schema_validation.py (lines 21, 26-119 — _load_schema/_validate_with_schema harness + _check/main() -> int style)
    - delivery/config_loader.py + run_all.py (_load_config, resolve_config, _load_schema, _validate_with_schema)
    - the Task 1 fixtures (legacy + twin)
    - .planning/phases/20-config-language-loader-matrix/20-CONTEXT.md (D-07 committed JSON golden + two-way equality; JSON normalization is Claude's Discretion)
    - .planning/phases/20-config-language-loader-matrix/20-PATTERNS.md (effective-config golden test section — normalization + two-way assertion + schema-harness style)
  </read_first>
  <action>Create `tests/test_effective_config_golden.py` matching the neighboring config-test harness style (`test_phase4_schema_validation.py` uses `_check`/`main() -> int` + `sys.exit(main())`; pytest `test_*` also acceptable). Define a `_normalize(groups) -> str` helper: take the resolved `list[dict]`, wrap as `{"groups": groups}`, and serialize with `json.dumps(obj, sort_keys=True, indent=2, ensure_ascii=False)` (normalization details are Claude's Discretion, D-07 — pick sorted-keys deterministic serialization and document it in a docstring). Resolve BOTH fixtures via `_load_config(config_path=...)` (legacy fixture path → single-file mode; twin `contacts.yaml` path with sibling `deliveries.d/` → directory mode). GENERATE the committed golden `tests/baselines/effective_config_golden.json` from the LEGACY resolution the first time (write `_normalize(legacy_groups)` to the file), then commit it — so the golden is machine-derived, not hand-authored (prevents drift). Assertions: (a) `_normalize(legacy_groups)` equals the committed golden string byte-for-byte; (b) `_normalize(twin_groups)` equals the SAME committed golden byte-for-byte (the two-way equality, D-07); (c) both resolved configs pass the unchanged schema — `_validate_with_schema({"groups": legacy_groups}, _load_schema()) == []` and same for the twin. Use `_BASELINES_DIR = Path(__file__).parent / "baselines"` convention. Add a brief comment on how to regenerate the golden if resolution intentionally changes.</action>
  <verify>
    <automated>python -m pytest tests/test_effective_config_golden.py -x -q 2>/dev/null || python tests/test_effective_config_golden.py</automated>
  </verify>
  <acceptance_criteria>
    - `tests/baselines/effective_config_golden.json` exists, is valid JSON, and contains a top-level `groups` array.
    - Test asserts legacy resolution == committed golden (byte-identical) — passes.
    - Test asserts twin resolution == the SAME committed golden (byte-identical) — passes (this is the two-way QUAL-06 gate).
    - Test asserts both resolved configs pass `_validate_with_schema(...) == []` against the unchanged schema.
    - `python -m pytest tests/test_effective_config_golden.py -q` exits 0 (or the `main()` harness exits 0).
    - The golden JSON contains only `example.invalid` addresses (`grep -oE "@[a-zA-Z0-9.-]+" tests/baselines/effective_config_golden.json | sort -u` shows only `@example.invalid`).
  </acceptance_criteria>
  <done>Committed machine-derived golden + two-way equality gate proves the migrated twin and the legacy single-file resolve to an identical effective config, and both pass the unchanged schema. QUAL-06 satisfied — the cutover is reversible in review.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture YAML → resolver under test | test resolves synthetic fixtures through the real loader |
| golden JSON → committed repo artifact | the golden is a committed, reviewable diff artifact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-13 | Information disclosure | golden JSON / fixtures leaking real recipient config | mitigate | HIGH-priority: all fixtures + golden use synthetic `example.invalid` identifiers only (D-08 / Hard Rule 2 / QUAL-05). Acceptance criteria greps the golden for non-example.invalid domains. Two prior git-history scrubs make this a hard bar. |
| T-20-14 | Tampering | hand-authored golden drifting from resolver output | mitigate | Golden is machine-generated from the legacy resolution (not hand-typed); regeneration procedure documented. Two-way equality catches silent normalization drift (D-07). |
| T-20-15 | Tampering | YAML parse of fixtures | mitigate | Resolution runs through the Plan-01 loader (yaml.safe_load only); test adds no bare yaml.load. |
| T-20-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependencies (Hard Rule 8); stdlib json + existing PyYAML/jsonschema only. No install task. |
</threat_model>

<verification>
- `python -m pytest tests/test_effective_config_golden.py -q` passes.
- Golden JSON is valid and contains only `example.invalid` addresses.
- Both legacy and twin resolve to the identical committed golden and both pass the unchanged schema.
</verification>

<success_criteria>
- Legacy single-file resolves byte-identical to the committed golden (QUAL-06).
- Migrated twin resolves to the SAME golden — two-way equality (D-07).
- Both resolved configs pass the unchanged `delivery_config.schema.yaml`.
- Synthetic `example.invalid` identifiers only (D-08).
</success_criteria>

<output>
Create `.planning/phases/20-config-language-loader-matrix/20-04-SUMMARY.md` when done
</output>
