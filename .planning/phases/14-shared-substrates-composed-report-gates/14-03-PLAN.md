---
phase: 14-shared-substrates-composed-report-gates
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - reports/composed_report.py
  - reports/modules/sc4_kwargs_stub_module.py
  - tests/test_composed_report_kwargs_gates.py
autonomous: false
requirements: [SUB-03]
must_haves:
  truths:
    - "SUB-03 / SC#3: composed_report.py gains _MODULES_NEEDING_TREND_SNAPSHOTS and _MODULES_NEEDING_RECAST_RULES frozensets + conditional fetch blocks following the existing _MODULES_NEEDING_FIXED_VULNS pattern; run_report() signature is UNCHANGED (D-15)"
    - "SC#3: existing composed-report groups pass --dry-run with no regression (D-15)"
    - "SC#4 / D-16: the trend gate forwards the FULL read_trend() dict {snapshots, insufficient_data} under one kwarg trend_snapshots; the recast gate forwards recast_rules_df (a DataFrame, matching the fixed_vulns_df naming)"
    - "SC#4: read_trend() result and recast_rules_df arrive at module compute() via **self._kwargs fan-out — verified by the sc4_kwargs_stub module asserting both kwargs are present when listed in the frozensets"
    - "D-17: Phase 14 seeds BOTH frozensets with the SC#4 stub module ID (sc4_kwargs_stub) ONLY; real v1.4 modules add themselves in the phase that builds them"
    - "D-17: the stub module needs NO run_all.py / _VALID_REPORTS / _REPORT_MODULE_MAP / schema registration — registry.discover() auto-discovers it via the *_module.py glob + @register_module"
  artifacts:
    - path: "reports/composed_report.py"
      provides: "two new frozensets + two conditional fetch blocks + two composer_kwargs lines"
      contains: "_MODULES_NEEDING_TREND_SNAPSHOTS"
    - path: "reports/modules/sc4_kwargs_stub_module.py"
      provides: "auto-discovered SC#4 stub asserting trend_snapshots + recast_rules_df arrive at compute()"
      contains: "sc4_kwargs_stub"
    - path: "tests/test_composed_report_kwargs_gates.py"
      provides: "tests proving the gates seed/forward the kwargs and run_report signature is unchanged"
      contains: "def test_"
  key_links:
    - from: "reports/composed_report.py composer_kwargs"
      to: "ReportComposer **self._kwargs -> compute()"
      via: "conditional fetch -> composer_kwargs -> fan-out"
      pattern: "trend_snapshots|recast_rules_df"
    - from: "reports/composed_report.py trend gate"
      to: "data.trend_store.read_trend()"
      via: "lazy import + read_trend(dimension, tag_filter, months) returning the full dict"
      pattern: "read_trend"
    - from: "reports/modules/sc4_kwargs_stub_module.py"
      to: "kwargs.get('trend_snapshots') / kwargs.get('recast_rules_df')"
      via: "compute() kwarg assertion"
      pattern: "kwargs.get"
---

<objective>
Add the two kwargs-forwarding gates to `reports/composed_report.py`
(`_MODULES_NEEDING_TREND_SNAPSHOTS`, `_MODULES_NEEDING_RECAST_RULES`) following
the existing `_MODULES_NEEDING_FIXED_VULNS` pattern exactly, and create the SC#4
stub module that proves both kwargs reach `compute()` via the `**self._kwargs`
fan-out. Delivers ROADMAP Success Criteria #3 and #4 and SUB-03.

Purpose: every Phase 15-17 module that needs trend history or recast rules
self-describes by adding its MODULE_ID to a frozenset — `composer.py` and
`base.py` stay untouched, `run_report()`'s signature stays unchanged (D-15).
Phase 14 seeds the frozensets with the stub ID only (D-17).

Output: `reports/composed_report.py` (modified), `reports/modules/sc4_kwargs_stub_module.py`, `tests/test_composed_report_kwargs_gates.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-shared-substrates-composed-report-gates/14-CONTEXT.md
@.planning/phases/14-shared-substrates-composed-report-gates/14-PATTERNS.md

<interfaces>
<!-- Source-of-truth analog is composed_report.py ITSELF — copy-paste-shaped extension. -->
<!-- Existing frozensets: composed_report.py lines 73 (_MODULES_NEEDING_FIXED_VULNS) -->
<!--   and 79 (_MODULES_NEEDING_ENV_TOTAL). Add two new frozensets right after line 79. -->
<!-- Existing conditional fetch: lines 186-193 (need_fixed / fixed_vulns_df). -->
<!--   Add the two new need_trend / need_recast blocks after the fixed_vulns_df block (~line 193). -->
<!-- _log_scope is already computed at line 169-171 ("Category=Value" or "all assets"). -->
<!-- Existing composer_kwargs build: lines 289-293. Append two `if X is not None: composer_kwargs[...]` lines. -->
<!-- ReportComposer(**composer_kwargs) at lines 295-302; composer stores self._kwargs and fans out -->
<!--   to compute(..., **self._kwargs) — UNTOUCHED. -->

<!-- read_trend signature (data/trend_store.py:338): -->
<!--   read_trend(dimension: str, tag_filter: str = "all_assets", months: int = 6, trend_dir=None) -> dict -->
<!--   returns {"snapshots": list[dict], "insufficient_data": bool}; cold-start safe, never raises. -->
<!--   File lookup: trend_{dimension}_{tag_filter}.json AND filters on stored tag_filter field; -->
<!--   emits a WR-05 warning if filename suffix and stored field diverge. -->
<!-- _sanitise_tag_for_filename(tag_category, tag_value) (data/trend_store.py:73) is the capture-time -->
<!--   convention. Phase 12 used tag_filter="all_assets" (bypasses sanitise); Phase 13 parameterised it. -->
<!-- fetch_recast_rules(tio, cache_dir) (data/fetchers.py) -> recast_rules.parquet DataFrame; already exists. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the two kwargs gates to composed_report.py</name>
  <files>reports/composed_report.py</files>
  <read_first>
    - reports/composed_report.py (lines 60-80 frozensets; lines 160-200 fetch section; lines 286-303 composer_kwargs + ReportComposer call) — the exact _MODULES_NEEDING_FIXED_VULNS pattern to mirror at all three sites
    - data/trend_store.py (lines 73-90 _sanitise_tag_for_filename; lines 338-393 read_trend + the WR-05 filename/field-divergence warning) — to wire the trend fetch with the correct tag_filter convention
    - data/fetchers.py — fetch_recast_rules(tio, cache_dir) signature and that it already returns the recast_rules.parquet DataFrame (no new fetcher)
    - .planning/phases/14-shared-substrates-composed-report-gates/14-PATTERNS.md (§ composed_report.py) — the verbatim new frozenset text, the two need_trend/need_recast fetch blocks, the composer_kwargs append lines, AND the tag-filter sanitisation gotcha note
  </read_first>
  <action>
    Add two frozensets to `reports/composed_report.py` immediately after the existing `_MODULES_NEEDING_ENV_TOTAL` (line 79), seeded with the stub ID ONLY (D-17): `_MODULES_NEEDING_TREND_SNAPSHOTS = frozenset({"sc4_kwargs_stub"})` and `_MODULES_NEEDING_RECAST_RULES = frozenset({"sc4_kwargs_stub"})`, each with a comment matching the existing-frozenset comment style documenting the D-16 payload (trend = full read_trend() dict under kwarg `trend_snapshots`; recast = `recast_rules_df` DataFrame) and the D-17 seeding note (real modules add themselves in their build phase).
    Add two conditional fetch blocks after the existing `fixed_vulns_df` block (~line 193), following the `need_fixed` / `if need_fixed:` idiom exactly. `need_trend = bool(_MODULES_NEEDING_TREND_SNAPSHOTS.intersection(modules))`; when true, lazily `from data.trend_store import read_trend` and call it with `dimension="severity"`, `months=13`, and the tag_filter that MATCHES the capture-time convention. CRITICAL (14-PATTERNS.md gotcha + trend_store WR-05): `read_trend` filters on the stored `tag_filter` field and looks up `trend_{dimension}_{tag_filter}.json`; the bare `_log_scope` string ("Category=Value" / "all assets") is NOT the sanitised capture-time value. Read `_sanitise_tag_for_filename(tag_category, tag_value)` and pass the SAME value the capture path uses (use `_sanitise_tag_for_filename` for the tag-scoped case; pass `"all_assets"` for the no-filter case — note capture uses `all_assets` with an underscore, not the `_log_scope` "all assets" with a space). Store the full returned dict in `trend_snapshots` (D-16 — forward the whole `{snapshots, insufficient_data}` dict, NOT just the snapshots list). `need_recast = bool(_MODULES_NEEDING_RECAST_RULES.intersection(modules))`; when true, lazily `from data.fetchers import fetch_recast_rules` and call `fetch_recast_rules(tio, cache_dir)` into `recast_rules_df`.
    Append to the `composer_kwargs` build block (after line 293, before the `ReportComposer(` call): `if trend_snapshots is not None: composer_kwargs["trend_snapshots"] = trend_snapshots` and `if recast_rules_df is not None: composer_kwargs["recast_rules_df"] = recast_rules_df`. Do NOT change the `run_report()` signature (D-15), do NOT touch `ReportComposer`/`composer.py`/`base.py`, do NOT add the stub to `_VALID_REPORTS`/`_REPORT_MODULE_MAP`/schema (D-17 auto-discovery). Initialise `trend_snapshots`/`recast_rules_df` to `None` (typed `Optional`) alongside the existing `fixed_vulns_df: Optional[pd.DataFrame] = None` pattern so the composer_kwargs guards work.
  </action>
  <acceptance_criteria>
    - `python -c "import reports.composed_report as c; assert c._MODULES_NEEDING_TREND_SNAPSHOTS == frozenset({'sc4_kwargs_stub'}); assert c._MODULES_NEEDING_RECAST_RULES == frozenset({'sc4_kwargs_stub'}); print('OK')"` exits 0
    - The `run_report` signature is unchanged (no new parameters) — verify via `python -c "import inspect, reports.composed_report as c; print(inspect.signature(c.run_report))"` matches the pre-change signature
    - The trend fetch forwards the full read_trend() dict (not just snapshots) into composer_kwargs["trend_snapshots"]
    - The tag_filter passed to read_trend matches the capture-time _sanitise_tag_for_filename convention ("all_assets" for no-filter), not the raw "all assets" _log_scope
    - composer.py and base.py are byte-unchanged (`git diff --stat` shows only composed_report.py among existing files)
  </acceptance_criteria>
  <verify>
    <automated>python -c "import reports.composed_report as c; assert c._MODULES_NEEDING_TREND_SNAPSHOTS == frozenset({'sc4_kwargs_stub'}); assert c._MODULES_NEEDING_RECAST_RULES == frozenset({'sc4_kwargs_stub'}); import inspect; assert 'trend_snapshots' not in inspect.signature(c.run_report).parameters; print('OK')"</automated>
  </verify>
  <done>composed_report.py has both frozensets (stub-seeded), both conditional fetch blocks with correct tag_filter convention, both composer_kwargs appends; run_report signature unchanged; composer/base untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Create the SC#4 stub module</name>
  <files>reports/modules/sc4_kwargs_stub_module.py</files>
  <read_first>
    - reports/modules/sc4_kwargs_stub_module.py (file being created — does not yet exist)
    - reports/modules/example_module.py — the minimal @register_module class skeleton, compute() signature (vulns_df, assets_df, report_date, config, **kwargs), and the try/except -> self._empty_result fail-soft fallback this stub copies
    - reports/modules/base.py — BaseModule, ModuleConfig, ModuleData signatures and _empty_result(msg, config) so the stub's return shape is correct
    - .planning/phases/14-shared-substrates-composed-report-gates/14-PATTERNS.md (§ sc4_kwargs_stub_module.py) — the full skeleton, the kwarg-assertion logic, the auto-discovery note (no run_all.py registration)
  </read_first>
  <action>
    Create `reports/modules/sc4_kwargs_stub_module.py` (filename matches the `*_module.py` auto-discovery glob) per the 14-PATTERNS.md skeleton. Decorate with `@register_module`; set `MODULE_ID = "sc4_kwargs_stub"` (the exact ID seeded in both frozensets in Task 1), plus DISPLAY_NAME/DESCRIPTION/VERSION marking it a Phase-14 acceptance-test stub, NOT for production delivery.
    Implement `compute(self, vulns_df, assets_df, report_date, config, **kwargs)`: read `kwargs.get("trend_snapshots")` and `kwargs.get("recast_rules_df")`; if either is None, log an error and return `self._empty_result("<which kwargs missing>", config)` (fail-soft — never raise, per the batch contract). When both present, return a `ModuleData` whose metrics record `trend_snapshots_present=True`, `recast_rules_df_present=True`, `trend_snapshot_count=len(trend_snapshots.get("snapshots", []))`, and `recast_rules_row_count=len(recast_rules_df)` (proving the full read_trend dict and the DataFrame arrived, D-16). Wrap the body in the example_module try/except -> `_empty_result` fail-soft pattern.
    Add NOTHING to `run_all.py`, `_VALID_REPORTS`, `_REPORT_MODULE_MAP`, `delivery_config.schema.yaml`, or CLAUDE.md (D-17 — auto-discovery via registry.discover() on `reports.modules` import is sufficient). Match the exact ModuleData field names by reading base.py — do not invent fields.
  </action>
  <acceptance_criteria>
    - `python -c "import reports.modules; from reports.modules.registry import registry" ` (or the project's discovery entrypoint) shows `sc4_kwargs_stub` registered after import
    - The stub's compute() returns a non-error ModuleData when both kwargs are passed, and an _empty_result when either is missing (verified by Task 3)
    - `grep -rn "sc4_kwargs_stub" run_all.py` returns NOTHING (D-17 — not registered as a top-level slug)
    - ModuleData field names used by the stub exist in base.py (no invented fields)
  </acceptance_criteria>
  <verify>
    <automated>python -c "import reports.modules; from reports.modules import registry; r = registry.registry if hasattr(registry,'registry') else registry; print('sc4_kwargs_stub discovered'); import reports.modules.sc4_kwargs_stub_module as m; assert m.Sc4KwargsStubModule.MODULE_ID == 'sc4_kwargs_stub'; print('OK')"</automated>
  </verify>
  <done>sc4_kwargs_stub_module.py exists, auto-discovered, asserts both kwargs in compute(), fail-soft via _empty_result, zero run_all.py/schema registration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Gate-forwarding + no-regression tests (SC#3, SC#4)</name>
  <files>tests/test_composed_report_kwargs_gates.py</files>
  <read_first>
    - tests/test_composed_report_kwargs_gates.py (file being created — does not yet exist)
    - reports/composed_report.py (the modified gates from Task 1) and reports/modules/sc4_kwargs_stub_module.py (Task 2)
    - tests/ — read an existing composed_report / module test (e.g. the test files added with the composed_report slug or tag_severity_share) to match the project's fixture + monkeypatch conventions for driving the composer without a live tio
    - .planning/ROADMAP.md (Phase 14 SC#3 and SC#4 exact wording) to map assertions to criteria
  </read_first>
  <behavior>
    - test_frozensets_seeded_with_stub_only: both frozensets == frozenset({"sc4_kwargs_stub"}) (D-17)
    - test_run_report_signature_unchanged: inspect.signature(run_report) has no trend_snapshots/recast_rules_df parameters (D-15)
    - test_stub_compute_receives_both_kwargs: call the stub's compute() directly with a fake trend dict {"snapshots":[...],"insufficient_data":False} and a small recast DataFrame -> ModuleData.error is None and metrics record both present (SC#4 unit-level)
    - test_stub_compute_missing_kwarg_failsoft: call compute() with one kwarg missing -> returns _empty_result (error set), does not raise
    - test_gate_intersection_logic: _MODULES_NEEDING_TREND_SNAPSHOTS.intersection(["sc4_kwargs_stub"]) is truthy; intersection(["some_other_module"]) is falsy (proves a non-listed module triggers no fetch — the no-regression guarantee for existing groups)
  </behavior>
  <action>
    Create `tests/test_composed_report_kwargs_gates.py`. Cover the behavior block. The SC#4 forwarding proof is best done at the unit level (call `Sc4KwargsStubModule().compute(...)` directly with a synthetic trend dict and a synthetic recast DataFrame) PLUS the gate-membership assertions on the frozensets and the `.intersection()` logic — this proves both the seeding (D-17) and that a module NOT in a frozenset triggers no fetch (the SC#3 no-regression mechanism) without needing a live `tio`. Use synthetic-only data (QUAL-05): a fake `{"snapshots":[{"month":"2026-05",...}], "insufficient_data": False}` dict and a tiny DataFrame with synthetic recast columns.
    Assert `run_report`'s signature is unchanged (inspect-based, D-15). If the project's existing composed_report tests already drive `--dry-run` end-to-end, mirror that style; otherwise the SC#3 no-regression bar is satisfied by the dedicated `--dry-run` CLI check in Task 4's verification (do not duplicate a heavy end-to-end harness here).
  </action>
  <acceptance_criteria>
    - `pytest tests/test_composed_report_kwargs_gates.py -x` exits 0
    - Tests assert: both frozensets seeded with stub-only; run_report signature unchanged; stub.compute() with both kwargs -> error is None + both-present metrics; stub.compute() missing a kwarg -> _empty_result no-raise; intersection logic gates a non-listed module to no fetch
    - Test data is synthetic-only (no real recast UUIDs / hostnames / IPs)
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_composed_report_kwargs_gates.py -x -q</automated>
  </verify>
  <done>tests prove the gates are stub-seeded (D-17), run_report signature is unchanged (D-15), both kwargs reach compute() (SC#4), and a non-listed module triggers no fetch (SC#3 no-regression).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Existing-composed-group --dry-run no-regression check (SC#3)</name>
  <action>Run `python run_all.py --dry-run` and a targeted existing-composed-group dry-run, capture the output, and confirm composer.py/base.py are byte-unchanged before presenting for human verification.</action>
  <what-built>
    The two kwargs gates were added to composed_report.py seeded with the SC#4 stub ID only (D-17). Existing composed-report groups must still validate and run --dry-run with no regression (SC#3). The executor has run `python run_all.py --dry-run` (all groups) and a targeted `python run_all.py --group "<existing composed group>" --dry-run`, capturing output.
  </what-built>
  <how-to-verify>
    1. Confirm the executor ran `python run_all.py --dry-run` and it succeeded (exit 0, all groups validate) — this is the SC#3 no-regression bar for ALL existing groups including any using `reports: [composed_report]`.
    2. Confirm a targeted run against an existing composed group (if one exists in the operator's live `delivery_config.yaml`, which is gitignored) also passed `--dry-run`. If NO composed group exists in the live config, confirm the executor instead added a temporary throwaway composed group (or used the example config) to exercise the path, then reverted it.
    3. Confirm `composer.py` and `base.py` are byte-unchanged (`git diff --stat` shows only composed_report.py, the new stub module, and the new test among code files).
    4. Confirm NO live trend/recast fetch fired for groups that do NOT list the stub (the gate `.intersection()` short-circuits) — check the dry-run log shows no "reading trend snapshots" / "fetching recast rules" lines for existing groups.
  </how-to-verify>
  <resume-signal>Type "approved" if --dry-run passes with no regression for existing groups, or describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| modules list (from YAML) → frozenset .intersection() | Operator-supplied module IDs gate which fetches fire; a typo'd ID simply misses the gate (no fetch), never executes arbitrary code |
| read_trend() / fetch_recast_rules() → composer_kwargs | Pre-read trend dict + recast DataFrame cross into the **self._kwargs fan-out reaching every compute() |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-07 | Tampering | run_report() signature / contract drift | mitigate | Signature explicitly unchanged (D-15), asserted by Task 3 inspect test; composer/base untouched |
| T-14-08 | Information Disclosure | recast_rules_df may carry recast_reason free-text (hostnames/IPs) | mitigate | Phase 14 only FORWARDS the DataFrame to the stub (which counts rows); no logging of recast_reason at INFO; the stub never emits asset-level rows; Phase-15 consumers own the per-field PII boundary |
| T-14-09 | Tampering | npm/pip/cargo installs | accept | No package installs — uses existing read_trend / fetch_recast_rules; zero new dependencies |
| T-14-10 | Denial of Service | read_trend cold-start / missing trend file | mitigate | read_trend is cold-start-safe (returns insufficient_data, never raises); the stub fail-soft via _empty_result; batch fail-soft preserved |
</threat_model>

<verification>
- SC#3: `python run_all.py --dry-run` succeeds with no regression (Task 4 checkpoint); composer/base untouched.
- SC#4: `pytest tests/test_composed_report_kwargs_gates.py` proves both kwargs reach compute() via the stub.
- D-15: run_report signature unchanged (inspect test). D-16: full read_trend dict forwarded under trend_snapshots; recast_rules_df DataFrame forwarded. D-17: frozensets seeded with stub ID only; stub auto-discovered, no run_all.py registration.
</verification>

<success_criteria>
- composed_report.py carries both gates (stub-seeded), both fetch blocks (correct tag_filter convention), both composer_kwargs appends; run_report signature unchanged.
- sc4_kwargs_stub_module.py auto-discovered, asserts both kwargs, fail-soft.
- Tests green; existing composed groups pass --dry-run.
</success_criteria>

<output>
Create `.planning/phases/14-shared-substrates-composed-report-gates/14-03-SUMMARY.md` when done
</output>
