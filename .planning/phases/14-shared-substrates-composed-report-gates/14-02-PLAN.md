---
phase: 14-shared-substrates-composed-report-gates
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - config.py
  - utils/asset_count.py
  - tests/test_asset_count.py
autonomous: true
requirements: [SUB-02]
must_haves:
  truths:
    - "SUB-02 / SC#2: asset_count returns the current-run on-time-scanned licensed asset count from assets_df via a pure function, unit-tested including the zero-asset guard"
    - "D-01 (OD-2 locked): denominator basis = on-time-scanned licensed assets (last_licensed_scan_date within the on-time window — the scanned_on_time split), NOT all-licensed; supersedes ROADMAP SC#2 / SUB-02 'current-run licensed asset count' phrasing"
    - "D-12: the function is PURE — it accepts an injected report_date (the run's generated_at, UTC) and computes cutoff = report_date - window; NO datetime.now() inside; unit-testable with an injected date"
    - "D-13: the on-time window (30 days) is a single canonical constant ON_TIME_SCAN_WINDOW_DAYS in config.py; asset_count imports it from config (NO utils -> reports.modules backward import)"
    - "D-14: returns None sentinel (not 0) when no on-time-scanned licensed assets are in scope, distinguishing 'no assets' from 'no vulns'; consumers None-check before dividing"
    - "QUAL-05: tests use synthetic-only data (no real hostnames/IPs/asset fields)"
  artifacts:
    - path: "config.py"
      provides: "ON_TIME_SCAN_WINDOW_DAYS canonical on-time scan window constant (D-13)"
      contains: "ON_TIME_SCAN_WINDOW_DAYS"
    - path: "utils/asset_count.py"
      provides: "count_on_time_assets(assets_df, report_date, window_days=config.ON_TIME_SCAN_WINDOW_DAYS) -> int | None"
      contains: "def count_on_time_assets"
      min_lines: 40
    - path: "tests/test_asset_count.py"
      provides: "unit tests for on-time split, injected-date purity, zero-asset None sentinel, window-boundary, missing-column guard"
      contains: "def test_"
  key_links:
    - from: "utils/asset_count.py"
      to: "config.ON_TIME_SCAN_WINDOW_DAYS"
      via: "import — single canonical window source (D-13)"
      pattern: "ON_TIME_SCAN_WINDOW_DAYS"
    - from: "utils/asset_count.py"
      to: "assets_df last_licensed_scan_date column"
      via: "cutoff = report_date - window; count rows scanned on time"
      pattern: "report_date|cutoff"
---

<objective>
Add the canonical `ON_TIME_SCAN_WINDOW_DAYS` constant to `config.py` and create
`utils/asset_count.py` — a pure-compute helper returning the current-run
on-time-scanned licensed asset count (the Vulnerability Density denominator).
Delivers ROADMAP Success Criterion #2 and SUB-02.

Purpose: Phase 15's Vulnerability Density module calls this inline inside
`compute()` (no I/O) for its current-run denominator. The denominator basis is
on-time-scanned licensed assets (D-01, OD-2 locked) — consistent with the
board_summary managed-asset baseline.

Output: `config.py` (+1 constant), `utils/asset_count.py`, `tests/test_asset_count.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-shared-substrates-composed-report-gates/14-CONTEXT.md
@.planning/phases/14-shared-substrates-composed-report-gates/14-PATTERNS.md

<interfaces>
<!-- Source-of-truth analogs: -->
<!-- config.py SLA_DAYS block (lines 28-33): labeled comment-block + named constant convention -->
<!-- scan_coverage_sla_module.py (lines 262-291): the on-time split this util extracts — -->
<!--   rd_ts tz-normalize, cutoff = rd_ts - pd.Timedelta(days=ON_TIME_WINDOW_DAYS), -->
<!--   on_time_flag = licensed[_lsd] >= cutoff; total_licensed==0 -> None sentinel (lines 281-286) -->
<!-- utils/open_count.py (lines 1-40): pure-compute file header, injected-date param, zero-row guard -->
<!-- NOTE: board_report_utils.py line 59 has a SECOND ON_TIME_WINDOW_DAYS=30; this plan adds the -->
<!--   canonical config constant ONLY. Migrating scan_coverage/board_report_utils to import it is -->
<!--   DEFERRED (out of scope this phase) to avoid scope creep — see 14-PATTERNS.md migration note. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ON_TIME_SCAN_WINDOW_DAYS constant to config.py</name>
  <files>config.py</files>
  <read_first>
    - config.py (lines 18-60) — the SLA_DAYS labeled-comment-block convention (lines 23-33) and the SEVERITY_ORDER block that follows; insert the new constant immediately after SLA_DAYS, before SEVERITY_ORDER
    - .planning/phases/14-shared-substrates-composed-report-gates/14-PATTERNS.md (§ config.py) — exact constant block text, value 30, and the D-13 single-source rationale + the deferred-migration note
    - reports/modules/board_report_utils.py line 59 — the existing ON_TIME_WINDOW_DAYS = 30 (the second source of truth this constant will eventually consolidate; do NOT touch it this phase)
  </read_first>
  <action>
    Add a single new module-level constant `ON_TIME_SCAN_WINDOW_DAYS: int = 30` to `config.py`, placed immediately after the `SLA_DAYS` block and before `SEVERITY_ORDER`, with a labeled comment block in the same `# ===` style as `SLA_DAYS` (per D-13 and the 14-PATTERNS.md text). The comment documents: this is the canonical on-time scan window; consumed by `utils/asset_count.py` (and, in a future phase, `scan_coverage_sla_module.py`); single source of truth to prevent drift.
    Do NOT modify `board_report_utils.py` line 59 or `scan_coverage_sla_module.py` — migrating their imports to this constant is explicitly deferred to avoid scope creep (14-PATTERNS.md migration note). Surgical addition only — touch nothing else in config.py.
  </action>
  <acceptance_criteria>
    - `python -c "import config; assert config.ON_TIME_SCAN_WINDOW_DAYS == 30; print('OK')"` exits 0
    - The constant is placed between SLA_DAYS and SEVERITY_ORDER with a labeled comment block
    - `git diff config.py` shows ONLY the new constant block added (no other lines changed)
  </acceptance_criteria>
  <verify>
    <automated>python -c "import config; assert config.ON_TIME_SCAN_WINDOW_DAYS == 30; print('OK')"</automated>
  </verify>
  <done>config.py exposes ON_TIME_SCAN_WINDOW_DAYS = 30 as the single canonical on-time scan window; no other code changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement utils/asset_count.py + unit tests (SC#2)</name>
  <files>utils/asset_count.py, tests/test_asset_count.py</files>
  <read_first>
    - utils/asset_count.py + tests/test_asset_count.py (files being created — do not yet exist)
    - utils/open_count.py (lines 1-40) — the pure-compute file header, `from __future__ import annotations`, injected-datetime parameter pattern, zero-row guard, and stdlib+pandas-only import set this util mirrors exactly
    - reports/modules/scan_coverage_sla_module.py (lines 252-291) — the licensed-asset filter + on-time split (rd_ts tz-normalize, cutoff = rd_ts - pd.Timedelta(days=window), on_time_flag = licensed[_lsd] >= cutoff) and the total==0 -> None sentinel; identify the exact licensed-scan-date column name `_lsd` resolves to (last_licensed_scan_date)
    - config.py — ON_TIME_SCAN_WINDOW_DAYS (added in Task 1) to import as the default window
    - .planning/phases/14-shared-substrates-composed-report-gates/14-PATTERNS.md (§ asset_count.py + Critical Constraints table) — signature, None-sentinel (D-14), no-datetime.now() (D-12), config-import (D-13)
  </read_first>
  <behavior>
    - count_on_time_assets(df, report_date): counts licensed assets whose last_licensed_scan_date >= report_date - window_days
    - injected report_date controls the cutoff entirely — same df + different report_date yields different counts (proves D-12 purity, no datetime.now())
    - window boundary: an asset scanned exactly window_days ago (== cutoff) is counted (>= cutoff, inclusive)
    - an asset scanned window_days+1 ago is NOT counted
    - all licensed assets stale (none within window) -> returns None (D-14 sentinel, not 0)
    - empty assets_df -> returns None
    - assets_df missing the licensed-scan-date column -> returns None (fail-soft), logs a warning
    - default window comes from config.ON_TIME_SCAN_WINDOW_DAYS when window_days not passed (D-13)
  </behavior>
  <action>
    Create `utils/asset_count.py` with `from __future__ import annotations`, stdlib `logging` + `datetime`, `pandas`, and `from config import ON_TIME_SCAN_WINDOW_DAYS`. NO import from `reports.modules.*` (D-13 — utils must not depend backwards on the module layer; that is why the window lives in config, not board_report_utils).
    Implement `count_on_time_assets(assets_df: pd.DataFrame, report_date: datetime, window_days: int = ON_TIME_SCAN_WINDOW_DAYS) -> int | None`. NO `datetime.now()` inside (D-12) — the cutoff derives solely from the injected `report_date`. Mirror the scan_coverage_sla on-time split (14-PATTERNS.md): tz-normalize `report_date` to a UTC pd.Timestamp (handle tz-aware and tz-naive per the analog), `cutoff = rd_ts - pd.Timedelta(days=window_days)`, filter `assets_df` to licensed rows (last_licensed_scan_date not-null), count rows where `last_licensed_scan_date >= cutoff`.
    Return `None` (NOT 0) when the count is zero / no licensed-scan-date column / empty input (D-14 sentinel) so the Phase-15 density module can None-check before dividing and render cold-start / No-Data via the empty-data guard (QUAL-03). Add the pure-compute header docstring (no file I/O, no network) and the docstring note that callers MUST None-check the return before division.
    CoW: if any intermediate column is added, use `.assign()` (no post-filter `df["col"]=`). Add an `if __name__ == "__main__":` argparse stub per Code Quality requirements (library-only; help/no-op acceptable).
    Then create `tests/test_asset_count.py` covering every behavior-block case with SYNTHETIC-only data (QUAL-05): build small assets_df fixtures with a `last_licensed_scan_date` column of UTC timestamps relative to a fixed injected `report_date` (e.g. report_date = datetime(2026,6,1,tzinfo=timezone.utc)). Assert: count of on-time assets, the window-boundary inclusive/exclusive cases, the None sentinel for all-stale / empty / missing-column, that two different injected report_dates on the same df yield different counts (D-12 purity), and that the default window resolves to config.ON_TIME_SCAN_WINDOW_DAYS (D-13).
  </action>
  <acceptance_criteria>
    - `python -c "from utils.asset_count import count_on_time_assets"` exits 0
    - `pytest tests/test_asset_count.py -x` exits 0
    - Tests assert: on-time count correct; cutoff boundary (>= inclusive); all-stale -> None; empty -> None; missing column -> None; two injected report_dates yield different counts (purity); default window == config.ON_TIME_SCAN_WINDOW_DAYS
    - `grep -n "datetime.now" utils/asset_count.py` returns NOTHING (D-12 — no wall-clock inside the pure fn)
    - `grep -n "reports.modules" utils/asset_count.py` returns NOTHING (D-13 — no backward import)
    - Test fixtures contain no real hostnames/IPs (PII clean)
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_asset_count.py -x -q</automated>
  </verify>
  <done>utils/asset_count.py exists: pure count_on_time_assets with injected report_date (no datetime.now), config-sourced window, None sentinel on zero/empty/missing-column; tests green and synthetic-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tenable assets export → count_on_time_assets() | Untrusted `last_licensed_scan_date` values from the live tenant cross into pandas timestamp comparison |
| asset_count return → density module division | The None sentinel crosses into a consumer that must guard before dividing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04 | Denial of Service | zero/empty/missing-column input to count_on_time_assets | mitigate | None sentinel + empty/missing-column guards return None without raising (D-14, Task 2); fail-soft preserved |
| T-14-05 | Tampering | npm/pip/cargo installs | accept | No package installs — stdlib datetime + already-pinned pandas only; zero new dependencies |
| T-14-06 | Information Disclosure | test fixtures / committed artifacts | mitigate | asset_count is aggregate-count-only (returns an int|None, no asset-level rows); fixtures synthetic-only (QUAL-05) |
</threat_model>

<verification>
- SC#2: `pytest tests/test_asset_count.py -x` passes; pure function returns the on-time-scanned licensed count with injected report_date; zero-asset guard returns None.
- D-01: denominator basis is on-time-scanned licensed (the scanned_on_time split), not all-licensed.
- D-12 / D-13: no datetime.now() inside; window sourced from config.ON_TIME_SCAN_WINDOW_DAYS; no utils->reports.modules import.
</verification>

<success_criteria>
- `config.ON_TIME_SCAN_WINDOW_DAYS == 30` is the single canonical window.
- `count_on_time_assets` is pure, injected-date, returns int | None (None on empty per D-14).
- Tests green, synthetic-only, assert purity + boundary + sentinel + config-default.
</success_criteria>

<output>
Create `.planning/phases/14-shared-substrates-composed-report-gates/14-02-SUMMARY.md` when done
</output>
