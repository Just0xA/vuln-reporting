---
phase: 14-shared-substrates-composed-report-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - utils/external_scope.py
  - tests/test_external_scope.py
autonomous: true
requirements: [SUB-01]
must_haves:
  truths:
    - "SUB-01 / SC#1: external_scope classifies an asset as external when it carries Location=External OR Location=DMZ tag (D-04, tags authoritative), and flags public-IPv4-but-untagged assets as a gap (D-05)"
    - "SC#1 / D-08: the helper returns a (scoped_df, mismatches_df) tuple mirroring extract_owner's tuple shape; gap assets appear in BOTH frames"
    - "D-10: tag parse mirrors extract_owner._parse_tags — case-insensitive Location category, exact External/DMZ value match"
    - "D-06: DMZ-tagged + private IP is the NORMAL state (not a mismatch); only External-tag-on-private-IP is the defensive anomaly; empty/unpopulated DMZ tag is a valid zero state (not an error)"
    - "D-05: is_public_ipv4 NEVER overrides a tag — its only output is the public_ip_untagged gap reason"
    - "D-18 / SC#1: positive external classification asserted via monkeypatched is_global (RFC 5737 doc ranges return is_global=False and cannot be a positive fixture); CGNAT 100.64.x.x, loopback, IPv6 link-local tested as negative/edge with synthetic addresses"
    - "QUAL-05 / D-11: no real hostnames/IPs in committed test fixtures; mismatches_df asset-level fields are runtime-only, never committed"
  artifacts:
    - path: "utils/external_scope.py"
      provides: "external_scope(assets_df) -> (scoped_df, mismatches_df) + is_public_ipv4(ip_str) -> bool"
      contains: "def external_scope"
      min_lines: 60
    - path: "tests/test_external_scope.py"
      provides: "unit tests for tag classification, public-IP gap detector, CGNAT/loopback/link-local negatives, empty-input guard, monkeypatched positive external case"
      contains: "def test_"
  key_links:
    - from: "utils/external_scope.py"
      to: "ipaddress.ip_address().is_global"
      via: "is_public_ipv4 stdlib classifier"
      pattern: "is_global"
    - from: "utils/external_scope.py"
      to: "assets_df 'tags' + 'ipv4' columns"
      via: "_parse_tags-style token split + per-row IP check"
      pattern: "tags|ipv4"
---

<objective>
Create `utils/external_scope.py` — a pure-compute, stdlib-only classifier that
identifies external-scope assets and emits an analyst mismatch list. Delivers
ROADMAP Success Criterion #1 and SUB-01.

Purpose: stands up the external-exposure scope substrate that Phase 15's
External/DMZ module consumes inline inside `compute()` (no I/O). Tags are
authoritative (D-04); the IP signal is demoted to a gap detector only (D-05).

Output: `utils/external_scope.py` + `tests/test_external_scope.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-shared-substrates-composed-report-gates/14-CONTEXT.md
@.planning/phases/14-shared-substrates-composed-report-gates/14-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs. Source-of-truth analog: board_report_utils.py -->
<!-- _parse_tags (lines 263-291): case-insensitive category match, exact value retain -->
<!-- identify_on_time_assets (lines 135-207): tuple[pd.DataFrame, pd.DataFrame] return, -->
<!--   both sub-frames .copy().reset_index(drop=True), empty-input guard at top -->
<!-- Adaptation: replace OWNER_TAG_CATEGORY with "location" (casefold category match), -->
<!--   value membership check `val_s in {"External", "DMZ"}` (EXACT, case-sensitive per D-10). -->
<!-- is_public_ipv4: ipaddress.ip_address(ip_str), return addr.version==4 and addr.is_global; -->
<!--   except ValueError: return False (never raises on malformed input). -->
<!-- assets_df columns (data/fetchers.py:519,549): primary 'ipv4' (ipv4s collapsed to ipv4[0]), -->
<!--   'tags' is a "Category=Value;Category=Value" semicolon-delimited string. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement utils/external_scope.py classifier</name>
  <files>utils/external_scope.py</files>
  <read_first>
    - utils/external_scope.py (file being created — does not yet exist)
    - reports/modules/board_report_utils.py — `_parse_tags` inner fn (lines 263-291) for the case-insensitive category / exact value token parse; `extract_owner` absent-column fail-soft guard (lines 293-306); `identify_on_time_assets` (lines 135-207) for the tuple-return + empty-input-guard shape (D-08/D-10)
    - utils/open_count.py (lines 1-40) — the pure-compute file-header + `from __future__ import annotations` + zero-row guard idiom this util mirrors
    - data/fetchers.py lines 519, 549 — the `ipv4` primary column collapse and `tags` "Category=Value;…" format (D-09/D-10)
    - .planning/phases/14-shared-substrates-composed-report-gates/14-PATTERNS.md (§ external_scope.py) — exact adaptation notes + is_public_ipv4 skeleton + CoW .assign() mandate
  </read_first>
  <behavior>
    - is_public_ipv4("8.8.8.8") -> True; is_public_ipv4("10.0.0.5") / "192.168.1.1" / "172.16.0.1" -> False (RFC1918)
    - is_public_ipv4("100.64.0.1") -> False (CGNAT); "127.0.0.1" -> False (loopback); "169.254.0.1" -> False (link-local)
    - is_public_ipv4("fe80::1") -> False (IPv6 link-local, version!=4); is_public_ipv4("not-an-ip") / "" / None -> False (never raises)
    - external_scope: asset with Location=External tag -> in scoped_df, NOT in mismatches_df
    - external_scope: asset with Location=DMZ tag + private IP -> in scoped_df, NOT in mismatches_df (D-06: normal state)
    - external_scope: asset with public IPv4 but NO Location tag -> in BOTH scoped_df AND mismatches_df with untagged_reason="public_ip_untagged" (D-05/D-08)
    - external_scope: asset with private IP + no Location tag -> in NEITHER frame
    - external_scope: empty assets_df -> returns two zero-row frames, never raises (QUAL-03)
    - external_scope: assets_df missing 'tags' or 'ipv4' column -> logs warning, returns two zero-row frames (fail-soft)
  </behavior>
  <action>
    Create `utils/external_scope.py` with `from __future__ import annotations`, stdlib `ipaddress`, `logging`, and `pandas` only — NO new dependency, NO import from `reports.modules.*` (keep `utils` dependency-free of the module layer, D-13 spirit).
    Implement `is_public_ipv4(ip_str) -> bool` per the 14-PATTERNS.md skeleton: `ipaddress.ip_address(ip_str)`, return `addr.version == 4 and addr.is_global`, wrap in `try/except (ValueError, TypeError)` returning `False` — this is the THREAT-14-01 defensive parse (malformed IP strings must never raise into the batch). Use `is_global` NOT a hand-rolled RFC1918 check (D-05/D-07; covers CGNAT, loopback, link-local, doc ranges).
    Implement `external_scope(assets_df: pd.DataFrame) -> tuple[pd.DataFrame, pd.DataFrame]` returning `(scoped_df, mismatches_df)` mirroring `identify_on_time_assets`'s tuple shape (D-08). Empty/absent-column guard at top: if `assets_df.empty` or `"tags"`/`"ipv4"` not in columns, log a warning and return `assets_df.iloc[0:0].copy(), assets_df.iloc[0:0].copy()`.
    Tag classification: adapt `_parse_tags` — split each `tags` value on `";"`, partition each token on `"="`, casefold-compare category to `"location"` (case-insensitive per D-10), and exact-match value against the set `{"External", "DMZ"}` (case-sensitive). Build per-row boolean masks `has_external_tag`, `has_dmz_tag`, `has_location_external_dmz` via `.apply()` over the `tags` column.
    Public-IP gap detection: build `has_public_ip` mask via `assets_df["ipv4"].apply(is_public_ipv4)`. The gap reason `public_ip_untagged` (D-05) applies to rows where `has_public_ip AND NOT has_location_external_dmz`. is_public_ipv4 NEVER promotes a tagged-internal asset and NEVER overrides a tag.
    scoped_df = rows where `has_location_external_dmz OR has_public_ip` (gap rows included per D-08). mismatches_df = the gap subset (public IP, no Location tag) carrying asset-level columns `asset_uuid`, `ip_address` (from `ipv4`), `owner_tag`, `untagged_reason="public_ip_untagged"` (D-11 schema). Keep the defensive-only External-tag-on-private-IP anomaly reason available but do NOT emit `tagged-but-private` as a routine flag (D-06).
    CoW (Pitfall 9 / 260611-b1x): all column additions use `.assign()` — NEVER `df["col"]=` or `df.loc[:, col]=` after a filter. Add a module docstring stating the D-11 PII boundary (mismatches_df is operator-local / internal-email only; never committed, never sent to AI).
    Add an `if __name__ == "__main__":` argparse stub per the project Code Quality requirement (it may simply print the function signatures / a no-op help message; this util is library-only).
  </action>
  <acceptance_criteria>
    - `python -c "from utils.external_scope import external_scope, is_public_ipv4"` exits 0
    - `is_public_ipv4` returns False for "10.0.0.5", "192.168.1.1", "172.16.0.1", "100.64.0.1", "127.0.0.1", "169.254.0.1", "fe80::1", "not-an-ip", "" (verified by Task 2 tests)
    - `external_scope` returns a 2-tuple of DataFrames; gap (public-IP-untagged) assets appear in BOTH frames (verified by Task 2 tests)
    - `external_scope(pd.DataFrame())` returns two zero-row DataFrames without raising
    - No `df["col"]=` or `.loc[:, col]=` after a filter anywhere in the file (grep -nv '^#' utils/external_scope.py | grep -E '\]\s*=' shows only .assign / dict / mask construction, no post-filter column set)
    - File has a module docstring documenting the D-11 PII boundary
  </acceptance_criteria>
  <verify>
    <automated>python -c "import pandas as pd; from utils.external_scope import external_scope, is_public_ipv4; assert is_public_ipv4('8.8.8.8') is True; assert is_public_ipv4('100.64.0.1') is False; assert is_public_ipv4('fe80::1') is False; assert is_public_ipv4('bad') is False; a,b=external_scope(pd.DataFrame()); assert len(a)==0 and len(b)==0; print('OK')"</automated>
  </verify>
  <done>external_scope.py exists with external_scope + is_public_ipv4; tag-authoritative classification, public_ip_untagged gap detection, tuple return with gap assets in both frames, empty/absent-column fail-soft, CoW-compliant, PII-boundary docstring.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit tests for external_scope (SC#1)</name>
  <files>tests/test_external_scope.py</files>
  <read_first>
    - tests/test_external_scope.py (file being created — does not yet exist)
    - utils/external_scope.py (the module under test — created in Task 1)
    - .planning/phases/14-shared-substrates-composed-report-gates/14-CONTEXT.md (D-18 test strategy: monkeypatch is_global for positive external; RFC 5737 cannot be a positive fixture)
    - tests/ — read one existing test module (e.g. any tests/test_*.py) to match the project's pytest style, fixture conventions, and import path
    - .planning/research/PITFALLS.md (Pitfall 7 + Pitfall 10) — the IP-classifier exclusion list and the synthetic-fixture (RFC 6761/5737) PII rule
  </read_first>
  <behavior>
    - test_is_public_ipv4_negatives: parametrized over RFC1918 (10/172.16/192.168), CGNAT 100.64.0.1, loopback 127.0.0.1, link-local 169.254.0.1, IPv6 link-local fe80::1, malformed "bad"/""/None -> all False
    - test_is_public_ipv4_positive_monkeypatched: monkeypatch ipaddress.ip_address(...).is_global True (or monkeypatch the helper's is_global access) so NO real public IP appears in the fixture (D-18) -> True
    - test_external_tag_scoped: asset tags "Location=External" -> in scoped_df, absent from mismatches_df
    - test_dmz_tag_private_ip_not_mismatch: tags "Location=DMZ" + private IP 10.x -> in scoped_df, absent from mismatches_df (D-06)
    - test_public_ip_untagged_gap: monkeypatched-public IP + no Location tag -> in BOTH frames, untagged_reason=="public_ip_untagged"
    - test_private_untagged_excluded: private IP + no Location tag -> in NEITHER frame
    - test_empty_input_guard: external_scope(empty df) -> two zero-row frames
    - test_missing_columns_failsoft: df without 'tags'/'ipv4' -> two zero-row frames, no raise
    - test_location_category_case_insensitive: tags "location=External" (lowercase category) -> classified external (D-10)
  </behavior>
  <action>
    Create `tests/test_external_scope.py` covering every branch in the Task-1 behavior block. Use ONLY synthetic data (QUAL-05 / D-11): hostnames `*.example.invalid`, asset UUIDs of the `00000000-0000-0000-0000-00000000000N` form, and for the positive-external case MONKEYPATCH `is_global` (D-18) rather than embedding a real public IP — RFC 5737 doc ranges (192.0.2.x / 198.51.100.x / 203.0.113.x) return `is_global=False` and CANNOT serve as the positive external fixture. Negative/edge IP branches use genuinely synthetic RFC-reserved addresses (RFC1918, 100.64.0.1 CGNAT, 127.0.0.1, 169.254.0.1, fe80::1).
    Build small fixture DataFrames with the `tags` ("Category=Value;…") and `ipv4` columns matching the fetcher format. Assert the (scoped_df, mismatches_df) membership for each case, the `untagged_reason=="public_ip_untagged"` value, the both-frames presence of gap assets (D-08), and the case-insensitive Location category match (D-10). Include the empty-input and missing-column fail-soft assertions (QUAL-03).
    Run the suite under pandas CoW strict mode where practical (set `pd.options.mode.copy_on_write = True` at module top or via a fixture) so any chained-assignment regression in external_scope.py surfaces as a test failure.
  </action>
  <acceptance_criteria>
    - `pytest tests/test_external_scope.py -x` exits 0
    - The positive-external test uses a monkeypatch of is_global (no real public IP literal in the file)
    - `grep -nE '\b(10|172\.(1[6-9]|2[0-9]|3[01])|192\.168)\.' tests/test_external_scope.py` shows only intentionally-synthetic RFC1918 negatives; no real-network hostnames/IPs present (PII clean)
    - Tests cover: tag-external, DMZ+private (non-mismatch), public-untagged gap in both frames, private-untagged excluded, empty input, missing columns, case-insensitive Location
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_external_scope.py -x -q</automated>
  </verify>
  <done>tests/test_external_scope.py passes; covers all classifier branches with synthetic-only data; positive external via monkeypatched is_global per D-18.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tenable export data → external_scope() | Untrusted `ipv4`/`tags` string fields from the live tenant cross into the stdlib `ipaddress` parser |
| mismatches_df → output artifacts | Asset-level rows (asset_uuid, ip_address) must reach only operator-local analyst tabs + internal email, never repo/AI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Denial of Service | `is_public_ipv4` parsing untrusted `ipv4` strings | mitigate | `try/except (ValueError, TypeError)` returns False on malformed/empty/None input — never raises into the fail-soft batch (Task 1 + tested in Task 2) |
| T-14-02 | Information Disclosure | `mismatches_df` asset-level PII (D-11) | mitigate | Module docstring documents the D-11 boundary; Task-2 fixtures are synthetic-only (RFC 6761/5737, monkeypatched is_global); no asset-level data committed; mismatch list is runtime-output-only |
| T-14-03 | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — stdlib `ipaddress` + already-pinned pandas only; zero new dependencies (RESEARCH confirmed) |
</threat_model>

<verification>
- SC#1: `pytest tests/test_external_scope.py -x` passes; classifier returns (scoped_df, mismatches_df) with tag-authoritative scope + public_ip_untagged gap; CGNAT/loopback/IPv6-link-local excluded; positive external via monkeypatched is_global (D-18).
- QUAL-03: empty / missing-column inputs return two zero-row frames without raising.
- QUAL-05 / D-11: no real hostnames/IPs in committed fixtures.
</verification>

<success_criteria>
- `utils/external_scope.py` exists, importable, stdlib-only, CoW-compliant.
- `external_scope` emits the D-08 tuple; gap assets in both frames; D-06 DMZ-private is not a mismatch; D-05 IP never overrides a tag.
- `tests/test_external_scope.py` green, synthetic-only, D-18 monkeypatch for the positive case.
</success_criteria>

<output>
Create `.planning/phases/14-shared-substrates-composed-report-gates/14-01-SUMMARY.md` when done
</output>
