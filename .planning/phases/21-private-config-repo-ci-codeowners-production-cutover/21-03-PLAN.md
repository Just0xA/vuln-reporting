---
phase: 21-private-config-repo-ci-codeowners-production-cutover
plan: 03
type: execute
wave: 2
depends_on: [21-01]
files_modified:
  - deploy/config-repo/CODEOWNERS.example
  - deploy/config-repo/contacts.example.yaml
  - deploy/config-repo/deliveries.d/README.md
autonomous: true
requirements: [CONF-04]
must_haves:
  truths:
    - "A CODEOWNERS reference exists with per-file entries 1:1 with the deliveries.d/ split (D-08)"
    - "Every owner entry resolves to the VM team placeholder (central stewardship), and a default * rule covers shared files (contacts.yaml, defaults, schema copy) via the VM team (D-09)"
    - "Concrete VCS handles are documented as D-10 provisioning placeholders, not invented real handles"
    - "A reference contacts.yaml + deliveries.d/ layout shows the real-config shape the private repo holds, using example.invalid identifiers only"
    - "The reference tree resolves cleanly through the Phase 20 loader (contacts sibling present, unique delivery names, contact: refs defined, no inline email:)"
  artifacts:
    - path: "deploy/config-repo/CODEOWNERS.example"
      provides: "Governance reference: per-file VM-team ownership + default rule (D-08/D-09)"
      contains: "deliveries.d/"
    - path: "deploy/config-repo/contacts.example.yaml"
      provides: "Reference contacts + defaults shape for the private repo"
    - path: "deploy/config-repo/deliveries.d/README.md"
      provides: "One-file-per-team layout guidance + owner field convention"
  key_links:
    - from: "CODEOWNERS.example deliveries.d/ entries"
      to: "deliveries.d/<team>.yaml files"
      via: "1:1 per-file mapping to VM-team placeholder"
      pattern: "deliveries.d/"
---

<objective>
Author the PRIVATE config repo's governance + config-tree reference artifacts (D-08/D-09/D-10):
a CODEOWNERS with per-file entries 1:1 with the `deliveries.d/` split where every owner currently
resolves to the Vulnerability Management team (central stewardship), plus a reference
`contacts.yaml` + `deliveries.d/` layout showing the exact shape the private repo holds — all using
synthetic `example.invalid` identifiers.

The 1:1 CODEOWNERS structure is locked now (so per-team delegation can be enabled later without
restructuring), but the roster resolves to the VM team, and concrete VCS handles are D-10
provisioning inputs filled at repo creation — the reference uses a clearly-marked placeholder
handle (`@ORG/vuln-management-team`).

**Reconciliation note (carry into SUMMARY):** ROADMAP SC2 literally says a PR "requires *that
team's* owner." D-08 centralizes review to the VM team now; the 1:1 mapping that ENABLES per-team
ownership exists, but the roster resolves to VM. This is intentional, not an unmet criterion.

Purpose: CONF-04 SC2 (CODEOWNERS half) + SC1 (the config tree the private repo holds).
Output: `deploy/config-repo/CODEOWNERS.example`, `contacts.example.yaml`, `deliveries.d/README.md`.

**Repo target:** PUBLIC app repo under `deploy/config-repo/`, as clearly-labelled REFERENCE
templates for the operator to place into the PRIVATE config repo. Real recipient config lives ONLY
in the private repo (Hard Rule 2 / D-02); these committed samples use `example.invalid` only.
</objective>

<execution_context>
@/home/jmonroe/.claude/plugins/cache/gsd-plugin/gsd/4.0.2/workflows/execute-plan.md
@/home/jmonroe/.claude/plugins/cache/gsd-plugin/gsd/4.0.2/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-private-config-repo-ci-codeowners-production-cutover/21-CONTEXT.md
@.planning/phases/21-private-config-repo-ci-codeowners-production-cutover/21-PATTERNS.md
@contacts.example.yaml

<interfaces>
<!-- Exact config shape to copy. From Phase 20 example + fixtures. -->

contacts.example.yaml (this repo) — contacts + defaults shape:
  contacts: { <key>: { recipients: [...], cc: [...], reply_to: ... } }
  defaults: { analyst_mailbox: analyst-team@example.invalid }   # default Reply-To + standing Cc

tests/fixtures/phase20_config_twin/deliveries.d/exec.yaml — per-team file shape:
  owner: <team-slug>
  deliveries:
    - name: "<Delivery Name>"       # globally unique across all deliveries.d/ files
      subject: "..."
      contact: <key>                # references contacts.yaml by name; NEVER inline email:
      schedule: { frequency: weekly, day_of_week: monday, time: "07:00" }
      filters: { tag_category: ..., tag_value: ... }
      reports: [ executive_kpi, trend_analysis ]

resolve_config(config_path) errors on: missing contacts.yaml sibling; duplicate delivery name
  across files; undefined contact: ref; inline email: in directory mode. The CI gate (Plan 02)
  surfaces exactly these — so the reference tree must resolve cleanly.

CODEOWNERS syntax (no repo analog): `<path-pattern>  @owner` per line; `*` default rule first.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the CODEOWNERS reference (per-file VM-team ownership + default rule)</name>
  <files>deploy/config-repo/CODEOWNERS.example</files>
  <read_first>
    - .planning/phases/21-private-config-repo-ci-codeowners-production-cutover/21-PATTERNS.md §"CODEOWNERS — config-repo governance" (lines 179-198; D-08/D-09/D-10 structure + reconciliation flag)
    - .planning/phases/21-private-config-repo-ci-codeowners-production-cutover/21-CONTEXT.md §"decisions" D-08/D-09/D-10
    - tests/fixtures/phase20_config_twin/deliveries.d/ (the team-file set the 1:1 mapping mirrors: exec.yaml, remediation.yaml, tag_profile.yaml)
  </read_first>
  <action>
    Create `deploy/config-repo/CODEOWNERS.example` in GitHub/GitLab CODEOWNERS syntax. Structure per
    D-08/D-09: (1) a leading default rule `*  @ORG/vuln-management-team` covering shared cross-cutting
    files — `contacts.yaml`, the `defaults` block, and the schema copy (D-09); (2) one per-file entry
    per team delivery file, 1:1 with the `deliveries.d/` split (e.g.
    `deliveries.d/exec.yaml  @ORG/vuln-management-team`, and the same for `remediation.yaml`,
    `tag_profile.yaml`), each currently resolving to the SAME VM-team placeholder handle (central
    stewardship). Add a header comment block stating: the 1:1 structure exists so per-team delegation
    can be enabled later by swapping a handle without restructuring; `@ORG/vuln-management-team` is a
    D-10 provisioning placeholder to be replaced at repo creation; and the SC2 reconciliation
    (central VM-team review now, per-team wording satisfied by the 1:1 structure). Do NOT invent real
    GitHub org/team handles.
  </action>
  <verify>
    <automated>test -f deploy/config-repo/CODEOWNERS.example && grep -q "^\*[[:space:]].*vuln-management-team" deploy/config-repo/CODEOWNERS.example && grep -q "deliveries.d/" deploy/config-repo/CODEOWNERS.example && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File has a leading `*` default rule pointing at the `@ORG/vuln-management-team` placeholder (D-09)
    - There is one `deliveries.d/<team>.yaml` entry per fixture team file (exec, remediation, tag_profile), each pointing at the VM-team placeholder (D-08 1:1)
    - Header comment documents the D-10 placeholder and the SC2 reconciliation (central review now, delegation later)
    - No real org/team handle literals present (only the `@ORG/...` placeholder)
  </acceptance_criteria>
  <done>A CODEOWNERS.example with a default VM-team rule plus 1:1 per-file VM-team entries and documented D-10/SC2 notes.</done>
</task>

<task type="auto">
  <name>Task 2: Author the reference contacts.yaml + deliveries.d/ layout guidance</name>
  <files>deploy/config-repo/contacts.example.yaml, deploy/config-repo/deliveries.d/README.md</files>
  <read_first>
    - contacts.example.yaml (this repo — the exact contacts + defaults shape to mirror)
    - tests/fixtures/phase20_config_twin/deliveries.d/exec.yaml (the per-team file shape: owner + deliveries list, contact: refs)
    - .planning/phases/21-private-config-repo-ci-codeowners-production-cutover/21-PATTERNS.md §"contacts.yaml + deliveries.d/<team>.yaml" (lines 202-247; resolver contract + Hard Rule 2)
  </read_first>
  <action>
    Create two reference artifacts documenting the config tree the private repo holds. (1)
    `deploy/config-repo/contacts.example.yaml` — mirror this repo's root `contacts.example.yaml`
    shape (a `contacts:` map with `recipients`/optional `cc`/`reply_to`, and a `defaults:` block with
    `analyst_mailbox`), `example.invalid` addresses only, with a header comment: "This is the SHARED
    who — contacts + defaults live ONLY here, never duplicated in deliveries.d/ files (the Nothing
    Defined Twice guardrail)." (2) `deploy/config-repo/deliveries.d/README.md` — explain the
    one-file-per-team convention: each `<team>.yaml` carries an `owner:` field (feeds the matrix +
    the 1:1 CODEOWNERS entry) and a `deliveries:` list; every delivery references a contact by
    `contact: <key>` (never inline `email:`); delivery `name` must be globally unique across all team
    files (the loader rejects duplicates); reports come from the registered slugs. Show one short
    illustrative `<team>.yaml` snippet inside the README using `example.invalid`. Do NOT commit a real
    `deliveries.d/*.yaml` config file here — the README plus the snippet is the reference; real team
    files live only in the private repo (Hard Rule 2 / D-02).
  </action>
  <verify>
    <automated>.venv/bin/python3 -c "import yaml; d=yaml.safe_load(open('deploy/config-repo/contacts.example.yaml')); assert 'contacts' in d and 'defaults' in d and 'analyst_mailbox' in d['defaults']; s=open('deploy/config-repo/contacts.example.yaml').read(); assert 'example.invalid' in s and '@example.com' not in s; print('contacts OK')" && grep -Eiq "owner|contact:|unique" deploy/config-repo/deliveries.d/README.md && echo README_OK</automated>
  </verify>
  <acceptance_criteria>
    - `contacts.example.yaml` parses, has `contacts:` + `defaults.analyst_mailbox`, and uses only `example.invalid` addresses (no real domains)
    - Header comment states contacts + defaults live ONLY in contacts.yaml (Nothing-Defined-Twice guardrail)
    - `deliveries.d/README.md` documents the owner field, `contact:` refs (no inline email:), and global delivery-name uniqueness, with an `example.invalid` snippet
    - No real `deliveries.d/*.yaml` config file committed under `deploy/config-repo/`
  </acceptance_criteria>
  <done>Reference contacts + deliveries.d/ guidance that mirrors the Phase 20 shape, enforces the guardrails in prose, and carries only synthetic identifiers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR author → deliveries.d/<team>.yaml | A config change is reviewed via CODEOWNERS before it can merge and reach production |
| committed reference files → public repo | Reference samples must never carry real recipient PII into the public app repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-09 | Elevation of Privilege | CODEOWNERS bypass | mitigate | Default `*` rule + per-file 1:1 entries ensure NO config path is unowned; every PR requires VM-team review (private repo must also enable branch protection "require review from code owners" — documented in Plan 04 runbook) |
| T-21-10 | Repudiation | unowned shared files | mitigate | `contacts.yaml`, `defaults`, and the schema copy are covered by the default VM-team rule (D-09) — a change to the shared "who" is always a reviewed change |
| T-21-11 | Information Disclosure | committed reference config | mitigate | All reference samples use `example.invalid` identifiers (Hard Rule 2 / QUAL-05 / D-02); no real recipients, no real VCS handles; verify gate greps for real-domain leakage |
| T-21-12 | Tampering | duplicate/undefined config refs | mitigate | The reference tree is documented to satisfy the resolver contract (unique names, defined `contact:` refs, no inline email:); the CI gate (Plan 02) enforces this on every real PR |
</threat_model>

<verification>
- `deploy/config-repo/CODEOWNERS.example` has a default VM-team rule + 1:1 per-team entries; no real handles.
- `deploy/config-repo/contacts.example.yaml` parses, carries `contacts:` + `defaults.analyst_mailbox`, `example.invalid` only.
- `deploy/config-repo/deliveries.d/README.md` documents owner/contact-ref/uniqueness; no real config committed.
</verification>

<success_criteria>
- CONF-04 SC2 (CODEOWNERS half): per-file 1:1 governance mapped to the `deliveries.d/` split, VM-team central stewardship (D-08), default rule for shared files (D-09), D-10 placeholder handles.
- CONF-04 SC1 support: the config-tree shape the private repo holds is documented and resolves cleanly through the Phase 20 loader.
- Hard Rule 2 / D-02 honored: synthetic `example.invalid` only; no real config or handles in the public repo.
</success_criteria>

<output>
Create `.planning/phases/21-private-config-repo-ci-codeowners-production-cutover/21-03-SUMMARY.md` when done.
</output>
