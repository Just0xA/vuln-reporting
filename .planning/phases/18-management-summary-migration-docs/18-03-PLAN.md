---
phase: 18-management-summary-migration-docs
plan: 03
type: execute
wave: 2
depends_on: ["18-02"]
files_modified:
  - scripts/backfill_trend_reconstruction.py
  - tests/test_backfill_reconstruction.py
  - data/trend_store.py
autonomous: false
requirements: [GEN-01, QUAL-04]
must_haves:
  truths:
    - "A one-time idempotent seeding script reconstructs ~12 months (2025-06 → now) of real MoM history from Tenable fixed+open exports and writes it to the S1 trend store BEFORE the cutover (D-18-01, D-18-08, decoupled from the 2,200-line removal)"
    - "Reconstructed snapshots are provenance-marked (source='reconstructed') and IMMUTABLE — never overwrite an existing captured or reconstructed month (D-18-03)"
    - "asset_count is null on every reconstructed month (Tenable does not retain historical asset population) so Vulnerability Density stays cold-start for those months — no fabricated denominator (D-18-04)"
    - "Jun–Aug 2025 taper-edge months carry a partial/approximate flag (D-18-02)"
    - "An embedded overlap-test gate reconstructs a month that has a real captured snapshot (fallback: reconstruct today vs live) and asserts finding-derived fields match within a stated tolerance; exits non-zero on failure BEFORE any reconstructed history is written (D-18-09, D-18-10 gate 2)"
    - "The reconstruction predicate reuses open_findings_at() reopened-aware two-interval form with fixed-data add-back — no naive last_fixed-null-or-gt predicate (QUAL-02)"
  artifacts:
    - path: "scripts/backfill_trend_reconstruction.py"
      provides: "One-time idempotent reconstruction seeding script with embedded overlap-test gate, provenance + immutability + partial-flag + null-asset_count"
      min_lines: 120
    - path: "tests/test_backfill_reconstruction.py"
      provides: "Unit tests: overlap-test pass/fail, immutability skip (captured + reconstructed), partial flag on taper months, asset_count null, reopened-aware predicate"
      contains: "def test_overlap"
  key_links:
    - from: "scripts/backfill_trend_reconstruction.py"
      to: "utils.open_count.open_findings_at"
      via: "reconstruction predicate at each past month-boundary with fixed add-back"
      pattern: "open_findings_at"
    - from: "scripts/backfill_trend_reconstruction.py"
      to: "data.trend_store (_atomic_write_json / _load_trend_json / TREND_DIR)"
      via: "immutable provenance-marked write to trend_severity_all_assets.json"
      pattern: "_atomic_write_json"
---

<objective>
Reconstruct ~12 months of real MoM history (2025-06 → now) from Tenable's
now-retrievable fixed+open exports and seed the S1 trend store BEFORE the
cutover (D-18-01, D-18-08). This is its OWN plan that runs and is verified
independently — the migration (Plan 04) then simply reads a store that already
has history. The whole sequencing design (D-18-10) keeps a real build out of the
2,200-line atomic removal.

The reconstruction is paired with audit-honesty guardrails that MUST NOT be
simplified away (CONTEXT specifics): provenance-marked immutable snapshots
(D-18-03), faithful partial backfill with null asset_count rather than a
fabricated denominator (D-18-04), a partial flag on taper-edge months (D-18-02),
and an embedded overlap-test against ground truth (D-18-09) that gates the whole
write. DOC-02 trust is the point.

Purpose: Ship migrated trend with 12mo of REAL history rather than cold-starting,
with provenance honesty an auditor can trust.
Output: idempotent seeding script + overlap-test gate + unit tests. Operator runs
the script once (checkpoint) to seed before cutover.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-management-summary-migration-docs/18-CONTEXT.md
@.planning/phases/18-management-summary-migration-docs/18-RESEARCH.md
@.planning/spikes/002-trend-reconstruction-lookback/README.md
<interfaces>
<!-- data/trend_store.py [VERIFIED]: -->
<!--   TREND_DIR = ROOT_DIR / "data" / "trend"; file = trend_{dimension}_{tag_filter}.json -->
<!--   _load_trend_json(path) -> list[dict];  _atomic_write_json(path, {"snapshots": [...]}) -->
<!--   capture_snapshot(df, assets_df, date, dimension="severity", tag_filter="all_assets", ...) -> Path -->
<!--   read_trend(dimension, tag_filter="all_assets", months=6) -> {"snapshots": list, "insufficient_data": bool} -->
<!-- utils/open_count.py [VERIFIED]: open_findings_at(df, date) -> pd.DataFrame (reopened-aware two-interval) -->
<!-- D-18-03 provenance field: "source" (absent → defaults to "captured" per D-16-09 implicit-optional convention; no schema_version) -->
<!-- D-18-04: asset_count = None on reconstructed months -->
<!-- D-18-02: "partial": true on months < 2025-09 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write reconstruction + overlap-test unit tests (RED)</name>
  <read_first>
    - tests/test_trend_store.py (existing trend-store test conventions, synthetic snapshot fixtures, trend_dir override pattern — pass trend_dir to avoid touching real data/trend/)
    - utils/open_count.py (open_findings_at signature + the two-interval reopened-aware semantics: open at D if (first_found<=D OR resurfaced_date<=D) AND NOT (last_fixed<=D AND resurfaced_date>D))
    - data/trend_store.py lines 100-160 (_load_trend_json, _atomic_write_json shapes) and 426-481 (read_trend)
    - .planning/spikes/002-trend-reconstruction-lookback/README.md (the validated predicate + the +2/160,453 overlap benchmark that sets the tolerance)
    - reports/modules/mttr_trend_module.py tests for the synthetic RFC-5737/6761 data style (QUAL-05)
  </read_first>
  <action>
    Create tests/test_backfill_reconstruction.py with synthetic-only data (RFC-5737 IPs, RFC-6761 hostnames — QUAL-05) using a tmp trend_dir. Write tests (RED — the script does not exist yet):
    (1) test_overlap_pass — given a synthetic captured snapshot for month M and a synthetic fixed+open frame, the reconstruction of M matches captured per-severity counts within tolerance.
    (2) test_overlap_fail_exits_nonzero — when reconstruction diverges beyond tolerance, the gate raises / returns failure and NO snapshots are written.
    (3) test_immutability_skips_captured — a month already present with source='captured' is never overwritten.
    (4) test_immutability_skips_reconstructed — a month already present with source='reconstructed' is skipped (idempotent re-run, D-18-08).
    (5) test_partial_flag_on_taper_months — months before 2025-09 carry partial=True; Sep+ do not (D-18-02).
    (6) test_asset_count_null_on_reconstructed — every reconstructed entry has asset_count is None (D-18-04).
    (7) test_reopened_aware_predicate — a REOPENED finding (first_found 200d ago, resurfaced 10d ago, last_fixed 2d ago style) is counted open-at-D correctly via open_findings_at, NOT dropped by a naive predicate (QUAL-02).
    Define the tolerance constant in the test referencing the spike benchmark (≤2% relative OR ≤5 absolute per severity — RESEARCH A5, within D-18-09 discretion).
  </action>
  <verify>
    <automated>cd "D:/Projects/vuln-reporting" && python -m pytest tests/test_backfill_reconstruction.py -x 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_backfill_reconstruction.py exists with the 7 named tests above (grep "def test_overlap_pass", "def test_immutability_skips_captured", "def test_partial_flag", "def test_asset_count_null", "def test_reopened_aware" all match)
    - Tests use a tmp/override trend_dir (no writes to real data/trend/)
    - All synthetic data uses RFC-5737/6761 addresses (QUAL-05)
    - At RED the suite fails because scripts/backfill_trend_reconstruction.py does not yet exist (import error or NotImplemented)
  </acceptance_criteria>
  <done>The reconstruction test suite encodes the overlap gate, immutability, partial flag, null asset_count, and reopened-aware predicate; RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the reconstruction seeding script + embedded overlap-test gate (GREEN)</name>
  <behavior>
    - Test: reconstruction of a captured month matches within tolerance (overlap gate passes)
    - Test: divergence beyond tolerance exits non-zero and writes nothing
    - Test: existing captured/reconstructed months are skipped (immutable, idempotent)
    - Test: taper months (<2025-09) carry partial=True; asset_count is None on all reconstructed months
    - Edge case: REOPENED findings counted open-at-D via the two-interval predicate
  </behavior>
  <read_first>
    - tests/test_backfill_reconstruction.py (the RED tests from Task 1 — the implementation target)
    - data/trend_store.py (TREND_DIR, _load_trend_json, _atomic_write_json, the snapshot list shape {"snapshots":[...]}, the month_str = date.strftime("%Y-%m") LOCAL convention, generated_at UTC)
    - utils/open_count.py (open_findings_at — the predicate to reuse; the reconstruction df = current-open rows + fixed rows where last_fixed > month_end_D)
    - data/fetchers.py (fetch_all_vulnerabilities + fetch_fixed_vulnerabilities — now bounded after Plan 02; the script fetches both, or accepts cached parquet via cache_dir for the one-time run)
    - scripts/capture_trend_snapshot.py (the forward cron entry-point conventions: sys.path bootstrap, argparse, tag_filter handling — the backfill script mirrors its bootstrap but is one-time)
    - CLAUDE.md timezone policy (month key local, generated_at UTC) + pandas CoW (.assign() only)
  </read_first>
  <action>
    Create scripts/backfill_trend_reconstruction.py (D-18-08). It: (1) loads current-open vulns via fetch_all_vulnerabilities and fixed vulns via the now-bounded fetch_fixed_vulnerabilities (or reads cached parquet via --cache-dir for the one-time operator run); (2) for each month-boundary from 2025-06 through the current month, builds the reconstruction input as current-open rows + fixed rows where last_fixed > month_end_D, then applies open_findings_at(combined, month_end_D) (reopened-aware, QUAL-02) and groups by severity; (3) BEFORE writing anything, runs the embedded overlap-test gate (D-18-09): auto-detect months with an existing source='captured' entry and reconstruct them for comparison (fallback: reconstruct "today" and compare to live fetch_all_vulnerabilities open count); assert each severity within tolerance (≤2% relative OR ≤5 absolute per severity); if any fails, log the divergence and exit non-zero WITHOUT writing reconstructed history; (4) only on gate pass, write each reconstructed month via the immutable provenance-marked path — skip months already present (captured OR reconstructed, D-18-03/D-18-08), set source='reconstructed', asset_count=None (D-18-04), partial=True for months < 2025-09 (D-18-02), generated_at UTC; use _atomic_write_json to write {"snapshots": [...]} to trend_severity_all_assets.json. Add a small helper in data/trend_store.py if a clean immutable-write primitive is warranted (e.g. write_reconstructed_snapshot) OR keep the write logic in the script and reuse _atomic_write_json/_load_trend_json directly — pick the smaller change; if added to trend_store.py it must follow the existing single-writer + atomic-replace contract and not regress capture_snapshot. argparse: --cache-dir, --window-start (default 2025-06), --dry-run (run the gate + report but never write). Use .assign()/masks, never df["col"]= after a filter (QUAL-03).
  </action>
  <verify>
    <automated>cd "D:/Projects/vuln-reporting" && python -m pytest tests/test_backfill_reconstruction.py -x 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - scripts/backfill_trend_reconstruction.py exists, parses, and tests/test_backfill_reconstruction.py passes (pytest exits 0)
    - The script calls open_findings_at (grep matches) and never uses a naive `last_fixed.isnull() | last_fixed > D` open predicate (grep shows no such inline form)
    - Reconstructed entries have source='reconstructed', asset_count=None, and partial=True only for months < 2025-09 (asserted by tests)
    - Immutability: a second invocation writes no new months (idempotent — asserted)
    - The overlap-test gate exits non-zero on divergence and writes nothing (asserted by test_overlap_fail_exits_nonzero)
    - `python -W error::FutureWarning -m pytest tests/test_backfill_reconstruction.py` raises no ChainedAssignment FutureWarning
    - If data/trend_store.py was modified, existing tests/test_trend_store.py still passes
  </acceptance_criteria>
  <done>The idempotent reconstruction script with embedded overlap gate is GREEN; provenance/immutability/partial/null-asset_count all enforced and tested.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator one-time seed run + overlap-gate verification</name>
  <files>data/trend/trend_severity_all_assets.json</files>
  <action>Operator runs scripts/backfill_trend_reconstruction.py once against the live tenant to seed ~12mo of reconstructed history into the S1 store after the embedded overlap-test gate passes (D-18-08, gate 2). Executor cannot auto-run this — it needs live Tenable credentials and writes the real gitignored trend store.</action>
  <what-built>
    scripts/backfill_trend_reconstruction.py — a one-time, idempotent reconstruction
    seeding script with an embedded overlap-test gate. It reconstructs ~12 months
    (2025-06 → now) of real MoM severity history from Tenable fixed+open exports,
    validates the predicate against ground truth, and seeds data/trend/trend_severity_all_assets.json
    with provenance-marked, immutable, partial-flagged, null-asset_count snapshots.
    This must be run ONCE against the live tenant to seed history BEFORE the Plan 04
    cutover (D-18-08, D-18-10 gate 2). It cannot be auto-run by the executor because
    it needs live Tenable credentials and writes the real (gitignored) trend store.
  </what-built>
  <how-to-verify>
    1. Dry-run the overlap gate first (no writes):
       `python scripts/backfill_trend_reconstruction.py --dry-run`
       Confirm it prints an overlap-test result for the captured month(s) (or the
       today-vs-live fallback) and reports PASS within tolerance. If it reports a
       divergence, STOP — the predicate must be corrected before seeding.
    2. Inspect the planned reconstructed months and confirm Jun–Aug 2025 are flagged
       partial and asset_count is null for all reconstructed months.
    3. Run the real seeding once:
       `python scripts/backfill_trend_reconstruction.py`
       Confirm it exits 0 and reports the count of months written.
    4. Confirm idempotency: run it a SECOND time and confirm it writes 0 new months
       (all already present).
    5. Spot-check data/trend/trend_severity_all_assets.json: reconstructed entries
       carry source="reconstructed", asset_count=null, and partial=true on Jun–Aug 2025.
    6. Confirm read_trend("severity","all_assets",months=13) now returns >=2 snapshots
       (insufficient_data is False) so Plan 04's modules will NOT cold-start.
  </how-to-verify>
  <resume-signal>Type "approved" once the overlap gate passed and 12mo of reconstructed history is seeded and verified, or describe the divergence.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tenable API → reconstruction script | Credentials + row-level fixed/open data enter the script |
| reconstruction script → data/trend/*.json | Aggregate counts cross into the persisted (gitignored) trend store |
| reconstructed history → migrated report consumers | Predicate output becomes the trend basis every MoM module reads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-06 | Information Disclosure | trend_severity_all_assets.json reconstructed entries | mitigate | Aggregate severity counts only — no hostnames/IPs/plugin names/asset UUIDs (QUAL-05/D-04-08); asset_count=null not fabricated; data/trend/ is gitignored (legacy scrub precedent); tests assert entry shape |
| T-18-07 | Tampering (false history) | reconstruction predicate output | mitigate | Embedded overlap-test gate (D-18-09) validates against captured ground truth within tolerance BEFORE any write; exits non-zero on divergence; reopened-aware predicate (QUAL-02) avoids the ~19% REOPENED drop |
| T-18-08 | Tampering (overwrite ground truth) | existing captured snapshots | mitigate | Immutability check (D-18-03): never overwrite an existing captured OR reconstructed month; idempotent re-run asserted (Pitfall 2) |
| T-18-09 | Repudiation | provenance of reconstructed vs captured data | mitigate | source='reconstructed' marker + partial flag + generated_at UTC make the data origin auditable; runbook (Plan 05) discloses the predicate (D-18-07) |
| T-18-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs; existing pandas/datetime/pyTenable only; no legitimacy gate required |
</threat_model>

<verification>
- `python -m pytest tests/test_backfill_reconstruction.py -x` exits 0
- Overlap gate passes on dry-run against the live tenant (operator checkpoint)
- 12mo reconstructed history seeded; re-run writes 0 months (idempotent)
- read_trend returns insufficient_data=False after seeding
- No ChainedAssignment FutureWarning
</verification>

<success_criteria>
~12 months of real, provenance-honest, overlap-validated history is seeded into
the S1 store before the cutover; D-18-10 gate 2 is GREEN; the store is ready for
Plan 04 to read.
</success_criteria>

<output>
Create `.planning/phases/18-management-summary-migration-docs/18-03-SUMMARY.md` when done
</output>
