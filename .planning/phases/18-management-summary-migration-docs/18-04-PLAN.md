---
phase: 18-management-summary-migration-docs
plan: 04
type: execute
wave: 3
depends_on: ["18-01", "18-02", "18-03"]
files_modified:
  - reports/management_summary.py
  - run_all.py
  - tests/test_phase6_run_group_chrome.py
  - tests/test_management_summary.py
autonomous: false
requirements: [GEN-01, QUAL-04]
must_haves:
  truths:
    - "management_summary delivers via ReportComposer.run_full_pipeline() composing all seven modules (total_vulns_by_severity, scan_coverage_sla, mttr_trend, patch_compliance_rate, aged_vulns_assets, accepted_recast, new_vs_remediated) (GEN-01, D-18-10 gate 4)"
    - "The bespoke _save_trend_snapshot/_load_trend_history/_trend_file_path/_sanitise_tag_for_filename/_compute_metric_1..7/compute_all_metrics/_build_age_bar_chart/_build_trend_line_chart/_build_pdf/build_email_kpi_tiles/build_email_body are deleted in the SAME commit that routes reads through read_trend() — no dual-writer window (QUAL-04, D-18-10)"
    - "management_summary is added to _CHROME_AWARE_SLUGS and run_report() accepts privacy_label/scope_subtitle/report_title, inheriting the PDF chrome header/footer (GEN-01)"
    - "The email body routes through build_email_body_modular() via a non-empty email_body_html in the return dict (GEN-01)"
    - "trend_snapshots from read_trend() reaches the seven modules' compute() so MoM modules use the 12mo reconstructed history and do NOT cold-start (Pitfall 4)"
    - "The post-cutover smoke script passes against the Plan 01 baseline — section count, RAG cell count, module presence all match (D-04-05 structure-locked, QUAL-04)"
    - "Existing delivery_config.yaml groups referencing management_summary deliver with NO YAML changes (QUAL-04 no regression)"
    - "Legacy management_summary_*.json is archived to data/trend/legacy_archive/, not deleted (D-18-11)"
  artifacts:
    - path: "reports/management_summary.py"
      provides: "Migrated run_report() on ReportComposer pipeline; _MGMT_MODULE_CONFIGS list of 7; read_trend wiring; chrome kwargs; _bundle key; bespoke path removed"
      contains: "ReportComposer"
    - path: "run_all.py"
      provides: "management_summary in _CHROME_AWARE_SLUGS; CHROME-COMPAT-01 exclusion removed"
      contains: "management_summary"
    - path: "tests/test_management_summary.py"
      provides: "module presence, email_body_html non-empty, bespoke functions removed, chrome kwargs accepted, trend forwarded"
      contains: "def test_"
  key_links:
    - from: "reports/management_summary.py::run_report"
      to: "reports.modules.ReportComposer.run_full_pipeline"
      via: "_MGMT_MODULE_CONFIGS + trend_snapshots + fixed_vulns_df + PdfChromeConfig (board_summary analog)"
      pattern: "run_full_pipeline"
    - from: "reports/management_summary.py::run_report"
      to: "data.trend_store.read_trend"
      via: "read_trend('severity', tag_filter, months=13) passed to ReportComposer so modules receive trend_snapshots"
      pattern: "read_trend"
    - from: "run_all.py::_CHROME_AWARE_SLUGS"
      to: "reports.management_summary.run_report chrome kwargs"
      via: "chrome injection block passes privacy_label/scope_subtitle/report_title"
      pattern: "management_summary"
---

<objective>
The atomic migration cutover (GEN-01, D-18-10 gate 4, the roadmap's highest-risk
work). Replace `management_summary`'s ~2,200-line bespoke render path with
`ReportComposer.run_full_pipeline()` composing the seven existing registered
modules — exactly mirroring the verified `board_summary` analog. In the SAME
commit: delete the bespoke trend writer/reader/compute/PDF/email functions, route
reads through `read_trend()`, add management_summary to `_CHROME_AWARE_SLUGS`,
add the chrome kwargs to `run_report()`, and route email through
`build_email_body_modular()` via a non-empty `email_body_html`. No dual-writer
window (QUAL-04). Archive (don't delete) the legacy JSON (D-18-11).

This plan ONLY proceeds when gates 1–3 are GREEN: Plan 02 (bounded fetch + no
drift), Plan 03 (12mo seeded + overlap-validated), Plan 01 (smoke baseline
committed). The migration just reads a pre-seeded store.

Purpose: Complete GEN-01 — management_summary becomes a thin four-channel
composed report, chrome-aware, with zero regression to existing delivery.
Output: migrated report + tests + post-cutover smoke parity + operator visual UAT.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-management-summary-migration-docs/18-CONTEXT.md
@.planning/phases/18-management-summary-migration-docs/18-RESEARCH.md
@reports/board_summary.py
<interfaces>
<!-- board_summary wiring to copy (reports/board_summary.py ~L278-305) [VERIFIED]: -->
<!--   PdfChromeConfig(title, subtitle, generated_at, header_bg=HEADER_BG_COLOR, logo_path=LOGO_PATH, privacy_label) -->
<!--   ReportComposer(vulns_df, assets_df, report_date=generated_at, module_configs=_MGMT_MODULE_CONFIGS, fixed_vulns_df, pdf_chrome) -->
<!--   results = composer.run_all() -->
<!--   bundle = composer.run_full_pipeline(results, output_dir, slug="management_summary", report_date, generate_analyst=analyst_detail, pdf_title, pdf_subtitle, scope_label) -->
<!-- run_all.py [VERIFIED]: -->
<!--   L95 _CHROME_AWARE_SLUGS = frozenset({"board_summary","composed_report"}) -->
<!--   L725 chrome injection: report_kwargs["privacy_label"/"scope_subtitle"/"report_title"] when slug in _CHROME_AWARE_SLUGS -->
<!--   L93-94 CHROME-COMPAT-01 comment excluding management_summary — REMOVE it -->
<!-- tests/test_phase6_run_group_chrome.py:32 asserts _CHROME_AWARE_SLUGS == frozenset({"board_summary","composed_report"}) — MUST update expected set (project_frozenset_gate_test_coupling) -->
<!-- Bespoke removal surface (reports/management_summary.py) [VERIFIED line numbers]: -->
<!--   _sanitise_tag_for_filename L128, _trend_file_path L152, _load_trend_history L166, _save_trend_snapshot L184, -->
<!--   _compute_metric_1..7 L257-764, compute_all_metrics L772, _build_age_bar_chart L1105, _build_trend_line_chart L1154, -->
<!--   _build_pdf L1205, build_email_kpi_tiles L1793, build_email_body L1934, _email_preview_html L2138, run_report L2270 -->
<!-- Seven module IDs → files: total_vulns_by_severity, scan_coverage_sla, mttr_trend, patch_compliance_rate, aged_vulns_assets, accepted_recast, new_vs_remediated -->
<!-- read_trend('severity','all_assets',months=13) -> {"snapshots":[...],"insufficient_data":bool} -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration acceptance tests (RED)</name>
  <read_first>
    - reports/management_summary.py lines 2270-2400 (current bespoke run_report signature + return dict shape — the contract callers depend on; what must stay: REPORT_NAME, REPORT_SLUG, run_report() callable signature, the standard return keys pdf/excel/charts/metrics/analyst_excel/email_body_html)
    - reports/board_summary.py full (the analog: _MGMT_MODULE_CONFIGS equivalent, run_report wiring, the _bundle key pattern, how it returns email_body_html/analyst_excel/email_inline_images)
    - tests/test_board_summary_baseline.py (how board asserts module presence + bundle structure — mirror it)
    - reports/modules/composer.py (ReportComposer constructor + run_all + run_full_pipeline kwargs; how trend_snapshots is forwarded to modules — the **kwargs fan-out; confirm whether trend_snapshots is a constructor kwarg or run_all kwarg, per Open Question 1 follow the board_summary direct-wiring answer)
    - reports/modules/mttr_trend_module.py / new_vs_remediated_module.py / accepted_recast_module.py (confirm each compute() consumes trend_snapshots via **kwargs — Pitfall 4)
    - tests/test_management_summary.py target rows in 18-RESEARCH.md Validation Architecture (test_module_presence, test_bespoke_functions_removed)
  </read_first>
  <action>
    Create tests/test_management_summary.py with synthetic-only fixtures (QUAL-05). Tests (RED — migration not done yet):
    (1) test_module_presence — calling run_report() (with a synthetic/no-live composer path, mirroring test_board_summary_baseline) yields a bundle whose module_results contain all seven module IDs.
    (2) test_email_body_html_nonempty — the run_report() return dict has a non-empty email_body_html (so build_email_body_modular() routing fires).
    (3) test_bespoke_functions_removed — the listed bespoke functions (_save_trend_snapshot, _load_trend_history, _trend_file_path, _sanitise_tag_for_filename, _compute_metric_1.._7, compute_all_metrics, _build_age_bar_chart, _build_trend_line_chart, _build_pdf, build_email_kpi_tiles, build_email_body) are NOT attributes of the module (assert not hasattr) — proves atomic removal.
    (4) test_run_report_accepts_chrome_kwargs — run_report accepts privacy_label/scope_subtitle/report_title without TypeError (Pitfall 3).
    (5) test_trend_forwarded_no_coldstart — given a seeded (>=2 snapshot) trend via a tmp trend_dir / injected read_trend, the MoM-consuming modules do NOT report cold-start (Pitfall 4).
    Also UPDATE tests/test_phase6_run_group_chrome.py line 32 expected set to include "management_summary" (project_frozenset_gate_test_coupling — this co-edit is mandatory or the frozenset test fails).
  </action>
  <verify>
    <automated>cd "D:/Projects/vuln-reporting" && python -m pytest tests/test_management_summary.py tests/test_phase6_run_group_chrome.py -x 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_management_summary.py exists with the 5 named tests (grep matches each def)
    - tests/test_phase6_run_group_chrome.py:32 expected frozenset now includes "management_summary"
    - All fixtures use synthetic RFC-5737/6761 data (QUAL-05)
    - At RED the migration tests fail (bespoke functions still present, no _bundle/email_body_html, chrome kwargs rejected)
  </acceptance_criteria>
  <done>Migration acceptance tests + frozenset co-edit are in place; RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Atomic cutover — migrate run_report onto ReportComposer, delete bespoke path, wire chrome + trend (GREEN)</name>
  <behavior>
    - Test: run_report() bundle contains all seven module IDs
    - Test: return dict email_body_html is non-empty (modular email routing)
    - Test: bespoke functions are removed (not hasattr)
    - Test: run_report accepts privacy_label/scope_subtitle/report_title
    - Test: with seeded trend, MoM modules do not cold-start
    - Edge case: a delivery group with no tag filter ("all_assets") and a tag-filtered group both produce a valid bundle
  </behavior>
  <read_first>
    - reports/board_summary.py (the exact wiring to replicate — _MGMT_MODULE_CONFIGS, PdfChromeConfig, ReportComposer, run_all, run_full_pipeline, _bundle return key, email_body_html/analyst_excel/email_inline_images)
    - reports/management_summary.py FULL bespoke surface (lines 128-2270 — every function being deleted; confirm _PDF_CSS/_AGE_BUCKETS/_OPEN_STATES constants are consumed ONLY by deleted functions before removing them; preserve REPORT_NAME/REPORT_SLUG/module imports/run_report signature shell/__main__ block updated to call the new pipeline)
    - run_all.py lines 90-120 (_CHROME_AWARE_SLUGS L95, _REPORT_MODULE_MAP — management_summary already mapped at L118), lines 715-728 (chrome injection block + the CHROME-COMPAT-01 exclusion comment L93-94 to remove)
    - data/trend_store.py read_trend (the call to wire) + capture_snapshot (the forward-write call to replace _save_trend_snapshot)
    - reports/modules/composer.py (how trend_snapshots reaches modules — pass it the same way board_summary passes fixed_vulns_df; follow Open Question 1 recommendation: direct-wire trend_snapshots in run_report, not the composed_report frozenset)
    - CLAUDE.md "Modular reports — bundle-driven routing" (email_body_html non-empty predicate, analyst_excel Path, email_inline_images) + timezone policy + fail-soft
  </read_first>
  <action>
    In ONE commit (atomic, QUAL-04, no dual-writer window — RESEARCH Pitfall 5):
    (a) Add `_MGMT_MODULE_CONFIGS = [ModuleConfig(id) for the seven IDs]` to reports/management_summary.py.
    (b) Rewrite run_report() to mirror board_summary: fetch vulns/assets/fixed_vulns (shared parquet cache), apply tag filter, compute tag_filter_label, call read_trend("severity", tag_filter_label, months=13), build PdfChromeConfig, instantiate ReportComposer(... module_configs=_MGMT_MODULE_CONFIGS, fixed_vulns_df=..., pdf_chrome=...) forwarding trend_snapshots so the seven modules' compute() receive it (Pitfall 4), call composer.run_all() then composer.run_full_pipeline(...), render/save outputs, and return the standard dict including a non-empty email_body_html, analyst_excel, email_inline_images, and a private _bundle key (matching board_summary, used by the smoke script).
    (c) Add privacy_label="Confidential", scope_subtitle=None, report_title=None, analyst_detail=True kwargs to run_report() signature and wire them into PdfChromeConfig.
    (d) Replace the end-of-run _save_trend_snapshot(...) call with capture_snapshot(...) (forward write to the S1 store) — and DELETE _save_trend_snapshot, _load_trend_history, _trend_file_path, _sanitise_tag_for_filename, _compute_metric_1.._7, compute_all_metrics, _build_age_bar_chart, _build_trend_line_chart, _build_pdf, build_email_kpi_tiles, build_email_body, and the now-orphaned _PDF_CSS/_AGE_BUCKETS/_OPEN_STATES constants in the SAME edit. (_email_preview_html may be kept or removed — no functional dependency.)
    (e) Update the __main__ block to call the new pipeline.
    (f) In run_all.py: add "management_summary" to _CHROME_AWARE_SLUGS (L95) and remove the CHROME-COMPAT-01 exclusion comment (L93-94).
    Honor fail-soft (a module render error must not kill the batch) and pandas CoW (.assign() only). Do NOT touch the seven module files (they self-register; no run_all re-registration needed).
  </action>
  <verify>
    <automated>cd "D:/Projects/vuln-reporting" && python -m pytest tests/test_management_summary.py tests/test_phase6_run_group_chrome.py -x 2>&1 | tail -25 && python run_all.py --dry-run 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_management_summary.py + tests/test_phase6_run_group_chrome.py pass (pytest exits 0)
    - `python run_all.py --dry-run` validates all groups with no regression (exits 0)
    - reports/management_summary.py contains "ReportComposer", "run_full_pipeline", "read_trend", "_MGMT_MODULE_CONFIGS"; assert NOT hasattr for all listed bespoke functions
    - run_all.py _CHROME_AWARE_SLUGS includes "management_summary"; CHROME-COMPAT-01 exclusion comment removed
    - run_report return dict has non-empty email_body_html and a _bundle key
    - No `management_summary_*.json` write path remains (grep shows no _trend_file_path / _save_trend_snapshot references); only capture_snapshot writes forward
    - `python -W error::FutureWarning -m pytest tests/test_management_summary.py` raises no ChainedAssignment FutureWarning
  </acceptance_criteria>
  <done>management_summary rides ReportComposer with seven modules, chrome-aware, modular email, trend-forwarded; bespoke path removed atomically; dry-run green.</done>
</task>

<task type="auto">
  <name>Task 3: Post-cutover smoke parity + archive legacy JSON</name>
  <read_first>
    - scripts/smoke_management_summary_cutover.py (Plan 01 — switch its capture source to result["_bundle"] now that the composer path exists, keeping the SAME structural keys it captured from the bespoke path)
    - tests/baselines/management_summary_structural_baseline.json (the Plan 01 baseline the migrated output must match — section count, RAG cell count, module presence)
    - reports/board_summary.py (_bundle key shape the smoke script reads)
    - data/trend/ (locate management_summary_*.json legacy files to move; confirm data/trend/ is gitignored so legacy_archive/ inherits the ignore)
    - .gitignore (confirm data/trend/ rule covers data/trend/legacy_archive/)
  </read_first>
  <action>
    Update scripts/smoke_management_summary_cutover.py to capture structure from result["_bundle"] (the composer pipeline output) now that the migrated run_report returns it — keeping the SAME structural keys as the Plan 01 baseline so the diff is apples-to-apples (D-04-05: structure locked, values not). Run the smoke script against the migrated output and confirm it passes against the committed baseline (section count, RAG cell count, module presence match; structural drift exits 1). Then archive the legacy trend JSON (D-18-11): create data/trend/legacy_archive/ and MOVE any data/trend/management_summary_*.json into it (move, never delete — preserved for auditor cross-check). Confirm no live code reads the archived files (the bespoke readers were deleted in Task 2). This archival may be the same commit as the cutover or an immediate follow-up commit.
  </action>
  <verify>
    <automated>cd "D:/Projects/vuln-reporting" && python scripts/smoke_management_summary_cutover.py; echo "smoke-exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `python scripts/smoke_management_summary_cutover.py` exits 0 against the migrated output (structural parity with the Plan 01 baseline — section/RAG/module-presence match, D-04-05/QUAL-04)
    - The smoke script now reads result["_bundle"] (grep "_bundle" matches in the script)
    - data/trend/legacy_archive/ exists and contains the moved management_summary_*.json (no management_summary_*.json remains in data/trend/ root)
    - No production code imports/reads management_summary_*.json (grep across reports/, data/, run_all.py shows no reference)
    - data/trend/legacy_archive/ is covered by the data/trend/ gitignore rule (git check-ignore returns 0 for a file inside it)
  </acceptance_criteria>
  <done>Migrated output is structurally identical to the bespoke baseline; legacy JSON archived not deleted; no dual-writer remains.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Operator visual UAT of the migrated PDF + email</name>
  <files>reports/management_summary.py</files>
  <action>Operator visually verifies the migrated management_summary PDF shows all seven metric sections, the chrome header/footer, real-trend (non-cold-start) MoM panels, and a modular email body, with no regression to an existing delivery_config.yaml group (roadmap criterion 4). Executor cannot eyeball a rendered PDF/email.</action>
  <what-built>
    management_summary fully migrated onto ReportComposer.run_full_pipeline() with
    all seven metric modules, chrome-aware PDF (header/footer band), modular email
    body, and 12mo reconstructed trend wired through read_trend(). The bespoke
    ~2,200-line render path is removed; the structural smoke baseline passes; legacy
    JSON is archived. Roadmap criterion 4 requires operator visual UAT confirming all
    seven metric sections are present in the migrated PDF before the bespoke path is
    declared removed — the executor cannot eyeball a rendered PDF.
  </what-built>
  <how-to-verify>
    1. Generate a real migrated management_summary PDF for a representative group:
       `python run_all.py --group "<a management_summary group>" --no-email`
       (or `python reports/management_summary.py --output-dir output/mgmt_uat`).
    2. Open the resulting management_summary.pdf and confirm ALL SEVEN metric
       sections render and are recognizable: Total Vulns by Severity, Scan Coverage
       SLA, MTTR (rolling-30, window disclosed), Patch Compliance Rate, Aged
       Vulns/Assets, Accepted & Recast, New vs Remediated.
    3. Confirm the PDF has the chrome header/footer band (matching board_summary
       chrome) — i.e. management_summary is now chrome-aware.
    4. Confirm the MoM panels (MTTR trend, New vs Remediated, Accepted & Recast)
       show real trend history, NOT a cold-start "Trend data being established"
       notice (because Plan 03 seeded 12mo). Vulnerability-Density-style asset_count
       fields legitimately remain cold-start on reconstructed months (D-18-04) — that
       is expected, not a defect.
    5. Generate the email body (or --no-email preview) and confirm it renders via the
       modular path (per-module panels), not the legacy KPI-tile shell.
    6. Confirm an existing delivery_config.yaml group referencing management_summary
       runs with NO YAML changes (QUAL-04 no regression).
  </how-to-verify>
  <resume-signal>Type "approved" once all seven sections render in the chrome-aware PDF, MoM panels show real trend, and the modular email renders correctly — or describe what is missing/regressed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| read_trend()/modules → email HTML render | Module-supplied content crosses into HTML delivered to Outlook/Gmail/Apple Mail |
| run_all chrome injection → run_report kwargs | Group config values cross into the report's PDF chrome |
| migrated report → existing recipient groups | Behavior change must not regress live delivery |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-10 | Tampering / XSS-style injection | modular email body HTML (build_email_body_modular) | mitigate | Modules emit aggregate counts/labels only via the proven four-channel render contract used by board_summary; inline CSS only, no unescaped row-level data injected; reuses the existing Outlook/Gmail/Apple-Mail-safe panel pattern (CLAUDE.md email constraint) |
| T-18-11 | Denial of Service (batch abort) | migrated run_report in the fail-soft batch | mitigate | Fail-soft semantics preserved — a module render error must not kill the batch (CLAUDE.md); empty-data guard (safe_pct/safe_int/safe_format) on zero-row inputs; tested via the all_assets + tag-filtered edge case |
| T-18-12 | Tampering (dual-writer corruption) | trend store during partial cutover | mitigate | Atomic single-commit removal (QUAL-04, Pitfall 5): _save_trend_snapshot deleted in the SAME commit read_trend()/capture_snapshot wiring lands; test_bespoke_functions_removed asserts no legacy writer remains |
| T-18-13 | Information Disclosure | TypeError leaking via chrome kwarg mismatch | mitigate | Chrome kwargs added to run_report signature in the SAME commit as the frozenset membership (Pitfall 3); test_run_report_accepts_chrome_kwargs asserts no TypeError |
| T-18-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs; existing pandas/WeasyPrint/openpyxl/matplotlib/pyTenable only; no legitimacy gate required |
</threat_model>

<verification>
- `python -m pytest tests/test_management_summary.py tests/test_phase6_run_group_chrome.py -x` exits 0
- `python run_all.py --dry-run` exits 0 (no group regression)
- `python scripts/smoke_management_summary_cutover.py` exits 0 (structural parity)
- Operator visual UAT confirms seven sections + chrome + real-trend MoM panels + modular email
- No `management_summary_*.json` write path remains; legacy JSON archived
</verification>

<success_criteria>
GEN-01 complete: management_summary is a thin chrome-aware composed report on
ReportComposer with seven modules, modular email, and reconstructed trend; the
bespoke path is atomically removed with no dual-writer window; structural smoke
parity and operator UAT pass; existing delivery unregressed. D-18-10 gate 4 GREEN.
</success_criteria>

<output>
Create `.planning/phases/18-management-summary-migration-docs/18-04-SUMMARY.md` when done
</output>
