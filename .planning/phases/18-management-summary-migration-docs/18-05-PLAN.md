---
phase: 18-management-summary-migration-docs
plan: 05
type: execute
wave: 4
depends_on: ["18-03", "18-04"]
files_modified:
  - docs/management_summary_calculations.md
autonomous: false
requirements: [DOC-02]
must_haves:
  truths:
    - "An auditor can recompute each of the seven v1.4 module metrics from the runbook alone — exact formula, real field names, data source (fetcher + columns), reopened-aware predicate, disclosed window, edge cases (DOC-02, D-18-07)"
    - "The runbook discloses the reconstructed month range (2025-06 → now) and the exact reconstruction predicate (open_findings_at + fixed add-back at month-end) (D-18-01/03, D-18-07)"
    - "The runbook discloses the per-metric reconstructed-vs-cold-start split — including that asset_count is null on reconstructed months so Vulnerability Density stays cold-start there (D-18-04)"
    - "The runbook discloses the rolling-30 MTTR window as a deliberate recent-velocity choice, not a data limit (D-18-06/07)"
    - "The runbook documents the external-scope rule (Location=External/DMZ OR public IPv4) where applicable (D-18-07)"
  artifacts:
    - path: "docs/management_summary_calculations.md"
      provides: "Extended with a v1.4 Module Metrics section: seven per-module runbooks + reconstruction disclosure + rolling-30 MTTR intent + external-scope rule"
      contains: "reconstructed"
  key_links:
    - from: "docs/management_summary_calculations.md"
      to: "the seven module compute() formulas + reconstruction predicate"
      via: "auditor-reproducible per-module sections matching the existing *_calculations.md convention"
      pattern: "Data Source"
---

<objective>
Write the DOC-02 auditor-facing calculation runbooks for all seven v1.4 modules
that `management_summary` now composes, following the existing
`docs/*_calculations.md` convention (D-18-07). Each module section gives an
auditor enough to recompute the value from the doc alone: exact formula with real
field names, data source (which fetcher, which columns), the reopened-aware
predicate, disclosed window, cold-start branch behavior, empty-data guard
behavior. Plus the mandatory honesty disclosures that make the reconstructed
history auditable: the reconstructed month range + predicate (D-18-01/03), the
per-metric reconstructed-vs-cold-start split with null asset_count (D-18-04), the
rolling-30 MTTR intent (D-18-06), and the external-scope rule.

This runs LAST (D-18-10 gate 5) so the runbook documents the FINAL cutover
semantics and the actual reconstruction predicate as implemented.

Purpose: DOC-02 is fundamentally about auditor trust — the runbook is where the
audit-honesty guardrails (provenance, null denominators, overlap validation,
disclosed windows) become legible to a reviewer.
Output: extended docs/management_summary_calculations.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-management-summary-migration-docs/18-CONTEXT.md
@.planning/phases/18-management-summary-migration-docs/18-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the calculations runbook with seven per-module sections + mandatory disclosures</name>
  <read_first>
    - docs/management_summary_calculations.md (the EXISTING file + convention to extend: header [file/audience/outputs/schedule], TOC, per-metric sections [Calculation / Data Source / Thresholds-Colors / Edge Cases], reference tables, PDF Structure + Email Structure, Trend Data Store, Troubleshooting — match this structure exactly)
    - docs/board_summary_calculations.md (the second reference for the per-module-reproducible bar D-18-07 extends)
    - reports/modules/total_vulns_by_severity_module.py (formula + fields for the Total Vulns runbook section)
    - reports/modules/scan_coverage_sla_module.py (Scan Coverage formula/fields)
    - reports/modules/mttr_trend_module.py (MTTR: sample-weighted mean, reopened-aware clock, the rolling-30 window to disclose as deliberate — D-18-06)
    - reports/modules/patch_compliance_rate_module.py (Patch Compliance formula/fields)
    - reports/modules/aged_vulns_assets_module.py (Aged Vulns/Assets formula/fields)
    - reports/modules/accepted_recast_module.py (Accepted vs Recast separate-counts + expiry-aware formula/fields)
    - reports/modules/new_vs_remediated_module.py (inflow/outflow MoM formula/fields; the per-snapshot basis)
    - reports/modules/external_dmz_module.py (the external-scope rule disclosure — Location=External/DMZ OR public IPv4); reports/modules/vuln_density_module.py if present (the own-snapshot-asset_count rule central to the D-18-04 reconstructed-vs-cold-start split — even if vuln_density is not one of the seven composed mgmt modules, document the external-scope rule and the asset_count-null cold-start consequence per D-18-07)
    - scripts/backfill_trend_reconstruction.py (Plan 03 — the AS-IMPLEMENTED reconstruction predicate, window 2025-06→now, partial-flag months, asset_count=null, overlap tolerance — document exactly what shipped, not the proposal)
    - reports/management_summary.py (Plan 04 — the AS-MIGRATED seven-module composition + read_trend wiring, to keep the runbook faithful to the final cutover)
    - utils/open_count.py (the reopened-aware two-interval predicate to state precisely)
    - config.py SLA_DAYS (authoritative SLA values — Medium=60; do NOT cite the stale CLAUDE.md table; memory project_sla_days_config_py_authoritative)
  </read_first>
  <action>
    Extend docs/management_summary_calculations.md with a new "v1.4 Module Metrics" section (single-file extension per Claude's-discretion grouping decision D-18-07 / RESEARCH A4 — avoids doc fragmentation while meeting the per-module-reproducible bar). For EACH of the seven modules (total_vulns_by_severity, scan_coverage_sla, mttr_trend, patch_compliance_rate, aged_vulns_assets, accepted_recast, new_vs_remediated) write a subsection with: exact Calculation formula using REAL field names, Data Source (fetcher fn + the columns consumed), the reopened-aware predicate where open-counts are involved (open_findings_at two-interval form — QUAL-02), disclosed window, cold-start branch behavior, empty-data guard behavior (safe_pct/safe_int/safe_format, _empty_result). Then add the MANDATORY disclosure subsections: (1) Reconstruction Disclosure — the month range (2025-06 → now), the exact predicate as implemented in scripts/backfill_trend_reconstruction.py (current-open rows + fixed rows where last_fixed > month_end_D, then open_findings_at at month_end), the provenance markers (source=reconstructed vs captured, partial flag on Jun–Aug 2025), the overlap-test tolerance used, and asset_count=null on reconstructed months; (2) Per-metric reconstructed-vs-cold-start split — a table stating which metrics carry 12mo reconstructed history vs which cold-start on reconstructed months (asset_count-dependent / Vulnerability-Density-style metrics stay cold-start — D-18-04); (3) Rolling-30 MTTR intent — explicitly a "recent velocity" window, NOT a data limit (D-18-06); (4) External-scope rule — Location=External/DMZ tag OR public IPv4 (non-RFC1918 / non-CGNAT / non-loopback / non-link-local). Cite SLA values from config.py SLA_DAYS (authoritative), not the stale CLAUDE.md table. No row-level / PII examples in the doc (QUAL-05).
  </action>
  <verify>
    <automated>cd "D:/Projects/vuln-reporting" && python -c "t=open('docs/management_summary_calculations.md',encoding='utf-8').read(); req=['total_vulns_by_severity','scan_coverage_sla','mttr_trend','patch_compliance_rate','aged_vulns_assets','accepted_recast','new_vs_remediated','reconstructed','rolling','open_findings_at']; missing=[k for k in req if k not in t]; print('MISSING:',missing); assert not missing, missing"</automated>
  </verify>
  <acceptance_criteria>
    - docs/management_summary_calculations.md contains a per-module subsection for each of the seven module IDs (all seven IDs appear in the file)
    - Each module subsection has Calculation, Data Source, and Edge Cases content (the existing convention's headings appear under each)
    - The file contains a Reconstruction Disclosure section naming the 2025-06→now window, the open_findings_at-based predicate, source/partial provenance markers, and asset_count=null on reconstructed months
    - The file contains a per-metric reconstructed-vs-cold-start split table
    - The rolling-30 MTTR window is documented as deliberate "recent velocity", not a data limit
    - The external-scope rule (Location=External/DMZ OR public IPv4) is documented
    - SLA values cited match config.py SLA_DAYS (Medium=60), not the stale CLAUDE.md table
    - The automated verify (grep of required tokens) exits 0
  </acceptance_criteria>
  <done>The runbook lets an auditor recompute each of the seven metrics and discloses the reconstruction predicate, the reconstructed-vs-cold-start split, the rolling-30 MTTR intent, and the external-scope rule.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator runbook reproducibility + disclosure-accuracy review</name>
  <files>docs/management_summary_calculations.md</files>
  <action>Operator reviews the extended runbook to confirm an auditor could recompute each of the seven module metrics from the doc alone, and that the reconstruction predicate/range, reconstructed-vs-cold-start split, rolling-30 MTTR intent, and external-scope rule disclosures are accurate against what shipped (DOC-02). Reproducibility-by-a-third-party is a human judgment the executor cannot make.</action>
  <what-built>
    docs/management_summary_calculations.md extended with auditor-reproducible
    runbooks for all seven v1.4 modules plus the mandatory honesty disclosures
    (reconstruction predicate + range, per-metric reconstructed-vs-cold-start split,
    rolling-30 MTTR intent, external-scope rule). DOC-02 is fundamentally an
    auditor-trust deliverable — the executor cannot judge whether a third-party
    auditor could actually recompute the values from the prose alone, so an operator
    reviews for reproducibility.
  </what-built>
  <how-to-verify>
    1. Open docs/management_summary_calculations.md and pick two modules (e.g.
       mttr_trend and accepted_recast). For each, confirm you could recompute the
       headline number from the doc alone: the formula, the exact field names, the
       fetcher/columns, and the window are all stated.
    2. Confirm the Reconstruction Disclosure section honestly states which months are
       reconstructed (Jun 2025 → now), that Jun–Aug 2025 are partial/approximate, that
       the predicate is the reopened-aware open_findings_at form, and that asset_count
       is null (so Density cold-starts) on reconstructed months.
    3. Confirm the reconstructed-vs-cold-start split table is correct against what
       Plan 03 actually seeded and Plan 04 actually wired.
    4. Confirm the rolling-30 MTTR window is described as a deliberate choice, not a
       data limitation.
    5. Confirm no real hostnames/IPs/plugin names appear as examples (QUAL-05).
  </how-to-verify>
  <resume-signal>Type "approved" once the runbook is auditor-reproducible and the honesty disclosures are accurate, or list what is unclear/inaccurate.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runbook prose → committed git artifact | Documentation crosses into a permanent shareable file |
| disclosed predicate → auditor trust | The doc is the basis on which a reviewer accepts the reconstructed history |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-14 | Information Disclosure | docs/management_summary_calculations.md examples | mitigate | Aggregate formulas and field NAMES only — no real hostnames/IPs/plugin names as worked examples (QUAL-05/D-04-08); acceptance + checkpoint both confirm no PII examples |
| T-18-15 | Repudiation (misleading audit doc) | reconstruction/MTTR disclosure accuracy | mitigate | Runbook is written from the AS-IMPLEMENTED Plan 03 script and Plan 04 cutover (read_first), not the proposal; operator checkpoint verifies reproducibility and disclosure accuracy against what shipped |
| T-18-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs; docs-only change; no legitimacy gate required |
</threat_model>

<verification>
- The grep-token automated check exits 0 (all seven module IDs + reconstruction/rolling/predicate tokens present)
- Operator confirms auditor-reproducibility and disclosure accuracy (checkpoint)
- No PII examples in the doc
</verification>

<success_criteria>
DOC-02 complete: an auditor can reproduce each of the seven module metrics from
the runbook, and the reconstruction predicate, reconstructed-vs-cold-start split,
rolling-30 MTTR intent, and external-scope rule are all disclosed. D-18-10 gate 5
GREEN — phase complete.
</success_criteria>

<output>
Create `.planning/phases/18-management-summary-migration-docs/18-05-SUMMARY.md` when done
</output>
