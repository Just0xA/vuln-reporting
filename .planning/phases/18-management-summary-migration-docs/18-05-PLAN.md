---
phase: 18-management-summary-migration-docs
plan: 05
type: execute
wave: 4
depends_on: ["18-03", "18-04"]
files_modified:
  - docs/management_summary_calculations.md
autonomous: false
requirements: [DOC-02]
must_haves:
  truths:
    - "An auditor can recompute each of the seven v1.4 module metrics RENDERED in management_summary from the runbook alone — exact formula, real field names, data source (fetcher + columns), reopened-aware predicate, disclosed window, edge cases (DOC-02, D-18-07)"
    - "The runbook clearly SEPARATES 'management_summary's seven rendered modules' from 'related v1.4 disclosures NOT rendered in the composed report' (external exposure, vulnerability density) so auditors are not confused about why those appear in the management_summary runbook (review LOW change #13)"
    - "The runbook discloses the reconstructed month range (2025-06 → now) and the exact reconstruction predicate (open_findings_at + fixed add-back at month_end_utc) and states the reconstruction is ALL-ASSETS-ONLY — tag-scoped delivery groups cold-start their MoM history (pre-existing, acceptable, not a regression) (D-18-01/03, D-18-07, review change #4)"
    - "The runbook discloses the per-metric reconstructed-vs-cold-start split — including that asset_count is null on reconstructed months so Vulnerability Density stays cold-start there (D-18-04)"
    - "The runbook discloses the PER-METRIC parity-bucket outcome: M5 (aged_vulns_assets) and M7 (new_vs_remediated) are INTENTIONAL metric-design changes vs the old bespoke metrics (M5: age-bucket vuln histogram → % aged on-time-scanned assets; M7: simple MoM total-open delta → inflow/outflow trend) — documented as intentional changes, NOT regressions; the other five metrics are numerically parity-locked against the bespoke path (review HIGH change #1)"
    - "The runbook EXPLICITLY discloses the reopened temporal-paradox limitation: Tenable exports retain only most-recent state-transition timestamps, so a fix→reopen→fix cycle's intermediate months cannot be perfectly reconstructed; reconstructed months are a best-effort approximation BOUNDED by the D-18-09 overlap-test tolerance and computed with the validated two-interval open_findings_at predicate (review HIGH change #2, QUAL-02)"
    - "The runbook discloses the rolling-30 MTTR window as a deliberate recent-velocity choice, not a data limit (D-18-06/07)"
    - "The runbook documents the external-scope rule (Location=External/DMZ OR public IPv4) where applicable (D-18-07)"
  artifacts:
    - path: "docs/management_summary_calculations.md"
      provides: "Extended with a v1.4 Module Metrics section: seven per-RENDERED-module runbooks + a separate 'related disclosures not rendered' subsection + reconstruction disclosure (all-assets-only, tag-scoped cold-start, reopened temporal-paradox) + per-metric parity-bucket outcome (M5/M7 intentional changes) + rolling-30 MTTR intent + external-scope rule"
      contains: "reconstructed"
  key_links:
    - from: "docs/management_summary_calculations.md"
      to: "the seven rendered module compute() formulas + reconstruction predicate"
      via: "auditor-reproducible per-module sections matching the existing *_calculations.md convention"
      pattern: "Data Source"
---

<objective>
Write the DOC-02 auditor-facing calculation runbooks for all seven v1.4 modules
that `management_summary` now RENDERS, following the existing
`docs/*_calculations.md` convention (D-18-07). Each module section gives an
auditor enough to recompute the value from the doc alone: exact formula with real
field names, data source (which fetcher, which columns), the reopened-aware
predicate, disclosed window, cold-start branch behavior, empty-data guard
behavior. Plus the mandatory honesty disclosures that make the reconstructed
history auditable.

SCOPING CLARITY (review LOW change #13): the runbook must SEPARATE two things —
(1) the seven modules actually RENDERED in management_summary, and (2) related
v1.4 disclosures that are NOT rendered in the composed report (external exposure,
vulnerability density) but are documented for completeness/cross-reference. Label
them distinctly so an auditor is not confused about why external exposure and
density appear in a management_summary runbook when they are not in the composed
PDF.

PARITY-BUCKET DISCLOSURE (review HIGH change #1): the runbook must disclose the
per-metric migration parity outcome. Five metrics (total_vulns_by_severity,
scan_coverage_sla, mttr_trend rolling-30, patch_compliance_rate, accepted_recast
current-period) are numerically parity-locked against the bespoke path. Two metrics
are INTENTIONAL metric-design changes, NOT regressions: M5 aged_vulns_assets
(bespoke age-bucket vuln histogram → module % of aged on-time-scanned assets) and
M7 new_vs_remediated (bespoke simple MoM total-open delta → module inflow/outflow
trend with net-delta). State these as intentional design changes the milestone made.

Mandatory disclosures also include: the reconstructed month range + predicate
(D-18-01/03), the ALL-ASSETS-ONLY reconstruction scope + tag-scoped cold-start
(review change #4), the reopened temporal-paradox limitation (review change #2),
the per-metric reconstructed-vs-cold-start split with null asset_count (D-18-04),
the rolling-30 MTTR intent (D-18-06), and the external-scope rule.

This runs LAST (D-18-10 gate 5) so the runbook documents the FINAL cutover
semantics and the actual reconstruction predicate as implemented.

Purpose: DOC-02 is fundamentally about auditor trust — the runbook is where the
audit-honesty guardrails (provenance, null denominators, overlap validation,
disclosed windows, intentional metric-design changes, and the honest limits of
historical reconstruction) become legible to a reviewer.
Output: extended docs/management_summary_calculations.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-management-summary-migration-docs/18-CONTEXT.md
@.planning/phases/18-management-summary-migration-docs/18-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the calculations runbook — seven RENDERED-module sections, separated related disclosures, parity-bucket outcome, + mandatory disclosures (incl. reopened temporal-paradox + all-assets-only scope)</name>
  <read_first>
    - docs/management_summary_calculations.md (the EXISTING file + convention to extend: header [file/audience/outputs/schedule], TOC, per-metric sections [Calculation / Data Source / Thresholds-Colors / Edge Cases], reference tables, PDF Structure + Email Structure, Trend Data Store, Troubleshooting — match this structure exactly)
    - docs/board_summary_calculations.md (the second reference for the per-module-reproducible bar D-18-07 extends)
    - reports/modules/total_vulns_by_severity_module.py (formula + fields for the Total Vulns runbook section — EXACT-MATCH parity bucket)
    - reports/modules/scan_coverage_sla_module.py (Scan Coverage formula/fields — EXACT-MATCH)
    - reports/modules/mttr_trend_module.py (MTTR: sample-weighted mean, reopened-aware clock, the rolling-30 window to disclose as deliberate — D-18-06; EXACT-MATCH on the rolling-30 value)
    - reports/modules/patch_compliance_rate_module.py (Patch Compliance formula/fields — EXACT-MATCH)
    - reports/modules/aged_vulns_assets_module.py (Aged Vulns/Assets: % of on-time-scanned ASSETS with a >90d Med/High/Crit finding — the M5 INTENTIONAL change vs the bespoke vuln age-bucket histogram; DOCUMENTED-DIFFERENCE bucket)
    - reports/modules/accepted_recast_module.py (Accepted vs Recast separate-counts + expiry-aware formula/fields — EXACT-MATCH on current-period counts/rate)
    - reports/modules/new_vs_remediated_module.py (inflow/outflow MoM formula/fields; outflow from snapshot fixed_findings_count — the M7 INTENTIONAL change vs the bespoke simple total-open delta; DOCUMENTED-DIFFERENCE bucket)
    - reports/modules/external_dmz_module.py (the external-scope rule disclosure — Location=External/DMZ OR public IPv4) and reports/modules/vuln_density_module.py if present (the own-snapshot-asset_count rule central to D-18-04). These are RELATED DISCLOSURES NOT rendered in management_summary's seven — document them in the SEPARATE 'related disclosures' subsection (review change #13)
    - tests/baselines/management_summary_value_golden.json (Plan 01 — the per-metric bucket assignment exact_match vs documented_difference; the runbook's parity-bucket disclosure must match what the golden classified)
    - scripts/backfill_trend_reconstruction.py (Plan 03 — the AS-IMPLEMENTED reconstruction predicate, ALL-ASSETS scope, window 2025-06→now, month_end_utc boundary, partial-flag months, asset_count=null, overlap tolerance — document exactly what shipped, not the proposal)
    - reports/management_summary.py (Plan 04 — the AS-MIGRATED seven-module composition + read_trend('severity','all_assets') wiring, to keep the runbook faithful to the final cutover)
    - utils/open_count.py (the reopened-aware two-interval predicate to state precisely — and the basis for honestly disclosing the temporal-paradox limit)
    - .claude/skills/spike-findings-vuln-reporting/SKILL.md + .planning/spikes/002-trend-reconstruction-lookback/README.md (the validated reopened-aware predicate and the overlap benchmark that bounds the approximation — the source for the limitation disclosure)
    - config.py SLA_DAYS (authoritative SLA values — Medium=60; do NOT cite the stale CLAUDE.md table; memory project_sla_days_config_py_authoritative)
  </read_first>
  <action>
    Extend docs/management_summary_calculations.md with a new "v1.4 Module Metrics" section (single-file extension per Claude's-discretion grouping decision D-18-07 / RESEARCH A4). Structure it in two clearly-labeled groups (review change #13):
    GROUP A — "Modules Rendered in management_summary (the seven)": for EACH of the seven RENDERED modules (total_vulns_by_severity, scan_coverage_sla, mttr_trend, patch_compliance_rate, aged_vulns_assets, accepted_recast, new_vs_remediated) write a subsection with: exact Calculation formula using REAL field names, Data Source (fetcher fn + columns consumed), the reopened-aware predicate where open-counts are involved (open_findings_at two-interval form — QUAL-02), disclosed window, cold-start branch behavior, empty-data guard behavior (safe_pct/safe_int/safe_format, _empty_result).
    GROUP B — "Related v1.4 Disclosures NOT Rendered in management_summary": a clearly-separated subsection documenting external exposure (external_dmz / external-scope rule) and vulnerability density (own-snapshot asset_count rule) — explicitly stating these are documented for cross-reference/completeness and are NOT part of the seven-module composed report, so an auditor knows why they appear here (review change #13).
    Then add the MANDATORY disclosure subsections:
    (1) Reconstruction Disclosure — the month range (2025-06 → now), ALL-ASSETS-ONLY scope (review change #4: tag-scoped delivery groups cold-start their MoM history — pre-existing, acceptable, NOT a regression; note no active delivery group is tag-scoped on management_summary), the exact predicate as implemented in scripts/backfill_trend_reconstruction.py (current-open rows + fixed rows where last_fixed > month_end_utc(month), then open_findings_at at month_end_utc), the provenance markers (source=reconstructed vs captured, partial flag on Jun–Aug 2025, reconstructed months immutable incl. current), the overlap-test tolerance used, and asset_count=null on reconstructed months.
    (1b) Reopened Temporal-Paradox Limitation (review change #2 — MANDATORY) — explicitly disclose that Tenable vuln exports retain only the MOST-RECENT state-transition timestamps (last_fixed, last_found, resurfaced), so a finding that went fix→reopen→fix within the reconstructed window cannot have its intermediate months reproduced perfectly; state reconstructed months are a BEST-EFFORT APPROXIMATION, that the residual error is BOUNDED by the D-18-09 overlap-test tolerance (cite the actual tolerance and the spike's +2-of-160,453 benchmark), and that the validated two-interval open_findings_at predicate minimizes (but cannot fully eliminate) churned-finding error — captured months carry no such caveat. Do NOT imply perfect historical fidelity.
    (1c) Migration Parity-Bucket Outcome (review change #1 — MANDATORY) — a table/list disclosing which metrics were numerically parity-locked against the bespoke path (the five EXACT-MATCH: total_vulns_by_severity, scan_coverage_sla, mttr_trend rolling-30, patch_compliance_rate, accepted_recast current-period) and which were INTENTIONALLY changed in the migration (M5 aged_vulns_assets: bespoke age-bucket vuln histogram → % aged on-time-scanned assets; M7 new_vs_remediated: bespoke simple MoM total-open delta → inflow/outflow trend). State M5/M7 as intentional metric-DESIGN changes, NOT regressions — matching the bucket assignment in the Plan 01 golden.
    (2) Per-metric reconstructed-vs-cold-start split — a table stating which metrics carry 12mo reconstructed all-assets history vs which cold-start on reconstructed months (asset_count-dependent / Vulnerability-Density-style metrics stay cold-start — D-18-04).
    (3) Rolling-30 MTTR intent — explicitly a "recent velocity" window, NOT a data limit (D-18-06).
    (4) External-scope rule — Location=External/DMZ tag OR public IPv4 (non-RFC1918 / non-CGNAT / non-loopback / non-link-local).
    Cite SLA values from config.py SLA_DAYS (authoritative), not the stale CLAUDE.md table. No row-level / PII examples in the doc (QUAL-05).
  </action>
  <verify>
    <automated>cd "D:/Projects/vuln-reporting" && python -c "t=open('docs/management_summary_calculations.md',encoding='utf-8').read().lower(); req=['total_vulns_by_severity','scan_coverage_sla','mttr_trend','patch_compliance_rate','aged_vulns_assets','accepted_recast','new_vs_remediated','reconstructed','rolling','open_findings_at','best-effort','reopen','all_assets','intentional','not rendered']; missing=[k for k in req if k not in t]; print('MISSING:',missing); assert not missing, missing"</automated>
  </verify>
  <acceptance_criteria>
    - docs/management_summary_calculations.md contains a per-module subsection for each of the seven RENDERED module IDs in a clearly-labeled "rendered in management_summary" group
    - The file contains a clearly-separated "related v1.4 disclosures NOT rendered in management_summary" subsection covering external exposure and vulnerability density (review change #13) — distinct from the seven rendered modules
    - Each rendered-module subsection has Calculation, Data Source, and Edge Cases content
    - The file contains a Reconstruction Disclosure section naming the 2025-06→now window, the ALL-ASSETS-ONLY scope + tag-scoped cold-start note (review change #4), the open_findings_at/month_end_utc-based predicate, source/partial provenance markers, and asset_count=null on reconstructed months
    - The file contains a Reopened Temporal-Paradox Limitation disclosure (tokens "best-effort" and "reopen" present): exports retain only most-recent state transitions, fix→reopen→fix intermediate months are not perfectly reconstructable, best-effort bounded by the overlap tolerance, two-interval predicate minimizes but cannot eliminate the error
    - The file contains a Migration Parity-Bucket Outcome disclosure naming the five parity-locked metrics and the two INTENTIONALLY-changed metrics (M5 aged_vulns_assets, M7 new_vs_remediated) as design changes not regressions (token "intentional" present) — matching the Plan 01 golden bucket assignment
    - The file contains a per-metric reconstructed-vs-cold-start split table
    - The rolling-30 MTTR window is documented as deliberate "recent velocity", not a data limit
    - The external-scope rule (Location=External/DMZ OR public IPv4) is documented
    - SLA values cited match config.py SLA_DAYS (Medium=60), not the stale CLAUDE.md table
    - The automated verify (token grep incl. all_assets / intentional / not rendered) exits 0
  </acceptance_criteria>
  <done>The runbook lets an auditor recompute each of the seven rendered metrics, clearly separates the rendered seven from related non-rendered disclosures, discloses the all-assets-only reconstruction + tag-scoped cold-start, the reopened temporal-paradox limitation, the per-metric parity-bucket outcome (M5/M7 intentional), the reconstructed-vs-cold-start split, the rolling-30 MTTR intent, and the external-scope rule.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator runbook reproducibility + disclosure-accuracy review</name>
  <files>docs/management_summary_calculations.md</files>
  <action>Operator reviews the extended runbook to confirm an auditor could recompute each of the seven RENDERED module metrics from the doc alone, that the rendered-seven vs related-not-rendered separation is clear, and that the reconstruction predicate/range/all-assets-scope, the reopened temporal-paradox limitation, the parity-bucket outcome (M5/M7 intentional), the reconstructed-vs-cold-start split, rolling-30 MTTR intent, and external-scope rule disclosures are accurate against what shipped (DOC-02). Reproducibility-by-a-third-party and honest-limitation framing are human judgments the executor cannot make.</action>
  <what-built>
    docs/management_summary_calculations.md extended with auditor-reproducible
    runbooks for all seven RENDERED v1.4 modules (clearly separated from related
    non-rendered disclosures — external exposure, density) plus the mandatory honesty
    disclosures: reconstruction predicate + range + all-assets-only scope (tag-scoped
    cold-start noted), the reopened temporal-paradox limitation, the migration
    parity-bucket outcome (five parity-locked, M5/M7 intentional design changes),
    per-metric reconstructed-vs-cold-start split, rolling-30 MTTR intent, external-scope
    rule. DOC-02 is fundamentally an auditor-trust deliverable — the executor cannot
    judge whether a third-party auditor could actually recompute the values from the
    prose alone or whether the limitation/intentional-change framing is honest enough,
    so an operator reviews for reproducibility and disclosure accuracy.
  </what-built>
  <how-to-verify>
    1. Open docs/management_summary_calculations.md and pick two RENDERED modules (e.g.
       mttr_trend and accepted_recast). For each, confirm you could recompute the
       headline number from the doc alone: the formula, exact field names, the
       fetcher/columns, and the window are all stated.
    2. Confirm the rendered-seven group is clearly separated from the "related
       disclosures NOT rendered" group (external exposure, density) so it is obvious why
       the latter appear in the doc but not in the composed PDF.
    3. Confirm the Reconstruction Disclosure honestly states the months reconstructed
       (Jun 2025 → now), ALL-ASSETS-ONLY scope (tag-scoped groups cold-start), that
       Jun–Aug 2025 are partial/approximate, the reopened-aware open_findings_at +
       month_end_utc predicate, and asset_count=null (so Density cold-starts) on
       reconstructed months.
    4. Confirm the Reopened Temporal-Paradox Limitation disclosure is present and honest
       (most-recent transitions only, fix→reopen→fix best-effort, bounded by tolerance,
       captured months carry no caveat) — and does NOT overclaim perfect fidelity.
    5. Confirm the Migration Parity-Bucket Outcome states M5/M7 as intentional design
       changes (not regressions) and names the five parity-locked metrics.
    6. Confirm the reconstructed-vs-cold-start split table is correct against what
       Plan 03 actually seeded and Plan 04 actually wired.
    7. Confirm the rolling-30 MTTR window is described as a deliberate choice, not a
       data limitation, and no real hostnames/IPs/plugin names appear as examples (QUAL-05).
  </how-to-verify>
  <resume-signal>Type "approved" once the runbook is auditor-reproducible, the rendered/non-rendered separation is clear, and the honesty disclosures (reopened temporal-paradox, all-assets scope, M5/M7 intentional changes) are accurate — or list what is unclear/inaccurate.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runbook prose → committed git artifact | Documentation crosses into a permanent shareable file |
| disclosed predicate + limitation → auditor trust | The doc is the basis on which a reviewer accepts the reconstructed history |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-14 | Information Disclosure | docs/management_summary_calculations.md examples | mitigate | Aggregate formulas and field NAMES only — no real hostnames/IPs/plugin names as worked examples (QUAL-05/D-04-08); acceptance + checkpoint both confirm no PII examples |
| T-18-15 | Repudiation (misleading audit doc) | reconstruction/MTTR disclosure accuracy | mitigate | Runbook is written from the AS-IMPLEMENTED Plan 03 script and Plan 04 cutover (read_first), not the proposal; operator checkpoint verifies reproducibility and disclosure accuracy against what shipped |
| T-18-15b | Repudiation (overclaimed historical fidelity) | reopened temporal-paradox limitation disclosure | mitigate | MANDATORY disclosure (review change #2) states reconstructed months are best-effort, bounded by the overlap tolerance, fix→reopen→fix intermediate months not perfectly reproducible — preventing an auditor from trusting churned-finding history as exact |
| T-18-15c | Repudiation (parity overclaim / change mistaken for regression) | migration parity-bucket disclosure | mitigate | The runbook discloses M5/M7 as INTENTIONAL design changes matching the Plan 01 golden bucket assignment, and names the five parity-locked metrics — preventing an auditor from reading the intentional metric changes as silent regressions or from over-trusting parity where it doesn't apply (review change #1) |
| T-18-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs; docs-only change; no legitimacy gate required |
</threat_model>

<verification>
- The token-grep automated check exits 0 (all seven rendered module IDs + reconstruction/rolling/predicate/best-effort/reopen/all_assets/intentional/not-rendered tokens present)
- Operator confirms auditor-reproducibility, rendered/non-rendered separation, and disclosure accuracy incl. the temporal-paradox limitation and the M5/M7 intentional-change framing (checkpoint)
- No PII examples in the doc
</verification>

<success_criteria>
DOC-02 complete: an auditor can reproduce each of the seven rendered module
metrics from the runbook; the rendered seven are clearly separated from related
non-rendered disclosures; and the all-assets-only reconstruction + tag-scoped
cold-start, the reopened temporal-paradox limitation, the per-metric parity-bucket
outcome (M5/M7 intentional), the reconstructed-vs-cold-start split, rolling-30 MTTR
intent, and external-scope rule are all disclosed honestly. D-18-10 gate 5 GREEN —
phase complete.
</success_criteria>

<output>
Create `.planning/phases/18-management-summary-migration-docs/18-05-SUMMARY.md` when done
</output>
