---
phase: 16-mttr-rework
plan: 07
type: tdd
wave: 6
depends_on: ["16-06", "16-05"]
files_modified:
  - tests/test_mttr_trend_module.py
  - tests/test_capture_trend_snapshot_cli.py
  - tests/baselines/mttr_trend_test_pull.json
  - tests/baselines/mttr_trend_test_pull_zero_match.json
autonomous: true
requirements: [RPT-05]
gap_closure: true
must_haves:
  truths:
    - "Test: the 4 per-severity MTTR gauges are present in the PDF and email headline render for ALL three table modes — owner (unfocused), application (Owner-focused), and gauges-only (Application-focused) — i.e. gauges are no longer gated to severity/both"
    - "Test: MoM arrow polarity — a per-severity MTTR DECREASE month-over-month renders ▼ green (improving), an INCREASE renders ▲ red (slipping), and flat / no-prior renders — (MTTR lower-is-better)"
    - "Test: focus routing — unfocused (no Owner= filter) → Owner table; tag_category=='Owner' + tag_value set → Application table; tag_category=='Application' + tag_value set → no detail table (gauges only)"
    - "Test: the Severity TABLE is absent from every headline channel (PDF, email, Excel) — only gauges (+ Excel's compact severity numeric block) represent severity; mttr_view is fully removed from the module"
    - "Test: Excel renders a compact 4-row severity numeric block (Severity | MTTR | SLA | Status | MoM Delta) plus the focus-driven Owner/Application table; SLA values are sourced from config.py SLA_DAYS (Medium==60, not 45)"
    - "Test: a bad mttr_table value falls back to auto (no crash) and validate_config returns an error for it"
    - "Test: owner-only and gauges-only representative renders fit one PDF page (structural pdf_page_count assertion)"
    - "Test (real CLI): `python scripts/capture_trend_snapshot.py --dry-run` runs as a subprocess from a NON-root CWD and exits 0 with no ModuleNotFoundError — the import-from-root probe that masked UAT-5 is replaced by a real-invocation regression test"
    - "board_summary zero-diff still green (D-16-10): the three board_summary structural baselines remain byte-identical and mttr_by_severity_module.py is byte-unchanged"
    - "Existing mttr_trend structural baselines regenerated for the NEW always-on-gauge / focus-driven-table default render; the self-guarding compare_snapshots(actual, baseline) == [] tests pass against the regenerated files"
    - "All fixtures synthetic (RFC 5737/6761; asset_uuid all-zero prefix) — QUAL-05"
  artifacts:
    - path: "tests/test_mttr_trend_module.py"
      provides: "Gauge-presence, MoM-arrow-polarity, focus-routing, severity-table-absent, mttr_table-fallback, single-page tests (extends the existing suite; replaces the retired 16-05 mttr_view tests)"
      contains: "mttr_table"
    - path: "tests/test_capture_trend_snapshot_cli.py"
      provides: "Real-invocation subprocess regression test for the snapshot-script sys.path bootstrap (--dry-run from a non-root CWD, exit 0, no ModuleNotFoundError)"
      contains: "capture_trend_snapshot"
    - path: "tests/baselines/mttr_trend_test_pull.json"
      provides: "Regenerated populated structural baseline reflecting the always-on-gauge / focus-driven render"
    - path: "tests/baselines/mttr_trend_test_pull_zero_match.json"
      provides: "Regenerated zero-match structural baseline (cold-start path; rewrite only if it genuinely drifts)"
  key_links:
    - from: "tests/test_mttr_trend_module.py"
      to: "MTTRTrendModule.compute / render_pdf_section / render_excel_tabs / render_email_panel"
      via: "ModuleConfig options={'mttr_table':..., 'tag_category':..., 'tag_value':...} then assert gauges/table/arrows"
      pattern: "mttr_table"
    - from: "tests/test_capture_trend_snapshot_cli.py"
      to: "scripts/capture_trend_snapshot.py"
      via: "subprocess.run([python, 'scripts/capture_trend_snapshot.py', '--dry-run'], cwd=<non-root>) returncode==0"
      pattern: "capture_trend_snapshot"
    - from: "tests/test_mttr_trend_module.py TestStructuralBaselines"
      to: "tests/baselines/mttr_trend_test_pull*.json"
      via: "compare_snapshots(actual, load_baseline(name)) == []"
      pattern: "mttr_trend_test_pull"
---

<objective>
Encode the D-16-13 locked gap-closure spec as automated tests, regenerate the `mttr_trend` structural baselines for the new always-on-gauge / focus-driven-table default render, add a real-invocation regression test for the snapshot-script bootstrap fix, and re-assert the board_summary zero-diff (D-16-10).

The existing `tests/test_mttr_trend_module.py` already covers the reopened-clock lodestar, cold-start, min_sample, Owner-drift, tie-break, partial-month, four-channel empty guard, composed-pipeline smoke, and structural baselines. The 16-05 plan added mttr_view (owner/severity/both) tests — those are now STALE (mttr_view is retired by 16-06). REPLACE the mttr_view test classes with the D-16-13 gauge/focus tests; keep everything else.

Purpose: Lock the always-on 4-gauge band, MoM arrow polarity, focus-driven table routing, severity-table removal, and the snapshot real-CLI invocation against regression; prove the byte-unchanged board_summary guarantee survives.
Output: D-16-13 test classes replacing the retired mttr_view tests, a new subprocess regression test for the snapshot script, two regenerated mttr_trend baseline JSONs (self-guarded), and a confirmed board_summary zero-diff. All fixtures synthetic (QUAL-05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-mttr-rework/16-CONTEXT.md
@.planning/phases/16-mttr-rework/16-UAT.md
@.planning/phases/16-mttr-rework/16-05-PLAN.md
@.planning/phases/16-mttr-rework/16-06-PLAN.md
@CLAUDE.md

<interfaces>
<!-- Test infra + the existing suite this plan extends. Read before editing. -->

tests/test_mttr_trend_module.py (existing):
- Fixture helpers already present and REUSED (do not redefine): `_uuid(n)`, `_make_fixed_vulns(rows)`,
  `_make_assets(rows)`, `_make_snapshot_mttr(month, mttr_overall_days, mttr_by_severity, mttr_by_owner, generated_at)`,
  `_make_trend_snapshots(snapshots, insufficient_data)`, `_config(**options)`, `_run(fixed_rows, trend_snapshots, asset_rows, **options)`.
- `_run(...)` passes options through — call `_run(rows, mttr_table="application", tag_category="Owner", tag_value="X")`
  to drive focus. To make focus="application" meaningful, pass asset_rows with both Owner= and Application= tags.
- REF = 2026-06-12 UTC; `_UUID_PREFIX = "00000000-0000-0000-0000-00000000000"`. `pd.options.mode.copy_on_write = True` at module top.
- _make_assets default row tag is "Owner=Engineering" — extend asset_rows with "Owner=X;Application=Y" to exercise the application cut.
- _make_trend_snapshots + _make_snapshot_mttr drive the MoM arrow: build two snapshots (prior month + current month)
  with differing mttr_by_severity values to force a decrease (▼) or increase (▲) per severity.
- TestStructuralBaselines builds populated + zero-match bundles (mod.render_excel_tabs / render_pdf_section /
  render_email_panel) and has @pytest.mark.baseline tests comparing against the committed JSONs.

The 16-05 mttr_view test classes (TestMttrViewDefault / TestMttrViewOwner / TestMttrViewSeverity /
TestMttrViewBoth / TestOwnerSlaBasisFix / TestMttrViewBadValueFallback / TestSingleCutFitsOnePage)
are now STALE — mttr_view is retired. REMOVE/REPLACE the mttr_view-specific ones; the Owner-SLA-drop
and single-page intents survive in the new D-16-13 tests below. Do NOT delete the unrelated existing
classes (TestCriterion3ReopenedClock, cold-start, min_sample, four-channel empty, composed-smoke, baselines).

tests/baseline_utils.py: extract_structural_snapshot(bundle, slug) -> dict (incl. "pdf_page_count" key,
built by _pdf_page_count_from_html); compare_snapshots(actual, baseline) -> list[str] (empty == no regression);
write_baseline(path, snapshot) / load_baseline(path). pdf_page_count IS present (line 187).

tests/test_board_summary_baseline.py (16-03): asserts compare_snapshots == [] for the three board_summary
baselines. RE-RUN it; it must still pass (D-16-10), and the JSONs must remain byte-identical.

scripts/capture_trend_snapshot.py (as fixed by 16-06): has the --dry-run flag (exit 0, no fetch/write) and
the sys.path bootstrap. The regression test must invoke it via subprocess as `python scripts/capture_trend_snapshot.py --dry-run`
from a CWD that is NOT the repo root (e.g. a tempdir or the tests/ dir), so the bootstrap — not the ambient
CWD — is what puts the repo root on sys.path. Use sys.executable for the interpreter; pass cwd=<non-root>;
assert returncode == 0 and "ModuleNotFoundError" not in stderr. The repo root is the script's parent.parent
(resolve from the test file location or from the script path).

The always-on-gauge / focus-driven render is NEW after 16-06 — the committed mttr_trend baselines from 16-05
(owner-only default, no gauges) WILL drift. Regenerate them deliberately. board_summary baselines must NOT change.
</interfaces>
</context>

<feature>
  <name>D-16-13 gauge/focus acceptance tests + snapshot real-CLI regression + regenerated mttr_trend baselines + board_summary zero-diff re-assert</name>
  <files>tests/test_mttr_trend_module.py, tests/test_capture_trend_snapshot_cli.py, tests/baselines/mttr_trend_test_pull.json, tests/baselines/mttr_trend_test_pull_zero_match.json</files>
  <behavior>
    RED then GREEN against the 16-06 implementation. Replace the retired 16-05 mttr_view classes with D-16-13 classes
    (reuse the existing fixture helpers):
    - TestGaugeBandAllViews: build a fixture with all four severities >= min_sample. For mode owner (unfocused),
      application (tag_category="Owner", tag_value set, asset rows carrying Application= tags), and gauges-only
      (tag_category="Application", tag_value set) → assert the rendered PDF AND email contain 4 gauge images
      (4 `data:image/png;base64,` occurrences, or the chosen gauge marker) in ALL three modes. Proves the gate is gone.
    - TestMomArrowPolarity: build two snapshots (prior + current month) where, per severity, current mttr_by_severity
      < prior → assert the PDF shows the ▼ green marker for that severity; current > prior → ▲ red; flat / single
      snapshot → — grey. Assert the polarity is correct (decrease == green/down, increase == red/up).
    - TestFocusRouting: unfocused `_run(rows, asset_rows=[Owner+App tags])` → metadata["mttr_table_mode"]=="owner"
      and the PDF has an "MTTR by Owner" detail table; `_run(rows, tag_category="Owner", tag_value="X")` →
      mode=="application" and an "MTTR by Application" table; `_run(rows, tag_category="Application", tag_value="Y")`
      → mode=="none" and NO detail table (only gauges). Also test explicit mttr_table="owner"/"application" override.
    - TestSeverityTableAbsent: in every mode, the rendered PDF has no severity <table> (the "MTTR by Severity" <h3>+table
      is gone); the rendered email has no severity table; the Excel tab has the compact severity NUMERIC block
      (header row Severity/MTTR/SLA/Status/MoM Delta) but not the old 6-column "SLA Target (Days)"+"Variance (Days)"
      severity table. mttr_view is fully gone: assert no module/option references it.
    - TestExcelSeverityBlockAndSla: the Excel "MTTR Trend" tab contains the 4-row severity numeric block; the SLA
      value for Medium equals config.SLA_DAYS["medium"] (60) — sourced from config, NOT hardcoded 45.
    - TestMttrTableBadValueFallback: mttr_table="bogus" → no crash; metadata["mttr_table_mode"] resolves to a valid
      mode (auto path); validate_config(ModuleConfig("mttr_trend", options={"mttr_table":"bogus"})) returns a non-empty error.
    - TestSinglePageFit: a representative owner-only bundle (4-6 owners) and a gauges-only bundle each have
      extract_structural_snapshot(bundle, slug)["pdf_page_count"] == 1.
    - All new fixtures synthetic (asset_uuid "00000000-...000N"; no real hostnames/IPs/CVE/plugin names — QUAL-05).

    New file tests/test_capture_trend_snapshot_cli.py:
    - TestSnapshotCliRealInvocation: subprocess.run([sys.executable, str(script_path), "--dry-run"], cwd=<non-root>,
      capture_output=True, text=True). Assert returncode == 0 and "ModuleNotFoundError" not in result.stderr and
      "No module named 'config'" not in result.stderr. The non-root cwd is critical — it reproduces UAT-5. Use a
      tempfile.TemporaryDirectory() or tests/ as cwd. Compute script_path as repo_root/"scripts"/"capture_trend_snapshot.py".
      (Optionally also assert "DRY RUN" appears in output to confirm the script actually reached main(), not just imported.)

    Baseline regeneration: the committed mttr_trend baselines were captured against the 16-05 owner-only (no-gauge)
    render. After the suite is green, regenerate both via extract_structural_snapshot + write_baseline (same path as
    TestStructuralBaselines) so the @pytest.mark.baseline self-guard tests pass against the NEW always-on-gauge /
    focus-driven shape. The zero-match (cold-start) baseline likely does not change structurally — re-verify and only
    rewrite if it genuinely drifts.

    board_summary zero-diff: re-run tests/test_board_summary_baseline.py — it MUST stay green and the three
    board_summary baseline JSONs MUST remain byte-identical (D-16-10). Do not rewrite them.
  </behavior>
  <implementation>
    Append/replace the test classes in tests/test_mttr_trend_module.py using the existing `_run` / `_make_*` / `_config`
    helpers and the TestStructuralBaselines bundle-assembly pattern. Read the REAL metadata/option key names from
    reports/modules/mttr_trend_module.py as edited by 16-06 — mttr_table_mode, table_data_application,
    per_sev_mom_direction, the gauge marker representation, and the Owner/Application table header strings — do NOT
    hardcode assumed names. For "4 gauges present", count `data:image/png;base64,` occurrences in the PDF/email HTML
    (the existing gauge path emits an <img src="data:image/png;base64,...">). For the MoM arrow, read the chosen glyph
    from the module and assert its presence + color span. For pdf_page_count use extract_structural_snapshot.

    For the subprocess test, derive repo_root from the test file path (Path(__file__).resolve().parent.parent) and run
    from a guaranteed-non-root cwd; do NOT prepend the repo root to the subprocess env PYTHONPATH (that would mask the
    bug the way the import-from-root probe did — the bootstrap inside the script must be what fixes it). --dry-run needs
    no Tenable credentials (it returns before get_client()).

    Regenerate baselines: run the capture path (same write_baseline call shape TestStructuralBaselines / 16-03/16-05 used).
    Confirm `python -m pytest tests/test_mttr_trend_module.py -m baseline` passes.
  </implementation>
</feature>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED→GREEN — D-16-13 gauge/focus/MoM-arrow/severity-table-absent tests (replace retired mttr_view tests)</name>
  <read_first>
    - tests/test_mttr_trend_module.py (the existing suite — reuse _run/_make_*/_config and the TestStructuralBaselines bundle-assembly pattern; identify the 16-05 mttr_view classes to remove)
    - reports/modules/mttr_trend_module.py (as edited by 16-06 — read the REAL metadata keys mttr_table_mode / table_data_application / per_sev_mom_direction, the gauge img representation, the arrow glyph + colors, and the Owner/Application table header strings)
    - .planning/phases/16-mttr-rework/16-06-PLAN.md (what 16-06 actually built — always-on gauges, focus modes, mttr_table override)
    - .planning/phases/16-mttr-rework/16-UAT.md (`## Gaps` — the D-16-13 required-tests list is the authoritative acceptance set)
    - tests/baseline_utils.py (extract_structural_snapshot pdf_page_count key for the single-page assertion)
    - config.py (SLA_DAYS — Medium==60 for the SLA-source assertion)
  </read_first>
  <files>tests/test_mttr_trend_module.py</files>
  <action>
    Remove the stale 16-05 mttr_view classes (TestMttrViewDefault/Owner/Severity/Both, and rework TestOwnerSlaBasisFix /
    TestMttrViewBadValueFallback / TestSingleCutFitsOnePage into the D-16-13 forms below). Keep all unrelated existing
    classes intact.

    Append D-16-13 test classes reusing the fixture helpers: TestGaugeBandAllViews (4 base64 gauge images present in
    PDF AND email for owner / application / gauges-only modes), TestMomArrowPolarity (two-snapshot fixture: per-severity
    decrease→▼green, increase→▲red, flat/single→—), TestFocusRouting (unfocused→mode "owner"+Owner table;
    tag_category="Owner"+tag_value→mode "application"+Application table; tag_category="Application"+tag_value→mode "none"+no
    table; explicit mttr_table override), TestSeverityTableAbsent (no severity <table> in PDF/email; Excel has the numeric
    block not the old 6-col table; mttr_view fully gone), TestExcelSeverityBlockAndSla (Medium SLA == config.SLA_DAYS["medium"]
    == 60, sourced from config), TestMttrTableBadValueFallback (bad value → auto + validate_config error), TestSinglePageFit
    (owner-only and gauges-only bundles → pdf_page_count == 1).

    Read the real metadata/option/header/glyph names from the 16-06 module — do NOT hardcode assumed strings. Count
    `data:image/png;base64,` for gauge presence. All fixtures synthetic (asset_uuid "00000000-...000N"; asset rows carry
    "Owner=X;Application=Y" to exercise the application cut).
  </action>
  <verify>
    <automated>python -m pytest tests/test_mttr_trend_module.py -x -k "GaugeBand or MomArrow or FocusRouting or SeverityTableAbsent or ExcelSeverityBlock or MttrTableBadValue or SinglePageFit" -W error::FutureWarning</automated>
  </verify>
  <acceptance_criteria>
    - The retired mttr_view classes are gone: `grep -c "class TestMttrView" tests/test_mttr_trend_module.py` == 0 and `grep -c "mttr_view" tests/test_mttr_trend_module.py` == 0.
    - Gauge-band test present and counts 4 gauges across all modes: `grep -c "class TestGaugeBandAllViews" tests/test_mttr_trend_module.py` >= 1 and `grep -c "data:image/png;base64" tests/test_mttr_trend_module.py` >= 1.
    - MoM arrow polarity test present: `grep -c "class TestMomArrowPolarity" tests/test_mttr_trend_module.py` >= 1 and it asserts decrease→green/down and increase→red/up.
    - Focus routing test present and asserts the three modes: `grep -c "class TestFocusRouting" tests/test_mttr_trend_module.py` >= 1; `grep -c "mttr_table_mode" tests/test_mttr_trend_module.py` >= 1.
    - Severity-table-absent test present: `grep -c "class TestSeverityTableAbsent" tests/test_mttr_trend_module.py` >= 1.
    - Excel-SLA-from-config test present: `grep -c "SLA_DAYS\|config.SLA_DAYS\|\"medium\"" tests/test_mttr_trend_module.py` >= 1 and it asserts Medium == 60.
    - Bad-value fallback test present: `grep -c "class TestMttrTableBadValueFallback" tests/test_mttr_trend_module.py` >= 1.
    - Single-page test present: `grep -c "class TestSinglePageFit" tests/test_mttr_trend_module.py` >= 1 and `grep -c "pdf_page_count" tests/test_mttr_trend_module.py` >= 1.
    - The targeted tests pass: the verify command exits 0.
    - Suite passes under CoW strict mode: `python -m pytest tests/test_mttr_trend_module.py -W error::FutureWarning` exits 0.
    - PII-clean: new fixtures use the synthetic uuid prefix; `grep -c "00000000-0000-0000-0000-00000000000" tests/test_mttr_trend_module.py` >= 1; no real IPs/hostnames added.
  </acceptance_criteria>
  <done>The retired mttr_view tests are replaced by passing D-16-13 tests proving: 4 gauges in all views, MoM arrow polarity, focus routing (owner/application/none), severity-table absence, Excel severity numeric block with config-sourced SLA, mttr_table bad-value fallback, and single-page fit. The existing suite is extended, not forked; all fixtures synthetic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Snapshot-script real-CLI subprocess regression test (UAT-5 lock)</name>
  <read_first>
    - scripts/capture_trend_snapshot.py (as fixed by 16-06 — the --dry-run path returns 0 before get_client(); the sys.path bootstrap precedes first-party imports)
    - .planning/phases/16-mttr-rework/16-UAT.md (Test 5 — the real invocation `python scripts/capture_trend_snapshot.py` must run; the import-from-root probe masked it)
    - tests/test_mttr_trend_module.py (top-of-file conventions for a synthetic, dependency-light test module — match import style)
  </read_first>
  <files>tests/test_capture_trend_snapshot_cli.py</files>
  <action>
    Create tests/test_capture_trend_snapshot_cli.py with TestSnapshotCliRealInvocation. Compute repo_root =
    Path(__file__).resolve().parent.parent and script_path = repo_root / "scripts" / "capture_trend_snapshot.py".
    Run subprocess.run([sys.executable, str(script_path), "--dry-run"], cwd=<a guaranteed-non-root dir>,
    capture_output=True, text=True). The cwd MUST NOT be the repo root (use tempfile.TemporaryDirectory() so the
    ambient CWD cannot put the repo root on sys.path — the script's own bootstrap must be what makes the imports
    resolve). Do NOT set PYTHONPATH to the repo root in the subprocess env (that would re-mask the bug). Assert
    result.returncode == 0, "ModuleNotFoundError" not in result.stderr, and "No module named 'config'" not in
    result.stderr. Optionally assert the dry-run reached main() (e.g. "DRY RUN" in stdout+stderr). Keep the test
    free of Tenable credentials (--dry-run returns before get_client()). No real data, no network.
  </action>
  <verify>
    <automated>python -m pytest tests/test_capture_trend_snapshot_cli.py -x</automated>
  </verify>
  <acceptance_criteria>
    - The test file exists with the subprocess regression test: `grep -c "subprocess" tests/test_capture_trend_snapshot_cli.py` >= 1 and `grep -c "capture_trend_snapshot.py" tests/test_capture_trend_snapshot_cli.py` >= 1.
    - It runs from a non-root CWD (reproduces UAT-5): `grep -c "TemporaryDirectory\|cwd=" tests/test_capture_trend_snapshot_cli.py` >= 1 and does NOT inject the repo root into the subprocess PYTHONPATH (verify by reading — no env PYTHONPATH=repo_root).
    - It asserts no ModuleNotFoundError: `grep -c "ModuleNotFoundError" tests/test_capture_trend_snapshot_cli.py` >= 1.
    - `python -m pytest tests/test_capture_trend_snapshot_cli.py -x` exits 0.
    - The test would FAIL if the bootstrap were reverted (note this in the SUMMARY — the test is the regression guard; do not weaken it by setting PYTHONPATH).
  </acceptance_criteria>
  <done>A subprocess test invokes `python scripts/capture_trend_snapshot.py --dry-run` from a non-root CWD and asserts exit 0 with no ModuleNotFoundError — the real-invocation path that UAT-5 exercised is locked against regression.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Regenerate mttr_trend baselines + re-assert board_summary zero-diff (D-16-10) + full suite green</name>
  <read_first>
    - tests/test_mttr_trend_module.py (TestStructuralBaselines — _build_populated_bundle / _build_zero_match_bundle + the @pytest.mark.baseline self-guard tests)
    - tests/baseline_utils.py (extract_structural_snapshot / compare_snapshots / write_baseline / load_baseline)
    - tests/baselines/mttr_trend_test_pull.json + mttr_trend_test_pull_zero_match.json (the 16-05 owner-only baselines that will drift)
    - tests/test_board_summary_baseline.py (the 16-03 zero-diff assertion — re-run, do not modify)
    - tests/baselines/board_summary_test_pull.json + board_summary_test_pull_analyst_off.json + board_summary_test_pull_zero_match.json (must stay byte-identical)
    - .planning/phases/16-mttr-rework/16-CONTEXT.md (D-16-10 — a board_summary diff means something broke)
  </read_first>
  <files>tests/baselines/mttr_trend_test_pull.json, tests/baselines/mttr_trend_test_pull_zero_match.json</files>
  <action>
    After Tasks 1-2 are green, regenerate both mttr_trend baselines via the same extract_structural_snapshot +
    write_baseline path TestStructuralBaselines uses (the populated bundle now renders the always-on gauge band +
    focus-driven table; the zero-match cold-start bundle likely does not change). Run
    `python -m pytest tests/test_mttr_trend_module.py -m baseline` and confirm the @pytest.mark.baseline tests pass
    against the regenerated files. Only rewrite the zero-match JSON if it genuinely drifts. Keep baselines
    structural-only — no metric values (QUAL-05).

    Then re-run the board_summary zero-diff test and the full suite to prove no regression. The three board_summary
    baseline JSONs MUST remain byte-identical — a non-zero diff is a FAILURE signal (scope violation), per D-16-10.
    Do NOT rewrite the board_summary baselines or loosen the test. If the full suite surfaces a regression, the fix
    belongs in the 16-06 module change, not in the test.
  </action>
  <verify>
    <automated>python -m pytest tests/ -x -m baseline; python -m pytest tests/ -x</automated>
  </verify>
  <acceptance_criteria>
    - Both mttr_trend baseline files are valid JSON with structural keys only: `python -c "import json; d=json.load(open('tests/baselines/mttr_trend_test_pull.json')); assert 'pdf_page_count' in d and 'overall_mttr' not in d"` exits 0.
    - The mttr_trend self-guarding baseline tests pass: `python -m pytest tests/test_mttr_trend_module.py -m baseline` exits 0.
    - The populated mttr_trend baseline reflects the new render (changed vs the 16-05 version): `git diff --stat tests/baselines/mttr_trend_test_pull.json` shows the file changed.
    - board_summary zero-diff: `python -m pytest tests/test_board_summary_baseline.py -x` exits 0.
    - board_summary baseline JSONs byte-unchanged: `git diff --quiet -- tests/baselines/board_summary_test_pull.json tests/baselines/board_summary_test_pull_analyst_off.json tests/baselines/board_summary_test_pull_zero_match.json` exits 0.
    - mttr_by_severity_module.py byte-unchanged: `git diff --quiet -- reports/modules/mttr_by_severity_module.py` exits 0.
    - Full suite green: `python -m pytest tests/ -x` exits 0.
  </acceptance_criteria>
  <done>The two mttr_trend structural baselines are regenerated to the always-on-gauge / focus-driven render and self-guarded by passing compare_snapshots == [] tests; board_summary baselines + mttr_by_severity are byte-identical (D-16-10); the full suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixtures → committed repo | Synthetic data + structural baselines persisted under version control |
| operator config (`mttr_table`) / focus signal → render path | Exercised in tests to prove whitelist + safe-default + focus routing |
| snapshot script subprocess → CWD-independent import resolution | The real-CLI test proves the bootstrap, not the ambient CWD, resolves imports |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-24 | Information Disclosure | committed test fixtures + regenerated baselines | mitigate | All fixtures synthetic (asset_uuid all-zero prefix; RFC 5737/6761). Regenerated baselines capture only structural keys — never metric values or row data (QUAL-05). |
| T-16-25 | Tampering | board_summary regression undetected | mitigate | Re-run the D-16-10 zero-diff assertion; any structural drift in board_summary fails the build. board_summary baselines asserted byte-identical via `git diff --quiet`. |
| T-16-26 | Tampering | `mttr_table` whitelist bypass | mitigate | TestMttrTableBadValueFallback proves an unknown value falls back to auto and validate_config rejects it — the operator-config trust boundary is enforced by test. |
| T-16-27 | Tampering | snapshot bootstrap regression re-masked | mitigate | The subprocess test runs from a non-root CWD and deliberately does NOT set PYTHONPATH to the repo root, so it fails if the in-script bootstrap is reverted (the exact gap that masked UAT-5). |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | No package installs — zero new dependencies. No legitimacy gate required. |

No high-severity threats. Tests over synthetic, aggregate, structural-only data plus a credential-free --dry-run subprocess; no new external I/O.
</threat_model>

<verification>
- `python -m pytest tests/test_mttr_trend_module.py -x -W error::FutureWarning` exits 0 (gauge band + MoM arrows + focus routing + severity-table-absent + single-page + existing gates).
- `python -m pytest tests/test_capture_trend_snapshot_cli.py -x` exits 0 (real-CLI bootstrap regression).
- `python -m pytest tests/test_mttr_trend_module.py -m baseline` exits 0 (regenerated self-guarded baselines).
- `python -m pytest tests/test_board_summary_baseline.py -x` exits 0; board_summary baselines byte-unchanged (D-16-10).
- `git diff --quiet -- reports/modules/mttr_by_severity_module.py` exits 0.
- `python -m pytest tests/ -x` full suite green.
</verification>

<success_criteria>
- 4 gauges present in PDF + email for owner / application / gauges-only modes (gate removed) — proven by test.
- MoM arrow polarity correct (decrease→▼green, increase→▲red, flat→—) — proven by test.
- Focus routing (unfocused→Owner, Owner-focused→Application, Application-focused→none) — proven by test.
- Severity table absent in all headline channels; Excel severity numeric block with config-sourced SLA (Medium==60); mttr_view fully removed — proven by test.
- `python scripts/capture_trend_snapshot.py --dry-run` runs from a non-root CWD (subprocess regression test).
- mttr_trend structural baselines regenerated; board_summary baselines + mttr_by_severity byte-identical (D-16-10).
- All committed fixtures synthetic (QUAL-05).
</success_criteria>

<output>
Create `.planning/phases/16-mttr-rework/16-07-SUMMARY.md` when done.
</output>
