---
phase: 16-mttr-rework
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - reports/modules/mttr_trend_module.py
  - reports/composed_report.py
autonomous: true
requirements: [RPT-05]
must_haves:
  truths:
    - "mttr_trend auto-discovers via @register_module with MODULE_ID == 'mttr_trend'; no run_all.py / _VALID_REPORTS registration"
    - "Overall MTTR is the flat sample-weighted mean of days_to_fix over durably-fixed (state==FIXED) findings (D-16-01 + D-16-02 consequence)"
    - "days_to_fix = (last_fixed - COALESCE(resurfaced_date, first_found)).days clipped >=0; criterion-3 fixture reads ~8 not 198 (D-16-02)"
    - "The rolling-window value (default 30, configurable mttr_window_days) is disclosed in all four channels (D-16-04)"
    - "MoM trend reads from kwargs['trend_snapshots']; cold-starts on insufficient_data without aborting the live per-severity gauges (QUAL-01)"
    - "Owner cut is MoM with per-Owner cold-start + drift reconciliation; calendar-month X-axis with partial-month flag (D-16-06/08)"
    - "Sub-threshold severities/Owners render 'Insufficient data (N findings — minimum 5 required)'; zero-fixed Owners omitted; zero-overall => _empty_result() gray RAG (D-16-07)"
    - "mttr_by_severity_module.py is byte-unchanged (D-16-10)"
  artifacts:
    - path: "reports/modules/mttr_trend_module.py"
      provides: "Four-channel MTTRTrendModule (pdf/excel/email panel/rag strip) reading rolling MTTR + MoM snapshots"
      contains: "MODULE_ID"
      min_lines: 250
    - path: "reports/composed_report.py"
      provides: "mttr_trend added to _MODULES_NEEDING_TREND_SNAPSHOTS so the trend_snapshots kwarg is fanned out"
      contains: "mttr_trend"
  key_links:
    - from: "reports/composed_report.py _MODULES_NEEDING_TREND_SNAPSHOTS"
      to: "MTTRTrendModule.compute kwargs['trend_snapshots']"
      via: "composer **self._kwargs fan-out"
      pattern: "mttr_trend"
    - from: "MTTRTrendModule.compute"
      to: "snapshot mttr_by_owner / mttr_by_severity / mttr_overall_days"
      via: "snap.get() cold-start-safe access"
      pattern: "mttr_by_owner"
---

<objective>
Build the new four-channel `mttr_trend` metric module that fixes the four undisclosed correctness gaps in `mttr_by_severity_module.py` — implicit window, unweighted mean-of-means, reopen-cycle inflation, no MoM/Owner cut — and register it for the `trend_snapshots` kwarg fan-out. `mttr_by_severity_module.py` is left byte-unchanged so board_summary keeps delivering (D-16-10). The module auto-discovers via `@register_module` and is consumed through composed reports / management_summary (Phase 18) — no `run_all.py` registration.

Purpose: This is the RPT-05 deliverable itself — the reworked MTTR.
Output: One new module file + one frozenset entry. The module renders gauges from live `fixed_vulns_df` and the MoM trend line from snapshots (two independent cold-start paths).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-mttr-rework/16-CONTEXT.md
@.planning/phases/16-mttr-rework/16-RESEARCH.md
@.planning/phases/16-mttr-rework/16-PATTERNS.md
@.planning/phases/16-mttr-rework/16-01-SUMMARY.md

<interfaces>
<!-- The dual-analog contracts the executor implements against. PATTERNS.md carries the section-by-section copy map; do NOT explore the codebase to re-derive these. -->

reports/modules/base.py:
- BaseModule.compute(self, vulns_df, assets_df, report_date, config, **kwargs) -> ModuleData (override).
- ModuleData fields incl. driver_narrative, analyst_rows, rag_strip (CONTRACT-04). _empty_result(error, config) returns a coherent gray-RAG failed ModuleData.
- Renderer defaults are concrete no-ops: render_pdf_section -> "", render_excel_tabs -> [], render_email_panel -> "", render_rag_strip_entry -> gray cell, render_analyst_tabs -> [].

reports/modules/format_utils.py: safe_pct, safe_int, safe_format — mandatory for any possibly-None value (QUAL-03). Inline f-string format specs on None-able values are forbidden.
reports/modules/rag_utils.py: build_rag_strip_entry(display_name, headline_value_str, status), STATUS_COLOR, NO_DATA_HEADLINE, NO_DATA_DRIVER. status in {"green","yellow","red","no_data"}.
reports/modules/board_report_utils.py: extract_owner(assets_df) -> Owner cut + "Unassigned" catch-all (consume read-only).
config.py: SLA_DAYS dict (critical/high/medium/low) — Critical SLA is the overall-MTTR RAG anchor.

reports/composed_report.py:
- _MODULES_NEEDING_TREND_SNAPSHOTS frozenset at line 86 (current members: sc4_kwargs_stub, new_vs_remediated, vuln_density, accepted_recast). The composer **self._kwargs fan-out delivers trend_snapshots to any module whose ID is in this set — no signature change.

Primary analog: reports/modules/new_vs_remediated_module.py (MoM + cold-start + Owner + calendar-month axis + render_email_panel + render_rag_strip_entry).
Secondary analog: reports/modules/mttr_by_severity_module.py (gauge draw, Excel SLA tab, _status_from_ratio) — COPY FROM, do not edit.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create reports/modules/mttr_trend_module.py (compute + four-channel renders)</name>
  <read_first>
    - reports/modules/new_vs_remediated_module.py (PRIMARY analog — copy: imports, _build_cold_start_result, compute() cold-start guard, _month_label partial-month helper, Owner-cut iteration, render_email_panel CONTRACT-01 shell, render_rag_strip_entry)
    - reports/modules/mttr_by_severity_module.py (SECONDARY analog — copy gauge draw, Excel SLA-tab structure, _status_from_ratio; this file is READ-ONLY, must stay byte-unchanged per D-16-10)
    - reports/modules/base.py (BaseModule, ModuleConfig, ModuleData, _empty_result)
    - reports/modules/format_utils.py (safe_pct/safe_int/safe_format)
    - reports/modules/rag_utils.py (build_rag_strip_entry, STATUS_COLOR, NO_DATA_HEADLINE)
    - reports/modules/board_report_utils.py (extract_owner)
    - config.py (SLA_DAYS)
    - .planning/phases/16-mttr-rework/16-PATTERNS.md (§"reports/modules/mttr_trend_module.py" — section-by-section copy map: every code block has a source-line citation)
    - .planning/phases/16-mttr-rework/16-RESEARCH.md (Open Item 2 Owner-drift join; Open Item 4 render paths + min_sample; Architecture Patterns four-channel table + RAG logic + disclosure wording; Pitfalls A-E)
  </read_first>
  <files>reports/modules/mttr_trend_module.py</files>
  <action>
    Create a new `@register_module class MTTRTrendModule(BaseModule)` with `MODULE_ID = "mttr_trend"`, `DISPLAY_NAME = "MTTR Trend (Reopened-Aware)"`, `REQUIRED_DATA = ["vulns", "assets", "fixed_vulns", "trend_snapshots"]`, `SUPPORTED_OUTPUTS = ["pdf", "excel", "email"]`. Structure copied from the dual analogs per PATTERNS.md. Concrete requirements:
    (1) compute() reads `window_days = int(config.options.get("mttr_window_days", 30))` and `min_sample = int(config.options.get("min_sample_size", 5))` (default raised from the old module's 1 to 5 per criterion 5).
    (2) D-16-01 population: source from `kwargs.get("fixed_vulns_df")`; if None/empty -> `_empty_result()` / cold-start (gray RAG). Filter `fixed_vulns_df[fixed_vulns_df["state"].astype(str).str.upper() == "FIXED"]`. Do NOT use the old `state=="fixed" OR last_fixed.notna()` clause.
    (3) D-16-02 clock: build `_nat` object series; coerce last_fixed/first_found/resurfaced_date with `pd.to_datetime(..., utc=True, errors="coerce")`; `clock_start = resurfaced_ts.where(resurfaced_ts.notna(), other=first_found_ts)`; `days_to_fix = (last_fixed_ts - clock_start).dt.days.clip(lower=0)`; attach via `.assign(days_to_fix=...)` (CoW: `.assign()` only); drop NaN/<0 rows. DROP the `time_taken_to_fix` preference entirely.
    (4) Apply the rolling window to the live frame: keep rows where last_fixed >= `report_date - window_days` (UTC-aware).
    (5) Overall MTTR = `round(float(fixed_df["days_to_fix"].mean()), 1)` when `len(fixed_df) >= min_sample` else None (flat mean = sample-weighted per D-16-02 consequence; NOT a mean of per-severity means).
    (6) Per-severity dict over critical/high/medium/low (None when n < min_sample); per-Owner via extract_owner (omit Owners with 0 fixed findings; None when 0<n<min_sample).
    (7) MoM from snapshots: `trend_snapshots = kwargs.get("trend_snapshots")`; `snapshots_cold = trend_snapshots is None or trend_snapshots.get("insufficient_data", True)` — cold MoM does NOT abort the module; live gauges still render. Deduplicate snapshots to latest `generated_at` per calendar month (D-16-08 tie-break). Build aligned `months`, `overall_mttr_series`, `sev_series`, and the per-Owner `owner_series` forward-fill join exactly per RESEARCH Open Item 2 (new Owner => leading None back-fill = cold start; vanished Owner => trailing None and omitted from the current table when `owner_series[owner][-1] is None`; missing month => "—"). Use the `(snap.get("mttr_by_owner") or {}).get(owner)` access form (Pitfall B) — never `snap[...][...]`. Owner MoM delta = absolute day delta `curr - prev` (None when either side None), NOT a percentage.
    (8) `_month_label` partial-month helper copied verbatim from new_vs_remediated; current calendar month gets the "(MTD — partial)" / "month-to-date (partial)" suffix (D-16-08; the rolling-30 value is complete, the flag marks calendar position).
    (9) D-16-04 disclosure in all four channels, reading the actual window_days value: PDF explanatory-text line ("Rolling {window_days}-day MTTR — findings remediated in the last {window_days} days. Current month ({month}) is month-to-date (partial)."), Excel row-1 title ("MTTR Trend — Rolling {window_days}-day window"), email panel inline-CSS footer span, and metadata.
    (10) D-16-07 sparse wording standardized: `f"Insufficient data ({n} findings — minimum {min_sample} required)"`.
    (11) Four channels: render_pdf_section (overall gauge vs Critical-SLA anchor + window disclosure + per-severity gauges + MoM line chart + Owner table), render_excel_tabs (single "MTTR Trend" tab, column order: Severity/Owner, MTTR (Days), SLA Target, Variance, Status, Sample Size, MoM Delta), render_email_panel (CONTRACT-01 table, NOT render_email_kpis; inline CSS only, cold-start branch, footer disclosure), render_rag_strip_entry (CONTRACT-03 via build_rag_strip_entry; headline overall MTTR string e.g. "14.3d"; RAG green/amber/red on `overall_mttr / SLA_DAYS["critical"]` with thresholds <=1.0 / <=1.25 / >1.25; no_data when overall is None). QUAL-03: every None-able value passes through safe_format/safe_pct/safe_int — no inline `f"{mttr:.1f}d"` on a None-able value. Wrap compute() body in try/except returning `self._empty_result(str(exc), config)`. Store `overall_mttr`, `cold_start`, `rag_status`, `window_days`, `current_month` in `data.metrics`/`data.metadata` so renderers and tests can read them.
  </action>
  <verify>
    <automated>python -c "from reports.modules.mttr_trend_module import MTTRTrendModule as M; assert M.MODULE_ID=='mttr_trend'; assert set(['render_pdf_section','render_excel_tabs','render_email_panel','render_rag_strip_entry']) <= set(dir(M))"</automated>
  </verify>
  <acceptance_criteria>
    - Import + MODULE_ID assertion above exits 0.
    - Auto-discovery registers it: `python -c "import reports.modules; from reports.modules.registry import _REGISTRY if False else None" ` — simpler: `python -c "import reports.modules as m; from reports.modules.registry import get_module; assert get_module('mttr_trend') is not None"` exits 0 (use the registry's actual lookup API discovered in registry.py).
    - days_to_fix uses date-math, not time_taken_to_fix: `grep -c "time_taken_to_fix" reports/modules/mttr_trend_module.py` == 0; `grep -c "resurfaced" reports/modules/mttr_trend_module.py` >= 1.
    - min_sample default is 5: `grep -c 'get("min_sample_size", 5)' reports/modules/mttr_trend_module.py` >= 1.
    - CoW-compliant: `grep -c "assign(days_to_fix" reports/modules/mttr_trend_module.py` >= 1; zero `["days_to_fix"] =` direct-assignment-after-filter occurrences.
    - mttr_by_severity_module.py untouched: `git diff --quiet -- reports/modules/mttr_by_severity_module.py` exits 0.
  </acceptance_criteria>
  <done>A self-contained, auto-discovered four-channel MTTRTrendModule exists implementing D-16-01/02/04/05/06/07/08 with the reopened-aware clock and the dual cold-start paths; mttr_by_severity_module.py is byte-unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add 'mttr_trend' to _MODULES_NEEDING_TREND_SNAPSHOTS</name>
  <read_first>
    - reports/composed_report.py (read _MODULES_NEEDING_TREND_SNAPSHOTS frozenset at line 86 and the `need_trend = bool(_MODULES_NEEDING_TREND_SNAPSHOTS.intersection(modules))` fan-out at ~line 215 — confirm no signature change is needed)
    - .planning/phases/16-mttr-rework/16-PATTERNS.md (§"reports/composed_report.py — add mttr_trend to frozenset")
  </read_first>
  <files>reports/composed_report.py</files>
  <action>
    Add a single member `"mttr_trend"` to the `_MODULES_NEEDING_TREND_SNAPSHOTS` frozenset (the set currently holding sc4_kwargs_stub, new_vs_remediated, vuln_density, accepted_recast) with an inline comment noting D-16-03 (reads rolling MTTR from trend snapshots). Make NO other change to composed_report.py — the `**self._kwargs` fan-out in composer.py already delivers `trend_snapshots` to any module whose ID is in this set. This is the only wiring needed; mttr_trend needs no `_MODULES_NEEDING_FIXED_VULNS` entry because composed_report only fetches fixed_vulns_df for that set (critical_remediation_sla) — confirm whether mttr_trend needs fixed_vulns_df fanned out in composed runs and, if the existing fetch gate does not cover it, add "mttr_trend" to `_MODULES_NEEDING_FIXED_VULNS` as well so the module receives `fixed_vulns_df` (it is a REQUIRED_DATA input). Document the choice in the SUMMARY.
  </action>
  <verify>
    <automated>python -c "from reports.composed_report import _MODULES_NEEDING_TREND_SNAPSHOTS as s; assert 'mttr_trend' in s"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from reports.composed_report import _MODULES_NEEDING_TREND_SNAPSHOTS as s; assert 'mttr_trend' in s"` exits 0.
    - `python run_all.py --dry-run` exits 0 (no config/registry regression).
    - Frozenset membership added without altering the fan-out logic: git diff touches only the frozenset literal(s), not `need_trend`/`need_fixed` predicate lines.
  </acceptance_criteria>
  <done>composed_report routes the trend_snapshots kwarg (and, if required, fixed_vulns_df) to mttr_trend; no other composed_report logic changed; dry-run green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| trend_snapshots kwarg -> MTTRTrendModule.compute | Persisted snapshot dicts (possibly old/partial) cross into the module; must be read cold-start-safe |
| Module render output -> PDF/Excel/email | Aggregate MTTR floats + internal Owner tag names rendered to all four channels |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-05 | Denial of Service | compute() on malformed/partial snapshot or zero-row data | mitigate | Whole compute() wrapped in try/except -> _empty_result(); cold-start guards on both the snapshot path (insufficient_data) and the live path (empty fixed_vulns_df); `(snap.get(...) or {}).get(...)` access never raises KeyError/TypeError on old snapshots (Pitfall B). Filtered-to-zero groups render gray RAG, not crash (fail-soft batch). |
| T-16-06 | Information Disclosure | four-channel render | mitigate | Only aggregate floats + internal Owner tag names rendered (within PII boundary; project_pii_rule_is_ai_not_email). No hostnames/IPs/plugin names/asset rows reach any channel. Owner cut comes from extract_owner aggregate, not row data. |
| T-16-07 | Tampering | npm/pip/cargo installs | accept | No package installs — zero new dependencies. No legitimacy gate required. |
</threat_model>

<verification>
- `python -c "from reports.modules.mttr_trend_module import MTTRTrendModule"` imports clean.
- `python run_all.py --dry-run` exits 0.
- `git diff --quiet -- reports/modules/mttr_by_severity_module.py` exits 0 (byte-unchanged, D-16-10).
- Existing suite `python -m pytest tests/ -x` stays green.
</verification>

<success_criteria>
- mttr_trend is auto-discovered and receives trend_snapshots (and fixed_vulns_df) via composed-report fan-out.
- Overall MTTR is sample-weighted; days_to_fix is reopened-aware (criterion-3 logic in place); window disclosed in all four channels; MoM + Owner cut present with cold-start + drift handling; sparse rows render "Insufficient data (N findings — minimum 5 required)".
- mttr_by_severity_module.py is byte-unchanged.
</success_criteria>

<output>
Create `.planning/phases/16-mttr-rework/16-02-SUMMARY.md` when done.
</output>
