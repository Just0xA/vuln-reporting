---
phase: 16-mttr-rework
plan: 03
type: tdd
wave: 3
depends_on: ["16-02"]
files_modified:
  - tests/test_mttr_trend_module.py
  - tests/baselines/mttr_trend_test_pull.json
  - tests/baselines/mttr_trend_test_pull_zero_match.json
  - tests/test_board_summary_baseline.py
autonomous: true
requirements: [RPT-05]
must_haves:
  truths:
    - "The criterion-3 fixture (first_found -200d, resurfaced -10d, last_fixed -2d) asserts days_to_fix == 8 / overall_mttr == 8.0, not 198 (D-16-02, acceptance lodestar)"
    - "Zero fixed findings -> _empty_result()/cold-start with gray RAG; no crash (QUAL-03)"
    - "min_sample=5 sub-threshold (3 Critical) renders 'Insufficient data (3 findings — minimum 5 required)'"
    - "Owner cold-start (new Owner only in snapshot 2) -> series [None, x], MoM delta None; Owner vanished (only snapshot 1) -> omitted from current table"
    - "Multiple snapshots same month -> latest generated_at wins (D-16-08 tie-break)"
    - "All four render channels survive a zero-row ModuleData without raising (QUAL-03)"
    - "board_summary structural baselines are byte-identical after Phase 16 (D-16-10 zero-diff); a new mttr_trend structural baseline is captured"
    - "All committed fixtures are synthetic (RFC 5737/6761) — no real hostnames/IPs/plugin names (QUAL-05)"
  artifacts:
    - path: "tests/test_mttr_trend_module.py"
      provides: "Unit suite encoding criterion-3 lodestar + cold-start + min_sample + Owner-drift + tie-break + four-channel empty-data guard + CoW"
      contains: "TestCriterion3"
      min_lines: 200
    - path: "tests/baselines/mttr_trend_test_pull.json"
      provides: "Structural smoke baseline for a populated mttr_trend bundle"
    - path: "tests/baselines/mttr_trend_test_pull_zero_match.json"
      provides: "Structural smoke baseline for the zero-fixed-findings _empty_result bundle"
    - path: "tests/test_board_summary_baseline.py"
      provides: "Zero-diff assertion that board_summary structural baselines are unchanged by Phase 16"
      contains: "compare_snapshots"
  key_links:
    - from: "tests/test_mttr_trend_module.py"
      to: "MTTRTrendModule.compute"
      via: "direct module invocation with synthetic fixtures"
      pattern: "MTTRTrendModule"
    - from: "tests/test_board_summary_baseline.py"
      to: "tests/baselines/board_summary_test_pull*.json"
      via: "compare_snapshots(actual, baseline) == []"
      pattern: "board_summary_test_pull"
---

<objective>
Encode the Phase 16 acceptance gates as automated tests and capture structural smoke baselines. The criterion-3 reopened-clock fixture (~8 days, not 198) is the acceptance lodestar and MUST be a unit test. Add a board_summary zero-diff assertion proving `mttr_by_severity_module.py` and the new optional `capture_snapshot` params did not regress board_summary (D-16-10), and capture a new structural baseline for `mttr_trend`.

Purpose: Lock RPT-05 behavior against regression and prove the byte-unchanged guarantee with an automated gate rather than a manual claim.
Output: One new test module, two new baseline JSON files, one board_summary zero-diff test. All fixtures synthetic (QUAL-05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-mttr-rework/16-CONTEXT.md
@.planning/phases/16-mttr-rework/16-RESEARCH.md
@.planning/phases/16-mttr-rework/16-PATTERNS.md
@.planning/phases/16-mttr-rework/16-02-SUMMARY.md

<interfaces>
<!-- Test infra contracts. PATTERNS.md §"tests/test_mttr_trend_module.py" carries the fixture helpers + test-class list verbatim. -->

tests/baseline_utils.py:
- extract_structural_snapshot(bundle, group_slug) -> dict of 12 STRUCTURAL keys (pdf_page_count, pdf_rag_cell_count, excel_tab_names_sorted, email_panel_count, email_inline_image_cids_per_module, bundle_keys_present, analyst_excel_present, rag_cells_all_no_data, panel_drivers_all_no_data_in_scope, + 3). NEVER captures metric values.
- compare_snapshots(actual, baseline) -> list[str] of diff lines (empty list == no regression).
- write_baseline(...) / load_baseline(...) — read these signatures in baseline_utils.py before use.

tests/baselines/ (existing, committed): board_summary_test_pull.json, board_summary_test_pull_analyst_off.json, board_summary_test_pull_zero_match.json. These three MUST survive Phase 16 byte-identical (D-16-10).

Primary test analog: tests/test_new_vs_remediated_module.py (cold-start, partial-month, owner, four-channel render guard, synthetic fixtures, CoW strict mode).
</interfaces>
</context>

<feature>
  <name>MTTR Trend acceptance suite + structural baselines</name>
  <files>tests/test_mttr_trend_module.py, tests/baselines/mttr_trend_test_pull.json, tests/baselines/mttr_trend_test_pull_zero_match.json, tests/test_board_summary_baseline.py</files>
  <behavior>
    RED then GREEN. The criterion-3 fixture test is committed first as the failing lodestar, then proven green against the Plan 16-02 module. Test cases (mirror tests/test_new_vs_remediated_module.py class structure):
    - TestCriterion3ReopenedClock: fixed_vulns_df row first_found -200d / resurfaced_date -10d / last_fixed -2d (UTC-aware) -> data.metrics["overall_mttr"] == 8.0 (the old time_taken_to_fix path would yield 198). min_sample_size=1 in this config so a single finding produces a value.
    - TestZeroFixedFindings: empty fixed_vulns_df -> data.error is None, metrics["cold_start"] is True, RAG strip gray.
    - TestColdStartMoM: 1 snapshot in trend_snapshots -> renders cold-start notice on MoM, no NaN%, no crash; live per-severity gauges still computed.
    - TestMinSampleThreshold: 3 Critical findings, min_sample_size=5 -> Critical renders "Insufficient data (3 findings — minimum 5 required)", no MTTR value.
    - TestOwnerColdStart: 2 snapshots, Owner "Ops" only in snapshot 2 -> owner_series["Ops"] == [None, value]; MoM delta None.
    - TestOwnerVanished: 2 snapshots, Owner "Ops" only in snapshot 1 -> "Ops" omitted from the current Owner table.
    - TestTieBreak: 2 snapshots month="2026-06" with different generated_at -> only the latest generated_at snapshot used.
    - TestPartialMonthLabel: current-month label contains "partial".
    - TestFourChannelEmptyGuard: zero-row ModuleData -> render_pdf_section/render_excel_tabs/render_email_panel/render_rag_strip_entry each return without raising (QUAL-03).
    - TestPandasCoW: pd.options.mode.copy_on_write = True at module top; suite runs with zero ChainedAssignmentError.
    - All fixtures synthetic: asset_uuid "00000000-0000-0000-0000-00000000000N"; no real hostnames/IPs/CVE/plugin names (QUAL-05).
  </behavior>
  <implementation>
    Copy the fixture-helper and class scaffolding from tests/test_new_vs_remediated_module.py; extend `_make_snapshot` with the three MTTR fields per PATTERNS.md `_make_snapshot_mttr`. Set `pd.options.mode.copy_on_write = True` before importing the module. Read the exact metrics/metadata keys the Plan 16-02 module actually populates (overall_mttr, cold_start, rag_status, window_days, current_month) from 16-02-SUMMARY.md and the module source — assert against those real keys, not assumed ones.

    Baselines: after the unit suite is green, build a synthetic composed-report-shaped bundle including mttr_trend (synthetic fixed_vulns_df + a 2-snapshot trend_snapshots), call extract_structural_snapshot(bundle, "mttr_trend_test_pull"), write tests/baselines/mttr_trend_test_pull.json via write_baseline(). Build the zero-fixed-findings bundle (_empty_result path) and write tests/baselines/mttr_trend_test_pull_zero_match.json. Commit both. Add an assertion test that re-extracts and compares to the just-written baseline (== [] diff) so the baseline is self-guarding.

    board_summary zero-diff (D-16-10): create tests/test_board_summary_baseline.py that runs board_summary with its standard synthetic fixtures, calls extract_structural_snapshot(bundle, "board_summary_test_pull") for each of the three existing baselines, and asserts compare_snapshots(actual, load_baseline(name)) == []. Do NOT re-write the three board_summary baseline files — a non-zero diff is a FAILURE signal (something outside the allowed change list was modified), per D-16-10 / Pitfall E. If no shared board_summary synthetic-fixture builder exists, reuse the one that originally produced the three baselines (find it via the baseline filenames); if genuinely absent, build the minimal synthetic board_summary bundle the existing baselines imply and document the source in the SUMMARY.
  </implementation>
</feature>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED then GREEN — tests/test_mttr_trend_module.py acceptance suite</name>
  <read_first>
    - tests/test_new_vs_remediated_module.py (primary analog — fixture helpers, class structure, CoW strict-mode setup, four-channel render guards)
    - reports/modules/mttr_trend_module.py (the unit under test — read which metrics/metadata keys compute() populates)
    - .planning/phases/16-mttr-rework/16-02-SUMMARY.md (the real metric/metadata key names + any deviations from the plan)
    - .planning/phases/16-mttr-rework/16-PATTERNS.md (§"tests/test_mttr_trend_module.py" — fixture helpers, criterion-3 test, class list)
    - .planning/phases/16-mttr-rework/16-RESEARCH.md (Validation Architecture table — exact fixtures + assertions per test)
  </read_first>
  <files>tests/test_mttr_trend_module.py</files>
  <action>
    Set `pd.options.mode.copy_on_write = True` before importing MTTRTrendModule. Implement `_make_fixed_vulns(rows)` (defaults state="fixed", severity="critical", asset_uuid synthetic; coerce first_found/resurfaced_date/last_fixed to UTC-aware) and `_make_snapshot_mttr(month, mttr_overall_days, mttr_by_severity, mttr_by_owner, generated_at, **extra)` per PATTERNS.md. Implement all classes from the <behavior> block. The criterion-3 test is the lodestar: assert `data.metrics["overall_mttr"] == 8.0` for the (-200d found, -10d resurfaced, -2d fixed) row with min_sample_size=1; include a comment that the old module path would yield 198. For TestMinSampleThreshold assert the exact substring `"Insufficient data (3 findings — minimum 5 required)"` appears in the rendered Excel/panel output (or the per-severity row note). Use synthetic data only (RFC 5737/6761; asset_uuid "00000000-0000-0000-0000-00000000000N"). Assert against the REAL metric/metadata keys the module populates (read from source, do not assume). Commit the criterion-3 test RED first if the module path is not yet wired, then drive to GREEN.
  </action>
  <verify>
    <automated>python -m pytest tests/test_mttr_trend_module.py -x -W error::FutureWarning</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_mttr_trend_module.py -x` exits 0.
    - Criterion-3 test asserts `overall_mttr == 8.0` (grep the test: `grep -c "8.0" tests/test_mttr_trend_module.py` >= 1) and the test name contains "Criterion3".
    - The min_sample test asserts the literal string `Insufficient data (3 findings — minimum 5 required)`: `grep -c "minimum 5 required" tests/test_mttr_trend_module.py` >= 1.
    - CoW strict mode enabled: `grep -c "copy_on_write = True" tests/test_mttr_trend_module.py` >= 1; suite passes under `-W error::FutureWarning` (zero ChainedAssignmentError).
    - PII-clean: `grep -c "00000000-0000-0000-0000-00000000000" tests/test_mttr_trend_module.py` >= 1; no real IPs (no `192.168.`/`10.0.` outside RFC 5737 ranges) — RFC 5737 (198.51.100.x / 203.0.113.x) only if IPs appear at all.
  </acceptance_criteria>
  <done>All Phase-16 behavioral gates (criterion-3 lodestar, cold-start, min_sample, Owner-drift, tie-break, partial-month, four-channel empty guard, CoW) pass with synthetic fixtures.</done>
</task>

<task type="auto">
  <name>Task 2: Capture mttr_trend structural smoke baselines (populated + zero-match)</name>
  <read_first>
    - tests/baseline_utils.py (extract_structural_snapshot / compare_snapshots / write_baseline / load_baseline signatures)
    - tests/baselines/board_summary_test_pull.json (structural-shape reference for the new baseline files)
    - tests/test_mttr_trend_module.py (reuse its synthetic fixture helpers to build the bundle)
    - .planning/phases/16-mttr-rework/16-RESEARCH.md (Open Item 6 §"New mttr_trend baseline capture" — the 4-step capture procedure)
  </read_first>
  <files>tests/baselines/mttr_trend_test_pull.json, tests/baselines/mttr_trend_test_pull_zero_match.json</files>
  <action>
    Build a synthetic composed-report-shaped bundle that includes mttr_trend (synthetic fixed_vulns_df with >=5 fixed findings across severities/owners + a 2-snapshot trend_snapshots), call `extract_structural_snapshot(bundle, "mttr_trend_test_pull")`, and write `tests/baselines/mttr_trend_test_pull.json` via `write_baseline()`. Build the zero-fixed-findings bundle (empty fixed_vulns_df -> _empty_result / cold-start path), call `extract_structural_snapshot(bundle, "mttr_trend_test_pull_zero_match")`, and write `tests/baselines/mttr_trend_test_pull_zero_match.json`. Add a self-guarding assertion in tests/test_mttr_trend_module.py that re-extracts each bundle and asserts `compare_snapshots(actual, load_baseline(name)) == []`. Only STRUCTURAL keys are captured — no metric values (QUAL-05 + the structural-only baseline contract).
  </action>
  <verify>
    <automated>python -m pytest tests/test_mttr_trend_module.py -x -k baseline</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist: `tests/baselines/mttr_trend_test_pull.json` and `tests/baselines/mttr_trend_test_pull_zero_match.json`.
    - Each is valid JSON containing only the structural keys (no `overall_mttr`/metric-value keys): `python -c "import json; d=json.load(open('tests/baselines/mttr_trend_test_pull.json')); assert 'pdf_page_count' in d and 'overall_mttr' not in d"` exits 0.
    - The self-guarding baseline test passes: `python -m pytest tests/test_mttr_trend_module.py -k baseline` exits 0.
    - No PII in either JSON: no real hostnames/IPs/plugin names (structural-only by construction).
  </acceptance_criteria>
  <done>Two committed structural smoke baselines for mttr_trend (populated + zero-match), each self-guarded by a compare_snapshots == [] assertion.</done>
</task>

<task type="auto">
  <name>Task 3: board_summary zero-diff assertion (D-16-10)</name>
  <read_first>
    - tests/baseline_utils.py (extract_structural_snapshot / compare_snapshots / load_baseline)
    - tests/baselines/board_summary_test_pull.json + board_summary_test_pull_analyst_off.json + board_summary_test_pull_zero_match.json (the three baselines that must survive byte-identical)
    - tests/test_baseline_extractor.py (how board_summary bundles are structurally shaped in tests; find the synthetic board_summary fixture builder used originally)
    - .planning/phases/16-mttr-rework/16-RESEARCH.md (Open Item 6 §"Zero-diff confirmation procedure" + Pitfall E timing)
  </read_first>
  <files>tests/test_board_summary_baseline.py</files>
  <action>
    Create tests/test_board_summary_baseline.py. For each of the three existing board_summary baselines, run board_summary with its standard synthetic fixtures, call `extract_structural_snapshot(bundle, <group_slug>)`, and assert `compare_snapshots(actual, load_baseline(<name>)) == []`. The three keys most likely to drift (pdf_rag_cell_count, excel_tab_names_sorted, email_panel_count) must match. Do NOT re-write the three board_summary baseline JSON files — a non-zero diff is a FAILURE (a scope violation outside the allowed Phase-16 change list), per D-16-10 / Pitfall E. Reuse the synthetic board_summary bundle builder that originally produced the baselines (locate via tests/test_baseline_extractor.py / the baseline filenames). If no reusable builder exists, construct the minimal synthetic board_summary bundle the existing baselines imply and document the source in the SUMMARY. Because mttr_by_severity_module.py is byte-unchanged (Plan 16-02 Task 1 acceptance) and the new capture_snapshot params are not passed by the board_summary code path, this diff MUST be zero.
  </action>
  <verify>
    <automated>python -m pytest tests/test_board_summary_baseline.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_board_summary_baseline.py -x` exits 0 with all three board_summary baselines asserting zero diff.
    - The three board_summary baseline JSON files are byte-unchanged by this plan: `git diff --quiet -- tests/baselines/board_summary_test_pull.json tests/baselines/board_summary_test_pull_analyst_off.json tests/baselines/board_summary_test_pull_zero_match.json` exits 0.
    - The test uses compare_snapshots and load_baseline: `grep -c "compare_snapshots" tests/test_board_summary_baseline.py` >= 1.
    - Full suite green: `python -m pytest tests/ -x` exits 0.
  </acceptance_criteria>
  <done>An automated test proves board_summary structural baselines are byte-identical after Phase 16 (D-16-10 zero-diff), confirming the byte-unchanged mttr_by_severity + the new optional snapshot params caused no regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixtures -> committed repo | Synthetic data persisted in test/baseline files under version control |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-08 | Information Disclosure | committed test fixtures + baselines | mitigate | All fixtures synthetic (RFC 5737/6761; asset_uuid all-zero prefix). Structural baselines capture only the 12 structural keys — never metric values or row data (QUAL-05). grep gate asserts no real IPs/hostnames. |
| T-16-09 | Tampering | board_summary regression undetected | mitigate | Zero-diff assertion test (D-16-10) is the tamper-evidence gate: any structural drift in board_summary (accidental RAG cell, extra tab/panel) fails the build, catching scope violations outside the allowed change list. |
| T-16-10 | Tampering | npm/pip/cargo installs | accept | No package installs — zero new dependencies. No legitimacy gate required. |
</threat_model>

<verification>
- `python -m pytest tests/test_mttr_trend_module.py -x` exits 0 (criterion-3 lodestar + all behavioral gates).
- `python -m pytest tests/test_board_summary_baseline.py -x` exits 0 (D-16-10 zero-diff).
- `python -m pytest tests/ -x` full suite green.
- Both mttr_trend baseline JSONs committed; three board_summary baselines byte-unchanged.
</verification>

<success_criteria>
- The criterion-3 reopened-clock fixture is an automated test asserting ~8 days (not 198).
- min_sample=5 sub-threshold, cold-start, Owner-drift, tie-break, partial-month, and four-channel empty-data guards are all covered.
- board_summary structural baselines are proven byte-identical (D-16-10); new mttr_trend baselines captured.
- All committed fixtures are synthetic (QUAL-05).
</success_criteria>

<output>
Create `.planning/phases/16-mttr-rework/16-03-SUMMARY.md` when done.
</output>
