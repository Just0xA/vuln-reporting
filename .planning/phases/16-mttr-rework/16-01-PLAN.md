---
phase: 16-mttr-rework
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/trend_store.py
  - scripts/capture_trend_snapshot.py
autonomous: true
requirements: [RPT-05]
must_haves:
  truths:
    - "A severity snapshot written by capture_trend_snapshot.py contains mttr_overall_days, mttr_by_severity, and mttr_by_owner keys"
    - "An older snapshot lacking the three MTTR fields is read back as cold-start (snap.get returns None), never a crash (D-16-09)"
    - "mttr_overall_days is the flat sample-weighted mean of days_to_fix over durably-fixed findings in the rolling window (D-16-02 consequence)"
    - "days_to_fix uses (last_fixed - COALESCE(resurfaced_date, first_found)).days clipped >=0 (D-16-02)"
    - "A failure computing the MTTR aggregate does not abort the severity snapshot (fail-soft); the three fields write null"
  artifacts:
    - path: "data/trend_store.py"
      provides: "capture_snapshot() accepts mttr_overall_days/mttr_by_severity/mttr_by_owner optional kwargs and writes them into new_entry"
      contains: "mttr_overall_days"
    - path: "scripts/capture_trend_snapshot.py"
      provides: "Rolling-window MTTR aggregate computation passed to capture_snapshot()"
      contains: "mttr_by_owner"
  key_links:
    - from: "scripts/capture_trend_snapshot.py"
      to: "data/trend_store.py capture_snapshot()"
      via: "mttr_overall_days/mttr_by_severity/mttr_by_owner kwargs"
      pattern: "mttr_overall_days="
---

<objective>
Extend the S1 snapshot store to persist a forward-accumulating rolling-30-day MTTR aggregate (overall + per-severity + per-Owner) as PII-safe floats, and wire `scripts/capture_trend_snapshot.py` to compute and pass that aggregate using the D-16-02 reopened-aware date-math clock. This is the data-persistence foundation the MoM trend in `mttr_trend` reads from; the ~29-day Tenable fixed-retention wall means MTTR history cannot be reconstructed — it must accumulate forward from the first snapshot (D-16-03).

Purpose: Without persisted MTTR snapshots, the new module has no MoM source. RPT-05's month-over-month trend is precisely snapshot-over-snapshot rolling-30-day MTTR.
Output: Two modified files; snapshots now carry `mttr_overall_days`, `mttr_by_severity`, `mttr_by_owner`. Backward-compatible (absent field => cold-start, no `schema_version`, D-16-09).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-mttr-rework/16-CONTEXT.md
@.planning/phases/16-mttr-rework/16-RESEARCH.md
@.planning/phases/16-mttr-rework/16-PATTERNS.md

<interfaces>
<!-- Extracted from data/trend_store.py — capture_snapshot current signature + new_entry shape. -->
<!-- Executor implements against these directly; PATTERNS.md §"data/trend_store.py" + RESEARCH.md Open Item 1 carry the exact extension. -->

data/trend_store.py:
- capture_snapshot() defined at line 222; existing optional kwargs end with `fixed_vulns_df: Optional[pd.DataFrame] = None` (line 234).
- The `new_entry: dict` literal is built at line 358; existing tail keys include `"new_findings_count"`, `"fixed_findings_count"`, then `"generated_at"`.
- fixed_findings_count / new_findings_count are already derived from `fixed_vulns_df` at lines 339-356 (last_fixed coerce + state upper-case pattern to mirror).

scripts/capture_trend_snapshot.py:
- `fixed_vulns_df` is already fetched (fail-soft) and `extract_owner` already imported for the existing owner-dimension snapshot call.
- The severity-dimension `capture_snapshot(df, assets_df, snapshot_date, "severity", "all_assets", ...)` call already passes `reopened_count`, `accepted_count`, `recast_count`, `fixed_vulns_df`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add three optional MTTR kwargs to capture_snapshot() and write them into new_entry</name>
  <read_first>
    - data/trend_store.py (the file being modified — read capture_snapshot() lines 222-392 in full: signature, docstring, the fixed_findings derivation block ~339-356, and the new_entry dict ~358-385)
    - .planning/phases/16-mttr-rework/16-RESEARCH.md (Open Item 1 — exact field types, before/after snapshot record, implicit-null convention)
    - .planning/phases/16-mttr-rework/16-PATTERNS.md (§"data/trend_store.py — modify capture_snapshot()" — verbatim signature + new_entry extension)
  </read_first>
  <files>data/trend_store.py</files>
  <action>
    Append three keyword-only optional params to `capture_snapshot()` after `fixed_vulns_df`, defaulting to None: `mttr_overall_days: Optional[float] = None`, `mttr_by_severity: Optional[dict] = None`, `mttr_by_owner: Optional[dict] = None`. Add them to the `new_entry` dict literal immediately before the `"generated_at"` key, storing the value verbatim (explicit `None` => JSON `null`; never omit the key — this is the D-16-09 implicit-optional-field convention so `snap.get("mttr_overall_days")` returns None unambiguously on both old and new snapshots, never KeyError). Extend the docstring's Parameters section with one line per new field describing: `mttr_overall_days` = sample-weighted mean days_to_fix over durably-fixed findings in the rolling window (None when sample below threshold); `mttr_by_severity` = dict[str,float|null] keyed critical/high/medium/low; `mttr_by_owner` = dict[str,float|null] keyed by internal Owner tag name, empty dict when no fixed findings. Do NOT add a `schema_version` field. Do NOT add conditional logic — the caller computes the values; trend_store only stores them. Match the existing Phase-15 pattern (on_time_asset_count etc. are stored as explicit None when absent).
  </action>
  <verify>
    <automated>python -c "import data.trend_store as t, inspect; p=inspect.signature(t.capture_snapshot).parameters; assert 'mttr_overall_days' in p and 'mttr_by_severity' in p and 'mttr_by_owner' in p, list(p); assert p['mttr_overall_days'].default is None"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import inspect, data.trend_store as t; assert {'mttr_overall_days','mttr_by_severity','mttr_by_owner'} <= set(inspect.signature(t.capture_snapshot).parameters)"` exits 0
    - grep confirms all three keys appear in the new_entry dict body: `grep -v '^\s*#' data/trend_store.py | grep -c '"mttr_overall_days":'` >= 1 (same for the other two keys)
    - No `schema_version` token introduced: `grep -c "schema_version" data/trend_store.py` == 0
  </acceptance_criteria>
  <done>capture_snapshot() accepts the three new optional kwargs and writes them as explicit JSON values (null when not passed) into every severity snapshot entry; no schema_version; existing callers unaffected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compute rolling-window MTTR aggregate in capture_trend_snapshot.py and pass it through</name>
  <read_first>
    - scripts/capture_trend_snapshot.py (the file being modified — read the fixed_vulns_df fetch, the existing severity capture_snapshot() call, and the owner-dimension snapshot block with its WR-03 fail-soft exit handling)
    - .planning/phases/16-mttr-rework/16-PATTERNS.md (§"scripts/capture_trend_snapshot.py — modify main()" — the full MTTR aggregate compute block + the extended capture_snapshot call)
    - .planning/phases/16-mttr-rework/16-RESEARCH.md (Open Item 3 dtype/tz pitfalls; Pitfall C rolling-window-applied-in-capture-script-not-module; Pitfall A .where() dtype safety)
    - reports/modules/board_report_utils.py (extract_owner — consumed read-only for the per-Owner cut; do NOT modify)
  </read_first>
  <files>scripts/capture_trend_snapshot.py</files>
  <action>
    After the `fixed_vulns_df` fetch and before the severity-dimension `capture_snapshot()` call, insert a fail-soft MTTR aggregate computation block. Initialize `mttr_overall_days = mttr_by_severity = mttr_by_owner = None`. When `fixed_vulns_df` is not None and not empty, inside a `try/except Exception` (on except: log a `logger.warning("MTTR aggregate computation failed — fields will cold-start: %s", exc)` and leave all three as None — the severity snapshot must still succeed, mirroring the WR-03 fail-soft pattern): (1) `window_days = 30`; (2) D-16-01 durably-fixed filter: `state_upper = fixed_vulns_df["state"].astype(str).str.upper()`, keep `state_upper == "FIXED"`; (3) rolling-window mask: `last_fixed` coerced via `pd.to_datetime(..., utc=True, errors="coerce")` >= `pd.Timestamp(snapshot_date, tz="UTC") - pd.Timedelta(days=window_days)`; (4) D-16-02 COALESCE clock: build `_nat` object series, coerce `last_fixed`/`first_found`/`resurfaced_date` all with `utc=True, errors="coerce"`, `clock_start = resurfaced_ts.where(resurfaced_ts.notna(), other=first_found_ts)`, `days_col = (last_fixed_ts - clock_start).dt.days.clip(lower=0)`, attach via `.assign(days_to_fix=days_col)` (CoW: `.assign()` only — never `df["col"]=` after a filter), then drop rows where days_to_fix is NaN or <0; (5) when the windowed frame is non-empty, `mttr_overall_days = round(float(windowed["days_to_fix"].mean()), 2)` (flat mean = sample-weighted overall per D-16-02 consequence); (6) per-severity dict over critical/high/medium/low, each value `round(mean,2)` only when `len(sev_df) >= MIN_SAMPLE` (MIN_SAMPLE = 5) else None; (7) per-Owner dict via `extract_owner(assets_df)` -> dedup asset_uuid->owner map -> `.map(...).fillna("Unassigned")`, group by owner, each value `round(mean,2)` only when `len(grp) >= 5` else None. Then extend the existing severity `capture_snapshot(...)` call with `mttr_overall_days=mttr_overall_days, mttr_by_severity=mttr_by_severity, mttr_by_owner=mttr_by_owner`. Log a single info line summarizing overall + by_owner keys (PII-safe: keys only, no row data). Keep all date math UTC-aware (subtracting tz-naive from tz-aware raises TypeError in pandas 2.2+).
  </action>
  <verify>
    <automated>python scripts/capture_trend_snapshot.py --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - `python scripts/capture_trend_snapshot.py --dry-run` exits 0 (no MTTR-related exception in the log)
    - `python -m py_compile scripts/capture_trend_snapshot.py` exits 0
    - The capture_snapshot severity call passes all three kwargs: `grep -c "mttr_overall_days=mttr_overall_days" scripts/capture_trend_snapshot.py` >= 1
    - Filter uses durably-fixed state, not the `last_fixed.notna()` clause: `grep -c '== "FIXED"' scripts/capture_trend_snapshot.py` >= 1
    - CoW-compliant column attach: `grep -c "assign(days_to_fix" scripts/capture_trend_snapshot.py` >= 1; no chained-assignment FutureWarning under `python -W error::FutureWarning -c "import scripts.capture_trend_snapshot"`
  </acceptance_criteria>
  <done>capture_trend_snapshot.py computes the rolling-30-day reopened-aware MTTR aggregate (overall sample-weighted + per-severity + per-Owner, min_sample 5) and passes it to capture_snapshot(); a computation failure is fail-soft (fields cold-start to null, snapshot still written).</done>
</task>

<task type="auto">
  <name>Task 3: Backward-compat smoke check — old snapshot reads back as cold-start</name>
  <read_first>
    - data/trend_store.py (read the `read_trend()` return contract `{snapshots, insufficient_data}` and any `if __name__ == "__main__":` smoke block already present)
    - .planning/phases/16-mttr-rework/16-RESEARCH.md (Open Item 1 "Cold-start rule" + "Backward-compat proof"; D-16-09 verify-with-a-trend-store-smoke-check instruction)
  </read_first>
  <files>data/trend_store.py</files>
  <action>
    Confirm (and if the module's `if __name__ == "__main__":` smoke block does not already exercise it, add a minimal assertion there) that a snapshot dict written WITHOUT the three MTTR fields reads back such that `snap.get("mttr_overall_days") is None`, `(snap.get("mttr_by_severity") or {})` is an empty dict, and `(snap.get("mttr_by_owner") or {})` is an empty dict — i.e. an older snapshot is a valid cold-start for the MTTR series, never a KeyError. Use the `(snap.get(...) or {})` access form, NOT `snap["mttr_by_owner"][owner]` (which raises KeyError on old snapshots and TypeError on None — Pitfall B). Keep the change confined to the smoke block / a docstring note; do not alter read_trend()'s logic. This is the D-16-09 verification the CONTEXT mandates at plan time.
  </action>
  <verify>
    <automated>python data/trend_store.py</automated>
  </verify>
  <acceptance_criteria>
    - `python data/trend_store.py` exits 0 (smoke block green)
    - Inline assertion proves the cold-start access: `python -c "d={'month':'2026-05'}; assert d.get('mttr_overall_days') is None; assert (d.get('mttr_by_owner') or {})=={}"` exits 0
    - read_trend() logic is unchanged except for smoke-block/docstring additions (git diff shows no edits inside the read_trend body)
  </acceptance_criteria>
  <done>A pre-Phase-16 snapshot lacking the MTTR fields is provably a valid cold-start on read-back (no KeyError, no crash); trend_store smoke block green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tenable API -> capture_trend_snapshot.py | Fixed-vuln export (severity, state, dates, asset_uuid) crosses into the process; untrusted in shape but read-only |
| Process -> data/trend/*.json snapshot store | Aggregate MTTR floats + internal Owner tag names persisted to local disk (gitignored per the 2026-05-14 scrub) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Information Disclosure | snapshot store payload | mitigate | Only aggregate floats + internal Owner tag names persisted (mttr_overall_days/mttr_by_severity/mttr_by_owner). No hostnames/IPs/plugin names/asset-level rows. Within the established PII boundary (aggregate + internal tag names OK; project_pii_rule_is_ai_not_email). `data/trend/` is gitignored — never committed, never emailed at row level. |
| T-16-02 | Tampering | snapshot integrity | accept | Forward-accumulating store, no schema_version, local-disk only. A tampered prior snapshot can skew a MoM delta but cannot crash the reader (cold-start guard). Low-value local target; rationale documented. |
| T-16-03 | Denial of Service | MTTR aggregate compute | mitigate | Fail-soft try/except around the entire MTTR block — a malformed export (bad dtypes, NaT storm) logs a warning and writes null fields; the severity snapshot still completes (fail-soft batch semantics). |
| T-16-04 | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — zero new dependencies (stdlib + already-pinned pandas only). No legitimacy gate required. |
</threat_model>

<verification>
- `python data/trend_store.py` smoke block exits 0.
- `python scripts/capture_trend_snapshot.py --dry-run` exits 0 with an info line reporting the computed MTTR aggregate.
- `python -m pytest tests/ -x` (existing suite) stays green — no regression to the Phase-15 snapshot consumers (new_vs_remediated / vuln_density / accepted_recast / management_summary).
</verification>

<success_criteria>
- The three MTTR fields are present in every newly written severity snapshot (explicit null when uncomputed).
- An old snapshot (no MTTR fields) reads back as cold-start, never KeyError (D-16-09).
- The aggregate uses the D-16-02 reopened-aware clock and the durably-fixed (state==FIXED) population (D-16-01), with min_sample 5 nulling sparse cuts.
- MTTR computation failure is fail-soft; the severity snapshot is still written.
</success_criteria>

<output>
Create `.planning/phases/16-mttr-rework/16-01-SUMMARY.md` when done.
</output>
