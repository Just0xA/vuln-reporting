---
phase: 16-mttr-rework
plan: 04
type: execute
wave: 4
depends_on: ["16-02"]
files_modified:
  - reports/modules/mttr_trend_module.py
  - delivery_config.yaml
autonomous: true
requirements: [RPT-05]
gap_closure: true
must_haves:
  truths:
    - "D-16-11: `mttr_trend` reads `config.options.get('mttr_view', 'owner')` and whitelists the value to {owner, severity, both} with an unknown value falling back to the 'owner' default (no crash, no interpolation into any sink)"
    - "D-16-11: the single combined `table_data` breakdown is split into two independent groups — a Severity table/section and an Owner table/section, each with its own header — across PDF, Excel, and email panel"
    - "D-16-12: with `mttr_view` unset the module renders Owner-only (no severity rows) in PDF, Excel, and email panel — exec headline default that fits one page"
    - "`mttr_view=severity` renders only the Severity table; `mttr_view=both` renders both as two distinct tables (not one concatenated list)"
    - "Latent SLA bug fixed: the Owner table/section no longer emits the hard-coded Critical-SLA (15) value as an 'SLA Target' — that column is dropped or relabelled for the Owner cut (~:605 anchor removed from the Owner-facing render)"
    - "The analyst-detail tab (CONTRACT-02 `render_analyst_tabs`) MAY retain full detail (both cuts) regardless of `mttr_view`, since it is drill-down, not a headline channel"
    - "Out of scope held: MTTR math, rolling window, MoM line, and `mttr_by_severity_module.py` are byte-unchanged (D-16-10); no changes to `board_summary`"
    - "`delivery_config.yaml` documents `module_options.mttr_view` as a settable per-group option on the composed-report `mttr_trend` example"
  artifacts:
    - path: "reports/modules/mttr_trend_module.py"
      provides: "mttr_view selector + split Severity/Owner render across PDF/Excel/email; Owner SLA-basis fix"
      contains: "mttr_view"
    - path: "delivery_config.yaml"
      provides: "Documented module_options.mttr_view example on the composed-report mttr_trend group"
      contains: "mttr_view"
  key_links:
    - from: "reports/modules/mttr_trend_module.py compute()"
      to: "config.options.get('mttr_view', 'owner')"
      via: "options read mirroring mttr_window_days / min_sample_size (~:313-314)"
      pattern: "options.get\\(\"mttr_view\""
    - from: "reports/modules/mttr_trend_module.py render_pdf_section/render_excel_tabs/render_email_panel"
      to: "split Severity / Owner table groups"
      via: "view-gated section emission"
      pattern: "mttr_view"
---

<objective>
Close the Phase 16 UAT gap (locked as D-16-11 / D-16-12): the `mttr_trend` module currently builds ONE combined `table_data` list (severity rows ~:574-592, Owner rows ~:595-611) that feeds the PDF table (~:826), the Excel "MTTR Trend" tab (~:954), and the analyst tab (~:648), producing a confusing "Severity / Owner" mixed table that bleeds onto a second page. Split the breakdown into two independent sections (Severity / Owner) and add a `mttr_view ∈ {owner, severity, both}` module option (default `owner`) applied consistently across PDF + Excel + email panel. Fix the latent Owner SLA-basis bug where Owner rows hard-code Critical SLA (15) as their "SLA Target" (~:605).

Purpose: Deliver an audience-appropriate, single-page MTTR breakdown — exec leadership gets the Owner cut by default; groups scoped to a single owner set `mttr_view: severity`.
Output: A view-aware `mttr_trend_module.py` (compute + three headline render channels) and a documented `module_options.mttr_view` example in `delivery_config.yaml`. MTTR math, window, MoM line, and `board_summary` are untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-mttr-rework/16-CONTEXT.md
@.planning/phases/16-mttr-rework/16-UAT.md
@CLAUDE.md

<interfaces>
<!-- The module under change. Key contracts the executor must preserve. -->

reports/modules/mttr_trend_module.py — current shape (read in full before editing):
- compute() reads options at ~:313-314: `window_days = int(config.options.get("mttr_window_days", 30))`,
  `min_sample = int(config.options.get("min_sample_size", 5))`. Add `mttr_view` read here using the SAME pattern.
- The combined breakdown is built at ~:574-611: severity rows appended first (loop over `_SEVERITIES`),
  then Owner rows appended (loop over `sorted(owner_mttr.keys())`). Owner rows hard-code
  `"sla_days": SLA_DAYS["critical"]` at ~:605 (the latent bug).
- PDF render (render_pdf_section) iterates `data.table_data` at ~:826 into one table with header
  "Severity / Owner" and an "SLA Target" column (~:873-880).
- Excel render (render_excel_tabs) iterates `data.table_data` at ~:954 into one "MTTR Trend" tab with
  a "SLA Target (Days)" column header (~:939-947).
- Email panel (render_email_panel) currently renders only the overall MTTR headline (no per-row table) —
  ~:1036-1051. The split applies to whichever per-row breakdown the panel shows; if the panel does not
  enumerate rows today, the view selector still governs which (if any) breakdown summary it surfaces —
  keep email-client-safe inline CSS (no <style> blocks) per CLAUDE.md.
- Analyst tab (render_analyst_tabs / analyst_rows built ~:627-650) — MAY keep both cuts (drill-down).
- validate_config (~:1101) already validates mttr_window_days / min_sample_size; extend it to validate mttr_view.

Empty-data guard (CLAUDE.md): use safe_pct / safe_int / safe_format for any possibly-None value;
return safe defaults, never raise. Cold-start path (metrics["cold_start"]) must still render unchanged.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add view-aware split breakdown in compute() + Owner SLA-basis fix</name>
  <read_first>
    - reports/modules/mttr_trend_module.py (the file under change — read compute() ~:239-702 in full, esp. the options read ~:313-314, the combined table_data build ~:574-611, and the Owner SLA hard-code ~:605)
    - .planning/phases/16-mttr-rework/16-UAT.md (the `## Gaps` section — the authoritative locked D-16-11/D-16-12 spec with line numbers)
    - .planning/phases/16-mttr-rework/16-CONTEXT.md (D-16-05 channel layout, D-16-07 sparse/omit rules, D-16-10 out-of-scope guard)
    - CLAUDE.md (four-channel render contract; empty-data guard with safe_pct/safe_int/safe_format)
  </read_first>
  <files>reports/modules/mttr_trend_module.py</files>
  <action>
    In compute(), add `mttr_view = str(config.options.get("mttr_view", "owner")).lower().strip()` alongside the existing window_days / min_sample reads (~:313-314). Whitelist it: if `mttr_view not in {"owner", "severity", "both"}`, log a WARNING and fall back to `"owner"` (the D-16-12 default). NEVER interpolate the raw value into any HTML/Excel sink — only the validated token reaches render branches.

    Stop building ONE combined `table_data` list. Instead build TWO independent lists on the ModuleData (e.g. `severity_table` and `owner_table`) carried via new `metrics` or `metadata` keys (e.g. `metadata["mttr_view"]`, plus `table_data_severity` / `table_data_owner` — choose keys consistent with how the module already stashes table rows; do NOT break the cold-start ModuleData shape). Severity rows keep per-severity `sla_days = SLA_DAYS[sev]`. Owner rows MUST NOT carry the hard-coded `SLA_DAYS["critical"]` anchor as an SLA Target — drop the `sla_days` field from Owner rows (or set it to None and have renderers omit/relabel it). Preserve all other Owner fields (label, mttr_days, variance→reconsider: variance vs Critical SLA is also meaningless for Owner, so drop/relabel Owner variance too if it derived from the Critical anchor; keep sample_size and mom_delta which are SLA-independent).

    Persist the validated `mttr_view` into `metadata["mttr_view"]` so all three headline renderers read one source of truth. Keep `analyst_rows` (CONTRACT-02) building BOTH cuts regardless of mttr_view — analyst detail stays full per the locked spec. Do NOT change the MTTR math, the rolling window, the MoM series, the RAG strip, or the cold-start branch. Use .assign() (never df[col]=) for any pandas mutation (CoW). Use safe_* helpers for any None-able value.

    Extend validate_config (~:1101) to validate `mttr_view`: if present and not in {owner, severity, both}, append a clear error string (mirror the existing per-key error format).
  </action>
  <verify>
    <automated>python -c "import reports.modules.mttr_trend_module as m; import pandas as pd; pd.options.mode.copy_on_write=True; from reports.modules.base import ModuleConfig; mod=m.MTTRTrendModule(); print('mttr_view' in mod.validate_config.__doc__ or True)"</automated>
  </verify>
  <acceptance_criteria>
    - `config.options.get("mttr_view", "owner")` is read in compute(): `grep -c "options.get(\"mttr_view\"" reports/modules/mttr_trend_module.py` >= 1.
    - The whitelist set is present: `grep -c "owner" reports/modules/mttr_trend_module.py` and a literal membership check against {owner, severity, both} exists (grep for `"severity"` and `"both"` near the mttr_view read).
    - The Owner-row build no longer assigns `SLA_DAYS["critical"]` as an SLA Target: `grep -n "SLA_DAYS\[.critical.\]" reports/modules/mttr_trend_module.py` shows the Critical SLA is used ONLY for the RAG anchor / disclosure text, NOT inside the Owner per-row dict (the ~:605 `"sla_days": SLA_DAYS["critical"]  # anchor` line is removed/changed).
    - compute() exposes the validated view to renderers via metadata: `grep -c "mttr_view" reports/modules/mttr_trend_module.py` >= 4 (read, whitelist, metadata stash, validate_config).
    - validate_config rejects a bad value: a config with `options={"mttr_view": "bogus"}` returns a non-empty error list (assert via a one-liner python invocation, or covered by the 16-05 test).
    - Module still imports and auto-discovers: `python -c "import reports.modules; import reports.modules.registry as r; assert 'mttr_trend' in r._modules"` exits 0.
    - mttr_by_severity_module.py byte-unchanged: `git diff --quiet -- reports/modules/mttr_by_severity_module.py` exits 0.
  </acceptance_criteria>
  <done>compute() reads + whitelists mttr_view (default owner), builds two independent Severity/Owner breakdowns, drops the meaningless Critical-SLA anchor from Owner rows, stashes the validated view in metadata, and keeps analyst_rows full. validate_config validates mttr_view. MTTR math/window/MoM/RAG/cold-start unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the view selector across PDF, Excel, and email-panel renders</name>
  <read_first>
    - reports/modules/mttr_trend_module.py (render_pdf_section ~:715-893, render_excel_tabs ~:899-988, render_email_panel ~:994-1051 — read all three in full as edited by Task 1)
    - .planning/phases/16-mttr-rework/16-UAT.md (`## Gaps` — "Apply the selected view consistently across PDF + Excel + email panel")
    - CLAUDE.md (email-client compatibility: inline CSS only, no <style> blocks, base64 CID charts; four-channel render contract)
  </read_first>
  <files>reports/modules/mttr_trend_module.py</files>
  <action>
    Read `mttr_view = data.metadata.get("mttr_view", "owner")` at the top of each headline renderer (PDF, Excel, email panel). Branch render output on it:
    - `owner` (default): emit ONLY the Owner section/table (its own header, e.g. "MTTR by Owner"); no Severity rows.
    - `severity`: emit ONLY the Severity section/table (its own header, e.g. "MTTR by Severity") with per-severity SLA Target column intact.
    - `both`: emit TWO distinct, separately-headed sections/tables — Severity first, then Owner — NOT one concatenated table under a mixed "Severity / Owner" header.

    PDF (render_pdf_section ~:826-885): replace the single "Severity / Owner" table with view-gated section(s). The Owner table MUST drop or relabel the "SLA Target" column (per Task 1 the Owner rows no longer carry a meaningful SLA anchor) — drop the column for the Owner table; keep it for the Severity table. Update the trailing explanatory-text (~:886-892) so it no longer claims "Owner rows use Critical SLA as the anchor" — instead state the Owner cut shows velocity (MTTR + MoM delta) without an SLA anchor. The per-severity GAUGES (~:770-822) are a Severity-channel visual — render them only for `severity` and `both`, not for `owner` (owner default = single Owner table, fits one page).

    Excel (render_excel_tabs ~:899-988): keep tab name "MTTR Trend". Under `owner`, write only Owner rows and a header set WITHOUT the meaningless "SLA Target (Days)" column. Under `severity`, write only Severity rows with the full header. Under `both`, write the Severity block then a blank row then the Owner block (or two clearly-headed regions in the one tab) — distinct, not interleaved.

    Email panel (render_email_panel ~:994-1051): the overall MTTR headline stays. If/where the panel surfaces a per-row breakdown, gate it on mttr_view the same way; keep inline CSS only (no <style>), Outlook/Gmail/Apple-Mail safe. Cold-start and error branches stay exactly as-is.

    Do not touch render_analyst_tabs (CONTRACT-02) — it keeps both cuts.
  </action>
  <verify>
    <automated>python -c "import pandas as pd; pd.options.mode.copy_on_write=True; import reports.modules.mttr_trend_module as m; from reports.modules.base import ModuleConfig; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Each headline renderer reads the view: `grep -c "metadata.get(\"mttr_view\"" reports/modules/mttr_trend_module.py` >= 3 (PDF, Excel, email panel).
    - PDF header is no longer the fixed mixed string: `grep -c "Severity / Owner" reports/modules/mttr_trend_module.py` == 0 in the PDF <th>/analyst-agnostic render path (the mixed header is replaced by per-view headers like "MTTR by Owner" / "MTTR by Severity"). NOTE: the analyst-tab column name "Severity / Owner" at ~:629 MAY remain since analyst detail is full — restrict the count assertion to the render_pdf_section/render_excel_tabs bodies (verify by reading, not a blanket grep).
    - The Owner render path drops the SLA Target column: the Owner-only Excel/PDF output contains no "SLA Target" header (assert via the 16-05 tests: owner-view excel headers list does not include "SLA Target (Days)").
    - render_analyst_tabs is unchanged from Task 1 (still returns both cuts): confirmed by reading the method body; analyst tab list length unchanged.
    - Module imports clean and renders without raising on cold-start (covered end-to-end by 16-05 tests).
    - mttr_by_severity_module.py byte-unchanged: `git diff --quiet -- reports/modules/mttr_by_severity_module.py` exits 0.
  </acceptance_criteria>
  <done>PDF, Excel, and email-panel renders honor mttr_view: owner-only by default (single Owner table, no meaningless SLA Target column, no severity gauges), severity-only, or both-as-two-distinct-tables. Analyst tab keeps full detail. Cold-start/error branches unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Document module_options.mttr_view in delivery_config.yaml</name>
  <read_first>
    - delivery_config.yaml (the "Custom Composed Report — example" group ~:69-88 that already references `mttr_trend` and shows the commented `module_options:` block ~:77-79)
    - CLAUDE.md (Delivery Configuration — module_options is a per-module options dict on composed_report groups)
    - .planning/phases/16-mttr-rework/16-UAT.md (`## Gaps` — settable per group as module_options)
  </read_first>
  <files>delivery_config.yaml</files>
  <action>
    On the "Custom Composed Report — example" group (the one with `reports: [composed_report]` and `modules: [mttr_trend]`), add a documented `module_options:` example showing `mttr_trend: {mttr_view: owner}` with an inline comment listing the allowed values `{owner, severity, both}` and noting the default is `owner` (D-16-12). Keep it as a real, valid example (uncommented, since this group is a live example group) OR a clearly-commented illustrative block consistent with the existing commented `module_options` convention on that group — match the surrounding style. Do NOT change any other group, schedule, recipient, or filter. This is documentation/surface only — no schema-validation rule changes required (module_options is already a permissive pass-through per CLAUDE.md).
  </action>
  <verify>
    <automated>python -c "import yaml; d=yaml.safe_load(open('delivery_config.yaml')); assert isinstance(d['groups'], list); print('yaml valid')"</automated>
  </verify>
  <acceptance_criteria>
    - delivery_config.yaml documents mttr_view: `grep -c "mttr_view" delivery_config.yaml` >= 1.
    - The allowed-values and default are documented in a comment: `grep -ci "owner" delivery_config.yaml` >= 1 and the comment names {owner, severity, both}.
    - YAML still parses: the verify command exits 0.
    - `python run_all.py --dry-run` does not regress on the edited group (config validation passes). If `--dry-run` requires live credentials, instead assert the YAML loads and the edited group still has its original required keys (name, schedule, reports, email).
    - Only the one example group changed: `git diff delivery_config.yaml` shows no edits to the "Test Pull", "Test Pull — Analyst Off", "Test Pull — Zero Match", or "Tag Severity & Type Profile — example" groups.
  </acceptance_criteria>
  <done>The composed-report mttr_trend example group documents module_options.mttr_view with allowed values {owner, severity, both} and the owner default; no other group is touched; YAML parses.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| delivery_config.yaml (operator-supplied) → mttr_trend render branches | `mttr_view` is operator config read via `config.options.get`; it selects a render path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-13 | Tampering | `mttr_view` operator config | mitigate | Whitelist to {owner, severity, both} with a safe `owner` default; unknown values log a WARNING and fall back — never branch on or interpolate the raw value into any HTML/Excel sink. validate_config rejects bad values at startup (`--dry-run`). |
| T-16-14 | Information Disclosure | split Owner/Severity render | accept | No new data source, no new I/O, no credentials. Same already-fetched aggregate MTTR data; analyst-detail PII boundary (aggregate-only, QUAL-05) is unchanged. |
| T-16-15 | Tampering | board_summary / mttr_by_severity regression | mitigate | mttr_by_severity_module.py asserted byte-unchanged (`git diff --quiet`); board_summary untouched. The 16-05 zero-diff baseline test re-confirms (D-16-10). |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | No package installs — zero new dependencies. No legitimacy gate required. |

No high-severity threats. The change is a config-driven view selector over already-fetched aggregate data with no new external I/O.
</threat_model>

<verification>
- `python -c "import reports.modules; import reports.modules.registry as r; assert 'mttr_trend' in r._modules"` exits 0.
- `git diff --quiet -- reports/modules/mttr_by_severity_module.py` exits 0 (byte-unchanged, D-16-10).
- `grep -c "options.get(\"mttr_view\"" reports/modules/mttr_trend_module.py` >= 1.
- delivery_config.yaml parses; only the example group changed.
- Full behavioral proof is the 16-05 TDD plan (owner/severity/both/default/Owner-SLA-drop/single-page).
</verification>

<success_criteria>
- mttr_view selector (default owner) splits the breakdown into independent Severity and Owner sections across PDF + Excel + email panel.
- Owner cut no longer shows the meaningless hard-coded Critical-SLA "SLA Target".
- Analyst-detail tab retains both cuts.
- MTTR math, window, MoM line, RAG strip, cold-start, and board_summary / mttr_by_severity are unchanged.
- delivery_config.yaml documents the new module_options.mttr_view surface.
</success_criteria>

<output>
Create `.planning/phases/16-mttr-rework/16-04-SUMMARY.md` when done.
SUMMARY MUST call out the backward-compat note (D-16-12): any existing composed group already referencing `mttr_trend` switches from the old combined breakdown to owner-only by default; groups wanting the severity cut must set `module_options.mttr_view: severity` (or `both`).
</output>
