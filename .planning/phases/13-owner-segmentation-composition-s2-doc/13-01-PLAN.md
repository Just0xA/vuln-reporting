---
phase: 13-owner-segmentation-composition-s2-doc
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - reports/modules/board_report_utils.py
  - tests/unit/test_owner_segmentation.py
autonomous: true
requirements: [SEG-01, SEG-02, SEG-04]
must_haves:
  truths:
    - "extract_owner returns per-Owner buckets whose counts sum to total row count"
    - "Assets with no Owner tag land in a single Unassigned bucket (configurable label)"
    - "extract_owner does not raise when the tags column is absent — all rows Unassigned"
    - "extract_owner also populates an application column for the nested drill-down"
    - "compute_per_bu_breakdown output column is owner, not business_unit"
  artifacts:
    - path: "reports/modules/board_report_utils.py"
      provides: "Owner-primary generalized helper (extract_owner, OWNER_TAG_CATEGORY, owner-column breakdown/risk)"
      contains: "def extract_owner"
    - path: "tests/unit/test_owner_segmentation.py"
      provides: "Unit tests for SEG-01/02/04 (reconcile-to-whole, Unassigned catch-all, missing-column fail-soft)"
      contains: "def test_"
  key_links:
    - from: "reports/modules/board_report_utils.py extract_owner"
      to: "owner + application columns on assets_df"
      via: "single-pass tag parser"
      pattern: "owner.*application"
    - from: "compute_per_bu_breakdown"
      to: "owner output column"
      via: "rename(columns={bu_column: \"owner\"})"
      pattern: "owner"
---

<objective>
Generalize the shared board helper `reports/modules/board_report_utils.py` from the interim `Application`-tag "Business Unit" grouping to be **Owner-primary and tag-category-parameterized** (D-01, D-03), rename the grouping column `business_unit` → `owner` (D-04), keep `Application` as a separate parsed column for nested drill-down (D-05), and standardize the catch-all label from `"Untagged"` to a configurable `"Unassigned"` (D-06) with fail-soft behavior when the tag is absent (D-07). Promote the `Owner` parsing logic that currently lives privately in `critical_remediation_sla_module.py` into this shared helper as the single source of truth (D-03).

Purpose: This is the load-bearing foundation of Phase 13 — the four consumer modules (Plan 02) and the supplemental/trend composition (Plan 03) all consume the generalized helper. Nothing downstream can land until the helper exposes `extract_owner`, the `owner` column, and the `Unassigned` label.

Output: Generalized `board_report_utils.py`; new `tests/unit/test_owner_segmentation.py` proving SEG-01/02/04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-owner-segmentation-composition-s2-doc/13-CONTEXT.md
@.planning/phases/13-owner-segmentation-composition-s2-doc/13-RESEARCH.md
@.planning/phases/13-owner-segmentation-composition-s2-doc/13-PATTERNS.md

<interfaces>
<!-- Current shape of board_report_utils.py the executor generalizes IN PLACE. Source: 13-PATTERNS.md §board_report_utils.py, 13-RESEARCH.md §Critical Code Surfaces. -->

Current constants (board_report_utils.py §47-48):
  BU_TAG_CATEGORY: str = "Application"   →  becomes OWNER_TAG_CATEGORY = "Owner"

Current extract_business_unit (§206-280): single-category parser, returns "Untagged" on no-match,
  sets df["business_unit"]. Inner closure _bu_from_tags matches BU_TAG_CATEGORY.casefold().

Current compute_per_bu_breakdown (§287, rename at §370):
  signature compute_per_bu_breakdown(df, num_mask, den_mask, bu_column="business_unit", ...)
  ALWAYS does .rename(columns={bu_column: "business_unit"})  ← gotcha: must become "owner"

Current compute_bu_risk_scores (§407): references "business_unit" at §460 (bu_map slice) and §479 (groupby).

Private parser to PROMOTE — critical_remediation_sla_module.py §976-991 _extract_owner_tag:
  parses "Owner" from "Cat=Val;Cat=Val", returns "" on no-match. The promoted version must
  return the configurable unassigned_label (default "Unassigned"), NOT "" (Pitfall 3).

Test fixture analog — tests/content/test_trend_store.py hand-built DataFrame pattern.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Write failing SEG-01/02/04 unit tests (Wave 0 scaffold)</name>
  <files>tests/unit/test_owner_segmentation.py</files>
  <read_first>
    - tests/content/test_trend_store.py (fixture/hand-built-DataFrame pattern, lines 1-60)
    - reports/modules/board_report_utils.py (current extract_business_unit signature at §206)
    - .planning/phases/13-owner-segmentation-composition-s2-doc/13-PATTERNS.md (§test_owner_segmentation.py, lines 550-577)
  </read_first>
  <action>
    Create tests/unit/test_owner_segmentation.py. Build a hand-built assets DataFrame fixture with known tag strings covering: an asset with both Owner and Application tags ("Owner=Network Defense;Application=Finance"), an asset with Owner only, an asset with Application only (no Owner → must become "Unassigned"), an asset with empty tags string "", and an asset with tags None. Write these tests against the NOT-YET-EXISTING extract_owner / compute_per_bu_breakdown:
    - test_owner_buckets_reconcile_to_whole: extract_owner(df)["owner"].value_counts().sum() == len(df) (SEG-01/02).
    - test_no_owner_tag_is_unassigned: assets with no Owner tag have owner == "Unassigned" (the configurable default), and there is NO "Untagged" value anywhere in the owner column (SEG-02, D-06).
    - test_application_column_populated: extract_owner(df)["application"] carries the Application tag value for the dual-tagged asset (D-05).
    - test_missing_tags_column_fail_soft: extract_owner(pd.DataFrame({"asset_uuid": ["x"]})) returns a frame with owner == "Unassigned" for every row and does NOT raise (SEG-04, D-07).
    - test_output_has_owner_not_business_unit: extract_owner output has an "owner" column and NO "business_unit" column (D-04, Pitfall 2).
    - test_breakdown_output_column_is_owner: compute_per_bu_breakdown over a small frame returns a DataFrame whose grouping column is "owner" and that has NO "business_unit" column (D-04, Pitfall 2).
    - test_configurable_unassigned_label: extract_owner(df, unassigned_label="No Owner") puts no-Owner assets under "No Owner".
    Import names: from reports.modules.board_report_utils import extract_owner, compute_per_bu_breakdown, OWNER_TAG_CATEGORY. These tests MUST fail (ImportError / AttributeError) before Task 1 — that is the RED state.
  </action>
  <verify>
    <automated>cd D:/Projects/vuln-reporting; .venv/Scripts/python.exe -m pytest tests/unit/test_owner_segmentation.py -q 2>&1 | Select-String -Pattern "error|failed|Error"</automated>
  </verify>
  <acceptance_criteria>
    - tests/unit/test_owner_segmentation.py exists and contains `def test_owner_buckets_reconcile_to_whole`
    - File imports `extract_owner` and `compute_per_bu_breakdown` from `reports.modules.board_report_utils`
    - Running the file before Task 1 fails on import/attribute error (RED state confirmed)
    - Fixture includes at least one no-Owner asset and one None-tags asset
  </acceptance_criteria>
  <done>Test file present, references extract_owner/compute_per_bu_breakdown/OWNER_TAG_CATEGORY, fails on missing symbols.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Generalize board_report_utils.py to Owner-primary helper</name>
  <files>reports/modules/board_report_utils.py</files>
  <read_first>
    - reports/modules/board_report_utils.py (full file — constants §47-48, extract_business_unit §206-280, compute_per_bu_breakdown §287-370, compute_bu_risk_scores §407-479, module docstring §17/§26-30)
    - reports/modules/critical_remediation_sla_module.py (§976-991 _extract_owner_tag — the parser to promote)
    - .planning/phases/13-owner-segmentation-composition-s2-doc/13-PATTERNS.md (§board_report_utils.py, lines 28-127 — exact change sites and excerpts)
    - .planning/phases/13-owner-segmentation-composition-s2-doc/13-RESEARCH.md (§Critical Code Surfaces, Pitfalls 1/2/3)
    - tests/unit/test_owner_segmentation.py (Task 0 — the spec these changes must satisfy)
  </read_first>
  <behavior>
    - extract_owner(df) populates owner (primary, from "Owner" tag) and application (nested, from "Application" tag); no-match owner → unassigned_label.
    - extract_owner on a frame with no tags column → all rows owner == unassigned_label, application == "", no raise (logs a warning).
    - compute_per_bu_breakdown output grouping column is "owner" (not "business_unit").
    - extract_owner(df, unassigned_label="No Owner") honors the override.
  </behavior>
  <action>
    Generalize board_report_utils.py in place (D-03, blast-radius file 1 of 5 per D-08):
    1. Constants: replace `BU_TAG_CATEGORY = "Application"` with `OWNER_TAG_CATEGORY = "Owner"` (primary, D-01), add `APPLICATION_TAG_CATEGORY = "Application"` (nested, D-05), add `_DEFAULT_UNASSIGNED_LABEL = "Unassigned"` (D-06).
    2. Rename `extract_business_unit` → `extract_owner(assets_df, tag_column_name="tags", unassigned_label=_DEFAULT_UNASSIGNED_LABEL)`. Replace the single-category `_bu_from_tags` closure with a single-pass parser that extracts BOTH the Owner value (→ owner column) and the Application value (→ application column) from the semicolon-delimited `"Cat=Val;Cat=Val"` string. Case-insensitive category match via `.casefold()`. Multi-value join with `"; ".join(sorted(set(...)))`. Owner no-match → unassigned_label; Application no-match → "". This promotes and supersedes critical_remediation_sla_module._extract_owner_tag (which returned ""); the promoted logic returns unassigned_label instead (Pitfall 3). Set `df.loc[:, "owner"]` and `df.loc[:, "application"]`. Missing tags column → both columns filled (owner=unassigned_label, application=""), warning logged, no raise (SEG-04/D-07).
    3. compute_per_bu_breakdown: keep the `bu_column` parameter for extensibility but change the output rename at §370 from `.rename(columns={bu_column: "business_unit"})` to `.rename(columns={bu_column: "owner"})` (D-04, Pitfall 2). Default the bu_column param to "owner".
    4. compute_bu_risk_scores: change the two `"business_unit"` column references (§460 bu_map slice, §479 groupby) to `"owner"`; update its docstring's enriched-param description.
    5. Module-level docstring (§17, §26-30): update listed function names (extract_business_unit→extract_owner) and replace "Application"/"business_unit" narrative with the Owner model (Owner = Application Support / patching-responsible per D-01; Application now nested per D-05).
    Do NOT rename callers in this plan — that is Plan 02. Do NOT change the report-facing "Business Unit" display strings here (none live in this file). Do NOT add the supplemental writer here (Plan 03).
  </action>
  <verify>
    <automated>cd D:/Projects/vuln-reporting; .venv/Scripts/python.exe -m pytest tests/unit/test_owner_segmentation.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def extract_owner" reports/modules/board_report_utils.py` returns a match
    - `grep -n "OWNER_TAG_CATEGORY" reports/modules/board_report_utils.py` returns a match with value "Owner"
    - `grep -c "Untagged" reports/modules/board_report_utils.py` (excluding comment lines via `Select-String -NotMatch '^\s*#'`) returns 0
    - `grep -n 'columns={bu_column: "owner"}' reports/modules/board_report_utils.py` returns a match (the corrected rename)
    - `grep -c "business_unit" reports/modules/board_report_utils.py` returns 0 (no residual column-name or symbol references; `bu_column` param name is allowed and does not match `business_unit`)
    - `tests/unit/test_owner_segmentation.py` exits 0 (all SEG-01/02/04 tests green, including reconcile-to-whole and missing-column fail-soft)
    - extract_owner output frame contains both `owner` and `application` columns
  </acceptance_criteria>
  <done>board_report_utils.py exposes extract_owner + owner-column breakdown/risk; Untagged and business_unit eliminated from the file; SEG-01/02/04 tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tenable parquet → tag parser | `tags` column is Tenable-sourced free-text; parsed into owner/application columns |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | `extract_owner` tag-string parser | mitigate | Parser already guards `isinstance(tags_val, str)` and splits on ";"; no eval/exec; arbitrary tag values flow only into in-memory columns, never code. Carry the guard forward. |
| T-13-02 | Denial of Service | missing/partial `Owner` tag (fail-soft) | mitigate | Missing tags column → all-Unassigned, no raise (SEG-04/D-07); test_missing_tags_column_fail_soft proves no crash so a filtered-to-zero or untagged fleet never aborts the batch. |
| T-13-03 | Info Disclosure | owner/application column values | accept | Owner/Application labels are internal org names, not row-level PII (hostnames/IPs); no persistence to repo or AI context in this plan. |
| T-13-SC | Tampering | npm/pip installs | mitigate | No package installs in this phase (RESEARCH §Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- `.venv/Scripts/python.exe -m pytest tests/unit/test_owner_segmentation.py -q` exits 0
- `.venv/Scripts/python.exe -m pytest tests/unit -q` exits 0 (no regression in existing unit tests)
- No `business_unit` or `Untagged` tokens remain in board_report_utils.py
</verification>

<success_criteria>
- extract_owner returns per-Owner buckets that reconcile to the whole, with a configurable Unassigned catch-all (SEG-01, SEG-02)
- extract_owner is fail-soft on absent tags column (SEG-04)
- owner + application columns both produced (D-05); business_unit column name freed (D-04)
- Owner parsing consolidated into the shared helper (D-03); ready for Plan 02 consumer repoint
</success_criteria>

<output>
Create `.planning/phases/13-owner-segmentation-composition-s2-doc/13-01-SUMMARY.md` when done.
</output>
