---
phase: 13-owner-segmentation-composition-s2-doc
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - reports/modules/aged_vulns_assets_module.py
  - reports/modules/high_risk_assets_module.py
  - reports/modules/scan_coverage_sla_module.py
  - reports/modules/critical_remediation_sla_module.py
autonomous: true
requirements: [SEG-01, SEG-02, SEG-04]
must_haves:
  truths:
    - "All four board modules import extract_owner and group by the owner column"
    - "No module references the business_unit column or the Untagged label"
    - "No module renders the display heading Business Unit — all read Owner"
    - "critical_remediation_sla_module no longer defines a private _extract_owner_tag"
    - "All four modules still deliver (compute + render) without crashing on empty/untagged data"
  artifacts:
    - path: "reports/modules/aged_vulns_assets_module.py"
      provides: "Owner-repointed consumer (extract_owner, owner column, Owner heading)"
      contains: "extract_owner"
    - path: "reports/modules/critical_remediation_sla_module.py"
      provides: "Owner-repointed consumer with _extract_owner_tag removed"
      contains: "extract_owner"
  key_links:
    - from: "4 consumer modules"
      to: "board_report_utils.extract_owner / owner column"
      via: "import + groupby/merge/row.get on owner"
      pattern: "extract_owner"
---

<objective>
Repoint the four board consumer modules from the interim `Application`/`business_unit` grouping to the generalized `Owner` helper from Plan 01 (D-04, D-08). Each module: imports `extract_owner` (was `extract_business_unit`), references the `owner` column (was `business_unit`) at every merge/groupby/row-access site, uses the `"Unassigned"` label in its secondary fillna guards (was `"Untagged"`, D-06), and renders the report-facing heading `"Owner"` (was `"Business Unit"`, D-02). `critical_remediation_sla_module.py` additionally removes its now-redundant private `_extract_owner_tag` (promoted into the shared helper in Plan 01).

Purpose: Completes the owner-segmentation repoint across the entire confirmed blast radius (D-08 files 2–5 of 5). After this plan, every board table is grouped and routed by the actual patching-responsible stakeholder (Owner = Application Support), with `Application` available as a nested drill-down (Plan 03 supplemental).

Output: Four repointed module files; the two known catch-all sites and all "Business Unit" headings converted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-owner-segmentation-composition-s2-doc/13-CONTEXT.md
@.planning/phases/13-owner-segmentation-composition-s2-doc/13-PATTERNS.md
@.planning/phases/13-owner-segmentation-composition-s2-doc/13-RESEARCH.md
@.planning/phases/13-owner-segmentation-composition-s2-doc/13-01-SUMMARY.md

<interfaces>
<!-- New symbols from Plan 01 that this plan consumes. Source: 13-01-PLAN.md / board_report_utils.py post-Plan-01. -->
From reports.modules.board_report_utils (after Plan 01):
  extract_owner(assets_df, tag_column_name="tags", unassigned_label="Unassigned") -> DataFrame
    (adds owner + application columns; owner no-match → "Unassigned")
  compute_per_bu_breakdown(df, num_mask, den_mask, bu_column="owner", ...) -> DataFrame
    (output grouping column is now "owner")
  compute_bu_risk_scores(...) -> Series indexed by "owner"

<!-- Per-module change sites (verified line numbers). Source: 13-RESEARCH.md §Consumer Module Reference Map + 13-PATTERNS.md. -->
aged_vulns_assets_module.py: §241 extract_business_unit→extract_owner; §260 merge on; §329-330 defensive re-extract;
  §341 column slice; §356/§368 col refs; §389-394 groupby+fillna; §399/§402 sort/worst-name; §580/§728 row.get.
high_risk_assets_module.py: identical pattern at §246, §265, §336-337, §348, §363, §373, §392-402, §584, §732.
scan_coverage_sla_module.py: §315 extract; §362-363 defensive; §381/§394 refs; §414-433 sort/best-worst;
  §602/§648 <th>Business Unit</th>; §675 explanatory paragraph; §762/§777 openpyxl headers; §19/§988 docstring.
critical_remediation_sla_module.py: §55 import; §588/§740 row.get; §976-991 DELETE _extract_owner_tag;
  §1061 empty-frame columns; §1065 extract; §1067 enriched col; §1071-1072 fw assignment+fillna.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repoint aged_vulns_assets_module.py + high_risk_assets_module.py</name>
  <files>reports/modules/aged_vulns_assets_module.py, reports/modules/high_risk_assets_module.py</files>
  <read_first>
    - reports/modules/aged_vulns_assets_module.py (full — import block ~§55, compute() §241-402, render methods §580/§728)
    - reports/modules/high_risk_assets_module.py (full — identical pattern at §246, §265, §336-337, §348, §363, §373, §392-402, §584, §732)
    - reports/modules/board_report_utils.py (post-Plan-01: extract_owner signature, owner output column)
    - .planning/phases/13-owner-segmentation-composition-s2-doc/13-PATTERNS.md (§aged_vulns_assets_module.py, lines 130-185; §high_risk_assets_module.py, lines 251-260; §Shared Patterns, lines 595-634)
  </read_first>
  <action>
    These two modules share an identical pattern (PATTERNS.md confirms exact analog). For EACH file apply:
    1. Import: change `extract_business_unit` → `extract_owner` in the `from reports.modules.board_report_utils import (...)` block.
    2. compute(): `extract_business_unit(...)` call → `extract_owner(...)`; merge `on="business_unit"` → `on="owner"`; defensive re-extract guard `if "business_unit" not in ...columns` → `"owner"`; column slices and references `"business_unit"` → `"owner"`; groupby `"business_unit"` → `"owner"`; sort key and worst-name variable references → `"owner"`.
    3. Secondary fillna catch-all (the SECOND of the two known sites, Pitfall 1): `...["business_unit"].fillna("Untagged").replace("", "Untagged")` → `...["owner"].fillna("Unassigned").replace("", "Unassigned")` (D-06).
    4. render methods: `row.get("business_unit", "")` → `row.get("owner", "")` (D-04).
    5. Display heading (D-02, Pitfall 4): in HTML render strings change `<th>Business Unit</th>` → `<th>Owner</th>` and any "...Business Units" title text → "...Owners"; in openpyxl `ws.cell(value="Business Unit")` / `headers = ["Business Unit", ...]` → `"Owner"`.
    6. Docstring/comment sweep (Pitfall 7): update any "Application tag"/"business unit" narrative to the Owner model.
    Use `.assign()` rather than `.loc[:, col]=` for post-merge column assignment to avoid pandas chained-assignment warnings (PATTERNS §F-DTYPE). Touch ONLY these two files in this task.
  </action>
  <verify>
    <automated>cd D:/Projects/vuln-reporting; .venv/Scripts/python.exe -m pytest tests/unit tests/content -q -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "business_unit" reports/modules/aged_vulns_assets_module.py reports/modules/high_risk_assets_module.py` returns 0 for both files
    - `grep -c "Untagged" reports/modules/aged_vulns_assets_module.py reports/modules/high_risk_assets_module.py` returns 0 for both files
    - `grep -ci "Business Unit" reports/modules/aged_vulns_assets_module.py reports/modules/high_risk_assets_module.py` returns 0 for both files (display heading converted)
    - `grep -c "extract_owner" reports/modules/aged_vulns_assets_module.py reports/modules/high_risk_assets_module.py` returns ≥1 for both files
    - `.venv/Scripts/python.exe -m pytest tests/unit tests/content -q -x` exits 0
  </acceptance_criteria>
  <done>Both modules import extract_owner, group/merge/render on owner, use Unassigned, render Owner heading; no business_unit/Untagged/Business-Unit residue; suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint scan_coverage_sla_module.py + critical_remediation_sla_module.py (remove _extract_owner_tag)</name>
  <files>reports/modules/scan_coverage_sla_module.py, reports/modules/critical_remediation_sla_module.py</files>
  <read_first>
    - reports/modules/scan_coverage_sla_module.py (full — §315, §362-363, §381, §394, §414-433, §602, §648, §675, §762, §777, §19, §988)
    - reports/modules/critical_remediation_sla_module.py (full — §55 import, §588/§740 row.get, §976-991 _extract_owner_tag, §1046-1091 _compute_bu_breakdown)
    - reports/modules/board_report_utils.py (post-Plan-01 extract_owner — confirm it returns "Unassigned" so the removed _extract_owner_tag's behavior is fully replaced)
    - .planning/phases/13-owner-segmentation-composition-s2-doc/13-PATTERNS.md (§scan_coverage_sla_module.py lines 263-293; §critical_remediation_sla_module.py lines 189-247)
    - .planning/phases/13-owner-segmentation-composition-s2-doc/13-RESEARCH.md (Pitfalls 1, 3, 4, 7)
  </read_first>
  <action>
    scan_coverage_sla_module.py:
    1. Import + compute(): `extract_business_unit` → `extract_owner`; `"business_unit"` column references at §315/§362-363/§381/§394/§414-433 → `"owner"`; any `"Untagged"` fillna → `"Unassigned"`.
    2. render_email_panel HTML (§602, §648): `<th>Business Unit</th>` → `<th>Owner</th>`; §675 explanatory paragraph: change the `"Application" tag category` wording → `"Owner" tag category` and `"Untagged"` → `"Unassigned"`.
    3. render_excel_tabs openpyxl (§762, §777): `headers = ["Business Unit", ...]` → `["Owner", ...]`; `row.get("business_unit", "")` → `row.get("owner", "")`.
    4. Module docstring (§19, §988): `Application` tag references → `Owner`.

    critical_remediation_sla_module.py:
    1. Import (§55): `extract_business_unit` → `extract_owner`.
    2. render methods (§588, §740): `row.get("business_unit", "")` → `row.get("owner", "")`; any "Business Unit" heading → "Owner" (D-02).
    3. _compute_bu_breakdown (§1046-1091): empty-frame columns list (§1061) `["business_unit", ...]` → `["owner", ...]`; `extract_business_unit(on_time_assets)` (§1065) → `extract_owner(...)`; `enriched_assets["business_unit"]` (§1067) → `["owner"]`; uuid map var rename for clarity; fw assignment + fillna (§1071-1072) `fw["business_unit"] = ...map(...).fillna("Untagged")` → `fw["owner"] = ...map(...).fillna("Unassigned")` (the FIRST catch-all site here, Pitfall 1).
    4. DELETE the private `_extract_owner_tag` function (§976-991) entirely — its logic is now in the shared extract_owner (Pitfall 3). First confirm via grep that no remaining call site in the module references it before deleting.
    Touch ONLY these two files in this task.
  </action>
  <verify>
    <automated>cd D:/Projects/vuln-reporting; .venv/Scripts/python.exe -m pytest tests/unit tests/content -q -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "business_unit" reports/modules/scan_coverage_sla_module.py reports/modules/critical_remediation_sla_module.py` returns 0 for both files
    - `grep -c "Untagged" reports/modules/scan_coverage_sla_module.py reports/modules/critical_remediation_sla_module.py` returns 0 for both files
    - `grep -ci "Business Unit" reports/modules/scan_coverage_sla_module.py reports/modules/critical_remediation_sla_module.py` returns 0 for both files
    - `grep -c "_extract_owner_tag" reports/modules/critical_remediation_sla_module.py` returns 0 (function and all call sites removed)
    - `grep -rc "Untagged" reports/modules/` across all five blast-radius files returns 0 (both catch-all sites + helper converted — phase-wide gate)
    - `grep -rc "Business Unit" reports/modules/aged_vulns_assets_module.py reports/modules/high_risk_assets_module.py reports/modules/scan_coverage_sla_module.py reports/modules/critical_remediation_sla_module.py` returns 0
    - `.venv/Scripts/python.exe -m pytest tests/unit tests/content -q -x` exits 0
  </acceptance_criteria>
  <done>Both modules repointed to owner; _extract_owner_tag deleted; phase-wide grep gates (Untagged=0, Business Unit=0 across all 4 consumers) pass; suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tenable parquet → module compute | Same `tags` boundary as Plan 01; modules now read owner/application via the shared helper |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04 | Denial of Service | 4 modules on empty/untagged data | mitigate | All four delegate parsing to extract_owner (fail-soft, Plan 01); secondary fillna guards now produce "Unassigned" not NaN; empty-data render paths use safe_pct/safe_int per CLAUDE.md — fail-soft batch preserved (Constraint). pytest tests/unit tests/content green proves no regression. |
| T-13-05 | Tampering | "Business Unit" residue causing mixed display | mitigate | Phase-wide grep gates (Business Unit=0, Untagged=0, business_unit=0 across all 4 consumers) enforce a clean, consistent Owner display — no half-converted module. |
| T-13-06 | Repudiation | removal of _extract_owner_tag silently dropping a call site | mitigate | Acceptance criterion greps `_extract_owner_tag` to 0 in the module AFTER confirming no remaining caller; suite green confirms no broken reference. |
</threat_model>

<verification>
- `.venv/Scripts/python.exe -m pytest tests/unit tests/content -q` exits 0
- `grep -rc "Untagged" reports/modules/` returns 0 across all five blast-radius files (phase-wide catch-all gate)
- `grep -rc "Business Unit" reports/modules/{aged_vulns_assets,high_risk_assets,scan_coverage_sla,critical_remediation_sla}_module.py` returns 0
- `grep -rc 'row.get("business_unit"' reports/modules/` returns 0
</verification>

<success_criteria>
- All four consumer modules grouped and rendered by Owner (SEG-01/02/04 flow through the helper)
- Both catch-all sites and all display headings converted (D-06, D-02)
- _extract_owner_tag duplication removed (D-03); blast radius D-08 fully repointed
</success_criteria>

<output>
Create `.planning/phases/13-owner-segmentation-composition-s2-doc/13-02-SUMMARY.md` when done.
</output>
