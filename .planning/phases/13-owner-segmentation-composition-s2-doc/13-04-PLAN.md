---
phase: 13-owner-segmentation-composition-s2-doc
plan: 04
type: execute
wave: 3
depends_on: ["13-01", "13-03"]
files_modified:
  - docs/trend_and_segmentation_calculations.md
autonomous: true
requirements: [DOC-01]
must_haves:
  truths:
    - "docs/trend_and_segmentation_calculations.md exists in the docs/*_calculations.md style"
    - "It documents the reopened-aware two-interval open predicate"
    - "It documents the ~29-day Tenable fixed-retention constraint and forward-accumulation (no backfill) model"
    - "It documents the Owner/Unassigned segmentation model (Owner = Application Support / patching-responsible; reconcile-to-whole)"
    - "It documents the PII rule as AI/repo exposure (NOT an internal-email ban), per D-10"
    - "It documents dimension=owner trend file location and the tag_filter-consistency requirement"
  artifacts:
    - path: "docs/trend_and_segmentation_calculations.md"
      provides: "Auditor runbook for the trend + Owner-segmentation substrates"
      contains: "two-interval"
  key_links:
    - from: "docs/trend_and_segmentation_calculations.md open-predicate section"
      to: "utils/open_count.py open_findings_at"
      via: "documents the implemented predicate"
      pattern: "open_findings_at"
---

<objective>
Ship the auditor-facing runbook `docs/trend_and_segmentation_calculations.md` (DOC-01) documenting the two substrates exactly as implemented: the reopened-aware two-interval open predicate (`utils/open_count.py`), the ~29-day Tenable fixed-retention constraint and forward-accumulation (snapshot-not-reconstruction) model, the Owner/Unassigned segmentation model, the combined supplemental and its PII rule, and the per-Owner trend composition. Match the established `docs/*_calculations.md` structure.

Purpose: Auditors and operators need a single canonical reference for how open-counts, trend history, and Owner grouping are computed — including the hard constraints (no backfill) and the real PII rationale (D-10: AI/repo exposure, not an internal-email ban). This closes the documentation requirement for the v1.3 substrate milestone.

Output: One new doc file. No code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-owner-segmentation-composition-s2-doc/13-CONTEXT.md
@.planning/phases/13-owner-segmentation-composition-s2-doc/13-PATTERNS.md
@.planning/phases/13-owner-segmentation-composition-s2-doc/13-01-SUMMARY.md
@.planning/phases/13-owner-segmentation-composition-s2-doc/13-03-SUMMARY.md
@.claude/skills/spike-findings-vuln-reporting/SKILL.md

<interfaces>
<!-- Source-of-truth files DOC-01 documents (read, do not modify). -->
utils/open_count.py: open_findings_at(df, date) — reopened-aware two-interval predicate
  (OPEN = never fixed; REOPENED = fixed only during [last_fixed, resurfaced_date); FIXED = fixed at last_fixed).
data/trend_store.py: capture_snapshot(df, assets_df, date, dimension, tag_filter, trend_dir, enriched_assets),
  read_trend(dimension, tag_filter, months, trend_dir); trend_<dimension>_<tagsuffix>.json file-per-dimension shape;
  _count_by_owner (Plan 03); tag_filter consistency requirement (Pitfall 6).
reports/modules/board_report_utils.py: extract_owner (Plan 01) — Owner primary, Application nested, Unassigned catch-all.
reports/owner_supplemental.py: write_owner_supplemental (Plan 03) — combined supplemental, output/ only.

<!-- Style template. Source: docs/management_summary_calculations.md (PATTERNS lines 505-547). -->
Structure: Title + File/Audience/Outputs/Schedule header → Table of Contents → numbered sections
  (Quick Start, Data Sources, metric/model sections, Output Files, Trend Data Store, Troubleshooting).

<!-- Settled constraints to document (do NOT re-decide). Source: SKILL.md requirements, STATE.md v1.3 settled constraints. -->
- Reopened-aware predicate mandatory (naive form drops ~19% of findings).
- Snapshot-capture, not reconstruction; ~29-day fixed-retention wall; cold start is real; accumulate forward.
- Snapshot payloads aggregate-only (no PII).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author docs/trend_and_segmentation_calculations.md</name>
  <files>docs/trend_and_segmentation_calculations.md</files>
  <read_first>
    - docs/management_summary_calculations.md (structure/style template — header block, ToC, section ordering)
    - utils/open_count.py (open_findings_at — the predicate to document accurately)
    - data/trend_store.py (capture_snapshot/read_trend/_count_by_owner/_count_by_severity — file shape, tag_filter matching)
    - reports/modules/board_report_utils.py (extract_owner — Owner/Application/Unassigned model)
    - reports/owner_supplemental.py (write_owner_supplemental — supplemental contents + output location)
    - .planning/phases/13-owner-segmentation-composition-s2-doc/13-CONTEXT.md (D-01 ownership model, D-10/D-11 PII rationale)
    - .claude/skills/spike-findings-vuln-reporting/SKILL.md (reopened-aware predicate, ~29-day retention, forward-accumulation — settled constraints)
    - .planning/notes/trend-reconstruction-engine.md (forward-accumulation model, no backfill)
  </read_first>
  <action>
    Create docs/trend_and_segmentation_calculations.md matching the docs/management_summary_calculations.md structure (header block: File / Audience=auditors+operators / Outputs / Schedule; Table of Contents; numbered sections). Required sections (PATTERNS §DOC-01 required sections):
    1. Quick Start — how to capture an owner snapshot (scripts/capture_trend_snapshot.py) and read it back (read_trend("owner","all_assets")).
    2. Open Predicate — the reopened-aware two-interval predicate from utils/open_count.py open_findings_at: OPEN = never fixed; REOPENED = fixed only during [last_fixed, resurfaced_date); FIXED at last_fixed. State explicitly that the naive `last_fixed null OR last_fixed>D` form silently drops ~19% of findings (the entire REOPENED population) and why the two-interval model is mandatory.
    3. ~29-Day Retention & Forward-Accumulation — Tenable retains fixed findings only ~29 days, so history CANNOT be reconstructed/backfilled; the substrate is forward-accumulating snapshot-capture only; cold start is real; multi-month history accrues from the first snapshot forward.
    4. Owner / Unassigned Segmentation Model — Owner tag = the Application Support group responsible for patching (the routing dimension, D-01); Business Unit and Technical Support are future/reference dimensions (not built); Application is now a nested analyst drill-down (D-05); the Unassigned catch-all (configurable label) guarantees per-Owner totals reconcile to the whole (SEG-02); fail-soft when Owner is absent (SEG-04).
    5. Combined Supplemental — what reports/owner_supplemental.py produces (flat Owner|Application|Open Findings|Asset Count tab; Unassigned rows = "needs Owner assignment" worklist); PII rule stated as D-10: the asset-level supplemental may be emailed internally but MUST NOT be committed to the repo or fed into AI/Claude context — this is the REAL rationale (AI/repo exposure), NOT an internal-email ban; lands under output/ (gitignored).
    6. Trend Composition / Data Store — file-per-dimension shape trend_<dimension>_<tagsuffix>.json under data/trend/; the owner file is data/trend/trend_owner_all_assets.json; payloads are aggregate counts only (TREND-06/D-11); the tag_filter string passed to read_trend MUST match the value stored at capture time (Pitfall 6).
    7. Troubleshooting — insufficient_data=True causes (≤1 snapshot / cold start), tag_filter mismatch producing empty reads, all-Unassigned diagnosis (Owner tag absent or assets_df not enriched).
    Cross-reference the source files by path (utils/open_count.py, data/trend_store.py, reports/modules/board_report_utils.py, reports/owner_supplemental.py). Auditor-facing prose; no fabricated numbers beyond the settled ~19% / ~29-day figures from the skill.
  </action>
  <verify>
    <automated>cd D:/Projects/vuln-reporting; if ((Test-Path docs/trend_and_segmentation_calculations.md) -and ((Get-Content docs/trend_and_segmentation_calculations.md -Raw) -match "two-interval") -and ((Get-Content docs/trend_and_segmentation_calculations.md -Raw) -match "29")) { exit 0 } else { exit 1 }</automated>
  </verify>
  <acceptance_criteria>
    - `Test-Path docs/trend_and_segmentation_calculations.md` is True
    - File contains the strings: "two-interval", "resurfaced_date", "29", "forward", "Unassigned", "Owner", "open_findings_at"
    - File documents the PII rule as AI/repo exposure (contains "not" near "email" framing per D-10) — `Select-String` confirms a statement that internal email is permitted while repo/AI transmission is banned
    - File documents the trend_owner file location string "trend_owner" and the tag_filter consistency requirement
    - File header block follows the docs/*_calculations.md pattern (File / Audience / Outputs lines present)
  </acceptance_criteria>
  <done>docs/trend_and_segmentation_calculations.md exists with all seven sections, matching the established runbook style and the settled spike constraints.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Documentation-only plan; no code, no input, no persisted data crosses a boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-13 | Info Disclosure | example snippets in the runbook | mitigate | Use synthetic/placeholder owner names and asset values in any illustrative example; no real hostnames/IPs/asset_uuids in the committed doc (D-10 repo-commit ban applies to docs too). |
| T-13-14 | Repudiation | doc drifting from implemented behavior | accept | Doc authored after Plan 01/03 land (depends_on), describing what exists; future drift handled by normal doc maintenance, not a Phase 13 concern. |
</threat_model>

<verification>
- `Test-Path docs/trend_and_segmentation_calculations.md` is True
- Doc contains the open-predicate, ~29-day-retention/forward-accumulation, Owner/Unassigned, PII-rationale, and trend-composition sections
- No real PII (hostnames/IPs/asset_uuids) appears in committed examples
</verification>

<success_criteria>
- DOC-01 satisfied: auditor runbook documents the open predicate, the no-backfill retention constraint, and the Owner/Unassigned model in the established style
- PII rationale documented as D-10 intends (AI/repo exposure, not internal-email ban)
</success_criteria>

<output>
Create `.planning/phases/13-owner-segmentation-composition-s2-doc/13-04-SUMMARY.md` when done.
</output>
