---
phase: 19-v1-4-closure-int-warn-1-2-3-fixes-phase-17-human-verificatio
plan: 07
type: execute
wave: 3
depends_on: ["19-02", "19-04"]
files_modified:
  - config.py
  - reports/modules/reopened_vulns_module.py
  - reports/modules/external_dmz_module.py
  - reports/modules/new_vs_remediated_module.py
  - reports/modules/vuln_density_module.py
  - utils/open_count.py
  - data/trend_store.py
  - scripts/backfill_trend_reconstruction.py
  - docs/management_summary_calculations.md
  - docs/trend_and_segmentation_calculations.md
  - scripts/smoke_management_summary_cutover.py
  - run_all.py
  - scripts/smoke_email_phase2.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "18-REVIEW WR-08: config.py VPR_SEVERITY_MAP bands are contiguous (no 8.91-8.99 / 6.91-6.99 / 3.91-3.99 gap dropping a VPR score to native fallback)"
    - "18-REVIEW IN-01: config.py import math is hoisted to module top-level (no per-call import inside vpr_to_severity)"
    - "18-REVIEW WR-04: dead _months_in_range helper + its unused dateutil import removed from backfill_trend_reconstruction.py (locked-stack: no dateutil); callers use _months_in_range_stdlib"
    - "18-REVIEW IN-03: docs/management_summary_calculations.md pre-v1.4 sections describing the removed bespoke path are reconciled or marked historical"
    - "18-REVIEW IN-04: utils/open_count.py docstring no longer references the removed management_summary._OPEN_STATES symbol"
    - "18-REVIEW IN-05: data/trend_store.py capture_snapshot docstring notes owner-dimension omits aggregate counts"
    - "15-REVIEW IN-01: unused safe_format import removed from reopened_vulns/external_dmz/new_vs_remediated modules"
    - "15-REVIEW IN-02: dead _rag_fill removed from the modules that never call it"
    - "15-REVIEW IN-03: dead _safe_mom_delta removed (location confirmed by grep)"
    - "15-REVIEW IN-04: unused imports removed from reopened_vulns_module"
    - "CR-G4: scripts/smoke_email_phase2.py attaches the inline CID images it generates (MIMEImage / Content-ID) so the panel `<img src=\"cid:...\">` references resolve"
    - "CR-G1..G3, CR-G5, CR-G8..G12: stale docstrings/comments/help-text/doc sections + planning-doc inconsistencies corrected (CR-G6/G7 deferred to Plan 09 closeout per triage)"
    - "D-02: this cleanup wave is separated from correctness commits — no correctness logic changes here"
  artifacts:
    - path: "config.py"
      provides: "Contiguous VPR bands + module-level math import"
      contains: "VPR_SEVERITY_MAP"
    - path: "scripts/backfill_trend_reconstruction.py"
      provides: "Dead _months_in_range + dateutil import removed"
      contains: "_months_in_range_stdlib"
    - path: "utils/open_count.py"
      provides: "Docstring without stale _OPEN_STATES reference"
      contains: "open_findings_at"
  key_links:
    - from: "scripts/backfill_trend_reconstruction.py"
      to: "_months_in_range_stdlib"
      via: "all call sites use the stdlib variant; dead dateutil import gone"
      pattern: "_months_in_range_stdlib"
---

<objective>
Clear the low-value cleanup backlog (dead code, unused imports, stale docs/comments) per D-02 fix-all, kept in its own wave so a cleanup change never rides on a correctness commit. Covers the remaining 18-REVIEW items (WR-08 VPR band gap, IN-01/03/04/05), all 15-REVIEW IN-01..04 dead-import/symbol removals, and the CodeRabbit doc/comment Work-stream-G items (CR-G1..G5, G8..G12). CR-G6/G7 (ROADMAP/STATE status) are intentionally deferred to the Plan 09 closeout which already rewrites those.

Note: 18-REVIEW IN-02 (`_first_str` / deprecated `fetch_vulnerabilities` dead-code chain in `data/fetchers.py`) is owned by Plan 06 Task 2 (fetcher scope) — it is not a deliverable of this plan and is intentionally absent from this plan's truths.

Purpose: Fully clear the v1.4 review backlog; remove dead code and stale docs that mislead future readers; close the one genuine correctness item bundled here (WR-08 VPR band gap).
Output: Contiguous VPR bands, hoisted import, removed dead helpers/imports, reconciled docs.

Wave note: Wave 3 so this plan can own the docstring/dead-helper edits in utils/open_count.py (Plan 02, Wave 1), data/trend_store.py and scripts/backfill_trend_reconstruction.py (Plan 04, Wave 2) AFTER those correctness plans land — no same-wave file overlap.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-v1-4-closure-int-warn-1-2-3-fixes-phase-17-human-verificatio/19-PATTERNS.md
@.planning/phases/19-v1-4-closure-int-warn-1-2-3-fixes-phase-17-human-verificatio/19-CODERABBIT-TRIAGE.md
@.planning/phases/18-management-summary-migration-docs/18-REVIEW.md
@.planning/phases/15-independent-new-modules/15-REVIEW.md

<interfaces>
From config.py:
  VPR_SEVERITY_MAP: list[tuple[float, float, str]]   # L95-100; gaps at upper bounds 8.9/6.9/3.9
  vpr_to_severity(...)                               # L147 inline `import math` to hoist

<!-- CLAUDE.md SLA derives severity from VPR; bands must be contiguous so no VPR score falls to native fallback -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: config.py WR-08 contiguous VPR bands + IN-01 math hoist</name>
  <files>config.py, tests/test_config.py</files>
  <read_first>
    - config.py L95-100 (VPR_SEVERITY_MAP) and L147 + L152-157 (vpr_to_severity inline `import math` + band-match loop)
    - 18-REVIEW.md L272-306 (WR-08 contiguous-band fix using half-open lower bounds; IN-01 hoist)
    - 19-PATTERNS.md L562-579 (WR-08 + IN-01 fix patterns)
    - CLAUDE.md SLA table + memory project_sla_days_config_py_authoritative (VPR authoritative; bands must not drop scores to native)
  </read_first>
  <behavior>
    - Test test_vpr_score_8_95_is_high: vpr_to_severity(8.95) returns "high" (not native fallback) — the band gap is closed.
    - Test test_vpr_boundary_9_0_is_critical: vpr_to_severity(9.0) returns "critical".
    - Test test_vpr_score_6_95_is_medium and test_vpr_score_3_95_is_low: in-gap scores resolve to the correct lower band.
    - Test test_vpr_score_0_05_below_low: a score below the low band floor still falls through to fallback as before (no over-reach).
  </behavior>
  <action>
    WR-08: make the VPR bands contiguous. Per 18-REVIEW.md L293-295, prefer half-open lower bounds keyed off the next tier (critical `>= 9.0`; high `>= 7.0 and < 9.0`; medium `>= 4.0 and < 7.0`; low `>= 0.1 and < 4.0`) — removing the `8.9/6.9/3.9` magic upper-bound literals and the gaps. If retaining the `(lo, hi, label)` tuple shape is preferred for minimal churn, set the upper bounds to `8.99`/`6.99`/`3.99` as the documented quick fix (19-PATTERNS.md L566-573) — but the half-open form is cleaner and gap-proof; choose one and apply consistently to the match loop. IN-01: hoist `import math` from inside `vpr_to_severity` (L147) to the module-level imports at the top of config.py; remove the inline import. Add the band tests to tests/test_config.py (create if absent).
  </action>
  <acceptance_criteria>
    - `pytest tests/test_config.py -q -o addopts=""` exits 0 with the VPR-band tests passing (8.95→high, 9.0→critical, 6.95→medium, 3.95→low).
    - `import math` appears in the module-level import block of config.py and NOT inside vpr_to_severity — `grep -n "import math" config.py` shows it only at top-level.
    - No VPR score in (8.9,9.0) / (6.9,7.0) / (3.9,4.0) falls to fallback (proven by the tests).
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_config.py -q -o addopts=""</automated>
  </verify>
  <done>VPR bands contiguous (no gap drops to native); math import hoisted; band tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Dead-code/import removals — 15-REVIEW IN-01..04 + 18-REVIEW WR-04 + open_count/trend_store docstrings (IN-04/IN-05)</name>
  <files>reports/modules/reopened_vulns_module.py, reports/modules/external_dmz_module.py, reports/modules/new_vs_remediated_module.py, reports/modules/vuln_density_module.py, scripts/backfill_trend_reconstruction.py, utils/open_count.py, data/trend_store.py</files>
  <read_first>
    - 15-REVIEW.md L226-298 (IN-01 unused safe_format ×4; IN-02 dead _rag_fill ×3; IN-03 dead _safe_mom_delta; IN-04 unused rag_utils imports in reopened_vulns)
    - 18-REVIEW.md L191-205 (WR-04 dead _months_in_range + dateutil) and L330-338 (IN-04 open_count _OPEN_STATES docstring) and 18-REVIEW IN-05 trend_store owner-dim docstring
    - 19-PATTERNS.md L534-543 (WR-04 confirm-no-callers grep), L661-691 (15-REVIEW IN-01..05 patterns), L716-721 (dead-import removal convention), L588-594 (open_count + trend_store docstrings)
    - reports/modules/external_dmz_module.py L36-57 (clean reference import block)
  </read_first>
  <action>
    For EACH dead-symbol/import removal, FIRST confirm zero live call sites with Grep (e.g. `Grep("_months_in_range[^_]")`, `Grep("_safe_mom_delta")`, `Grep("safe_format")` per module, `Grep("_rag_fill")` per module, `Grep("_first_str")` is Plan 06's scope — skip here) before deleting, then remove only the confirmed-dead name/def:
    - 15-REVIEW IN-01: remove unused `safe_format` from the import line in reopened_vulns_module, external_dmz_module, new_vs_remediated_module (vuln_density uses it — leave). Confirm zero call sites per file first.
    - 15-REVIEW IN-02: remove the dead `_rag_fill` def from the modules that never call it (reopened_vulns, external_dmz, new_vs_remediated per 15-REVIEW L244-256) — keep it where used (vuln_density, accepted_recast).
    - 15-REVIEW IN-03: grep-locate `_safe_mom_delta` (15-REVIEW says new_vs_remediated_module L83); delete it if zero callers.
    - 15-REVIEW IN-04: remove unused `NO_DATA_DRIVER`/`STATUS_COLOR`/`STATUS_LABEL` (and any other unused names) from the reopened_vulns_module import block, keeping used names.
    - 18-REVIEW WR-04: delete `_months_in_range` (backfill L189-208) and its `dateutil.relativedelta` import; ensure all call sites use `_months_in_range_stdlib`. (Locked-stack: dateutil is not in requirements.)
    - 18-REVIEW IN-04: update utils/open_count.py docstring (L49,L84) to drop the stale `management_summary._OPEN_STATES` reference (replace with the lowercase `{"open","reopened"}` contract).
    - 18-REVIEW IN-05: extend data/trend_store.py capture_snapshot docstring to note dimension="owner" does not populate the aggregate counts (reopened/accepted/recast/etc.). Docstring only.
  </action>
  <acceptance_criteria>
    - `python -c "import reports.modules.reopened_vulns_module, reports.modules.external_dmz_module, reports.modules.new_vs_remediated_module, reports.modules.vuln_density_module, scripts.backfill_trend_reconstruction, utils.open_count, data.trend_store"` (repo root) succeeds.
    - `grep -c "_months_in_range[^_]" scripts/backfill_trend_reconstruction.py` == 0 (dead helper gone); `grep -c "dateutil" scripts/backfill_trend_reconstruction.py` == 0.
    - In each module where safe_format was unused, it no longer appears on the `from ...format_utils import` line; the module still imports cleanly.
    - `grep -c "_OPEN_STATES" utils/open_count.py` == 0.
    - Full affected-module + backfill test suites pass: `pytest tests/test_backfill_reconstruction.py tests/ -k "reopened or external_dmz or new_vs_remediated or vuln_density" -q -o addopts=""` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_backfill_reconstruction.py tests/ -k "reopened or external_dmz or new_vs_remediated or vuln_density" -q -o addopts=""</automated>
  </verify>
  <done>All confirmed-dead helpers/imports removed; stale docstrings corrected; everything imports + tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Doc / comment staleness — CR-G1..G5, CR-G8..G12 + 18-REVIEW IN-03 runbook + CR-G4 functional CID attach</name>
  <files>docs/management_summary_calculations.md, docs/trend_and_segmentation_calculations.md, scripts/smoke_management_summary_cutover.py, run_all.py, scripts/smoke_email_phase2.py</files>
  <read_first>
    - 19-CODERABBIT-TRIAGE.md L83-99 + L515-528 (CR-G1..G5, G8..G12 file/line/action table; note CR-G6/G7 are deferred to closeout)
    - 18-REVIEW.md L316-328 (IN-03 stale pre-v1.4 runbook sections)
    - scripts/smoke_management_summary_cutover.py L1-7; run_all.py L94-102; scripts/smoke_email_phase2.py L133-139 + L332-346; docs/trend_and_segmentation_calculations.md L182-191; docs/management_summary_calculations.md (pre-v1.4 sections)
    - delivery/email_sender.py email_inline_images attach pattern (MIMEImage + `Content-ID = <{cid}>`) — the reference CR-G4 mirrors
  </read_first>
  <action>
    Apply the text-only fixes from the triage table (19-CODERABBIT-TRIAGE.md L515-528):
    - CR-G1: smoke_management_summary_cutover.py L1-7 docstring → describe the current `result["_bundle"]` shape, drop "old bespoke path".
    - CR-G2: run_all.py L94-102 → delete/update the stale CHROME-COMPAT-01 comment (management_summary now accepts chrome kwargs post-GEN-01).
    - CR-G3: smoke_email_phase2.py L332-346 → fix the `--no-stub-panels` help text ("disables empty-panels stub only, not the legacy KPI path").
    - CR-G4 (FUNCTIONAL fix, not a comment): smoke_email_phase2.py L133-139 → the script generates inline panel images but never attaches them, so the `<img src="cid:...">` references in the panels do not resolve. Add the MIMEImage attach loop mirroring delivery/email_sender.py's `email_inline_images` handling: for each generated image build a `MIMEImage`, set `Content-ID = <{cid}>` (and `Content-Disposition: inline`), and attach it to the message so the cids resolve. Keep it a smoke-only change.
    - CR-G5: docs/trend_and_segmentation_calculations.md L182-191 → separate unsupported ad-hoc backfill from the sanctioned backfill_trend_reconstruction.py workflow.
    - CR-G8: 19-CONTEXT.md ref — already resolved during triage; no action (verify, note in SUMMARY).
    - CR-G9..G12 + IN-03: reconcile the planning-doc/SUMMARY/PLAN inconsistencies (18-VALIDATION.md status/checklist; 13-03-SUMMARY enriched_assets empty-vs-None doc; 15-02-SUMMARY set notation; 14-03-PLAN malformed shell check) and the pre-v1.4 docs/management_summary_calculations.md sections describing the removed bespoke path (mark historical or reconcile to the ReportComposer path). These are doc edits — confirm each line against the file before editing.
  </action>
  <acceptance_criteria>
    - `grep -ni "bespoke" scripts/smoke_management_summary_cutover.py` returns nothing in the docstring describing current behavior (or only a clearly-marked historical note).
    - `grep -ni "CHROME-COMPAT-01" run_all.py` returns nothing OR the comment is corrected to reflect chrome kwargs now flow to management_summary.
    - CR-G4: smoke_email_phase2.py actually attaches the inline images near the panel-generation block — `grep -qi "MIMEImage\|Content-ID" scripts/smoke_email_phase2.py` must match (the functional attach code is present, not just an import).
    - `python -c "import run_all"` and `python -c "import scripts.smoke_email_phase2, scripts.smoke_management_summary_cutover"` (repo root) import cleanly.
    - The 18-REVIEW IN-03 pre-v1.4 sections are reconciled or labeled historical in docs/management_summary_calculations.md (grep the doc for the historical marker or the removed-symbol names no longer presented as current).
  </acceptance_criteria>
  <verify>
    <automated>python -c "import run_all, scripts.smoke_email_phase2, scripts.smoke_management_summary_cutover" && grep -qi "MIMEImage\|Content-ID" scripts/smoke_email_phase2.py</automated>
  </verify>
  <done>All G-bucket doc/comment items + IN-03 runbook reconciled; smoke scripts import; CR-G4 functionally attaches the CID images (verified by grep); CR-G6/G7 left for closeout.</done>
</task>

</tasks>

<verification>
- `pytest tests/test_config.py tests/test_backfill_reconstruction.py tests/ -k "reopened or external_dmz or new_vs_remediated or vuln_density" -q -o addopts=""` → exits 0.
- `python -c "import config, run_all, data.trend_store, utils.open_count, scripts.backfill_trend_reconstruction, scripts.smoke_email_phase2, scripts.smoke_management_summary_cutover"` (repo root) imports cleanly.
</verification>

<success_criteria>
WR-08 (contiguous bands), IN-01 (math hoist), WR-04 (dead helper+dateutil), IN-03..05, 15-REVIEW IN-01..04, CR-G1..G5/G8..G12 all closed in a cleanup-only wave; no correctness logic touched.
</success_criteria>

<output>
Create `.planning/phases/19-v1-4-closure-int-warn-1-2-3-fixes-phase-17-human-verificatio/19-07-SUMMARY.md` when done
</output>
</content>
