---
phase: 19-v1-4-closure-int-warn-1-2-3-fixes-phase-17-human-verificatio
plan: 08
type: execute
wave: 4
depends_on: ["19-01", "19-02", "19-03", "19-04", "19-05", "19-06", "19-07"]
files_modified: []
autonomous: false
requirements: []

must_haves:
  truths:
    - "D-07: Phase 16 UAT tests 1, 2, 3, 5 and Phase 17 checks 1, 2, 3 are run ONCE against the fully-fixed build (after all code plans land)"
    - "D-05: the Phase 17 check #2 live sla_rate_crit_high snapshot the operator inspects reflects the corrected (NaT-excluded) computation"
    - "Phase 16 UAT Test 1: composed-report PDF shows the 4-gauge MTTR headline band (Critical/High/Medium/Low, each vs config.py SLA) with MoM direction arrows + focus-driven detail table"
    - "Phase 16 UAT Test 5: python scripts/capture_trend_snapshot.py runs without ModuleNotFoundError and writes mttr_overall_days/mttr_by_severity/mttr_by_owner"
    - "Phase 17 check 1/2/3: SMTP email render (4-tile KPI row + sparklines + Owner table), live sla_rate_crit_high snapshot round-trip, WeasyPrint PDF sparkline-row/table visual"
    - "Task 1 generates real artifacts (timestamped output/<group>/ dir + trend snapshot JSON carrying sla_rate_crit_high) BEFORE the Task 2/3 human checkpoints inspect them — the gate confirms existence, not just config validity"
  artifacts: []
  key_links:
    - from: "output/<group>/ PDF+Excel+email-preview + data/trend/trend_severity_all_assets.json (Task 1 generated artifacts)"
      to: "Task 2 (Phase 16 UAT) + Task 3 (Phase 17) human-verify checkpoints"
      via: "Task 1 SUMMARY records the artifact paths the operator opens; the Task 1 automated gate asserts those artifacts exist + the snapshot carries sla_rate_crit_high before the checkpoints run"
      pattern: "sla_rate_crit_high"
---

<objective>
Run the outstanding human verification debt ONCE against the fully-fixed build per D-07 (code first → verify-all at end → close). These are operator checkpoints: Claude prepares each artifact (renders the PDF, sends the email to a test inbox, runs the live snapshot capture) and the operator visually confirms. Per D-05, this MUST run after Plan 02 so the live `sla_rate_crit_high` value the operator inspects in Phase 17 check #2 is already correct.

Purpose: Close the Phase 16 UAT (tests 1,2,3,5) and Phase 17 (3 checks) human-verification debt that the milestone audit flagged.
Output: Operator-confirmed render/live checks; results captured for the Plan 09 status flips.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-mttr-rework/16-UAT.md
@.planning/phases/17-program-health-overview/17-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prepare all verification artifacts against the fixed build</name>
  <files>output/ (generated PDF/Excel/email-preview artifacts), data/trend/trend_severity_all_assets.json</files>
  <read_first>
    - .planning/phases/16-mttr-rework/16-UAT.md (tests 1, 2, 3, 5 expected outcomes; D-16-13 gauge-band design)
    - .planning/phases/17-program-health-overview/17-VERIFICATION.md L7-16 (the 3 human_verification entries + expected)
    - CLAUDE.md run_all.py invocation patterns (`--group`, `--no-email`); scripts/capture_trend_snapshot.py CLI
  </read_first>
  <action>
    Drive the suite end-to-end against the fixed build to produce the artifacts the operator will inspect, automating everything Claude can:
    - Phase 16 Test 1 + Phase 17 Test 3: run a composed_report delivery group that includes `mttr_trend` and `program_health` with `--no-email` to generate the PDF (4-gauge MTTR band + focus-driven table; program_health sparkline row + Owner velocity table). Record the output PDF path.
    - Phase 16 Test 2: confirm the same run's Excel has the "MTTR Trend" tab (owner default, window disclosure).
    - Phase 16 Test 3 + Phase 17 Test 1: render the delivery email body (and, where a test inbox is configured, send it) so the operator can inspect the MTTR panel + the 4-tile KPI row + sparklines + Owner table in a real client. If no SMTP test inbox is available, produce the email HTML preview artifact and note that the live-client send is the operator's step.
    - Phase 16 Test 5 + Phase 17 Test 2: run `python scripts/capture_trend_snapshot.py` (the documented invocation, from repo root) against the operator's data and report the written snapshot path + the `mttr_*` fields and the `sla_rate_crit_high` value (which now reflects the Plan 02 NaT-excluded computation). Confirm no ModuleNotFoundError (Phase 16 Test 5's prior blocker — already fixed per CONTEXT, re-confirm against this build).
    After generating, record in the SUMMARY: the exact timestamped `output/<group>/` directory path, the PDF + Excel + email-preview filenames within it, and the snapshot JSON path. The automated verify below confirms these artifacts actually exist (not just that config validates) so the Task 2/3 human checkpoints cannot be reached with artifacts of unknown state.
    List every produced artifact path in the SUMMARY for the operator checkpoint that follows.
  </action>
  <acceptance_criteria>
    - A timestamped `output/<group>/` directory was created this run and contains the composed_report PDF + Excel + email-preview artifacts at the reported paths.
    - `python scripts/capture_trend_snapshot.py` (or `--dry-run`) completes without ModuleNotFoundError; the written snapshot JSON exists and the newest entry contains `mttr_overall_days` and a (numeric-or-null) `sla_rate_crit_high` key.
    - `python run_all.py --dry-run` validates all groups (no regression from the code plans).
  </acceptance_criteria>
  <verify>
    <automated>python run_all.py --dry-run && python -c "import glob,os,json,sys; ds=sorted(glob.glob('output/*/'), key=os.path.getmtime); assert ds, 'no output/<group>/ dir generated'; d=ds[-1]; pdfs=glob.glob(d+'**/*.pdf', recursive=True); xls=glob.glob(d+'**/*.xlsx', recursive=True); assert pdfs, 'no PDF in '+d; assert xls, 'no Excel in '+d; snaps=glob.glob('data/trend/trend_severity_*.json'); assert snaps, 'no trend snapshot json'; obj=json.load(open(sorted(snaps, key=os.path.getmtime)[-1])); newest=obj['snapshots'][-1] if isinstance(obj, dict) else obj[-1]; assert 'sla_rate_crit_high' in newest, 'snapshot missing sla_rate_crit_high'; print('ARTIFACTS OK:', d, pdfs[0], xls[0])"</automated>
  </verify>
  <done>All verification artifacts generated against the fixed build, asserted to exist (output/<group>/ dir + PDF + Excel + snapshot carrying sla_rate_crit_high); paths recorded for the operator checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator verifies Phase 16 UAT (tests 1, 2, 3, 5)</name>
  <files>output/ (PDF + Excel from Task 1), data/trend/trend_severity_all_assets.json</files>
  <what-built>
    Against the fully-fixed Phase 19 build: a composed_report PDF (4-gauge MTTR headline band + focus-driven detail table), the matching Excel "MTTR Trend" tab, the rendered email MTTR panel, and a fresh `capture_trend_snapshot.py` run writing the MTTR fields. The Phase 16 D-16-13 gauge-band design and the Test-5 sys.path bootstrap are in the build.
  </what-built>
  <action>
    Present the operator with the Task 1 artifact paths and the four Phase 16 UAT checks to confirm. Do not modify code in this task — it is a verification gate. Capture the operator's pass/fail for each of tests 1, 2, 3, 5 verbatim into the SUMMARY.
  </action>
  <how-to-verify>
    1. Open the generated PDF (path in Task 1 SUMMARY). Confirm Test 1: a HEADLINE BAND of 4 per-severity MTTR gauges (Critical/High/Medium/Low, each vs its config.py SLA) each with a MoM direction arrow (▼ green improving / ▲ red slipping / — flat), fixed at top of page 1, plus the focus-driven detail table (Owner table when unfocused). The gauge graphic must be PRESENT (the round-2 blocker was a missing gauge).
    2. Open the Excel file. Confirm Test 2: an "MTTR Trend" tab with the window disclosure row and the owner-default columns (no spurious "SLA Target" on Owner rows).
    3. Inspect the email body (preview or test-inbox message). Confirm Test 3: the MTTR panel renders with the rolling-window + active-view footer; sub-threshold owners read "Insufficient data".
    4. Confirm Test 5: `python scripts/capture_trend_snapshot.py` ran without `ModuleNotFoundError: No module named 'config'` and the newest entry in trend_severity_all_assets.json carries mttr_overall_days / mttr_by_severity / mttr_by_owner.
  </how-to-verify>
  <verify>
    <human-check>Operator confirms Phase 16 UAT tests 1, 2, 3, 5 against the Task 1 artifacts.</human-check>
  </verify>
  <resume-signal>Type "approved" if all four pass, or describe which test failed (test number + what you saw) so a gap-closure plan can be scoped.</resume-signal>
  <done>Phase 16 UAT tests 1, 2, 3, 5 operator-confirmed (or failures recorded as gap items).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator verifies Phase 17 checks (1, 2, 3)</name>
  <files>output/ (PDF + email-preview from Task 1), data/trend/trend_severity_all_assets.json</files>
  <what-built>
    Against the fully-fixed build: a composed_report group including program_health delivered to (or previewed for) a test inbox; a live `capture_trend_snapshot.py` snapshot whose `sla_rate_crit_high` now uses the Plan 02 NaT-excluded shared helper; and a composed_report PDF with the program_health sparkline row + Owner velocity table.
  </what-built>
  <action>
    Present the operator with the Task 1 artifact paths and the three Phase 17 checks to confirm. Do not modify code in this task — it is a verification gate. Capture the operator's pass/fail for each of checks 1, 2, 3 verbatim into the SUMMARY.
  </action>
  <how-to-verify>
    1. Check 1 (email): the delivered/previewed email body contains the 4-tile KPI row (Open Critical / Net Velocity / SLA Posture (Crit+High) / MTTR (30-day)) with MoM arrows, sparkline images, and an Owner velocity table. Cold-start groups show "Trend Being Established" instead of NaN.
    2. Check 2 (live snapshot): inspect the written snapshot JSON — `sla_rate_crit_high` is a numeric float 0–100 (or null when zero Crit+High open), not a missing key. Sanity-check the value is plausible for your tenant; it should reflect the corrected denominator (NaT first_found rows excluded).
    3. Check 3 (PDF): the PDF shows 4 colored sparklines in a row above the Owner velocity table; outlier owners show the red "▲ Outlier" marker; cold-start renders the notice instead of sparklines.
  </how-to-verify>
  <verify>
    <human-check>Operator confirms Phase 17 checks 1, 2, 3 against the Task 1 artifacts.</human-check>
  </verify>
  <resume-signal>Type "approved" if all three pass, or describe which check failed so a gap-closure plan can be scoped.</resume-signal>
  <done>Phase 17 checks 1, 2, 3 operator-confirmed (or failures recorded as gap items).</done>
</task>

</tasks>

<verification>
- All three checkpoint tasks return "approved" (or any failures are captured as gap items for `/gsd-plan-phase 19 --gaps`).
- Results recorded in the SUMMARY for Plan 09 to flip 16-UAT.md / 17-VERIFICATION.md statuses.
</verification>

<success_criteria>
Phase 16 UAT tests 1,2,3,5 and Phase 17 checks 1,2,3 verified against the fixed build (D-07), with the corrected sla_rate_crit_high value confirmed (D-05).
</success_criteria>

<output>
Create `.planning/phases/19-v1-4-closure-int-warn-1-2-3-fixes-phase-17-human-verificatio/19-08-SUMMARY.md` when done, recording each test's pass/fail result for the closeout.
</output>
</content>
