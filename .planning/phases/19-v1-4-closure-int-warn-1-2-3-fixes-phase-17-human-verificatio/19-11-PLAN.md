---
phase: 19-v1-4-closure-int-warn-1-2-3-fixes-phase-17-human-verificatio
plan: 19-11
type: execute
wave: 6
depends_on: ["19-10"]
gap_closure: true
autonomous: true
files_modified:
  - reports/modules/program_health_module.py
  - tests/test_program_health_module.py
requirements: "none (gap closure — re-verification fixes for 19-10, no REQ-IDs)"

must_haves:
  truths:
    - "D-7: the composite RAG, the 'N of 4 On Track' count, and the narrative all consume the current-sign Net Velocity status (the same one the tile shows) — not the old delta-of-deltas sig2_status. With Open Critical red + Net Velocity green + SLA/MTTR missing the panel reads '1 of 4 On Track', narrative 'worsened on 3 of 4', composite stays red"
    - "D-17-06 missing-cap still applies only to genuinely missing signals (SLA, MTTR without history); Net Velocity is no longer 'missing' because current-sign always yields green/red/flat"
    - "D-8 single arrow: the PDF Net Velocity tile shows exactly ONE arrow (the embedded-arrow + appended-arrow double-render is removed)"
    - "D-8 two-row PDF layout: page-1 chart area is a top row of 4 uniform-height, uniform-font tiles (sparkline + one headline value + one arrow) and a separate bottom row of 4 caption cells aligned under each chart; definition text lives in the caption row, not inside the tiles"
    - "D-8 Net Velocity headline: the tile shows the net value only (e.g. 'net -8,272') at the same font as the other three tiles; the 'in {new} / fixed {fixed}' breakdown appears in that tile's caption cell below"
    - "Four-channel render contract and QUAL-03 empty-data/cold-start guards stay intact; module test suite green"
  artifacts:
    - "reports/modules/program_health_module.py compute() + PDF/email render channels updated"
    - "tests/test_program_health_module.py extended with D-7/D-8 regression tests"
---

<objective>
Close the two issues the operator found re-verifying Plan 19-10's Program Health polish:
(1) a correctness inconsistency where the composite RAG / "N of 4 On Track" / narrative still
use the old delta-of-deltas Net Velocity status while the tile shows the new current-sign
status, and (2) PDF Net Velocity tile defects (double arrow, shrunken font from the long
intake/fixed/net string) by moving captions/definitions out of the tiles into a row below and
showing only the net value in the tile. Authoritative design: the Revision 2026-06-26b section
of docs/superpowers/specs/2026-06-26-program-health-pdf-excel-polish-design.md (D-7, D-8). Do
not redesign or expand scope. Keep D-17-01 (no fixed_vulns_df threading).
</objective>

# Read before editing:
# - docs/superpowers/specs/2026-06-26-program-health-pdf-excel-polish-design.md  (D-7, D-8 — authoritative)
# - reports/modules/program_health_module.py  (compute(): signal_statuses ~L507; current-sign
#     net status ~L776; PDF: nv_curr_str arrow embed ~L1074; _render_sparkline_b64 title ~L943)
# - tests/test_program_health_module.py
# - ./CLAUDE.md  (four-channel contract, safe_* utils, CoW .assign(), inline-CSS email)

<tasks>

<task type="auto" tdd="true">
<title>Task 1 — D-7 composite consistency</title>
<objective>
Make the composite RAG, the "N of 4 On Track" count, and the narrative use the current-sign
Net Velocity status instead of the old delta-of-deltas sig2_status.
</objective>
<steps>
- Compute the current-sign Net Velocity status (net<0 → "green", net>0 → "red", net==0 →
  "flat") ONCE in compute(), before the composite block (it is currently derived later only for
  rendering at ~L776). Reuse the existing curr_net_delta value.
- Replace signal_statuses[1] (the Net Velocity slot, currently sig2_status from
  _signal_direction delta-of-deltas) with this current-sign status, so _composite_rag_od5(),
  green_count, missing_signal_names, and the narrative branches all see it.
- Leave the sparkline TREND series (history-based) unchanged — only the signal STATUS used for
  the composite/count/narrative changes.
- Keep sig2_status (delta-of-deltas) available if any sparkline/MoM annotation still needs it;
  otherwise remove if now unused (remove only what THIS change orphans).
- RED→GREEN tests: with Open Critical red, Net Velocity net<0, SLA & MTTR missing → green_count
  == 1, headline "1 / 4 On Track", narrative uses the "worsened on 3 of 4" branch, composite
  stays red; a net>0 case flips Net Velocity to red and is excluded from green_count.
</steps>
<commit>feat(19-11): D-7 composite RAG / on-track count / narrative use current-sign Net Velocity status</commit>
</task>

<task type="auto" tdd="true">
<title>Task 2 — D-8 PDF chart-row layout</title>
<objective>
Fix the double arrow and restructure the page-1 chart area into a uniform top tile row + a
caption row below, with Net Velocity showing only the net in the tile.
</objective>
<steps>
- Single arrow: stop embedding the arrow inside nv_curr_str (drop the trailing "{nv_arrow}" at
  ~L1074) OR pass mom_arrow="" for the Net Velocity tile — pick one so exactly one arrow renders
  via _render_sparkline_b64's "{current_val_str} {mom_arrow}".
- Net Velocity tile headline = net value only, e.g. "net -8,272" (thousands separator), uniform
  with the other three tiles' value font. Move the "in {new} / fixed {fixed}" breakdown into the
  caption cell below this tile.
- Two-row layout: render a top row of 4 tiles (sparkline + one headline value + one arrow, all
  uniform height/font) and, after a small vertical gap, a bottom row of 4 caption cells aligned
  under each chart. The verbatim definition captions (D-2) move from inside the tiles to the
  caption row. Net Velocity's caption cell also carries the "in / fixed" breakdown.
- Keep cold-start path: when cold_start, the tile row is replaced by the established-trend
  notice as today; caption row may still show definitions.
- html.escape all owner/label strings; safe_int/safe_format for values.
<human-check>Re-render the composed report (all-assets) to a real PDF and confirm via the actual render (NOT layout math — WeasyPrint flex/page-break quirks): top row = 4 uniform tiles with single arrows, Net Velocity tile shows only "net -8,272 v", caption row below with the intake/fixed breakdown under Net Velocity; page 2 still the Owner table alone.</human-check>
</steps>
<commit>feat(19-11): D-8 PDF single-arrow + two-row tile/caption layout, net-only Net Velocity tile</commit>
</task>

<task type="auto" tdd="true">
<title>Task 3 — email consistency + test sweep</title>
<objective>
Confirm the email panel reflects the D-7 fix and run the full module suite.
</objective>
<steps>
- Verify the email 4-tile panel "N of 4 On Track" + narrative now read from the shared
  current-sign status (Task 1 is in compute(), so email inherits it) — add an assertion that the
  email panel and PDF agree on the on-track count for the same data.
- Email Net Velocity tile: ensure a single arrow and the net value; the intake/fixed breakdown
  may stay in the tile's inline caption (email tiles are wider than PDF sparkline cells) — keep
  inline-CSS only, no <style> blocks.
- Run the full tests/test_program_health_module.py suite; cold-start + QUAL-03 empty-data
  guards stay green.
</steps>
<commit>test(19-11): D-7/D-8 regression + email/PDF on-track parity; full module suite green</commit>
</task>

</tasks>
