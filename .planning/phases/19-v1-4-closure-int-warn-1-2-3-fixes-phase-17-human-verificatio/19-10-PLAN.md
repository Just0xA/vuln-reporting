---
phase: 19-v1-4-closure-int-warn-1-2-3-fixes-phase-17-human-verificatio
plan: 19-10
type: execute
wave: 6
depends_on: []
gap_closure: true
autonomous: true
files_modified:
  - reports/modules/program_health_module.py
  - tests/test_program_health_module.py
requirements: "none (gap closure — closure/tech-debt phase, no REQ-IDs)"

must_haves:
  truths:
    - "Net Velocity status comes from the current-month net sign (net<0 green ▼, net>0 red ▲, net==0 flat), not delta-of-deltas — works with one month of data"
    - "compute() surfaces new_current and fixed_current; each owner row carries share_pct and asset_count"
    - "Owner table share_pct guards divide-by-zero (zero in-scope total → '—') and across owners sums to ~100%"
    - "PDF renders charts+captions on page 1 and the Owner table alone on page 2 via an explicit page-break — no mid-table straddle (verified by a real PDF render)"
    - "Each PDF chart shows its verbatim approved caption; Net Velocity shows 'in {new} / fixed {fixed} · net {net} {arrow}'; MTTR shows 'establishing from monthly snapshots' when mttr_current is None"
    - "Email Net Velocity tile shows intake/fixed/net + current-sign arrow; MTTR tile keeps '—' + establishing caption; per-tile definitions are inline-CSS, no <style> blocks"
    - "Excel Program Health row-header fill equals mttr_trend's _FILL_HEADER (E3F2FD); a definitions block is present; Owner Velocity tab has Share % and Assets columns"
    - "Cold-start and empty-data (QUAL-03) guards stay green — new values use safe_pct/safe_int/safe_format and never crash on zero rows"
  artifacts:
    - path: "reports/modules/program_health_module.py"
      provides: "compute() surfaced new_current/fixed_current + per-owner share_pct/asset_count; two-page PDF; captioned charts; readable Excel header + definitions; email/Excel intake/fixed/net"
      contains: "new_current"
    - path: "tests/test_program_health_module.py"
      provides: "velocity-direction-from-current-sign cases, surfaced-field assertions, share_pct sum/divide-by-zero guard, MTTR caption, Excel header-fill assertion"
      contains: "E3F2FD"
  key_links:
    - from: "reports/modules/program_health_module.py compute()"
      to: "metrics['new_current'] / metrics['fixed_current'] / metrics['net_velocity_status_current']"
      via: "surface curr_new/curr_fix and sign(net_current)"
      pattern: "new_current"
    - from: "render_excel_tabs Program Health tab header cells"
      to: "mttr_trend readable header style"
      via: "PatternFill solid fgColor E3F2FD + Font(bold=True)"
      pattern: "E3F2FD"
---

<objective>
Close the gaps the operator found during the Plan 19-08 human-verification checkpoint for the
Program Health Overview module. The design is **already approved and fully specified** in
`docs/superpowers/specs/2026-06-26-program-health-pdf-excel-polish-design.md` (locked decisions
D-1..D-6 + verbatim caption text). This plan turns that spec into executable, atomic-commit
tasks. **Do not redesign or add scope beyond the spec.**

Purpose: readability + current-month correctness fixes across the four render channels —
NOT the historical-data gap (forward-fill is the accepted answer; `fixed_vulns_df` stays
unthreaded per D-17-01).

Output: an updated `reports/modules/program_health_module.py` and its test file, delivered as
four atomic commits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# THE AUTHORITATIVE SPEC — all locked decisions D-1..D-6 + verbatim caption text
@docs/superpowers/specs/2026-06-26-program-health-pdf-excel-polish-design.md

# Operator findings + root cause (forward-fill accepted; do NOT re-fix the data gap)
@.planning/phases/19-v1-4-closure-int-warn-1-2-3-fixes-phase-17-human-verificatio/19-08-CHECKPOINT-FINDINGS.md

# Phase decisions — D-17-01 stays: do NOT thread fixed_vulns_df into program_health
@.planning/phases/19-v1-4-closure-int-warn-1-2-3-fixes-phase-17-human-verificatio/19-CONTEXT.md

# The module being changed (read compute() + all four render_* methods before editing)
@reports/modules/program_health_module.py

# Reuse THIS Excel header style on the Program Health tab (D-6)
@reports/modules/mttr_trend_module.py

# Existing tests — extend, do not rewrite
@tests/test_program_health_module.py

<interfaces>
<!-- Grounding facts extracted from the codebase. Use these directly. -->

Existing compute() locals already hold the velocity inputs (program_health_module.py ~461-471):
- curr_new = curr.get("new_findings_count");  curr_fix = curr.get("fixed_findings_count")
- curr_net_delta = float(curr_new) - float(curr_fix)   # net for the CURRENT month
  (prev_net_delta exists for the OLD delta-of-deltas sparkline status — leave that logic intact)

metrics dict already exposes (program_health_module.py ~744-774):
- "net_delta_current": curr_net_delta            # keep
- "signal_net_velocity_status": sig2_status      # delta-of-deltas — drives SPARKLINE trend; leave it
- sparkline_* series                              # leave intact
This plan ADDS: "new_current", "fixed_current", and a NEW current-sign status key
(e.g. "net_velocity_status_current") used by the current-value annotation/arrow.
The sparkline/MoM history logic (sig2_status, net_velocity_series) is UNCHANGED.

Owner rows are built ~653-660; each dict has owner/open_crit_high/prev_open/mom_delta/
mom_delta_pct/outlier. This plan ADDS "share_pct" and "asset_count" per row.
Per-owner asset_count comes from assets_df via extract_owner() (enriched already maps
asset_uuid→owner ~588-591); count assets per owner, NOT vuln rows.

Excel header styles (the readable target — mttr_trend_module.py:118, 1176-1177):
  _FILL_HEADER = PatternFill("solid", fgColor="E3F2FD")   # light blue, readable
  cell.font = Font(bold=True); cell.fill = _FILL_HEADER
program_health's UNREADABLE current header (program_health_module.py:1386):
  _FILL_HDR = PatternFill("solid", fgColor="1F3864")      # dark blue, black text → fix this

Verbatim approved captions (spec lines 40-45 — copy EXACTLY):
  Open Critical — open Critical-VPR findings — lower is better; ▼ green = falling
  Net Velocity  — new findings minus fixed this window (intake − fixed) — negative is good
                  (you fixed more than came in); ▼ green when fixed > intake, ▲ red when intake > fixed
  SLA Posture   — % of open Critical + High findings still within SLA — higher is better; ▲ green = rising
  MTTR          — average days to remediate (rolling 30-day) — lower is better; ▼ green = falling

Empty-data contract (CLAUDE.md): use safe_pct/safe_int/safe_format on any possibly-None value;
inline f-string format specs on None are forbidden. Inline-CSS-only email; no <style> blocks.

Test harness already present (tests/test_program_health_module.py): a snapshot-builder helper,
openpyxl.Workbook() Excel fixtures (~859), and render-channel tests (~844-1056). Extend these.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Compute layer — surface new/fixed; per-owner share% + assets; current-sign net status</name>
  <files>reports/modules/program_health_module.py, tests/test_program_health_module.py</files>
  <behavior>
    - metrics["new_current"] == curr_new, metrics["fixed_current"] == curr_fix (the values
      already read for the net delta — just exposed; None when absent).
    - A NEW current-sign status (e.g. metrics["net_velocity_status_current"]) derives from
      sign(curr_net_delta): net<0 → "green", net>0 → "red", net==0 → "flat". This does NOT
      replace signal_net_velocity_status (the delta-of-deltas sparkline status), which stays.
    - Each owner row gains "share_pct" = owner open_crit_high ÷ in-scope total open_crit_high
      (sum across owner rows). Zero/None total → share_pct = None (renders "—" via safe_pct).
    - Each owner row gains "asset_count" = number of assets owned by that owner from assets_df
      via extract_owner() (per-owner asset count, not vuln-row count).
    - Across owners with a non-zero total, sum of share_pct ≈ 100% (within float tolerance).
    - Cold-start ModuleData also carries new_current=None/fixed_current=None and the
      net_velocity_status_current key (value "flat"/None as appropriate) — no KeyError downstream.
  </behavior>
  <action>
    In compute() expose curr_new/curr_fix as metrics["new_current"]/metrics["fixed_current"]
    (mirroring the existing net_delta_current). Add metrics["net_velocity_status_current"] from
    the sign of curr_net_delta per the behavior block — net==0 maps to "flat". Do NOT alter
    sig2_status, prev_net_delta, net_velocity_series, or any sparkline/MoM history computation
    (D-3 changes the CURRENT-value annotation only). When building owner_rows, compute the
    in-scope total Open Crit+High once (sum of open_crit_high), then set each row's share_pct via
    safe division (zero/None total → None); compute per-owner asset_count from the already-built
    extract_owner() enrichment (count distinct assets per owner). Mirror new_current/fixed_current/
    net_velocity_status_current into _build_cold_start_result's metrics so renderers never KeyError.
    Use safe_* helpers for any formatting; never inline-format a possibly-None value.
    Add unit tests in test_program_health_module.py: net<0→green, net>0→red, net==0→flat;
    new_current/fixed_current surfaced and equal to snapshot inputs; share_pct sums to ~100%;
    zero-total owner set → share_pct None; asset_count present per owner.
  </action>
  <verify>
    <automated>python -m pytest tests/test_program_health_module.py -x -q</automated>
  </verify>
  <done>compute() surfaces new_current/fixed_current + net_velocity_status_current; owner rows carry share_pct (divide-by-zero guarded) and asset_count; new unit tests pass; existing compute tests stay green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PDF render — two-page split, verbatim captions, intake/fixed/net annotation, MTTR caption</name>
  <files>reports/modules/program_health_module.py, tests/test_program_health_module.py</files>
  <behavior>
    - render_pdf_section returns markup where the charts+captions+status line live on page 1 and
      the Owner Velocity table is preceded by an explicit page-break so it begins on page 2.
    - Each of the 4 charts shows its verbatim approved caption (D-2, spec lines 40-45) — exact text.
    - Net Velocity annotation reads "in {new} / fixed {fixed} · net {net} {arrow}" with thousands
      separators; the arrow + color come from the current net sign (▼ green net<0, ▲ red net>0,
      — flat net==0), using metrics["net_velocity_status_current"].
    - When mttr_current is None, the MTTR caption appends "establishing from monthly snapshots".
    - New Owner table columns (D-5): Owner | Open Crit+High | Share % | Assets | MoM Delta |
      MoM Delta %. Share % via safe_pct (None → "—"); Assets via safe_int. MoM columns blank ("—")
      until history accrues (forward-fill) — preserve the existing owner_mom_suppressed handling.
    - Cold-start and zero-row paths still render without crashing.
  </behavior>
  <action>
    Restructure render_pdf_section per D-1: keep header + "N of 4 signals on track — <RAG label>"
    status line + the 4-chart sparkline row + narrative on page 1; emit an explicit page-break
    element (the project's PDF page-break mechanism — match whatever board/management PDFs already
    use; do not invent a new CSS class) immediately before the Owner Velocity table so the table
    starts on page 2 and never straddles. Add the verbatim per-chart caption under each sparkline
    cell (copy the four caption strings exactly from the spec; html.escape any interpolated owner/
    tag strings, but the caption literals are static). Build the Net Velocity current annotation
    string "in {new} / fixed {fixed} · net {net} {arrow}" with thousands separators and the
    current-sign arrow/color; append "establishing from monthly snapshots" to the MTTR caption when
    mttr_current is None. Extend the Owner table header + body to the six D-5 columns, reading
    share_pct (safe_pct) and asset_count (safe_int) from the rows produced in Task 1; keep the
    owner_mom_suppressed branch so MoM Delta / MoM Delta % show "—" when history is absent. Do not
    change sparkline rendering or _render_sparkline_b64.
    Add tests asserting: the four verbatim caption strings are present; the Net Velocity annotation
    string format; the MTTR establishing caption appears only when mttr_current is None; the Owner
    table contains the Share %/Assets headers; a page-break element exists between the chart block
    and the Owner table.
  </action>
  <verify>
    <automated>python -m pytest tests/test_program_health_module.py -x -q -k "pdf or caption or owner or velocity or page_break"</automated>
    <human-check>Re-render the composed report (all-assets) to a real PDF and confirm via the actual render (NOT layout math — WeasyPrint flex/page-break quirks): page 1 = charts + captions + status line, page 2 = Owner table alone, with no mid-table split.</human-check>
  </verify>
  <done>PDF splits into two pages with the Owner table alone on page 2; every chart carries its verbatim caption; Net Velocity shows intake/fixed/net + current-sign arrow; MTTR shows the establishing caption when None; Owner table has the six D-5 columns; tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Email + Excel — intake/fixed/net + definitions; readable Excel header; Owner Velocity columns</name>
  <files>reports/modules/program_health_module.py, tests/test_program_health_module.py</files>
  <behavior>
    - Email Net Velocity tile shows the intake/fixed/net display + a colored current-sign arrow
      (D-3) using metrics["net_velocity_status_current"]; MTTR tile keeps "—" plus the
      "establishing from monthly snapshots" caption (D-4).
    - Email adds a compact one-line definition under each tile (or a short legend row) using the
      approved caption text, inline-CSS only — no <style> blocks; tile count/layout unchanged.
    - Excel Program Health tab: row-header cells use mttr_trend's readable style — fill
      PatternFill("solid", fgColor="E3F2FD") with Font(bold=True) (the current "1F3864" dark fill
      is removed). A small definitions block (the 4 approved lines) is written below the signal rows.
    - Excel Owner Velocity tab: columns match the new PDF table — Share % and Assets columns added
      (alongside Owner / Open Crit+High and the existing MoM Delta / Status when not suppressed).
    - Cold-start + zero-row Excel/email paths still render without crashing.
  </behavior>
  <action>
    In render_email_panel, update the Net Velocity tile to the intake/fixed/net display with a
    current-sign arrow (reuse the existing _arrow_html style but feed it the current-sign status),
    keep the MTTR "—" tile and add its establishing caption, and add a compact inline-CSS
    definition line per tile (or a single legend row) using the verbatim caption text — no <style>
    blocks, Outlook-safe. Do not change the tile count or the cold-start branch's structure beyond
    adding the same captions.
    In render_excel_tabs, replace _FILL_HDR usage on the Program Health header row with the
    mttr_trend readable style (fill fgColor "E3F2FD", Font(bold=True)); after the signal rows,
    write a small definitions block (4 rows, the approved caption text). Extend the Owner Velocity
    tab headers/widths and row writes to include Share % (safe_pct) and Assets (safe_int), matching
    the new PDF column set; preserve the mom_suppressed branch.
    Add tests: Program Health header-cell fill equals "E3F2FD" (assert the PatternFill fgColor);
    a definitions block is present on the Program Health tab; the Owner Velocity tab header row
    contains "Share %" and "Assets"; the email body contains the Net Velocity intake/fixed/net
    string and the MTTR establishing caption and the per-tile definition text, and contains no
    "<style" substring.
  </action>
  <verify>
    <automated>python -m pytest tests/test_program_health_module.py -x -q -k "excel or email or header or definition or owner_velocity"</automated>
  </verify>
  <done>Email Net Velocity shows intake/fixed/net + current-sign arrow with per-tile inline-CSS definitions; MTTR caption present; Excel Program Health header fill == E3F2FD with a definitions block; Owner Velocity tab has Share %/Assets; tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Test sweep — guards + cold-start + full-suite green</name>
  <files>tests/test_program_health_module.py</files>
  <behavior>
    - The velocity-direction cases (net<0/>0/==0), surfaced-field, share_pct sum + divide-by-zero,
      asset_count, MTTR-caption, and Excel header-fill assertions added in Tasks 1-3 all pass.
    - Cold-start render (all three channels) and empty-data/zero-row (QUAL-03) tests stay green —
      no crashes, no NaN%, safe defaults intact.
    - The four-channel render contract and empty-data guards remain intact (no renderer raises).
  </behavior>
  <action>
    Consolidate/round out the test file: ensure the existing render-channel tests
    (test_all_channels_render_normal / _cold_start / _zero_row, test_email_no_nan_percent,
    test_analyst_tabs_aggregate_only) still pass against the new columns/captions, adjusting any
    assertions that are tightly coupled to the OLD Owner table column set or the OLD dark Excel
    fill. Add an explicit cold-start assertion that new_current/fixed_current/
    net_velocity_status_current keys exist and the MTTR establishing caption renders. Run the full
    module test file and confirm everything is green. Do NOT relax an assertion to hide a real
    regression — if a guard test fails, fix the renderer, not the test.
  </action>
  <verify>
    <automated>python -m pytest tests/test_program_health_module.py -q</automated>
  </verify>
  <done>The full program_health test file passes; velocity-direction, surfaced-field, share% sum/divide-by-zero, MTTR-caption, and Excel header-fill assertions are present and green; cold-start + empty-data guards stay green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| snapshot store → module | Owner-name / count fields read from trend snapshots (no asset-level PII) |
| module → PDF/Excel/email markup | Owner/tag strings interpolated into HTML/PDF must be escaped |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-10-01 | Information Disclosure | new Share %/Assets owner columns | mitigate | Aggregate-only owner-name + counts; no UUIDs/IPs/hostnames/plugin IDs (QUAL-05 preserved — analyst rows unchanged) |
| T-19-10-02 | Tampering/XSS | verbatim captions + intake/fixed/net annotation in PDF/email | mitigate | Caption literals are static; any interpolated owner/tag strings keep html.escape() (T-17-07) |
| T-19-10-03 | Denial of Service | zero-total share% / None values | mitigate | safe_pct/safe_int/safe_format guards; zero denominator → "—"; empty-data path unchanged (QUAL-03) |
| T-19-10-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan — pure edits to existing module + tests |
</threat_model>

<verification>
- `python -m pytest tests/test_program_health_module.py -q` is fully green.
- Spec coverage: D-1 two-page PDF (page-break before Owner table), D-2 verbatim captions,
  D-3 current-sign Net Velocity intake/fixed/net (email + PDF), D-4 MTTR establishing caption +
  no fixed_vulns_df threading, D-5 six Owner-table columns, D-6 readable Excel header + definitions
  + Owner Velocity Share %/Assets — all present.
- Human (required for D-1): a REAL PDF render of the composed report (all-assets) confirms page 1 =
  charts+captions, page 2 = Owner table alone, no mid-table split (WeasyPrint page-break quirks —
  do NOT verify via layout math).
- D-17-01 preserved: program_health is NOT added to _MODULES_NEEDING_FIXED_VULNS; no fixed_vulns_df
  is threaded.
</verification>

<success_criteria>
- All four tasks delivered as four atomic commits.
- compute() surfaces new_current/fixed_current + a current-sign net status; owner rows carry
  share_pct (divide-by-zero guarded) and asset_count.
- PDF: two-page split with captioned charts and the six-column Owner table on page 2.
- Email: Net Velocity intake/fixed/net + current-sign arrow; MTTR establishing caption; per-tile
  inline-CSS definitions (no <style>).
- Excel: readable header fill (E3F2FD) + definitions block; Owner Velocity Share %/Assets columns.
- Four-channel render contract and empty-data/cold-start guards (QUAL-03) intact and green.
</success_criteria>

<output>
Create `.planning/phases/19-v1-4-closure-int-warn-1-2-3-fixes-phase-17-human-verificatio/19-10-SUMMARY.md` when done.
</output>
