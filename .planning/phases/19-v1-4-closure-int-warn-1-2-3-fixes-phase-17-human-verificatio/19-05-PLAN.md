---
phase: 19-v1-4-closure-int-warn-1-2-3-fixes-phase-17-human-verificatio
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - reports/modules/composer.py
  - delivery/email_sender.py
  - reports/modules/aged_vulns_assets_module.py
  - reports/modules/high_risk_assets_module.py
  - tests/test_composer_fail_soft.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "CR-F1: composer assemble_pdf validates render_pdf_section() return is a str before .strip() — a non-string return converts to a safe fallback instead of crashing assembly"
    - "CR-F2: composer assemble_analyst_workbook still emits a _Metadata tab when every module FAILED (failures non-empty, collected empty) — no-data and all-failed paths are split"
    - "CR-F4: email_sender prebuilt_charts (management_summary) inline images are size-budget-checked like email_inline_images (respecting MAX_ATTACHMENT_SIZE_MB)"
    - "CR-D1: aged_vulns_assets_module docstring/PDF copy/audit metadata say 'highest risk score / sorted by risk_score DESC' matching the actual sort (no logic change)"
    - "CR-D2: high_risk_assets_module copy + get_audit_info() reference risk_score ordering and Owner via extract_owner() (no logic change)"
  artifacts:
    - path: "reports/modules/composer.py"
      provides: "CR-F1 return-type guard + CR-F2 metadata-on-all-fail split"
      contains: "isinstance"
    - path: "delivery/email_sender.py"
      provides: "CR-F4 prebuilt_charts size-budget check"
      contains: "prebuilt_charts"
    - path: "tests/test_composer_fail_soft.py"
      provides: "CR-F1/F2 regression tests"
      exports: ["test_non_string_pdf_section_safe", "test_metadata_tab_on_all_modules_failed"]
  key_links:
    - from: "reports/modules/composer.py::assemble_pdf"
      to: "instance.render_pdf_section"
      via: "isinstance(str) guard before .strip()"
      pattern: "isinstance"
    - from: "delivery/email_sender.py"
      to: "prebuilt_charts size-budget helper"
      via: "per-image byte check before MIMEImage attach"
      pattern: "prebuilt_charts"
---

<objective>
Close the Work-stream-C fail-soft / module-render findings (CR-F1, CR-F2, CR-F4) and the Work-stream-D module-copy accuracy findings (CR-D1, CR-D2). These harden the composer against a misbehaving module and align audit/copy text with the actual risk_score sort.

Purpose: A single module returning a non-string PDF section, or all modules failing, must not crash or silently drop the audit metadata; management_summary inline charts must respect the attachment budget; module copy must not lie about its sort.
Output: Composer return-type guard + metadata-on-all-fail split + email size-budget on prebuilt_charts + two copy/audit corrections; regression tests for F1/F2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-v1-4-closure-int-warn-1-2-3-fixes-phase-17-human-verificatio/19-PATTERNS.md
@.planning/phases/19-v1-4-closure-int-warn-1-2-3-fixes-phase-17-human-verificatio/19-CODERABBIT-TRIAGE.md

<interfaces>
<!-- CLAUDE.md: render channels are concrete with no-op defaults; fail-soft batch semantics (a module render error must not kill the batch). -->
From reports/modules/composer.py:
  ReportComposer.assemble_pdf(...)              # L653-681 fail-soft per-module render
  ReportComposer.assemble_analyst_workbook(...) # L1073-1166; _Metadata tab + failures audit

From delivery/email_sender.py:
  # email_inline_images path already has a per-image size-budget check (MAX_ATTACHMENT_SIZE_MB, default 25)
  # prebuilt_charts path (L487-501) attaches base64 PNG with NO budget check — must mirror the existing helper
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CR-F1 + CR-F2 — composer return-type guard and metadata-on-all-fail</name>
  <files>reports/modules/composer.py, tests/test_composer_fail_soft.py</files>
  <read_first>
    - reports/modules/composer.py L653-681 (assemble_pdf per-module render + existing except fallback) and L1073-1166 (assemble_analyst_workbook; the `if not collected` branch and the _Metadata + failures write)
    - 19-PATTERNS.md L339-376 (CR-F1 isinstance guard before .strip(); CR-F2 split all-empty vs all-failed and still emit _Metadata)
    - CLAUDE.md "PDF assembly note" + fail-soft batch semantics
  </read_first>
  <behavior>
    - test_non_string_pdf_section_safe: a stub module whose render_pdf_section returns None (or an int) does not crash assemble_pdf — it is converted to a safe fallback / skipped, and the PDF for the other modules still assembles.
    - test_metadata_tab_on_all_modules_failed: when every module errors (collected empty, failures non-empty), assemble_analyst_workbook still writes a workbook containing a _Metadata tab with the failure audit (does not return None / drop the tab).
    - test_no_data_still_returns_none: when there are no failures and no data (genuinely empty), assemble_analyst_workbook returns None (no file) — the original no-data behavior is preserved.
  </behavior>
  <action>
    CR-F1 (L653-681): after `html_section = instance.render_pdf_section(data, config)`, add `if not isinstance(html_section, str):` → log a warning naming the module_id and the returned type, set `html_section = ""`; then keep the existing `if not html_section or not html_section.strip(): continue`. This prevents `.strip()` on a non-string.
    CR-F2 (L1158-1166 region): split the `if not collected:` branch — if `failures` is empty, keep the existing "skip workbook / return None" path; if `failures` is non-empty, log a warning and fall through to workbook creation so the `_Metadata` tab (with the failure audit) is still emitted. Mirror the existing `_Metadata` + failures write that runs when `collected` is non-empty (19-PATTERNS.md L362-376).
    Create tests/test_composer_fail_soft.py with the three behaviors using small stub modules (register a throwaway module or monkeypatch render_pdf_section / force a compute failure). Keep fixtures synthetic (QUAL-05).
  </action>
  <acceptance_criteria>
    - `pytest tests/test_composer_fail_soft.py -q -o addopts=""` exits 0 with all three tests passing.
    - assemble_pdf contains an `isinstance(html_section, str)` guard before any `.strip()` on the section — `grep -n "isinstance" reports/modules/composer.py` shows it in the assemble_pdf region.
    - The all-failed analyst path emits `_Metadata` — the test_metadata_tab_on_all_modules_failed assertion confirms a workbook is written with a `_Metadata` sheet.
    - Existing composer tests still pass: `pytest tests/ -k composer -q -o addopts=""` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_composer_fail_soft.py -q -o addopts=""</automated>
  </verify>
  <done>Non-string PDF sections are safe; all-failed analyst runs still emit _Metadata; no-data still returns None; three tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: CR-F4 — prebuilt_charts size-budget check in email_sender</name>
  <files>delivery/email_sender.py</files>
  <read_first>
    - delivery/email_sender.py L487-501 (prebuilt_charts decode + MIMEImage attach with no budget) and the existing `email_inline_images` size-budget helper in the same file
    - 19-PATTERNS.md L393-397 (CR-F4 fix: apply the same size-budget helper, cap from MAX_ATTACHMENT_SIZE_MB)
    - CLAUDE.md email-delivery rules (MAX_ATTACHMENT_SIZE_MB default 25; on exceed: log warning, omit, note in body)
  </read_first>
  <action>
    Locate the per-image size-budget helper / check used by the `email_inline_images` path. Apply the same check to each `prebuilt_charts` entry (the management_summary inline images) before decoding/attaching: compute the byte size of the decoded PNG, compare against the per-image cap derived from `MAX_ATTACHMENT_SIZE_MB`, and on exceed log a warning (naming the cid) and skip that image rather than attaching an oversized payload — consistent with the existing helper's behavior. Do not change the `email_inline_images` path.
  </action>
  <acceptance_criteria>
    - The prebuilt_charts loop in delivery/email_sender.py performs a size check before attaching — `grep -n "MAX_ATTACHMENT_SIZE_MB" delivery/email_sender.py` shows the cap is referenced on the prebuilt_charts path (or a shared helper is called there).
    - `python -c "import delivery.email_sender"` (repo root) imports cleanly.
    - Existing email_sender tests still pass: `pytest tests/ -k "email_sender or email" -q -o addopts=""` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/ -k "email_sender or email" -q -o addopts=""</automated>
  </verify>
  <done>prebuilt_charts inline images respect the attachment size budget like email_inline_images.</done>
</task>

<task type="auto">
  <name>Task 3: CR-D1 + CR-D2 — module copy/audit metadata accuracy (no logic change)</name>
  <files>reports/modules/aged_vulns_assets_module.py, reports/modules/high_risk_assets_module.py</files>
  <read_first>
    - reports/modules/aged_vulns_assets_module.py L115-116 (docstring/PDF copy saying "highest percentage / affected DESC, percentage DESC")
    - reports/modules/high_risk_assets_module.py L117-119 (copy + get_audit_info() saying "highest percentage" + Application-tag grouping)
    - 19-PATTERNS.md L401-413 (CR-D1/D2 — text-only alignment to the actual risk_score sort + Owner via extract_owner())
    - CLAUDE.md memory project_business_unit_interim_application (Owner is the reporting dimension)
  </read_first>
  <action>
    CR-D1 (aged_vulns_assets_module.py L115-116): update the docstring and any PDF/audit copy to say "highest risk score" / "sorted by risk_score DESC" to match the actual `table_data`/`top_5` sort. Text only — do NOT change the sort logic.
    CR-D2 (high_risk_assets_module.py L117-119): update the copy and `get_audit_info()` text from "highest percentage" + "Application-tag grouping" to "highest risk score" ordering and "Owner" grouping via `extract_owner()`. Text only — no logic change.
  </action>
  <acceptance_criteria>
    - `grep -ni "highest percentage" reports/modules/aged_vulns_assets_module.py reports/modules/high_risk_assets_module.py` returns nothing (the inaccurate phrasing is gone).
    - `grep -ni "risk_score\|risk score" reports/modules/aged_vulns_assets_module.py reports/modules/high_risk_assets_module.py` shows the corrected copy.
    - high_risk_assets_module get_audit_info references Owner/extract_owner — `grep -ni "owner" reports/modules/high_risk_assets_module.py` shows it in the audit/copy text.
    - Existing module tests still pass: `pytest tests/ -k "aged_vulns or high_risk" -q -o addopts=""` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/ -k "aged_vulns or high_risk" -q -o addopts=""</automated>
  </verify>
  <done>Both modules' copy/audit metadata describe the real risk_score sort + Owner grouping; no logic changed.</done>
</task>

</tasks>

<verification>
- `pytest tests/test_composer_fail_soft.py -q -o addopts=""` → exits 0.
- `pytest tests/ -k "composer or email_sender or aged_vulns or high_risk" -q -o addopts=""` → exits 0.
- `python -c "import reports.modules.composer, delivery.email_sender"` (repo root) imports cleanly.
</verification>

<success_criteria>
CR-F1, CR-F2, CR-F4, CR-D1, CR-D2 all closed; composer is crash-proof against a misbehaving module; inline charts respect the budget; copy matches the sort.
</success_criteria>

<output>
Create `.planning/phases/19-v1-4-closure-int-warn-1-2-3-fixes-phase-17-human-verificatio/19-05-SUMMARY.md` when done
</output>
