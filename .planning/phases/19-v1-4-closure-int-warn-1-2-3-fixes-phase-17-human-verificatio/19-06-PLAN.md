---
phase: 19-v1-4-closure-int-warn-1-2-3-fixes-phase-17-human-verificatio
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/test_groups.py
  - tests/test_consumer_audit.py
  - tests/baselines/management_summary_structural_schema.py
  - tests/baselines/management_summary_value_golden.json
  - data/fetchers.py
  - scripts/update_from_github.sh
autonomous: true
requirements: []

must_haves:
  truths:
    - "CR-T1: tests/e2e/test_groups.py guards/skips _GROUPS[0] access when _load_groups() is empty (no import-time collection break)"
    - "CR-T2: test_consumer_audit _PASS_THROUGH_CALLERS uses module-qualified file::function identifiers (not bare function names)"
    - "CR-T6: management_summary_structural_schema pdf_page_count is not treated as drift when derived from the WeasyPrint-fallback heuristic"
    - "CR-T7: management_summary_value_golden.json _meta.fixture_dir is repo-relative (no machine-specific absolute Windows path)"
    - "CR-B5: fetch_fixed_vulnerabilities includes lookback_days in the cache key (vulns_fixed_{N}d) so different windows do not reuse the same parquet"
    - "18-REVIEW IN-02: dead _first_str helper (and the deprecated fetch_vulnerabilities chain) removed from data/fetchers.py"
    - "CR-U1: update_from_github.sh anchors non-absolute symlink targets against INSTALL_ROOT only via a shared resolver helper (no releases/releases/ duplication)"
    - "CR-U2: update_from_github.sh refuses --force re-extraction on the active release dir (or stages-then-swaps) so rollback is not broken"
  artifacts:
    - path: "tests/e2e/test_groups.py"
      provides: "Empty-groups guard"
      contains: "skip"
    - path: "data/fetchers.py"
      provides: "lookback_days-keyed fixed cache + _first_str removed"
      contains: "vulns_fixed_"
    - path: "scripts/update_from_github.sh"
      provides: "symlink resolver helper + --force active-release refusal"
      contains: "INSTALL_ROOT"
  key_links:
    - from: "data/fetchers.py::fetch_fixed_vulnerabilities"
      to: "_cache_path(cache_dir, f\"vulns_fixed_{lookback_days}d\")"
      via: "lookback_days folded into the parquet dataset name"
      pattern: "vulns_fixed_"
---

<objective>
Close the Work-stream-E test-rigor / portability findings (CR-T1, CR-T2, CR-T6, CR-T7), the CR-B5 fixed-vuln cache-key correctness fix (with the IN-02 dead-helper removal in the same fetcher), and the Work-stream-F deploy-script findings (CR-U1, CR-U2). These tighten test correctness and remove a deploy-time rollback hazard.

Purpose: Make tests fail when they should (not silently pass on empty groups, broad caller matches, env-dependent page counts, or absolute fixture paths); stop different lookback windows colliding in the fixed cache; make the updater symlink/rollback logic safe.
Output: Four test-rigor fixes + CR-B5 cache key + _first_str removal + two updater hardening fixes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-v1-4-closure-int-warn-1-2-3-fixes-phase-17-human-verificatio/19-PATTERNS.md
@.planning/phases/19-v1-4-closure-int-warn-1-2-3-fixes-phase-17-human-verificatio/19-CODERABBIT-TRIAGE.md

<interfaces>
From data/fetchers.py:
  _cache_path(cache_dir: Path, dataset: str) -> Path   # returns cache_dir / f"{dataset}.parquet"  (L203-204)
  fetch_fixed_vulnerabilities(..., lookback_days=...)   # L379-435; cache key currently "vulns_fixed" (ignores lookback_days)
  _first_str(...)                                        # L163; only used by deprecated fetch_vulnerabilities (L973)

From scripts/update_from_github.sh:
  # symlink resolver L327-331; --force re-extraction L904-916; INSTALL_ROOT layout; current/.last rollback symlinks
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test-rigor fixes — CR-T1, CR-T2, CR-T6, CR-T7</name>
  <files>tests/e2e/test_groups.py, tests/test_consumer_audit.py, tests/baselines/management_summary_structural_schema.py, tests/baselines/management_summary_value_golden.json</files>
  <read_first>
    - tests/e2e/test_groups.py L103 (the unconditional `_GROUPS[0]` import-time access)
    - tests/test_consumer_audit.py L552-555 (_PASS_THROUGH_CALLERS bare-name set)
    - tests/baselines/management_summary_structural_schema.py L81-102 (pdf_page_count drift check)
    - tests/baselines/management_summary_value_golden.json L4 (_meta.fixture_dir absolute Windows path)
    - 19-PATTERNS.md L417-478 (CR-T1/T2/T6/T7 fix patterns)
    - CLAUDE.md memory project_full_suite_test_collection_quirks (the level1/level2 collection-break class)
  </read_first>
  <action>
    CR-T1 (test_groups.py L103): guard the module-level `_GROUPS[0]` access — when `_load_groups()` returns empty, `pytest.skip("No delivery groups configured — skipping e2e group tests")` (or restructure so the index access is inside a test guarded by a skipif), per 19-PATTERNS.md L421-427. Import-time indexing must not break collection.
    CR-T2 (test_consumer_audit.py L552-555): change `_PASS_THROUGH_CALLERS` from bare function names to module-qualified `"<path>::<function>"` identifiers (e.g. `"reports/management_summary.py::run_report"`), and update the matching logic to compare against the qualified form so the exemption no longer matches any `run_report` everywhere.
    CR-T6 (management_summary_structural_schema.py L81-102): make the `pdf_page_count` comparison not flag drift when the count came from the WeasyPrint-fallback heuristic (skip/soft-assert when WeasyPrint is unavailable or the count is heuristic-derived), per 19-PATTERNS.md L472-474.
    CR-T7 (management_summary_value_golden.json L4): replace the hardcoded absolute Windows path in `_meta.fixture_dir` with a repo-relative value (e.g. `"tests/baselines/fixtures"` or `"."`).
  </action>
  <acceptance_criteria>
    - `pytest tests/e2e/test_groups.py tests/test_consumer_audit.py -q -o addopts=""` collects and exits 0 (no import-time IndexError even with empty groups; emulate by confirming the guard exists).
    - `grep -n "::" tests/test_consumer_audit.py` shows `_PASS_THROUGH_CALLERS` entries contain `::` (file-qualified).
    - `grep -niE "[A-Z]:\\\\|/Users/|/home/" tests/baselines/management_summary_value_golden.json` returns nothing (no absolute machine path); `_meta.fixture_dir` is repo-relative.
    - `pytest tests/baselines/ -k management_summary -q -o addopts=""` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/e2e/test_groups.py tests/test_consumer_audit.py -q -o addopts=""</automated>
  </verify>
  <done>Empty-groups guard, file-qualified callers, env-tolerant page count, repo-relative fixture path all in place.</done>
</task>

<task type="auto">
  <name>Task 2: CR-B5 fixed-vuln cache key + IN-02 dead _first_str removal</name>
  <files>data/fetchers.py</files>
  <read_first>
    - data/fetchers.py L379-435 (fetch_fixed_vulnerabilities + the `_cache_path(cache_dir, "vulns_fixed")` call at ~L432) and L203-204 (_cache_path)
    - data/fetchers.py L163 (_first_str def) and L973 (its only caller — deprecated fetch_vulnerabilities)
    - 19-PATTERNS.md L268-283 (CR-B5 one-line cache-key change) and L580-582 (IN-02 dead _first_str removal — confirm no other callers via Grep)
    - CLAUDE.md memory project_tenable_fixed_retention_trend (why lookback_days matters for the fixed cache)
  </read_first>
  <action>
    CR-B5: change the fixed-vuln cache dataset name to include the window — `_cache_path(cache_dir, f"vulns_fixed_{lookback_days}d")` — at the L432 site, so different lookback windows write/read distinct parquet files. This is the one-line change in 19-PATTERNS.md L280; confirm no other caller depends on the bare `vulns_fixed.parquet` basename.
    IN-02: confirm with Grep that `_first_str` (L163) is referenced only by the deprecated `fetch_vulnerabilities` (L973). Per D-02 (fix-all incl. dead code): if `fetch_vulnerabilities` is itself dead/deprecated and safe to remove, remove `_first_str` and the deprecated chain together; if `fetch_vulnerabilities` is still imported anywhere (Grep its name across the repo), remove only `_first_str` if it has zero live callers, otherwise leave `_first_str` and note the chain remains until the deprecated fetcher is retired. Do NOT break any live import — Grep first, decide, document the decision in the SUMMARY.
  </action>
  <acceptance_criteria>
    - `grep -c "vulns_fixed_" data/fetchers.py` >= 1 (lookback_days folded into the dataset name).
    - `python -c "import data.fetchers"` (repo root) imports cleanly after the change.
    - If `_first_str` removed: `grep -c "_first_str" data/fetchers.py` == 0; if retained: the SUMMARY documents why (live deprecated caller remains).
    - Existing fetcher tests still pass: `pytest tests/ -k "fetcher or fetch" -q -o addopts=""` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/ -k "fetcher or fetch" -q -o addopts=""</automated>
  </verify>
  <done>Fixed-vuln cache key includes lookback_days; dead _first_str removed (or retention documented).</done>
</task>

<task type="auto">
  <name>Task 3: CR-U1 + CR-U2 — update_from_github.sh symlink resolver + --force active-release refusal</name>
  <files>scripts/update_from_github.sh</files>
  <read_first>
    - scripts/update_from_github.sh L327-331 (symlink target normalization) and L904-916 (--force re-extraction path)
    - 19-PATTERNS.md L484-507 (CR-U1 resolver helper; CR-U2 refuse-on-active-release)
    - .planning memory feedback_real_env_verification + project_updater_python3_dependency (updater is RHEL/systemd; verify intent, real-VM check is the operator's)
  </read_first>
  <action>
    CR-U1 (L327-331): extract a `_resolve_target()` helper that echoes the target unchanged when absolute (`/*`) and prepends `${INSTALL_ROOT}/` when relative, and route the symlink-target normalization through it so a legacy `releases/v1.2.0` cannot duplicate to `INSTALL_ROOT/releases/releases/...`. Share the helper at both normalization sites.
    CR-U2 (L904-916): before `--force` re-extraction deletes/rebuilds a release dir, resolve the active release (`readlink -f "${INSTALL_ROOT}/current"`) and refuse with a clear error + non-zero exit when the target dir is the active release (so rollback's `PREV`/`.last` is not pointed at a rebuilt tree) — or stage to a temp dir then atomically swap. Match the existing error/exit-code style in the script (usage_error / fail-loud exit pattern).
  </action>
  <acceptance_criteria>
    - `bash -n scripts/update_from_github.sh` exits 0 (script parses).
    - `grep -c "_resolve_target" scripts/update_from_github.sh` >= 2 (def + at least one call) OR the inline absolute-vs-relative anchoring is present at both normalization sites.
    - `grep -nE "current|readlink" scripts/update_from_github.sh` shows the --force path resolves the active release before destructive re-extraction.
    - The --force refusal emits a non-zero exit on the active release (confirm the guard branch calls the script's error/exit helper).
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/update_from_github.sh</automated>
  </verify>
  <done>Symlink targets anchored to INSTALL_ROOT; --force refuses the active release; script parses clean. (Real RHEL/systemd behavior is operator-verified per project memory.)</done>
</task>

</tasks>

<verification>
- `pytest tests/e2e/test_groups.py tests/test_consumer_audit.py tests/baselines/ -k "groups or consumer or management_summary" -q -o addopts=""` → exits 0.
- `python -c "import data.fetchers"` (repo root) imports cleanly.
- `bash -n scripts/update_from_github.sh` → exits 0.
</verification>

<success_criteria>
CR-T1/T2/T6/T7, CR-B5, IN-02 (_first_str), CR-U1/U2 all closed; tests fail when they should; fixed cache is window-keyed; updater rollback is safe.
</success_criteria>

<output>
Create `.planning/phases/19-v1-4-closure-int-warn-1-2-3-fixes-phase-17-human-verificatio/19-06-SUMMARY.md` when done
</output>
