{"type":"review_context","reviewType":"all","currentBranch":"main","baseBranch":"main","baseCommit":"f3f1c4f","workingDirectory":"/mnt/d/Projects/vuln-reporting"}
{"type":"status","phase":"connecting","status":"connecting_to_review_service"}
{"type":"status","phase":"analyzing","status":"setting_up","message":"Just0xA/vuln-reporting is not connected to a CodeRabbit organization you can access, so this review will use the free CLI allowance.\n\nIf you expected an org plan here, check your signed-in account with coderabbit auth status, or install CodeRabbit for Just0xA/vuln-reporting at https://app.coderabbit.ai"}
{"type":"status","phase":"setup","status":"setting_up"}
{"type":"status","phase":"analyzing","status":"summarizing"}
{"type":"status","phase":"analyzing","status":"reviewing"}
{"type":"heartbeat","status":"reviewing"}
{"type":"finding","severity":"major","fileName":"reports/modules/scan_coverage_sla_module.py","codegenInstructions":"Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate.\n\nIn @reports/modules/scan_coverage_sla_module.py around lines 555 - 557, Untrusted values are being inserted directly into the HTML output in the error-box rendering path, so escape them before interpolation. Update the HTML construction around the error display in the relevant rendering method(s) that use data.error and the owner label strings, using a proper HTML-escaping helper consistently in both locations referenced by the review. Ensure the rendered error message still shows the same content, but with any markup characters safely escaped.","suggestions":[]}
{"type":"finding","severity":"major","fileName":"reports/owner_supplemental.py","codegenInstructions":"Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate.\n\nIn @reports/owner_supplemental.py around lines 246 - 248, The output directory setup in the owner supplemental report flow currently allows any path, including the forbidden trend storage location, so add a runtime guard before Path(output_dir) and mkdir to reject data/trend when this function is called. Update the relevant function in reports/owner_supplemental.py to enforce the documented PII output-path policy by checking the incoming output_dir value and failing fast if it resolves to the restricted trend storage path.","suggestions":["    output_dir = Path(output_dir).resolve()\n    parts = output_dir.parts\n    if any(parts[i] == \"data\" and parts[i + 1] == \"trend\" for i in range(len(parts) - 1)):\n        raise ValueError(\"owner_supplemental output_dir must not be under data/trend/\")\n    output_dir.mkdir(parents=True, exist_ok=True)"]}
{"type":"finding","severity":"minor","fileName":"tests/baselines/management_summary_structural_schema.py","codegenInstructions":"Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate.\n\nIn @tests/baselines/management_summary_structural_schema.py around lines 81 - 102, The page-count comparison in _pdf_page_count_from_html is environment-dependent because it returns a WeasyPrint-derived count when available and a fallback heuristic otherwise. Update the management summary smoke/diff logic to avoid treating pdf_page_count as drift when the count came from the fallback path, or make WeasyPrint a required prerequisite before asserting that field. Use the existing _pdf_page_count_from_html helper and the pdf_page_count comparison in the baseline/diff flow to locate the fix.","suggestions":[]}
{"type":"finding","severity":"minor","fileName":"scripts/smoke_management_summary_cutover.py","codegenInstructions":"Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate.\n\nIn @scripts/smoke_management_summary_cutover.py around lines 1 - 7, The top-level docstring and the nearby run comment/print in the smoke script still describe the old bespoke management_summary path, but `_dispatch_management_summary` now returns `result[\"_bundle\"]`. Update the opening summary and the “Run bespoke management_summary” wording to describe the Plan 04 `_bundle`/post-cutover behavior consistently. Keep the references aligned with `_dispatch_management_summary`, `result[\"_bundle\"]`, and the surrounding smoke-run messaging so the script documentation matches the actual execution path.","suggestions":[]}
{"type":"finding","severity":"minor","fileName":"tests/baselines/management_summary_value_golden.json","codegenInstructions":"Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate.\n\nIn @tests/baselines/management_summary_value_golden.json at line 4, The golden metadata currently hardcodes a machine-specific absolute Windows path in fixture_dir, making the committed artifact non-portable. Update the management summary golden generation so _meta.fixture_dir uses a repo-relative path instead of the developer-local absolute path, and verify the baseline fixture emission logic preserves portability across environments.","suggestions":[]}
{"type":"heartbeat","status":"reviewing"}
{"type":"finding","severity":"minor","fileName":".planning/phases/19-v1-4-closure-int-warn-1-2-3-fixes-phase-17-human-verificatio/19-CONTEXT.md","codegenInstructions":"Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate.\n\nIn @.planning/phases/19-v1-4-closure-int-warn-1-2-3-fixes-phase-17-human-verificatio/19-CONTEXT.md around lines 66 - 67, The reference in the planning context is ambiguous and should use the exact canonical path instead of the shortened placeholder. Update the mention in 19-CONTEXT.md to point to the concrete 19-CODERABBIT.md location within the phase 19 directory so downstream readers can resolve it reliably. Use the existing 19-CONTEXT.md reference and the 19-CODERABBIT.md symbol as the anchors when making the replacement.","suggestions":[]}
{"type":"finding","severity":"minor","fileName":".planning/phases/18-management-summary-migration-docs/18-VALIDATION.md","codegenInstructions":"Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate.\n\nIn @.planning/phases/18-management-summary-migration-docs/18-VALIDATION.md around lines 4 - 7, The validation metadata in this phase doc is internally inconsistent: the top-level status fields and the Wave 0 checklist do not match the approved sign-off section. Update the status markers in 18-VALIDATION.md so the fields tied to wave completion and the checklist items for Wave 0 align with the final approved state, using the existing markers and checklist entries in the validation section to make the phase status unambiguous.","suggestions":[]}
{"type":"finding","severity":"minor","fileName":"scripts/backfill_trend_reconstruction.py","codegenInstructions":"Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate.\n\nIn @scripts/backfill_trend_reconstruction.py around lines 216 - 218, The reconstruction window and fallback month logic in the trend boundary helpers is using local naive time instead of UTC, which can misclassify the current month around rollover. Update the relevant month calculations in the reconstruction path (including the code around the reconstruction window setup and the fallback month handling) to use UTC-based current time consistently, aligning with the existing UTC-based helper logic and any month comparisons in the affected reconstruction flow.","suggestions":[]}
{"type":"finding","severity":"major","fileName":"tests/e2e/test_groups.py","codegenInstructions":"Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate.\n\nIn @tests/e2e/test_groups.py at line 103, The test module currently indexes _GROUPS[0] unconditionally in the parametrization setup, which breaks collection when _load_groups() returns an empty list. Update the test setup around _GROUPS and the affected parametrized tests to guard for an empty group config before accessing the first element, and skip or avoid creating those parameters when no groups are loaded. Use the existing _load_groups() and _GROUPS references to locate the import-time setup and the two _GROUPS[0] usages.","suggestions":[]}
{"type":"finding","severity":"major","fileName":"scripts/backfill_trend_reconstruction.py","codegenInstructions":"Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate.\n\nIn @scripts/backfill_trend_reconstruction.py around lines 503 - 511, The cache-loading path in `backfill_trend_reconstruction` returns `vulns_all.parquet` without applying the same OPEN/REOPENED filter used by `fetch_all_vulnerabilities()`, so cached and live runs diverge. Update the cached branch that reads `current_open_df` to apply the same status filter before returning, matching the live path’s `OPEN`/`REOPENED` population. Use the existing `cache_dir`, `open_path`, `fixed_path`, and `current_open_df` flow so both paths reconstruct from the same input set.","suggestions":[]}
{"type":"finding","severity":"major","fileName":"docs/trend_and_segmentation_calculations.md","codegenInstructions":"Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate.\n\nIn @docs/trend_and_segmentation_calculations.md around lines 182 - 191, The “No backfill” guidance is outdated and conflicts with the supported reconstruction workflow. Update this section to clearly separate unsupported ad-hoc/manual backfill from the intended `scripts/backfill_trend_reconstruction.py` path, and explain that only reconstructed entries with proper provenance and immutable source data are acceptable. Use the existing “No backfill” heading and surrounding trend/segmentation documentation to keep the operator guidance aligned with current behavior.","suggestions":[]}
{"type":"finding","severity":"major","fileName":"tests/test_backfill_reconstruction.py","codegenInstructions":"Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate.\n\nIn @tests/test_backfill_reconstruction.py around lines 247 - 264, The synthetic add-back test in reconstruct_month is too permissive because it accepts large absolute/relative error, which can still pass when rows 3 and 4 are dropped. Tighten the assertions in test_backfill_reconstruction.py so this scenario fails exactly, using the existing captured_counts, reconstructed_counts, and within_tolerance helpers to require an exact match (or a zero-tolerance check) for the add-back case rather than MAX_ABSOLUTE_ERROR/MAX_RELATIVE_ERROR.","suggestions":["        # The captured ground truth: rows 0+3=2 critical, rows 1+4=2 high, 1 medium, 0 low\n        captured_counts = {\"critical\": 2, \"high\": 2, \"medium\": 1, \"low\": 0}\n\n        # Run the reconstruction\n        reconstructed_counts = reconstruct_month(current_open_df, fixed_df, month)\n\n        assert reconstructed_counts == captured_counts\n\n        # Also test the within_tolerance helper\n        assert within_tolerance(captured_counts, reconstructed_counts)"]}
{"type":"finding","severity":"minor","fileName":".planning/ROADMAP.md","codegenInstructions":"Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate.\n\nIn @.planning/ROADMAP.md at line 9, The v1.4 milestone status is inconsistent between the top-level summary and the progress table, so update the milestone badge/state in ROADMAP.md to match the completed phase information shown in the table. Check the v1.4 summary entry and the Phase 18/progress table entries together, and make the wording consistent so the overall milestone reflects completion rather than “in progress.”","suggestions":[]}
{"type":"finding","severity":"minor","fileName":".planning/STATE.md","codegenInstructions":"Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate.\n\nIn @.planning/STATE.md around lines 24 - 31, The state metadata is contradictory between the current focus summary and the detailed phase/status fields. Update the entries in STATE.md around the current focus and current position so they all describe one consistent state, and also align the Phase 19 resume target with that same truth; use the existing section headings like Current focus, Current Position, Phase, Plan, Status, and Last activity to locate and normalize the values.","suggestions":[]}
{"type":"finding","severity":"minor","fileName":".planning/milestones/v1.3-phases/13-owner-segmentation-composition-s2-doc/13-03-SUMMARY.md","codegenInstructions":"Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate.\n\nIn @.planning/milestones/v1.3-phases/13-owner-segmentation-composition-s2-doc/13-03-SUMMARY.md around lines 65 - 67, Clarify the `enriched_assets` handling in the `capture_snapshot` and `_count_by_owner` documentation so it does not contradict itself: state that an empty `enriched_assets` DataFrame is accepted and falls back to all-Unassigned, but `None` is invalid only when `dimension=\"owner\"` and should raise `ValueError`. Update the wording around the `capture_snapshot` dispatch and `_count_by_owner` behavior to make the distinction explicit and consistent.","suggestions":[]}
{"type":"finding","severity":"minor","fileName":"tests/test_backfill_reconstruction.py","codegenInstructions":"Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate.\n\nIn @tests/test_backfill_reconstruction.py around lines 565 - 588, The partial-flag checks in the reconstruction test are too weak because the conditional guards let the assertions pass even if the expected months were never produced. Strengthen the test around run_reconstruction and _load_snapshots by asserting the reconstructed snapshot set contains the expected taper months (2025-06, 2025-07, 2025-08) and the expected non-taper months before checking their partial values, so the test fails if the gate did not generate those months at all.","suggestions":["        result = run_reconstruction(\n            current_open_df=current_open_df,\n            fixed_df=fixed_df,\n            trend_dir=tmp_path,\n            window_start=\"2025-06\",\n            dry_run=False,\n        )\n        assert result[\"gate_passed\"] is True\n\n        snaps = _load_snapshots(tmp_path)\n        reconstructed = {s[\"month\"]: s for s in snaps if s.get(\"source\") == \"reconstructed\"}\n\n        # Taper months: Jun, Jul, Aug 2025 → partial=True\n        for taper_month in (\"2025-06\", \"2025-07\", \"2025-08\"):\n            assert taper_month in reconstructed\n            assert reconstructed[taper_month].get(\"partial\") is True\n\n        # Non-taper months: Sep 2025 onward → partial absent or False\n        for non_taper in (\"2025-09\", \"2025-10\", \"2025-11\"):\n            assert non_taper in reconstructed\n            assert not reconstructed[non_taper].get(\"partial\")"]}
{"type":"finding","severity":"minor","fileName":".planning/phases/15-independent-new-modules/15-02-SUMMARY.md","codegenInstructions":"Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate.\n\nIn @.planning/phases/15-independent-new-modules/15-02-SUMMARY.md around lines 88 - 89, The summary contains invalid set notation in the ACCEPTED/RECASTED example; update the text near the severity_modification_type logic to use the correct set form with both values listed separately. Fix the expression reference in the summary so it clearly matches the intended logic in the surrounding content, using the same symbols `df[\"severity_modification_type\"].str.upper().isin(...)` and `fetch_fixed_vulnerabilities()` as anchors.","suggestions":[]}
{"type":"finding","severity":"major","fileName":"scripts/backfill_trend_reconstruction.py","codegenInstructions":"Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate.\n\nIn @scripts/backfill_trend_reconstruction.py around lines 133 - 149, Preserve the add-back rows that were fixed after the boundary before they reach open_findings_at(), because that helper treats state=\"fixed\" as terminal and will drop them. Update the backfill logic around add_back_df and combined so fixed-after-boundary findings are converted or filtered into an open-eligible form only for the boundary check, while keeping the original fixed metadata for later reconstruction. Use the existing symbols add_back_df, combined, and open_findings_at() to locate the flow and ensure these rows are not lost during the historical count.","suggestions":["    if not fixed_df.empty and \"last_fixed\" in fixed_df.columns:\n        lf = pd.to_datetime(fixed_df[\"last_fixed\"], utc=True, errors=\"coerce\")\n        add_back_mask = lf > boundary\n        add_back_df = fixed_df[add_back_mask].copy()\n        if not add_back_df.empty and \"state\" in add_back_df.columns:\n            # These rows are fixed today, but were still open at the historical boundary.\n            add_back_df[\"state\"] = \"open\"\n    else:\n        add_back_df = pd.DataFrame(columns=current_open_df.columns)\n\n    # Build combined frame: current opens + fixed-after-D (which were open at M)\n    if add_back_df.empty:\n        combined = current_open_df.copy()\n    elif current_open_df.empty:\n        combined = add_back_df.copy()\n    else:\n        combined = pd.concat([current_open_df, add_back_df], ignore_index=True)\n\n    # Apply the reopened-aware two-interval predicate at the month boundary\n    open_at_boundary = open_findings_at(combined, boundary)"]}
{"type":"finding","severity":"minor","fileName":"scripts/backfill_trend_reconstruction.py","codegenInstructions":"Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate.\n\nIn @scripts/backfill_trend_reconstruction.py around lines 477 - 482, Validate the --window-start argument in argparse before any Tenable/cache work begins, since invalid month strings can currently slip through and fail later. Update the argument handling near the parser.add_argument for --window-start to parse and reject malformed YYYY-MM values up front, using the existing window reconstruction flow around DEFAULT_WINDOW_START and the downstream logic that consumes the parsed month.","suggestions":["    parser.add_argument(\n        \"--window-start\",\n        default=DEFAULT_WINDOW_START,\n        type=_month_arg,\n        metavar=\"YYYY-MM\",\n        help=f\"Earliest month to reconstruct (default: {DEFAULT_WINDOW_START}).\",\n    )"]}
{"type":"finding","severity":"minor","fileName":".planning/phases/14-shared-substrates-composed-report-gates/14-03-PLAN.md","codegenInstructions":"Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate.\n\nIn @.planning/phases/14-shared-substrates-composed-report-gates/14-03-PLAN.md around lines 139 - 146, The stub-discovery verification needs to be made executable and stricter: fix the malformed inline command in the acceptance criteria so it is a valid shell check, and update the automated verify step around registry.registry and Sc4KwargsStubModule to assert that sc4_kwargs_stub is actually present in the discovered registry, not just that the module imports and MODULE_ID matches. Keep the check focused on the existing registry/registry.registry symbols and ensure it fails when the stub is not registered.","suggestions":["    - `python -c \"import reports.modules; from reports.modules.registry import registry; assert 'sc4_kwargs_stub' in registry._modules; print('OK')\"` (or equivalent project discovery entrypoint) confirms registration\n    - The stub's compute() returns a non-error ModuleData when both kwargs are passed, and an _empty_result when either is missing (verified by Task 3)\n    - `grep -rn \"sc4_kwargs_stub\" run_all.py` returns NOTHING (D-17 — not registered as a top-level slug)\n    - ModuleData field names used by the stub exist in base.py (no invented fields)\n  </acceptance_criteria>\n  <verify>\n    <automated>python -c \"import reports.modules; from reports.modules.registry import registry; import reports.modules.sc4_kwargs_stub_module as m; assert m.Sc4KwargsStubModule.MODULE_ID == 'sc4_kwargs_stub'; assert 'sc4_kwargs_stub' in registry._modules; print('OK')\"</automated>\n  </verify>"]}
{"type":"heartbeat","status":"reviewing"}
{"type":"heartbeat","status":"reviewing"}
{"type":"finding","severity":"minor","fileName":"run_all.py","codegenInstructions":"Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate.\n\nIn @run_all.py around lines 94 - 102, Update the stale CHROME-COMPAT-01 comment in run_all.py so it no longer says management_summary must not receive the chrome kwargs. Keep the _CHROME_AWARE_SLUGS change as-is, and revise the downstream gate comment near the report dispatch logic to match the current run_report() behavior for management_summary, using the existing symbols _CHROME_AWARE_SLUGS and run_report() as the reference points.","suggestions":[]}
{"type":"finding","severity":"minor","fileName":"reports/management_summary.py","codegenInstructions":"Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate.\n\nIn @reports/management_summary.py around lines 202 - 240, The “never raises” contract in run_report is currently violated because the fetch/compose path is still unguarded. Update run_report to catch exceptions around the data-loading and composition steps in fetch_all_vulnerabilities, fetch_all_assets, fetch_fixed_vulnerabilities, read_trend, and the composer.run_all/run_full_pipeline calls, then reflect failures in the returned status dict and logs like the existing PDF/Excel/snapshot fallbacks; alternatively, if you don’t want to wrap that path, soften the docstring to match the actual behavior.","suggestions":[]}
{"type":"heartbeat","status":"reviewing"}
{"type":"finding","severity":"major","fileName":"tests/test_consumer_audit.py","codegenInstructions":"Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate.\n\nIn @tests/test_consumer_audit.py around lines 552 - 555, The pass-through exemption in _PASS_THROUGH_CALLERS is too broad because it matches any run_report scope, so tighten the audit in test_consumer_audit.py to use module-qualified identifiers instead of bare function names. Update the discovery/gating logic that checks these exemptions so it compares a file+scope or module::function symbol for each caller, and adjust the existing run_report entries to the specific report modules that are intentionally exempted.","suggestions":[]}
{"type":"finding","severity":"minor","fileName":"tests/test_management_summary.py","codegenInstructions":"Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate.\n\nIn @tests/test_management_summary.py around lines 550 - 595, The fallback lookups in _check_float_tolerance and _check_mixed are treating valid 0/0.0 values as missing. Update the presence checks for keys like scan_coverage_pct, coverage_pct, overall_rate, compliance_rate, total_exceptions, and open_exceptions to distinguish “present but zero” from “absent”, using explicit key existence logic instead of or-chaining. Keep the comparison behavior in _check_float_tolerance, _check_mixed, and related metric lookups the same otherwise.","suggestions":["        av = actual[\"scan_coverage_pct\"] if \"scan_coverage_pct\" in actual else actual.get(\"coverage_pct\")\n        if bv is not None and av is not None:\n            if abs(float(av) - float(bv)) > tol:\n                failures.append(\n                    f\"{metric_key} ({mid}): coverage_pct drift — \"\n                    f\"bespoke={bv}, modular={av}, tol={tol}\"\n                )\n        elif bv is not None:\n            failures.append(\n                f\"{metric_key} ({mid}): scan_coverage_pct/coverage_pct missing in modular metrics. \"\n                f\"Keys: {sorted(actual.keys())}\"\n            )\n\n    elif mid == \"mttr_trend\":\n        # Per-severity MTTR values (rolling-30 window)\n        bespoke_mttr = bespoke.get(\"mttr\", {})\n        actual_mttr  = actual.get(\"mttr_by_severity\") or actual.get(\"mttr\", {})\n        for sev in (\"critical\", \"high\", \"medium\", \"low\"):\n            bv = bespoke_mttr.get(sev)\n            av = actual_mttr.get(sev)\n            if bv is None:\n                continue  # bespoke didn't have this severity\n            if av is None:\n                # Module may report None when sample is too small — acceptable\n                continue\n            if abs(float(av) - float(bv)) > tol:\n                failures.append(\n                    f\"{metric_key} ({mid}): mttr[{sev}] drift — \"\n                    f\"bespoke={bv}, modular={av}, tol={tol} days\"\n                )\n\n    elif mid == \"patch_compliance_rate\":\n        bv = bespoke.get(\"overall_rate\")\n        av = actual.get(\"overall_rate\") or actual.get(\"compliance_rate\")\n        if bv is not None and av is not None:\n            if abs(float(av) - float(bv)) > tol:\n                failures.append(\n                    f\"{metric_key} ({mid}): overall_rate drift — \"\n                    f\"bespoke={bv}, modular={av}, tol={tol}\"\n                )\n        elif bv is not None:\n            failures.append(\n                f\"{metric_key} ({mid}): overall_rate/compliance_rate missing \"\n                f\"in modular metrics. Keys: {sorted(actual.keys())}\"\n            )"]}
{"type":"finding","severity":"major","fileName":"data/fetchers.py","codegenInstructions":"Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate.\n\nIn @data/fetchers.py around lines 379 - 435, fetch_fixed_vulnerabilities currently ignores lookback_days in its cache identity, so different windows can reuse the same parquet and return the wrong fixed-vulnerability set. Update the cache key/path used by _cache_path in fetch_fixed_vulnerabilities so it includes lookback_days (for example by deriving a window-specific dataset name) and ensure _load_cache reads/writes that per-window cache consistently. Locate the change in fetch_fixed_vulnerabilities and the cache helpers it calls (_cache_path, _load_cache).","suggestions":["def fetch_fixed_vulnerabilities(\n    tio,\n    cache_dir: Path,\n    lookback_days: int = FIXED_LOOKBACK_DAYS,\n) -> pd.DataFrame:\n    \"\"\"\n    Fetch fixed/remediated vulnerability findings from Tenable.\n\n    Used by ``reports/management_summary.py`` to compute MTTR metrics and\n    by the module infrastructure (board_summary, composed_report) to deliver\n    fixed_vulns_df to CriticalRemediationSLAModule and MTTRTrendModule.\n\n    D-18-05: A bounded ``last_fixed`` time filter is applied so the fetch\n    returns findings fixed within the last ``lookback_days`` days (default\n    ``config.FIXED_LOOKBACK_DAYS`` = 365).  Before this change, Tenable's\n    API default (no time filter) returned only ~30 days of fixed findings\n    — not a platform retention limit, just an API default.  The filter\n    shape (plain integer Unix epoch seconds) was proven against the live\n    API by the Task 0 probe (scripts/probe_last_fixed_filter.py).\n\n    Each result-consuming module applies its OWN explicit date window at\n    compute() time (D-18-06 consumer-audit gate, plan 18-02 Task 1-2):\n    - CriticalRemediationSLAModule: last_fixed >= report_date − 30d\n    - MTTRTrendModule: last_fixed >= report_date − window_days (default 30)\n    Widening the fetch therefore causes NO silent metric drift.\n\n    The ``time_taken_to_fix`` field (seconds) is available for MTTR; the\n    reopened-aware fallback ``(last_fixed - COALESCE(resurfaced_date,\n    first_found))`` is applied by the consuming module (D-16-02).\n\n    Caches to ``<cache_dir>/vulns_fixed.parquet``.  Informational findings\n    are excluded (no SLA; MTTR is meaningless for them).\n\n    Parameters\n    ----------\n    tio : TenableIO\n        Authenticated Tenable client.\n    cache_dir : Path\n        Run-scoped directory for parquet cache files.\n    lookback_days : int, optional\n        Number of days to look back for fixed findings.  Defaults to\n        ``config.FIXED_LOOKBACK_DAYS`` (365).  Pass a smaller value to\n        narrow the window for testing or bandwidth-limited runs.\n        The resulting cutoff is ``now - lookback_days`` expressed as a\n        Unix epoch integer (the bare-integer Tenable filter shape; the\n        date-range dict shape is rejected by the API).\n\n    Returns\n    -------\n    pd.DataFrame\n        Same column schema as ``fetch_all_vulnerabilities()`` but containing\n        only state=fixed findings.\n    \"\"\"\n    cache = _cache_path(cache_dir, f\"vulns_fixed_{int(lookback_days)}d\")\n    cached = _load_cache(cache)\n    if cached is not None:\n        return cached"]}
{"type":"finding","severity":"major","fileName":"reports/modules/composer.py","codegenInstructions":"Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate.\n\nIn @reports/modules/composer.py around lines 653 - 681, The renderer fail-soft path still assumes `render_pdf_section()` returns a valid string, so a buggy module can still break assembly when `html_section.strip()` is called after the try/except in `ReportComposer.assemble_pdf`. Add explicit return-type/shape validation immediately after the render call (and before any `.strip()` or downstream use) so non-string or empty/invalid results are skipped or converted into a safe fallback, keeping the error handling around `mod_class`, `render_pdf_section`, and `logger.error` intact.","suggestions":[]}
{"type":"finding","severity":"major","fileName":"reports/modules/aged_vulns_assets_module.py","codegenInstructions":"Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate.\n\nIn @reports/modules/aged_vulns_assets_module.py around lines 115 - 116, Update the aging-vulns report documentation and metadata to match the new `risk_score` ordering used by `table_data` and `top_5`. In `AgedVulnsAssetsModule`, revise the class docstring, the PDF/report copy, and the audit metadata so they no longer describe “highest percentage” or “affected DESC, percentage DESC,” and instead clearly state that the Top-5 and per-owner breakdown are sorted by `risk_score`. Keep the language consistent across the affected report-generation symbols so the emitted ordering and the audit trail explain the same ranking.","suggestions":[]}
{"type":"finding","severity":"major","fileName":"data/trend_store.py","codegenInstructions":"Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate.\n\nIn @data/trend_store.py around lines 389 - 390, The month key generation in trend_store should use server-local time, but `date.strftime(\"%Y-%m\")` is currently formatting timezone-aware snapshot dates as UTC. Update the month derivation in the snapshot handling logic (and the related first/fixed month path around the same trend-store month computation) to convert `date` to local time before formatting `month_str`, keeping the month labels consistent with the local-time finding month calculations.","suggestions":[]}
{"type":"finding","severity":"major","fileName":"data/trend_store.py","codegenInstructions":"Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate.\n\nIn @data/trend_store.py around lines 456 - 458, The trend filename is built directly from tag_filter, so path traversal inputs like /../ can escape the intended directory. Update the file_path construction in the trend_store logic (both the write path and the read path around the trend file handling) to sanitize or normalize the tag_filter used in filenames, while keeping the stored tag_filter field unchanged. Use the existing trend_store file path code and the related read/write helpers to ensure only the filename suffix is sanitized.","suggestions":[]}
{"type":"finding","severity":"major","fileName":"reports/modules/high_risk_assets_module.py","codegenInstructions":"Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate.\n\nIn @reports/modules/high_risk_assets_module.py around lines 117 - 119, The report copy and audit metadata in high_risk_assets_module no longer match the actual logic: the breakdown is sorted by risk_score, not by “highest percentage,” and the BU breakdown source is Owner via extract_owner(), not the Application tag. Update the descriptive text in the summary/report sections and the get_audit_info() messaging so they consistently describe the risk_score-based ordering and Owner-based grouping, and make sure all affected report strings in the referenced sections use the same terminology.","suggestions":[]}
{"type":"finding","severity":"major","fileName":"reports/modules/composer.py","codegenInstructions":"Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate.\n\nIn @reports/modules/composer.py around lines 1158 - 1166, The early return in ReportComposer.assemble_analyst_workbook treats “no collected sheets” and “all modules failed” the same, which drops the analyst _Metadata tab in failure-only runs. Split the empty-data path from the failure-only path by checking failures versus collected sheets before returning, and when every module fails still create the workbook with the _Metadata tab populated from the failures instead of returning None; keep the existing skip-only behavior for true no-data/no-failure cases.","suggestions":["        # D-20: all-empty workbook → no file written\n        if not collected and not failures:\n            logger.info(\n                \"ReportComposer.assemble_analyst_workbook: every module \"\n                \"returned [] or empty DataFrames — skipping analyst \"\n                \"workbook (failures=%d).\",\n                len(failures),\n            )\n            return None"]}
{"type":"finding","severity":"major","fileName":"data/trend_store.py","codegenInstructions":"Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate.\n\nIn @data/trend_store.py around lines 427 - 433, The fixed-findings counting logic in the branch that uses fixed_vulns_df should distinguish an empty dataset from missing data. Update the code around fixed_findings_count so that when fixed_vulns_df is present but empty, the value is explicitly set to 0 instead of დარჩaining None, while keeping the existing calculation for non-empty data in the same trend_store path.","suggestions":["        fixed_findings_count = 0\n        if not fixed_vulns_df.empty:\n            lf = pd.to_datetime(fixed_vulns_df[\"last_fixed\"], utc=True, errors=\"coerce\")\n            lf_month = lf.dt.tz_convert(local_tz).dt.strftime(\"%Y-%m\")\n            state_upper = fixed_vulns_df[\"state\"].astype(str).str.upper()\n            fixed_findings_count = int(\n                ((lf_month == month_str) & (state_upper == \"FIXED\")).sum()\n            )"]}
{"type":"finding","severity":"major","fileName":"data/trend_store.py","codegenInstructions":"Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate.\n\nIn @data/trend_store.py around lines 153 - 193, The _load_trend_json helper currently returns whatever data.get(\"snapshots\", []) provides, which can be the wrong type or contain invalid entries and later crash callers like capture_snapshot. Update _load_trend_json to validate that the parsed root is a dict, that \"snapshots\" is a list, and that every item is a dict before returning it; otherwise treat it as a parse failure and keep the existing corrupt-file handling and logging path.","suggestions":["def _load_trend_json(path: Path) -> list[dict]:\n    \"\"\"\n    Load snapshot entries from a trend JSON file.\n\n    Returns an empty list if the file does not exist or cannot be parsed.\n    Never raises.  Named differently from the MS private helper\n    ``_load_trend_history`` to avoid any collision.\n\n    Contract (IN-05): the file root must be a JSON object of the shape\n    ``{\"snapshots\": [...]}``.  Any other shape (a bare list, a scalar, etc.)\n    causes ``.get`` to fail and is treated as unparseable — see the corrupt-file\n    handling below.\n\n    Corrupt-file handling (IN-02): the caller (``capture_snapshot``) does\n    read-modify-write, so returning ``[]`` on a parse failure would let the next\n    write OVERWRITE the corrupt file and permanently discard recoverable\n    history.  To avoid silent destruction we rename the bad file to\n    ``*.corrupt`` (best-effort) before returning ``[]`` and log at ERROR.\n    \"\"\"\n    if not path.exists():\n        return []\n    try:\n        with path.open(\"r\", encoding=\"utf-8\") as fh:\n            data = json.load(fh)\n        if not isinstance(data, dict):\n            raise ValueError(\"trend file root must be an object\")\n        snapshots = data.get(\"snapshots\", [])\n        if not isinstance(snapshots, list) or not all(\n            isinstance(snap, dict) for snap in snapshots\n        ):\n            raise ValueError(\"trend file snapshots must be a list of objects\")\n        return snapshots\n    except Exception as exc:\n        logger.error(\n            \"Could not parse trend file %s: %s — preserving it as *.corrupt so \"\n            \"accumulated history is not overwritten on the next write\",\n            path, exc,\n        )\n        try:\n            path.replace(path.with_suffix(path.suffix + \".corrupt\"))\n        except OSError as rename_exc:\n            logger.error(\n                \"Could not preserve corrupt trend file %s: %s\", path, rename_exc\n            )\n        return []"]}
{"type":"finding","severity":"major","fileName":".claude/hooks/block_tenable_fetch.py","codegenInstructions":"Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate.\n\nIn @.claude/hooks/block_tenable_fetch.py around lines 127 - 131, The parse-failure path in main currently exits successfully in the except block, which effectively allows execution when the PreToolUse payload is malformed or changed. Update the error handling so that the hook fails closed by emitting the generic deny response instead of calling sys.exit(0), and make sure the change is applied in the main flow where json.load(sys.stdin) is handled.","suggestions":[]}
{"type":"finding","severity":"critical","fileName":".claude/hooks/block_tenable_fetch.py","codegenInstructions":"Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate.\n\nIn @.claude/hooks/block_tenable_fetch.py around lines 82 - 90, The bypass handling in effective_head currently only strips simple wrappers and VAR=val prefixes, so guarded fetches can still slip through when Python is invoked via nested shell payloads or dynamic execution. Update the command inspection logic to recursively parse wrapper payloads (for example shell -c / bash -lc forms) and teach the Python detection path to inspect -c and -m arguments for guarded module references, denying those cases in the same place that currently evaluates the command head. Add regression coverage for the wrapper and Python payload shapes mentioned so the deny decision is enforced consistently.","suggestions":[]}
{"type":"finding","severity":"major","fileName":"delivery/email_sender.py","codegenInstructions":"Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate.\n\nIn @delivery/email_sender.py around lines 487 - 501, Apply the same inline-image size enforcement to the prebuilt_charts path used in email_sender.py, since it currently decodes and attaches images in the prebuilt management_summary flow without any 2 MB / 5 MB budget check. Update the logic around the prebuilt_charts loop to use the same size-budget helper and validation as email_inline_images before base64 decoding and MIMEImage attachment, so both branches are capped consistently.","suggestions":[]}
{"type":"finding","severity":"minor","fileName":"scripts/smoke_email_phase2.py","codegenInstructions":"Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate.\n\nIn @scripts/smoke_email_phase2.py around lines 332 - 346, The --no-stub-panels option in scripts/smoke_email_phase2.py is misleading because it only disables the empty-panels stub fallback, not the legacy KPI-tile render path described in the help text. Update the flag/help text in the argument parsing block and _build_synthetic_outputs call site to match the actual behavior, or implement the missing legacy fallback branch so the option name, documentation, and execution path all align.","suggestions":[]}
{"type":"finding","severity":"major","fileName":"scripts/smoke_email_phase2.py","codegenInstructions":"Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate.\n\nIn @scripts/smoke_email_phase2.py around lines 133 - 139, The smoke email flow is generating panel HTML that references cid-based gauges, but the inline images are never attached to the outgoing message. Update the synthetic output path in _build_synthetic_outputs() and the downstream send/compose logic so the collected email_inline_images are propagated into the final email as MIME inline attachments, using the existing module_id-based cid names from render_email_panel(). Keep the CID mapping consistent so each referenced {module_id}_gauge resolves in the sent message.","suggestions":[]}
{"type":"finding","severity":"major","fileName":"docs/management_summary_calculations.md","codegenInstructions":"Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate.\n\nIn @docs/management_summary_calculations.md around lines 137 - 148, The pre-v1.4 runbook sections still describe retired metric formulas and SLA thresholds, which conflict with the authoritative v1.4 ReportComposer guidance later in the document. Update the earlier sections tied to the outdated SLA, MTTR, Metric 5, and Metric 7 descriptions so they either match the live ReportComposer behavior or are explicitly labeled historical, and ensure the definitions align with the current v1.4 module section.","suggestions":[]}
{"type":"finding","severity":"major","fileName":"scripts/update_from_github.sh","codegenInstructions":"Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate.\n\nIn @scripts/update_from_github.sh around lines 327 - 331, The release-target handling in update_from_github.sh is normalizing relative symlink targets inconsistently, causing legacy values like releases/v1.2.0 to become duplicated under INSTALL_ROOT. Update the logic around readlink/current and the related target-resolution branches so any non-absolute target is anchored only against INSTALL_ROOT, not INSTALL_ROOT/releases, and apply the same normalization consistently in the other affected paths; a shared helper around the current target resolution would prevent the repeated duplication bug.","suggestions":[]}
{"type":"finding","severity":"major","fileName":"scripts/update_from_github.sh","codegenInstructions":"Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate.\n\nIn @scripts/update_from_github.sh around lines 904 - 916, The FORCE re-extraction path in update_from_github.sh can delete the currently active release directory before a replacement is staged, which breaks rollback because PREV may end up pointing back to the rebuilt tree. Update the TARGET_DIR handling to refuse --force when it resolves to the release currently referenced by current, or otherwise stage the new content in a separate temporary directory and swap it into place only after extraction succeeds. Use the existing FORCE, TARGET_DIR, PREV, and current flow to locate the guard and ensure the active release is never removed in-place.","suggestions":[]}
{"type":"complete","status":"review_completed","findings":41}
