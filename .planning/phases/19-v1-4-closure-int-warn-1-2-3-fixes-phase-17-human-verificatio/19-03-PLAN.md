---
phase: 19-v1-4-closure-int-warn-1-2-3-fixes-phase-17-human-verificatio
plan: 03
type: execute
wave: 2
depends_on: ["19-01", "19-02"]
files_modified:
  - reports/management_summary.py
  - tests/test_management_summary.py
  - tests/test_composed_report_kwargs_gates.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "INT-WARN-1 / D-03: management_summary's capture_snapshot() forward-write emits the FULL aggregate field set (on_time_asset_count, reopened_count, accepted_count, recast_count, mttr_overall_days, mttr_by_severity, mttr_by_owner, sla_rate_crit_high) matching the cron writer — no longer a partial writer"
    - "INT-WARN-1 / D-03b: a regression guard asserts management_summary forwards the same capture_snapshot kwarg set the cron path does, so a future partial-write cannot silently regress"
    - "INT-WARN-2 / D-04: management_summary fetches recast_rules_df fail-soft and forwards it to ReportComposer so the accepted_recast expiry cross-check runs (pending_reeval no longer always 0)"
    - "INT-WARN-3: tests/test_composed_report_kwargs_gates.py::test_frozensets_membership asserts exact membership for _MODULES_NEEDING_FIXED_VULNS == {critical_remediation_sla, mttr_trend}"
    - "CR-F3: management_summary.run_report fetch/compose path is wrapped so a fetch/compose failure is reflected in the status dict (never raises), honoring the docstring contract"
    - "CR-T5: test_management_summary _check_float_tolerance/_check_mixed use explicit key-existence checks so a valid 0/0.0 is not treated as missing (empty-data 0-vs-absent)"
  artifacts:
    - path: "reports/management_summary.py"
      provides: "Complete trend writer + recast fetch/forward + guarded fetch/compose"
      contains: "recast_rules_df"
    - path: "tests/test_composed_report_kwargs_gates.py"
      provides: "Third frozenset membership gate (_MODULES_NEEDING_FIXED_VULNS)"
      contains: "_MODULES_NEEDING_FIXED_VULNS"
    - path: "tests/test_management_summary.py"
      provides: "INT-WARN-1 full-field-set regression guard + INT-WARN-2 recast-forward test + CR-T5 key-existence checks"
  key_links:
    - from: "reports/management_summary.py"
      to: "data.trend_store.capture_snapshot"
      via: "full aggregate kwarg set forwarded"
      pattern: "capture_snapshot\\("
    - from: "reports/management_summary.py"
      to: "data.fetchers.fetch_recast_rules"
      via: "fail-soft fetch + recast_rules_df= forwarded to ReportComposer"
      pattern: "fetch_recast_rules"
---

<objective>
Close the three INT-WARN correctness gaps on the `management_summary` path plus CR-F3 and CR-T5 (both touch this report's code/tests). Make management_summary a COMPLETE trend writer (INT-WARN-1/D-03), fetch+forward `recast_rules_df` so the expiry cross-check runs (INT-WARN-2/D-04), add the missing `_MODULES_NEEDING_FIXED_VULNS` co-edit gate (INT-WARN-3), wrap the fetch/compose path so `run_report` honors its "never raises" contract (CR-F3), and fix the test idiom that treats a valid 0 as missing (CR-T5). Per the triage, CR-F3 and INT-WARN-1/2 share the same code path and land together.

Purpose: Stop management_summary from nulling a fully-populated monthly snapshot and from silently skipping recast-expiry; close the frozenset maintenance blind spot; make the management_summary tests robust to legitimate zeros.
Output: Complete forward-write + recast fetch/forward + guarded run_report; third frozenset gate; CR-T5 key-existence test fix; regression tests.

Wave note: depends_on includes 19-01 (Wave 1) as well as 19-02 — Task 3 reads `data/trend_store.py` for the `capture_snapshot` signature, and Plan 01 edits that same file (CR-S2 filename, different line range) in Wave 1. The explicit dependency serializes Wave 1 → Wave 2 so the executor reads trend_store.py in its post-Plan-01 state. This plan does NOT modify data/trend_store.py (no same-wave files_modified overlap is introduced).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-v1-4-closure-int-warn-1-2-3-fixes-phase-17-human-verificatio/19-PATTERNS.md
@.planning/phases/19-v1-4-closure-int-warn-1-2-3-fixes-phase-17-human-verificatio/19-CONTEXT.md

<interfaces>
<!-- Source-of-truth gated fetch/forward in composed_report.py to MIRROR (do not invent a new path) -->
From reports/composed_report.py:
  _MODULES_NEEDING_RECAST_RULES: frozenset   # {"sc4_kwargs_stub", "accepted_recast"}
  _MODULES_NEEDING_FIXED_VULNS:  frozenset   # {"critical_remediation_sla", "mttr_trend"}  (membership at L73-76)
  # gated recast fetch block at L245-259 (fail-soft: failure → recast_rules_df = None, not abort)

From data/fetchers.py:
  fetch_recast_rules(tio, cache_dir) -> pd.DataFrame

<!-- Complete capture_snapshot kwarg set the cron writer emits (scripts/capture_trend_snapshot.py L420-432) -->
data/trend_store.py::capture_snapshot(df, assets_df, date, dimension, tag_filter, *,
    on_time_asset_count=None, reopened_count=None, accepted_count=None, recast_count=None,
    fixed_vulns_df=None, mttr_overall_days=None, mttr_by_severity=None, mttr_by_owner=None,
    sla_rate_crit_high=None, enriched_assets=None, trend_dir=...) -> Path
  # CONFIRM the exact signature in data/trend_store.py during execution before wiring kwargs
  # (read it in its post-Plan-01 state — Plan 01 edits the CR-S2 filename site in the same file).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: INT-WARN-3 — add the third frozenset membership gate (co-edit gate)</name>
  <files>tests/test_composed_report_kwargs_gates.py</files>
  <read_first>
    - tests/test_composed_report_kwargs_gates.py L44-47 (import block) and L92-108 (the two existing exact-membership assertions in test_frozensets_membership)
    - reports/composed_report.py L73-76 (_MODULES_NEEDING_FIXED_VULNS current membership {"critical_remediation_sla", "mttr_trend"})
    - 19-PATTERNS.md L125-139 (INT-WARN-3 fix pattern — add a third assert mirroring the existing two)
    - CLAUDE.md memory note project_frozenset_gate_test_coupling (this is an exact co-edit gate)
  </read_first>
  <action>
    Import `_MODULES_NEEDING_FIXED_VULNS` alongside the existing two frozenset imports (L44-47). Add a third `assert _MODULES_NEEDING_FIXED_VULNS == frozenset({"critical_remediation_sla", "mttr_trend"})` block to `test_frozensets_membership` (after the existing two at L100-108), with an assertion message in the same `"(D-NN / Phase NN)"` citation format (e.g. "(INT-WARN-3 / Phase 19 / SUB-03)"). Do not change the other two assertions. Confirm the current membership against composed_report.py L73-76 first — if it differs, use the actual membership and note it.
  </action>
  <acceptance_criteria>
    - `pytest tests/test_composed_report_kwargs_gates.py::test_frozensets_membership -q -o addopts=""` exits 0.
    - `grep -c "_MODULES_NEEDING_FIXED_VULNS" tests/test_composed_report_kwargs_gates.py` >= 2 (import + assertion).
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_composed_report_kwargs_gates.py::test_frozensets_membership -q -o addopts=""</automated>
  </verify>
  <done>The third frozenset has an exact-membership co-edit gate; the test passes.</done>
</task>

<task type="auto">
  <name>Task 2: INT-WARN-2 + CR-F3 — fetch/forward recast_rules_df and guard the fetch/compose path</name>
  <files>reports/management_summary.py, tests/test_management_summary.py</files>
  <read_first>
    - reports/management_summary.py L202-240 (CR-F3 unguarded fetch/compose region) and L345-361 (the ReportComposer construction where trend_snapshots is forwarded — recast_rules_df must join here)
    - reports/composed_report.py L245-259 (the gated recast fetch fail-soft pattern to mirror)
    - reports/management_summary.py L393-411 (existing PDF/Excel fail-soft fallback pattern to mirror for CR-F3)
    - 19-PATTERNS.md L96-121 (INT-WARN-2 adaptation: accepted_recast is always present so fetch unconditionally but fail-soft) and L378-391 (CR-F3 wrap pattern)
  </read_first>
  <action>
    INT-WARN-2 (D-04): add a fail-soft `recast_rules_df` fetch mirroring composed_report.py L245-259 — `try: from data.fetchers import fetch_recast_rules; recast_rules_df = fetch_recast_rules(tio, cache_dir) except Exception: logger.error(...); recast_rules_df = None`. Because `accepted_recast` is always in `_MGMT_MODULE_CONFIGS`, fetch unconditionally (no `need_recast` intersection needed) but still fail-soft. Forward `recast_rules_df=recast_rules_df` into the `ReportComposer(...)` constructor alongside the existing `trend_snapshots=` kwarg at L353-361.
    CR-F3: wrap the fetch/compose block (the region ~L225-374 that fetches data and runs the composer) so a fetch/compose failure is caught, logged with `exc_info=True`, and reflected in the returned status dict (set the relevant outputs to None and record the failure) rather than raising out of `run_report` — matching the existing PDF/Excel fallback style at L393-411. The "never raises" docstring contract must hold.
    Add to tests/test_management_summary.py: (a) a test asserting `recast_rules_df` is fetched and forwarded into the composer (e.g. monkeypatch `fetch_recast_rules` and assert it is called + the kwarg reaches the composer); (b) a test asserting a forced fetch/compose exception makes `run_report` return a status dict (does not raise).
  </action>
  <acceptance_criteria>
    - `grep -c "fetch_recast_rules" reports/management_summary.py` >= 1 AND `grep -c "recast_rules_df" reports/management_summary.py` >= 2.
    - `pytest tests/test_management_summary.py -q -o addopts=""` exits 0 with the two new tests present and passing.
    - The new "never raises" test forces an exception in the fetch/compose path and asserts `run_report(...)` returns (a dict) rather than propagating.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_management_summary.py -q -o addopts=""</automated>
  </verify>
  <done>recast_rules_df is fetched fail-soft and forwarded; run_report never raises on fetch/compose failure; tests prove both.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: INT-WARN-1 complete forward-write + D-03b guard + CR-T5 test-idiom fix</name>
  <files>reports/management_summary.py, tests/test_management_summary.py</files>
  <read_first>
    - reports/management_summary.py L407-427 (the incomplete capture_snapshot() call — currently only df/assets_df/date/dimension/tag_filter/fixed_vulns_df)
    - scripts/capture_trend_snapshot.py L420-432 (the COMPLETE field-set call — source of truth for D-03)
    - data/trend_store.py capture_snapshot signature, READ IN ITS POST-PLAN-01 STATE (CONFIRM the exact optional aggregate param names before wiring; Plan 01 edits the CR-S2 filename site in the same file in Wave 1)
    - tests/test_management_summary.py L550-595 (CR-T5: _check_float_tolerance/_check_mixed or-chains treating 0/0.0 as missing)
    - 19-PATTERNS.md L56-92 (INT-WARN-1 fix: extract aggregate counts from the composer `results` per-module `result.metrics`; None default when `result.error is not None`), L723-727 (full-field-set convention), L460-470 (CR-T5 explicit key-existence checks)
  </read_first>
  <behavior>
    - Test test_management_summary_forwards_full_field_set: the capture_snapshot kwargs forwarded by management_summary equal the cron path's optional aggregate kwarg set (on_time_asset_count, reopened_count, accepted_count, recast_count, mttr_overall_days, mttr_by_severity, mttr_by_owner, sla_rate_crit_high, fixed_vulns_df) — minus the always-positional df/assets_df/date/dimension/tag_filter and the trend-internal trend_dir/enriched_assets.
    - Test test_partial_write_regression_guard: if a future edit drops one of the aggregate kwargs, the guard test fails (the asserted kwarg set is exact).
    - Test test_missing_module_result_forwards_none: when a contributing module's result has error != None, the corresponding aggregate value is forwarded as None (safe default), not omitted/raised.
    - CR-T5: a metric whose value is a legitimate 0 / 0.0 is treated as PRESENT (not missing) by the comparison helpers.
  </behavior>
  <action>
    Replace the incomplete `capture_snapshot(...)` call at L420-427 with the full field set. Derive the aggregate counts from the module results already computed by the composer run (`results` list): iterate for the module producing each metric — `accepted_recast` → accepted_count/recast_count; `scan_coverage_sla` → on_time_asset_count; `mttr_trend` → mttr_overall_days/mttr_by_severity/mttr_by_owner; `program_health` → sla_rate_crit_high — reading from `result.metrics`. Use `None` as the safe default whenever a module's `result.error is not None` or the metric key is absent (explicit-null per D-16-09). Keep `fixed_vulns_df` forwarded as today. Confirm each aggregate param NAME against the live `capture_snapshot` signature in data/trend_store.py (post-Plan-01 state).
    D-03b: add the regression guard tests (test_management_summary_forwards_full_field_set + test_partial_write_regression_guard) that compare the kwarg set management_summary forwards to the `capture_snapshot` parameter set minus df/assets_df/date/dimension/tag_filter/trend_dir/enriched_assets — by monkeypatching capture_snapshot to capture kwargs (no live run; per 19-PATTERNS.md L92).
    CR-T5 (tests/test_management_summary.py L550-595): replace the `val or default` / or-chain idiom in `_check_float_tolerance` / `_check_mixed` with explicit `key in data` existence checks so a valid `0` / `0.0` is not treated as missing.
  </action>
  <acceptance_criteria>
    - The capture_snapshot call in management_summary.py forwards all of: on_time_asset_count, reopened_count, accepted_count, recast_count, mttr_overall_days, mttr_by_severity, mttr_by_owner, sla_rate_crit_high, fixed_vulns_df — `grep -c -E "on_time_asset_count|reopened_count|accepted_count|recast_count|mttr_overall_days|mttr_by_severity|mttr_by_owner|sla_rate_crit_high" reports/management_summary.py` >= 8.
    - `pytest tests/test_management_summary.py -q -o addopts=""` exits 0 with the three new INT-WARN-1 tests + CR-T5 idiom fix passing.
    - test_partial_write_regression_guard asserts an EXACT kwarg set (a dropped kwarg fails the test).
    - CR-T5: the comparison helpers use `in` key-existence checks — `grep -n "in data" tests/test_management_summary.py` (or equivalent explicit-existence form) shows the replaced idiom around L550-595.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_management_summary.py -q -o addopts=""</automated>
  </verify>
  <done>management_summary is a complete trend writer; the partial-write regression guard is in place; CR-T5 idiom fixed; tests pass.</done>
</task>

</tasks>

<verification>
- `pytest tests/test_management_summary.py tests/test_composed_report_kwargs_gates.py -q -o addopts=""` → exits 0.
- `python -c "import reports.management_summary"` (from repo root) imports cleanly.
- `python run_all.py --dry-run` still validates all groups.
</verification>

<success_criteria>
INT-WARN-1 (complete writer + D-03b guard), INT-WARN-2 (recast fetch/forward), INT-WARN-3 (frozenset gate), CR-F3 (never-raises), CR-T5 (0-vs-absent) all closed with regression tests.
</success_criteria>

<output>
Create `.planning/phases/19-v1-4-closure-int-warn-1-2-3-fixes-phase-17-human-verificatio/19-03-SUMMARY.md` when done
</output>
</content>
