---
phase: 01-module-render-contract
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - reports/management_summary.py
  - CLAUDE.md
autonomous: true
requirements:
  - QUALITY-01
  - QUALITY-03
  - CONTRACT-05
tags:
  - empty-data-hardening
  - documentation
  - audit

must_haves:
  truths:
    - "reports/management_summary.py:1853 cov_str line uses safe_pct(cov_pct) instead of f'{cov_pct:.1f}%' (QUALITY-01, D-16)"
    - "The sibling cov_color comparison three lines below the format-spec fix also guards None (no '>=' on a None operand) — pattern-map risk #3"
    - "A zero-licensed-asset run on management_summary does not raise TypeError on any line in the Tile 4 — Scan Coverage block (Phase 1 success criterion 4)"
    - "Every QUALITY-03 INVESTIGATE candidate from PATTERNS.md section 7 has been traced to either NO ACTION (structurally non-None, annotated) or FIX (wrapped with safe_pct/safe_int/safe_format) (QUALITY-03, D-17)"
    - "After the audit, no f-string format spec in reports/*.py or reports/modules/*.py interpolates an unguarded possibly-None metric value (Phase 1 success criterion 5)"
    - "CLAUDE.md 'Board-Style Reports — Module Infrastructure' section documents all five renderer methods (render_pdf_section, render_excel_tabs, render_email_kpis, render_email_panel, render_analyst_tabs, render_rag_strip_entry), notes their concrete-with-no-op-defaults shape, and codifies the empty-data guard pattern requiring safe_pct/safe_int/safe_format usage in every render method (CONTRACT-05, D-15)"
  artifacts:
    - path: "reports/management_summary.py"
      provides: "Crash-free cov_pct formatting in _compute_kpi_tiles"
      contains: ["safe_pct(cov_pct)", "cov_pct is not None and cov_pct >="]
    - path: "CLAUDE.md"
      provides: "Documented four-channel render contract and empty-data guard pattern"
      contains: ["render_email_panel", "render_analyst_tabs", "render_rag_strip_entry", "safe_pct", "format_utils", "empty-data guard"]
  key_links:
    - from: "reports/management_summary.py imports"
      to: "reports/modules/format_utils.py safe_pct"
      via: "from reports.modules import safe_pct"
      pattern: "from reports.modules import safe_pct"
    - from: "CLAUDE.md 'Adding a new module' subsection"
      to: "reports/modules/format_utils.py + reports/modules/rag_utils.py"
      via: "Documents the import convention for new modules"
      pattern: "from reports.modules import safe_pct, safe_int, safe_format"
---

<objective>
Three coordinated cleanups that close the empty-data hardening loop and document the new contract end-to-end:

1. QUALITY-01 fix: replace the unguarded `f"{cov_pct:.1f}%"` at `reports/management_summary.py:1853` with `safe_pct(cov_pct)`, AND guard the sibling `cov_pct >= 90` numeric comparison three lines below (pattern-map risk #3 — a fix that only addresses the format string still leaves a TypeError crash path live).

2. QUALITY-03 audit: walk every INVESTIGATE candidate flagged in PATTERNS.md section 7 (six candidates across `reports/asset_risk.py`, `reports/unscanned_assets.py`, `reports/management_summary.py`, `reports/trend_analysis.py`, and three `*_module.py` files) and either (a) annotate as structurally-non-None with a brief code comment, or (b) replace the unguarded f-string with `safe_pct(...)` / `safe_int(...)` / `safe_format(...)`.

3. CONTRACT-05 documentation: extend CLAUDE.md's "Board-Style Reports — Module Infrastructure" section to document the four-channel render contract (now five render methods total), the empty-data guard pattern requiring `safe_*` helpers, and the no-op-default semantics that allow un-migrated modules to keep instantiating.

Purpose: Bake the empty-data discipline into the codebase NOW so Phases 2 and 3 cannot regress it. The 2026-05-04 incident hit two recipient groups in production; QUALITY-01 closes the third site that's been waiting for a sibling fix.

Output: One updated source file, one documentation file, and zero remaining unguarded format-spec interpolations on possibly-None metric values across `reports/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-module-render-contract/01-CONTEXT.md
@.planning/phases/01-module-render-contract/01-PATTERNS.md
@.planning/phases/01-module-render-contract/01-01-PLAN.md
@CLAUDE.md
@.planning/codebase/CONVENTIONS.md
@reports/management_summary.py

<interfaces>
<!-- The Plan 01 helpers this plan consumes -->

From reports/modules/format_utils.py (NEW in Plan 01, re-exported via reports/modules/__init__.py in Plan 02):
```python
def safe_pct(val, default="—", precision=1) -> str
def safe_int(val, default="—") -> str
def safe_format(val, fmt, default="—") -> str
```

From reports/modules (package re-export, Plan 02 wires this):
```python
from reports.modules import safe_pct, safe_int, safe_format
```

Existing site to fix (reports/management_summary.py lines 1846-1855, current state):
```python
# Tile 4 — Scan Coverage
if m2.get("error"):
    cov_str   = "N/A"
    cov_color = _GREY
    cov_sub   = "data unavailable"
else:
    cov_pct   = m2["coverage_pct"]
    cov_str   = f"{cov_pct:.1f}%"                                            # ← QUALITY-01 site (line 1853)
    cov_color = _GREEN if cov_pct >= 90 else (_AMBER if cov_pct >= 75 else _RED)  # ← also crashes on None
    cov_sub   = f"{m2['scanned']:,} of {m2['total_licensed']:,} assets scanned"
```

Established sibling-guarded pattern in the same file (the 2026-05-04 fix shape):
- `management_summary.py:1830` — `comp_str = f"{comp_rate:.1f}%" if comp_rate is not None else "N/A"`
- `management_summary.py:1865` — `exc_str  = f"{exc_rate:.1f}%" if exc_rate is not None else "N/A"`

QUALITY-03 audit catalog (from PATTERNS.md section 7) — six INVESTIGATE candidates the planner has confirmed need executor follow-up:
1. `reports/asset_risk.py:455` — `f"{int(h):,}\n({pct:.0f}%)"` (pct may or may not be None depending on histogram input)
2. `reports/unscanned_assets.py:470` — `f"{metrics['on_time'] / total * 100:.1f}%  ← Scan Coverage SLA numerator"` (guard appears earlier; verify)
3. `reports/management_summary.py:1124` — `pct_str = f"{b['pct']:.1f}%"` in `_build_age_bar_chart`
4. `reports/trend_analysis.py:765` — `f"... {pct_old:.1f}% of current backlog"`
5. `reports/modules/aged_vulns_assets_module.py:335` — `pct_display = f"{gauge_value:.1f}%"`
6. `reports/modules/critical_remediation_sla_module.py:363` — `pct_display = f"{gauge_value:.1f}%"`
7. `reports/modules/high_risk_assets_module.py:341` — `pct_display = f"{gauge_value:.1f}%"`
8. `reports/modules/total_vulns_by_severity_module.py:284` — `f"{pct:.1f}%"` per-severity loop

For each, the executor must trace the producing code and decide:
- **NO ACTION** if the value is structurally non-None — add a single-line code comment annotating the contract (e.g., `# pct guaranteed by histogram bucket builder; never None`).
- **FIX** if the value can be None — wrap with `safe_pct(...)` / `safe_int(...)` and import the helper from `reports.modules`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix QUALITY-01 cov_pct crash + complete QUALITY-03 audit</name>
  <files>reports/management_summary.py, reports/asset_risk.py (if needed), reports/unscanned_assets.py (if needed), reports/trend_analysis.py (if needed), reports/modules/aged_vulns_assets_module.py (if needed), reports/modules/critical_remediation_sla_module.py (if needed), reports/modules/high_risk_assets_module.py (if needed), reports/modules/total_vulns_by_severity_module.py (if needed)</files>
  <read_first>
    - reports/management_summary.py lines 1820-1880 (the full _compute_kpi_tiles function around the QUALITY-01 site, plus the sibling guards at 1830 and 1865 that establish the existing pattern)
    - .planning/phases/01-module-render-contract/01-CONTEXT.md (D-16, D-17 — exact fix scope)
    - .planning/phases/01-module-render-contract/01-PATTERNS.md sections 6 and 7 (the full reference replacement and the audit catalog)
    - reports/modules/format_utils.py (Plan 01 output — confirms `safe_pct` is available)
    - reports/modules/__init__.py (Plan 02 output — confirms `safe_pct` is re-exported at the package level)
    - For each INVESTIGATE candidate (PATTERNS.md section 7 catalog), read the file lines around the cited line number AND the producing-code lines that compute the value being formatted (typically a `compute()` method earlier in the same module, or a per-bucket builder helper).
  </read_first>
  <action>
**Subtask A — QUALITY-01 fix (D-16; PATTERNS.md section 6).**

In `reports/management_summary.py`:

1. Add the import. Locate the existing import block at the top of the file. Mirror the pattern of any existing `from reports.modules import ...` line; if none exists, add a new one in the local-import section (look for `from utils.formatters import ...` or the section just below the third-party imports). The exact line to add:

   ```python
   from reports.modules import safe_pct  # Phase 1: None-safe percent formatter (QUALITY-01)
   ```

2. Replace the Tile 4 — Scan Coverage block at lines 1846-1855. Replace:

   ```python
   # Tile 4 — Scan Coverage
   if m2.get("error"):
       cov_str   = "N/A"
       cov_color = _GREY
       cov_sub   = "data unavailable"
   else:
       cov_pct   = m2["coverage_pct"]
       cov_str   = f"{cov_pct:.1f}%"
       cov_color = _GREEN if cov_pct >= 90 else (_AMBER if cov_pct >= 75 else _RED)
       cov_sub   = f"{m2['scanned']:,} of {m2['total_licensed']:,} assets scanned"
   ```

   With:

   ```python
   # Tile 4 — Scan Coverage
   if m2.get("error"):
       cov_str   = "N/A"
       cov_color = _GREY
       cov_sub   = "data unavailable"
   else:
       cov_pct   = m2["coverage_pct"]
       cov_str   = safe_pct(cov_pct, default="N/A")  # Phase 1 QUALITY-01: was f"{cov_pct:.1f}%"
       if cov_pct is None:
           cov_color = _GREY
       elif cov_pct >= 90:
           cov_color = _GREEN
       elif cov_pct >= 75:
           cov_color = _AMBER
       else:
           cov_color = _RED
       cov_sub   = f"{m2['scanned']:,} of {m2['total_licensed']:,} assets scanned"
   ```

   Notes on this exact replacement:
   - Pass `default="N/A"` to `safe_pct` so the empty-data display string matches the surrounding tile-4 error branch's `"N/A"` (PATTERNS.md section 6 anti-pattern note allows either default; the surrounding branch uses `"N/A"` so symmetry wins here).
   - Replacing the bare ternary `cov_color = _GREEN if cov_pct >= 90 else ...` with the explicit None-guarded if/elif chain is REQUIRED — the original ternary throws `TypeError: '>=' not supported between instances of 'NoneType' and 'int'` on a None `cov_pct`. This is pattern-map risk #3.
   - On a None `cov_pct`, `cov_color = _GREY` mirrors the `m2.get("error")` branch's color, keeping the visual coherent.
   - DO NOT change the `m2.get("error")` outer branch (the existing `cov_str = "N/A"` etc. is fine — leave it as-is to avoid scope creep).
   - DO NOT modify the `cov_sub` line — `m2["scanned"]` and `m2["total_licensed"]` are integer counts, never None.

**Subtask B — QUALITY-03 audit (D-17; PATTERNS.md section 7).**

For EACH of the eight candidate sites listed in PATTERNS.md section 7's audit table (the lines marked INVESTIGATE), follow this procedure:

1. **Read the cited line** plus 5-10 lines of surrounding context.
2. **Trace the producing code** for the value being interpolated. Typically:
   - For `gauge_value` in the four `*_module.py` files (aged_vulns:335, critical_remediation:363, high_risk:341, total_vulns:284) — find the `compute()` method on the same class and inspect how `gauge_value` (or `pct`) is assigned. If it's structurally non-None (e.g. always `0.0` placeholder when no data, or always derived from a non-empty count), annotate with a one-line comment and move on.
   - For `pct_old` in `trend_analysis.py:765` — search backward in the same function for where `pct_old` is computed; if it's `denominator > 0` guarded, annotate. Otherwise wrap.
   - For `b['pct']` in `management_summary.py:1124` (`_build_age_bar_chart`) — find where the bucket dicts are constructed (typically `m5_data` upstream); if every bucket dict guarantees a non-None `pct`, annotate. Otherwise wrap with `safe_pct(b['pct'])`.
   - For `pct` in `asset_risk.py:455` — find the histogram computation; if `pct` derives from a non-empty histogram bin count, annotate.
   - For `unscanned_assets.py:470` — locate the surrounding `if total ...` block; verify the line cannot be reached with `total == 0`; annotate if so.

3. **Decide and apply:**
   - **NO ACTION (annotate only):** Insert a one-line comment immediately above the f-string expression of the form:
     ```python
     # safe: <value_name> guaranteed non-None by <producer>
     # (QUALITY-03 audit: 2026-05-05)
     ```
     The comment is a single concise line that says WHO guarantees it (the producer) and WHY (e.g. "BU breakdown helper excludes zero-denominator BUs", "histogram only enumerates non-empty bins", "if total > 0 guard upstream").
   - **FIX:** Replace the f-string with the appropriate `safe_*` call. Add the import `from reports.modules import safe_pct  # or safe_int / safe_format` to the file's import section if not already present (mirror any existing local-imports block). Use `safe_pct(val)` (default `"—"`) for new code; only override `default=` when the surrounding context expects `"N/A"` or another sentinel.

4. **Commit-friendly diff hygiene:**
   - One audit-finding fix per file change is fine; one big commit is also fine.
   - Each FIX must add EXACTLY ONE import line (or zero, if `safe_pct`/etc. is already imported). Do not over-export.
   - Each NO ACTION annotation must be a single comment line, not a block comment.

5. **After processing all eight candidates:** run the audit grep one more time to confirm no NEW unguarded format specs were introduced:

   ```bash
   grep -rnP ":\.[0-9]+f\}%" reports/*.py reports/modules/*.py | grep -v ' is not None' | grep -v 'safe_pct\|safe_int\|safe_format\|# safe:'
   ```

   The expected output is empty (or only contains lines whose surrounding context already proves non-None — these are the "structurally non-None" sites, all of which should now have an annotation comment within 3 lines above).

**Subtask C — verify QUALITY-01 doesn't regress sibling lines.**

The sibling lines `management_summary.py:1830` (`comp_str`) and `management_summary.py:1865` (`exc_str`) already use the `if X is not None else "N/A"` ternary pattern. DO NOT modify them in this task — they are not in scope for QUALITY-01 (which names `cov_pct` specifically). They serve as the reference pattern, and changing them would expand the diff unnecessarily. Phase 4 or v2 may opt to migrate them to `safe_pct` for consistency.

Anti-patterns to avoid (per PATTERNS.md sections 6 and 7):
- Do NOT keep the inline `f"{cov_pct:.1f}%"` "for now".
- Do NOT silence the audit with `noqa` — either fix or annotate.
- Do NOT call `safe_pct` inside a `compute()` method; formatting is render-time only (CONVENTIONS.md / ARCHITECTURE.md "Side effects in compute()" adjacent anti-pattern).
- Do NOT change the surrounding `"N/A"` strings unrelated to the cov_pct site (scope creep).
- Do NOT introduce `safe_pct(my_pct, default="N/A")` everywhere — only when the surrounding code visibly expects `"N/A"`. New code defaults to `"—"`.
  </action>
  <verify>
    <automated>python -c "
import importlib.util
spec = importlib.util.spec_from_file_location('management_summary', 'reports/management_summary.py')
m = importlib.util.module_from_spec(spec)
spec.loader.exec_module(m)

# safe_pct is imported and reachable
assert hasattr(m, 'safe_pct'), 'safe_pct not imported into management_summary.py'

# _compute_kpi_tiles is callable with cov_pct=None and does not raise
fake_m2_none = {'coverage_pct': None, 'scanned': 0, 'total_licensed': 0}
fake_m2_normal = {'coverage_pct': 87.4, 'scanned': 87, 'total_licensed': 100}
print('Loaded management_summary.py without import errors')
print('OK')
"
# Now grep-verify the fix is in place and the sibling guard is added
grep -n 'safe_pct(cov_pct' reports/management_summary.py
grep -n 'cov_pct is None' reports/management_summary.py
# Confirm the original unguarded f-string is GONE
! grep -n 'cov_str   = f"{cov_pct:.1f}%"' reports/management_summary.py
# Audit grep — no unguarded :.<digit>f}% specs without either guard pattern OR safe annotation
grep -rnP ':\.[0-9]+f\}%' reports/*.py reports/modules/*.py | grep -v 'is not None' | grep -v 'safe_pct\|safe_int\|safe_format\|# safe:' | wc -l
# Expected: 0 — every f-string format spec interpolating a metric value is either guarded with the ternary OR replaced with safe_pct OR annotated # safe:</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'safe_pct(cov_pct' reports/management_summary.py` returns at least one line near line 1853.
    - `grep -nF 'cov_str   = f"{cov_pct:.1f}%"' reports/management_summary.py` returns zero lines (the original unguarded format is gone).
    - `grep -nP "cov_color = _GREEN if cov_pct >= 90" reports/management_summary.py` returns zero lines (the unguarded numeric ternary is gone).
    - `grep -nP "cov_pct is None" reports/management_summary.py` returns at least one line (the sibling None-guard is present).
    - `grep -n "from reports.modules import safe_pct" reports/management_summary.py` returns at least one line.
    - `python -c "import reports.management_summary as ms; assert hasattr(ms, 'safe_pct'); print('IMPORT OK')"` exits 0 and prints `IMPORT OK`.
    - For EACH of the eight QUALITY-03 INVESTIGATE candidates listed in PATTERNS.md section 7, the cited line is either replaced with `safe_pct(...)` / `safe_int(...)` / `safe_format(...)` OR has a `# safe: ...` annotation comment within 3 lines above. Verify by inspecting each file and running the audit grep below.
    - The audit grep `grep -rnP ':\.[0-9]+f\}%' reports/*.py reports/modules/*.py | grep -v 'is not None' | grep -v 'safe_pct\|safe_int\|safe_format\|# safe:' | wc -l` returns `0`.
    - No `noqa` was added to silence the audit (`grep -rn '# noqa.*safe_pct\|# noqa.*format_utils' reports/` returns zero lines).
    - No new top-level imports beyond `safe_pct` / `safe_int` / `safe_format` were added (verify via diff that imports are minimal).
  </acceptance_criteria>
  <done>The cov_pct site at management_summary.py:1853 uses safe_pct AND its sibling numeric comparison is None-guarded; every QUALITY-03 INVESTIGATE candidate is either fixed with a safe_* helper or annotated as structurally non-None; the audit grep returns zero unaccounted-for matches.</done>
</task>

<task type="auto">
  <name>Task 2: Document the four-channel render contract in CLAUDE.md</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (full file — current "Board-Style Reports — Module Infrastructure" section, "Adding a New Report — Required Steps" section, surrounding context)
    - .planning/phases/01-module-render-contract/01-CONTEXT.md (D-15 — empty-data guard requirement; "Claude's Discretion" — sibling-section vs inline placement)
    - .planning/phases/01-module-render-contract/01-PATTERNS.md section 8 (full reference for the documentation extension)
    - reports/modules/base.py (after Plan 02 — confirms the exact method names and signatures to document)
    - reports/modules/__init__.py (after Plan 02 — confirms the public import path for `safe_pct` etc.)
  </read_first>
  <action>
Edit `CLAUDE.md` (project root) to extend the existing "Board-Style Reports — Module Infrastructure" section with three new pieces (CONTRACT-05, D-15, PATTERNS.md section 8).

Locate the existing section. The current "Module anatomy" subsection reads:

```markdown
### Module anatomy

Every metric module lives in `reports/modules/` and must:
1. Be named `*_module.py` (auto-discovered by `registry.discover()` on package import)
2. Decorate the class with `@register_module`
3. Extend `BaseModule` and implement `compute()`, `render_pdf_section()`, `render_excel_tabs()`, and `render_email_kpis()`
```

**Edit A: Update the "Module anatomy" enumeration.**

Replace step 3 with:

```markdown
3. Extend `BaseModule` and implement `compute()`. Override any of the renderer methods (`render_pdf_section`, `render_excel_tabs`, `render_email_kpis`, `render_email_panel`, `render_analyst_tabs`, `render_rag_strip_entry`) whose channel the module contributes to. All renderers are concrete with no-op defaults — un-overridden methods produce empty contributions (gray "No Data" cell for the RAG strip; empty string for email panel; empty list for analyst tabs).
```

**Edit B: Insert a new "Four-channel render contract" subsection** immediately after the "Module anatomy" subsection. The section content:

```markdown
### Four-channel render contract

Every metric module can render itself into up to four channels via concrete methods on `BaseModule`. All are **concrete with no-op defaults** (NOT `@abstractmethod`) so existing modules continue to instantiate without changes.

| Method | Channel | Default | Override when |
|--------|---------|---------|---------------|
| `render_pdf_section(data, config) -> str` | PDF | `""` | `"pdf"` in `SUPPORTED_OUTPUTS` |
| `render_excel_tabs(data, workbook, config) -> list[str]` | Excel | `[]` | `"excel"` in `SUPPORTED_OUTPUTS` |
| `render_email_kpis(data, config) -> list[dict]` | Email KPI tiles (legacy) | `[]` | Module surfaces a KPI tile |
| `render_email_panel(data, config) -> str` | Email body panel (CONTRACT-01) | `""` | Module appears as a per-module panel in composed email body |
| `render_analyst_tabs(data, config) -> list[tuple[str, pd.DataFrame]]` | Analyst-detail companion workbook (CONTRACT-02) | `[]` | Module produces drill-down rows for analysts |
| `render_rag_strip_entry(data, config) -> dict` | Cover-page RAG strip (CONTRACT-03) | Gray "No Data" cell | Module appears in the cover-page strip |

The supporting fields on `ModuleData` (CONTRACT-04) carry the data each renderer needs:

| Field | Purpose |
|-------|---------|
| `driver_narrative: str` | 1-line "what's driving it" string consumed by `render_email_panel`. Populated inside `compute()`. |
| `analyst_rows: list[tuple[str, pd.DataFrame]]` | Pivot-friendly drill-down data consumed by `render_analyst_tabs`. Populated inside `compute()`. |
| `rag_strip: dict` | Pre-built cover-page strip cell `{label, headline_value, rag_color, rag_label}` consumed by `render_rag_strip_entry`. Populated inside `compute()`. |
```

**Edit C: Insert a new "Empty-data guard pattern" subsection** immediately after the "Four-channel render contract" subsection.

```markdown
### Empty-data guard pattern

Filtered-to-zero recipient groups are a regular occurrence (Owner=Configuration Mangement typo, etc.) — render methods MUST not crash on a zero-row `ModuleData`. Two rules:

1. **Use `safe_pct` / `safe_int` / `safe_format` from `reports.modules.format_utils`** when interpolating any metric value that could be `None` or `NaN`. Inline f-string format specs on possibly-`None` values are forbidden.

   ```python
   from reports.modules import safe_pct, safe_int, safe_format

   # Good
   panel_html = f"<p>Coverage: {safe_pct(cov_pct)}</p>"

   # Bad — crashes on cov_pct = None
   panel_html = f"<p>Coverage: {cov_pct:.1f}%</p>"
   ```

2. **Return safe defaults instead of raising** in every render method. The `BaseModule._empty_result()` helper produces a coherent failed-`ModuleData` with a gray "No Data" strip cell and "No data in scope." driver narrative — overrides can rely on it as a reference shape.

   ```python
   def render_rag_strip_entry(self, data, config):
       if data.error or not data.metrics:
           from reports.modules import build_rag_strip_entry
           return build_rag_strip_entry(self.DISPLAY_NAME, "—", "no_data")
       headline = safe_pct(data.metrics["scan_coverage_pct"])
       status = rag_status_from_value(data.metrics["scan_coverage_pct"], 95.0, 90.0)
       return build_rag_strip_entry(self.DISPLAY_NAME, headline, status)
   ```

The shared RAG palette + sentinels live in `reports/modules/rag_utils.py` (`STATUS_COLOR`, `STATUS_LABEL`, `NO_DATA_HEADLINE`, `NO_DATA_DRIVER`). The shared formatters live in `reports/modules/format_utils.py`. Both are also re-exported at the package level so modules can `from reports.modules import safe_pct, build_rag_strip_entry, rag_status_from_value`.
```

**Edit D: Update the "Key files" table.**

Locate the existing "Key files" table in the same section. Add two rows (or update if a `rag_utils.py` row already exists):

```markdown
| `reports/modules/rag_utils.py` | Shared RAG palette (`STATUS_COLOR`, `STATUS_LABEL`), classifier wrapper (`rag_status_from_value`), strip-cell builder (`build_rag_strip_entry`), no-data sentinels |
| `reports/modules/format_utils.py` | None/NaN-safe formatters (`safe_pct`, `safe_int`, `safe_format`) for use in render methods |
```

**Edit E: Verify cross-references.** The "Adding a New Report — Required Steps" section (which currently lists three registration sites in `run_all.py` / `_REPORT_MODULE_MAP` / CLAUDE.md) is a SEPARATE concept (top-level reports vs metric modules). DO NOT modify it. The Phase 1 documentation lives entirely inside the "Board-Style Reports — Module Infrastructure" section.

Anti-patterns to avoid (per PATTERNS.md section 8):
- Do NOT remove or rewrite the existing "Adding a New Report — Required Steps" section.
- Do NOT mark the new render methods as `@abstractmethod` in the docs (D-01 keeps them concrete).
- Do NOT add a "CONTRACT_VERSION" discussion (explicitly rejected per CONTEXT.md "Established Patterns").
- Do NOT add v2 features (YAML-driven module composition, GEN-01..04) to the docs — they are out of scope for Phase 1.
  </action>
  <verify>
    <automated>echo "Documentation verification:" && grep -c 'render_email_panel' CLAUDE.md && grep -c 'render_analyst_tabs' CLAUDE.md && grep -c 'render_rag_strip_entry' CLAUDE.md && grep -c 'safe_pct' CLAUDE.md && grep -c 'format_utils' CLAUDE.md && grep -c 'Empty-data guard pattern' CLAUDE.md && grep -c 'Four-channel render contract' CLAUDE.md && echo "ALL OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'render_email_panel' CLAUDE.md` returns at least `2` (in the contract table and the empty-data example or anatomy update).
    - `grep -c 'render_analyst_tabs' CLAUDE.md` returns at least `2`.
    - `grep -c 'render_rag_strip_entry' CLAUDE.md` returns at least `2`.
    - `grep -n 'Four-channel render contract' CLAUDE.md` returns one line (the new subsection heading).
    - `grep -n 'Empty-data guard pattern' CLAUDE.md` returns one line (the new subsection heading).
    - `grep -c 'safe_pct\|safe_int\|safe_format' CLAUDE.md` returns at least `4` (table + example).
    - `grep -c 'format_utils' CLAUDE.md` returns at least `2`.
    - `grep -c 'rag_utils' CLAUDE.md` returns at least `2`.
    - `grep -c 'driver_narrative\|analyst_rows\|rag_strip' CLAUDE.md` returns at least `3` (new ModuleData fields documented).
    - `grep -c 'concrete with no-op default' CLAUDE.md` returns at least `1` (D-01 semantics documented).
    - The "Adding a New Report — Required Steps" section is unchanged (verify by spot-checking that it still mentions `run_all.py` / `_REPORT_MODULE_MAP` / `_VALID_REPORTS`).
    - No mention of `@abstractmethod` for the new render methods (D-01).
  </acceptance_criteria>
  <done>CLAUDE.md documents the four-channel render contract (with the table of six renderer methods including the three new ones), the empty-data guard pattern requiring `safe_*` helpers, the new ModuleData fields, and the helper-module locations. The "Adding a New Report — Required Steps" section is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan modifies one production Python file (`management_summary.py`) and one documentation file. No new I/O surface, no auth changes, no network changes, no new external dependencies. The QUALITY-03 audit's targeted touch-ups touch additional report files only when a fix is required (audit-driven, scope-bounded). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10 | D (Denial of Service) | management_summary.py crash on zero-licensed-asset run (the QUALITY-01 site itself) | mitigate | Replace `f"{cov_pct:.1f}%"` with `safe_pct(cov_pct, default="N/A")` AND replace the bare `cov_pct >= 90` ternary with an explicit None-guarded if/elif chain. The smoke test in Task 1 verifies the module imports cleanly; the audit grep verifies no analogous unguarded sites remain elsewhere. |
| T-01-11 | D (Denial of Service) | Other unguarded format-spec sites flagged as INVESTIGATE | mitigate | Each of the eight catalog candidates from PATTERNS.md section 7 is processed individually. Each gets either a structural-non-None annotation (with comment naming the producer) or a `safe_*` helper wrap. The post-audit grep confirms the catalog is closed. |
| T-01-12 | I (Information Disclosure) | CLAUDE.md leaking internal paths or credentials | accept | The CLAUDE.md update only adds: method signatures (already public), import paths (already public), the documented empty-data guard pattern, and a code example. No credentials, no `.env` paths, no internal hostnames. Risk: none. |
| T-01-13 | T (Tampering) | Audit annotations being added without verifying the producer | mitigate | The audit procedure (Task 1 Subtask B) requires reading the producing code BEFORE adding a NO ACTION annotation. The annotation comment must name the producer and the reason. Code review catches blanket "# safe:" comments without justification. |
| T-01-14 | E (Elevation of Privilege) | (none) | accept | No new permissions, no new entry points, no new file operations. Both edits are confined to existing files. |
</threat_model>

<verification>
After both tasks complete:

```bash
# 1. QUALITY-01 smoke: confirm management_summary.py imports cleanly and the fix is in place
python -c "
import importlib.util
spec = importlib.util.spec_from_file_location('management_summary', 'reports/management_summary.py')
m = importlib.util.module_from_spec(spec)
spec.loader.exec_module(m)
assert hasattr(m, 'safe_pct'), 'safe_pct not imported'
print('management_summary import OK')
"

# 2. QUALITY-03 audit grep — every percent format spec is either guarded, replaced, or annotated
grep -rnP ':\.[0-9]+f\}%' reports/*.py reports/modules/*.py | grep -v 'is not None' | grep -v 'safe_pct\|safe_int\|safe_format\|# safe:'
# Expected: empty output

# 3. CONTRACT-05 docs check
grep -E 'Four-channel render contract|Empty-data guard pattern|safe_pct|render_email_panel|render_analyst_tabs|render_rag_strip_entry' CLAUDE.md | wc -l
# Expected: at least 10 matches
```
</verification>

<success_criteria>
- `reports/management_summary.py:1853` uses `safe_pct(cov_pct, default="N/A")` instead of `f"{cov_pct:.1f}%"` (QUALITY-01 closed).
- The sibling `cov_color = _GREEN if cov_pct >= 90 else ...` ternary is replaced with an explicit None-guarded if/elif chain (pattern-map risk #3 closed).
- A None `cov_pct` no longer raises TypeError anywhere in the Tile 4 — Scan Coverage block.
- Every QUALITY-03 INVESTIGATE candidate from PATTERNS.md section 7 is resolved (either fixed with `safe_*` or annotated `# safe:`); the audit grep returns zero unaccounted-for matches.
- CLAUDE.md "Board-Style Reports — Module Infrastructure" section documents the four-channel render contract, the new ModuleData fields, the empty-data guard pattern, and the new helper-module locations.
- The "Adding a New Report — Required Steps" section is unmodified.
- No `# noqa: ...` is used to silence the audit anywhere in `reports/`.
</success_criteria>

<output>
After completion, create `.planning/phases/01-module-render-contract/01-03-SUMMARY.md` per the template.
</output>
