---
phase: 08-warm-cache
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/fetchers.py
autonomous: true
requirements: [CACHE-04]
must_haves:
  truths:
    - "Parquet writes from data/fetchers.py go through a single atomic helper"
    - "A concurrent reader never observes a partially-written parquet file"
    - "All four pre-fetch fetchers (vulns_all, fixed, assets, recast) use the helper"
    - "run_all.py's existing pre-fetch path is unchanged at the call site but now writes atomically by virtue of the fetcher change"
  artifacts:
    - path: "data/fetchers.py"
      provides: "_atomic_write_parquet helper; _save_cache routes through it"
      contains: "_atomic_write_parquet"
  key_links:
    - from: "data/fetchers.py::_save_cache"
      to: "data/fetchers.py::_atomic_write_parquet"
      via: "direct call"
      pattern: "_atomic_write_parquet\\("
    - from: "data/fetchers.py::_atomic_write_parquet"
      to: "os.replace"
      via: "stdlib"
      pattern: "os\\.replace\\("
---

<objective>
Introduce a single atomic-write helper for parquet caches and route ALL four pre-fetch fetchers in `data/fetchers.py` through it, so that BOTH paths that warm the cache (the existing `run_all.py` daemon/manual pre-fetch AND the new `scripts/warm_cache.py` in plan 02) are atomic by construction.

Purpose: Satisfy CACHE-04 — concurrent daemon + cron invocations cannot observe partial parquet files. Picking Option A from the discussion artifact (helper inside `data/fetchers.py`) was a deliberate choice: Option B (atomic only in the warm-cache script) would leave the daemon write path non-atomic, which violates CACHE-04 the moment the two run concurrently.

Output: A modified `data/fetchers.py` with `_atomic_write_parquet(df, path)` used by `_save_cache()`. No public API change; no new dependencies; no changes to `run_all.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- Key code that this plan modifies. Executor should NOT re-explore the codebase. -->

From data/fetchers.py (current state, lines 175-196):

```python
def _cache_path(cache_dir: Path, dataset: str) -> Path:
    """Return the parquet cache path for a given cache directory and dataset name."""
    return cache_dir / f"{dataset}.parquet"


def _load_cache(path: Path) -> Optional[pd.DataFrame]:
    """Return cached DataFrame if the file exists, else None."""
    if path.exists():
        logger.info("[CACHE HIT] Loading %s from cache", path.name)
        return pd.read_parquet(path, engine="fastparquet")
    return None


def _save_cache(df: pd.DataFrame, path: Path) -> None:
    """Persist DataFrame to parquet, logging any write errors non-fatally."""
    try:
        path.parent.mkdir(parents=True, exist_ok=True)
        df.to_parquet(path, index=False, engine="fastparquet")
        logger.debug("Cached %d rows to %s", len(df), path)
    except Exception as exc:  # noqa: BLE001
        logger.warning("Could not write cache file %s: %s", path, exc)
```

Each fetcher (fetch_all_vulnerabilities, fetch_fixed_vulnerabilities, fetch_all_assets, fetch_recast_rules) currently calls either `_save_cache(df, cache)` OR `df.to_parquet(cache_path)` directly. Grep both patterns and consolidate.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add _atomic_write_parquet helper and route all parquet writes through it</name>
  <files>data/fetchers.py</files>
  <behavior>
    - Calling `_atomic_write_parquet(df, target)` writes `df` to a sibling `target.with_suffix(target.suffix + ".tmp.<pid>")` file, fsyncs (best-effort), then calls `os.replace(tmp, target)` so the final filename never points at an incomplete file.
    - If the write raises, the temp file is removed (try/except, non-fatal) and the original `target` (if it existed) is untouched.
    - `_save_cache()` delegates to `_atomic_write_parquet()` and keeps its existing non-fatal warning behavior on exceptions.
    - Any fetcher in `data/fetchers.py` that currently calls `df.to_parquet(...)` directly for cache output is converted to use `_atomic_write_parquet()` (NOT raw `to_parquet`). After the change, `grep -n "to_parquet" data/fetchers.py` shows calls only inside `_atomic_write_parquet` itself and inside `_load_cache` (read path, unaffected).
    - All four cache datasets (`vulns_all`, `fixed`, `assets`, `recast` — actual dataset names per current code) still write to the same parquet path as before; no schema or location change.
  </behavior>
  <action>
    Edit `data/fetchers.py`:

    1. Add `import os` and `import tempfile` to the top imports if not already present (os is needed for `os.replace` and `os.getpid`; tempfile is optional — `os.replace` plus a `.tmp.<pid>` suffix is sufficient and avoids cross-filesystem rename issues by writing the temp file in the SAME parent directory as the final target).

    2. Insert `_atomic_write_parquet(df: pd.DataFrame, path: Path) -> None` immediately after `_cache_path`. Implementation contract:
       - Compute `tmp = path.with_name(f"{path.name}.tmp.{os.getpid()}")` (same directory ensures `os.replace` is an atomic rename on POSIX and Windows).
       - `path.parent.mkdir(parents=True, exist_ok=True)`.
       - `df.to_parquet(tmp, index=False, engine="fastparquet")`.
       - `os.replace(tmp, path)`.
       - On any exception inside the try block: best-effort `tmp.unlink(missing_ok=True)`, then re-raise to the caller. `_save_cache` (its caller) already swallows and logs, so the non-fatal contract is preserved without burying the failure in this helper.

    3. Update `_save_cache` to call `_atomic_write_parquet(df, path)` instead of `df.to_parquet(...)` directly. Keep the surrounding `try/except` + `logger.warning` shape unchanged so behavior is identical to callers.

    4. Grep `data/fetchers.py` for any `to_parquet(` call OUTSIDE `_atomic_write_parquet`. If any of the four fetcher functions bypass `_save_cache` and write parquet inline, switch them to call `_save_cache(df, cache_path)` (preferred — matches the existing pattern in `fetch_all_vulnerabilities`).

    5. No docstring rewrite of the surrounding fetchers — minimum diff per Karpathy guideline #3 (Surgical Changes).

    Style notes from CLAUDE.md / CONVENTIONS.md: keep type hints; keep `logger.debug` line in `_save_cache` after the helper succeeds (caller-level "Cached N rows" message stays); do NOT log inside `_atomic_write_parquet` itself (helper stays pure).
  </action>
  <verify>
    <automated>cd D:/Projects/vuln-reporting &amp;&amp; python -c "from data.fetchers import _atomic_write_parquet, _save_cache; import pandas as pd, tempfile, pathlib, os; d = pathlib.Path(tempfile.mkdtemp()); p = d / 'x.parquet'; df = pd.DataFrame({'a':[1,2,3]}); _atomic_write_parquet(df, p); assert p.exists(); assert not any(f.name.startswith('x.parquet.tmp') for f in d.iterdir()); df2 = pd.read_parquet(p, engine='fastparquet'); assert df2['a'].tolist() == [1,2,3]; print('OK')"</automated>
    <automated>cd D:/Projects/vuln-reporting &amp;&amp; python -c "import re, pathlib; src = pathlib.Path('data/fetchers.py').read_text(); body = re.sub(r'#[^\n]*', '', src); calls = [m.start() for m in re.finditer(r'\.to_parquet\(', body)]; print(f'to_parquet calls: {len(calls)}'); assert len(calls) == 1, 'expected exactly one to_parquet call (inside _atomic_write_parquet)'; print('OK')"</automated>
  </verify>
  <done>
    `_atomic_write_parquet` exists in `data/fetchers.py`; `_save_cache` routes through it; exactly one non-comment `.to_parquet(` call remains in the file (inside the helper); the round-trip smoke test in verify passes; no `.tmp.*` files leak on success.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Concurrent processes → shared filesystem | Daemon `run_all.py` and cron `warm_cache.py` may write to the same `data/cache/<date>/*.parquet` simultaneously. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | `data/cache/*.parquet` partial writes | mitigate | `_atomic_write_parquet` writes to `<file>.tmp.<pid>` then `os.replace` — readers always see either the previous complete file or the new complete file, never a torn one. |
| T-08-02 | Denial of Service | Leftover `.tmp.<pid>` files on crash | accept | On write exception we `unlink(missing_ok=True)` the tmp file. A hard process kill mid-write may still leak a tmp file, but it is named with `.tmp.` so it will never be read as cache, and the next `--prune-stale` sweep (plan 02) removes the parent dir on day-rollover. |
</threat_model>

<verification>
- `python -c "from data.fetchers import _atomic_write_parquet; ..."` round-trip smoke (above).
- `grep -n "to_parquet" data/fetchers.py` shows the single helper-internal call plus `_load_cache`'s `read_parquet` (read path) only.
- Existing `python run_all.py --dry-run` still completes without error (regression check on the daemon write path).
</verification>

<success_criteria>
- Helper exists and is the sole parquet-write site in `data/fetchers.py`.
- Concurrent-write semantics: `os.replace` provides atomic rename on both POSIX and Windows (NTFS), so any process reading `vulns_all.parquet` sees either the pre-write or post-write version, never a partial.
- No public API change. `run_all.py` is not modified.
- Single atomic commit: `feat(08-01): atomic parquet writes in data/fetchers.py for CACHE-04`.
</success_criteria>

<output>
Create `.planning/phases/08-warm-cache/08-01-SUMMARY.md` when done.
</output>
