---
milestone: v1.6
milestone_name: Delivery Config at Scale
audited: 2026-07-09
status: gaps_found
scores:
  requirements: 5/7
  phases: 1/2
  integration: 6/6
  flows: 1/1
gaps:
  requirements:
    - id: "CONF-04"
      status: "unsatisfied"
      phase: "21-private-config-repo-ci-cutover (NOT BUILT)"
      claimed_by_plans: []
      completed_by_plans: []
      verification_status: "missing"
      evidence: "Phase 21 has no phase directory, no PLAN, no SUMMARY, no VERIFICATION. Requirement is roadmapped and traceability-mapped to Phase 21 (status Pending) but not yet planned or executed. Long-lead: private-repo provisioning goes through change management."
    - id: "QUAL-07"
      status: "unsatisfied"
      phase: "21-private-config-repo-ci-cutover (NOT BUILT)"
      claimed_by_plans: []
      completed_by_plans: []
      verification_status: "missing"
      evidence: "Formal dual-source-fallback production cutover is Phase 21 scope. Backward-compat SPIRIT is already preserved (directory-presence opt-in keeps the legacy single-file path byte-identical — integration seam #6 WIRED), but the QUAL-07 cutover requirement itself is unbuilt."
  integration: []
  flows: []
tech_debt:
  - phase: 20-config-language-loader-matrix
    items:
      - "WR-01: generate_delivery_matrix.py:206-209 render_html interpolates cell values without html.escape() — &/</> in operator strings could corrupt/inject the published matrix (affects CONF-05)."
      - "WR-02: generate_delivery_matrix.py:191-192 render_markdown does not escape literal | in cell values — a pipe in a name/owner/filter breaks the Markdown table (affects CONF-05)."
      - "WR-03: config_loader.py:276-283 inline email.cc/email.reply_to without recipients silently dropped in directory mode with no error/warning — violates the phase's surface-everything-loudly intent for a narrow edge (affects CONF-02/03)."
      - "WR-04: run_all.py _dry_run hardcodes ROOT_DIR/delivery_config.yaml; no --config CLI flag. Dry-run surfacing cannot be pointed at an arbitrary deliveries.d/ via CLI (only via Python kwarg). Matters once Phase 21 wires CI against the shared/ symlink layout (affects CONF-03/CONF-04)."
      - "W-1 (integration): _dry_run runs BOTH the legacy single-file schema block and the directory-mode block when both a single-file config and deliveries.d/ coexist, whereas _load_config short-circuits to directory mode. Latent today (production is mutually exclusive); a mixed layout during the Phase-21 cutover could double-surface with disagreeing precedence (affects CONF-03, QUAL-07)."
      - "W-2 (integration): generate_delivery_matrix.py:252 calls resolve_config unconditionally; in legacy single-file mode resolve_config returns empty with no error, so the tool exits 0 with a silently-empty matrix and no 'you're in legacy mode' diagnostic (affects CONF-05)."
nyquist:
  compliant_phases: ["20-config-language-loader-matrix"]
  partial_phases: []
  missing_phases: []
  overall: "compliant (for built scope)"
security:
  phase_20: "threats_open: 0 (16/16 verified, 1 accepted risk)"
---

# Milestone v1.6 "Delivery Config at Scale" — Audit Report

**Audited:** 2026-07-09
**Status:** `gaps_found` — milestone in progress (Phase 21 not yet built)
**Built scope verdict:** Phase 20 is complete, verified, secure, and Nyquist-compliant, with all six integration seams wired and backward compatibility preserved. The `gaps_found` status reflects **unbuilt milestone scope** (Phase 21), not a defect in delivered work.

## Milestone Definition of Done

v1.6 spans **Phases 20–21** and **7 requirements** (CONF-01/02/03/04/05, QUAL-06/07). The milestone is done when a private, CODEOWNERS-gated, CI-validated config repo feeds production through a reversible dual-source cutover — with the new resolve-before-validate loader proven to resolve every existing config identically.

| Phase | Status | Requirements |
|-------|--------|--------------|
| 20 — Config Language + Loader + Matrix | ✅ Complete & verified | CONF-01, CONF-02, CONF-03, CONF-05, QUAL-06 |
| 21 — Private Config Repo + CI + CODEOWNERS + Cutover | ⬜ Not built | CONF-04, QUAL-07 |

## Requirements Coverage (3-Source Cross-Reference)

Each requirement cross-checked against VERIFICATION.md status, SUMMARY frontmatter `requirements_completed`, and REQUIREMENTS.md traceability checkbox.

| REQ-ID | Phase | VERIFICATION | SUMMARY frontmatter | Traceability | Final Status |
|--------|-------|--------------|---------------------|--------------|--------------|
| CONF-01 | 20 | passed | listed (20-01) | `[x]` | ✅ satisfied |
| CONF-02 | 20 | passed | listed (20-01) | `[x]` | ✅ satisfied |
| CONF-03 | 20 | passed | listed (20-01, 20-02) | `[x]` | ✅ satisfied |
| CONF-05 | 20 | passed | listed (20-03) | `[x]` | ✅ satisfied |
| QUAL-06 | 20 | passed | listed (20-04) | `[x]` | ✅ satisfied |
| CONF-04 | 21 | missing | absent | `[ ]` | ❌ unsatisfied (Phase 21 not built) |
| QUAL-07 | 21 | missing | absent | `[ ]` | ❌ unsatisfied (Phase 21 not built) |

**Score: 5/7 satisfied.** No orphaned requirements — every REQ-ID maps to a roadmapped phase; the two unsatisfied ones are cleanly assigned to the not-yet-built Phase 21.

## Phase Verification Summary

| Phase | Verified | Status | Score | Blocking Gaps |
|-------|----------|--------|-------|---------------|
| 20 | 2026-07-09 | passed | 5/5 observable truths, 9 required artifacts, 6 key links WIRED | None |

Supporting per-phase gates (Phase 20):
- **Security:** `threats_open: 0` — 16/16 threats verified in code, 1 documented accepted risk (T-20-04). See `20-SECURITY.md`.
- **Nyquist validation:** `nyquist_compliant: true` — all 6 requirement behaviors have committed automated tests (the one gap, `_dry_run` surfacing, was filled with `tests/test_dry_run_surfacing.py`). See `20-VALIDATION.md`.

## Cross-Phase Integration

Scope note: only Phase 20 is built, so "cross-phase" means Phase 20's loader subsystem integrating into the pre-existing execution model. **6 of 6 seams WIRED, 0 orphaned exports, 0 broken connections.**

| # | Seam | Status |
|---|------|--------|
| 1 | `_load_config` → `resolve_config` (dir-mode delegation, preserves `list[dict]`) | WIRED |
| 2 | resolved config → UNCHANGED `delivery_config.schema.yaml` | WIRED |
| 3 | `_dry_run` → `resolve_config` (error→exit 1 / warning→exit 0) | WIRED |
| 4 | `generate_delivery_matrix.py` → `resolve_config` (no fetcher import, Hard Rule 1) | WIRED |
| 5 | `scheduler.py` → `_load_config` (hot-reload, unmodified) | WIRED |
| 6 | Legacy single-file path unchanged (backward compat) | WIRED |

**E2E flow complete:** `contacts.yaml` + `deliveries.d/*.yaml` → `resolve_delivery_email` builds the `email` block → `run_group` passes it intact → `send_report_email` reads `recipients`/`cc`/`reply_to` into SMTP headers (`email_sender.py:473-481`). CONF-02's standing Cc + default Reply-To reach the actual headers, not dropped mid-chain.

**Backward compatibility preserved:** directory mode is opt-in strictly by `deliveries.d/` presence. Production has no such directory, so `board_summary`/`management_summary`/`ops_remediation`/`vuln_export`/`unscanned_assets` continue through the byte-identical legacy branch. Confirmed live via sanctioned `run_all.py --dry-run` (exit 0) and `M_load_config_legacy_single_file_still_works`.

## Tech Debt (non-blocking)

**Phase 20 code-review warnings (WR-01…WR-04)** — re-confirmed present at verification time; none break any of the 5 Phase-20 success criteria:
- WR-01 — HTML matrix cells not `html.escape()`-d (CONF-05).
- WR-02 — Markdown matrix cells don't escape literal `|` (CONF-05).
- WR-03 — inline `email.cc`/`reply_to` without `recipients` silently dropped in directory mode (CONF-02/03).
- WR-04 — `_dry_run` has no `--config` flag; hardcoded to `ROOT_DIR/delivery_config.yaml` (CONF-03/04; matters for Phase 21 CI).

**Integration warnings (W-1, W-2)** — both concern the mixed legacy+directory transition window that Phase 21 will exercise:
- W-1 — `_dry_run` double-surfaces (legacy schema block + directory-mode block) when both coexist; precedence disagrees with `_load_config`. Latent today. Recommend: skip the single-file block when `deliveries.d/` is present, mirroring `_load_config`.
- W-2 — matrix generator exits 0 with a silently-empty table in legacy single-file mode (no "legacy mode" diagnostic). Recommend: emit a notice when `deliveries.d/` is absent.

**Total: 6 items across 1 built phase.** WR-04, W-1, and W-2 are natural inputs to Phase 21 planning (they surface precisely in the cutover window Phase 21 addresses).

## Verdict & Next Step

Phase 20 delivered its full scope cleanly. The milestone is **not yet complete** because Phase 21 (private config repo + CI + CODEOWNERS + production cutover — CONF-04, QUAL-07) has not been planned or executed. Advance by building Phase 21 through the standard phase chain; do **not** insert a separate closure phase (the roadmap already accounts for Phase 21). Fold WR-04, W-1, and W-2 into Phase 21's discussion as cutover-window robustness items.
